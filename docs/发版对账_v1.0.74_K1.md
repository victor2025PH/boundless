# 发版对账 v1.0.74 · K-1 在途改动收口（2026-09-05）

> 指令：`docs/指令_K-1_在途收口_2026-09-05.md`。预算 $120。类型：**零新功能，只把树上 10–28 小时没人提交的 hunk 按归属切成 commit 落地**。
> 开工基线 HEAD `b656da2c`（19:30）；收工 HEAD 见 §7。保险：tag `k1-before`（`stash create` 快照，79 个已跟踪文件）+ `engines/chengjie/tmp/k1_untracked_before.zip`（32 个未跟踪文件，610 KB）。
> 一句话结论：**A1–A4 / B / C 六个 commit 全部落地，对应 #160 #145③ #146族 #32 #155 #156 #159 #166 #120/#159族；孤儿线「unlimited outbound mode」实为 21 个文件（不是 8 个）且两文件被整体改成 CRLF，只盘点未动（K-D1）。** 除 C 的两条 e2e 载荷行外没写过一行代码。`.py` 改动全部要等 K-5 ①段重启装载；模板 / CSS / i18n / cp-goal.js 已热更（`?v=` 已双戳）。

## 0. 盘点快照（19:31，§2）

- `git status --short -- engines/chengjie`：**64 modified + 1 deleted + 17 untracked**；index 空（`agent_probe` 报 index FREE）。
- ACTIVE（≤10 min）：`skill_manager.py`（HOT-ZONE，19:28）、`deploy/desktop/SEAT_LOG_MONITOR_LOG.md` / `monitor_seat_logs.state.json`。意向板同时在跑：K-2（J-7）、K-3、K-4、J-10 phase3、installer brand round2、J-8 ×3（5–6h 前）、D1b（14h 前，已死）、duty。
- 分组结果：A 组 4 件（§1）、B 组 18 文件（§2）、C 组 3 文件（§3）、D 组其余（§4）。指令里几处与树上实况不一致，逐条记在各段「与指令的差别」。

## 1. A —— I-5 收口五件

| 件 | 工单 | 文件 / 认领子串 | commit | 门禁（在 index 预览 worktree 跑，不是工作树） |
|---|---|---|---|---|
| **A1** guard-check 影子 | #160 | `unified_inbox_desktop_routes.py` 整文件（唯一 hunk：`block` 恒 False + `autosend_shadow_log.record(track_outcome=False)` 记 `desktop_guard`） | `82ee11a5` | `test_i5_remaining_wiring -k guard_check` 2 passed |
| **A2** 选品与出站守卫人设同源 | #145③ | `goals/service.py` 3 hunk（`catalog_persona_id`）；`goal_routes.py` 2/5 hunk（`_account_persona(` / `persona_id=pid`，其余 3 个 `_attach_sprint_live` 归 B）；`tests/test_goal_growth_loop.py` +67（指令未列，唯一 hunk 就是 #145③ 的 2 例，随本 commit） | `71355568` | `test_goal_growth_loop / test_goal_wiring / test_persona_catalog_isolation / test_goal_acquire_funnel` 257 passed |
| **A3** 撤回引用出站接线（K-D4 例外） | #146 族（#32② / #145⑤） | `withdrawn_cite.py` 整文件（`apply_to_reply` + `__all__`）；`drafts.py` L1226 1 hunk（14 行，`--mine withdrawn_cite`）；`skill_manager.py` L4969 1/4 hunk（`_apply_outbound_text_guard` 出稿口，其余 3 个 J-10 三期 hunk 未动）；`tests/test_withdrawn_cite.py` +20 | `c468483a` | `test_withdrawn_cite / test_i5_remaining_wiring / test_impl74_b4alt_pins / test_outbound_text_guard` 89 passed；暂存版 `ast.parse` OK。**等到 `skill_manager.py` 静默 35 分钟（19:45→20:21）才动** |
| **A4** 悬浮清单下线 + 三项 UI | #32 #159 #156 #155 | `unified_inbox.html` 16/17 hunk（`_cpTearWanted`→`return [];`、`_acUnreadBadge(…,plat,aid)`、`data-ac-unread` / `_openAcctUnreadPop`、`persona_unselected` / `data-ac-nopersona` / `_openAcctPersonaBind`、`cust-call-peer` / `cust-peer-calls` / `_loadAddrNames` / `_saveAddrNames`、`Object.assign(window,…)`；`sticker-panel.js?v=` 1 行他线未动）；`unified-inbox.css` 整文件（单 hunk 混 #32 隐藏规则与 `ac-unread-pop` / `info-addr-in`，**切不开，故 #32 与三项 UI 合一个 commit**）；`i18n_packs/inbox_workspace.py` 2 hunk（`inbox.acct.unread_*` 7 键 zh/en）；`tests/test_i5_remaining_wiring.py`（新）；双戳 `unified-inbox.css?v=20260905b→c` + `ui-build 20260905-1949` | `69e62aba` | `test_i5_remaining_wiring / test_inbox_inline_handlers_exported / test_template_orphan_refs / test_template_unique_ids / test_i18n_coverage / test_workspace_i18n_render / test_template_inline_js_syntax / test_template_undefined_identifiers / test_template_inline_handler_arg_quoting` 399 passed |

与指令的差别：① `test_template_window_t_keys_resolve.py` 不存在，改跑 `test_i18n_coverage` + `test_workspace_i18n_render`；② `inbox_workspace.py` 的 hunk 是 `inbox.acct.unread_*`（`inbox.info.call_peer` 等键早已在 HEAD）；③ `unified_inbox.html` 里没有 `cp-goal.js?v=` 的 D1b 行（`abe96ba3` / `9f045de9` 已提交过）；④ `_cpTearWanted` hunk 的新增行只有 `return [];`，工具按新增行匹配，故认领子串补了 `"return [];"`。

## 2. B —— D1b 收尾（K-D7 接管）

| 件 | 工单 | 文件 | commit | 门禁 |
|---|---|---|---|---|
| **P0-6 liveness + 收尾** | #166 | 新 `goals/liveness.py` / `tools/goal_sprint_drill.py` / `tests/test_goal_liveness.py`；`sprint_ticker.py` 整文件（`next_daily_ts` 抽出 + `__all__`）；`goal_routes.py` 剩余 3 hunk（`_attach_sprint_live` 自然档同挂）；`health_watchdog.py` 3/6 hunk（`--mine goal_sprint --mine liveness`）；`webhook_notifier.py` 1/4 hunk（`goal_sprint_sends`）；`cp-goal.js` **双树**（`diff --no-index` 一致）；`i18n_packs/goals.py` 整文件（推进器文案人话化为「自动跟进」）；`config.example.yaml` 1/2 hunk（`health_watchdog.goal_sprint_liveness`；`outbound.unlimited_mode` 归 D）；`tests/test_goal_sprint_ticker.py` +13 / `test_goal_engine_status_166.py` +22 / `test_alert_delivery_e2e.py` 1/2 hunk（`goal_sprint_sends` 告警+恢复载荷）；双戳 `cp-goal.js?v=20260905c→d`（`unified_inbox.html` + `shared/copilot/app.html` 双树）+ `ui-build 20260905-2003` | `124dd921`（18 文件 +956/−92） | `test_goal_liveness / test_goal_sprint_ticker / test_goal_engine_status_166 / test_alert_delivery_e2e / test_copilot_shared_sync / test_sync_copilot_mirror / test_copilot_js_parses / test_copilot_i18n_keys` 165 passed；`tests/test_goal_*.py` 34 文件 **962 passed**（J-2 A2 口径 948 + 新例） |

与指令的差别：① `config.example.yaml` 里没有 `goals` 段 hunk 也没有 `inbox.workflows.auto_start.max_per_tick`（后者仍是 K-3 的文档键项）；② `shared/copilot/app.html` 双树开工时是干净的，只因本次 `?v=` bump 才进 commit；③ `tests/test_alert_delivery_e2e.py` 的 +7 是 **B** 的 `goal_sprint_sends`，不是指令写的 C。

## 3. C —— 对话 B `phantom_unread_alert`

| 件 | 工单 | 文件 | commit | 门禁 |
|---|---|---|---|---|
| 幽灵未读告警接线 | #120 / #159 族（#145④） | `health_watchdog.py` 3 hunk（`_pu_*` 状态 / `_check_phantom_unread` 调用 / 定义，读 `unread_aggregate.phantom_unread_report`，默认阈值 5，重提 360 min）；`webhook_notifier.py` 3 hunk（别名 / `_build_message` 分支 / `_CARD_META` 🔵）；`tests/test_alert_delivery_e2e.py` `_EMITTED_ALERTS` **+2 条 `phantom_unread_alert` 载荷（告警 + 恢复）——K-1 唯一手写的 8 行**，镜像 publish 字段；没有它 `test_source_alerts_all_have_e2e_payload` 在 HEAD 会红（红龄自 09-03） | `35b224c4` | `test_alert_delivery_e2e`（**`test_source_alerts_all_have_e2e_payload` 转绿**，98 passed）/ `test_health_watchdog / test_buried_conv_watchdog / test_buried_unread_badge / test_scan_loop_stall / test_watchdog_tick_behavior / test_ops_incidents / test_alert_link_health` 共 229 passed；`test_python_undefined_names` 1 红为**他线预存**（见 §5） |

未做：`config.example.yaml` 没有 `health_watchdog.phantom_unread_remind.{enabled,min_phantom,interval_min}` 文档键 → 交 K-3 的 config.example 项一并加。

## 4. D —— 只盘点不动（K-D1 及各线认领）

### 4.1 「unlimited outbound mode」孤儿线（09-04 13:27–13:51，意向板已无）——**21 个文件，不是 8 个**

`git diff --stat` / hunk 头 / 归属依据（新增行注释含 `outbound.unlimited_mode` / `outbound_policy` / 「0=不限」）：

| 文件 | +/− | hunk 头（函数） | 依据 | 建议 |
|---|---|---|---|---|
| `src/ops/outbound_policy.py`（**新**） | 279 行 | `is_unlimited / business_cap / record_block / record_unlimited_bypass` | 整条线的单一开关与计数器 | 老板拍 K-D1 后整线一起提交或一起回滚，**不能拆** |
| `tests/test_outbound_policy.py`（**新**） | 215 行 | — | **24 passed**（工作树） | 同上 |
| `src/utils/rate_limiter.py` | +343/−167（**含 EOL 噪音**；`--ignore-cr-at-eol` 后 **+9**） | `allow()` 头部 `is_unlimited()` 放行 | HEAD 是 LF（CR=0），工作树被整体改成 CRLF（CR=176） | **提交前必须归一化回 LF**，否则 blame 全毁 |
| `src/skills/account_limiter.py` | +473/−218（EOL 噪音；净 **+59/−13**） | 整文件 | 同上：HEAD CR=0 → 工作树 CR=255 | 同上 |
| `src/integrations/protocol_autoreply_limits.py` | +34 | `_DAY` 后 / `AutoReplyLimiter` ×5 | `business_cap` 「0=不限」 | 随线 |
| `src/licensing/quota_state.py` | +10 | `collect_quota_state` | 暴露 `unlimited_mode` 状态 | 随线 |
| `src/licensing/token_ledger.py` | +8 | `should_degrade_action` | 注释明写「不短路 token_ledger.enforce」 | 随线 |
| `src/skills/companion_send_gate.py` | +26 | `gate_decision` ×2 | 健康红灯不短路，仅业务档放行 | 随线 |
| `src/client/telegram_client.py` | +28 | `_note_outbound_block` 新函数 + 限流/冷却 7 处计数 + `reply_logic_cooldown` 放行 | **不是指令猜的 J-5 透传**；import `src.ops.outbound_policy`（未跟踪文件） | 随线；单独提交会 import 不存在的模块（有 try/except 兜底但无意义） |
| `src/companion/proactive_topic.py` | +45/−4 | `maybe_start_companion_proactive` ×4 | 「一键关掉叠加降频层」/ 变体守卫降级 | 随线 |
| `src/companion/deep_persona.py` | +19/−7 | `life_share_allowed` | life_share 周配额 0=不限 | 随线 |
| `src/contacts/bootstrap.py` | +3/−1 | `_safe_init_limiter` | `daily_cap` 「显式 0=不限（P2-1 语义统一）」 | 随线 |
| `src/contacts/care_budget.py` | +7 | `budget_allows` | care 联系人预算放行 | 随线 |
| `src/contacts/reactivation_loop.py` | +7/−2 | `ReactivationLoop` ×3 | 同 | 随线 |
| `src/integrations/companion_proactive.py` | +7/−1 | `plan_proactive_sends` | `max_per_tick<=0` 不截断 | 随线 |
| `src/utils/daily_ritual.py` | +4/−1 | `plan_daily_rituals` | 同 | 随线 |
| `src/inbox/peer_bot_guard.py` | +32 | `evaluate` ×2 / `budget_flags` | 真人日预算视同救济 | 随线 |
| `src/integrations/line_rpa/service.py` | +11/−1 | `LineRpaService` ×3 | `daily_cap` 守门 0=不限 | 随线 |
| `src/integrations/whatsapp_rpa/service.py` | +28/−5 | 模块头 +20 / `WhatsAppRpaService` ×3 | 同 | 随线 |
| `src/web/routes/drafts_routes.py` | +14 | `register_metrics_route` ×2 | `outbound_blocked_total{layer,reason}` 指标 | 随线 |
| `src/web/admin.py` | +11 | `create_app` L1437 | 「出站策略单一开关」读取面（mtime 09-05 13:43 但唯一 hunk 就是它） | 随线；**K-2 要动 `admin.py`（voice-eval / 会话回收）时用 `stage_hunks --theirs unlimited_mode`** |
| `config/config.example.yaml` | +19 | `bug_intake:` 后新增 `outbound.unlimited_mode: false` 段 | 文档键 | 随线；K-3 加文档键时 `--theirs unlimited_mode` |

语法：21 个 `.py` 全部 `ast.parse` OK。自洽：默认 `unlimited_mode: false` 时行为应等于现状（未逐行核）。**不提交不回滚。**

### 4.2 其余 dirty / untracked（每文件一行）

| 文件 | +/− | 归属猜测（依据） | 建议 |
|---|---|---|---|
| `src/web/static/workspace/sticker-panel.js` +1/−1（09-04 20:59）、`unified_inbox.html` 的 `sticker-panel.js?v=20260904a` 1 行、**untracked** `tests/test_media_send_unknown_toast.py`（09-04 21:02） | 3 件 | **#164** 媒体发送「结果未知」措辞（测试头写「钧机 1.0.73 诊断包 3PZ95W」）。JS 改用 `inbox.media.send_fail_net_restart` 键，但**该键不在 HEAD 任何 i18n 包**，测试在工作树 **1 failed / 1 passed**（模板半边没写） | 不完整，**交 K-3 / J-4**：补键 + 模板半边后再提交，或回滚这 1 行 |
| `src/web/i18n_packs/errors.py` +6（09-05 13:30） | | **J-8 #185** `err.ca.config_unavailable / err.ca.enable_failed` zh/en；HEAD 里**无任何消费方** | 交 J-8：要么提交消费方一起来，要么删键 |
| `src/web/i18n_packs/zh_hant_auto.py` +230/−19（09-05 13:45） | | 自动生成的繁体包（新增 `ov2_s_rq` 等 I-4 / J 批键），regen 产物 | 交 K-5 ③段发版前统一 regen 后提交 |
| `desktop/renderer/shell-i18n-ext.zh_hant.js` +1/−1（09-04 09:43） | | 只有生成时间戳变了（340 键不变） | 同上，随 regen |
| `config/eval/xiaozhi_dialog_questions.yaml` +13（09-04 14:09） | | 「v1.0.73 发版批（I-2 三项入口 + 全自动影子档）」评测题 | I-2/I-5 尾巴，无风险；K-5 可随发版提交 |
| `src/web/templates/base.html` +3/−3、`workspace_base.html` +3/−3、`src/web/routes/assistant_pair_routes.py` +15/−5（**mtime 20:07，ACTIVE**）、`shared/assistant/assistant-agent.js` +499 / `assistant-ball.js` +1349 / `assistant-teach.js` +166、`tools/verify_assistant_ui.py` +28 | | **assistant-ball 线**（意向板无声明，但 20:07 仍在改） | 只记不动 |
| `desktop/build/installer.nsh` +315、`package.json`、`README.md`、`.gitignore`、`write-build-info.js`、`test/uninstall-nsh-invariants.test.js`、删 `installer-notice.txt`、untracked `installer-notice.{en_US,zh_CN}.txt` / `license_*.rtf` / `installerHeader*.bmp` / `installerSidebar*.bmp` / `write-installer-notice.js` | | **installer brand round2**（意向板有声明，18:47–20:1x ACTIVE） | 只记不动；K-5 ③段 bump 版本前等它静默 ≥30 min |
| `desktop/build/i18n-packs-staged/*.py` ×6（09-03～09-04） | | 打包同步产物 | 值守侧随打包重生成 |
| `src/skills/skill_manager.py` 剩余 3 hunk（L2055 / L3897 / L6143） | +64 | J-10 三期（`memory_promises` / `episodic` / `review_reason`） | J-10 自己提交 |
| `src/web/templates/unified_inbox.html` 剩余 8 hunk + `src/integrations/whatsapp_baileys_login.py` | | K-3 A（#181 dns_retry）在途，K-3 已于 `ba19ac50` 提交一批 | K-3 自己 |
| untracked `@AutomationLog.txt`（08-31）、`pc_runner_audit.jsonl`（08-31）、`tests/test_line_sync_bootstrap.py.reqseq`（09-05 05:55） | | 杂物 / 编辑器副本 | 建议 .gitignore 或删；未动 |

## 5. 有疑问交回原线

1. **`test_python_undefined_names::test_no_new_undefined_names` 红（HEAD 预存）**：`src/contacts/care_dispatcher.py` L822 返回注解 `-> Dict[str, Any]`，但 L23 `from typing import Any, Awaitable, Callable, List, Optional` 没有 `Dict`。引入 commit **J-8 B `a4a5ab63`**（09-05 14:46）。一词修复，交 J-8。
2. **#164 sticker 尾巴**（§4.2 第一行）不完整，不能按现状提交。
3. **孤儿线两文件 CRLF**（§4.1）：批准提交前先 `git add --renormalize` 或手工转回 LF。
4. `health_watchdog.phantom_unread_remind` 文档键缺（§3）→ K-3。

## 6. 事故记录（共享 index 竞态）

19:57 A4 首次 `git commit` 时，J-10 线刚把 5 个文件（`episodic_memory_store.py` / `memory_review.py` / `episodic_page.py` / `episodic_memory.html` / `test_episodic_review_queue.py`）暂存进共享 index，被我的 commit `80371f3c` 一并带走。处置：HEAD 未推送且仍是该 commit → 用临时 index 从父 commit 重建只含我 5 个文件的 tree，`commit-tree` + `update-ref`（带旧值守卫）把分支指回新 commit **`69e62aba`**；J-10 的 5 个文件自动回到「已暂存」原状（他们随后已自行提交）。`80371f3c` 仅存于 reflog。**此后 B / C / A3 全部改用私有 `GIT_INDEX_FILE` 临时 index 构建 → 预览 worktree 跑门禁 → `commit-tree` → 旧值守卫 `update-ref` → `git restore --staged` 同步共享 index 里我的文件条目**，全程不碰共享 index（脚本 `engines/chengjie/tmp/k1_build.ps1` / `k1_commit.ps1`，tmp/ 已 gitignore）。建议写进 `stage_hunks.py`：多线并发提交时「stage → commit」两步之间任何人的 `git add` 都会被卷走，临时 index 是唯一稳的路。

## 7. 收工状态

- 收工 HEAD：见 `git log`（K-1 六个 commit：`82ee11a5` `71355568` `69e62aba` `124dd921` `35b224c4` `c468483a`；期间 K-2 / K-3 / K-4 / J-7 / J-10 也在同分支持续提交）。
- `git status --short -- engines/chengjie`：**46 modified + 1 deleted + 15 untracked**（开工 64 + 1 + 17）。剩余全部在 §4 有归属或只记不动。
- **K-3 现在可以碰**：`unified_inbox.html`（A4 `69e62aba` + B `124dd921` 已落，K-3 已在用）、`config/config.example.yaml`（只剩孤儿线 `outbound` hunk，加文档键时 `--theirs unlimited_mode`）。
- **K-5 ①段现在可以重启装载**：`.py` 改动 = A1 `unified_inbox_desktop_routes.py` / A2 `goals/service.py` `goal_routes.py` / A3 `withdrawn_cite.py` `drafts.py` `skill_manager.py` / B `sprint_ticker.py` `liveness.py` `health_watchdog.py` `webhook_notifier.py` `goal_routes.py` `i18n goals.py` / C `health_watchdog.py` `webhook_notifier.py`。重启前 `restart_preflight`；`skill_manager.py` 仍有 J-10 3 个在途 hunk，重启只装 HEAD 版无妨。
- 台账标 fixed（K-D2，等两机装 1.0.74）：#160 → `82ee11a5`；#145③ → `71355568`（+ `bdae979e`）；#146 族 / #32② / #145⑤ → `c468483a`（+ `60c2d428`）；#32 #155 #156 #159 → `69e62aba`；#166 → `124dd921`（+ P0-1..5）；#120 / #159 族幽灵未读 → `35b224c4`。
- 预览 worktree `tmp/k1_preview` 已清；意向板 `-Done`；快照 tag `k1-before` / `backlog-snapshot-20260905-1930` 保留（老板看完孤儿线再删）。
- 已花：约 $70–80（估）。
