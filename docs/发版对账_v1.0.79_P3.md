# 发版对账 v1.0.79 · P-3 媒体承诺执行型（#259 #252）

> 指令：`docs/指令_P-3_媒体承诺执行型_2026-09-08.md`；底座 `docs/修复指令_P批_并行_2026-09-08.md`。证据 `tmp_diag\6SRA2B` / `BBRGMG`（本机工作树目录为空，按指令文件里的时间线复盘）；RRYKF6 第三次复现；CW6RP4 假成功待用户确认。
> 执行：2026-09-08 15:4x 开工（`agent_probe -Intent` 已登记）。**每个 commit 落地后立刻补一行。** 归属：`image_autosend.py` / `outbound_promise_guard.py` / `companion_selfie.py` / `autosend_helpers.py`（`[autosend image]` 与配文绑定一处 + 紧随的承诺守卫块）/ 对应 tests。`persona_reply.py` 未动（结构化 `send_photo` 段实为 `photo_directive` 协议，注入点在 `ai_client`，不在该文件；且有他人在途 hunk）；`autosend_worker.py` 未动（E 段改在投递层等效实现）。

## §〇 先答一问：守卫为什么「看见了却没拦」——识别 → 无动作权 三段

1. **[PHOTO] 请求失败后，正文仍按「图已发」写。** `build_photo_protocol_prompt` 明令「正文把照片当作已经拍好正在发来写（可以说"刚拍的"）」——这在图真发出时是对的；`run_autosend_image` 按请求选图失败（相册避重耗尽 / 冷却 / 无匹配场景 / 能力关）返回 False 之后，`_autosend_deliver` 只是「继续走文本」，配文「Just took this one for you—」离开了图就是谎，而承诺词表没有 `took this … for you` 这条，`detect_media_promise` / `detect_media_claim` 都判 ''。识别层零命中 → 无动作。
2. **第二句配文体在图发出后「语境闭合」。** `_mctx`（客户在索图）的判定是 `_media_ts < _req_ts`：14:49:20 图真发出后请求已兑现、门关上，14:49:51 的第二句 claim 即便词表认得也不会进 `detect_media_claim(media_context=True)`；`near_duplicate 跨轮相近放行`（worker）只看相似度不看「这句是不是照片文案」，放行。
3. **已发假声明对照 30 分钟账本被判「真话」。** 14:51「I thought it went through—let me try sending it again」`detect_sent_claim` 命中，但 `_media_sent_recently(window=30min)` 看到 14:49:20 那张 → 「近窗真发过＝真话放行」。账本口径没有「客户刚说没收到」这一维；而 `lie_caught` 在 `skill_manager._maybe_flag_media_complaint` 只写 `_media_coherence_hint` 让模型「别编」——实录证明 hint 式纠偏挡不住传输借口。14:51 客户「Where honey? No picture」连 `lie_caught` 都没认出（词表无 `no picture`）。

**修法 = 把四处接成固定动作（全部在 B 线投递层 `autosend_helpers._autosend_deliver`，那里才知道本轮有没有已回执的 send_media）**：A `[PHOTO]` 请求失败 → 正文强制进承诺链，回喂「无图可发」重生一句（`[media-bind]`）；B 承诺/断言无已回执媒体 → `strip → rewrite → review`（`[media_promise]`）；C 最新入站 `lie_caught` → 重发上一张（回执账本 / 相册投放账本）/ 强制实话模板 / 连续第二次转人工（`[media_complaint] action=`）；D 人设 `photos=false` → 剥空换诚实拒绝模板；E 5 分钟内刚真发过 + 本轮又是配文体 → 强制媒体语境判、必须附图否则改写。「真发成功」= `send_media` 回执 `delivered=True`（M-2 D 口径）+ `message_id` 进账本。

## 一、根因（每张单一句）

| 单 / 报告 | 症状 | 根因 |
|---|---|---|
| #259 6SRA2B | 14:49:51 第二句照片文案无图放行；14:50 / 14:51 两句传输借口谎话；「Where honey? No picture」未识别 | §〇 三段：请求失败正文不改 / 语境闭合 / 30 分钟账本判真 + lie_caught 只给 hint + 复诉词缺 |
| #259 BBRGMG · #252 RRYKF6 | 第三次「说发了没发」 | 同上；识别函数在，缺动作权 |
| #252 CW6RP4 | 假成功待确认 | 回执 mid 进 `[media-bind]` 日志，回访请 skuio 对 14:49 气泡 |

## 二、落点（每 commit 一行，落地即写）

| 项 | commit | 落点 |
|---|---|---|
| **B/C 识别层** 词表 + 模板 | `07f2168b` | `src/ai/outbound_promise_guard.py`：`_IMG_CLAIM_STRONG` 补 `just took this/that/it/one` / `took this (one) for you` / `here it is` / `here you go` / `there it is` / `should be there`；`_MEDIA_DONE_WEAK` 补 `let me send it (now/over)`；`_SENT_CLAIM_IMG` 补 `it went through` / `took a moment to load` / `loading on your end` / `check your phone/chat/inbox` / zh `看手机`（排除面补 `别看/少看/老看手机`）；新 `detect_photo_caption_claim` / `strip_photo_caption_claims`（承诺 ∪ 强制语境断言 ∪ 假声明合流）；新三语模板 `honest_no_photo_line`（D 段诚实拒绝）/ `lie_caught_honest_line`（C②，禁加载慢·再试·网络）/ `lie_caught_resend_caption`（C① 重发配文）；新 `build_photo_unsent_rewrite_instruction`（A 段无图回喂）。`src/ai/companion_selfie.py`：`lie_caught` 词表 +30（no picture / no pic / nothing came / still nothing / never got / don't see it / where honey / 没有照片 / 图在哪 / 还是没有…）；新 `_LIE_CAUGHT_WEAK_KEYS`（Where? / where is it / 哪呢 / 没有啊…）只在 `detect_media_complaint(text, media_pending=True)` 时采信 |
