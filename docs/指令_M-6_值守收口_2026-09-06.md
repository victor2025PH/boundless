# 指令 M-6：值守收口——钧 #215 定性 · 22 单回访 · 1.0.76 装载 → 两包三渠道 → 装机 → 台账

> **预算硬上限 $100。** 正常花费 $40–70。花到 $85 停下写落点表 + 总账。
> 类型：**值守 + 发版，非业务代码。** L-7 已把登记链自动化（报告到达 → 挂单 / 立单 → 回执带 #N；`duty_channel_reminder.py` 群提交提醒兼回执，skuio 与钧都在名单），本条不再做流程，只做**回访与 1.0.76 收口**。
> ⛔ 任何写台账先 dry-run 打印将要改什么；回访文案先贴落点表；对外一律走 `tools/duty_reply.py`（不教改配置文件）。

---

## 0. 开工前必读

1. `docs/修复指令_M批_并行_2026-09-06.md` / `docs/M批_进度总账.md`（M-1…M-5 落点表的「交 M-6 回访口径」节是回访文案的唯一来源）。
2. `docs/发版对账_v1.0.75_L7.md`（L-7 E 段 1.0.75 收口原命令：装载 / 回归 / 两包 / publish `-Channel internal` + lite + clean / 坐席三台 / 两机核版本；照抄改版本号）与 `docs/发版对账_v1.0.75_L5.md` §5（两机装载验证清单）。
3. `docs/值守_报障群用户画像_2026-08.md`（09-06 两处更新：skuio 每份报告都要单号回音；钧要短、结构化、逐条对应原话、不劝休息）。
4. 工具：`tools/duty_reply.py`、`tools/duty_ledger_fix.py --mark-fixed-file`、`tools/duty_notify_summary.py`、`tmp/mapping_c6efrs.py`（改 in74 集合 → 生成 1.0.76 版对账表）、`tmp/duty_tail_0906.py` / `duty_poll_0906.py` / `skuio_reports_today.py`（水位）。

## 1. 红线与边界

- 提交只用 `git add <明确文件>`；⛔ `git add -A` / `-a` / `reset --hard` / `checkout .` / `restore .` / 裸 stash。
- 台账只经端点 / `duty_*` 工具；不改业务代码——发现缺陷写落点表分派 M-1…M-5。
- 回访不轰炸：skuio 一份（≤2 条）、钧一份（≤1 条 + #215 定性一条）。

开工登记意向：
```
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "M-6 duty closeout: jun #215 verdict / 22-ticket callbacks + reconcile table v1.0.76 / 1.0.76 load -> regression -> packs -> 3 channels -> seats -> testers -> ledger"
```

---

## 2. A —— 钧 #215 定性回访（等 M-3 拿到 `dr-mtpwnp9h-wac8zc` 结论）

09-06 22:3x 已发受理回执（mid 1285 前后，问了两句：火焱最终收到没 / 现在再发一张能不能出）。M-3 落点表给出 13:39 两次失败是 WA 重连窗（→ M-2 A 的「未连接禁发 + 提示」）还是上传阻塞（→ M-3 A）后，一条短回复钉死原因 + 1.0.76 里对应哪条；语音「已核有声 vs 疑似无声」矛盾归 #161 族，告诉他修法在 1.0.76 或下一版。他要「照此执行」式的短句，不要过程。

## 3. B —— 22 张新单回访 + 对账表 1.0.76 版

1. 等 M-1…M-5 落点表齐 → 汇总每单「验收口径」；`tools/duty_ledger_fix.py --mark-fixed-file`（**先 dry-run 贴落点表再 `--apply`**）把 #214–#235 中已修的标 fixed（#216 / AC5XP9 验收通过的直接标 verified 口径写清）。
2. skuio 那份：第一条是对账表 1.0.76 版（`tmp/mapping_c6efrs.py` 把 in74 集合换成「1.0.75 已修 / 1.0.76 已修」两档重跑），第二条是 22 单逐条口径 + 「请优先验的 5 项」（建议：#218 关怀原文直发 / #232 发送打勾以回执为准 + 登录默认半自动 / #234 语言闸 / #227 98MB 视频 / #228 附件不串会话）；提醒他 1.0.76 也从 `downloads/internal/` 自动更新（已切内测渠道）。
3. 钧那份：他的单（#215 + #195 #213 复验口径）逐条对应原话，一条发完。
4. 记账 `mark_notified` → `duty_notify_pending.py --alert` 不再告警。

## 4. C —— 1.0.76 全批收口

照 `发版对账_v1.0.75_L7.md` E 段：`restart_preflight` → zhiliao 装载（M-1…M-5 全部 `.py`）→ 全量回归 + gate_sweep → 合并 `docs/发版对账_v1.0.76.md` → 打 smart + clean 两包 → `publish_chatx.ps1` internal + lite + clean 三渠道（L-5 落点表）→ 坐席三台 → 两机核版本（`duty_evidence probe`）→ 群播报（文案先贴落点表）→ 更新 `docs/值守症状族对账单.md`（新立「族 8 通道真相 / 登录默认」）。

---

## 5. 收工

- 落点表 `docs/发版对账_v1.0.76_M6.md`；总账 **M-6 行** + 「全批收口」勾选。
- 结束报告必含：① 钧 #215 定性原文；② 两份回访实际发出文案；③ 台账 dry-run 与 apply 数字；④ 两机装载后版本与 L-5 §5 四项核对；⑤ 本条发现但不该你动的缺陷 → 分派到 M-1…M-5 哪条。
- 收工清意向板 `-Done`。

**开对话粘这段：**

```
执行 M-6（值守收口：A 钧 #215 定性回访（等 M-3 结论）/ B #214–#235 回访 + 对账表 1.0.76 版 + 标 fixed（先 dry-run）/ C 1.0.76 装载 → 回归 → 两包 → publish internal+lite+clean → 坐席三台 → 两机核版本 → 播报 → 症状族更新）。先读 D:\boundless\docs\M批_进度总账.md 与 D:\boundless\docs\修复指令_M批_并行_2026-09-06.md，再读 D:\boundless\docs\指令_M-6_值守收口_2026-09-06.md 与 D:\boundless\docs\发版对账_v1.0.75_L7.md E 段（原命令照抄改版本号）。等 M-1…M-5 总账行都写「收工」再开 C；任何写台账先 dry-run 给我看；回访文案先贴落点表；对外只走 tools/duty_reply.py；不改业务代码。落点表 docs/发版对账_v1.0.76_M6.md；预算上限 $100。
```
