# 发版对账 v1.0.85 — Q-35（相册链：批量上传逐文件可见失败 + 自拍触发无命中坐席侧可见 · #316 #306）

> 指令：`docs/指令_Q-35_相册链_批量上传逐文件可见失败与自拍触发无命中可见_2026-09-12.md`。工单 **#316（4HK54G，jun，1.0.84，13:37）** / **#306（BPMEWX，Vanessa，1.0.84，11:43）**。总账：`docs/Q批_进度总账.md` Q-35 行。预算 $60。
> 红线：不改 Q-6 匹配打分与 `album_miss_addendum` 行为（AI 侧「无命中不沉默 / 改口」已在库不重做）；`personas.html` 工作树有 Q-26 C 未提交 `psn-ar-` hunk + 他线 `cp-i18n-ext ?v=` bump → 只用 `stage_hunks --mine`；`conv_state` 保持只读聚合（写路径独立成 marker 模块）。
> 执行：09-12 23:0x–。

## 0. 开工基线

| 项 | 事实 |
|---|---|
| 意向板 | `agent_probe -Intent "Q-35 album chain: pmaUpload per-file fail list + logger fmt gate + album_no_match note (…)"` → slug `q-35-album-chain-pmaupload-per-file-fail-list-lo`；index FREE。同刻 ACTIVE 他线：ASR P1（`asr_corrections.py` / `asr_eval` / `run_eval.py` / `ops_overview`）、zh_hant i18n（`cp-i18n-ext.zh_hant.js` ×2 / `shell-i18n-ext` / `personas.html` `?v=` bump）、label-leak（`l1_reason` / `autodraft_helpers` / `unified_inbox_account_routes`）、Q-26 C（`personas.html` `psn-ar-` +11 行未提交）。zhiliao 22:55 刚重启（14.6 min 前）→ 本线 `.py` 不自行重启，搭下一窗 |
| git | HEAD `10619433`；`personas.html` 三个未提交 hunk 归属：`@@ -1234` / `@@ -5230` = Q-26 C（`psn-ar-`），`@@ -9202` = zh_hant 线 `cp-i18n-ext ?v=20260912b`——**都不是本线**；`skill_manager.py` 一个他线 hunk（`@@ -5307` label-leak `system_label_hits`）；`ui-build.txt` 他线 bump 到 `20260912-2304` 未提交 |
| 现场（HEAD 核） | ① `pmaUpload()` L3819–3870 逐文件串行 POST，失败只 `fail++`，不记文件名 / 状态 / 响应体；② 上传路由 `persona_media_routes.py` 七个拒绝分支全是 `raise HTTPException(4xx, 文案)` → `{detail}` 无机器码，**且 raise 在成功日志之前 → 被拒的上传服务端零痕迹**（4HK54G「13:26 两张成功之后无上传行」正是这个形状；`accept="image/*"` 放进来的 iPhone `.HEIC` 走 `ext_not_allowed` 400 无日志是最像的解释）；③ 全仓 AST 扫 `logger.*("…%s…", …)` 占位符≠实参：`src/` 1269 文件 **3 处**（全在 `skill_manager.py`：L2015 `"%s意图链模式 %s case=%s chain=%s"` 配 3 参——第 4 参被乱码注释掉；L3910 格式串整行被注释掉 → `intent` 当 msg 跟 3 个多余实参；L3916 `"[Auto-Pilot] 已自动切换 %d …, switched"` 变量被卷进字符串 → 0 参），`scripts/` `tools/` 0 处——**三处都是 `not enough / not all arguments` 的 TypeError**，与 4HK54G 日志形状一致；④ `image_autosend.pick_registered_media` L1123 `[album_match] … candidates=0` 只打日志 + `note_album_miss`（进程内、生成侧 consume 即销）；`conv_state.compute` 六源里没有相册；状态带 `cs-band` 无相册一字 |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| B 服务端 + logger | #316 | `8751d114` | `persona_media_routes.py`：**新** `MediaReject(HTTPException)`（`reason` + `extra`）+ `_media_reject_handler` → `JSONResponse {ok:false, reason, detail, …extra}`（`detail` 键保留，旧前端 `r.json().detail` 仍可读），`register_persona_media_routes` 里 `app.add_exception_handler`（子类按 MRO 先于默认 handler）；`_reject(request, status, reason, key, pid/name/size, extra, **fmt)` 造拒绝**并落一行** `[pmedia] 上传拒绝 pid= name= bytes= status= reason=`；上传路由七分支改走它：`file_required`(400) / `ext_not_allowed`(400, extra `ext`+`allowed`) / `empty_file`(400) / `too_large`(413, `limit_mb`+`bytes`+`media_type`) / `bad_content`(400, `why`) / `save_failed`(500, `err`) / `too_long`(413, `max_sec`+`duration_ms`)；共用守卫 `_require_store`(503 `store_unavailable`) / `_require_write`(403 `readonly`) / `_require_persona`(404 `persona_not_found`) / `_owned_row`(404 `not_found`) 同形；face-ref 上传四分支同改；成功日志加 `name=`（与前端逐文件列表可对账）。sha 字节级重传仍 200 `deduped:true`、pHash 近重复仍 200 `near_dup`（旧契约不动）。核对路由前限制：无 body-size 中间件（Session / Compression / CORS / CSRF），uvicorn 默认无 body 上限，Starlette multipart `max_files=1000`——2MB×N 不会在路由前被拒；坐席机后端本地直连无 nginx `client_max_body_size`。`skill_manager.py` 三处 logger 修正（见 §0 ③；`stage_hunks --mine` 单独切出，不带他线 `system_label_hits` hunk）。**新** `tests/test_logger_format_args.py` 静态门禁：AST 扫 `src/ scripts/ tools/ main.py` 的 `<logger>.debug|info|warning|warn|error|exception|critical|fatal|log(字面量, …)` 占位符数（`%%` 不计 / `%(name)s` 映射式恰 1 参 / `*` 宽度精度各 +1）≠ 实参数即红；f-string / `%` 运算 / 非字面 msg / `*args` 刻意不管；`_ALLOWLIST` 空 + 防过期门禁 + 扫描器自证 8 坏 / 11 好 | `tests/test_persona_media_routes.py` **+8**：六个拒绝分支 `{ok:false, reason, detail}` 形状 + extra 数字；拒绝路径必有服务端日志行（caplog）；**复现**：一次 6 张 `PNG 头 + 2MB 随机字节` 逐个 POST（与 `pmaUpload` 串行同口径）→ 6/6 200、6 条入库、root logger 挂 `_FormatErrorTrap`（每条 record 强制 `getMessage()`）全程零格式化错误。`test_persona_media_routes` 33 + `test_persona_media_ai_routes` 6 + `test_logger_format_args` 21 = **57 passed**；`cjk_ledger_ratchet` + `test_admin_route_inventory` 5 passed。门禁自证：对 HEAD 版 `skill_manager.py` 跑扫描器 → 点名 L2015 / L3916 两处（第三处 msg 非字面量，手修） |

| A 逐文件结果 | #316 | `c3931380` | `personas.html`（`stage_hunks --mine "Q-35 #316" …`，5 hunk 全本线；Q-26 C `psn-ar-` ×2 + zh_hant 线 `cp-i18n-ext ?v=` 留工作树）：`pmaUpload(retryFiles)` 逐文件记 `{name, size, file, ok, existed, status, reason, detail, limit_mb/ext/max_sec}`——200 → ok（`deduped` → 已存在）、**409 → 已存在不算失败**、其余 4xx/5xx → `reason = d.reason || d.error || http_<status>` + `detail`（非 JSON 响应也记状态码）、抛错 → `AbortError/TimeoutError` = `timeout` 否则 `network:<msg>`；结果持久到 `_pmaUpLast{pid, results, ok, fail, existed, open}`，**新** `#pma-up-result` 容器（uploader 内）由 `_pmaRenderUploadResult()` 渲染并挂在 `_pmaRender` 尾部——修「结尾状态句被 `pmaLoad()` 整页重绘抹掉」的旧病；成功数 / 已存在数 / 失败数 + 「展开失败明细」（文件名 · 体积 · 原因人话 `_pmaFailReasonText`：太大(上限 {mb}MB) / 类型不支持({ext})——手机 HEIC 请先转 JPG / 内容不是有效图片 / 视频太长 / 空文件 / 超时 / 网络中断 / 只读账号 / 登录过期 / 服务端错误(HTTP n) / 路由前裸 413 也按太大）+ 「重试失败项（n）」（只重发 `!ok && file` 的 File[] 交回 `pmaUpload`）；失败 >0 默认展开；`console.info('[pma] upload … fails=[…]')` 一行便于截图报障；切人设 `_pmaUpLast=null`。CSS `.pma-up-result` 全 token（`--th-ink-emerald6/red7/amber7` 均在 theme-tokens.css）。词条 `pma_up_*` ×8 + `pma_fr_*` ×11 zh/en（`persona_apply_modal.py`）+ 繁体 regen（`i18n_hant.py generate` → `zh_hant_auto.py` 只切本线两 hunk，他线未提交键留工作树）。`ui-build.txt` 本线曾 bump `20260912-2358`，提交前被 Q-31 线 `a7fa8f11` 前一 commit 推到 `20260913-0002`（晚于本线模板改动 → 陈旧页横幅已覆盖本批，不再重 bump） | **新** `tests/test_album_upload_visibility_q35.py` 3 passed（记录字段 / 409·deduped 语义 / reason 优先服务端码 / 结果条挂 `_pmaRender` / 重试只挑失败项 / window 暴露 / 转义 / `pma_fr_*` 覆盖 B 段全部机器码 + zh·en 占位符齐平）。Node 直跑 `personas.html` 三个内联 script `node --check` 0 错；Node 行为 harness（假 apiFetch 9 种响应：200 / deduped / HEIC 400 / 413 / 409 / TypeError / AbortError / 500 JSON / 502 非 JSON）26/26：ok=1 existed=2 fail=6、人话映射、重绘后结果条回填、重试只重发 6 个失败项、切人设隐藏。前端门禁：`test_template_unique_ids` / `orphan_refs` / `dynamic_dot_access` / `free_capture` / `i18n_coverage` / `zh_hant` / `extra_langs` / `ui_build_freshness` / `css_comment_trap` 本文件相关全绿；红的 7 条全是他线文件（`unified_inbox.html` `loadConnectModes` 未挂 window / `t.cat_'+` / `toTimeString` / `--th-ink-gray6`、`reply_settings.html` `rps-po-empty`、`_client_tier_upgrading` / `settings` token）+ 1 条 `personas.html` 内联色旧债（`ca1deed8` 09-10 `al-chk-wrap rgba(255,255,255,.85)`，非本线）——只报告不代改 |

| D 2MB 闸豁免 | #316 追加（09-13 02:54 钧 mid 1548，明日香语 成功 0 / 失败 25） | 待 commit | `admin.py`：`is_persona_album_upload_path` 只认 `/api/personas/{pid}/media` 与 `…/face-ref`（恰好 5 段），body 闸与 send-media 同豁免。路由级上限仍是图 10MB / 视频 50MB。`/media/test` `/media/retag` 等 JSON 口不放行。落点表 §0「无 body-size 中间件」是误判——M-3 2MB 闸一直在，相册口当时没进豁免表 | `tests/test_album_body_limit_q35.py`：matcher + 中间件接线 + 2.5MB PNG 不再被 `max_body_bytes=2MB` 413 + `/media/test` 2.5MB 仍 413 |

| C #306 坐席侧一行 | #306 | `1ae6c3dc`；`conv_state.py` 三 hunk 已被 Q-30 线 `6ae59bdb`（00:19）整文件提交顺手带走 | **新** `src/inbox/album_miss_marker.py`（写路径独立成 marker，`conv_state` 保持零写）：KV `album_no_match:<cid>={ts, first_ts, n, query≤80, scene, persona_id}`（复用 `app_settings`，与 `ai_fail_marker` 同款不建表）；`mark(cid, query, scene, persona_id, store=None)` store 缺省经 `protocol_bridge.get_inbox_store()` 惰性取（拿不到＝静默 no-op），6h 内重复无命中只累计 `n`；`clear` 有 note 才写；`get(ttl=24h)` 陈旧不显不删；`deep_link(pid)` → `/personas?pid=<quote(pid)>&pma=1&filter=notrg`。`image_autosend.pick_registered_media` L1130：`row is None and _ask` → 在既有 `note_album_miss` 之后**旁路** `mark(conv_key, query=peer_text, scene, persona_id)`；`row and conv_key` → `clear`（命中即清）——Q-6 打分 / `note_album_miss` / `album_miss_addendum` 一字不动。`conv_state.compute`：新 `_src_album_miss` 只读第七源 → `out["notes"]=[{kind:album_no_match, query, scene, persona_id, n, ts, hhmm, ago_sec, link, text_key}]`，**不进 `sources`（六源契约不动）、不改 state / tone**（AI 已改口照常回；与 Q-30 同文件并行：`_src_album_miss` 单独成 hunk、`notes` 放 `out` 之后避免与其 `peer_guard/pacing_hold` 混 hunk）。`unified_inbox.html`（4 hunk `cs-note`）：`#cs-band` 下新 `#cs-note`（`class="cs-band cs-warn cs-s-note"` 复用既有样式**零新 CSS 零 ?v= bump**）—— `_csRenderNotes(st)` 在 `_csRender` 显 / 隐两分支都刷（切会话不残留），文案 `inbox.cs.note.album_no_match{,_n}`（q＝客户原话 ≤12 字省略 / 空回落「自拍」，`hhmm`，n>1 带「今天 n 次」），title `_t` 解释「AI 已改口没空头承诺 · 补触发词下次就能发 · 补完自动消失」，动作 `_csNoteAction` 只认站内 `/` 深链 `window.open(_blank)`；旧后端无 `notes` → 永久隐藏。`personas.html`（3 hunk `pma-deeplink`）：DOMContentLoaded 解析 `?pid=&pma=1&filter=` → `_pendingHashProfile/_pendingHashTab='album'` 走既有 `#profile=&tab=` 深链机制 + `_pmaPendingFilter`（只认 notrg/nohit/off/untagged/photo/video 枚举）由 `pmaLoad` 首次渲染消费一次。词条 `inbox.cs.note.*` ×6 zh / en / **手写繁体**（`conv_state_q26.py` 三 dict；该 pack 的 ZH_HANT 属人工包，auto regen 不碰） | **新** `tests/test_album_no_match_note_q35.py` **9 passed**：marker 写 / 累计 n / first_ts 不变 / 24h 陈旧不显不删 / clear 有则写无则零写 / 无 store no-op / deep_link 转义；`pick_registered_media` 索风景无命中 → Q-6 `consume_album_miss` 仍出 `scene=outdoor`（红线自证）+ 持久 note 带 query·persona·scene，闲聊「在吗」无命中不记（宁缺勿滥），运营补词后同句命中 → note 清，无 conv_key 不记不抛；`compute` 零写 · state 仍 auto · `sources` 不含 album_miss · notes 形状 / link / text_key / ago_sec · 人审档也带；无 note / 陈旧 / 脏 JSON / 坏 store 全 `notes=[]` 不抛；模板契约（cs-note 行 + 显隐双刷 + window 暴露 + 只认 `/` 深链；personas 深链消费 + 筛选枚举）；词条三语占位符齐平。`--preview` 游离 worktree（HEAD+index）跑 Q-35 全部门禁 + `test_conv_state_q26` + `test_album_match_q6` **89 passed**；主树 `test_conv_state_q26`（含 Q-30 未提交 peer_guard/pacing_hold 断言）42 passed 同绿；`unified_inbox.html` 六个内联 script `node --check` 0 错；`test_i18n_zh_hant` / `extra_langs` 13 passed。`test_image_autosend` 7 红为旧债（`git stash push -- image_autosend.py` 探针：HEAD 版同 7 红——Q-6 09-09「随机兜底默认关」后该文件 09-02 起未更新），非本线 |

## 2. 收工（09-13 00:3x）

| 项 | 事实 |
|---|---|
| commit | B `8751d114`（#316 服务端 + logger 门禁）→ A `c3931380`（#316 逐文件结果）→ C `1ae6c3dc`（#306 坐席侧一行）→ 收工 docs commit；`conv_state.py` 三 hunk 随 Q-30 `6ae59bdb` 先行落地（fail-soft import，marker 模块在 C 落地前 HEAD 不崩） |
| 生效 | **热更已生效**：`personas.html`（逐文件结果条 / 重试 / 深链消费）、`unified_inbox.html`（cs-note 行；旧后端无 `notes` 字段 → 隐藏零噪音）、`persona_apply_modal.py` / `conv_state_q26.py` / `zh_hant_auto.py` 词条。**须 zhiliao 重启**：`persona_media_routes.py`（结构化拒绝 + 拒绝日志）、`skill_manager.py`（3 处 logger）、`album_miss_marker.py` / `image_autosend.py` / `conv_state.py`（note 链）。本线**不重启**（zhiliao 22:55 刚重启过、Q-30 / Q-36 同时段落 `.py`），攒下一窗 04:00 一并装载；重启前 `restart_preflight`。坐席包：随 R85 |
| 门禁 | 本线：`test_persona_media_routes` 33 / `test_persona_media_ai_routes` 6 / `test_logger_format_args` 21 / `test_album_upload_visibility_q35` 3 / `test_album_no_match_note_q35` 9 / `test_conv_state_q26` 22（含 Q-30 未提交断言）/ `test_album_match_q6` + `test_prompt_addenda_q6` —— 全绿；`--preview` 游离树 89 passed。**他线红（只报告）**：`unified_inbox.html` `loadConnectModes` 未挂 window（`test_inbox_inline_handlers_exported` / `test_rpa_inline_handlers_exposed`）、`t.cat_'+`/`t.pol_'+` 动态点拼接、`toTimeString().slice` 旧日期格式、`--th-ink-gray6` 未定义 token、`--th-bg-gray100`/`slate4` fallback 漂移（connect-modes 线）；`reply_settings.html` `rps-po-empty` 孤儿引用（Q-26 C）；`_client_tier_upgrading` / `settings.html` token；`personas.html` 内联色 1 处旧债（`ca1deed8` 09-10）；`test_image_autosend` 7 红旧债（Q-6 语义变更后测试未更新） |
| 4HK54G / 明日香语真因 | 4HK54G 当时前端不记、后端拒绝无日志，本轮不可回溯。**09-13 02:54 钧 mid 1548**（1.0.85 结果条已可见）：25 张 `lin-yourou-selfie-*.png` 全是 2.1–2.7MB，「网络中断 Failed to fetch」、zhiliao 零 `[pmedia]` 行＝M-3 同一把 2MB 全局 body 闸（413 + Connection:close）。§0 / B 段「无 body-size 中间件」是误判。D 段豁免相册 / face-ref 上传口。须重启工作台后端才进 117；坐席包随下一版 |
| 预算 | $60 内；未做：坐席侧 note 的浏览器真机门禁（`unified_inbox.html` 同刻被 connect-modes 线活跃编辑，静态契约 + Node 语法检查代替）；`pma_up_*` 词条 vi/th/id 三语走 EXTRA_LANG_BASE 回落简体（与全站同口径） |

### 1.1 回访 / 证据

- **#306**（Vanessa「后台传了很多图为什么不发」）：C 段之后，客户索图相册无命中时会话头出琥珀「相册没有匹配『发张自拍』的图 · AI 已改口 · 21:03」+「去相册补标签」→ 相册「缺触发词」筛选；补完下一次命中自动消失。AI 侧行为不变（`album_miss_addendum` 早已让 AI 改口，本轮零改动）。`.py`（marker / image_autosend / conv_state）须 zhiliao 重启才生效，模板 / 词条已热更；**本线不重启**，攒下一窗。
- **#316**（4HK54G「上传很多图只成功两张、其余不知为何」）：B 段之后每个拒绝都带 `reason` + 服务端一行日志；A 段把它逐文件摆到坐席眼前。4HK54G 里到底几张、为什么——**本轮无法回溯**（当时前端不记、后端不写），下一次报障日志里会有 `[pmedia] 上传拒绝 … reason=` 行。`--- Logging error --- not enough arguments for format string` 三处源头已修，门禁防再犯。
