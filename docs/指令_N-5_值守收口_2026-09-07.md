# 指令 N-5：值守收口——并单 · 钧机取证脚本升级 · 两包定性 · 自动立单去重 · 1.0.77

> **预算硬上限 $100。** 正常花费 $40–70。花到 $85 停下写落点表 + 总账。
> 类型：**值守 + 发版，非业务代码。** 登记链与回执已自动化（L-7 A / `duty_channel_reminder`），本条做人工判断的部分。
> ⛔ 任何写台账先 dry-run 打印将要改什么；回访文案先贴落点表；对外一律走 `tools/duty_reply.py`。

---

## 0. 开工前必读

1. `docs/修复指令_N批_并行_2026-09-07.md` / `docs/N批_进度总账.md`（N-1…N-4 + 接手 M-7 落点表「交 N-5」节是回访文案唯一来源）。
2. `docs/发版对账_v1.0.76_M6.md` C 段（1.0.76 收口原命令：装载 / 回归 / 三包 / publish 三渠道 / 坐席 / 两机；照抄改版本号）；`docs/指令_I-6_值守流程与积压_2026-09-04.md` §3 F2（zl_collect v1.3.1 下发口径：**明说覆盖旧文件**）。
3. 已做（09-07 22:3x，本文作者）：钧 #250 受理回复（含「你的脚本是旧版 v1.2.2」+ #215① 真因）；新 diag-request `dr-mtrcb0iy-svzop9`；`TAZ9QS` 登记到 #215；#250 版本回填 1.0.76。
4. 工具：`tools/duty_reply.py`、`tools/duty_ledger_fix.py`、`tools/duty_auto_ticket.py`（L-7 A）、`tools/duty_evidence.py`、`tmp/skuio_reports_today.py <since>` / `tmp/group_since.py "<时刻>"` / `tmp/jun_report_1317.py`（水位）。

## 1. 红线与边界

- 提交只用 `git add <明确文件>`；⛔ `git add -A` / `-a` / `reset --hard` / `checkout .` / `restore .` / 裸 stash。
- 台账只经端点 / `duty_*` 工具；`bug_intake.py` 不改（去重规则改在 `tools/duty_auto_ticket.py`）。
- 回访不轰炸：skuio 一份 ≤2 条、钧一份 ≤1 条。

开工登记意向：
```
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "N-5 duty closeout: merge dup tickets / jun zl_collect v1.3.1 resend / TAZ9QS + new pack verdicts (#215 #250) / auto-ticket dedup / 1.0.77 load-packs-channels-callbacks"
```

---

## 2. A —— 并单与关单（先 dry-run）

- #244 #245 #246 #247 #248 #249 → `dup_of=243`、closed（一件事七张单：自动链对「核实 / 复查 / 补证」类 note 各立新单）；#251 → `dup_of=250`、closed（钧贴文本重复登记）；MJGHKQ 的证据从 #232 改挂 #237；#242（版本确认）closed；VC9ERR（无备注）不立单。
- 用 `tools/duty_ledger_fix.py` 或照 09-05 `duty_split` 脚本形态写一次性脚本，**默认 dry-run 打印，老板点头再 `--apply`**；每张并单发 `bug_dup` 事件。

## 3. B —— 钧机取证脚本升级（D-N5，K-5 ⑤ / I-6 F2 遗留）

`deploy/field-agent/zl_collect_jun.ps1`（v1.3.1）+ `AGENTS_JUN.md` 以**文件消息**发进群（`duty_reply` 不支持文件 → 用 09-04 10:31 mid 1088 那次的发文件路径：`/api/unified-inbox/send-media` 或 bot 链 `send_document`，落点表写用的哪条），正文**明说「覆盖旧文件，不是新增」**，让他跑一次 `-Task selfcheck` 回 6 位码；顺带把 N-4 E 的 `[tts]` 关键词加进 AGENTS 剧本。skuio 机若也是 v1.2.2（他的报告头写 v1.3.1，应已升）核一下。

## 4. C —— 两包定性（#215 / #250）

- `TAZ9QS`（17:41）：核 09-06 13:39 WA 发图 / 发视频那两条有没有 `[send-media]` 或 413 记录——若零记录 + 文件 >2MB → 坐实 M-3 A「全局 2MB 闸」；写进 #215 note 并把 #215 标 fixed（1.0.76 已修，回访口径见 M-3 落点表）。
- `dr-mtrcb0iy-svzop9`（到达后）：交 N-4 A 用；本条只登记 `received` 并在 #250 note 写「包已到，交 N-4」。

## 5. D —— 自动立单去重加强（`tools/duty_auto_ticket.py`）

- 同报障人 **90 分钟内** note 以「复查 / 核实 / 补证 / 补 <码> / 第 N 次」开头，或含同一实体（会话 id / 客户名 / `id=N` / 单号）→ 挂到该实体最近的开放单，不新立。
- note 为空（VC9ERR）→ 只回执不立单（已是）；`verify` 类不立单（已是）。
- 门禁：`tests/test_duty_watch_autoticket.py` 加 #243 七连发的回放样本 → 只立一单。

## 6. E —— 1.0.77 收口（等 N-1…N-4 + 接手 M-7 总账行都「收工」）

照 `发版对账_v1.0.76_M6.md` C 段：`restart_preflight` → zhiliao 装载（+ 若 N-1 改了边车则 Node 也重启）→ 全量回归 + gate_sweep → 合并 `docs/发版对账_v1.0.77.md` → 三包 → publish 三渠道 → 坐席三台 → 两机核版本 → 播报（文案先贴落点表）→ #236–#251 按落点表标 fixed（dry-run 先过目）→ 回访两份（skuio 对账表重跑 `tmp/mapping_c6efrs.py`；钧短句）→ `docs/值守症状族对账单.md`（族 5 关怀 + 立即发层 / 新族 9 域包错配）。

---

## 7. 收工

- 落点表 `docs/发版对账_v1.0.77_N5.md`；总账 **N-5 行** + 「全批收口」勾选。
- 结束报告必含：① 并单 dry-run 与 apply 数字；② 钧脚本文件发出的 mid 与他 selfcheck 回码；③ 两包定性原文；④ 两份回访实际文案；⑤ 两机装载后版本；⑥ 本条发现但不该你动的缺陷 → 分派到 N-1…N-4 哪条。
- 收工清意向板 `-Done`。

**开对话粘这段：**

```
执行 N-5（值守收口：A 并单 #244–#249→#243、#251→#250、MJGHKQ 改挂 #237、#242 关（先 dry-run 给我看）/ B 钧机 zl_collect v1.3.1 + AGENTS_JUN 文件发进群并明说覆盖，等他 selfcheck 回码 / C 读 TAZ9QS 定性 #215（2MB 闸）并标 fixed，新包到达登记交 N-4 / D 自动立单去重加强（同人 90 分钟内复查·核实·补证或同实体 → 挂最近单）/ E 1.0.77 装载 → 回归 → 三包 → 三渠道 → 坐席 → 两机 → 台账 → 回访（等 N-1…N-4 与接手 M-7 都收工再开）。先读 D:\boundless\docs\N批_进度总账.md 与 D:\boundless\docs\修复指令_N批_并行_2026-09-07.md，再读 D:\boundless\docs\指令_N-5_值守收口_2026-09-07.md 与 D:\boundless\docs\发版对账_v1.0.76_M6.md C 段。任何写台账先 dry-run；对外只走 tools/duty_reply.py；不改业务代码；落点表 docs/发版对账_v1.0.77_N5.md；预算上限 $100。
```
