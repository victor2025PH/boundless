# 修复指令 P 批（全自动出站链：不群发 · 不编事实 · 不谎称发图 · 出厂态与安全）—— 并行公共底座

> **本文件只是「索引 + 去向表 + 文件归属 + 老板决策」的公共底座，不要拿本文件开对话。**
> 每条指令各自成文，**一条指令 = 一个新对话**。开对话粘贴话术在各指令文件末尾，也汇总在 `docs/P批_进度总账.md`。
>
> 依据：`docs/全自动出站链_三视角方案_2026-09-08.md`（1.0.77 复测 11 份报告 → #258 #259 + 挂 #252 #254；误挂 #106）。
> **版本边界**：O 批（O-1 A–E / O-2 / O-3 / O-4 / O-5）全部在 HEAD，由 **R78 正在打 1.0.78**（15:1x 装载回归中）。**P 批目标版本 1.0.79**，不拦 1.0.78（D-P3 修正，见下）。
> 分成两波：**第一波 P-1～P-5 并行**；**第二波 P-6** 在 P-1 收工后开（同一文件 `outbound_humanize.py`）。

| 指令 | 预算 | 内容 | 文件 |
|---|---|---|---|
| **P-1** | $150 | 起草即净化（去 AI 标点前移到起草层，草稿 = 将发文本）+ 引用锚点守卫 `claim_guard`（you mentioned / 你之前说 无锚点即改中性）+ 质检页「AI 指纹」卡 | `docs/指令_P-1_起草即净化与引用锚点_2026-09-08.md` |
| **P-2** | $220 | 登录 / 切档不群发：边车回填标记不进自动化 + 起草入站年龄闸 72h + 切档 / 登录确认框 + 「沉寂会话待你决定」清单；停联名单账号级持久 + 登录后历史扫描；自聊会话「我自己 · 备忘」排除自动化 | `docs/指令_P-2_登录切档不群发与停联持久_2026-09-08.md` |
| **P-3** | $160 | 媒体承诺执行型：文字绑动作（结构化 send_photo → 真发才配文）、承诺句无动作即改写、lie_caught 固定动作（重发上一张或说实话）、photos=false 硬拒、5 分钟内第二句照片文案必须附图 | `docs/指令_P-3_媒体承诺执行型_2026-09-08.md` |
| **P-4** | $150 | 出厂态与安全：后台默认回环 + 「允许局域网设备连接」显式开关；陪伴域首装 KB 为空 + 预填模板 + 系统话术移到系统设置；打包清单（handoff yaml 进包 / fatex.db 出包 / 必须在·必须不在门禁）；`mb_trial_line` None 分支；日志域名标签 | `docs/指令_P-4_出厂态与安全_2026-09-08.md` |
| **P-5** | $80 | 值守链三修 + 归位：挂单规则收紧（`#N` 上下文）+ 不升 fixed/closed 单；回执重试上限 + 幂等 + 空备注不回执；send 失败不留幻影 out 行且 502 带原因；MTRCH2 从 #106 摘出新立单；#259 去向注记；回访口径 | `docs/指令_P-5_值守链三修与归位_2026-09-08.md` |
| **P-6**（第二波） | $150 | 口语简写层「口语化程度」三档（默认中）：英 / 中 / 日 词典按概率、会话内一致；称呼词只随人设；偶发拼写错可关；与拟人程度联动；门禁 + 盲评 | `docs/指令_P-6_口语简写层_2026-09-08.md` |

**投放顺序**：P-1～P-5 同时开；P-6 等 `P批_进度总账.md` P-1 行变「收工」再开。**R78 不等 P 批**；P 批收口 = 1.0.79（发版由 P-5 交接给发版线 R79，或 P-5 自己按 `docs/发版对账_v1.0.77_N5.md` §6 全链做）。
**跨对话交接唯一入口**：`docs/P批_进度总账.md`。
**背景必读**：`docs/全自动出站链_三视角方案_2026-09-08.md`；`docs/发版对账_v1.0.78_O1.md`（O-1 A 停联 / B 去标点 / C 底稿 / D 节奏 / E 三小件的落点——P-1 P-2 P-6 都建在它上面）；证据 `tmp_diag\{MTRCH2,32PTMK,Y3C6PS,ZH3ZQ5,34585H,U4ZAD7,BBRGMG,6SRA2B,A7PB2F,H3BAJD}\`。

## 去向

| 去向 | 报告 / 单 | 说明 |
|---|---|---|
| P-1 | U4ZAD7 Y3C6PS（#259 破折号仍在）· ZH3ZQ5③（编造回忆）· 34585H E/F（生成侧 few-shot、日志、门禁） | O-1 B 在 HEAD 但只挂发送门 |
| P-2 | ZH3ZQ5②（切全自动批量 opener）· H3BAJD（登录 3 秒批量 + 含 Sinue）· A7PB2F / H3BAJD①（自聊会话） | 触发层 |
| P-3 | 6SRA2B BBRGMG（RRYKF6 第三次） | `outbound_promise_guard` 只识别不执行 |
| P-4 | MTRCH2 ①②③④（fatex.db / 0.0.0.0 / handoff yaml / 文案）· 32PTMK（首装 KB 残留）· MTRCH2「日志标签仍写 conversion」 | 出厂态 |
| P-5 | MTRCH2 误挂 #106 · 32PTMK 12 次 502 幻影行 · 2WD7FY / ZFFBG6 空备注 | 值守链 |
| P-6 | 34585H A–D、G | 第二波 |
| 不在 P 批 | MTRCH2「System prompt 仍 1868 字」「stop_contact 未生效」「care interval 600s 与精确调度相反」「帮助语料 292 条」 | 前两条随 1.0.78（O-1 C / A）；后两条 P-4 只核实记录不改 |

## 文件归属矩阵（共享工作树；开工先 `scripts\agent_probe.ps1` 看 ACTIVE；同文件混有他人在途改动时用 `python tools/stage_hunks.py --file <路径> --mine <regex> --apply` 只暂存自己的 hunk）

| 文件 / 目录 | 归属 |
|---|---|
| `src/inbox/drafts.py`（**只动** `auto_generate_draft` 返回前的净化调用一处 + 入站年龄闸调用一处——两处分别归 P-1 / P-2，各自只加自己那一行块）、`src/inbox/outbound_humanize.py`、新 `src/inbox/claim_guard.py`、`src/inbox/persona_reply.py`（生成侧提示词禁令 + few-shot 段）、质检页模板 / 路由（`ops_quality*` 或既有质检页）、对应 tests | **P-1**（P-6 第二波接手 `outbound_humanize.py`） |
| `services/whatsapp-baileys/server.js`（历史 / 同步事件打标）、`src/inbox/normalizer.py`（`backfill` 落库不触发 + `peer == self` 标记）、`src/inbox/account_bulk_mode.py` + `src/web/routes/unified_inbox_stored_read_routes.py` L612（切档 plan 带沉寂统计 + 确认）、新 `src/inbox/dormant_review.py`（沉寂清单）、`src/inbox/stop_contact.py` + 新账号级 `blocklist` 持久 + `account_scope_migration.py`、`unified_inbox.html` **只用 stage_hunks 暂存自己的 hunk**（他人在途）、对应 tests | **P-2** |
| `src/inbox/image_autosend.py`、`src/ai/outbound_promise_guard.py`、`src/ai/companion_selfie.py`（`detect_media_complaint` 词表 + lie_caught 动作）、`src/inbox/autosend_helpers.py`（**只动** `[autosend image]` 与配文绑定一处）、`src/inbox/persona_reply.py`（**只动** 结构化 `send_photo` 输出段——与 P-1 分段，先看 P-1 是否已在改；冲突则 P-3 等 P-1 提交后再动）、对应 tests | **P-3** |
| `desktop/backend-launcher.js` + `desktop/test/backend-launcher.test.js`、设置页「局域网访问」开关（模板 + 路由 + i18n）、`src/utils/kb_store.py`（`seed_kb_format_examples` / `seed_default_data` / `system_seed_plan` 按域）、KB 页模板（系统话术类移出）、系统设置「兜底策略」页、`desktop/build/build_backend.py` L60–64 + `stage_clean_assets.py` / `stage_internal_assets.py`、`desktop/test/package-layout.test.js`（必须在 / 必须不在）、`src/web/i18n_packs/membership.py` L89 + 数据源、日志标签处、对应 tests | **P-4** |
| `tools/duty_auto_ticket.py`、`tools/duty_channel_reminder.py`、`src/web/routes/unified_inbox_send_routes.py`（**只动** 失败分支：不落幻影行 / 502 带原因——该文件有他人在途 hunk，用 stage_hunks）、台账（只经端点 / `duty_*`）、`docs/**` | **P-5** |
| `src/inbox/outbound_humanize.py`（P-1 收工后）、`reply_settings.html` 「口语化程度」滑块 + i18n、人设词表（称呼词）、对应 tests | **P-6** |
| R78 在动的：`package.json` 版本、changelog、发版脚本、`docs/发版对账_v1.0.78_发版.md` | **R78，P 批不碰** |
| QQ 两线在动的：`platform_readiness.py` qq 分支、`qq_*`、`official_api_worker.py` qqbot 分支、`services/qq-milky` | **不碰** |
| 微信线（未登记意向板但 11:1x 仍在写）：`wechat_*`、`src/integrations/wechat_pc/`、`desktop_bridge_presence.py`、`compliance/*` | **不碰** |
| `store.py`、`autosend_worker.py`（O-1 刚收工，`.py` 待装载；P 批任何人要动它先在总账登记一行） | 默认不碰 |

## 老板决策（老板 09-08 15:1x：「需要拍板的按你的建议来做」——以下即生效）

| 决策 | 定案 | 说明 |
|---|---|---|
| **D-P1** 入站年龄阈值 | **72 小时**（`inbox.auto_draft.max_inbound_age_hours`，可配）；超龄入站不自动起草，进「沉寂会话待你决定」清单 | P-2 |
| **D-P2** 局域网访问默认 | **默认关**（绑 127.0.0.1）；设置页显式开关「允许局域网设备连接（手机扫码操控需要）」，公共网络类别开启需二次确认；#57 扫码操控用户首次用时引导开启 | P-4 |
| **D-P3** 1.0.78 是否等 P 批 | **修正为「不等」**：O-1 五段 13:5x 已收工、R78 15:1x 已在装载回归；停联硬停与零破折号已验过，多等一小时就多一小时带指纹跑。**1.0.78 = O 批（拟人止血一）照发；P 批 = 1.0.79（拟人止血二）紧跟**。回访口径：「1.0.78 发出去的消息零破折号；草稿预览里的净化随 1.0.79」 | 全批 |
| **D-P4** 停联名单持久层 | **账号级**，随账号 / 人设备份导出导入；登录同步完成后自动扫历史（双语词表复用 O-1 A） | P-2 |
| **D-P5** 口语简写层 | **做，1.0.79 第二波**；默认中档；称呼词只随人设词表（性别 / 关系阶段），禁止全局一律 | P-6 |
| **D-P6** 陪伴域首装 KB | **完全为空** + 「新建条目」域内预填模板；「系统话术」整类移到系统设置「兜底策略」（开发者模式可见）；陪伴域默认零直发兜底，AI 失败 = 静默 + 转人工提示；销售域保留现状 | P-4 |

## 共同纪律 / 省钱纪律 / 断点续传

与 J～O 批相同（`docs/修复指令_J批_并行_2026-09-05.md` 三节）。再钉三条：① 提交前 `git diff --cached --name-only` 为空再 add，共享文件用 `tools/stage_hunks.py`；② **每个 commit 落地后立刻补落点表一行**（`docs/发版对账_v1.0.79_P<n>.md`）；③ 对外答复一律 `engines/chengjie/tools/duty_reply.py`，只给版本号不给日期。
