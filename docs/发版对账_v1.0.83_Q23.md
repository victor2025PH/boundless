# 发版对账 v1.0.83 — Q-23（自动出站单一闸门 + GuardContext 场景维度 · 事故 #303）

> 指令：`docs/指令_Q-23_自动出站单一闸门与场景维度_2026-09-12.md`。底座：`docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵：本线是本批唯一动 `autosend_worker.py` / `autosend_policy.py` 的线；`drafts.py` / `protocol_autoreply.py` 钩子一行；`persona_reply.py` 只加一个短生成入口）。在 Q-15（`发版对账_v1.0.79_Q15.md`）/ Q-17（`发版对账_v1.0.81_Q17.md`）/ Q-18（`发版对账_v1.0.82_Q18.md`）三张落点表之上改。总账：`docs/Q批_进度总账.md` Q-23 行。预算 $140。
> 执行：09-12 02:5x–。

## 〇 事故根因（一句话）

Q-15 成人软回应经 `svc._inbox_deliver_cb`（= `AutosendWorker.deliver_human_approved`，「坐席人工通过」专用直投链）出站——绕过 `autosend_policy.decide` / `_human_priority_gate`（含 mode_changed）/ 节奏与 Q-3 闸；守卫本身无「群 / 发送方 / 档位」场景维度：报障群（TG 负 peer，`mode=review`）里**我方值守**的「给我看你的日志」被 zh 词表裸命中 explicit → 4 条罐头软回应直投进群（mid 1445 1454 1459 1461，发送方 7331682688）。

## 0. 开工基线（02:5x–03:0x）

| 项 | 事实 |
|---|---|
| 意向板 | `agent_probe -Intent "Q-23 autosend single gate + GuardContext (…)"` 已登记；同刻在途：i18n 线（`admin.py` / `i18n_packs/*` / 模板 HOT-ZONE）、桌面壳线（`deploy/desktop/*`）——本线不碰 |
| git | 索引空闲；HEAD `7781e97f`（Q-23/24/25 指令 + 总账） |
| 工作树 21:01 止血 | `adult_grader.py`（+66：`_conv_chat_id` / `blocks_adult_outbound` / `dispatch_soft_reply` / `send_soft_reply` / `regrade_inbound` skipped=public_chat / zh 词表消歧）、`drafts.py`（+5：`_adult_conv` 带 `chat_type`）、`test_adult_grader_gate.py`（+57）；**`bug_intake.py` 工作树 diff 是 `build_fix_notify_text` 多行文案（配 `test_bug_intake.py` +1 断言），不是 `is_bug_group`——`is_bug_group` 早在 HEAD（L271），指令原文写「bug_intake.is_bug_group 止血」有偏差，本线不带该文件** |
| 台账 | `D:\chengjie-instances\zhiliao\data\config\bug_intake.db` 最新 #302；报障群 = `-1004345824259`（299 单），运维群 = `-1004290740529`；`bug_intake.support_accounts` = 6834964252 / 8506426282；`peer_bot_guard.enabled=true daily_reply_budget=0` |
| 出站直投调用方盘点 | `_inbox_deliver_cb` 非坐席调用方**只有** `adult_grader._deliver_cb`；Q-2 C 委婉延后（drafts L1251）与 stop_contact farewell（drafts L1274 / worker `_farewell`）**已经**是 L2 稿经 `decide` → worker 批处理（review/manual 档天然不发）；A 线 farewell（protocol_autoreply L633）走 A 线自己的直发链 + 群 opt-in 闸（L394） |
| 测试基线 | `test_adult_grader_gate.py` 36 passed（含 21:01 新增 5 例） |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| 0 立单 | **#303**（新，P0，reporter 值守，chat -1004345824259，account 7331682688，`reporter_version=1.0.82.0`） | — | `record_bug_ticket`（`%TEMP%\q23_ticket.py` dry-run → `--apply` 03:01）→ 自动判 P2 按口径 UPDATE **P0**、title 取首行「群聊 / 人审档软回应外泄：报障群 4 条」、body 现象 / 证据 mid 1445 1454 1459 1461 + 发送方 / 根因 / 六点要求 / 去向 Q-23；事件 `duty_ticket` | 库核 #303 new P0 body 673 字 |
| 0 入库止血 A0 | #303 | `fbb75d15` | `src/inbox/adult_grader.py` / `src/inbox/drafts.py` / `tests/test_adult_grader_gate.py`（21:01 原样 + 新门禁 `test_telegram_negative_peer_dispatch_is_skip_public_chat`：无 chat_type 仅负 chat_key 的 TG 会话 immediate / followup 两档 dispatch 都回 `skip_public_chat`，私聊只被空文本挡） | `test_adult_grader_gate.py` **37 passed**；`bug_intake.py` / `test_bug_intake.py` 留工作树给其归属线 |
| A GuardContext | #303 | （本行 commit 见下 A+B+C 合一） | **新** `src/inbox/guard_context.py`：`GuardContext{is_group, mode, sender_kind, lang, chat_id, …}` + `evaluable / in_review_mode / auto_outbound_allowed / skip_reason()`；`build(conv, automation_mode, lang, cfg, store, sender_id, direction)`——群判定 = `is_group_conversation` ∪ `bug_intake.is_bug_group` ∪ `peer_bot_guard.never_auto_reply_reason(ops_group)`；发送方 = out→self / ==account_id→self / 同事名单→agent / 服务 bot 或 chat_type=bot 或 peer_is_bot→peer_bot / 其余 customer；档位缺省按 `resolve_automation_mode`（解析不了 → review fail-closed）。`drafts.auto_generate_draft` 在 `quick_analyze` 后构造一次传三处：`keyword_risk_hits(text, direction, ctx)`（skip → `(None, [])`，日志 `[guard-ctx] skip= stage=keyword`）/ `risk_grader.regrade_inbound(ctx=)`（skip → 原判定 + `info.skipped`）/ `adult_grader.regrade_inbound(ctx=)`（同）。`ingest.conv_dict` 透传 `sender_id`（`lm.sender_id` / `source.sender_id`） | `test_guard_context_gate.py` ①③ |
| B 单一闸门 | #303 | 同上 | `autosend_policy.decide(kind="soft_reply", ctx=)` → `_decide_soft_reply`：ctx.skip → L0 `scene:<why>`；冻结 → L0；mode≠auto_ai → `mode_level` + hold `mode:<m>` review_required；他因持有（非 `SOFT_REPLY_OWN_HOLDS=("adult",)`）→ L1 `risk_hold:<r>`；否则 L2。`autosend_worker`：`deliver_human_approved` 抽成 `_deliver_now(item, stage="human")`（**人工通过行为逐字不变**：计数 / 影子 `ai_reply_delivered_human` / 日志 / `_publish_deliver_failed`）；新 `deliver_soft_reply(row)`（stage=soft_reply）：`guard_context.from_row` → decide → `_human_priority_gate`（只豁免自家 adult 持有；agent_sent / agent_typing 对软回应是**放弃**不是 defer——不动 Q-18）→ `_inflight_register` → 生成（`row.final_text` 空时）→ 再过闸 → `_deliver_now(stage="soft_reply")` → `adult_grader.record_sent`；计数 `total_soft_reply_{delivered,errors,aborted}` 进 snapshot；abort 走 `_ledger_abort(stage="soft_reply")`。`adult_grader`：`SOFT_ALT_PREFIX="adult_soft_alt:"`（manual/review/multi_choice 档 → reasons 追加、`soft_reply_status=review_candidate`、不排队）；`dispatch_soft_reply` 改排 `svc._soft_reply_cb` 行 `{kind:soft_reply, peer_text, lang, level, policy, mode, tag_ts, automation_mode, guard_ctx, persona_id}`（状态 `scheduled / skip_public_chat / no_soft_reply_cb / no_loop / empty`）；`send_soft_reply` 不再挑句、`text` 恒空；账本改由 `record_sent` 在投递成功后写；`_deliver_cb` 删除。`drafts.set_inbox_deliver_callback(cb, stale_approve_hours, soft_reply_cb=)`；`web_app.py` 两处（自动 worker / deliver_only worker）传 `app=web_app` + `soft_reply_cb=<worker>.deliver_soft_reply`。`test_adult_grader_gate.py` 桩改 `_soft_reply_cb`（模拟闸门成功并调 `record_sent`），新增「只接人工通过回调 → `no_soft_reply_cb`」断言 | `test_adult_grader_gate.py` 37 passed；`test_human_deliver_independent_of_autosend` 等 autosend 7 套 126 passed；`test_guard_context_gate.py` ②（worker 真闸：review 行 `mode_changed`、显式切档 `mode_changed`、群行 `scene:*`、send 零调用） |
| C 短生成 | #303 | 同上 | `persona_reply.generate_soft_deflect(app, platform, chat_key, account_id, conversation_id, target_lang, peer_text, persona_id, level)`：`resolve_effective_persona` → `generate_reply_with_intent(intent="adult_soft_deflect", ctx.account_persona_id)`，directive = 人设口吻 / 只一句 / 轻轻带开 / 不迎合不说教不道歉不引规则 / 必须目标语言；后检 `_soft_deflect_first_sentence`（多句取首句、>120 字或空 → 失败）；**无 ai.chat 兜底、无固定句**。worker `_soft_reply_generate`：`resolve_outbound_lang` → 回落 row/ctx lang → 空 = `lang_unknown` 不发；`app is None` → `no_app` 不发；日志 `[adult] soft_reply conv= level= policy= mode= lang= gen=persona|given|skip status=` | `test_guard_context_gate.py` ②（gen 失败 → `gen_skip` 零发送零账本；gen 成功 → 发的是生成文本且 ∉ 固定句库） |

## 1.1 回访

（待收工统一写：#303 回访口径「群 / 人审档不再自动出站；随 1.0.83 报」。）

## 2. 收工核对

（待收工。）
