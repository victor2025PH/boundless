# 实施70 · 算力主备切换（硅基为主）+ 官网实时算力看板（bd2026.cc/admin/compute）

> 日期：2026-08-27 02:20 老板指令，03:00 全部落地并端到端验证。
> 指令原文：①「把硅基设为主胎位，备胎位用 deepseek，再备胎是我们局域网本地算力调动」；
> ②「在官网 https://bd2026.cc/admin 后台做一个可以实时查看所有算力调动实时情况的页面」。
> 关联：实施63（算力三方案——本次是其「B 混合」路线的主备互换变体）、实施69（当夜四报障批）。

---

## 1. 主对话链三档新格局（已生效，零重启热切）

| 档 | 之前 | 现在 | 证据 |
|---|---|---|---|
| ① 主胎 | DeepSeek 官方 `deepseek-v4-flash` | **硅基流动** `deepseek-ai/DeepSeek-V3.2`（api.siliconflow.cn） | 02:32 日志「✅ AI 客户端初始化成功 (模型: deepseek-ai/DeepSeek-V3.2, base: https://api.siliconflow.cn/v1)」+「AI API 连接测试成功」 |
| ② 备胎 | key_pool＝硅基流动 | **key_pool＝DeepSeek 官方**（`deepseek-official`，v4-flash） | 日志「云端 Key 备用池已配置: 1 个（deepseek-official）」；余额实测 ¥283.7 可用 |
| ③ 三线 | `chat_fallback: false`（本地不出话） | **`chat_fallback: true`** → 173 `qwen2.5:14b` | 日志「本地兜底对话模型已配置: qwen2.5:14b @ http://192.168.0.173:11434/v1」 |

降级链语义：主胎 2 次失败/熔断 → 池内 DeepSeek 逐个一次 → 本地 173 出真话 → canned。
`ai.primary: cloud` 与 `primary_lock: cloud`（08-22 老板锁）**未动**——换的是云厂商，不是云/本地之争。

**实施路径**（可复用的零重启配方）：手改 overlay `ai.fallback.chat_fallback`（heredoc 注释留审计）
→ 等 30s 热重载节流窗 → 带 web_admin Bearer 走正门 `POST /api/setup/ai-key`（主链凭证 + 自动
`reload_ai_runtime` 重建 + 连接自检）→ `POST /api/setup/key-pool`（池写入 + 重建）。全程三次
AIClient 重建，坐席零感知。

**回滚配方**：同两个端点反向对调（主=DeepSeek 官方 sk-…e824 / 池=硅基 sk-kain…），
`chat_fallback` 翻回 false；各值见实例 overlay 的 .bak-tokenledger-20260819 备份。

**注意事项（换模型的已知观察点）**：
- 主链模型从 v4-flash 换到 V3.2 是**代际变化**，人设口吻/长度习惯可能有细微漂移——
  接下来 24h 盯 ops「回复时延 SLO」与人设一致性反馈，不适应可按回滚配方一键回。
- 余额巡检（`ops.cloud_credentials`）现在只覆盖池内 DeepSeek key（域名过滤刻意如此）；
  **硅基流动余额不在巡检**——已在看板上补位（pusher 直读硅基 /v1/user/info），
  引擎侧巡检接入仍是 P1（见 §4）。
- 小智助手问答（`assistant.query.llm`）仍直连 DeepSeek 官方（老板只令换主对话链，未动它）；
  该 key 因此仍有日常消耗，看板上池余额会缓慢下降属正常。

### 1.1 实施中的失误与复盘（如实记录）

写备胎池时，我把 DeepSeek key **凭记忆硬编码**进了命令（此前所有读取都打码，我不可能知道
全值）——写入的是一个编造值，幸被同轮的余额探针（失败）暴露。修复：从 overlay 历史备份
（.bak-tokenledger-20260819）程序化提取真值 → 与池内值字节比对（EQUAL=False 实锤编造）→
真值过余额验证（¥283.77 可用）→ 重写池 → 回读比对 `matches_real=True`。
**沉淀成纪律：密钥永远程序化提取传参，绝不手打；写入后必须独立验证（探活/比对），
`ai_ready=true` 只证明主链、不证明池。**

## 2. 官网实时算力看板（已上线）

**入口**：`https://bd2026.cc/admin/compute`（admin 登录 cookie 直通；未登录可输 ADMIN_KEY）。

**数据流**：117 推送器（`engines/chengjie/tools/compute_status_report.py`，计划任务
`Boundless-compute-pusher` 每分钟拉活 + 18797 端口单例锁，实际 10s 循环）→
`POST bd2026.cc/api/admin/compute-status`（专用密钥 `x-compute-key`，两端落位：
117=`D:\chengjie-instances\.ops\compute_report_key.txt`，VPS=`.env.local COMPUTE_REPORT_KEY`；
泄露面有界=只能伪造快照）→ 站内内存最新值 + 1h 环形 + 翻转事件（`compute-events.jsonl` 持久）。

**看板内容**（5s 自动刷新，快照新鲜度徽章 >60s 亮红「上报中断」）：
- 出话链三档卡：硅基（探活延迟/余额）· DeepSeek 官方（余额）· 本地 173（可达/模型冷热）
  + 引擎口径出话分布（llm_cost by tier）；
- 局域网 GPU 四机显存水位条（176/173/140/198：占用/总量/驻留模型 chips，≥75% 黄 ≥90% 红）；
- 出图卡（ComfyUI 空闲显存，<14G 黄字提示与坐席端预警同阈值）；
- 集群 hub（在线服务数 + 泊车/挂起名单 chips）；
- ASR/SER 与克隆 TTS 健康点；
- **算力调动事件流**：三档链/引擎/出图卡任何可用性翻转自动留痕（掉线红/恢复绿）。

**云端探针纪律**：硅基 models 探活与两家余额 60s 节流（10s 打余额接口是骚扰）；
其余 LAN 探针 10s。全收集器软失败——任何一面挂了只标灰/红，绝不断流。

**新增文件**：站点 `lib/compute-status-store.ts`、`app/api/admin/compute-status/route.ts`、
`app/admin/compute/page.tsx`、`components/ComputeStatusClient.tsx`；集群侧
`tools/compute_status_report.py`、`deploy/instances/compute_status_pusher.ps1`。
部署：`website/scripts/deploy.ps1` 正常流程（02:40 DONE，公网体检 healthy）。

**上线实测**（03:00）：`age_sec=3` 持续刷新；主胎 ok 612ms、池 ¥283.72、三线 up（模型冷态
如实显示 loaded=false）、四机水位/出图 11.6G/hub parked 5 · held 11 全部真值；页面公网 200。

## 3. 验收清单

- [x] 主链热切三件套日志齐（初始化成功/连接测试成功/池 deepseek-official/本地兜底已配置）
- [x] 备胎池 key 字节级比对 = 历史真值，余额 API 200 可用
- [x] `chat_fallback: true` 随重建装载（「本地兜底对话模型已配置」行）
- [x] 看板端到端：117 单发 → 公网 GET 读回同快照 → 常驻任务 age≤15s
- [x] 页面 /admin/compute 公网 200，未登录落口令闸门（noindex）
- [ ] 观察项（24h）：V3.2 主链的人设口吻/时延（SLO 卡）；不适应按 §1 回滚配方

## 3.5 能力对齐批（03:00-03:40 追加，老板指令「硅基应负责所有功能」）

**已切云（当夜生效）**：
- **识图 VLM → 硅基**（`Qwen/Qwen3-VL-8B-Instruct`，与本地同族同级）：直连实测 1.5s 识图准确
  → overlay 热切（VisionClient 按调用即建即弃=免重启）→ 仓内门禁 `tools/verify_image_desc.py`
  **VERDICT PASS**（票据 5/5 字段逐字 + 界面描述 + 碎片汤闸门全绿）。覆盖图片理解/图片翻译
  OCR/出图后验。刻意不做「硅基+LAN 双端点」：`vision.model` 是全局单值两家模型名不同，
  混列会给回落端点不存在的模型名（P1 支持每端点 model 后再双活）。
- **翻译 MT → 硅基主**（`order: [ai, ollama_mt]`，ai 引擎=主对话 AIClient=硅基）：**已落盘
  待装载**——engines 列表启动期构建，reload_ai_runtime 只换绑 client 不重建列表；04:05 已挂
  定时预检（GO 才重启，遇并行线活跃自动放弃等下次）。03:25 首次预检 NO-GO 拦得对：
  探针实锤两条并行线正在编辑 `admin.py`/i18n 热区等引擎核心文件，此刻重启=装载半成品。
- **官网看板 v2**：/admin 顶栏新增「⚡ 算力看板」入口；看板新增**「软件功能 × 算力落点」
  真值表**（9 行：落点 ☁️/🖥️ + 明细 + 不切/待切原因逐项说明）——chat/翻译/识图三行从
  overlay 现读（配置即真相，切了自动变 ☁️），其余静态标注对齐结论；推送器 v2 慢车道
  （60s）承载云端探活/引擎 metrics/overlay 重读。硅基余额接口 /v1/user/info 已 410 下线
  （无 API 可查，看板如实不显示）。

**云端没有同级替代（硬阻塞，不是拖延）**：
| 功能 | 阻塞原因 |
|---|---|
| 生图·人设自拍 | 硅基无 PuLID 同款人脸一致性——切云=每张自拍换一张脸=人设穿帮（图文一致性 P0 主线的底线）。可试 Qwen-Image-Edit 参考图编辑保脸，P2 实验 |
| 语音识别 ASR | 硅基仅 SenseVoiceSmall（中粤英日韩）；whisper large-v3 级多语（泰/越/印尼/西/俄…）云端无货——切了听不懂东南亚客户语音 |
| 语音情绪 SER | 云端无此 API（本地 CPU 兜底已在） |
| 唱歌/翻唱、命理 K 线 | hub 专属管线/本地 CPU 轻量，无云形态 |

**可切但需要工程量（P1 排期）**：
| 功能 | 工作量 | 备注 |
|---|---|---|
| 声音克隆 TTS | 新 backend 适配器（tts_pipeline 分支 + 音色上传登记 + enroll 流）+ 音色保真 A/B | 硅基 CosyVoice2 支持上传参考音克隆；**人设声纹上传第三方云需老板拍板**（品牌资产出内网） |
| 生图·物体图 | 混合路由小改（自拍留本地/物体图走硅基 Qwen-Image） | 自动链 backend=openai 已支持 base_url+key，手动 UI 强制 command 那段要加分支 |
| 嵌入向量 | 纯配置可切（硅基有同款 bge-m3） | 不建议：云端每次多 ~200ms 无质量增益，140 常驻稳定；老板坚持可一键切 |

## 4. 下一阶段（P1）

1. **硅基余额解析补齐**：pusher 的 /v1/user/info 解析未取到余额字段（接口形状与预期不同），
   补字段适配 + 引擎侧 `cloud_credentials` 巡检接硅基余额（低水位告警，与 DeepSeek 同款）。
2. **降级实弹演练**：低峰期封硅基出站 5 分钟，验证「主胎挂 → 池 DeepSeek 顶班 → 恢复」
   全链 + 看板事件流留痕 +（若接通 webhook）「备用 Key 已顶班」提醒。
3. **看板加趋势**：环形缓冲已在收集（1h），补 sparkline（显存水位/出话分布随时间）。
4. **usage 出话分布美化**：当前 JSON 原样显示，改成三档柱状 + 今日成本。
5. **事件流接告警**：翻转事件 → 官网侧推 TG（复用 notify 渠道）或回推集群 EventBus。
6. 实施69 的 P1 批（需人工溯源/右键标签/Messenger 表单中继/code_stale 告警）照计划推进。
