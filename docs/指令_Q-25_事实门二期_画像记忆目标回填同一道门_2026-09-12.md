# 指令 Q-25：事实门二期——画像 / 记忆 / 目标回填共用一道 FactGate，「AI 问出来的答案」不回填，确认按钮必有反应

> 预算 **$120**。工单 **#295（89MDQA / C9F7TG）/ #300（记忆「越南出差 → 一起去」）/ #272 追加（Y39D8U，1.0.82 上复现）**。分析 `docs/分析_1.0.81首日问题归类与工作台三视角方案_2026-09-12.md` §二 R2、§四 P0-3。落点表 `docs/发版对账_v1.0.83_Q25.md`。
> 前置：Q-19 已收工随 1.0.82（`slot_validate` / `anchor_reason` / `lang_mismatch_reason` / `run_extraction` 不送 reply）。本线在其上把门抽成共享件并补两条链。

## 事实（证据 `tmp_diag\{Y39D8U,C9F7TG}`、#300 手贴；代码只读核对）

- **Y39D8U（1.0.82，20:58）**：AI 自己问「inspection work 做多久」，客户只答「couple months」→ 右侧职业写成「AI work」`source=ai_inferred`。客户从未说过 AI / 巡检。说明 Q-19 的锚定放行了它——要核：① 锚定是否用了 token 部分匹配（`work` 在客户历史里出现过）；② 这条是不是走目标引擎 `verify_pending_probe → covered` 的回填路（不经 `profile_fill.apply` 的门）；③ 抽取输入是否仍含 AI 的问句。
- **C9F7TG（#295）**：「收入水平 已提取未确认」行只有状态无值，点勾**全程无 confirm 请求进后端**，同分钟职业 / 地点确认成功 → 前端对空值 `return`（死点击）；17:27 renderer `loadThread Failed to fetch`。
- **#300**：记忆把「客户越南出差」写成「一起去越南」——事实主体被改写；episodic 链没有 Q-19 那道门。
- `profile_fill.apply()` 已有门；episodic `add_fact` 与目标引擎回填各自落库，三处三套规则。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **A FactGate 共享件** | 新 `src/companion/fact_gate.py`：`check(value, *, slot_or_kind, evidence, inbound_texts, customer_lang) -> (ok, reason)`——类型 / 长度尺子（沿用 Q-19 `slot_validate`）+ **原句锚定升级为「值的核心 token 必须出现在 evidence 且 evidence 逐字出现在最近 30 条客户入站」**（禁 token 部分匹配、禁跨句拼接）+ 语种守卫 + 来源守卫（evidence 来自 `direction=in`）；`profile_fill.apply` 改调它（行为等价，测试全过） | 新文件、`profile_fill.py` |
| **B 「AI 问 → 客户答」不回填** | 目标引擎 `verify_pending_probe` 判 `covered` 时**只把客户原句作为 evidence** 送 FactGate；客户只答时长 / 是 / 好 / 数字（「couple months」「yes」「3」）→ 不回填工种 / 地点等实体槽，只把槽标 `mentioned`；AI 问句里的实体词（inspection / AI）**永不**成为候选值；复验 Y39D8U：同序列回放 → 职业不写 | `goals/service.py`（Q-1 收工，只动回填一处）、`profile_slots.py` |
| **C 记忆链同门** | episodic `add_fact` 前过 FactGate（kind=fact：必须附客户原句、主体守卫——事实主语只能是客户或客户明确提到的人，「我出差去越南」不得写成「一起去越南」）；无原句不写；`[memory] drop reason=` 日志；50 条连续 0 写入告警沿用 | episodic 抽取模块、`skill_manager.py` 抽取调用一处 |
| **D #295 确认按钮** | cp-goal：`mentioned` 无具体值 → 不显示「已提取未确认」（显示「AI 有线索，待补值」且不可确认）；有值 → 确认按钮**必发请求**（`POST /api/goals/profile/confirm`）+ 成功 toast + 状态变 confirmed，失败红字；空值 `return` 改为按钮禁用 + title 说明；`loadThread Failed to fetch` 补重试与提示 | `cp-goal.js`（双树）、`goal_routes.py` 只读字段 |
| **E 门禁 + 清洗二期** | `tests/test_fact_gate_q25.py`：「couple months → AI work」被拦；「我出差去越南」→ 主体守卫拦「一起去」；token 部分匹配（`work`）不放行；#295 空值不可确认 / 有值确认必请求；`tools/profile_purge_unanchored.py` 扩记忆（`--memory --dry-run`） | tests、tools |

## 红线

- `confirmed` 值一字不动；只处理自动来源。
- 不动 `decide_probe_target`（Q-1 决策）与 `persona_reply.py`。
- 判不准就不写：三处链的默认都是「丢弃并落日志」。

## 开新对话粘贴

```
读 docs/指令_Q-25_事实门二期_画像记忆目标回填同一道门_2026-09-12.md，按它做 Q-25（#295 #300 #272追加 Y39D8U：新 fact_gate.check 共享件（类型尺子 + 值核心 token 必在 evidence 且 evidence 逐字在最近 30 条客户入站、禁部分匹配、语种与来源守卫），profile_fill.apply 改调它 / goals verify_pending_probe covered 只送客户原句，客户只答时长·是·数字不回填实体槽，AI 问句实体永不成候选，回放 Y39D8U 职业不写 / episodic add_fact 前过 FactGate + 主体守卫（我出差去越南 ≠ 一起去）无原句不写 / cp-goal 无值不显示已提取、有值确认必发请求 + toast，空值改禁用，loadThread 失败重试 / 门禁 + 清洗脚本扩记忆）。预算 $120。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与 docs/发版对账_v1.0.82_Q19.md（在其上抽门，不重做），再 scripts\agent_probe.ps1 -Intent 登记；goals/service.py 只动回填一处，不动 decide_probe_target；confirmed 一字不动。每个 commit 带 #295 并立刻补落点表 docs/发版对账_v1.0.83_Q25.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-25 行并 agent_probe -Done。
```
