# 指令 Q-38：画像 `name` 禁写人设自称 + 摸底进度卡确认钮不被裁（#272 追加 HPS7C3 / E2GXEP）

> 预算 **$60**。工单 **#272 追加**（未立新单：01:21–01:23 两包按「同报障人同实体」自动挂 #272）。skuio **1.0.84**，WhatsApp 账号1 Mizuki/12084403608 × G M S Jeo new/917983336714。落点表 `docs/发版对账_v1.0.85_Q38.md`。
> 前置：Q-19（槽校验 / 原话锚定）+ Q-25（FactGate 三链同门）+ Q-20（出站称呼方向）**都已收工**——本条不重做，只补它们没挡住的洞。

## 事实（证据 `tmp_diag\{HPS7C3,E2GXEP}`；HEAD 代码只读核对）

- **E2GXEP / HPS7C3① 称呼 = Mizuki（AI 推断）**。Mizuki 是本号人设名；对方是 G M S Jeo，一直在**叫我们** Mizuki，不是自称。要求：称呼 / 姓名槽只取客户自称或他人对客户的叫法；出现在我方人设名单里的词必须排除；AI 推断的称呼不得直接展示为已填。
- Q-19 `slot_validate('name')` 只验「≤12 字短语、非整句」——`Mizuki` 过。Q-25 FactGate 要值的 token 在**同一条客户入站**里——客户说「Hi Mizuki」时 `Mizuki` 在 evidence 里，门放行。Q-20 管的是**出站**（空值不沿用人设爱称 / 不得当面否认 Alicia），**不管画像写槽**。
- 抽取提示词**已经写了**（`profile_fill.build_merged_prompt`）：「『hi X』是客户在称呼助手，不是客户名字；只有『我是 X / my name is X / call me X』才算。绝不抽助手自己的名字」。LLM 仍写了 → 必须有**硬闸**，不能再靠提示词。
- 人设自称名单已经在库：`persona_guard.build_self_name_allowlist`（`persona.name` + `names.full_western/english/german/french/nickname` + 拼音变体）。会话级「对方怎么叫我」= `peer_calls_you`（Q-20）也是**我方**在客户那边的名字，同样不许进客户 `name` 槽。`skill_manager._forbidden_self_names` 是角色标签（线上陪伴 / 客服），不是本洞。
- **HPS7C3② 摸底进度 3/10 · 待确认 5**：卡片条目超出高度，收入水平 / 当前居住地 / 个人简介三行与「现在就问一个」按钮重叠，右侧「确认」被裁掉点不到。根因两处都在 `cp-goal.js`（双树）：① Q-19 D `.gl-slotchk { max-width:100%; overflow:hidden; white-space:nowrap }` 为防长文本撑爆，把行内 ✓ / ✎ / ✕ **一并裁掉**；② `.gl-slots-row` 只 `flex-wrap` 不限高，`.cp-card { overflow:hidden }` 把探问钮和末三行叠在一起。Q-32 美化线待拍板，本条只修「能点、不重叠」，不改颜色 / 字号令牌。

## 要做的三段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 人设名硬闸** | 新薄函数 `reserved_self_names(persona, *, peer_calls_you="")` = `build_self_name_allowlist(persona)` ∪ 会话 `peer_calls_you`（归一：NFKC + casefold + 去空白）。`fact_gate.check` 对 `slot=name` 加两刀（放在类型尺子之后、锚定之前，拿不准就丢）：① 值 ∈ reserved → `own_name`；② evidence 只是呼格（`hi/hey/hello/你好/嗨/哈喽 X`、或整句就是 X）且 X ∈ reserved → `vocative`。`profile_fill.apply` / `nickname_prefill` / LLM 回填三处给 `check` 传 reserved（人设解析沿用 `resolve_effective_persona` + Q-20 `contact_names`，缺人设 = 空集不误伤）。日志 `[profile] drop slot=name reason=own_name\|vocative value=`。启发式 `_NAME_RE`（`profile_slots` L1648）抽到的值同样过这道门，不另开写口 | `persona_guard.py`（只加收集函数或复用 allowlist，不改出站守卫）、`fact_gate.py`、`profile_fill.py`（传 reserved，不改 Q-19/Q-25 既有判据） |
| **B 进度卡确认钮可见** | `.gl-slotchk` 改 `overflow:visible`；值文本单独 `.gl-slot-val` 做 ellipsis（`_clipVal` 已在）；✓ / ✎ / ✕ `flex-shrink:0` 永不裁。`.gl-slots-row` `max-height`（约 7.5rem / 5 行）+ `overflow-y:auto`；`gl-probe-row` 保持行外兄弟，探问钮钉在滚动区下方，与收入 / 居住地 / 简介零重叠。双树同步（`shared/copilot` + `desktop/renderer/shared/copilot`）。零新颜色、零改 `--cp-*` 令牌 | `cp-goal.js` 双树（只动 `_renderSlotsProgress` 的 chip 结构 + 同文件 `<style>` 四条） |
| **C 门禁 + 回放 + 清洗** | `tests/test_profile_own_name_q38.py`：① 人设 Mizuki + 入站「Hi Mizuki」→ `name` 不写 `own_name`/`vocative`；② 「My name is Jeo / 我叫 Jeo」→ `name=Jeo` 过；③ `peer_calls_you=Alicia` + 「hey Alicia」→ 不写 Alicia；④ 无 persona → 不误伤普通名；⑤ E2GXEP 回放（会话 whatsapp:12084403608:917983336714，人设 Mizuki）。Q-19 41 + Q-25 40 **零改动**。`tools/profile_purge_unanchored.py` 加 `--own-name`（dry-run 列出 `ai_inferred/nickname` 且值 ∈ 该会话人设 reserved 的 `name` 槽，`--apply` 才清，默认 dry-run）。UI：沿用 `test_goal_ui_revamp` / `test_fact_gate_q25` 的 node 抽源方式钉「mention 行含 slot_confirm 且 CSS 不含 `.gl-slotchk{overflow:hidden}`」+ 「`.gl-slots-row` 有 max-height」 | tests、tools |

## 红线

- `confirmed`（坐席确认 / 用户手填）一字不动；清洗 `--apply` 只动自动来源，交回访附用法，本线默认不 apply。
- 不改 `decide_probe_target`（Q-1）、不改 `persona_reply.py`、不改 Q-20 出站称呼方向。
- FactGate 对非 `name` 槽零新判据；Q-19 / Q-25 既有 81 例必须绿。
- 不进 Q-32：不改字号 / 间距 / 圆角 / hex / emoji。
- `.py` 须 zhiliao 重启才生效；本线**不许自己重启**（发版线统一装载）。`cp-goal.js` 热更，戳沿用既有 pin 规则（只加本线 hunk）。

## 开新对话粘贴

```
读 docs/指令_Q-38_画像禁写人设名与摸底进度卡确认钮不被裁_2026-09-13.md，按它做 Q-38（#272 追加 HPS7C3 / E2GXEP，skuio 1.0.84：WhatsApp Mizuki/12084403608 × Jeo/917983336714。A 画像 name 槽禁写人设自称——复用 persona_guard.build_self_name_allowlist ∪ 会话 peer_calls_you 作 reserved_self_names，fact_gate.check 对 slot=name 加 own_name（值∈reserved）与 vocative（evidence 只是 hi/你好 X 且 X∈reserved），profile_fill.apply / nickname_prefill / LLM 回填都传 reserved，启发式 _NAME_RE 也过这道门，日志 [profile] drop reason=own_name|vocative；提示词已写「hi X 不是客户名字」不要再加一段。B 摸底进度卡：.gl-slotchk 去掉 overflow:hidden，值进 .gl-slot-val ellipsis，确认/改/否钮 flex-shrink:0；.gl-slots-row max-height+overflow-y:auto，现在就问一个钉在滚动区外不与收入/居住地/简介重叠；cp-goal.js 双树只动 _renderSlotsProgress + 同文件 style，零新颜色。C tests/test_profile_own_name_q38.py 五例含 E2GXEP 回放 + Q-19 41 / Q-25 40 零改动 + profile_purge_unanchored.py --own-name 默认 dry-run + node 抽源钉确认钮可见）。预算 $60。先读 docs/发版对账_v1.0.83_Q25.md 与 fact_gate.py / profile_fill.build_merged_prompt L559–567 / cp-goal.js L760 与 L1919–2014，再 scripts\agent_probe.ps1 -Intent 登记；confirmed 不动；不改 decide_probe_target / persona_reply / Q-20 出站方向；不自己重启 zhiliao。每个 commit 带 #272 并立刻补落点表 docs/发版对账_v1.0.85_Q38.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-38 行并 agent_probe -Done。
```
