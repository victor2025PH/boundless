# 修复指令 K 批（报障群未修复收口）—— 并行公共底座

> **本文件只是「索引 + 去向表 + 文件归属 + 老板决策」的公共底座，不要拿本文件开对话。**
> 每条指令各自成文，**一条指令 = 一个新对话**。开对话粘贴话术在各指令文件末尾，也汇总在 `docs/K批_进度总账.md`。
>
> 依据：2026-09-05 18:3x 对 `D:\chengjie-instances\zhiliao\data\config\bug_intake.db` 全量盘点（193 单：fixed 97 / closed 43 / verified 4 / confirmed 25 / new 24）× git 提交交叉核对 × I/J 批总账复核。
> **结论：49 张未关闭单里，只有 6 张没有任何代码动作**（全在人设工作室，J-7 指令现成）。其余要么代码已在 HEAD 但**没装载、没发版、没标台账**（35 张），要么**改了没提交**（I-5 收口 / D1b 收尾，裸奔 10–28 小时）。
> 所以本批五条里只有 K-2 / K-3 是「写新代码」；K-1 是「抢救在途改动」；K-4 是「复核返工单」；K-5 是「装载 → 发版 → 台账 → 回访」。**用户能感知到的修复，全部取决于 K-5 走完**——服务端 zhiliao 末次重启 09-04 19:38，两台内测机仍是 1.0.73。

| 指令 | 预算 | 类型 | 内容 | 文件 |
|---|---|---|---|---|
| **K-1** | $120 | 在途改动收口（零新功能，只提交树上已有 hunk） | I-5 收口五件（#145③ 选品人设同源 / #146 族撤回引用出站接线 / #32 悬浮清单删除 / guard-check 影子 / #155 #156 #159 三项 UI）+ D1b 收尾（#166 停摆看门狗 liveness）+ 对话 B `phantom_unread_alert`（#120/#159 族，修 `test_alert_delivery_e2e` 红）+ 孤儿线盘点 | `docs/指令_K-1_在途收口_2026-09-05.md` |
| **K-2** | $150 | 后台页 UX + 前端报错（**唯一一批零代码的单**） | #191 #193 #189 #190 #192 #186 | **直接用 `docs/指令_J-7_人设工作室体验_2026-09-05.md`**（开工条件已满足：J-4 已释放 `personas.html` / `persona_routes.py`，#178/#179 已由 J-4 做掉） |
| **K-3** | $150 | 工作台交办小件（跨线欠账，各线落点表「交 J-4」里没人接的） | WA DNS 提示坐席可见（J-6→J-4）/ 草稿旁记忆 chips + 「来源原话→定位消息」（J-10→J-4）/ ops 卡「已发假声明」行（#171）/ LINE 入站上限 20→32MB（J-3 C）/ `config.example.yaml` 三处文档键 / 语音入站合并窗（J-2 顺手，可选） | `docs/指令_K-3_工作台交办小件_2026-09-05.md` |
| **K-4** | $100 | 返工单与复合单复核（代码只读 + 每单一条回归钉 + 二次结论） | #61 #63 #64 #67（修过被打回）/ #145（5 件复合）/ #36（前半已交付）/ #170（J-4 判非缺陷） | `docs/指令_K-4_返工单复核_2026-09-05.md` |
| **K-5** | $120 | 值守：装载 → 全量回归 → 发版 1.0.74 → 台账写入 → 61 单回访 → 真机验收 | 非代码；I-6 F1② / F3 的继续 | `docs/指令_K-5_值守装载发版回访_2026-09-05.md` |

**投放顺序**：**K-1 先开**（它要提交的 hunk 就在 K-3 / K-5 要碰的文件里，先落地再动）→ K-2 / K-3 / K-4 互不重叠，可同时开 → K-5 在 K-1 提交后即可做第一段（装载），发版段等 K-2 / K-3 收工。
**跨对话 / 跨账号交接唯一入口**：`docs/K批_进度总账.md`。
**背景**：`docs/J批_进度总账.md`、`docs/I批_进度总账.md`、`docs/报障对账_钧_花无缺_20260905.md`、`docs/指令_I-6_值守流程与积压_2026-09-04.md`（§3 回访文案口径 / §4 台账校正）。

## 49 张未关闭单的去向（谁负责让它变 fixed）

| 去向 | 单 | 说明 |
|---|---|---|
| 代码已在 HEAD，等 K-5 装载 + 1.0.74 装机后标 fixed | #37 #38 #40 #65 #152 #153 #154 #155 #156 #158 #159 #160 #161 #162 #164 #166 #167 #168 #169 #170 #171 #172 #173 #174 #175 #176 #177 #178 #179 #180 #181 #182 #183 #184 #185 #188（35 张） | I 批（09-04）与 J 批（09-05）提交。#153 = #152 F2 同件（`e99a61c7`）；#181 是观测项（J-6 A/B/C 已落，真机待核）；#170 见 K-D5 |
| 代码改了**没提交** → K-1 | #32（悬浮清单 E5①）/ #145③ / #155 #156 #159 的 UI 半边 / #166 的 liveness 半边 | K-1 提交后并入上一行 |
| **零代码** → K-2 | #186 #189 #190 #191 #192 #193 | J-7 指令现成，未开工 |
| 复核后定性 → K-4 → K-5 回访 | #61 #63 #64 #67 #145 #36 | #63 / #67 的「验证未过」是同一句「LINE还是不能收发图片语音视频」被旧 verify 逻辑误归属（那是 #101 族）；#61 / #64 的「还是不行」按钮点在修复发版之前 |

## 文件归属矩阵（共享工作树；开工先 `scripts\agent_probe.ps1` 看 ACTIVE 再动）

| 文件 / 目录 | 归属 |
|---|---|
| 在途未提交 hunk 所在文件：`unified_inbox.html`（I-5 段 + `cp-goal.js?v=` 行）、`unified-inbox.css`、`unified_inbox_desktop_routes.py`、`goals/service.py`、`goals/sprint_ticker.py`、新 `goals/liveness.py`、`goal_routes.py`、`withdrawn_cite.py`、`drafts.py`（L1229 一处）、`skill_manager.py`（L5017 一处）、`health_watchdog.py`、`webhook_notifier.py`、`cp-goal.js` 双树、`shared/copilot/app.html` 双树、`i18n_packs/goals.py` / `inbox_workspace.py`、`config.example.yaml`（goals 段）、新 `tools/goal_sprint_drill.py`、对应 tests | **K-1**（只提交不改逻辑；提交完即释放） |
| `personas.html`、`persona_routes.py`、`persona_media_routes.py`、用户管理页模板、`admin.py`（只动 voice-eval 路由 + 会话回收两处）、`i18n_packs/persona*.py` | **K-2**（= J-7） |
| `unified_inbox.html`（**K-1 收工后**）、`src/integrations/whatsapp_baileys_login.py`、`ops_overview.html` + `i18n_packs/ops_overview_page.py`、`src/integrations/line_media.py`（一行常量）、`config/config.example.yaml`（**K-1 收工后**）、`src/inbox/inbound_debounce.py` | **K-3** |
| 只读全部；可写：新建 `tests/test_rework_*.py`、`docs/**` | **K-4** |
| `deploy/**`、`deploy/instances/restart_instance.ps1`、`engines/chengjie/scripts/restart_preflight.ps1`、`tools/duty_*`、`bug_intake.db`（只经 `/api/admin/bug-intake/*` 端点）、版本号 / changelog / 官网五值、`deploy/desktop/push_chatx.ps1` | **K-5** |
| `skill_manager.py`（**J-10 phase2 ACTIVE**）、`src/utils/memory_promises.py`、`episodic_*`、`memory_*` | **他线，不碰**（K-1 只提交 L5017 `withdrawn_cite` 一个 hunk，且必须等意向板显示 J-10 phase2 静默 ≥10 分钟） |
| `desktop/build/**`、`desktop/package.json`、`installer.nsh`、`brand-assets/**` | **他线（installer brand）不碰**；K-5 改版本号前先看意向板 |
| `src/ops/outbound_policy.py`、`rate_limiter.py`、`account_limiter.py`、`protocol_autoreply_limits.py`、`quota_state.py`、`token_ledger.py`、`companion_send_gate.py`、`tests/test_outbound_policy.py`（「unlimited outbound mode」孤儿线，09-04 13:50 起未提交，意向板已无） | **谁都不碰，等 K-D1** |
| `src/inbox/store.py` | 谁都不改 |

## 老板决策（默认按此执行；不同意的划掉，对应对话按划掉后执行）

| 决策 | 默认 | 影响 |
|---|---|---|
| K-D1 「unlimited outbound mode」孤儿线 8 个文件 | **K-1 只盘点**（diff 规模 / 是否自洽 / 有无测试 / 语法自检）写进落点表，**不提交不回滚**，老板看完再定 | K-1 |
| K-D2 台账标 fixed 的时机 | **两台内测机装上 1.0.74 之后**再标（回访文案「已修复上线」才成立；在 1.0.73 上标 = 再演一次「修了又说没修好」） | K-5 |
| K-D3 61 单回访（I-6 F1②） | **发**，按 `指令_I-6` §3 口径两份汇总（skuio 47 / 钧 13 / 其他 1）；#67 明写「请重传一次相册」 | K-5 |
| K-D4 `drafts.py` 禁改例外 | **允许 K-1 提交**既有的 L1229 `withdrawn_cite` 14 行 hunk（I-5 E4 出站接线的一半，带 `test_withdrawn_cite.py` 新例）；之后仍禁改 | K-1 |
| K-D5 #170 定性 | **fixed**（J-4 A `09bd0294` 做了被埋会话入口紧凑态可见 + 一键取消归档；88MP86 的 7 条 64h 被埋 = 人工归档后无入站，非缺陷） | K-5 |
| K-D6 #145 处置 | **拆**：件① 克隆声工具箱（B 批 `cfe9ad47` #149 + I-5 E2 结论）/ 件③ 人设产品串味（`bdae979e`）/ 件④ 对端删除（`60c2d428`）标 fixed；件② LINE 收发图/表情 → 并 #101 族随真机验收；件⑤ 虚拟未读 → 并 #120/#159/#170 族 | K-4 → K-5 |
| K-D7 D1b 收尾由 K-1 接管 | **接管**（意向板声明已 13h+，文件末次改动 08:12，对话已死；P0-1..5 已 commit，只剩 P0-6 liveness + 收尾） | K-1 |

## 共同纪律 / 省钱纪律 / 断点续传

与 J 批完全相同，见 `docs/修复指令_J批_并行_2026-09-05.md` 对应三节，此处不重复。三条硬红线再钉一次：

- 提交只用 `git add <明确文件>` 或 `python tools/stage_hunks.py --file … --mine … --apply`；⛔ `git add -A` / `-a` / `reset --hard` / `checkout .` / `restore .` / 裸 stash。
- 开工先 `powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "…"`，ACTIVE / HOT-ZONE 文件先避让；收工 `-Done`。
- 每项一个 commit 带工单号；落点表写 `docs/发版对账_v1.0.74_K<N>.md`；总账只改自己那一行。
