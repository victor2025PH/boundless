# 发版对账 v1.0.80 — WX 微信线（实施97 续做：门禁进清单 / 中继运维闭环 / 联调残留清单）

> 指令：`docs/指令_微信线_续做_2026-09-10.md`。底座：`docs/实施97_微信接入智聊_企微客服与个人微信PC副驾_开发方案与实施_2026-09.md`。总账：`docs/Q批_进度总账.md` 末尾「独立线」WX 行。预算 $150。
> 代码基点：`f00c4d00`（96 文件全部入库，开关默认关）+ `183e2322`（两处门禁红 + ui-build 戳）。1.0.79 不含本线；本线随 **1.0.80** 进包。
> 接手方：先 `git -C D:\boundless log --oneline -12` 核对本表 commit 真在。本次只做不依赖真企微账号的 A / B / C 三段；D / E / F 等主人给企微凭证后再开。
> 执行：2026-09-10 20:2x 起。意向板槽位 `wx-line-97-wechat-continue-gate-sweep-list-impor`。

## 0. 开工基线（20:2x，全只读）

| 项 | 事实 |
|---|---|
| 意向板 | 同板在途：tiktok TK-1、qq route-1、ops-group alert noise ×2（其一 pre-restart stale）。git 索引空闲。zhiliao 上次重启 178.9 min 前 |
| 工作树 | ~100 个他线脏文件（成本线 / 告警线 / Q 线 / 官网）；本线要动的 `gate_sweep.ps1` / `prod_edge_watchdog.ps1` / `relay/*` 在 git 里干净 → 显式路径 add；`unified_inbox.html` 是共享脏文件（4 个他线 hunk，工作树比 HEAD **旧** 60 行）→ 只用 `stage_hunks --mine` |
| 生产 | zhiliao 18799 / tongyi 18899 在线 → sweep 自动 BelowNormal + 4 workers；本线代码默认关、已在装载树内，**不重启** |

## 1. 落点

| 段 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|
| **A 门禁进 R 批清单** | `（A commit）` | `scripts/import_sweep.py`（新，替代 `tmp/r79_import_sweep.py` + `tmp/r79_import_names_sweep.py`，tmp 不入库）：①`src/**/*.py` 逐个 `importlib.import_module`（跳 `__main__`）②AST 扫 `from src.X import name` 校 name 真在（`try/except ImportError|Exception|裸 except` 护住的**可选依赖回落**跳过，不算雷——首版把 5 处防环回落 `from src.config_manager import …` 之类报成红，改判后 0 误报）；隔离同 conftest（`AITR_DATA_DIR`→tmp、`HOST_ALERT_SILENT=1`）；任一坏项 exit 1。`scripts/gate_sweep.ps1`：`$gates` 末尾追加 **实施97 块 11 个文件**（kf_client / kf_worker / kf_compliance / kf_window_guard / wechat_pc_core / desktop_bridge_presence / wecom_sso / relay_client / wechat_connect_guide / wechat_e2e_parity / **workspace_template_js_syntax**——后者此前只在 `-Full` 的 `verify_template_js_syntax.py` 跑，现每次 sweep 在场，无 node 自 skip）；红账本之后新增 **`[1b/2] import sweep`** 步（非零折进总 exit code，不进 pytest 红账本）。**顺手修了一个吞清单的老 bug**：文件是 BOM-less UTF-8 且含中文注释，`powershell`(5.1) 按 GBK 解码，中文尾字节吃掉紧随的 `'`，**107 条清单实际只求值出 50 条**（`183e2322` 时 96 条也只跑 ~40）——本次给文件加 UTF-8 BOM（首行 3 字节 diff），PS5.1 求值 107/107；pwsh 7 本来就对 | 第一次 sweep（BOM 前，50 文件）：11 红皆非 wechat_*，`[1b/2] import sweep modules=1225 bad=0 / names checked=3581 bad=0 (guarded skipped=3909) ALL GREEN`；本线 11 文件 136 例 `--collect-only` 齐；第二次 sweep（BOM 后，107 文件）见 §2 |
| **A2 本线 gate 红修复**（107 条清单跑起来后暴露的 3 项本线旧红） | `（A2 commit）` | `unified_inbox.html`（共享，`stage_hunks --mine "kfTransferToHuman\|p==='wechat_kf'\?\|tone==='warn'\?"` 3/7 hunk）：①`window.kfTransferToHuman=kfTransferToHuman`（会话头「转企微人工」按钮内联 onclick，整文件在 IIFE 内未挂 window＝点击 ReferenceError；`test_inbox_inline_handlers_exported` + `test_rpa_inline_handlers_exposed`）②`platIcon` 徽标 `#1b6df5/#4b5563/#fff` → `var(--wx-kf,#1b6df5)` / `var(--wx-badge,#4b5563)` / `var(--wx-on-badge,#fff)` ③`_renderKfSession` 状态点 `#f59e0b/#9ca3af` → `var(--wx-warn,…)`/`var(--wx-muted,…)`（`test_template_inline_color_ratchet` 天花板：unified_inbox 15→14，本线 f00c4d00 带进的 +2 归零；剩 14>11 是 f00c4d00^ 已有的 13 + 他线，非本线）；`login.html` 企微扫码按钮 `background:#1b6df5` → `var(--wx-kf,#1b6df5)`（1→0）；`connect_guide.html` `.cg-wrap` 新增 `--cg-badge-gray/--cg-on-badge` 两 token，徽标改引用（1→0）；`ui-build.txt` bump `20260910-2106 severity=notice`（他线工作树里未提交的 `-2046 silent` 被覆盖：本次是 JS 修复，开着的旧标签页应收到刷新提醒） | `test_template_inline_color_ratchet::ceiling` 违规清单不再含 connect_guide / login；`test_inbox_inline_handlers_exported` / `test_rpa_inline_handlers_exposed` / `test_workspace_template_js_syntax` / `test_ui_build_freshness` 10 passed |
| **B 中继运维闭环** | `（B commit）` | `deploy/instances/prod_edge_watchdog.ps1`：新 **leg C relay**（`-RelayUrl` 默认 `https://relay.bd2026.cc/healthz`，空串关）→ `Probe-Relay`（15s，HTTP 200 且 body.ok）；独立 `relay_strikes/relay_alerted` 状态字段、独立 2-strike → `[ChatX] relay.bd2026.cc (WeChat relay) DOWN …` / recovered，与 katie 公网域名同一 telegram 档；**不碰**隧道 verdict / 重启；日志行 `[RELAY] healthy (http=200 online=N stale=M)`；ASCII-only 保持。`relay/app.py`：`RELAY_STALE_SEC`（默认 86400）；`DeviceRegistry.touch()`（ping 刷 `last_seen`，落盘 ≤1 次/60s；断线时刻立即落盘＝计时起点）、`stale_ids/offline_ids/prune_stale()`（离线且 last_seen 早于阈值才删，**在线永不删**）、`pruned_total`；`/healthz` → `{ok, devices_online, devices_known, devices_stale(登记但此刻离线), devices_pruned_total, pruned_now[], stale_sec, ts}`；清理触发点＝healthz（watchdog 5 min 一打＝天然调度器）+ 设备上线。`relay/tests/test_relay.py` **+2**：`test_healthz_prunes_stale_offline_devices_and_reports_counts`（注入 25h/1h 两行离线：前者被清且落盘、后者留并计 stale、二打无新清理）、`test_online_device_never_pruned_and_ping_refreshes_last_seen`（在线做旧 3 天不清；ping 刷回当前；断线落盘）。`relay/deploy_relay.ps1`：新 **`-Status`** 只读核对（ssh：`systemctl is-active/status`、`ss :18790`、本机 healthz、state 文件、nginx vhost；公网 healthz 打印 5 项计数；任一不健康 exit 1；不 scp 不 restart）；文件补 UTF-8 BOM（同 gate_sweep 的 GBK 问题：PS5.1 下原本 ParserError 跑不起来，此前只能 pwsh 跑） | `python -m pytest relay/tests -q` **7 passed**（5+2）；watchdog `-DryRun` 三连：`RELAYSTRIKE 1/2 http=404` → `2/2 … DRYRUN would send relay DOWN alert` → `RELAY recovered … strikes reset`，state 归零、**未发真告警**；`deploy_relay -CodeOnly` 上 VPS：`active` + 本机 healthz `devices_pruned_total=1 pruned_now=["dev-866606c9288c0afc"]`（09-08 联调设备行，离线 >24h，按设计被清）；`deploy_relay -Status` exit 0：`systemctl is-active chatx-relay = active`，公网 `devices_online=0 devices_known=0 devices_stale=0`；watchdog 正式路径下一轮起 `stale=` 有值 |
| **C 联调残留清理清单（dry-run，未执行）** | `（C commit，只改本文）` | 见 §3 | 主人拍板后逐条执行 |

## 2. 第二次 gate_sweep（BOM 后，PS5.1，107 文件，21:07–21:31）

- `=== [1/2] gate sweep: 107 gate files ===`（第一次是 50）→ **1603 passed / 18 failed**（第一次 909/11：多出来的 ~700 例就是此前被 GBK 吞掉的 57 个文件）。
- **wechat_* 11 文件 136 例全绿**（FAILED 清单里无一本线文件）。
- `[1b/2] import sweep: modules=1226 bad=0 / names checked=3588 bad=0 (guarded skipped=3914) ALL GREEN`。
- 18 红归属：本线 0（A2 修完后复核：`test_workspace_emoji_ratchet` unified_inbox 60→58＝f00c4d00^ 水位，ceiling 54 的差额是他线旧债；`inline_color_ratchet` unified_inbox 15→14 同理）；其余 = 红账本 09-08 就在的 8 项（emoji/legacy_blue/theme_contrast/input_theme_contrast/silent_exception/assistant_qa_eval×2/takeover_trigger_surface——它们一直在清单里却因 GBK 吞行从未在 PS5.1 sweep 里跑到）+ Q-15 `t.cat_'+` + QQ/TK 线在途 hunk（`cp-i18n.js` 双副本、`deploy_profiles takeover_rearm`、`template_orphan_refs rps-po-empty`）+ `test_sql_phantom_columns` / `test_fixture_time_bombs` 两个把 xdist worker 跑崩的（`node down: Not properly terminated`，复跑 90s 超时），非本线。
- 复跑 14 个失败文件仍红（非瞬时）。总 exit 1（他线红），本线验收项全过。

## 3. C 段：联调残留清理清单（2026-09-10 21:1x 只读盘点；**一条都没执行**）

联调实例＝`D:\wxpc_dev`（端口 18898，账号 `wxpc-dev-1`，`tools\wechat_pc_devlink.ps1` 默认目标）。盘点方式全只读：`netsh … show`、`Get-NetFirewall*`、`Get-NetTCPConnection`、`Get-ScheduledTask`、`Get-CimInstance Win32_Process`、sqlite `mode=ro`。

| # | 残留 | 现状（只读实测） | 建议动作（待拍板） | 风险 / 备注 |
|---|---|---|---|---|
| C1 | LAN portproxy `192.168.0.149:18898 → 127.0.0.1:18898` | `netsh interface portproxy show all` 第 6 行在；18898 的唯一监听者是 portproxy 自身（`svchost iphlpsvc` pid 20100），**后面没有实例** | `netsh interface portproxy delete v4tov4 listenaddress=192.168.0.149 listenport=18898` | 同表其余 8 条是生产 18799/18999 三网卡映射，**不动** |
| C2 | 防火墙入站规则 `ChatX workspace LAN 18898`（Name `{d9fdadf5-57cb-4f3f-a3a1-806e8ade4f01}`，Enabled） | 在；同名系列 `ChatX workspace LAN 18799/18999` 是生产，**不动** | `Remove-NetFirewallRule -DisplayName 'ChatX workspace LAN 18898'` | 无 |
| C3 | 18898 实例 `D:\wxpc_dev`（69.0 MB：`data/config/*.db` + WAL、`state/`、`shots/*.png` 20 张、`svc_run.err.log` 1.3 MB、`TOKEN.txt`） | **无进程**（`Win32_Process` 无 wxpc_dev/18898/wechat_pc），无 Windows 服务，`D:\chengjie-instances` 下无该实例 | 二选一：(a) 整目录归档 `D:\wxpc_dev → D:\chengjie-instances\_archive\wxpc_dev_20260908\`（保留联调证据：真机截图、日志、数据）；(b) 直接删 | 建议 (a)：D 段真企微联调可能还要复用它做「不碰生产」的联调实例（改回 18898 即可） |
| C4 | 联调计划任务 `ChatX WeChat PC Copilot*` | `Get-ScheduledTask` / `schtasks /Query` **一条都没有**（含 action 里含 wxpc/devlink/18898 的任务：0）；副驾当时是 `wechat_pc_devlink.ps1` 前台起的 | 无需动作 | 验收条件「只剩生产那条或没有」已满足＝没有 |
| C5 | 18898 相关 ssh 隧道 / VPS nginx | 本机 6 个 ssh 进程只有 18799/18999/18411–18415 转发，**无 18898**；VPS 中继设备行 `dev-866606c9288c0afc`（09-08 联调注册）已被 B 段新代码按 24h 规则自动清掉（`devices_known=0`） | 无需动作 | 无 |
| C6 | 后台两条重复测试行（`D:\wxpc_dev\data\config\inbox.db`，表 `messages`，WAL 3 MB 未 checkpoint，ro 读） | **rowid 13** `message_id=wechat:wxpc-dev-1:wx:id:zhu0396002:h:afb8223add1ec410` in「急急急」ts=09-08 **09:10:21**（＝rowid 5 02:27 的旧气泡被按「现在」重入站）；**rowid 16** `message_id=wechat:wxpc-dev-1:wx:id:zhu0396002:h:06247b69efd66d24` out「收到，这条是从智聊工作台发出的联调测试消息」ts=09-08 **09:13:03**（＝rowid 7 04:08 的重入站）。两条都属会话 `wechat:wxpc-dev-1:wx:id:zhu0396002`，`deleted_at` 皆空 | 主人拍板后：优先走产品软删（`deleted_at/deleted_by` 列已有，FTS `messages_fts` 同步由产品路径负责）；若直接 SQL：`UPDATE messages SET deleted_at=strftime('%s','now'), deleted_by='wx-line-cleanup' WHERE message_id IN ('…afb8223add1ec410','…06247b69efd66d24')`，硬删则还要同步 `messages_fts`（external-content 表）与 `conversations.last_*` 字段 | **额外发现（不在主人清单里，一并等拍板）**：rowid 2「急急急」03:31:03 与 rowid 3「22」03:31:13 是更早一次重启重入站（同一缺陷修复前），也是重复行；rowid 1 `wx:name:腾讯新闻` 空文本是系统号入库（后已进 `SYSTEM_SESSION_NAMES` 跳过清单）。如果 C3 选 (b) 删整目录，C6 自然消失 |

## 4. 没做 / 不做

- D（企微真账号联调）/ E（副驾生产化）/ F：等主人给企微凭证，未开。
- 第一次 sweep 里的其余非本线红（`t.cat_'+` Q-15 线、`_qqDriverDemoHtml` QQ 线、`rps-po-empty` reply_settings、`--th-*` token 未定义 personas/settings/_client_tier_upgrading、`cp-i18n.js` 双副本不同步、`deploy_profiles takeover_rearm`、`test_fixture_time_bombs` 把 gw3 worker 跑崩 + 复跑 90s 超时）：只报不修，归各线。
- `gate_sweep.ps1` 里 `tests/test_template_encoding_guard.py` 重复登记两次（既有），未动。
- `config.local.yaml` 未改；不重启实例。
