# 实施64：08-23 值守报障批次集中修复 → 1.0.52 指令

（值守线汇总 2026-08-23 15:2x。总账=《实施49》B45-B65 各行（B53-B65 为今日新增，
B45-B52 为 08-22 晚遗留未落码项）；本文档是可执行指令收敛，交 117 执行线。
证据截图在 `D:\chengjie-instances\zhiliao\data\protocol_media\telegram\`。
版本事实：1.0.51 已于 08-23 08:58 发布，**不含本批**；目标包 1.0.52。
施工纪律照旧：动手前 `scripts\agent_probe.ps1 -Intent` 登记；`.py` 攒批走重启窗
（04:00/12:30/22:30），唯一入口 `deploy\instances\restart_instance.ps1 -Instance
zhiliao`，先跑 `restart_preflight.ps1`；模板/i18n 热更 bump `ui-build.txt`；
收口 `gate_sweep.ps1`；客户可见文案 zh+en 双语、绝不露配置键/裸错误码。）

---

## P0（用户当前被实际阻断 / 每次升级必复发）

### P0-1 B57+B59 并案「升级风暴」：更新杀 TG 会话 → 风控误判永久冻结外发
**现象链**（skuio 机，08-23 晨，全程截图在案）：升级后 Telegram 已登录账号全体
掉线需重登（09:19 报，WhatsApp 不受影响）→ 顶部「紧急停发开关已开启（运营手动
冻结外发）」全账号发不出（09:50，`_303`）→ 客户包无任何解除入口，横幅只给
「复制说明给管理员」。
**根因面**：
① 升级过程旧后端进程未完全退出/会话文件处理缺陷，新旧双进程抢同一份 pyrogram
会话 → Telegram 侧 AuthKeyDuplicated/SessionRevoked 强制注销（复现路径：老版登录
TG → 原地升级 → 观察会话存活与 app.log 异常族）。
② `ban_signal.py` `_BAN_NAMES` 含全部 auth 族（Unauthorized/AuthKeyUnregistered/
SessionRevoked/SessionExpired/AuthKeyDuplicated）→ kind=ban → **账号级 kill_switch
永久置位 + 注册表 meta.banned**（07-27 对端注销误冻同族前科，本次是自会话侧新变体）。
③ 解除卡只在 rpa_overview（客户 flavor 隐藏）＝开关打开没有出口。
④ i18n `inbox.gate.killswitch` 文案「运营手动冻结」与 actor=ban_signal 实情不符。
**修**：
1. 更新器/安装器确保旧进程完全退出并释放会话文件后再启新版（验收=升级绝不需要重登）。
2. 账号重新登录成功 → 自动解除该账号 auth 族 `auto_ban:*` kill_switch + 清
   meta.banned（登录成功=最强的「没被封」证据）。
3. 「登录变更窗口」（登出/重登后 N 分钟）内的 auth 族错误降档为 pause/none，不判 ban。
4. 客户包给解除出口：横幅带鉴权一键解除，或账号卡「解除冻结」入口。
5. 文案按 actor 区分自动/手动，写实情。
**涉**：updater/installer、`src/ops/ban_signal.py`、`src/ops/kill_switch.py` 消费方、
unified_inbox 横幅、i18n errors/inbox_send_gate。
**验收**：装包升级实测 TG 不掉线；人为制造 auth 错误→重登后自动解冻；skuio 机复验。

### P0-2 P1-15 托管能力存量激活专项（voice / vision / asr）
**现象**：skuio 机（老装升级世代）`avatar_voice.enabled:false` 持续未被自动激活——
B43 回炉二次（`_305`：入口统一后登记被拦「语音引擎未接入：请联系官方开通」＝
拦检→登记不了→「登记成功即联动开启」永远走不到，**死锁闭环**）+ B62（`_312`：
回落 edge_tts 时把克隆名「steven」透传当音色名 → `ValueError: Invalid voice
'Steven'` 裸抛 UI）。同族：**网关 08-22 全天 vision 事件 0**（chat 1965/asr 7/
tts 7/mint 4/vision 0）——B55（`_295`）人像照被回「你家猫」＝回复链未经识图接图。
**对照证据**：钧机 08-23 02:47Z `register_spk` 200＝其 1.048+ 已激活——复现差异
收窄到安装/配置世代。
**修**：
1. `ensure_hosted_*`（voice/asr/vision）改为**每次启动幂等重跑**（不只领取时跑）；
   `_hosted_auto` 对存量 `enabled:false` 的覆盖语义定清楚（自动写的 false 可覆盖，
   用户显式关过的不覆盖）。
2. B62：回落 edge_tts 必须映射合法通用音色 + 明示「克隆音色暂不可用，本条用通用
   音色」，绝不透传克隆名；该 ValueError 人话化。
3. 「请联系官方开通托管语音」在托管已开通场景是假话——文案按探测实情。
4. **落地打包能力对账单门禁**（实施49 工作项5 原案）：example 有、客户档关的能力
   必须登记处置——这是同构病第 5 例（B1/B2/B8/B9 之后），别再靠人记。
**验收**：模拟存量世代（1.046 装→逐版升到 1.0.52）voice/vision 零操作可用；
skuio 机升级后重试克隆登记直接成功；发人像照 AI 正确描述或诚实说看不清。

### P0-3 B61 hub 哑音的工程兜底（hub 侧排查已交语音线，ASK 已投 .ops）
**现象**：钧机克隆 TTS 全链 200、真字节回传（19 字/10s/~184KB），播放**无声**；
10:56 复测仍无声＝持续性（`_309`/`_311`）。hub 176 pressure=high（free 2GB）。
**修**（引擎侧，与语音线的 hub 侧排查并行）：
1. clone/ogg 路径加**静音检测**（能量阈值），哑音自动拦下重试——当前指纹守卫只盖
   wav/paced 路径，hosted clone 的 ogg 是盲区。
2. hosted 客户 TTS 纳入 `voice_outage` by_source 台账（现在 attempts_24h=0 但真实
   客户流量在跑＝观测盲区）。
3. 预览播放器对疑似静音产物给「音频异常，点重新生成」提示。
**协作**：hub/176 显存高压与引擎产物排查=语音线在办，见
`D:\chengjie-instances\.ops\ASK_from_duty_20260823_hub_silent_audio.md`。

---

## P1（对话质量 / 接入流程 / 取证链）

### P1-1 B46+B55 识图消费硬闸（并案验收）
回复链必须等待并消费**当前图**识别结果；拿不到描述 → media_degrade 诚实问询或
不接图片话题，**绝不对图内容下断言**。验收=两场景装包实测：`_271`（回旧图话题）、
`_295`（人像照说成猫）。

### P1-2 记忆与一致性三件（B50 / B51 / B52）
- **B50**（值守建议升 P1，8 小时内两报）：档案属性（年龄等）捕获即入画像+生成注入+
  **禁复问已知项**。验收用例=`_299`（客户三次自报 38 岁仍被问年龄段）。
- **B51**：出稿检查「回复不得与对方上一条雷同」；并调 `_285` 那次生成记录定位
  哪个环节把对方原话当成了要发的回复。
- **B52**：人设近期自述状态（要睡了/在健身…）入短期记忆，后续消息（尤其
  proactive_opener）须衔接或合理过渡，矛盾拦下重写（`_287`）。

### P1-3 B56 翻译专有名词保真
**现象**：出站英文「…see you in Cebu…」正确，操作员中文译文镜像却是「我很想在
马尼拉见到你」（`_297`）——地名被「上下文合理化」偷换，操作员靠译文监督出稿＝
监督链失真。
**修**：①翻译提示词铁律：地名/人名/数字绝不替换，无把握保留原词；②译后对账
校验：源文专名/数字在译文缺失或被换 → 重翻或括注原词（与 fabrication 守卫同族）。
**验收**：`_297` 原句重译宿务≠马尼拉 + 地名数字保真批量抽查回归。

### P1-4 B63 messenger 发送链可诊断化（边车 500 三件）
**现象**：skuio 机 messenger 自动发 7/7 全失败，UI 只报「messenger send failed:
Server error '500'」冒号后空（`_314`/`_322`/`_323`）；收件正常。
**修**：
1. `/send` 调用点接上 `messenger_web_login.http_error_detail`（**08-15 已建、
   发送路径一直没接**＝报错空原因的直接根子），透传边车 error/reason_code。
2. 边车「PIN/接受浮层/登出」态映射到账号卡状态+显式提示（「需要输入 Messenger
   PIN/重新验证」），不再让每条消息静默 500。
3. **失败消息留痕**：自动发送失败必须在会话里渲染失败气泡+一键重发（现在用户
   内容直接消失）。
**验收**：断会话场景发送→报错带真因、账号卡亮状态、失败条可见可重发。

### P1-5 B64 messenger 接入验证窗死锁
**现象**：接入 Messenger 的官方登录窗里，FB 机器人验证只渲染一小块且无法放大 →
验证过不去 → FB 不下发 6 位 2FA 码 → 接入弹窗永远等码（`_316`/`_317`）。
**修**：①登录窗支持缩放/最大化（验证组件不可只渲染一小块）；②验证反复失败给
「换系统浏览器完成登录」逃生口；③等码界面补实情说明「未通过机器人验证时不会发码」。
**验收**：带两步验证的 FB 账号走通接入。

### P1-6 B53 诊断上传链修复
**现象**：skuio 机「一键发给客服」持续「连不上官网」，官网入口日志零到达；同机
AI 网关流量（同 urllib 同代理语义）全程 200——egress 掐大/长 POST 嫌疑最大。
**修**：①失败自动重试；②报错三分人话（本地未就绪/官网不可达/被拒——urllib
HTTPError 现在也被折叠成 unreachable，413 与断网不可分辨）；③失败降级 **mini 包**
（meta+fatal ~10KB，历史实证该体量可过其线路）重传；④**远程诊断上传腿复用同
函数（hosted_gateway.py:993）＝同故障，一并覆盖**——修好前给 DC8F 建 diag-request
无意义；⑤修复后回收 skuio 机 app.log 的「诊断包转投官网失败」异常栈定案。
**验收**：模拟大包传输失败→自动降级 mini 包成功回码。

---

### P1-7 B66 呼格互换（B42 族扩展，08-24 00:10 补入）
**现象**：客户叫人设「baba」，AI 回复反用「..., baba.」称呼客户（`_327`）。
**根因**：B42 守卫只扫「自己人设名」呼格；「对方→我」的爱称/角色称不在面内。
**修**：①称呼对方唯一来源=客户档案「偏好称呼」字段；②入站「对方称呼我」词集
进呼格禁用表，persona_guard 加第四臂（同 strip_vocative_self_name 宿主）命中
拦下重写；③prompt 锚补「对方对你的称呼绝不能反用来称呼对方」。
**验收**：`_327` 场景她叫 baba、AI 只能回 babe/偏好称呼；B42 金标回归不回退。

---

### P1-8 B67 出站语言未跟随（08-24 00:12 补入）
> **08-24 10:5x 范围修正（案2 `_337`，v1.054）**：即时应答也复现（三条中文回英文客户）——旁路不限 proactive 链，守卫必须覆盖**全部**出站路径。**本项仍未施工（不在 0823 夜批），优先认领。**
**现象**：客户英文+「发→英」开启，AI 主动跟进三连发**中文原样出站**（`_328`，
19:49 三条=行程/约见面主动内容）。
**根因嫌疑**：proactive/跟进链绕过 outbound_translate 单闸（普通回复链有、
此链旁路；B22 同构病）。
**修**：①全部出站路径统一过 outbound_translate，禁止旁路 ②出站语言核对守卫
（目标语言≠客户语言/发→英 配置→翻译或拦下）③调 19:49 三条生成投递记录实证。
**验收**：`_328` 场景主动消息出英文；普通回复回归不回退。

---

### P1-9 B70 Messenger E2EE PIN 闭环（08-24 上午补入）
**现象**：①登录窗「登录成功」浮层遮住 FB 的 PIN 输入页（`_341`）②应用内 PIN
弹窗托管后死等「下次出现」，边车不被驱动、无进度反馈（`_344`/`_345`，「入站
半死」横幅常驻、发送持续失败）③弹窗文案「发送不受影响」与实况矛盾。
**修**：①PIN 托管即触发边车重放（重开 e2ee 页/提交 PIN）并回报成败 ②成功
浮层不得渲染在 checkpoint/e2ee 页（或可关闭且默认避让）③文案对齐实况。
**验收**：skuio 机 Roselyn 账号走通 PIN 恢复、发送恢复。

---

### P1-10 B73 delivblock 横幅可诊断化（08-24 上午补入）
**现象**：钧机 v1.054「AI 暂时无法自动回复，部分消息已安全拦下」常驻，AI 停回
（复制全部信息实录）；横幅不带具体闸名，用户与值守都无从下手。
**修**：横幅附最高频拦截原因摘要（翻译不可用/媒体块/额度让路/发前确认积压…）
+「查看拦截明细」直达待发队列筛选视图。**个案定位**：等其待发队列原因截图 +
B990 远程诊断 dr-mt6mxbne 的 app.log。

---

### P1-11 B74 自报姓名被呼格守卫误抹（08-24 11:08 补入）
**现象**：问「怎么称呼你」→「叫我。朋友都这么喊我。」名字位空（`_352`，v1.054）。
**根因嫌疑**：strip_vocative_self_name 自介排除漏「叫我X/call me X」句式，
把正当自报家门当误用呼格抹掉。
**修**：自介例外模式补齐（叫我X/我是X/我叫X/call me X/you can call me X），
`_352` 进金标；B42 原金标（_236 呼客户为 Steven 必须拦）回归不回退。
**附案升级（11:19，B75）**：用户已保存+重启3次仍错（父母职业/工作全错，AI 即兴自编「商业咨询」）——档案→指令文本编译/「应用到」/会话注入三段链断点待定位；已请用户跑工作室「验收考题」+确认「应用到」分流；**执行线请本地复现**：新建自定义人设填档案→绑会话→问档案题，修复目标=填了就算数或明示不进对话的字段。**11:26 加急（用户直问核心逻辑是否被改）**：1.054 携 0823 夜批记忆三件套——回归排查两点：①_inject_known_profile 硬注入块与 persona 档案块的 prompt 预算/顺序相互作用（是否挤掉人设背景）②strip_vocative_self_name 自介误伤（B74，同批面）；本地复现用户场景（自建人设逐项问档案题）为第一动作。

---

### P1-12 B86+B81 待审/待处理队列面断裂（0825 03:46 补入，加急）
**现象**：钧机体检报 4 条待审（最早 17.2h）→「打开待审队列」空（`_416`/`_417`）；skuio 待处理 12 → 空（`_389`）。计数为真、落点漏显示。
**根因嫌疑**：计数（全局聚合）与落点视图（会话级/筛选后）口径断裂；查 0823 unread-trust/last_in_ts 批对 attn/presend 聚合的回归。
**修**：①体检/角标按钮直达全局待审视图（逐条会话名+放行）②计数与列表同源契约门禁 ③两案截图为验收用例。
**影响**：delivblock「一键放行」用户补救路径当前断死——B73 族依赖本修复。

---

## P2（UI / 文案 / 低危）

- **P2-a B60 会话列表滚动不达底**：底部「已归档」入口行被截断不可达（`_307`，
  单账号筛选视图）；修=滚动容器高度/底部留白计算（`_renderArchivedFooter` 行与
  「群组动态」底栏都要算进去）。unified_inbox 热区，动前 agent_probe 协调。
- **P2-b B58 标签管理**：只能加不能改删（`_302` aisida 标签）；补改名/删除/取消关联。
- **P2-c B65 relay 表单占位跨平台错标**：`inbox.connect.relay_email_ph`
  （inbox_workspace.py:989）写死「登录 Facebook 的邮箱或手机号」，LINE 弹窗原样
  露出（`_325`）；修=占位按平台参数化/分键 zh+en；LINE 是否该露账密表单交产品
  确认；IG/Zalo/WA 同款弹窗一并核查。
- **P2-d B47 人设编辑器丢弃后锁死+数据丢失**：丢弃后表单复位可编辑；丢弃前自动留
  可找回副本；两套未保存状态机合一（`_273`）。
- **P2-e B45 未读角标已读后复活**：已读持久化+同步不回退（`_269`）；验收=点开→
  退出→数分钟不复活。
- **P2-f B49 关怀预览超时**：预览异步化「生成中…」+报错人话「生成超时请重试」
  （`_282`）。
- **P2-g B54 人设输入框偶发无光标**：等复现细节（已向 skuio 要：哪个输入框/是否
  丢弃后），先并 B47/B19 Shadow DOM 焦点族排查。
- **P2-i B68 care 捕获排除群聊/报障会话**：care_capture 把报障群消息捕成「关怀约定」（00:12 实锤 2 条，值守已现场 cancel #6/#7）；修=capture/画像链对群聊 contact 与 bug_intake 会话一律跳过；顺带查「到期日解析成远期离谱值」（care 远日期 sanity chip 已有，捕获侧也该拦）。
- **P2-j B69 更新装载后顶部菜单栏消失**（`_336`，v1.054）：更新装完旧壳/新页混装态菜单两头落空嫌疑（appmenu-inpage 过渡）；修=更新装载后菜单可用性验收；临时=重启恢复。
- **P2-k B71 危机信号误报**：正常接送孩子英文口语被立案紧急（`_335`）；已建 DC8F 远程诊断回收 [wellbeing] matched= 行定位误命中词→收紧词表，误报样本进金标。
- **P2-l B72 让路线语义标注**：账号级反封号闸门的让路线横幅要标来源+构成（当日 cap/auto_cap/reserve），与会话级「回复额度守卫」区分（_350 撞名实录）；账号级份额可调性并入 B34 拍板。
- **P2-o B84 学习队列题型过滤+客服腔（0825 03:27 `_405`）**：人设属性/闲聊问题不入学习队列（只收有标准答案的业务知识）；出稿加「人设属性问题禁客服式回避」，与 P1-11（B75 档案注入）并案验收。
- **P2-n B4 收纳漏网（0825 03:22 复报 `_401`）**：「AI 回复策略」页在客户flavor 仍露出——nav 收纳批补收（隐藏/折叠进高级区），顺带核对收纳清单还有哪些漏网页。**+第二例（0825 03:31 `_409`）**：「人设声音评测」页（内部音色 AB 盲听工具，客户端无采样工具=永远空态）同为漏网页，一并隐藏。
- **P2-h 优先级提升（0825 03:00 skuio 复报 `_399`）**：左上角顶栏控件截断（_193 老案）用户二次催办——本批必修，勿再顺延。
- **P2-h 前批遗留照旧**：B25-③ 英文三件、P2-11 排版+B15 额度耗尽显式徽章。

---

## 装包验收欠账（代码已在 1.0.48+，缺实测回访）
- B41 重复回复：多通道场景装包实测；B23 人设绑定：五图场景实测。
- 钧已在 1.048+ 可直接回访验收；skuio 升级并解冻（P0-1）后同验。

## 待老板拍板（不动手）
B26 浮层撤除、B32 计划类免逐条确认、B34 额度自定义、contacts 去向（实施49 工作项2）、
B48 通知归拢方向。
**+值守提案（0825 02:2x，B72 钧复报驱动）**：内测期（至 08-29，与 B40 同窗）给两位测试者限期放宽**发送节奏闸**（companion_send_gate 账号级 cap/auto_cap）——深夜高强度测试频繁顶闸已引发「为什么还没解决」级挫败；放开模式参照 B40（限期+可回滚+用量照记）。批了值守立即通知生效。**⚠️ 02:41 升级为加急**：钧原话「这个优先处理，不然不能进一步测试聊天」——闸门已实际卡死内测进度，请老板晨间优先批复。**02:44 再升级：钧实测「现在人工回复也已经受限制了」＝人工道份额亦顶满，测试完全停摆**（值守此前「人工不受限」说法有误，已群内更正）；无远程开关可解，只能等滚动窗恢复——放宽方案落地时请同时评估：人工道在内测期是否应直接不限。**04:12 量化（_436 横幅）**：钧机张浩然号「今日发送额度已用完（155/155）…最早 10:25 释放一个名额」＝完全停摆至少到 10:25，数字实锤。
**+钧的额度体系设计方案（0825 02:32 原话，入 B34/B72 决策包）**：额度只按字符余额管理——低于 10000 字符提醒、低于 5000 再提醒、0 字符关停全部功能；**不需要限制发送条数**。值守注：条数节奏闸的防封号职能不宜全撤，可评估「平时不限、账号风控迹象时才收紧」的自适应形态。**02:36 钧补充（原话）**：「担心平台封号要调整发送频率或者不要拆分太多条信息发送，而不是停止发送功能」——防封响应=自动降频+合并少拆条（软降速），非硬停发；附带优化点：AI 回复少拆条（现在惯性三连发）既降风控又省额度。**02:40 钧立场收官（原话）**：「风控不是我们应该担心的，是客户根据号的风控调整，我们优先保证客户充值了就可以使用」——用户自治 vs 产品强管的定位分歧，连同余额三段制/软降速构成完整用户方案，交产品/老板定夺。


## 运行中/协作（值守线自持，勿动）
B40 内测无限额度 08-29 到期（回滚+7 天用量商业化报告=值守线）；B61 hub 侧=语音线
（ASK 已投）；dr-mt3ch24q 待钧机上传（注意：B53 修复前远程诊断对 skuio 机取不到包）。

## 施工状态（执行线回写区）
（三态：落码 / 装载 / 验收。执行线＝impl64 exec lane (117)，2026-08-23 19:40-22:15 批次。）

### P0 —— 全部落码，随 22:30 窗重启装载
- **P0-1 落码**（B57+B59）：① 壳侧 `desktop/backend-launcher.js` 新增 `stopAndWait`
  （taskkill 后**轮询等 PID 真死**＝会话文件已释放，超时如实返 false），
  `main.js` 三处收口：`installUpdateNow`（quitAndInstall **之前**先等旧后端死透）、
  手动检查更新同路径、`before-quit` preventDefault+等死+二次 quit。
  ② 登录成功自动解冻：`ban_signal.clear_auth_ban_on_login`（只解 auth 族
  auto_ban + 清 meta.banned；手动冻结/真封禁不动；解除落 ops_events
  `kill_switch_clear` 审计）挂 `_persist_login_account`（全平台登录成功单点）。
  ③ 登录变更窗口降档：`in_login_flux_window`（进程启动 15min 内 / 账号登出、
  重登后 15min 内）auth 族错误 → pause+TTL（reason `auth_flux:*`），绝不永久
  ban；登出路由已打点。真封禁族（UserDeactivated*）语义不变。
  门禁：`test_ops_ban_signal.py` 22 例 + `backend-launcher.test.js` 66 例 +
  桌面全量 npm test 绿。**验收待装包**：壳侧改动要 1.0.52 安装包才到坐席
  （老版登录 TG→原地升级→不掉线；skuio 机复验）。横幅出口/文案两面＝killswitch
  可见性线已覆盖（勿重复）。
- **P0-2 落码**（B43回炉+B55+B62）：① `ensure_hosted_*` 幂等重跑核实＝main.py
  启动全跑 + refresh_once 每小时全跑 + trial-claim 钩子（本已在树）；**新增**
  enroll 路由 hosted 预授权入口当场重跑。② B43 死锁根修：`voice_routes` AvatarHub
  登记分支入口从 `enabled=true` 放宽为 `enabled or should_auto_enable_avatar_voice`
  （预授权形态先 `ensure_hosted_voice` 接线再 health 探针→登记→既有 B43 联动
  持久化翻开）——「拦检→登记不了→永不激活」闭环拆除。③ `_hosted_auto` 覆盖
  语义钉死：种子/自动写的 false 可被运行时纯内存覆盖，用户显式退出**唯一**表达
  ＝`hosted_opt_out`（交付档禁止预置，门禁
  `test_hosted_auto_sections_keep_optout_semantics`）；存量补齐机制核实＝
  feature_registry A 类 `_hosted_auto` 三键 + `_ensure_baseline` 启动回填（本已
  在树）。④ B62：`tts_pipeline.safe_edge_voice` 白形校验——克隆名绝不透传
  edge（`ValueError: Invalid voice 'Steven'` 根治），`_run_backend`/`_edge_tts`/
  回落三点收口 + `voice_mapped_from` 进 tts-test voice_meta + cp-voice 警示条件
  扩展（双树同步，cp-voice ?v=20260823e）。⑤ 假话文案：`_voice_missing_message`
  按托管实情分流（预授权在场→`err.voice.hosted_unavailable`「暂时不可用请稍后
  重试」；真没接→`err.voice.engine_missing` 维持开通指引），zh+en。
  ⑥ **打包能力对账单门禁落地**（同构病第 5 例收口）：
  `test_ship_config_disabled_capabilities_are_all_accounted`——交付档每个
  enabled:false 必须 registry 在册 / `_hosted_auto` 预授权 / allowlist 附理由，
  三处皆无即红。门禁：`test_deploy_profiles.py` 15 例 +
  `test_voice_enroll_hosted_linkage.py` 11 例 + `test_hosted_gateway.py` 全绿。
  **验收待装包**：模拟 1.046→1.0.52 升级链 voice/vision 零操作可用；skuio 重试
  克隆登记直接成功。
- **P0-3 落码**（B61 工程兜底）：① `avatar_voice.detect_silent_audio`（wav=stdlib
  PCM 峰值零依赖；ogg/mp3=ffmpeg 解码测能量，缺 ffmpeg → None 放行）挂
  `_try_hub_fish._do_synth`：哑音首见重试一次、复发判合成失败走既有回落链
  （extra `hub_silent_audio` 留痕）——尺寸/时长/200 全绿的零能量产物不再出门。
  ② hosted 客户 TTS 观测盲区收口：tts-test 预览成败进 `voice_outage` 台账
  （source=`preview`），attempts_24h 不再对真实客户流量装 0。③ 预览播放器
  哑音警示：cp-voice `_sniffSilence`（WebAudio 解码测峰值，best-effort）→
  「⚠ 音频异常（疑似无声）——请点重新生成」（`cp.voice.silent_note` zh+en，
  双树同步）。门禁：`test_hub_engine_attribution.py` 25 例（夹具改方波防误杀）+
  `test_voice_outage.py` 35 例绿。hub/176 侧根因排查仍属语音线（ASK 在册）。

### P1
- **P1-1 已被兄弟线覆盖，勿重复**：拟稿等图（media_wait_sec 20s）+ 同图去重锁 +
  盲断言闸门 + 诚实追问已在树（`media_enrich.py` / `autodraft_helpers.py` /
  `protocol_autoreply.py`，`test_vision_race_fix.py` 27 绿）。**转装包验收**：
  `_271`（回旧图话题）+ `_295`（人像照说成猫）双场景实测。
- **P1-2 落码**（B50/51/52）：
  - B50：年龄捕获进画像（`memory_heuristic` 我38岁/我今年38/I'm 38 三形态，
    追忆句与第三人称排除；`memory_slots.SLOT_AGE` 单值槽冲突按新近择一）+
    `skill_manager.resolve_age` + **已知画像硬注入禁复问**
    （`_inject_known_profile`：称呼/年龄/居住地/生日每轮无条件注入
    `_known_profile_block`「绝不要再问」——关键词召回挑不中恒真身份槽正是
    `_299` 三次自报仍被问的机制）。A/B 两链 + `_build_context_prompt` 三点接线。
  - B51：`reply_echo_guard.reply_echoes_inbound`（归一化 difflib+整包含，短呼应
    不判）挂 autodraft（重生成一次→复发放弃兜底）+ protocol_autoreply（重生成
    →复发本轮不回）。**`_285` 那次生成记录的环节定位待生产日志复查**（闸已
    先兜住，任何环节串稿都出不了门）。
  - B52：`companion/self_state.py`（自述状态保守正则六类，句级否定/对方向排除，
    TTL 睡 9h/其余 2-4h）——捕获挂 `_update_after_reply`（A/B 单点），注入挂
    两链 `_self_state_block` + **proactive 开场同源消费**
    （`build_proactive_prompt(self_state_note=)`，`_gen_text` 读 B 线同键
    user_context）。`_self_state_log` 随 ContextStore 持久化，两个 block 键进
    非持久表。门禁：`test_memory_consistency_trio.py` 14 例 + memory
    extract/recall eval + proactive_prompt 全绿（65 例）。
- **P1-3 落码**（B56）：`translation_fidelity.py`——①提示词铁律（AI 引擎 prompt
  + `_BARE_TRANSLATION_SYSTEM` 双注入：专名/数字绝不替换，无把握保留原词）；
  ②译后对账 `annotate_missing_anchors`（锚点=句中拉丁专名+多位数字；缺失→译文
  尾括注原词「（原词：Cebu）」；27 城音译词典命中不打扰；`swapped_geo` 换名
  检测）挂 `EngineRouter.translate()` 出口——**无论哪个引擎哪种偷换，读译文的
  人都能看到原词**。门禁：`test_translation_fidelity.py` 10 例（含 `_297` 宿务→
  马尼拉实录端到端）+ 翻译引擎/置信度 51 例绿。验收：`_297` 原句重译。
- **P1-4 落码齐**（B63）：① `/send` 接 `http_error_detail`——核实**本已在树**
  （channel_adapters + account_orchestrator 两处，08-15 建 08-17 后接上；skuio
  空冒号是旧包，1.0.52 自带）。② **新增**发送败因→会话健康：
  `note_send_auth_failure`（e2ee_pin→failed+`e2ee_pin_required` 精确 hint；
  needs_accept→failed；logged_out/not_logged_in→needs_login 连带注册表
  offline）挂两条 messenger 发送路径——PIN/登出不再逐条静默 500，账号卡/横幅/
  看门狗同源点亮。门禁：`test_platform_session_health.py` 55 例绿。
  ③ **失败留痕+一键重发已落码**（22:40 窗内 round2 装载）：
  `store.record_failed_outbound`（direction=out status=failed 消息行，**不**推进
  conversations）+ worker 终局失败经 `DraftService.record_failed_outbound_mirror`
  留痕（重试中不留）；气泡渲染「✗ 发送失败＋重发」（`resendStoreFailed`，
  skip_translate 原样直发 + `resend_of` 回传，发送成功服务端 failed→resent 收口
  防重复点）；**「真的发出去了」口径六处过滤契约**：首响 t_out / 回复时延 SLO /
  群发言台账×2 / 目标「已跟进」/ 草稿「已回过」护栏 / 近重复守卫（否则一键重发
  永远撞自己留痕 409）。门禁 `test_failed_outbound_trace.py` 12 例 + 涉改模块
  182 例绿。i18n `inbox.msg.resent` zh+en；ui-build 20260823-2245。
- **P1-5 大头落码**（B64，与兄弟线合力）：① 登录窗缩放/最大化＝兄弟线
  `server.js` viewport:null（humanWindow 跟随真实窗口，headed 流专属；离屏/截图
  流保持固定 1280×800）——**随 1.0.52 包走**。② 机器人验证识别补盲区：
  `login_classify.js` 新增「确认您是真人/confirm you're human」**可见文案判据**
  →归 checkpoint（`_316` 实录：验证组件渲染在非 /checkpoint/ 路径的白页上，
  URL 判据漏网 → 前端只会说「等待登录」让人干等）；压过 cookie 二因子推断
  （验证不过码不会发，「等码」是误导）；泛词 security check 刻意不收防误报。
  node 20 例绿，**随 1.0.52 包走**。③ 等码/检查点实话文案（hot-live 已生效）：
  hint_live_checkpoint/2fa 重写——「验证未通过前 Facebook 不会发验证码」+
  「窗口可最大化/Ctrl+滚轮放大」+「反复失败关窗重开或换网络」；顺带修掉
  「服务器上的登录窗口/那台机器」旧措辞（客户包登录窗弹在本机，旧话让用户
  找不存在的服务器）。**「换系统浏览器完成登录」完整逃生口刻意未做**：边车
  会话在 Playwright profile 里，系统浏览器登录的 cookie 不会自动进来，做全需要
  cookie 导入面（新安全面），交产品决策。验收=带两步验证的 FB 账号在 1.0.52
  实装机走通接入（skuio/钧机）。
- **P1-6 落码**（B53）：`diag_upload.build_and_upload` 三改——①传输失败自动
  重试一次（RETRY_DELAY_SEC=2s）；②报错三分 `classify_upload_error`
  （`bundle_failed` 本地打包失败 / `upload_rejected_<status>` 服务端拒收（413
  终于与断网可分辨）/ `upstream_unreachable` 连不上），`error_detail_for` 统一
  人话映射（新 i18n 键 zh+en），support + ops 两入口同源；③全尺寸两连败→
  **mini 包降级重传**（meta+fatal+主日志 24KB 尾，config/logs 目录不带；meta 记
  `full_bundle_error/bytes`；响应带 `mini:true` additive）。④远程诊断腿
  （hosted_gateway.check_remote_diag）复用同函数＝同修复，**dr-mt3ch24q 装载后
  可对 skuio 重发 diag-request**。⑤ skuio app.log 异常栈回收定案＝装载后值守
  回访。门禁：`test_support_routes.py` 30 例绿（含 egress 掐大包→mini 送达
  端到端）。

### P2
- **P2-c 落码**（B65）：relay 占位/检查点文案平台分键——基键中性化（「该平台
  登录用的邮箱或手机号」/「平台触发了安全验证」）+ messenger/line/instagram/
  zalo/whatsapp 五平台后缀键 zh+en；`connect_relay.js` 白名单选键（未知平台走
  中性基键绝不吐裸键），`driveTick` 透传 platform（unified_inbox 调用点已带）。
  node 21 例绿，connect_relay ?v=20260823e。**LINE 该不该露账密表单＝产品确认项，
  未动**（当前只修文案说谎面）。
- **P2-f 落码**（B49）：care 预览等待期真实反馈（`cs2_preview_busy`「正在生成
  预览…最长约一分钟」）+ 超时/中断人话（`cs2_preview_timeout`「生成超时请重试」，
  不再吐异常对象），zh+en，care_schedule.html 热更。
- **P2-b（B58 标签改删）/P2-e（B45 已读复活）＝兄弟线 12:30 窗已装载，勿动，
  转装包验收**。
- **P2-a（B60 会话列表滚动区）未做**：unified_inbox 热区 + 需截图复现底部留白
  计算，交下批（注意与上午 inbox polish 线的密度 ratchet 协调，行高天花板
  只降不升）。
- **P2-d（B47 编辑器丢弃复位）未做**：两套未保存状态机合一属结构改造，交下批。
- **P2-g（B54 光标）**：维持等复现（值守已向 skuio 要细节），与 B47/B19 焦点族
  并查。
- **P2-h（B25-③ 英文三件/P2-11 排版/B15 徽章）未做**：交下批。
- 附带：emoji 棘轮 50→53 代记归因（上午 unread-trust/tag 线的 🏷×2/🧹 控件位
  emoji，债在其名下应转 uiIcon）；`ops_overview` 5 CJK 红＝xiaozhi-obs 线自名
  在册，未动。

### 装载与验证（重启窗执行）
- **已装载 round1**：2026-08-23 21:43（22:30 窗内），restart_preflight GO（active-files
  仅坐席日志监控自动写盘，-SkipActiveCheck；701 dirty .py 语法 0 错；装配测试过）
  → restart_instance 窗口 **21s**（SLA 180s），/login 就绪，LAN 代理正常。
- **已装载 round2**：2026-08-23 22:41（同窗），P1-4③ 失败留痕链 + B64③ 文案
  （详见上方两条）；preflight GO、窗口 **23s**、app.log 零 ERROR。B64①②
  （边车 viewport + 机器人验证判据）是 Node 侧文件，**不随引擎重启装载，
  随 1.0.52 安装包到客户机**；117 本机边车下次例行重启时顺带取到。
- 装载前回归：批次专项 pytest **170 全绿**（ops_ban_signal/memory_consistency_trio/
  translation_fidelity/deploy_profiles/voice_enroll_hosted_linkage/
  hub_engine_attribution/voice_outage/support_routes/platform_session_health）；
  gate_sweep 837/838，唯一红＝ops_overview 5 CJK（xiaozhi-obs 线自名在册，非本批）。
- 装载后探针：avatar-status / ai-runtime-status 均 401＝已注册；boot log 双 TG 账号
  连接正常；app.log 尾 400 行零 ERROR/Traceback。**待真实流量**：outage.by_source
  的 preview 桶（首次 tts-test 后出现）；auth 错误→重登自动解冻（需真机场景，
  随 skuio 复验一并做）。
- 前端热更已生效批次：cp-voice/cp-i18n/connect_relay `?v=20260823e`、
  care_schedule、unified_inbox（platform 透传）；ui-build 20260823-2100。

### 0824 午间增补批（P1-8~P1-11 + P2-i/j/k/l，执行线 12:31-13:4x；方案存档=实施67）
- **P1-11 落码（B74+B75，加急）**：根因与派单嫌疑有出入（读码坐实，详见实施67 §1）——
  ① **B75 真凶＝交付档 compact 静默丢弃 70% 档案字段**（`config.desktop.internal.yaml`
  `persona_block_detail: compact`，而 compact 编译器不含 背景/家人职业/英文名/好恶/
  记忆/外貌 等编辑器 20+ 字段）；昨夜 `_inject_known_profile` 无预算冲突（主链无截断
  机制、trim 只在本地兜底链且从 system 尾砍）。修＝`format_persona_block` **tier 升档**
  （显式绑定 conv_override/chat_binding/account_profile 三 tier 在 compact 配置下自动
  升 full＝「填了就算数」；域/默认 tier 维持 compact 省 token）+ compact 补西方名字
  事实行 + **字段防丢弃契约门禁**（`test_persona_profile_field_coverage.py` 7 例：
  编辑器可填字段 ⊆ full 编译面，缺一即红）。
  ② **B74 真凶＝sanitize_self_name hard 档②+inline 抹名兜底**（呼格守卫两模式都要求
  逗号形态，「叫我X」根本不命中）——「叫我浩然，朋友都这么喊我」单句剥光走 inline
  抹名 → 「叫我，朋友都这么喊我」与 `_352` 逐字吻合。修＝**自介句禁残剥**（inline
  抹名只对 borrowed 借对方名档执行；David Lin 金标不回退）+ **白名单同源**
  （`_enforce_persona_consistency` 增 resolved_name 形参，A/B 两链把生成时 prompt
  锁定名直传白名单——prompt 让说的名字绝不该被守卫抹）。金标 `_352` 5 例进
  `test_persona_self_name_guard.py`（49 全绿含 B42/_236 回归不回退）。
- **P1-8 落码（B67）**：结构性根因＝「发→X」只存前端 localStorage（`_persistXlatePrefs`
  只写本地），服务端自动链对用户意图零可见。修＝① store 新表
  `conversation_outbound_lang`（独立小表，刻意不加列 conversation_settings——「行存在=
  档位显式设置」语义会被污染，有反向门禁）+ ② `GET/POST /api/unified-inbox/
  conv-xlate-out`（前端 `_persistXlatePrefs` best-effort 同步，特性探测 fail-soft）+
  ③ `translate_outbound_text` **显式目标最优先**（具体语种=钉死不依赖检测投票；
  'auto'=显式跟客户语言；显式意图在场时 gate_only 升级完整翻译；翻译不可用照旧
  HOLD 绝不中文裸发）——B 线 autosend 与 proactive 共用该函数**自动获益**（`_328`
  主动消息 + `_337` 即时应答都是 B 线）。A 线维持生成层 lang_policy 管辖（客户包
  即时应答=B 线，A 线不在事故链）。门禁 8 例新增（51 全绿）；流水实证待装载后调
  `_328`/`_337` 会话记录。
- **P1-10 落码（B73）**：`delivery_block.seat_banner` 增 `top_reason*`（近窗最高频
  (domain,reason) 环形明细现算——不用进程累计防旧故障顶位；人话映射复用弹窗词表）
  → 横幅「主因：XX（近 30 分钟 ×N）」行（已知码走前端词表 zh+en、未收录码服务端
  人话兜底）+「查看拦截明细 →」直达 /workspace（走 _win_unique 防双开）。门禁 3 例
  新增（37 全绿）。个案定位仍等 B990 诊断包。
- **P2-i 落码（B68）**：care 捕获三重排除——正则链（care_capture）群聊 chat_type
  一律不捕 + bug_intake.groups 在册会话不捕；LLM 影子链（care_shadow_scan）群聊不进
  扫描队列；**远期到期日 sanity 单点**（add_commitment：due/event > now+370 天＝日期
  解析错误直接拦，正则/LLM 两链共享）。门禁 62 全绿（含 2099 夹具按「锚 now」纪律
  重写——那类「永远的未来」正是 sanity 要拦的形态）。
- **P2-k 先行收紧（B71）**：读码锁定高嫌疑双义短语——`can't go on`（"can't go on
  **Monday**"=日常安排）/`no reason to go on`（"go on the highway"）/`hopeless`
  （"traffic is hopeless" 英式口语）。修＝`_GO_ON_MUNDANE_TAIL` 负向前瞻（日常宾语/
  日期后续不判危机；anymore/like this/句末照常立案）+ hopeless 收紧为情绪自述形态
  （feel hopeless / I'm hopeless 句末；"hopeless at cooking" 自嘲放行）。金标 `_335`
  场景 6 例 + 危机面回归 7 例；危机全家族 47 绿（severe 召回不回退）。**误命中词
  终案仍等 DC8F 诊断包 `[wellbeing] matched=` 行**，到证后二次收紧。
- **P2-l 落码（B72）**：`inbox.gate.auto_yield*` 三键改造——标来源【账号防封安全
  额度】+ 构成（AI 已发/今日上限/总已用+让路线算式）+ 显式「与会话级『回复额度守卫』
  是两回事」，zh+en。账号级份额可调性未动（并入 B34 拍板，纪律）。
- **P1-9 落码（B70，随 1.0.55 包）**：① **托管即重放**——`POST /accounts/:id/e2ee-pin`
  存好后异步 `driveE2eePinReplay`（当前页浮层在场就地提交；不在场找首条 e2ee 线程
  导航钓浮层（副作用=该线程标已读 vs 整账号瘫痪，两害相权），试完回原页；结果回写
  `_pinState` 经 /accounts 心跳出网），响应带 `replay` 字段。② **浮层避让**——
  `injectPostLoginBanner` 前探 `detectPinPrompt`（PIN 页不注入成功横幅）+
  **auto-hide 前最后核对**（PIN 浮层在场：有托管 PIN 先就地自愈，没有/失败→本轮
  不隐藏窗口摘横幅还输入给人——隐藏等 PIN 的窗=把账号推进入站半死）。③ **文案对齐**
  ——`inbox.pinm.why`「发送不受影响」改「加密会话的发送也会失败」+ `ok_armed`
  「等下次」改「正在主动验证」，zh+en。边车 node 45 测试绿 + server.js import 检查过。
  验收=skuio Roselyn 装包回访。
- **P2-j 落码（B69，随 1.0.55 包）**：壳 main.js **菜单认领看门狗**——原生菜单条
  autoHideMenuBar 隐藏的前提=页内菜单接管；`desktop:app-menu-spec` 被页面调用=认领，
  窗起 30s 未认领/did-fail-load → `setMenuBarVisibility(true)` 把原生条还回来（两头
  落空变不可能），页面随后认领→收回隐藏。npm test 全绿 + node --check 过。
  验收=1.0.54→1.0.55 原地升级菜单在场。
- **收口**：专项回归 292+4 全绿（persona/outbound/delivery_block/care/wellbeing/
  路由清单）；gate_sweep 唯一红=ops_overview 5 CJK（xiaozhi-obs 线自名在册，非本批）；
  ui-build 20260824-1330（unified_inbox conv-xlate-out 同步 + workspace_base 横幅主因行
  + i18n 三 pack）。**引擎 .py 攒批待 22:30 窗**（12:30 窗已过）；边车/壳侧随 1.0.55 包。
  三视角分析与逐项方案＝`docs/实施67_人设档案不生效与守卫误抹_出站语言旁路_三视角
  分析与优化方案_2026-08.md`。
- **📦 1.0.55 已发布（2026-08-24 19:16，老板令）**：lite 形态（公开链接替 1.0.54；
  已装 internal 机自动更新保形态保数据）——本批全部修复随包（引擎 .py 打进包内
  后端 + 边车 PIN 重放 + 壳菜单看门狗）。publish 全链绿：VPS sha512 验证/R2 边缘/
  公开验证 latest.yml=manifest=1.0.55 exe HTTP206/公告 feed release-v1.0.55/
  keep3 剪 1.0.50；TG 广播频道 @hykj7 msg119 + 群 @hykjz msg122。
  **知会值守**：装包验收欠账（B41 多通道/B23 五图/P0-1 升级不掉线/B63/B64
  messenger 链）+ 本批验收（Roselyn PIN、升级菜单、钧机 delivblock 主因、
  `_352`/`_355` 档案考题、`_328`/`_337` 出英文）可开始回访；两个远程诊断
  （DC8F=dr-mt6mrohy、B990=dr-mt6mxbne）证据到手后 B71 词表二次收紧与 B73
  个案定案照旧。117 生产 zhiliao 的 .py 装载仍走 22:30 窗（与客户包互不依赖）。

## 2026-08-25 凌晨增补：P1-13（B88，钧机 v1.0.54 实测，多群复现）

**现象**：①群会话线程视图在某条消息后停更：bug 群冻在 04:07、Ai未来 群冻在前晚 19:24；期间列表行时间戳持续更新（02:51）、AI 自动回复正常触发（智控王 02:50 仍回）→ 接收/处理链正常，**读取或渲染链在特定消息后中断**；重启不恢复（不补拉）。②「群组/@我/未读」筛选片自粘滞：点片上 × 删不掉、疑似清空筛选也失效，导致会话列表被长期错误过滤（用户以为"群消失"）。
**修复**：①查线程视图增量拉取/渲染游标——是否某条消息（毒消息嫌疑：转发嵌套截图、引用回复、特殊 markdown）解析抛错后整批中断且游标不前进；加防御：单条渲染失败降级占位，不阻塞后续消息；打开会话时若镜像最新 id > 视图最新 id 强制重拉。②筛选片：用户实测**清空筛选有效、单片 × 无效**（_446）→查片上 × 的点击事件绑定/命中区域即可，清空链路不用动；列表处于筛选态时顶部显式横幅提示。
**验收**：钧机 bug 群与 Ai未来 群打开即见最新消息；构造毒消息（转发嵌图+引用）线程不再停更；筛选片可逐个删、清空筛选一键全清；筛选态有可见提示。
**证据**：实施49 B88 行（_431/_435/_437/_439/_441/_443/_444），诊断包 QTSE67（dr-mt7ok40m-svjonx，B990）。
**诊断包结论（候升 P0）**：钧机实为 v1.0.55（非 1.0.54）。无致命堆栈；session 列表定出规律：冻结点=某次重启前最后一条消息（Ai未来 19:24←19:31:30 重启；bug群 04:07←04:07:48 重启），其后五次重启不自愈；引擎08-25 处理 193 条、AI 正常回。**修复方向收窄**：重启/离线窗口造成镜像缺口→线程视图增量游标卡缺口不前进、无自动补拉。落点：①启动/打开会话时对比 dialog 顶部消息 id 与镜像最大 id，有缺口即对该会话自动 sync-history补拉；②视图分页锚改为「最新优先」而非连续游标，缺口降级为「加载更早」；③补拉失败要在会话顶部显式报（禁止静默漏显）。验收加一条：重启期间往群里发 3 条消息，重启后打开该群 3 条全可见。

## 2026-08-25 凌晨增补 2：P2-p / P2-q（客户列表，钧 _450）

**P2-p（B89，产品建议已采纳）**：客户列表显示原始 @号/ID 或「未命名客户」。
改：①名称列优先显示平台昵称（Telegram display name），@号/ID 降为副行小字；②客户详情支持**备注名**（显示优先级：备注名>昵称>@号），列表/搜索/导出 CSV 同步生效。**扩口径（_452）**：客户全景→消息记录页的气泡此前只挂「Telegram」平台标，需同步显示发言人昵称/备注（群记录退化场景在 P2-q 修掉后自然只剩私聊对端）。验收：钧机客户列表 6 条全部显示昵称或备注，搜索备注名可命中；客户全景消息记录页每条气泡带发言人名。

**P2-q（B90，值守自查）**：①同一 @号（hwq25801）出现两行重复，疑似「会话→客户」映射按本方 account_id 各建一条、未按对端唯一键去重——查生成链去重键（应为 platform+对端 id，跨本方账号合并）；②群聊被计入客户池（本群 @hwq25801 成了客户，亲密 56）——客户池应过滤群/频道会话，只收私聊对端；③「未命名客户」=昵称抓取缺失，随 P2-p 昵称链一并补。验收：钧机客户列表无重复行、无群条目；群聊不再产生客户记录。

**P2-r（B91，skuio _455）**：英文界面下 Follow-up SOP 卡内链条模板名/描述仍中文——模板是种子数据仅有中文版。改：workflow 模板 name/desc 加 en 字段（新客破冰·7日跟进/沉默唤回·3步/成交后关怀·72h/报价后跟单 四条内置全覆盖），卡片与工作流页按 UI 语言取；另在「工作目标」「跟进 SOP」两卡各加一行区分说明（目标定方向·注入 AI 思路 / 链排动作·分步执行），中英双语。验收：EN 界面下四条模板名/描述全英文；两卡说明行可见。

**P2-s（B92，skuio _456）**：人设工作室「同步到 personas.yaml」按钮+确认弹窗是开发者口径（yaml/git/config 路径/「第2层」）。改：这组 i18n 键白话化（psn_sync_to_yaml、psn_sync_to_yaml_t、psn_js_360、psn_src_published_t、psn_js_010/075/342/344 及导出导入区标题）——按钮「保存人设存档」，弹窗讲“稳定备份、可恢复、重启自动加载；Messenger 自动导入的人设需先转正才入档”，文件名/路径收进小字或帮助；「已固化」徽标同口径改「已存档」。中英双语一起换，i18n 热更 bump ui-build。**扩口径（_462）**：全局规则区导入/导出同改——按钮旁说明「导出=生成规则备份文件（换机/恢复用）；导入=从备份恢复」，导出文件名可读化（如 全局规则备份_YYYYMMDD.yaml），界面注明「此文件供导入恢复用，无需手动打开」；并全面排查人设工作室其余导入导出/存档入口统一口径。验收：EN/中文界面下人设工作室全部导入导出/存档按钮、弹窗、徽标、提醒均无 yaml/git/config 等格式术语（帮助/小字除外），导出文件名可读。

**P2-t（B93，skuio _459，待产品拍板后动工）**：默认人设兜底页编辑面过窄。两案：①兜底页字段补齐到人设卡同权（成本高，两套编辑面长期漂移）；②推荐——「默认人设」改为从人设池挑选完整人设（读写 platform_login.default_persona_id，persona_runtime.yaml 域默认仅当池空时兜底），页面改为「当前默认：xxx［更换］」+生效范围说明（persona_scope 已有推导器）。验收（按②）：池中人设可一键设为默认；新登录账号自动绑定该人设；池空时才回落内置兜底。

**P2-u（B94，skuio _464）**：全局规则「新增约束」编辑器零引导、门槛高。改：①内置模板库/常用示例约 8-12 条（不承诺具体发货时效/不发外部链接/不谈退款以外补偿/不透露内部流程…），点选即填可再改；②输入框占位引导「一条规则=一句给 AI 的禁令或要求，AI 按语义执行」；③每条规则显示作用平台范围（全平台/平台专用）；④页头一句机理说明「全局规则对所有人设生效，优先于单人设设定；不影响人工审核开关」。中英双语。验收：新用户不看文档能在 1 分钟内加出一条有效规则；EN 界面全英文。

**P2-v（B96，skuio _467，微文案）**：「复制视角链接」用途不明。改：①按钮 tooltip 已有 inbox.acct.copy_link_t，补成动线说明「复制后粘贴到浏览器，可直达当前平台/账号/筛选视角（多屏/分享用，需登录）」；②复制成功 toast（inbox.acct.link_copied）同口径加半句用法。中英双语，i18n 热更。验收：悬浮与 toast 均能让新用户明白拿链接干嘛。

## 2026-08-25 凌晨增补 3：P1-14（B95，skuio 机 v1.0.55）

**现象**：侧栏「数据洞察（今日概览/运营总览/运营分析/主管看板）+ 用量与计费（用量与额度/会员中心）+ 安全合规（危机审计/操作记录）+ 账号资产中心」整组点击→「智聊 ChatX 正在连接…」加载屏挂起→自动弹回后台控制主页。人设工作室/聊天工作台等其余页面正常。
**排除**：非主管闸（302 应直落工作台无转圈）、非 feature_gate 档位锁（应落会员中心/锁标）。
**修复方向**：查这组页面在桌面壳中的加载链与其余页面的差异（窗口/URL 解析/端口/超时回退逻辑）；重点疑点=0816 管理面改造后这组页面的壳内路由 + 1.0.55 lite 打包对内测机形态的影响；「正在连接…」超时回退前应报具体错误而非静默弹回。
**验收**：skuio 机（DC8F，v1.0.55）上述全部入口可打开；加载失败时给出明确错误提示（含重试），不再静默回退。
**证据**：实施49 B95 行（_466/_469）。

**P2-w（B97，skuio _472）**：英文界面下原生菜单栏（文件/编辑/视图/窗口/帮助及全部子项）仍中文。改：桌面壳 menu builder 按界面语言取标签（zh/en 两套），监听语言切换事件即时 rebuild（无需重启）；首装无语言偏好时跟系统 locale。注意与 B69 菜单看门狗（menu-claim watchdog，1.0.55 已载）同文件域，改动合并提交防互踩。验收：EN 界面菜单全英文，切语言立即生效；B69 看门狗回归不破。

## 0825 验收派发记录（值守）

06:27（_475）应 skuio 主动请测，已派 1.0.55 六项实测清单：①B82 额度归零是否误暂停（其额度余 3%，天然测点，若暂停=B40 放开失效实锤）②B70 Messenger PIN 重连回填 ③B67 发→英含 AI 即时回复路径 ④B73 拦截原因横幅具体化 ⑤B74/B75 人设称呼考题 ⑥B41 多通道同客户不重复回。等回执（正常回序号打勾/异常截图+时间）。

## P1-9 残留（1.0.55 实锤，skuio 机 CUN2TM 诊断包，0825 06:38）

**结论**：B70 的 1.0.55 修复半生效——「弹窗避让 PIN 页」「auto-hide 遇 PIN 延迟」日志均可见已工作；但 **PIN 输入后 e2ee 会话仍未就绪**，失败链照旧：`e2ee pin prompt on screen` 反复在场 → composer 不可交互 → `composer still not cleared after retry` / `composer not found → re-navigate` / `elementHandle.type 超时`，`accepted:false`。
**关键分野**：同号同时段对普通会话发送全部成功（19:10-19:14 UTC 多条 verified:true）；失败全集中在 e2ee 线程（845305407858894 / 856185157054182 / 1536720154449497，其中 22:32 URL 显式为 /e2ee/t/）→ **排除 FB 风控**。
**修复落点**：①PIN 提交后等待 e2ee 会话就绪信号（PIN 页消失+composer 可交互）再放行发送，未就绪期间发送任务保留重试而非落失败；②查 PIN replay 在 1.0.55 是否真正执行（本包未见 replay 成功行，疑仍零重放）；③若 PIN 页短周期反复出现=浏览器档案未持久化 e2ee 设备密钥，查 sidecar profile 持久化。
**0649 升级（_485）**：其后 FB 对该号弹出「您被暂时阻止」（疑似滥用功能临时封锁）——时间线=反复重登+sidecar 无退避重试风暴之后，属可预期的风控反噬。修复加两条硬要求：④sidecar 发送重试加指数退避+上限；⑤页面检出 temporarily blocked/封锁类提示时立即停止该号全部自动动作并账号级冻结（接 ban_signal/kill_switch，messenger 面此前未接），冻结原因进拦截横幅。已教用户：停止重试、该号切手动、等数小时自动解除。
**用户侧**：已给 skuio 绕法（PIN 输完等聊天列表出现再关窗）并请其回报 PIN 页复现频率。证据：diag_CUN2TM（117 本机已解包 C:\Users\Administrator\diag_CUN2TM）。

## P1-15（B98，skuio 机 v1.0.55，LINE 入站不同步，待用户回报补定向）

**现象**：LINE 通道己方发送正常、客户入站消息工作台不可见（_483，vic001 会话）；前晚旁证 _443（02:51「收不到消息/能发出去」）。
**已发取证问**：手机 LINE 可见否（分流同步断/对方未发成）+最近入站时间点。
**修复线索**：①查 LINE 接入路径（line_protocol_login / line RPA sidecar）的入站事件订阅与镜像写入；②对照 22:30 窗前后是否有回归；③**诊断包缺口**：diag bundle 目前只带 msg/wa sidecar 日志，LINE 通道日志缺失——并入 B76/P2-m 的包内容修复（加 line sidecar/协议日志与主 app.log 尾部），否则 LINE 类问题永远无法远程取证。
**验收**：vic001 会话入站可见并实时更新；诊断包含 LINE 日志。

**P2-x（B100，skuio _487）**：AI 对客户的称呼镜像客户错拼（baba），未按跨平台档案「称呼」字段（babe）。与 P1-11（B74/75 AI 自称）不同向：此为客户侧称呼。改：①回复生成的 prompt 注入客户档案「称呼」字段并声明「称呼以档案为准，客户消息中的拼写变体不作依据」；②档案无称呼时才允许跟随客户用法；③可选轻量后置校正：出现档案称呼的近似变体时替换为档案值（编辑距离 1-2 且同词根，防误伤正常词）。验收：档案记 babe、客户打 baba，AI 回复称呼为 babe；档案无记录时行为不变。

## 待老板拍板增补（0825）：B101（让路线可调，skuio _491）

skuio 异议：让路线不应硬限，应「提醒+客户自主放开上限」。双面陈情：①用户侧——号归客户所有，节奏应客户掌控，硬限影响高峰期业务；②风险侧——上限按平台反机器人模型设定，放开=封号风险外移到用户（0825 其 FB 号因重试风暴被临时风控为同日实景）。**折中候选**：默认上限不变＋触达时横幅提醒＋用户点「知晓风险，今日放开」当日生效（次日回落）；或分档（保守/标准/激进）可选并显示各档封号风险说明。与在案 B34（额度自定义）同族，建议一并拍板。

**0756 skuio 补充论证（_493 原文要点，用户侧陈情增强）**：①本次 FB 风控主因是实时 PIN 环节反复重试（平台侧缺陷，非聊天量）——注：此点同时支持 P1-9 重试刹车修复；②各聊天工具封号机制不同，单一上限不能一概而论→若保留上限应按平台差异化；③号归用户所有，服务方只能建议不能替用户决定能不能发；④风险提示到位后，是否继续应由客户决定；⑤其立场：「限制客户使用的逻辑就是错的，我们是提供服务的，服务怎么用应该用户决定」。值守立场记录：**服务方=提醒+建议，使用决定权归用户**（供拍板参考）。

**P2-y（B102，钧 _495）**：人设「应用到…」弹窗账号默认列表漏运行时新增账号。根因：列表源=TelegramAccountRegistry.from_config（persona_routes.py ~1866，仅 config 声明账号），协议登录新号在 account registry（SSOT）不被读取；而绑定写接口 api_account_persona_set 本就走 registry。改：账号列表改为 config+account registry 在线账号合并去重（platform+account_id 键），显示昵称+平台徽标；Messenger/WA 侧同查同修（mrpa/wa 列表同文件同模式）。验收：钧机新加 TG 号出现在弹窗列表并可指定成功；「当前服务 N 账号」计数含 registry 绑定。

**P2-z（B103，钧 0826 需求，03:02 范围修正：UI 不做，做 AI 回复语言）**：AI 回复语言增加繁體中文（zh-Hant）与粤语（yue，聊天口语向 係/唔/嘅）。改动：①回复工坊语言选择器加两项；②lang_policy：normalize 支持 zh-Hant/yue，证据识别加繁体字形与粤语助词判定；③ai_client _LANG_NAMES 补两项+对应指令行（繁體书面/粤语口语，避免播音腔）；④外发翻译（发→X）目标语言加两项；⑤粤语 TTS 是否可用另行确认（供应商能力），不可用则语音维持粤语文本+现有音色并在 UI 注明；⑥**地区语感自适应（B112）**：人设档案加「地区」字段（或复用现有背景字段解析），地区=台湾时提示词自动注入台湾口语指令+用词映射表（喔/欸/齁 语尾、影片/传讯息/超~的、避大陆网络语）+3-5 条例句 few-shot；地区=香港时同理挂粤语口语（与 yue 选项联动）；⑦验收增补：TW 人设 10 条回复中语尾/用词抽检达标，B105（钧的小柔语气案，挂起中）以此复测关单；⑧**扩围（钧 0826 07:00「全地区自适应，否则都像大陆语气」）**：地区语感做成可扩档案体系——persona.region 驱动，首批 TW/HK（配 yue）/马新 三套（语尾+用词表+例句），**默认档改「地区中性」**（避免大陆网络语外溢到任何地区客户）；非中文语种客户按对方语言回，地道度纳入同专项压测。交付顺序：繁体+TW → HK/粤语 → 马新。验收：会话锁繁体→AI 全繁体；锁粤语→口语粤文；客户发繁体/粤语时证据链能识别并跟随。原 UI 繁体包需求作废（用户明确不要）。

（原方案备档：方案：①构建期用 OpenCC（s2twp 台湾正体+惯用词）从 zh 包自动生成 zh-Hant，人工只维护一张术语覆盖表（設定/訊息/影片/登入/上載→上傳 等，按需增补）；②语言切换器加「繁體中文」，i18n 键回落链 zh-Hant→zh→en；③原生菜单（P2-w）与 SOP 模板（P2-r）双语化时同步带繁体一份。验收：切繁体后主要页面（工作台/人设工作室/客户列表/设置）无简体残留（覆盖表未及的可后补），回落不出英文。注意与 B97/B91 同批做省一次全量扫描。）

**P2-aa（B104，钧 _504）**：AI 对新联系人虚构既往对话（「你之前不是说想来台北玩嘛」，上文无出处）。改：①生成守卫——回忆类句式（你之前说过/上次你说/你不是提过）仅当会话历史或注入记忆确有对应内容才允许，prompt 加硬约束；可选后置检测（正则命中回忆句式→校验上文/记忆出处→无出处改写为开放问句「你有没有想过来台北玩?」）；②跨平台档案与 AI 记忆隔离核查：确认新联系人不会被错误关联到他人档案（关联仅凭什么键?人工确认门槛?），错关联时记忆注入=合法串话。**分流已回（03:30）：全新联系人**→主修①生成守卫（纯幻觉实锤）；②隔离核查降为顺带自查（本例非串档，但关联确认机制仍值得查一遍）。验收：新会话无出处时不出现回忆句式；档案关联有确认机制。

**P2-ab（B106，钧 _514/_516，微文案）**：台词库被误解为「AI 话术学习库」（导入 50 条问为何不触发）。改：①备货面板台词库行加一句用途说明「预录语音提速用：夜间把固定短句预合成人设音色，AI 恰好说到且以语音发送时秒发；不影响 AI 说什么」；②台词库编辑页顶部同款说明+指路「想让 AI 学话术→知识库」链接；③命中/缺口数据在 AvatarHub 语音卡已有，备货页台词库行可附「已命中 N 次」小字（读 avatar_voice_stats）拉近感知。验收：新用户读一遍说明能分清台词库 vs 知识库。

**P2-ac（B107，skuio _521，产品建议）**：AI 自动回复支持选择性引用回复。实现：①决策层——回复管道输出可选 quote_target_msg_id，触发条件：待答消息非最新入站（被后续消息隔开≥2 条或时隔≥N 分钟）/客户连发多问只答其一/话题切换后回旧题；连续对话不引用（防机器人感）；②发送层——unified-inbox send 已有 quote 参数，TG/WA 原生带 reply_to；Messenger sidecar 需实现 hover-reply 模拟（成本中，先做 TG/WA，Messenger 二期）；LINE 能力先查协议支持；③灰度：先在 B 线/内测账号开。验收：客户连发三问 AI 分条答时各自带引用；正常对话不出现引用。

**P2-ad（B108，钧 _523）**：AI 同会话口头禅复用（被你抓到啦/洗耳恭听）。改：回复生成 prompt 注入「本会话最近 N=8 条 AI 消息的开场句+收尾句摘要」并要求避开同款句式（dup_guard 仅拦全同文本，不覆盖句式级复用）；可选：persona 风格里的示例句标注「示例勿照抄」。验收：同会话连续 10 条 AI 回复无重复开场/收尾句式（人工抽检）。附 B109 UI 观察项：目标卡「AI 做了什么」行加最近动作示例（如「刚在 04:13 问了对方称呼」），提升目标执行感知。

## P1-16（B54 并案升级，skuio+钧 双报告人，v1.0.55 仍现）

**现象**：输入框间歇性不可输入（skuio：人设编辑页偶发无光标 _291；钧：主动关怀页+「软件经常性」，退出重进偶可恢复 _526）。跨页面+重启自愈=全局焦点/状态问题，非单页字段 bug。
**修复方向**：①排查 Electron webview 焦点竞态：弹窗/overlay 关闭后焦点未归还、Shadow DOM 焦点重定向穿透（B19 同族）、人设编辑器双状态机（B47 同族）遗留 disabled/readonly 残留；②**通用自愈兜底**：全局委托监听——输入框 click/focus 时校验 document.activeElement，不符则强制 refocus 并清除异常 disabled/readonly/pointer-events 残留；③埋点：自愈触发时记一条 console/日志（带页面+元素 id），累计数据定位元凶页。
**复现路径（0826 05:29 _530，钧）**：主动关怀页 →「跳过关怀记录」弹窗 →点「取消」→ 页面输入框全部点不进。首查该弹窗 cancel 分支：焦点陷阱（focus trap）未释放/透明遮罩未移除/aria-hidden 残留在主容器。同款弹窗组件全局排查一遍。
**验收**：按复现步骤操作后输入框即点即输；复现期一周内两位报告人不再遇到，或遇到时点击输入框即恢复（自愈生效）；日志可见自愈触发计数与分布。

**P2-ae（B110，钧 _527，话术质量专项，与 P2-ad 同批）**：主动关怀生成话术死板（通用抒情模板感）。改：①关怀生成 prompt 必带该客户具体锚点（客户档案字段+最近聊天要点提取：工作/宠物/上次提的计划），要求至少指涉一个具体事实，无锚点时改用「场景+轻问题」而非空泛想念；②结尾带引导回复的轻问题（钩子），禁单向抒情收尾；③按关系阶段（初识/试探/稳定陪伴）分档语气模板；④注入最近 N 条已发关怀句式做防重（与 P2-ad 复用同机制）。验收：同客户连续 5 条关怀无重复句式、每条至少一个具体指涉；钧实测认可效果。

**P2-af（B111，钧，资料收集目标增强）**：收集节奏保守+无结构化清单。改：①目标模板库加「客户资料收集」预设：勾选字段（称呼/年龄段/城市/职业/联系时段/来源），生成的目标文本自动带「一次问一项、聊得顺直接问」指令；②进度改为清单覆盖率（已收集 n/m 项，字段命中从客户档案回写判定）；③节奏自适应：对方回复积极（短间隔+长回复）时提问频率上调一档；④收集到的字段自动写客户档案对应栏（与 P2-p 显示链衔接）。**增补（钧 06:43 主张）**：⑤基础信息收集默认化——新客户前 N 轮（建议 N=10）无目标也默认渐进收集轻信息（称呼/来源/城市，一次一项、节奏保守），全局设置可关；目标存在时按目标清单加速。默认行为变更，落地前过产品确认。
验收：勾 4 字段建目标，热络会话内 AI 逐项自然问完并回写档案，进度条随之走到 4/4；默认化开启时新客户 10 轮内至少自然获取称呼并入档。

---

## 0826 未修派工（值守整理，执行线认领）

未修项已抽成独立施工单，避免在本文 500+ 行里翻：**见《实施68_0826未修派工_给117》。**
原则：1.0.52~1.0.55 已落码的 P0/P1-1~11 勿重做；本批按实施68 §1 顺序修 P1，§2 顺手 P2，§0 待拍板不动手。
新案 B113（全自动开着不回，_543）一并进 §1。

### 实施68 §1 施工状态（执行线 117，2026-08-26 批次回写）

按落码文件 + 门禁 + 装包依赖逐项记。**引擎 .py 走 22:30 窗重启装载；模板/i18n/CSS 热更即上；
边车/壳侧改动随下个客户包**。

- **P1-13 / B88 群线程冻结 —— 落码**：① `inbox/store.py::max_numeric_platform_msg_id`
  （镜像最大数字 id）；② `protocol_bridge.py::probe_fill_tg_gap`（newest-first 拉到接上镜像即停，
  touch cap 标 capped）；③ `unified_inbox_account_routes.py::maybe_probe_tg_thread_gap`
  （per-cid 120s 冷却 + running 单飞 + 严格 client 路由防串号 + 让路账号级/全量同步）+
  `POST …/gap-probe`（手动重试豁免冷却）；④ `/thread` 端点接线，响应带 `gap_probe`；
  ⑤ 前端 `loadThread` 消费 gap_probe（补拉进新行 2.5s 快刷一拍）+ renderMessages 顶部
  error/capped 横幅（禁静默）+ `retryGapFill`。⑥ 筛选片 × 修复：事件委托 + 整片可点
  （`.fs-chip` 整片 clear，命中区从 13px 变整片），修「单片 × 删不掉、清空有效」。
  门禁 `tests/test_tg_gap_probe.py`（42 例含相邻回归）；CSS `?v=20260826a` + ui-build `20260826-1200`。**引擎 .py 需重启**。
- **P1-14 / B95 管理页整组弹回 —— 落码**：根因两半＝会话中途过期重登后回跳目标被丢弃。
  ① 壳 `main.js::bindBackendPopupLogin` 优先读 `/login?next=`（真实目的地）、重登可多次
  （8s 冷却 + 3 次上限，token 失效不无限打转）；② `renderer.js` 工作台 webview 登录链同步
  next 优先；③ 服务端 `_loading_overlay.html` L2 遮罩超时散开时出显式报错条（`ldov_navfail`
  + 重试，禁静默弹回）。门禁 `desktop/test/backend-popup-login.test.js`（12 例，已挂 npm test）。
  **壳侧随包**；`_loading_overlay` 热更即上。
- **P1-16 / B54 输入框点不进 —— 落码**：Electron 原生 confirm/alert 关闭后焦点态失步。
  ① `main.js::desktop:focus-fix`（blur+focus 复位，webview 宿主窗兼容）；② `popup-preload.js`
  新建（后台弹窗此前零 preload=confirm 病灶主场）+ `inbox-preload.js` 同名桥 `__chatxShellFocus`；
  ③ `openBackendPopup` 挂 popup-preload；④ 服务端 `_focus_selfheal.html`（壳内包 confirm/alert/
  prompt 返回即复位 + 全局点击自愈兜底 + `/api/telemetry/frontend-error` 埋点），base.html/
  workspace_base.html 双基座 include；`frontend_error_stats.py` 白名单加 `focus_selfheal`。
  门禁 `desktop/test/focus-selfheal.test.js`（11 例，已挂 npm test）+ `test_frontend_error_stats`。
  **壳侧随包**；partial 热更即上。
- **P1-12 / B86+B81 待审计数有列表空 —— 落码**：计数（store 按 cid 直查）与面板列表
  （全平台前 10 条再前端筛）口径断裂，平台积压 >10 挤出本会话稿。① `DraftService.list_drafts`
  加 `conversation_id` 会话级过滤；② `/api/drafts` 路由透传（旧 store TypeError 回落）；
  ③ 收件箱面板按 conversation_id 取数（limit 10→20）；④ 体检「打开待审队列」直达全局
  `/workspace/drafts`（旧行为=当前会话小面板，正是空屏点）。门禁
  `tests/test_draft_count_list_same_source.py`（含「计数真列表空」反面钉）。**引擎 .py 需重启**。
- **P1-9 残留 / B70+B99 Messenger PIN 闭环 —— 落码**：① PIN 浮层在场发送快速失败
  （503 e2ee_pin_pending，别当 DOM flake 撞）；② replay 全程 INFO 日志锚点（CUN2TM「零
  replay 行」无法区分没执行/没记）；③ profile 已 launchPersistentContext 持久化（核实无需改）
  + 恢复后 24h 内浮层复现打 WARN（设备密钥持久性存疑留证）；④ 连败指数退避
  `msg_ops.js::sendFailureBackoffMs`（5s→…→5min，成功清零）+ 发送闸；⑤ 临时封锁检出
  `login_classify.js::isTemporarilyBlockedText` → 冻结出站 + postStatus("blocked") → Python
  `session-status` 接 `ban_signal.apply_action(pause 2h)`（`blocked` 入 UNHEALTHY_STATUSES）。
  门禁 `services/messenger-web/{msg_ops,login_classify}.test.js`（74 例）+
  `tests/test_messenger_blocked_signal.py`。**边车随包 + 引擎 .py 需重启**。
- **P1-15 / B98 LINE 只出不进 —— 落码**：① 诊断包补 LINE 协议 transcript
  （`diag_upload.py::line_diag_files` → `sessions/_diag/login_*.log` 进 `logs/line/`，全尺寸+mini
  包都带）——LINE 类问题此前无 LINE 日志无法远程取证；② LINE worker 入站活性戳
  （`account_orchestrator.py` LineProtocolWorker：`recv_started_ts`/`last_inbound_ts`/
  `inbound_count`/`thread_alive` 进 status），把「登录在收不到」从无据可查变可读数
  （okline Bot.run reconnect 假活的判据）。门禁 `tests/test_line_inbound_diag.py`。
  用户线索齐后按活性戳定向。**引擎 .py 需重启**。
- **B87 手机删消息同步 —— 落码**：① `store.soft_delete_by_platform_msg_ids`（按 platform_msg_id
  跨会话软删，本账号限定、chat_key 可收窄）；② `protocol_bridge.report_deleted_messages`
  （软删 deleted_by=peer + 发 messages_deleted SSE）；③ `telegram_client.py` RawUpdateHandler
  接 UpdateDeleteMessages/UpdateDeleteChannelMessages；④ 前端 messages_deleted 消费兼容
  peer_delete（platform/account/chat_key 无 cid 匹配）；⑤「不主动提已删」＝`persona_reply.py`
  话题锚点查询改 include_deleted=False。门禁 `tests/test_peer_delete_sync.py`。**引擎 .py 需重启**。
- **B82 无限额顶栏倒计时 —— 落码**：`hosted_gateway.py::quota_probe` 识别无限额
  （官网 `unlimited` 位 / budget<=0）→ 归一 unlimited、绝不判用尽；前端 `_renderQuota`
  unlimited 时不算百分比/不显徽章/不弹「仅剩 X%」预警。门禁 `tests/test_hosted_gateway.py`
  （+4 例）。**引擎 .py 需重启**（前端热更即上，但数据源在 .py）。
- **P1-17 / B113 全自动开着不回 —— 核查+落码**：核查结论＝关系进阶确认（relationship_stage
  的 needs_confirmation/pending_advancement）是**纯展示层，不 gate 回复生成**（回复链从不读
  它）→ 「初识 100%+确认进阶」非不回原因。落码＝`reply_diagnosis.py` 新增 `customer_waiting_no_gate`
  诊断档：无 block + 无待审草稿 + 末条入站超宽限（600s）→ 明说「没闸拦下它、这条确实还没回，
  在途稍等或人工推一把」，把「一片绿一头雾水」变成定向结论。门禁
  `tests/test_reply_diag_customer_waiting.py`。个案仍等 `_543` 待发队列/日志定向。**引擎 .py 需重启**。
- **B80 镜像重复 —— 落码**：根因＝`msg_ops.js::synthMsgId` 吃 aria 时间文本，Messenger
  同消息时间表述跨日漂移（14:44→昨天 14:44→July 24…）→ id 变 → 重复落库。修＝
  `normalizeTsLabelForId` 归一到稳定时钟（剥相对日期前缀，只留 HH:MM[:SS] + AM/PM），
  跨日重拉同 id。门禁 msg_ops.test.js。**边车随包**。
- **B85 边车 stale click —— 落码**：`server.js::/send` 的 composer 点击加 re-resolve
  （`elementHandle.click: not attached to DOM` → 重新 waitForSelector 一次再点，仅重解元素
  引用不重发消息），而非让 stale 异常冒泡成 500。与 B70 PIN 态并存（PIN 态另有快速失败）。
  **边车随包**。

**收口回归**：本批新增/改动门禁 184 例（Python）+ 桌面壳 npm test 全绿 + messenger-web 48 例；
gate_sweep 收口 3 红中 2 红（ui-build 新鲜度 / 路由清单）为本批引入已即修（重 bump `20260826-1310`
+ 登记 gap-probe 路由），剩 1 红＝ops_overview 5 CJK（xiaozhi-obs 线在册既有债 74h，非本批）。
§2 P2 顺手项与 §0 待拍板项本批未动。

### 实施74 批施工状态（执行线 117，2026-08-27 午后回写；唯一进度记录见实施74 文档）

§2 未动项 + 后升级项 + 实施69 P1 的二次派工批，13 项全部落码、批内 14 个门禁文件
连续两轮 141/141 全绿。逐项：

- **B118 落码**：生成端硬禁 `persona_manager._INNER_MONOLOGUE_BAN`（full+compact
  双模式）+ 出稿守卫 `src/ai/outbound_text_guard.py::sanitize_inner_monologue`
  接 skill_manager A 线 5c1b / B 线 9a2。门禁 `test_outbound_text_guard.py`。**需重启**。
- **B121 落码**：同模块 `detect_lang_mix`/`strip_minority_script`（拉丁主体夹 CJK=hard
  剥除；中文夹整句英文=soft 只观测）。同上门禁。**需重启**。
- **B120 落码**：`voice_autosend.global_voice_reply_off`（显式 false 才全局禁声）→
  B 线原生同键 + proactive `_try_send_voice`（skip 原因 global_gate）+ A 线
  `sender._maybe_send_voice_reply`；手动链刻意豁免（反向门禁钉死）；RPA voice_output
  平台链不透传（备案）。门禁 `test_voice_global_gate.py`。**需重启**。
- **B119 落码**：`GET/POST /api/personas/selfie-gate`（persona_media_routes，读真值/
  写 overlay+审计）+ personas.html 相册 CapBar 总闸状态行/一键开启（热更已生效）+
  英文要图词回归钉（"send a previous selfie" 词表已命中）。**端点需重启**。
- **B117 落码**：ai_client 媒体块 kind 分流——sticker/animated_sticker/gif 改情绪
  回应指令（禁照片式评论），image/video 不动。门禁 `test_media_kind_split.py`。**需重启**。
- **B122 热更已生效**：dpick 弹窗「整段填入」（`_dpickJoinWhole` 标点连排 +
  `draftPickFillWhole` 已挂 window）；旧按行填入与出站 split_send 不动。
  门禁 `test_draft_full_text.py`。
- **B123 落码**：`src/ops/bug_intake_backfill.py`（seen 账本差集回放/首见群不回放/
  cap 不跳档）+ `telegram_client._bug_intake_backfill_loop`（300s 周期）；
  bug_intake.py 本体零改动（避让 xiaozhi 报障闭环批）。门禁
  `test_bug_intake_backfill.py`。**需重启**。
- **实施69 P1-1 落码**：store 迁移 `conversation_meta.handoff_meta` + tag_needs_human
  存 {reason,ts,source} + chats 行下发 + 行 chip 悬停自解释（8 原因键 zh+en）+
  摘标同清元数据。门禁 `test_handoff_tag_meta.py`。**store/打标链需重启**（chip 热更已生效）。
- **实施69 P1-2 热更已生效**：右键菜单「打标签」下平铺当前标签（cap 4）× 直摘
  （复用 `_saveConvTagsFor`，埋点 tag_ctx_remove）。
- **实施69 P1-4 落码**：watchdog `_check_messenger_code_stale`（worker 自报持续
  ≥30min → notify_host，恢复补绿窗；配置 `health_watchdog.code_stale_remind` 默认开）。
  门禁 `test_code_stale_watchdog.py`。**需重启**。
- **实施69 P1-5 判定等价已覆盖**：三态确定性文案（worker 宕机 502/无档案 404/成功
  开窗驻留）由 0827 chandown 线 ③ + 实施69 P0-2 达成，`test_relogin_triage.py`
  钉等价防回退，本批不重做。
- **B115 热更已生效**：cp-voice 会话档撤录入/登记整块（含死代码 prefillEnroll），
  登记单一入口=人设页 mode="enroll" 档；`?v=20260827c` 三处 + 双树镜像同步。
  门禁 `test_toolbox_enroll_removed.py`。
- **P2-h 热更已生效**：workspace_base 新增 1120px 中宽断点（英文/壳窗溢出档：
  导航图标化/三药丸收图标/额度徽章限宽）——收文字不收入口。

装包依赖：本批**无壳/边车改动**，全部引擎侧（.py 走重启窗）+ 热更面。
ui-build 终值 `20260827-1355`。未进本批的长尾 P2/待拍板项见实施68 追加段 §B。

