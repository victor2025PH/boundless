# 指令 N-2：人设工作室 1.0.76 回归三件——备份恢复报错 · 克隆音色登记反馈 · 相册 AI 识别未触发（#237 #239 #238）

> **预算硬上限 $150。** 正常花费 $80–120。花到 $130 停下写落点表 + 总账。
> 类型：**人设工作室前端 + 两个路由。** 证据 `tmp_diag\{RN2SKR,MJGHKQ,8CAJWA,R8ECFS,QENDQ2,F35X38}\`，不必再取证。
> 为什么 #237 是 P1：skuio 09-07 13:14 **卸载重装** 1.0.76（人设池 runtime=0），点新版「人设备份与迁移 → 从备份恢复…」选卸载前 13:01 导出的 `chatx-personas-20260907-1301.json`，页面报 **「请求失败 _esc is not defined」**——L-2 E 09-06 刚上线的功能，第一次真用就败，他只能手工重建 Mizuki / STEVEN。备份恢复是「丢数据前最后一道保险」，这种回归比任何 UX 都重。#239 / #238 是重建过程里立刻撞上的两件：克隆音色登记后没反馈、144 张相册 AI 识别 0 张（`autotag=False`，识别根本没跑）。

---

## 0. 开工前必读

1. `docs/修复指令_N批_并行_2026-09-07.md`：文件归属 / D-N3；与 N-4 的边界（本条做语音 tab **登记反馈**，N-4 做**体检数据回填**——落点表互指）。
2. `docs/发版对账_v1.0.75_L2.md` E（备份与迁移三按钮：`GET /api/personas/export` / `POST /api/personas/import?dry_run=1`；导入前预览「新增 N · 覆盖 N」）与 A（语音三态 + 登记克隆音色）；`docs/发版对账_v1.0.74_J7.md` B（相册 tab 重写：`auto_meta.triggers_suggest` / 「🤖 AI 补标」`retag-all only_missing` / 上传完无触发词 → 自动切筛选 + toast——**F35X38 说 1.0.76 上传后没有任何引导**，先核这段还在不在）。
3. 代码位置：`personas.html`（L3743 `function _esc(v)` 是模板内函数；备份恢复块若在 `static/js/persona_studio_core.js` 里调用 `_esc` 就是作用域问题）、`src/web/routes/persona_routes.py`（import/export）、`persona_media_routes.py`（上传 `autotag` 参数 / `retag-all` / 打标任务）、相册 VLM 打标任务（走 `hosted_gateway` 识图，L-6 A 已修 num_ctx）。

**读完直接从 A 开工。**

## 1. 红线与边界

- 提交只用 `git add <明确文件>`；提交前 `git diff --cached --name-only` 为空再 add；⛔ `git add -A` / `-a` / `reset --hard` / `checkout .` / `restore .` / 裸 stash。
- `personas.html` 先 Grep 后读窗口，**只动**：备份恢复块 / 语音 tab 登记反馈 / 相册上传引导三处。热更新即上生产，中间态自洽。
- 不重做导入格式；不改 TTS 判定（N-4）。

开工登记意向：
```
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "N-2 persona studio regressions: restore _esc scope + receipt (#237) / clone-voice register feedback (#239) / album autotag default + progress + guidance (#238)"
```

---

## 2. A —— 备份恢复 `_esc is not defined`（#237，P1）

**修法**：
1. 定位调用点（renderer 日志 `ReferenceError: _esc is not defined`）：恢复流程的预览 / 结果渲染在哪个作用域引用 `_esc`——若在 `persona_studio_core.js`，改用该文件已有的转义 helper 或显式传入；补 `tests/test_template_undefined_identifiers.py` 同类门禁覆盖 static JS 对模板函数的引用。
2. 恢复流程端到端跑一遍：导出 → 清空（测试库）→ 从文件恢复 → 预览「新增 N · 覆盖 N」→ 确认 → 回执「已恢复 N 个人设（含相册引用 M 条）」；失败时错误人话 + 不丢原文件。
3. 核 13:01 导出文件格式与 1.0.76 导入端契约一致（版本字段 / 相册引用路径）；卸载重装后 `personas.yaml=0 runtime=0` 时导入必须能走。
4. **给 skuio 的补救**：他手里有 `chatx-personas-20260907-1301.json`——修完让他再恢复一次（回访口径交 N-5），重建的 Mizuki / STEVEN 与备份重名时按预览选覆盖或保留。

**验收**：`tests/test_persona_restore_237.py`：空库导入成功 + 回执；重名预览；坏文件人话报错。

## 3. B —— 克隆音色登记后反馈缺失（#239，P2）

**事实**（R8ECFS）：后端 13:50:31–13:51:47 `backend=avatar_clone voice=STEVEN` 四次合成、`[voice/tts-test] 有声终审 speech=voiced energy=ok`——**后端全部成功**；界面：toast「克隆音色已登记并挂载到本人设: avatar_clone」约 1s 消失且含内部名；登记后「试听」按钮消失、四段试听音频无处播放；「音色体检：暂无体检数据」未回填；底部仍「有未保存的修改」，登记与保存关系不明。

**修法**：登记成功后常驻一行「已登记 STEVEN.ogg · ▶ 试听 · 解绑」；toast ≥5s 可关闭、去内部名 `avatar_clone`、内嵌 ▶；登记即持久化（或明确「登记已生效，其余改动需保存」）；试听终审结果回填「音色体检」由 **N-4** 做（本条只留占位与数据钩子，落点表互指）。

## 4. C —— 相册上传 AI 识别 0/144（#238，P2，D-N3）

**事实**（F35X38）：13:31 上传 144 张，每条 `[pmedia] 上传 … autotag=False`；至 13:44 零条补标 / vision / caption 任务日志——**识别根本没跑**；页面「144 个缺触发词」「AI 已识别 0/144」却无任何引导；「锚脸基准照 尚未设置」同样无引导。

**修法**：
1. 上传默认 `autotag=True`（D-N3）+ 进度条「AI 识别 N/144」；vision 不可达 → 明确报错「识图服务暂不可用，稍后点 AI 补标」而不是显示 0。
2. 上传完成若仍有缺触发词 → 弹引导并内嵌「AI 补标」按钮（J-7 B 的自动切筛选 + toast 若已丢失则补回）；补标点击落日志、有进度、结果可审改。
3. 首次上传引导设置锚脸基准照；完整度计算纳入「相册未打标」。

**验收**：`tests/test_album_autotag_default_238.py`：上传默认触发打标任务；不可达报错文案；引导出现条件。

---

## 5. 收工

- 落点表 `docs/发版对账_v1.0.77_N2.md`；总账 **N-2 行**。**每个 commit 落地后立刻补落点表一行。**
- **交 N-4**：B 项体检回填钩子名。**交 N-5 回访口径**：#237「备份恢复可用，请用 13:01 那份再恢复一次，重名会让你选」/ #239「登记后常驻显示已登记 + 可试听 + 可解绑」/ #238「上传即自动识别并显示进度；识不了会明说」。
- 收工清意向板 `-Done`。

**开对话粘这段：**

```
执行 N-2（人设工作室 1.0.76 回归三件：#237 备份恢复「_esc is not defined」+ 恢复回执 + 导出格式核（P1）/ #239 克隆音色登记后反馈缺失 / #238 相册上传 AI 识别未触发 → autotag 默认开 + 进度 + 不可达报错 + 补标与锚脸引导）。先读 D:\boundless\docs\N批_进度总账.md 与 D:\boundless\docs\修复指令_N批_并行_2026-09-07.md（决策 D-N3），再读 D:\boundless\docs\指令_N-2_人设工作室回归三件_2026-09-07.md，按 A → C → B 顺序做。证据 tmp_diag\RN2SKR MJGHKQ R8ECFS F35X38，不必再取证。personas.html 只动三处、先 Grep 后读窗口；体检数据回填归 N-4。每项一个 commit 带单号；每个 commit 后立刻补落点表一行 docs/发版对账_v1.0.77_N2.md；预算上限 $150。
```
