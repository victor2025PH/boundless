# 发版对账 v1.0.77 — N-2 人设工作室 1.0.76 回归三件（#237 备份恢复 · #239 克隆音色登记反馈 · #238 相册 AI 识别）

> 指令：`docs/指令_N-2_人设工作室回归三件_2026-09-07.md`；底座 `docs/修复指令_N批_并行_2026-09-07.md`（D-N3）。目标版本 **1.0.77**。
> 证据：`tmp_diag\{RN2SKR,MJGHKQ}`（#237）/ `{8CAJWA,R8ECFS}`（#239）/ `{QENDQ2,F35X38}`（#238），未再取证。
> 预算 $150；执行方 N-2 对话，09-08 00:47 开工（`agent_probe -Intent` 已登记）。顺序 A → C → B。
> **纪律：每个 commit 落地后立刻补本表一行（M-7 教训）。**

## 一、改动落点（每项一个 commit，均带工单号）

| 项 | 工单 | 症状 | 修法 / 落点 | commit | 验收 | 装载 |
|---|---|---|---|---|---|---|
| A | #237 P1 | 卸载重装（runtime=0）后「从备份恢复…」选 13:01 导出的 `chatx-personas-20260907-1301.json` → 页面「请求失败 _esc is not defined」；后端零导入日志、renderer 无错 | **真因**：`personas.html` 备份恢复预览渲染（L-2 E）直接调 `_esc(...)`——那是 `_exportFilteredCSV()` **体内**的 CSV 转义函数，全局作用域够不着 → ReferenceError 被 `catch` 吞成「请求失败 …」。注意它在 `await r.json()` **之后**才炸：dry_run 请求其实已发出（后端 dry_run 不落 INFO 日志，所以 MJGHKQ 看到「零请求」），预览永不出现、确认无从点起。**同根**：L-2 C「基础 tab 粘贴 JSON → 解析并预览」（`_importFromJson` / `_jiShowUndoToast`）12 处同样调 `_esc` → 1.0.76 该按钮一点也炸，尚无人报，一并改（见 §三越界说明）。① 24 处 `_esc(` → 页面全局 `_escHtml(`；② `_pbFail(where, e)`：人话（「核对备份失败 / 恢复未完成：{msg}。备份文件没有被改动，可重试」）+ `console.error('[persona/backup] …')` 进 renderer.log + `ReferenceError/TypeError/SyntaxError` 判为页面内部错误 → 文案「这不是备份文件的问题，请把这句话发给技术」+ `_feBeacon('pb.preview|pb.restore')` 归因遥测；③ 回执 `_pbReceipt`：「已恢复 N 个人设：新增 A · 覆盖 B（，含相册引用 M 条）（，跳过 K 条无法识别）」状态行 + 8s toast，M 由备份文件 `album_refs` 计；失败时 `_pbPending` / 预览框保留，直接再点「确认恢复」即重试；④ **重名预览**：`persona_routes` import dry_run 新增顾问字段 `name_clashes:[{id,name,existing_id}]`（去空白 + 忽略大小写同名、ID 不同、且不在覆盖集）——手工重建的 Mizuki 若 ID 与备份不同，预览红字列出「恢复后会并存两份，需要的话恢复完再删掉多余的那个」；同 ID 走既有覆盖预览；⑤ **导出格式核**：1.0.76 导出信封 `{format: chatx-personas-backup, version: 1, exported_at, count, profiles, album_refs}`，`pbPickRestore` 接受「数组」或「信封.profiles」，`album_refs[*].file` 只是文件名不含机器路径，整份信封贴回 import 只读 `profiles` 键——**契约一致，不重做格式**；空库（`list_profile_ids()==[]`）dry_run→import 全程可走。⑥ 门禁：`test_template_undefined_identifiers.py` 新增 **第二形态探测器** `scan_nested_scope_leaks`（只以嵌套 `function NAME(` 声明、却在声明所在顶层块之外被读取 → 必然 ReferenceError；用「顶层块」而非「最近块」当可达范围，避 Annex B 提升误报；形参/裸赋值/typeof/window 访问/环境全局全部放行）——对 HEAD 版 `personas.html` 报 `_esc: 24`，修后全站 0 命中；i18n 9 键 ×2 | `9c72cdd2` | 新 `tests/test_persona_restore_237.py` 5 例（空库恢复 + 回执字段 / 重名预览同 ID 覆盖·同名不同 ID 顾问 / 坏文件人话 + `console.error` + 内部错误归因 / 导出信封契约 + 无机器路径 / 恢复块与 JSON 导入块只用 `_escHtml`）+ `test_template_undefined_identifiers` 10 例（含探测器自证 #237 形态 + 零误报形态）+ `test_persona_backup_202` 7 例 = 22 passed；`test_i18n_coverage` / `test_i18n_extra_langs` / `test_persona_json_import_guard` 337 passed（1 处 pre-existing 失败见 §三）；`node --check` 三个脚本块语法通过 | 模板 / i18n 热更新已生效；`persona_routes.py` 待装载（只加 dry_run 顾问字段，旧前端忽略） |
| C | #238 P2（D-N3） | Mizuki 13:31 上传 144 张，每条 `[pmedia] 上传 … autotag=False`，至 13:44 零条补标 / vision 任务日志——识别根本没跑；页面「144 个缺触发词」「AI 已识别 0/144」无任何引导，「锚脸基准照 尚未设置」同样无引导 | **真因两层**：① `media_auto_tag.resolve_album_ai_cfg` 的 `enabled` 默认 **False**（「新子系统按约定默认关」），客户机配置没显式开 → 上传不排打标；② 打标模块出于隐私只认**私网**端点（`_is_private_url`），而外网客户机的识图是 `hosted_gateway.ensure_hosted_vision` 注入的官网网关 `https://bd2026.cc/api/ai/v1`（域名一律判公网）→ `_lan_vision_cfg` 返 None → 即便手点「AI 补标」也会 144 条全部 `failed(vlm_unavailable)`。**修法**：`media_auto_tag`：`enabled` 默认 True（显式 false 仍受尊重）；新 `_is_hosted_gateway_url`——只在 `vision._hosted_vision` 标记在场且 URL 正是注入那条（== `base_url` 或 env `AITR_HOSTED_VISION_BASE_URL`，https + `/api/ai/v1`）时放行，其他公网域名（含 siliconflow 云）照旧拒绝——网关后面仍是自家 176/140，不违「真人素材照不出自家机器」；新 `vision_probe(vision_cfg)`（**不发网络**）→ `{ready, reason: no_endpoint / client_init_failed / unreachable}`；**熔断**：`_vision_describe` 遇端点循环全败（`client.last_fail` 非空）或异常 → 90s 内后续任务直接 `failed(vlm_unavailable)`（单张转码失败 / 模型空答不熔断），否则 144 个排队任务会在单工位把 60s 超时跑满两个多小时、期间补标也排不进；`run_batch` 回 `vision_ready / vision_reason`，打不动时资产补齐照做、打标 `queued=0`。`persona_media_routes`：上传想打标先探针，打不动**不排必败任务**、条目留 untagged（补标 `only_missing` 能捞回）、回 `autotag / vision_ready / vision_reason`，日志改 `autotag=%s vision=ready|<reason>|off`；列表 `ai` 块加 `vision_ready / vision_reason / last_error / auto_on_upload`；补标点击落 `[pmedia] 补标 pid=… queued=… vision=…` INFO（F35X38 无法判断用户点过没有）。`persona_completeness(persona, album={total, with_triggers})`：有相册却零触发词 → `album.triggers`（权重 4）进 missing、分数下调，≥90% 有触发词满分、部分半分；无相册 / 不传不参与；`persona_routes` 列表 **peek**（不懒建）媒体库回填 `album_total / album_no_triggers / album_untagged` + 相册版 `completeness`。**前端**（相册 tab，三处之一）：`_pmaRenderGuide` **五态常驻引导条**——进度条「AI 识别中 done/total（还剩 N 张）」/ ⛔「识图服务暂不可用（未配置可用的识图端点｜刚刚连接失败｜识图客户端启动失败），N 张没有自动识别。服务恢复后点「AI 补标」」+ 内嵌按钮 / ⚠「N 张识别失败（识图服务不可达｜结果无法解析）」+ 重试 / ✨「N 张还没有 AI 识别」+ 内嵌 AI 补标 / ⚠「N 张缺触发词：看卡片 ✨ 建议词点即采纳」+ 只看缺触发词 / 🧑「锚脸基准照尚未设置——设好后 AI 识别会顺带核对每张是否本人」+ 设置按钮（`pmaFaceLoad` 回填 `_pmaFaceExists`）；`pmaUpload` 逐条读回执：「已上传 N 张，AI 识别已排队 N 张」或「已上传 N 张；识图服务暂不可用（…），没有自动识别」+ 9s 红 toast；首次上传（相册原为空）且无锚脸 → toast 引导；AI 在识别时不再弹 J-7 的「请填触发词」而是「AI 正在识别 N 张并生成建议」；`pmaRetagAll` 遇 `vision_ready=false` 明说 + `console.info('[pma] retag-all click …')`；轮询 `_pmaMaybePollAi` 按剩余量续命（每张 3 轮、墙钟 90 分钟兜底，此前 20 轮 × 4s = 80s 就停）且只走 `_pmaRefreshAi` 刷计数 / 引导条 / 摘要，焦点不在卡片输入框才重绘网格；抽屉完善向导加「相册有 N 张缺触发词（去相册 tab 点 AI 补标）」（`_updateCompletionWizard`，见 §三）。i18n `persona_apply_modal` `pma_*` 22 键 ×2 + `persona_studio` `psn_wiz_album_trg` ×2 | `a1fb8d72` | 新 `tests/test_album_autotag_default_238.py` 10 例（端点策略：私网放行 / 云拒 / 无标记网关拒 / 有标记网关放行且钉 provider·宽超时 / 标记在场但换域名拒 / http 拒 / env 回放形态；探针四种 reason；熔断：5 条排队只打 1 次端点、空答不熔断；`run_batch` 打不动 queued=0；上传默认排队 pending + 回执；不可达不排队 + 列表 / 补标同口径；显式 `enabled:false` 尊重；完整度无相册不参与 / 零→半→满单调 / 脏输入不炸；前端五态 + 三处内嵌补标 + 上传回执 + 轮询续命 + 向导纳入；i18n zh/en 占位符一致）+ `test_media_auto_tag` 25 例 + `test_persona_media_ai_routes` 8 例（三处改钉新默认）= 39 passed；`test_persona_*` 列表 / 完整度相关 80 passed；i18n 键解析 165 passed；`node --check` 通过 | 模板 / i18n 热更新已生效；**`media_auto_tag.py` / `persona_media_routes.py` / `persona_routes.py` / `persona_completeness.py` 待装载**（`restart_preflight` → zhiliao，随 1.0.77 装机）。**注意**：默认开后客户机首次上传会真的走网关识图（每张一次 VLM 调用，单工位串行）；网关侧 `ai-gateway.jsonl` 会多出 `vision=1` 流水——D-N3 已拍「成本可接受」 |
| B | #239 P2 | STEVEN 语音 tab 登记 STEVEN.ogg：后端 13:50–13:51 `backend=avatar_clone voice=STEVEN` 四次合成、`[voice/tts-test] 有声终审 speech=voiced`——**后端全部成功**；界面 ① toast「克隆音色已登记并挂载到本人设: avatar_clone」约 1s 消失且含内部名 ② 登记后「试听」按钮消失、四段试听音频无处播放 ③「音色体检：暂无体检数据」未回填 ④ 底部仍「有未保存的修改」 | **真因**：② 人设页唯一的 ▶ 试听键在预置声选择器 `#pe-preset-picker` 里，L-2 A 三态 `_vmRenderStatus` 在 mode=clone 时把它整块隐藏——登记成功 `peVcAfterEnrolled → _vmLoad(vp)` 切到 clone 的那一刻按钮就没了；试听音频是 `<cp-voice>` 组件 shadow DOM 里的 `_audition` 播放器，组件重渲染即丢。① `psn_vc_enrolled_synced` 直接把 `vp.backend`（`avatar_clone`）当文案参数，5s 无关闭。④ `<cp-voice mode="enroll">` 挂在抽屉 `#profile-drawer` 内，抽屉的 dirty 委托 `drw.addEventListener('input'|'change', _markDrawerDirty)` 把登记表单（名字 / 文件 / 授权勾选）的冒泡事件当成表单改动——登记本身服务端已写入 `voice_profile`（#149），「有未保存的修改」是假脏。**修法**（语音 tab，三处之一）：`#pe-vc-card` 新增 `#pe-vc-preview` ▶ 试听 + `#pe-vc-player` 常驻播放器，`peVcSync` 在 `row.is_clone && row.ready` 时三件同排「已登记 STEVEN.ogg · ▶ 试听 · 解绑克隆音色」（`psn_vc_bound` 文案改「已登记 {voice}」）；卡上常驻一句「登记成功即写入人设并立即生效，不需要再点底部「保存」」；`peVcPreview()`：`POST /api/voice/tts-test {text, persona_id, format:'mp3'}`（按本人设克隆档解析，**不带** `voice_cfg_override`），`<audio controls autoplay>` 留在卡上、换人设才清；每次试听把 `{ts, persona_id, filename, url, voice_meta}` 写进 `window.__PSN_VOICE_AUDITION[pid]` 并派发 `document` 事件 `persona-voice-audition`；`_peVcToast`：9s、× 可关、内嵌 ▶ 试听、`psn_vc_enrolled_ok`「已登记 {file}，本人设的语音将用这把声音。登记已写入人设并生效，无需再点底部「保存」（表单里其它改动仍需保存）」（旧 `psn_vc_enrolled_synced` 键保留不再调用）；`_markDrawerDirty` 忽略 `e.target.closest('#pe-vc-card')`；③ 只留占位 `#pe-vq-audition[data-hook=persona-voice-audition]` + 上述数据钩子，口径渲染交 **N-4**。顺带：`_presetPreview` 两处 `window.T('psn_voice_preview_*', '中文 fallback')` 改 `window.Tf(key, …)`——L-2 A 遗留、`test_sealed_pages_no_cjk_in_own_scripts[personas.html]` 转绿。i18n `psn_vc_*` 5 键 ×2 | `2cd84c02` | 新 `tests/test_persona_clone_voice_feedback_239.py` 5 例（常驻行三件同排 + 明示句 / 试听走 persona_id 不覆盖预置声 + 播放器常驻 + 钩子写入与事件 + 占位在 + 不渲染 N-4 口径 / toast ≥8s 可关内嵌 ▶ 无内部名 + zh/en 占位符一致 / 脏标记守卫先于标脏 / 中文 fallback 清零）；N-2 三件 + L-2 E/C 门禁 44 passed；i18n 键解析 + 裸中文门禁 166 passed（含此前红的 personas 裸中文项）；`test_template_cross_block_refs` 5 passed；`node --check` 通过 | 模板 / i18n 热更新已生效；无 `.py` 改动 |

## 二、验收（交 N-5 回访口径）

- **#237**：「备份恢复可用，请用 13:01 那份再恢复一次」。主账号 → 人设池底部「人设备份与迁移」→「从备份恢复…」选 `chatx-personas-20260907-1301.json` → 预览「新增 N · 覆盖 N」列出名字；若手工重建的 Mizuki / STEVEN 与备份同 ID → 列在「将覆盖」（备份版替换手工版，旧版可在编辑器撤销找回）；若 ID 不同 → 红字「N 个与现有人设同名但 ID 不同——恢复后会并存两份」，恢复完自己删掉多余的那个 → 确认 → 状态行 + toast「已恢复 N 个人设：新增 A · 覆盖 B，含相册引用 M 条」→ 列表刷新。任何一步失败：状态行人话 + 「备份文件没有被改动，可重试」，renderer.log 有 `[persona/backup] preview|restore failed: …`。
- **#238**：「上传即自动识别并显示进度；识不了会明说」。1.0.77 装载后，Mizuki 相册 tab 顶部出现引导条：若显示「N 张还没有 AI 识别」+「🤖 AI 补标」→ 点一下，进度条「AI 识别中 x/144」逐步走完，识别完的卡片出现 ✨ 建议触发词、点即采纳（或「全部采纳」）；再上传新图 → 状态行「已上传 N 张，AI 识别已排队 N 张」，无需再点补标。若引导条红字「识图服务暂不可用（…）」→ 说明网关 / 识图端点当时不通，服务恢复后点「AI 补标」即可，不会再显示「0/144」却什么都不说。「锚脸基准照尚未设置」那行点「设置锚脸基准照」上传一张清晰正脸即可，之后识别会顺带核对是否本人。backend.log 应见 `[pmedia] 上传 … autotag=True vision=ready` 与 `[pmedia] 补标 pid=Mizuki queued=N …`。
- **#239**：「登记后常驻显示已登记 + 可试听 + 可解绑」。语音 tab 登记 STEVEN.ogg 成功后：右上 toast 停留 9 秒且可点 × 关、写的是「已登记 STEVEN.ogg …登记已写入人设并生效，无需再点保存」（不再出现 avatar_clone 字样），toast 里有 ▶ 试听；「🎤 克隆音色（跟随人设）」那行常驻「已登记 STEVEN.ogg · ▶ 试听 · 解绑克隆音色」，点 ▶ 出一条可反复播放的音频条留在卡上；底部不再因为登记本身出现「有未保存的修改」（若之前改过表单别的字段，该提示照旧）。「音色体检」一行本次仍显示原口径，试听结果的回填由 N-4 接（同区）。

## 三、越界 / 顺带 / 刻意不做

| 类型 | 文件 / 位置 | 说明 |
|---|---|---|
| 越界（同根同修法） | `personas.html` `_importFromJson` / `_jiShowUndoToast`（L-2 C #203「解析并预览」块，不在指令「三处」清单） | 与 #237 完全同一个 bug（12 处 `_esc` 全局调用 = 一点即 ReferenceError），机械改名 `_escHtml`，无逻辑改动；不修则新门禁必须把它登记成已知崩溃。随 A 一个 commit。 |
| 越界（C 项要求所在） | `personas.html` `_updateCompletionWizard`（抽屉完善向导，不在「三处」清单）+ `src/utils/persona_completeness.py`（纯函数评分器，归属矩阵未列） | 指令 C-3「完整度计算纳入相册未打标」——完整度在这两处算，改 4 行加一个可选 `album` 入参 + 向导一条项，未动其它评分权重。`i18n_packs/persona_apply_modal.py`：`pma_*` 相册词条本来就住这里（J-7 B 落地处），新键随之，不搬家。 |
| 顺带（C） | `media_auto_tag._lan_vision_cfg` 端点策略 | 放行官网网关注入那一条是 D-N3 的必要条件（否则默认开＝144 条全 failed）；只在 `_hosted_vision` 标记 + URL 完全匹配时放行，云端点 / 任意公网域名仍拒，模块头「隐私」段已改写说明。`config/config.example.yaml` L2304 `album_ai.enabled: false` 注释与新默认不一致——该文件当时在 QQ 线 index 里，**未动**，交 N-5 收口时同步注释。 |
| pre-existing → 已顺带修（B） | `personas.html` `_presetPreview` 两处 `window.T('psn_voice_preview_text'|'psn_voice_preview_fail', '中文 fallback')` | `test_i18n_coverage::test_sealed_pages_no_cjk_in_own_scripts[personas.html]` 在 HEAD 上就红（L-2 A `3f31eeab` 引入）；就在 B 项动的语音 tab 试听块里，改成 `window.Tf(key, …)`（两键 zh/en 早已在包里），随 `2cd84c02` 转绿。 |
| 越界（B，1 行守卫） | `personas.html` `_markDrawerDirty`（抽屉脏标记委托，不在语音 tab 块内） | R8ECFS ④ 的机制就在这里：`<cp-voice>` 登记表单挂在抽屉内、其 input/change 冒泡被当成表单改动。加一行 `closest('#pe-vc-card') → return`，只对登记卡生效，其它字段脏判定不变。 |
| 边界（B → N-4） | 「音色体检」`#pe-vq-line` / `_loadVoiceQuality` / 「已核有声 vs 疑似无声」口径 | 本条**不渲染**任何终审口径（不改 TTS 判定），只留 `#pe-vq-audition` 占位 + 数据钩子（见 §四）。 |
| 未验证（B） | `<cp-voice>` 组件自身登记后是否清掉 shadow DOM 里的 `_audition` 播放器 | 未复现组件内部行为；无论如何，宿主侧 `#pe-vc-player` 常驻播放器 + ▶ 试听键已让「四段试听无处播放」不再成立。`shared/copilot/components/cp-voice.js` 是收件箱右栏同源组件，不在 N-2 归属，未动。 |
| 刻意不做 | 导入格式 / `psn_restore_done` 旧键 | 不重做导入格式（指令红线）；`psn_restore_done` 旧键保留不删（`zh_hant_auto.py` 有覆盖，删会触发 `test_extra_lang_overrides_subset_of_zh`），新回执走 `psn_restore_receipt*` 新键。 |

## 四、交 N-4 / N-5

- **交 N-4（B 项体检回填钩子）**：
  - 占位：`personas.html` 语音 tab「🩺 音色体检」行内 `<span id="pe-vq-audition" data-hook="persona-voice-audition"></span>`（紧跟 `#pe-vq-line`）。
  - 数据：`window.__PSN_VOICE_AUDITION[pid]` = `{ts, persona_id, filename, url, voice_meta}`，`voice_meta` 即 `/api/voice/tts-test` 回包原样（`speech: voiced|silent|garbled|unknown`、`speech_basis`、`transcript_chars`、`energy`、`provider`、`voice`、`fallback_from`、`fallback_reason`、`fallback_lang`、`requested_persona_id` / `persona_id`）。
  - 事件：每次 `peVcPreview()` 成功后 `document.dispatchEvent(new CustomEvent('persona-voice-audition', {detail: 同上}))`；`_loadVoiceQuality()`（夜间探针读数）未动，N-4 可在其后按 `_editingProfileId` 合并渲染。
  - 边界：N-2 未在人设页写任何「已核有声 / 疑似无声 / 试听已过期」文案；`unified_inbox.html` 业务助手语音面板与 `tts_pipeline` 判定完全未碰。
- **交 N-5 回访口径**：见 §二三条。另请 N-5 在 1.0.77 收口时：① `config/config.example.yaml` L2304–2307 `album_ai` 注释改「默认开（D-N3）」（该文件当时在 QQ 线 index，N-2 未动）；② MJGHKQ 改挂 #237（底座已写）；③ 装载清单：`media_auto_tag.py` / `persona_media_routes.py` / `persona_routes.py` / `persona_completeness.py`（C 项）+ `persona_routes.py`（A 项 dry_run 顾问字段）——模板 / i18n 三件已热更。
- **交老板知悉**：C 项默认开后，客户机每张上传都会真的经官网网关识图一次（单工位串行，144 张约十几到几十分钟），网关流水会多出 `vision=1`；这是 D-N3 拍的「成本可接受」，如需关闭：`companion.selfie.album_ai.enabled: false`。

## 五、预算与收工

- 三个 commit：A `9c72cdd2` / C `a1fb8d72` / B `2cd84c02`，每个只含自己文件（提交前 `git diff --cached --name-only` 为空再 add；期间 QQ 线两次占 index，均等其提交后再动）。
- 测试：新 3 个测试文件 20 例 + 改钉 4 处；相关回归 44 + 39 + 80 + 166 + 5 passed；无新增失败（`reply_settings.html` 孤儿引用一处是别的线在途，非本条）。
- 收工：总账 N-2 行已改；`agent_probe -Done`。
