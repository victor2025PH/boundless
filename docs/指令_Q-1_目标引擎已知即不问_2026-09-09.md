# 指令 Q-1：目标引擎「已知即不问」+ 识破族（撤 O-3 C 的无界重试）

> 预算 **$180**。工单 **#264 #269**（K24YJ2 / FCQFKF / 57S9XD / AEMAMJ；账号名≠人设名 AEMAMJ② / XAM4KV③）。目标版本 1.0.79（第一波）。
> 底座 `docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-1 行、接口约定①③）。落点表 `docs/发版对账_v1.0.79_Q1.md`。

## 开工前必读

1. `docs/聊天核心能力_三视角方案_2026-09-09.md` §1 Q1 / Q7、§2.1 第 1 条、§3 Q-1。
2. 证据 `tmp_diag\K24YJ2\ AEMAMJ\ FCQFKF\`：23:41 `[goal-inject] target=occupation cue=job mode=cue`；23:46 AI 自己说「it's a job… convention center」（已知）仍 `result=missed→retry`；02:05–03:17 注入 13 次、asked 3 次；客户「20th time」「I feel like you are a bot now」；AI「I promise I'm not a robot」；「Nori? No, that's not me, I'm Mizuki」。
3. 代码事实（已核）：`src/companion/goals/service.py` L1249 `decide_probe_target`：`all_unfilled = missing_slots(prof_fields)`（只看画像字段是否为空）；`ordered` 用 `asked_slot_keys` 排除问过的，**下一行 `unfilled = ordered + [k for k in all_unfilled if k not in ordered]` 又把问过的加回末尾**；`retry_slot` = 上轮 missed 即重试，无上限；`pick_probe_target` 在 `profile_slots.py`；`capture_status` 自己写了 `profile_llm_off`（明知抽取关着）。O-3 C 落点 `docs/发版对账_v1.0.79_P3.md` 之前的 `docs/发版对账_v1.0.78_O3.md` C 行（`183e43c1`）。
4. 接口约定：画像字段 schema `{value, source, status}`（Q-5 写、你读；容忍裸字符串 = confirmed）；`prompt_addenda.identity_addendum()` 你提供、Q-6 接线。
5. `scripts\agent_probe.ps1` 看 ACTIVE；Q-8 第二波会在你收工后动 `service.py` 注入分级，你收工时在总账写清接口。

## 红线

- 只动归属矩阵 Q-1 行；`persona_reply.py` 不动（身份补丁只写函数，Q-6 接线）；`autosend_worker.py` 不动。
- 默认行为必须**保守**：判不准就不问（宁漏不错）。
- `git add <显式路径>`；每 commit 带 `#264` / `#269`。

## A 槽位三态 + 30 轮扫描

- `profile_slots.slot_state(slot, prof_field, history_texts) -> unknown|mentioned|confirmed`：字段有值且 source=user/confirmed → confirmed；字段 `ai_inferred|nickname` 或最近 30 轮（客户 + 我方）文本命中该槽关键词表 / 抽取候选 → mentioned；否则 unknown。`decide_probe_target` 只从 unknown 里选；mentioned 只允许「深化式」提法（每槽每日 ≤1，模板「你说的那个 X，具体是…」），confirmed 永不问。修掉「问过的加回末尾」。
- 日志 `[goal-inject] target=… state=unknown|mentioned decision=probe|deepen|skip reason=…`。

## B retry ≤2 + covered

- 每槽每日 `retry ≤2`（`count_events_since`）；`missed` 判定前先查本轮回复是否已含槽位关键词（cue=job → 回复含 job / work / carpenter…）→ `covered` 不计 missed；连续两次 missed 后该槽当日休眠。

## C 出站重复提问守卫 `repeat_question_guard`

- 新 `src/inbox/repeat_question_guard.py`：抽出本稿问句，与 24h 内我方问句做规范化相似（词干 + 同义小表 + 槽位标签）；相似 ≥0.8 → 改写为非问句或删句，落 `[repeat-q] conv=… matched_ts=… action=strip|rewrite`；挂在起草层净化（P-1 A）之前。

## D 识破处置 `exposure_guard` + 自证句黑名单

- 新 `src/inbox/exposure_guard.py`：客户入站命中 识破词（you are a bot / are you a bot / asking me again / already asked / you told me / 20th time / you're a robot / 是机器人吧 / 你又问）→ ① 该会话全部目标暂停 24h（`goal paused_until`）；② needs_human + 标签「疑似识破」；③ 本轮回复只允许**一句随意不辩解挽回**（模板池，按语言 / 人设口吻：「lol sorry, long day. you did tell me, the carpentry thing」类），禁再问任何摸底；④ 出站黑名单：I promise I'm not a robot / I'm a real person / 我不是机器人 类自证句一律删（越辩越假）。日志 `[exposure] conv=… hit=… action=pause_goals|needs_human|recover`。

## E 总开关 + 卡片「已提及」

- 目标页顶部「暂停全部摸底目标」总开关（`goal_routes` 端点 + `cp-goal.js` 双树 `?v=`）；卡片槽位行显示「职业：已提及（未确认）」+ 「确认」按钮（写 status=confirmed）；i18n 三语。

## F 身份补丁函数（交 Q-6 接线）

- `src/inbox/prompt_addenda.py::identity_addendum(persona, account) -> str`：账号显示名 ≠ 人设名时返回「对方可能用 X 称呼你，那是你在这个平台的昵称，不得否认、不必解释」；相同返回空。tests。

## 收工

- 落点表 `docs/发版对账_v1.0.79_Q1.md`（§〇 「为什么问到第 20 次」四段：只判问否 / 抽取关 / 无上限 / 无守卫；A–F 每 commit 一行；回归用例「同会话职业已答后不得再问」）。
- `docs/Q批_进度总账.md` 只改 Q-1 行（写清给 Q-8 的接口：注入分级钩子位置）；`agent_probe.ps1 -Done`；回访文案交 Q-0 / R79。

## 开新对话粘贴

```
读 docs/指令_Q-1_目标引擎已知即不问_2026-09-09.md，按它做 Q-1（#264 #269：profile_slots 槽位三态 unknown/mentioned/confirmed + 注入前扫最近 30 轮、decide_probe_target 只选 unknown 且修掉「问过的加回末尾」/ 每槽每日 retry ≤2 + covered 判定 / 新 repeat_question_guard 24h 内相似问句拦 / 新 exposure_guard 识破词→停目标 24h + needs_human + 一句不辩解挽回 + 自证句黑名单 / 目标页「暂停全部摸底目标」总开关 + 卡片「已提及（未确认）」+ 确认 / prompt_addenda.identity_addendum 函数交 Q-6 接线）。预算 $180。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与接口约定①③、docs/发版对账_v1.0.78_O3.md C 行（在它之上改，不重做），再 scripts\agent_probe.ps1 -Intent 登记；不动 persona_reply.py / autosend_worker.py；判不准就不问。每个 commit 带 #264 并立刻补落点表 docs/发版对账_v1.0.79_Q1.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-1 行并 agent_probe -Done。
```
