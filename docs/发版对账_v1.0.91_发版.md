# 发版对账 v1.0.91 — 发版线 R91（1.0.90 同功能安装包重打 → 一个 clean 包 → **公共渠道 + 内测桥接 + 官网 + 播报**）

> 规矩：`.cursor/rules/chatx-desktop-install.mdc` 「单一版本」——一个 semver = 一个 clean 包。1.0.90 已于 09-19 上架，不能同号重切。
> 范围：公共渠道 1.0.90 → 1.0.91。未点名坐席机，不 `push_chatx`。140 不是坐席。

## 0. 一句话

**1.0.91 单包** `ChatX-Setup-1.0.91.exe` **578,037,159 B** sha256 `42d74dbf81645bc71c7bc0abc784c1dce5e5439b3bc3f43eaf545c4aeb0dadb6`（551 MB，clean，`MZ` 头核过，build-info flavor=clean commit=`50d1db71`，干净 worktree `D:\boundless-r91`）。产品内容与 1.0.90 相同（电脑微信三步启停 / 全自动档人设语音 / 接管迟到闸 / 人设「不设限」）；本号只为重打安装包。公共渠道 + 内测同字节桥 + 官网 + 频道 @hykj7 **messageId=197**（campaign `release-1091`）。

## 1. 未提交功能盘点（进包）

工作树相对 1.0.90 **无新的进包代码**（仅 version bump + 发版说明 JSON）。1.0.90 已进包项原样随包：

| 项 | 处置 |
|---|---|
| 微信 PC 副驾 / 冻结态 `backend.exe --wechat-pc-driver` | 进包（后端复用 r90 fingerprint `821d1097…`，freshness 绿） |
| 微信出站语音 / 入站请打字 | 进包 |
| Telegram 接管迟到闸 | 进包 |
| 人设 `adult_policy: open` | 进包 |
| 小智四题语料 | 进包（eval 四题 GUIDED） |
| `desktop/config.json` 本机令牌 / 坐席监控日志 / 117 运维脚本 | **不进** |

## 2. 全链

| 步 | 状态 | 事实 |
|---|---|---|
| 干净 worktree 打包 | ✅ | `dist:win:clean` @ `D:\boundless-r91`（backend-dist 与 r90 junction，ffmpeg/node_modules junction） |
| import_sweep | ✅ | modules=1306 bad=0 |
| 小智发版金标 | ✅ | `wechat_pc_start` / `wechat_pc_voice` / `takeover_late_gate` / `adult_policy_open` 四题 GUIDED |
| gate_sweep | ⏭ | 与 1.0.90 同树无产品 diff，未重跑全量 |
| publish 公共 | ✅ | `latest.yml=1.0.91` exe HTTP 200；manifest sha256 一致 |
| publish 内测桥接 | ✅ | 同字节；公共指针仍 1.0.91 |
| 官网 | ✅ | 五值 + `deploy.ps1 -FromHead -SkipAssets` |
| 频道 | ✅ | @hykj7 messageId=197 campaign `release-1091`。未发报障群（频道已链群） |
| 坐席三台 | ⏭ | 走 `latest.yml` 自动更新，未 push |

## 3. 下载

- https://bd2026.cc/download/chatx
- https://bd2026.cc/downloads/ChatX-Setup-1.0.91.exe
