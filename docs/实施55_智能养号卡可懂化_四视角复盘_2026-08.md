# 实施55：工具箱「智能养号」卡可懂化（裸键根治 + 工程调试台→产品面板）

日期：2026-08-22　|　线：nurture card UX revamp　|　状态：前端 HOT-LIVE 已验证 / 后端半件随下个重启窗

## 0. 事故与背景

06:21 老板截图：工具箱卡标题直显 **`cp.app.h_nurture`** 裸键；卡内是「金丝雀 / 动作探针 /
试运行 / 预检 / 真实执行 / `line:Uk4bhjJ4-HC…` 原始键」等工程黑话，坐席不可懂。

**裸键根因链**（已实锤到分钟级）：08-22 04:30 批次给 `app.html` 补养号卡并在 `cp-i18n.js`
新增标题键，但 `app.html` 里词典的 `?v=` 停在 `20260821a` 未随批 bump → 浏览器命中 8-21
旧缓存词典（无新键）→ `applyI18n` 拿 `t()` 缺键回落值（键名本身）**覆盖掉模板内联中文兜底**。
同类潜伏病同日盘点还有三例：`copilot-client`（app 20260818c vs inbox 20260822b）、
`cp-image`（22b vs 21a）、`cp-collab`（12a vs 01b）。

**深层问题**：一张卡混装两个受众——运维（引擎三档/金丝雀/探针/lint 警告）与坐席
（每号养护开关/档位），且账号显示原始内部键、零数据无语境、9-10px 字号、危险操作无危险感。

## 1. 根治（三层，均已上线）

1. **运行时降级**：`applyI18n` 缺键时保留节点内联兜底文字，fail-loud 改 console.warn
   （每键一次）；无兜底才写键名。旧行为=拿键名冲掉好文案，正是截图成因。
2. **缓存戳**：`app.html` 漏 bump 补齐；四例分叉全部拉齐（本批终态 cp-i18n/cp-nurture
   =`20260822d` 双宿主同值）。
3. **门禁**：`tests/test_copilot_i18n_keys.py`（已进 gate_sweep 常规清单）——
   ① 静态 `cp.*` 键（app.html 属性 + 组件字面量 t()/tf()/CP_T()）必须双语可解析
   （顺带扫出并补了 `beh_online` 缺键：探针下拉第三项此前必裸键）；
   ② 同一 `/copilot` 资源两宿主 `?v=` 必须相等（桌面树由 shared_sync 字节门禁兜底）。
   与并行线同晨的 `test_cp_app_i18n_keys.py`（h_image 同类事故）互锁，注释互认。

## 2. 可懂化改版（cp-nurture 重写 + 词典人话化，HOT-LIVE）

- **术语表**（钉在 cp-i18n.js 养号分区注释，勿再造同义词）：金丝雀→**试点账号**、
  动作探针→**上线前单步试跑**、试运行→**模拟运行**、正式启用→**开始自动养护**、
  预检/真实执行→**先检查 / 真跑一次**；死词条 master/master_sub/exec_p1（P0 遗留、
  零消费方）删除；`health` 键保留（旧缓存组件兼容，防混配窗口裸键）。
- **三步引导条**：① 配方案 → ② 模拟运行 → ③ 自动养护；当前步高亮、下一步 CTA 唯一；
  「开始自动养护」内联确认写明试点账号影响面（0 试点=诚实告知安全空转）。
- **模拟可见化**：消费现成 `GET /api/nurture/shadow`——「最近计划/执行记录」逐条可读
  （几分钟前 · 人话账号名 · 动作名 · 演练/真实/失败 chip）；探针真跑成功自动刷新。
- **受众分层**：探针 + lint 警告收「高级（管理员）」折叠；后端 `can_write`（=非 viewer）
  驱动前端隐藏全部写控件（与路由 403 同源；旧后端缺字段 fail-open）。
- **账号人话名**：后端 status 每行补 `label`（注册表 label → meta.self_name/self_username
  回落）；前端 label→短 id 回落，完整 key 收悬浮 title；探针下拉同享。
- **交互死角三修**（实施中深挖新增）：零启用方案时「模拟运行」禁用+悬浮指路（调度器
  只为 enabled 账号出计划，点了必空转）；引擎开但零启用 → 内联警示（不藏折叠里）；
  intro 引导横幅保存后就地隐现（`_syncCounts` 不整块重渲，保住「已保存 ✓」反馈）。
- **视觉**：字号全 ≥11px token 档；分区标题（机群概览/养护引擎/账号方案）；健康大灯+
  一句话判词+三个有语境数字；账号行两行网格；行为多选 chips（self_chat 危险色）；
  dirty 才亮保存；探针真跑两击 armed 红底；`STAGE_CLR` 硬编码 hex → `--cp-*` token
  （暗色补 `--cp-danger-bg` 定义，light/dark 两文件同步）；EN 溢出 ellipsis+title。

## 3. 验证

- **真浏览器门禁** `tools/verify_nurture_ui.py`（新建，已挂 gate_sweep `-Full`）：
  夹具模式（file:// + 真词典 + 真基类 + 真组件 + stub client，零实例依赖零写入），
  N0-N9 共 **55 断言首跑全绿**——N0 直接钉 0621 事故形态（缺键必须保留内联兜底）。
- pytest：新门禁 + 并行线 i18n 门禁 + panel-manifest + shared_sync + theme_tokens +
  nurture_scheduler + route_inventory 全绿；广域 sweep 799 绿（余 1 红=59.7h 存量
  inline-color ratchet，presence/membership 线挂账，非本批）。
- **线上只读探针**：18799 热供字节含 `nt-steps`/`eng_try_need_plan`/applyI18n 新逻辑
  ——坐席刷新即得新卡，无需重启。

## 4. 顺手抓到的并行线活性事故（已修已报备）

`cp-image.js` 块注释两处写了 `errc_*/errs_*`——`*/` 提前终结注释 → 整文件 SyntaxError，
**带着新缓存戳从 09:51 起热挂生产（生成图片卡全端挂死）**。最小注释修复（加空格）、
node 验绿、补镜像；意向板已向 image 线报备。教训：注释里写键名通配时 `*/` 是雷。

## 5. 后端半件（随下个重启窗，已在意向板留验证指引）

`nurture_routes.py`：status 响应 +`label` +`can_write`（纯增量字段，前端 fail-open）。
重启后验证：`pytest tests/test_copilot_i18n_keys.py tests/test_nurture_scheduler.py -q`
→ `GET /api/nurture/status` 出 `label`/`can_write` 字段。

## 6. 三批（2026-08-22 14:xx，试点 UI 化 + 埋点；P1 两项落地，ops 卡避让）

- **上轮 rider 线上闭环**：1047 批 12:28 重启装载后实测——`GET /api/nurture/status`
  出 `can_write=true` + `label` 实名（10 账号 4 平台全解析：yunshan zan / Calixa
  Lopez / katie / Vivian…），账号乱码问题在生产消失。
- **试点账号 UI 化**（canary_accounts 告别 YAML-only）：
  后端＝`POST /api/nurture/config {canary:{key,pilot}}` 单一 UI 写入口（整表回写
  保序去重、重复设置=剔旧置尾、坏 key 400 零写、审计 `nurture.set_canary`）+
  status 每行 `is_canary`；前端＝账号行「试点」chip（旧后端缺字段 fail-hidden；
  viewer 可见不可点；**引擎 live 时设为试点＝两击 armed 确认**——该号立即参与
  真实动作，危险方向要显式确认；取消试点/非 live 直达零阻力）。
  门禁＝`tests/test_nurture_canary_route.py`（4 例，**首跑即抓到 ncfg 未定义真 bug**，
  契约门禁当场回本）+ 浏览器门禁 N2b/N4b。后端半件随 22:30 重启窗。
- **ntr_ 埋点 9 桶**（与 cp-goal 同机制 sendBeacon → `/api/telemetry/ui-event`）：
  sim_start / golive_ask|confirm|cancel / save / pilot_on|off / probe_pre|run /
  adv_open；读数 `/api/admin/ui-event-trend?prefix=ntr_`；浏览器门禁 N10 截获
  sendBeacon 断言 9 桶全链路。两周后裁决步骤条/确认框是否降低误操作。
- **浏览器门禁扩容**：55 → **63 断言全绿**（试点两态 + 埋点全链路）。
- **ops 养号卡本轮避让**：image 线同小时登记了 ops_overview 卡意向（撞车合流纪律），
  留待热区安静后单独批次。

## 7. 下一阶段（更新后）

- **P2 ops 常驻读数**：ops-overview「智能养号」卡（引擎状态/今日动作/警告数/试点
  覆盖）——等 image 线的 ops 卡批次落地后再动（同热区避让中）；复用 status 端点
  零新后端。
- **P2 埋点周读**：ntr_ 数据攒两周（~09-05）后出裁决：golive_cancel/confirm 比值
  （确认框是否在拦真实误操作）、probe_run 频次（单步试跑是否被用）、pilot 增删频次。
- **P2 引擎能力**：online/browse/react 适配器补齐后，探针「暂不支持」项自动点亮
  （前端零改动，probe_no_adapter 语义已就位；引擎属 executor 线）。
- **P3 EN 全面走查**：EN 词条齐但未经母语走查；随 WP9 英文走查线合批。
- **P3 试点批量操作**：若试点账号规模上两位数，考虑「全选/按平台设试点」批量入口
  （当前逐号点选，10 账号内成本可忽略——先等埋点数据说话）。
