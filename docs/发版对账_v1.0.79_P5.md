# 发版对账 v1.0.79 — P-5（值守链三修 + 归位 + 回访口径：#106 误挂 / #259 / #254）

> 指令：`docs/指令_P-5_值守链三修与归位_2026-09-08.md`。底座：`docs/修复指令_P批_并行_2026-09-08.md`。总账：`docs/P批_进度总账.md` P-5 行。预算 $80。
> 接手方：先 `git -C D:\boundless log --oneline -10` 核对本表 commit 真在；台账写入先 dry-run；对外只走 `engines/chengjie/tools/duty_reply.py`，每条带工单号、只给版本号。
> 执行：2026-09-08 15:45 起。A / B 是值守工具（`tools/`，计划任务每 2 分钟拉起即生效）；C 是引擎代码随 1.0.79。

## 0. 开工基线（15:45，全只读）

| 项 | 事实 |
|---|---|
| 意向板 | P-5 15:45 登记；同板 ACTIVE：P-3（0.0h）、R78 发版收口（0.8h，pre-restart）；QQ 两线 / N-1～N-3 意向未 -Done（陈旧）。git 索引空闲；HEAD `6b7787f6`；`unified_inbox_send_routes.py` 脏 1 hunk（L2280，15 行新增，抖音线 10:55，30 分钟窗外不活跃） |
| #106 | `fixed` **P0**（13:37 auto_severity P1→P0，事件 1046/1047）；body 1652 字，尾两行 = MTRCH2 误挂 380 字 + 抬档说明 |
| #252 #254 | `confirmed` P0；#259 `new` P0 body 2987 字；库 max id 259 |
| **12 条「幻影行」实况** | `inbox.db` 群会话 13:47:04…14:09:02 十二行 message_id `…:fail:<hex>`，**已是 `status='failed'`**（`_trace_failed_manual_send` 早在写留痕行，前端 `_storeFailed` 标红「发送失败」），platform_msg_id 空、**`fail_reason` 空**；14:11:02 真发 mid 1345 `read`。指令说的「工作台看到 12 条已发」不成立——实际缺的是**原因**：A 线 `TelegramCompanionWorker._send_text_inner` 在 client 未连上时回 `{"delivered": False, "message_id": ""}` 不带 `error`，路由 `_fail_raw=""` → 502 `消息未送达：` 空、留痕 `fail_reason=''`。C 段按此收窄 |
| MTRCH2 重判（新规则 dry-run） | `--code MTRCH2 --force --dry-run` → 不再挂 #106；按 ② 关联码 RRYKF6 会挂 #259——仍不对，D 段人工归位到新单 |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| A 挂单规则收紧 + 不升 fixed/closed | #106 #259 | `cd76850c` | `tools/duty_auto_ticket.py`：`_TICKET_RE = (?:(?<![\w\u4e00-\u9fff)])\|(?<=工单)\|(?<=关联)\|(?<=[挂见同]))#\s*(\d{1,4})\b`；`RESOLVED_STATUSES=(fixed,closed,verified)`；`decide()` 把 #N 拆成 open_refs / resolved_refs——开放单照挂，已修单不挂不升、走 ②③④ 并带 `mentions_resolved`；`_lift_severity` 读 status，已修/已关直接返空；`apply()` 在去向单追加「[值守自动] 正文提及 #N（已修/已关，未挂不升）」。`tests/test_duty_watch_autoticket.py` +3 例 | 19 passed；MTRCH2 原文 `findall==[]`；「关联 #254」→ 254；「复验 #106」（fixed）→ 同人 90 分钟开放单 / 新立 + note；verify 报告仍记到 #106 |
| A 顺手：#106 严重度复原 | #106 | 台账（`tmp/p5_ledger_0908.py --step severity`，dry-run → apply 16:1x） | severity P0 → **P1**；note「[值守 P-5] 严重度 P0 → P1 复原：13:37 MTRCH2 误挂…撤销」；事件 `duty_fix` | 库核 severity=P1 |
| B 回执重试上限 + 幂等 + 空备注不回执 | #259 | `6c80d322` | `tools/duty_channel_reminder.py`：`RECEIPT_MAX_ATTEMPTS=3`（跨 tick 计数 `state.receipt_attempts`，满 3 次 `receipt_gave_up` → `.ops/duty_alerts.log` 一行 + 直接 receipted）；`already_in_inbox(text)` 发前查 `inbox.db` 群会话 10 分钟内**同文本且 `platform_msg_id<>''`** 的 out 行（failed 留痕行 mid 为空不算）→ 有则视为已发；`is_empty_note()` 空 / 「（无备注）」的 report 包只登记（ticket.txt 一行 skip）不回执；`_report_head` 改按报告头 `field-agent:<agent>:<kind>` 判类（2WD7FY 是 `selfcheck` 自检包，此前按文件名判成 report 回执了「（无备注）」）。新 `tests/test_duty_channel_reminder.py` 8 例 | 27 passed（含 A）；生产态 `--dry-run` 无异常；验收三条：模拟 502 三次 → 第四轮不发 + 告警；模拟带 mid 行 → 不发直接 receipted；空备注 / selfcheck → 无回执 |

| C send 失败 502 带原因 + 留痕 fail_reason 非空（引擎，随 1.0.79） | #259 | `29405d67` | `src/inbox/send_failure_class.py`：新 `undelivered_reason(result, platform, account_id)` → `(原因, reason_code)` 绝不为空——带 `error/error_kind` 原样；空 → 进程 uptime < 180s 或该账号 worker 非 running ⇒ `adapter_not_ready`「适配器未就绪（后端启动中）」，否则 `adapter_no_reason`（归 channel 类，原始码留括注）；`FAILURE_CLASS_I18N` + `adapter_not_ready`；`send_timeout` / `adapter_no_reason` 归 channel。`src/web/routes/unified_inbox_send_routes.py` **只动失败分支**：文本 L1325 / 媒体 L893 两处 `delivered=False` 改走 `_undelivered_reason()`，留痕 `_trace_failed_manual_send(... _fail_kind or _fail_raw)` 同源，`_log_send_fail()` 打 `[send] fail conv=… reason=… kind=…`；`i18n_packs/errors.py` zh/en `err.inbox.sendfail.adapter_not_ready`。新 `tests/test_send_fail_reason_p5.py` 12 例。**留痕行沿用 `status=failed` + 前端既有红标「发送失败 · 原因」**（不是不写行：坐席要一键重发）。他人在途 hunk（实施97 L2301）未卷入：`stage_hunks --mine` 3/4 | 12 passed + `test_messenger_send_failures.py` 26 passed；template smoke OK。**装载后验收**：关掉适配器发一条 → 502 detail「消息未送达：适配器未就绪（后端启动中）…（adapter_not_ready）」+ 收件箱一条 failed 行带原因 + backend.log `[send] fail conv=` |
| C 存量 12 行 | #259 | 台账（`tmp/p5_fix_phantom_rows.py` dry-run → apply 16:4x） | 13:47:04…14:09:02 十二行 `…:fail:*`（status 本就 failed）补 `fail_reason=adapter_not_ready: 适配器未就绪（后端启动中）telegram:6834964252 R78 装载重启窗口…`；工作台气泡即显「发送失败 · 原因」 | 复核 12/12 带原因；14:11:02 真发行 mid 1345 `read` 未动 |

| D 归位 | #106 → **#260** | 台账（`tmp/p5_ledger_0908.py --step split`，dry-run → apply 16:53） | 新立 **#260**「1.0.77 出厂态四件：Web 监听 0.0.0.0 / clean 包缺 handoff yaml / fatex.db 进包 / 首启体验档文案 None」reporter skuio · **P1** · reporter_version 1.0.77.0 · body 四件逐条 + 去向 P-4（`record_bug_ticket` is_new，未与开放单归并）；`duty_evidence received 260 --diag MTRCH2` rc=0；`tmp_diag/MTRCH2/ticket.txt` 追加 `attach #260（P-5 人工归位）`；#106 body **删掉**误挂的两行（`[报告 MTRCH2 …]` 380 字 + `[值守自动] 严重度 P1 → P0`），追加「MTRCH2 为 1.0.77 验收对照，误挂已摘出 → #260（出厂态四件，去向 P-4）」；事件 `duty_reattach` / `duty_split`（1062 / 1063） | 库核：#260 new→confirmed（回访时流转）P1；#106 fixed P1 body 1361 字无 MTRCH2 段 |
| D 去向注记 | #259 #252 | 台账（`--step notes`，apply 16:53） | #259 note：Y3C6PS / U4ZAD7 → P-1；ZH3ZQ5② / H3BAJD → P-2；6SRA2B → P-3；34585H → P-6；ZH3ZQ5③ → P-1 B；#252 note：BBRGMG → P-3 / A7PB2F → P-2 F；事件 `duty_ledger` ×2 | body 3229 / 1469 字（cap 4000 内） |
| D 回访 ① | #259 | `duty_reply.py --group neice --ticket 259 --receipt --status confirmed`（dry-run 16:54 过闸 → 16:54:42 `[ok] path=bug-intake/reply`） | 正文见 §2；#259 **new → confirmed**，note 回写 | `logs/duty_replies.jsonl` 16:54:42 ok（bug-intake/reply 路径不回 mid） |
| D 回访 ② | #254 / #260 | `duty_reply.py --ticket 260 --receipt --status confirmed`（dry-run → 16:55:26 ok） | 正文见 §2；#260 **new → confirmed**；#254 追加 note「回访已随 #260 一并发出」 | 同上 16:55:26 ok；guard 未拦（未教改配置文件，只说 1.0.79 给开关） |

## 2. 回访清单（交 R79；已发两条，1.0.79 装机后按单再回一轮）

| 单 | 已发文案（16:54 / 16:55，skuio） | 1.0.79 装机后补一句 |
|---|---|---|
| #259 | 「#259 进展：1.0.78 已经发出去的消息是零破折号（发送门上剥掉了）。你复测里剩下的四件——草稿预览里也净化、登录 / 切档不再批量起草老客户、停联名单随账号备份走、发图不说谎（没真发出图就不说「拍了张给你」）——都随 1.0.79。这段时间先别按账号开全自动，等 1.0.79 装上再开。」 | 「#259 四件随 1.0.79 到了：重装 + 登录看零自动草稿、工作台任意草稿无 —、照片会话三轮无谎话；现在可以按账号开全自动了」（P-1 / P-2 / P-3 各线收工文案汇到这里） |
| #254 / #260 | 「#254 / #260 进展：你 1.0.77 验收对照里的出厂态四件（后台监听全网卡、clean 包缺 handoff yaml、fatex.db 进了包、首启体验档「剩 None 小时」文案）已从 #106 摘出单独立 #260，和「陪伴域首装 KB 为空、系统话术移到系统设置」一起随 1.0.79。1.0.79 之前的临时办法：不在公共 Wi-Fi 上开着后台；1.0.79 会给「允许局域网设备连接」开关，默认关，要手机扫码操控时再开。」 | 「#260 随 1.0.79：`netstat` 只见 127.0.0.1；设置里有「允许局域网设备连接」开关；clean 包里 handoff yaml 在、fatex.db 不在；首启体验档文案正常」（P-4 收工文案） |
| #106 | 不回访（已 fixed，本次只是撤销误挂 / 复原 P1） | — |
| 值守链本身（A / B） | 不对外说（工具侧修复，提交即生效） | 1.0.79 发版说明里加一句「发送失败现在会写明原因（含后端启动中）」 |

## 3. 断点 / 交接

- **已收工**：A `cd76850c` / B `6c80d322` / C `29405d67` + 台账三步（severity / split / notes）+ 存量 12 行 + 两条回访。P-5 行见 `docs/P批_进度总账.md`。
- **C 段随 1.0.79 装载**（`send_failure_class.py` / `unified_inbox_send_routes.py` / `errors.py` 是引擎代码，zhiliao 装载后才生效）；A / B 在 `tools/`，计划任务 DutyWatchTick 下一拍即用新代码，**不需重启**。
- **给 R79 的装载后验收**（C）：关掉一个适配器（或刚重启 3 分钟内）从工作台发一条 → 502 detail 必带「适配器未就绪（后端启动中）…（adapter_not_ready）」、收件箱一条 failed 行气泡带原因、backend.log 有 `[send] fail conv=… reason=… kind=adapter_not_ready`。
- **没做 / 刻意不做**：① 不改 `telegram_companion_worker.py` 让 A 线 worker 自己带 error（不在 P-5 归属，微信 / QQ 线在动 integrations）——根因留在 #259 note 里，谁接 `_send_text_inner` 顺手补 `"error": "client not connected"` 即可；② `zh_hant_auto.py` 生成物未再生（他人在途），新键回落简体；③ MTRCH2 新规则下 dry-run 会按关联码 RRYKF6 挂 #259——不算错（它确实关联），但人工已归位 #260，ticket.txt 末行为准。
- **他人在途**：`unified_inbox_send_routes.py` 实施97 hunk（L2301，kf_window_guard / kf_session）仍在工作树未提交，本线未碰。
- 用到的临时脚本（可删）：`tmp/p5_inspect*.py` 只读巡检 / `tmp/p5_ledger_0908.py` 台账三步 / `tmp/p5_fix_phantom_rows.py` 存量 12 行 / `tmp/p5_after_cb.py` 回访后核对 / `tmp/p5_hunkcheck.py` stage_hunks 预检 / `tmp/p5_cb_*.txt` 回访正文。
