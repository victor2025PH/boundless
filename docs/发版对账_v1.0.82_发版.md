# 发版对账 v1.0.82 — 发版线 R82（单一版本：Q-18 + Q-19 + 09-11 16:3x 全部提交批 → 一个 clean 包 → 公共渠道 + 内测渠道桥接 + 坐席三台）

> 规矩：`.cursor/rules/chatx-desktop-install.mdc` 「单一版本（D-Q11）」——一个 semver = 一个 clean 包，不打 smart / 内测包；内测渠道只做同字节桥接。前例 `docs/发版对账_v1.0.81_发版.md`（本表按其结构逐节对齐）。
> 范围：`docs/Q批_进度总账.md` Q-18（收工，#292 #291 #293 #285 + #278 追加 → 本版把 cum 误触归到 #284）、Q-19（收工，#294 #272 追加）随本版；**Q-20 / Q-21 / Q-22 未开工不在本版**（#178 #289 #290 #286 #287 #288 不标 fixed）。HEAD 上 `06951ac4..8ae9b48a` 全部已提交内容一并进包：用户管理 P0（`911abdff` `6f855069` `21fcb3b9` `41b782f7` `c2716c6a`）、xlate-pop 自适应（`7c4aae3b`）、cp-goal 槽位按钮 #263 #264（`1e93486c`）、老板 16:3x「全部提交并进包」四批（`d0e5303b` 成本 / `c26afdc6` 运维降噪 / `8e8896b7` 合规 + 微信 KF / `d4055b4d` 控制台）。
> 接手方：先 `git -C D:\boundless log --oneline -12` 核对本表 commit 真在。意向板 slug `r82-single-public-release-1-0-82-desktop-package`（18:2x 登记，热区 10 分钟静默核过）。
> 执行：2026-09-11 18:2x → 20:3x，见 §5。

## 0. 一句话

**1.0.82 单包** `ChatX-Setup-1.0.82.exe` **523,068,004 B** sha256 `5acac8efcfd2ee9e94b0da1f6eea140dbe6f4349cad412dd0719fe120c47f434`（499 MB，clean 形态，`MZ` 头核过，干净 worktree `D:\boundless-r80@5ccc0013`）。内容 = 1.0.81 公共版全部 + **Q-18**（让位 = 延后不丢稿 + 接回 chip / 今日拦截卡 / 成人词消歧）+ **Q-19**（画像只写可锚定事实 + N/10 只数 confirmed）+ 用户管理 P0 + xlate-pop + cp-goal 按钮 + 老板 16:3x 四批 + 小智词条 4 / how-to 3。坐席三台 **yunsheng / kouxing / lianbei 全 UP · 1.082 · clean/public**（19:04 / 19:07 / 19:09 smoke 六端点全 OK + vision active，数据保留）。公共渠道 + 内测桥接 + 官网 + 台账 6 单 + 回访 6 条 + 群 / 频道播报见 §4。**1.0.82 对 1.0.81 零功能性回归**（§3）。

## 1. 本版收掉的坑（Q-18 / Q-19 根因，摘自各自落点表）

| 事故 | 根因 | 本版 |
|---|---|---|
| 华哥 16:46–16:51 手发后客户 5 分钟无回（#292 #291） | Q-3 `_human_priority_gate` 判 `agent_sent / agent_typing` 时稿直接 `cancelled`，界面零提示 | 改 **defer**：稿留 pending、`until=坐席最后活动+60s`、窗过 `resume=agent_window_passed` 自动发；会话头 `ay-` chip 倒数、点 chip / 重选全自动立即接回；体检面板 `agent_yield` finding |
| Mizuki × Jeeo「message cum reply」判成人需人工（#284 / #278 追加） | `_EXPLICIT` 裸 `cum(?:ming\|shot)?` | `neutralize_cum` + 强上下文才判 + `_AMBIGUOUS_EXPLICIT` 孤立歧义词降级 |
| 「今天被拦了多少、为什么」只在日志（#293 #285） | 无台账 | `abort_ledger:v1` KV 滚动 200 + 回复设置页「🚧 今日拦截 · AI 为什么没回」卡 + `why_no_reply.py` 段 |
| 画像职业写进我方日语稿整句、年龄乱填（#294） | 写口只查槽名 / 来源，无类型校验、无原话锚定、出站进 prompt | `slot_validate` 每槽尺子 + `anchor_reason`（最近 30 条入站逐字）+ `lang_mismatch_reason` + `run_extraction` 不送 reply；前端 N/10 只数 confirmed + 「待确认 M」 |

## 2. 开工基线（18:2x）

| 项 | 事实 |
|---|---|
| 主干 | HEAD `8ae9b48a`（Q-18 E / Q-19 D 收工后的文档 commit）；上一 release commit `06951ac4`（1.0.81） |
| 意向板 | 索引 FREE；热区（desktop / shared/inject / messenger-web）近 10 分钟零改动（仅 `deploy/desktop/*` 两文件 11.8 min，非热区）；zhiliao 上次重启 583 min 前 |
| 工作树 | 别线脏文件（`ops/bug_intake.py` / `i18n_packs/bug_tickets_page.py` / `tests/test_bug_*` `test_duty_*` / `tools/duty_auto_ticket.py` `duty_channel_reminder.py` / `deploy/desktop/SEAT_LOG_MONITOR_LOG.md` 等）**未动、未进任何 commit**——干净 worktree 只取 commit |
| 版本链 | `desktop/package.json` 1.0.81→**1.0.82** / 1.081→**1.082**（字节替换，CRLF 168→168）；`python -m scripts.i18n_hant generate` → **17510 键**；`gen-chatx-changelog add --version 1.0.82 --tags fix,improve,feature` **10 条双语**（让位延后 / 今日拦截卡 / 成人消歧 / 画像锚定 / N/10 / 退出真退出 / 用户管理重置密码 / xlate-pop / cp-goal 按钮 / 老板 AI 花费卡）；`bump_ui_build.py --severity notice` → **20260911-1838**；小智 `help_terms.py` +4（`agent_yield` / `abort_ledger` / `adult_cum_disambig` / `profile_anchored`）+ `howto_pack.py` +3（`agent-yield-resume` / `blocked-today-card` / `profile-anchored-facts`）；`test_howto_coverage` + 帮助页 / 来源隔离 / 助手核心 / 教程 **60 passed**、`test_i18n_coverage -k help/hant` 6 passed → 主干 **`5ccc0013`**（18:41） |
| 官网五值 | `chatxContent.ts` version / filename / url / sha256 / 注释 → 主干 **`e008298c`**（19:11）；`gate:content` 发布前 5/6（manifest 仍 1.0.81，预期） |

## 3. 回归 / 门禁（干净 worktree `D:\boundless-r80@5ccc0013`，四项并跑 18:43 起）

| 项 | 结果 |
|---|---|
| worktree 复用 | `D:\boundless-r80` 原 `06951ac4`：`git checkout -- .` 回滚 13 个 R81 构建改写的 `i18n-packs-staged`，`git clean -f` 只清该目录 6 个未跟踪构建产物，再 `checkout --detach 5ccc0013`（这些产物 R81 已拷回主干入库，worktree 里是重复副本） |
| `import_sweep` | modules=**1248** bad=0 / from-import names=**3638** bad=0 **ALL GREEN**（59s） |
| predist（clean） | `build:backend` 全量 + `dist:win:clean` 18:43–19:01：后端冒烟全过、干净包产物冒烟全过（首启零数据）、after-pack 随包交付物 **22 项**；`latest.yml` version 1.0.82 size 523068004 |
| `gate_sweep -NoSyntaxScan` | **18 failed / 1693 passed**（14:39），复跑仍 18。**与 R81 的 18 条逐条同名**：14 项 R79 已登记旧债（`assistant_qa_eval` ×2 / `deploy_profiles` / `inline_color_ratchet` ×3 / `orphan_refs` / `legacy_blue` ×2 / `input_theme_contrast` / `silent_exception` / `takeover_trigger_surface` / `copilot_tiny_font` / `workspace_emoji`）+ `sealed_templates_no_legacy_date_formatters`（Q-14 B）+ `ui_build_stamp_not_stale`（mtime 伪红，主树绿）+ `sql_phantom_columns`（TK-3）+ `no_dynamic_dot_access_concat`（Q-15）。Q-18 E 新加的三文件（`test_adult_grader_gate` / `test_risk_grader_gate` / `test_agent_yield_defer`）在清单内全绿。**0 条新增** |
| 全量回归（干净 worktree） | `scripts\regression.ps1` **127 failed / 27437 passed / 40 skipped，1:12:53**（生产在线 → BelowNormal + 并行度 4，与构建 / gate_sweep 并跑）。串行复跑 127 → **104 仍红 / 23 绿**（23 条并行 flaky：`test_risk_hold_q3` ×5、`care_send_now_243` ×4、`whatsapp_ingest_backfill` ×3 等）。**同 127 条在 1.0.81 release commit `06951ac4` 的临时 worktree `D:\_r82cmp` 上复跑：98 红 / 28 绿 / 1 skip**。差集「1.0.82 红 / 1.0.81 绿」= **6 条**：① `test_ui_build_stamp_not_stale` = worktree checkout mtime 伪红（`users.html` 18:41:59.03 vs 戳 18:41:58.99，主树绿）；② `test_avatar_voice_rescue::…rescue_broken_overrides_fix_line` = `c26afdc6` 告警卡 v2 把「救援链已停用 … /ENABLE」改成「自动救援已停用 … 重新启用」，测试未跟；③ `test_manual_outbound_extract_o2::…schedule_log_line…` = `d0e5303b` 在 `_schedule_episodic_memory_extract` 前置 `_episodic_extract_skip_reason` 成本门禁，测试桩 `_LogSM` 缺该属性；④ `test_dormant_review_p2::test_frontend_dr_wiring_static` 钉 `unified-inbox.css?v=20260908dr`，`7c4aae3b` 已前移到 `20260911a`；⑤⑥ `test_goal_discovery_pause_confirm_264` / `test_goal_probe_active_leg_236` 钉 `cp-goal.js?v=20260910[abcd]` 闭集，`1e93486c` → `20260911e`、`6563c3c9` → `20260911f`。**五条全是测试钉子过期，0 条功能性回归**；主干 **`da66a0f9`**（20:06）扶正：②断言跟新文案 / ③桩补 `staticmethod(SkillManager._episodic_extract_skip_reason)` / ④⑤⑥ 闭集改下界。安装包不含 tests，**1.0.82 包不重打**（字节 = `5ccc0013` 构建）。剩 **98 条两版同红** = HEAD 既有债（§6） |
| Q-18 / Q-19 门禁 | 随 gate_sweep：`test_adult_grader_gate` / `test_risk_grader_gate` / `test_agent_yield_defer` 绿；`test_profile_fill_anchor_q19` 在全量里绿（不在红表） |
| 未做 | zhiliao 活体核对（zhiliao 跑共享工作树不是安装包；本线未重启它——20:28 `watchdog_start` 已重启并载入 Q-18 D 路由，见 §6）；`profile_purge_unanchored.py --apply`（Q-19 交接项，值守核清单后做） |

## 4. 全链

| 步 | 状态 | 事实 |
|---|---|---|
| 坐席三台 | ✅ 19:02–19:09 | `push_chatx.ps1 -TargetSsh {yunsheng,kouxing,lianbei} -Setup <r80>\dist-clean\ChatX-Setup-1.0.82.exe -Install -Relaunch -Smoke`：三台 `[chatx] OK`、seatverify sentinel FRESH + 18799 login 200、smoke `app version: 1.0.82` 六端点 OK + vision active + pacing ok；`chatx_fleet_status` **三台 UP · 1.082 · clean/public**。日志 `%TEMP%\r82\push_{yunsheng,kouxing,lianbei}.txt` / `fleet.txt` |
| publish 公共 | ✅ 20:06–20:14 | 19:1x `-DryRun` 过 → `publish_chatx.ps1 -DistDir <r80>\dist-clean -Yes -NotesFile announce_1082.txt`：build valid sha512 ✓ → VPS exe 校验 ✓ → 指针 + pm2 reload (rolling) → R2 同步 → 公网核 `latest.yml=1.0.82 manifest=1.0.82 exe=HTTP206` → `announcements.json` 带 `release-v1.0.82`（20 条）→ 清理 1.0.79。日志 `%TEMP%\r82\publish_public.txt` |
| publish 内测渠道**桥接** | ✅ 20:25–20:28 | 同一个 clean exe + blockmap + `latest.yml` 复名 `latest-internal.yml`（`%TEMP%\r82\internal_bridge`，sha256 与 dist-clean 逐字节同 `5acac8ef…`）→ 19:1x `-Channel internal -DryRun` 过 → `-Yes`：`latest-internal.yml=1.0.82 manifest=1.0.82 exe=HTTP206`，公共指针未动（`latest.yml still v1.0.82`），清理 1.0.79。**不是第二个包** |
| 官网 | ✅ 20:28–20:31 | `gate:content` **6/6 PASS**（发布事实源 1.0.82 / 5acac8ef… 一致；更新记录最新 v1.0.82；测试数 1852 ≥ 宣传 1100+）→ `deploy.ps1 -FromHead` → **部署完成 [OK]**（healthy / webhookSecret / adminKey / deepseek 全 True，R2 同步）。日志 `%TEMP%\r82\website_deploy.txt` |
| 台账 / 回访 / 播报 | ✅ 20:31–20:35 | `duty_update_hint --set "1.0.82 已发布（单一版本）：…手装 …/dl/downloads/ChatX-Setup-1.0.82.exe…装完请重启一次工作台后端。修复：…（#292 #291）（#293 #285）（#284）（#294）"`（热重载）→ 报障群播报 `duty_reply --group neice --text-file broadcast_1082.txt --no-mention` → **mid 1453**（20:32:22，末段明示 #178 #289 #290 #286 #287 #288 随下一版）→ `duty_ledger_fix --mark-fixed-file mark_fixed_1082.json --apply`：**#284 #285 #291 #293 new→fixed、#292 #294 confirmed→fixed**（fix_note 各 ≤300 字取 Q-18 / Q-19 落点表口径）→ 逐单回访 6 条 `bug-intake/reply ok`（20:33:14–20:34:37，间隔 15s，每条带工单号 + 只给版本号 + 验收三步）→ `duty_notify_summary --reporter 8942577244 --ids 292,291,293,285,284,294 --msg-id 1453 --apply` **记账 6 单，该筛选积压 0**（全局 fixed 未回访 67 单为历史存量，非本版）→ 频道 @hykj7 **messageId=163**（campaign `release-1082`，`%TEMP%\r82\channel_post.py`） |
| release 真值 commit | ✅ | `desktop/build/i18n-packs-staged` 自 worktree 构建拷回（**8 改 + 4 新**：`abort_ledger.py` / `agent_yield.py` / `ops_act.py` / `zh_hant_team_page.py`）+ 本表 + Q 总账 R82 行（见 git log） |

## 5. 断点 / 交接

- 18:2x 读总账 / SOP / R81 前例 / Q-18 Q-19 落点表 → 意向登记 + 热区静默核 → 18:41 版本链 `5ccc0013` → 18:43 worktree 回滚 + detach → 18:43 回归 / 后端全量 + 单包 / import_sweep / gate_sweep 四项并跑 → 18:44 import_sweep 绿 → 18:59 gate_sweep 18 红（同 R81）→ 19:01 包出（`MZ` / sha256 / latest.yml 核）→ 19:02–19:09 坐席三台 + fleet → 19:11 `e008298c` 官网五值 → 19:1x 两个 publish dry-run → 19:56 回归收口 → 19:55–19:59 串行复跑 ×2（`D:\_r82cmp@06951ac4` 对照）→ 6 差异逐条定性 → 20:06 `da66a0f9` 五钉扶正、`_r82cmp` remove → 20:06–20:28 publish 公共 + 桥接 → 20:28–20:31 官网 → 20:31–20:35 台账 / 回访 / 播报 → 本表 + 总账 → 意向 -Done。
- worktree `D:\boundless-r80@5ccc0013` 保留（`dist-clean\ChatX-Setup-1.0.82.exe`，另存 1.0.80 / 1.0.81 两个旧 exe 可删）；`D:\_r82cmp` 已 `worktree remove`；`D:\boundless-wx` / `D:\boundless-r79` / `D:\boundless-r78` 仍可回收磁盘。
- 临时文书全在 `%TEMP%\r82\`（notes 双语 / announce / broadcast / channel_post / mark_fixed json / reply\*.txt / 各步日志），未入库。

## 6. 交回各线

- **Q-18 / Q-19 装载**：`adult_grader / autosend_policy / autosend_worker / abort_ledger / protocol_autoreply / reply_diagnosis / 两条路由` 与 `profile_slots / profile_fill / profile_llm / user_clock_resolver` 均为 `.py`，坐席三台已随包装载（seatverify 新后端）；**zhiliao 已于 20:28:05 被 `watchdog_start` 重启**（`restart_cooldown/last_restart_zhiliao.json`，非本线操作）——共享代码根当时 = HEAD 含 Q-18 / Q-19，`-Done` 收尾时只读探针 `GET /api/reply-settings/abort-ledger` 回 401（对照假路由 404）→ Q-18 D 路由已在线，视为已装载。值守可直接按 Q-18 §1.1 / Q-19 §四 回访：`[autosend] defer=` 出现 / `abort_ledger:v1` 起记 / `[profile] drop … reason=` 出现、`[episodic] residence captured` 不再带整句。
- **Q-19 存量**：`tools/profile_purge_unanchored.py --dry-run` 35 命中（11 真客户）未 apply，值守核清单后 `--apply`（可 `--platform/--chat-key` 单会话）。
- **HEAD 既有 98 条两版同红（安装包同样带着）**：家族与 R81 §6 同——`image_autosend` ×7 / `caption_truthfulness` ×3（Q-6 有意变更未更新断言）、`aline_disclosure_wiring` ×4（合规线 `8e8896b7` 已入库仍红：`_Sender._presend_blocked() got an unexpected keyword argument 'peer'`，请合规线看 sender 签名）、`workspace_sidebar` ×5 / `group_show_console` ×5 / `unified_inbox_stage1` ×3 / `stale_update_prompt` ×3 / `business_line_ceiling` ×3 / `document_file_translate` ×3 / `assistant_qa_eval` ×2 等；`test_goal_expiry_settlement_236` / `test_goal_ui_revamp` 两条与本版扶正的 ⑤⑥ 同病（`cp-goal.js?v=` 闭集钉），1.0.81 已红故未在本线改，Q-8 / #236 线可照 `da66a0f9` 的下界写法一并扶正。
- **测试钉子写法**：`da66a0f9` 把三处缓存戳闭集改为「≥ 下界」——以后 bump 戳不必再改测试；其他把 `?v=` 钉死的测试建议照改。
- **未进本版**：Q-20（#178 追加）/ Q-21（#289 #290 #286）/ Q-22（#287 #288）未开工，随下一版；群播报末段已明示。#272 追加（1.0.79 启动日志对照）随 #294 口径在回访里带过，台账未单独标。
- **skuio 回访待收**：#292 / #291 / #293 / #285 / #284 / #294 各三步（见 `%TEMP%\r82\reply\*.txt`，正文已发群）；升级后请重启一次工作台后端。
