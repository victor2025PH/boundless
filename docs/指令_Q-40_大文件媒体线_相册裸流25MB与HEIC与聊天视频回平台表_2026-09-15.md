# 指令 Q-40：大文件媒体线——相册裸流 + 图 25MB / 视频 100MB + HEIC 转码；聊天视频压顶回平台表；上限与失败文案同源

> 预算 **$120**。工单 **#316 追加**（钧 09-13 相册 2MB）+ **#322**（相册 UX，Q7HJDU / 4KV58W：删图回顶、大图独立窗）+ 无单需求「相册可传大图、聊天可发大文件图片 / 视频」（老板 09-15 19:52）。落点表 `docs/发版对账_v1.0.86_Q40.md`（新建）。
> 前置：Q-39 段 0 已把相册 2MB 豁免提交；M-3 A/B/D（send-media 裸流 / 视频换封装 / 四分类失败面）已在库；#169 `media_limits.py` 平台表已在库。本条不重做这些，只把相册拉到 send-media 同一水平，并撤掉视频临时压顶。
> 分析画布：`C:\Users\Administrator\.cursor\projects\d-boundless\canvases\unfixed-bugs-three-angle-plan.canvas.tsx`「大图 / 大视频」一档。

## 事实（HEAD 代码只读核对）

| 层 | 相册上传 `POST /api/personas/{pid}/media` | 聊天 `POST /api/unified-inbox/send-media` |
|---|---|---|
| 全局 body 闸 | 默认 2MB（`admin.py` L731）；豁免 `is_persona_album_upload_path` 已写（Q-39 段 0 提交） | 已豁免（`_BODY_LIMIT_CODE_EXEMPT_PREFIXES` L763） |
| 路由上限 | 图 **10MB** / 视频 **50MB** / **3 分钟**（`persona_media_routes.py` L63–65） | `media_limits.py`：LINE 100 / TG 200 / WA 64 / 其余 25；视频再 `min(平台, DEFAULT_VIDEO_CAP_MB=50)`（D-M5 临时压顶，注释写明「M-6 拿到四平台 98MB 真机证据后改回 0」） |
| 传输 | multipart 整包进 Starlette 临时文件；无进度 | 裸流 `request.stream()` + 1MB 写缓冲；超限 `_drain_stream` ≤256MB 再回 413；XHR `timeout=300000` 固定；进度条 |
| 客户端预检 | 无体积预检；HEIC/HEIF 后端 `ext_not_allowed` | `send-caps.media_max_mb / video_max_mb / video_exts` 同源预检；格式 / 上限 / 明确失败 / 无响应四分类 |
| 失败文案 | Q-35 `pma_fr_*` 人话已在；但 413 若在路由前发生仍显示「网络中断」 | 413 带「X MB 超过 Y MB（超出 Z）」 |
| UX | 删一张 `pmaDeleteItem` → `await pmaLoad()` 整页回顶（L3982 / L3088）；大图弹独立窗口，关窗连带退出 | — |

结论：聊天栈是对的，缺「撤压顶 + 超时随体积 + 上限提示」；相册栈要**学聊天栈**，不另起一套。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 相册改裸流** | `persona_media_routes` 上传口新增裸流分支：`Content-Type` 非 multipart → body 即文件，元数据走 query（`name / triggers / caption / tags / client_msg_id`）；multipart 旧分支保留（脚本 / 测试）。落盘复用 `unified_inbox_send_routes._send_media_streamed` 的读流 / 写缓冲 / 超限吞流片段（抽成 `src/web/media_stream.py` 共用，send-media 改 import，行为零变化）。每条退出路径落 `[pmedia] … reason= size= ms=`。`personas.html pmaUpload` 走 XHR 裸流 + `upload.onprogress` → Q-35 结果条每文件进度；`media_stream_upload` 能力键复用 send-caps 思路（后端无键＝仍 multipart） | `persona_media_routes.py`、新 `media_stream.py`、`unified_inbox_send_routes.py`（只改 import）、`personas.html` |
| **B 相册上限 + 转码** | 图 **25MB** / 视频 **100MB / 5 分钟**（常量改 `inbox.persona_media.limits` 可配，默认如上，夹紧 1–2048）。HEIC / HEIF：前端选文件即用 canvas 解码转 JPEG 0.9（Chromium 不解 HEIC 时后端 `pillow-heif` 兜底；两边都不行才 `ext_not_allowed` 并写明「请在手机相册导出为 JPEG」）。长边 >4096 或体积 >8MB 的静图**前端压一刀**（长边 4096 / JPEG 0.85）再传，原图不落库；PNG 透明保留 PNG。视频不压，超长 ffprobe 仍拒 | `persona_media_routes.py`、`personas.html`、`requirements` 如加 pillow-heif（可选依赖，import 失败即跳过） |
| **C 聊天视频回平台表** | `media_limits.DEFAULT_VIDEO_CAP_MB` 改 **0**（不压，回平台表 LINE 100 / TG 200 / WA 64）；Messenger 无真机证据保持平台表外 25（`PLATFORM_MEDIA_CAP_MB` 不加 messenger）。`_xhrSendMedia` `timeout = max(300s, size_MB × 3s)`；附件条第一行写「本会话最多 N MB（LINE）」（数字来自 `send-caps.video_max_mb / media_max_mb`，按文件类别）；超限在选文件阶段拦（已有）文案带「还差 Z MB」（已有 `describe_over_cap`）。静图 >8MB 给附件条一键「压后发送」（长边 2560 / JPEG 0.82），默认仍发原图 | `media_limits.py`、`unified_inbox.html _stageMedia / _xhrSendMedia / _mediaItemHtml`、`inbox_workspace.py` 词条 |
| **D 相册 UX（#322）** | `pmaDeleteItem` 成功只移除该卡片 + 更新计数，不 `pmaLoad`；失败才整刷并保住 `scrollTop`。大图改**页内灯箱**（复用工作台既有图片预览层或最小 `pma-lightbox`），Esc / 点遮罩关，关灯箱不关抽屉；多选批量删已有 `_alSel` 路径，补「删除所选」按钮。零新颜色，用 `--tk-*` | `personas.html` |
| **E 门禁 + 真机** | `tests/test_media_stream_q40.py`：A 裸流 25MB 图成功 / 26MB 413 带 size；multipart 旧路仍通；`/media/test` 仍吃 2MB。B HEIC 后端兜底（有 pillow-heif 时）；超长视频拒。C `media_cap_mb_for(video)` = 平台表；messenger 仍 25；`test_media_send_stream_m3` 零改动。D node 抽源：`pmaDeleteItem` 不再调 `pmaLoad` 于成功分支；灯箱不用 `window.open`。**真机**：117 或坐席一台，LINE 发 60–90MB MP4（`tools/probe_line_media.py --send-self --kind video --max-mb 120 --confirm`）+ TG 150MB，回读 magic；相册传 iPhone HEIC 与 8MB PNG 各一，结果条成功 | tests、`docs/发版对账_v1.0.86_Q40.md` 真机表 |

## 红线

- 不突破平台官方上限硬发；Messenger 网页链路没实测过，不放开。
- 相册不当网盘：不接 RAW、不存原图与压缩图两份、不接 4K 十分钟成片。
- 不改 Q-6 匹配 / 标签 / `media_auto_tag` 建议词链；只改传输与上限。
- send-media 抽共用模块时**行为零变化**（`test_media_send_stream_m3` 全绿），不顺手改四分类文案。
- 与 Q-39 D 同文件 `unified_inbox.html`：本线只动 `_stageMedia / _xhrSendMedia / _mediaItemHtml` 三个函数，`stage_hunks` 按函数切；`personas.html` 与 Q-39 无交集。
- `.py` 须 zhiliao 重启才生效；本线**不许自己重启**。模板热更。
- 不碰工作树其他线的脏文件；`git add` 显式路径，提交前 `git diff --cached --name-only` 只含本线文件。#316 / #322 只改 confirmed + fix_note 草稿，fixed 由发版线打。

## 开新对话粘贴

```
读 docs/指令_Q-40_大文件媒体线_相册裸流25MB与HEIC与聊天视频回平台表_2026-09-15.md，按它做 Q-40（#316 追加 + #322 + 老板要求「相册可传大图、聊天可发大文件图片/视频」）。前置：Q-39 段 0 已提交相册 2MB 豁免（admin.py is_persona_album_upload_path）——先 git log 核对，没提交就先按 Q-39 段 0 提交它。A 相册上传口加裸流分支（非 multipart → body 即文件、元数据走 query），落盘复用 send-media 的读流/写缓冲/超限吞流片段抽成 src/web/media_stream.py 共用（send-media 只改 import，test_media_send_stream_m3 零改动全绿），personas.html pmaUpload 改 XHR 裸流 + onprogress 进 Q-35 结果条，每条退出路径落 [pmedia] reason= size= ms=。B 相册上限图 25MB / 视频 100MB / 5 分钟（inbox.persona_media.limits 可配）；HEIC 前端 canvas 转 JPEG、后端 pillow-heif 可选兜底、都不行才 ext_not_allowed 并写明导出 JPEG；静图长边>4096 或>8MB 前端压一刀（4096 / 0.85，PNG 透明保留）；视频不压。C media_limits.DEFAULT_VIDEO_CAP_MB 改 0 回平台表（LINE 100 / TG 200 / WA 64，messenger 不加、仍 25）；_xhrSendMedia timeout=max(300s, MB×3s)；附件条写「本会话最多 N MB（平台）」取 send-caps；静图>8MB 给一键「压后发送」默认仍原图。D #322 pmaDeleteItem 成功只摘卡片不 pmaLoad、失败整刷保 scrollTop；大图改页内灯箱不用 window.open，关灯箱不关抽屉；补「删除所选」。E tests/test_media_stream_q40.py 覆盖 A–D + 真机：LINE 60–90MB MP4 / TG 150MB（tools/probe_line_media.py --send-self --confirm 回读 magic）+ 相册 iPhone HEIC 与 8MB PNG 结果条成功，写进落点表真机表。预算 $120。开工先 scripts\agent_probe.ps1 -Intent 登记；不突破平台官方上限、Messenger 不放开、相册不存两份、不改 Q-6 匹配链；unified_inbox.html 只动 _stageMedia/_xhrSendMedia/_mediaItemHtml 三个函数（Q-39 D 同文件另处）；不自己重启 zhiliao；每个 commit 带 #316 或 #322 并立刻补落点表 docs/发版对账_v1.0.86_Q40.md 一行；git add 显式路径且提交前 git diff --cached --name-only 只含本线文件；#316 #322 只改 confirmed + fix_note 草稿不标 fixed。收工只改 docs/Q批_进度总账.md 的 Q-40 行并 agent_probe -Done。
```
