# 指令 Q-32：Composer 最多两层 + 翻译二级选择改抽屉 + 空状态三步卡

> 预算 **$140**。五视角方案 P1-8/9/10（`docs/分析_聊天工作台_五视角美化优化方案_2026-09-12.md` §五）；关联工单 #290 #286（Q-21 翻译下拉可见性，1.0.84 已修 #289，本条收口 UI 形态）。落点表 `docs/发版对账_v1.0.85_Q32.md`。
> 前置：Q-30 D（`inbox.reason.*` 人话表）——草稿条原因 chip 取词依赖它；未落时先用现有键、留 TODO 键名不留硬编码文案。

## 事实（代码只读核对）

- Composer 栈 `unified_inbox.html` L980–1060 **13 层**：`quote-reply-bar` / 编辑条 / 媒体队列 / 语音预览 / `cdraft-bar`（草稿 + 原因 + 3 按钮）/ `outxl-preview`（译文预览）/ slash 面板 / mention 面板 / `lang-warn-bar` / `identity-bar` / `acct-out-banner` / textarea / 工具条；多个同时出现时输入框被顶到视口外（405 px）。
- 翻译弹层 `#xlate-pop`（L20928 `_persistXlatePrefs` 一带）：收 / 发两个选择器 + 语气 + 反译 + 预览 + 本会话 / 全局 + 自动检测，全部在一个 popover 里；`--xl-*` 令牌 L148–165 独立紫 / 青 / 绿三族色相，与品牌蓝并列。
- 空状态 `#chat-empty` 只有一句「选择一个会话」；首启无账号 / 无人设时用户不知道下一步。

## 要做的四段

| 段 | 内容 | 令牌 / 文件 |
|---|---|---|
| **A 上下文层互斥** | 引用 / 编辑 / 媒体队列 / 语音预览 **只出其一**（进新的自动关旧的，带 `uiIcon` 关闭），`--bg-soft` 底 + `--line-soft` 上边线，高 36；slash / mention 面板改**浮层**贴 textarea 上沿（`position:absolute`，`--bg-elev` + `--tk-shadow-md`，`--z-pop`），不占流 | L980–1060、`.quote-reply-bar` 等 CSS |
| **B AI 草稿层一行** | `cdraft-bar` = `uiIcon('sparkles')` + 「AI 草稿已就绪」+ 原因 chip（`inbox.reason.*`）+ 采用 Tab / 忽略 Esc 两按钮（`cdraft-btn` 26 高、`--tk-radius-sm`）；正文默认折叠，点「查看」展开 ≤4 行；`outxl-preview` 译文预览并入此层（同一行切换「原文 / 译文」）；`lang-warn-bar` / `identity-bar` / `acct-out-banner` 从 composer 撤走（Q-31 B 并入状态带；若 Q-31 未落，本条先隐藏其中重复的两条，只留一条） | `.cdraft-bar` CSS、L1020 一带 |
| **C 翻译抽屉** | 工具条只留「收→ / 发→」两个选择器 + 「本会话」小标 + 自动检测标签；语气 / 反译 / 预览 / 全局默认 → 右栏抽屉（与助手同层，`--bg-elev` + `--tk-shadow-md`，`--z-drawer`）；`--xl-accent → var(--tk-brand)`、`--xl-accent-bg → var(--bdg-info-bg)`，紫 / 青 / 绿三族只留媒体 / 语音 / 成功各一处；粤语等 Q-21 lang_catalog 项在选择器里可见可搜（不倒退） | `#xlate-pop`、`--xl-*` L148–165、`_persistXlatePrefs` |
| **D 空状态 + 门禁** | `#chat-empty` 三步卡：① 选人设 → ② 选档位 → ③ 看状态带，每步 `uiIcon` + 一句 + 按钮（`--tk-brand`）；无账号时第一步换「连接渠道」指向渠道向导；`tests/test_composer_q32.py`：同一时刻 composer 上下文层 DOM 可见 ≤1、草稿层 ≤1；`#xlate-pop` 一级控件 ≤4；`--xl-accent` 解析值 = `--tk-brand`；空态三步渲染 + 无账号分支 | `#chat-empty`、tests |

## 红线

- Q-21 已落的语种目录 / 粤语 / 字体不倒退（`test_xlate_*` 全绿）。
- 键盘：Tab 采用 / Esc 忽略 / Enter 发送 与现状一致。
- 只用现有令牌；`--xl-*` 只改指向不新增变量。
- `unified_inbox.html` 共享文件：`stage_hunks.py --mine`。

## 开新对话粘贴

```
读 docs/指令_Q-32_Composer两层与翻译抽屉与空状态_2026-09-12.md，按它做 Q-32（五视角 P1-8/9/10：A composer 上下文层（引用/编辑/媒体队列/语音预览）互斥只出其一，slash/mention 改浮层贴 textarea 上沿不占流 / B cdraft-bar 收成一行 sparkles + 「AI 草稿已就绪」+ 原因 chip（inbox.reason.*）+ 采用 Tab / 忽略 Esc，正文默认折叠点查看展开 ≤4 行，outxl-preview 并入同行原文/译文切换，lang-warn-bar / identity-bar / acct-out-banner 从 composer 撤走 / C 翻译工具条只留收→/发→ + 本会话小标 + 自动检测，语气/反译/预览/全局默认进右栏抽屉，--xl-accent→var(--tk-brand)、--xl-accent-bg→var(--bdg-info-bg)，粤语等 Q-21 目录项不倒退 / D #chat-empty 三步卡（选人设→选档位→看状态带，无账号首步改连接渠道）+ 门禁 tests/test_composer_q32.py）。预算 $140。先读 docs/分析_聊天工作台_五视角美化优化方案_2026-09-12.md §二 §五 P1 与 Q-21 指令文件，再 scripts\agent_probe.ps1 -Intent 登记；只用现有令牌；Tab/Esc/Enter 键位不变；unified_inbox.html 用 python tools/stage_hunks.py --file <路径> --mine <regex> --apply 只暂存自己的 hunk。每个 commit 带 Q-32 并立刻补落点表 docs/发版对账_v1.0.85_Q32.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-32 行并 agent_probe -Done。
```
