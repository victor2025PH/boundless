# 实施93 · 报障群清底二批（识图泄漏 + 遗留批 13 单）— 2026-08-31 深夜

> **给新修复对话的开工输入**。前情：实施92 全部主力单已被修复线打穿
> （#75/#78/#88/#101大部/#109/#111/#113/#115/#116/#120/#121/#54 全 fixed，
> 待 v1.0.65/66 发包）。本批=**剩余全部未修**：1 张新单 + 13 张遗留（0827-28
> 时代，实施91/92 派过无人接的尾巴）。工单库
> `D:\chengjie-instances\zhiliao\data\config\bug_intake.db`，接单先读 body 值守 note。
> 修完标 fixed + fix_note；桌面包改动攒 **v1.0.66**（或搭 66 的车）。

## 0. 先决（发包与在途，确认后再开工）

- v1.0.65 / v1.0.66 若未发包先发（前者压着 #97/#106 击穿真修复 + 9 单，后者已
  攒 #54/#111/#88-lazy/#78 二轮/#75 二轮）——测试人验证潮等包。
- #101 出站图/视频半边受 `line.media.outbound` 开关（协议发媒体有账号风险，
  默认关待产品定夺）——**产品决策项**，修复线不动；若测试机开着仍发不出，
  发送链每步失败原因已落日志，按日志归因即可。

## 1. 新单（今晚唯一残留）

**#124（P2）识图内部标注块渲染进会话流**：客户发图后「AI 识图 1.可见物体与人物…
2.无票据/订单…」完整内部分析文本以消息块出现在聊天里（原图 937，LINE 会话）。
幻觉层已撤销（原图为插画，识图内容正确），只修泄漏：与 #115（翻译报错进流，
已修）同哲学不同注入点——**识图输出走内部通道，会话视图渲染白名单化**（借 #115
的收口机制把识图 meta 一并纳管，顺手排查还有哪些引擎 meta 可能进流：OCR/翻译/
识图/转写，一次白名单全堵）。验收：发图后会话零内部文本块，AI 回复正常引用图内容。

## 2. 遗留批 13 单（0827-28 时代，先复测再施工）

> 全部来自旧版本，**动工前先在 v1.0.65/66 复现**；复现不了直接标 fixed
> （fix_note 写「未能在 1.0.6x 复现，疑历史版本已修，请点按钮反馈」）。

### 2a. Messenger 发送失败家族（5 单一锅端）

#21/#23/#34/#49（发送失败/重登仍失败）+ #31（工作台消息重复显示而手机端正常）。
先对意向板确认 impl86 时代 bugfix-dev 认领态——撂了就接。修向锚点：messenger-web
会话健康上报 + send 快速失败闸 + 镜像去重键（引擎侧 platform_session_health /
messenger_send_semantics 有同族方法论）。#27（登录二次验证码输入框被成功提示
遮蔽）同域顺修（webview 弹层 z-index/时序）。

### 2b. 单件（按性价比排序）

| 单 | 症状 | 修向锚点 |
|---|---|---|
| #7 | 告警通道测试「失败 HTTP 200」文案自相矛盾 | 测试结果判读：200 被判失败，改判定+文案（小改） |
| #16 | 主动关怀「测试预览」报预览失败 | care preview 端点在客户端部署形态的依赖缺失，读 backend.log 定 |
| #52 | 克隆音色弹层点外部整层关闭、试听生成中误触即丢 | 弹层背板判脏（引擎 cp 组件有同款防误触约定可平移） |
| #39 | 输入框不定期不能输入、重启恢复 | 焦点/遮罩残留；难复现——v1.0.64 起 renderer 日志已收，等复发按日志归因，先挂「复现即取证」note |
| #43 | 客户说过生日 AI 反问是否生日 | 生日 capture（引擎 Stage S）在客户端链路是否生效；复测+读记忆库 |
| #25 | AI 生图点击无反应 | 出图落点=176 ComfyUI 对外网客户端不可达——先定性（设计缺口 vs bug）：要么接公网中继要么按钮置灰+提示，别静默 |
| #33 | 工作链切英文界面仍中文 | i18n 覆盖缺口（工作链页词条漏翻）；「与工作目标功能重复」后半转产品 |

### 2c. 复测前置组（值守协同清账，施工线可跳过）

#3/#4/#5/#6/#8/#9（媒体/语音发送失败旧案，大概率已被 TTS 迁移+隧道修复+#101
覆盖）、#10（护栏文案）、#11（显示不全）、#18（宿务→马尼拉单例翻译）、#28
（1.054 陈旧报告直接关）——值守在 v1.0.65 发包后组织测试人顺手复测，逐单销号。

## 3. 边界

- **不重复实施92**：其列出的主力单全部已修，勿再开工；本批只做 §1+§2。
- 等待项非施工：#93（等报障人回执）、#38/#40/#65（goal sprint pace 线在途）、
  #32/#36/#37（产品决策）、VPS 22 端口（老板控制台）。
- 共享树纪律照旧（agent_probe 登记意向 / `.py` 攒批重启窗 / 禁全树回退 /
  显式列文件提交）；验证闭环归值守。
