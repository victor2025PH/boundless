# 指令 Q-39：发送真相四闸——#327 跟图循环 / #326 翻译空串硬 HOLD / #323 要图意图闸 / #324 全部账号筹码 + #316 相册 2MB 豁免提交

> 预算 **$160**。工单 **#327 #326 #323 #324**（全部 skuio 1.0.85.0，报告 9PYWPG / THBSHN / XG3UZU / YS54YK）+ **#316 追加**（钧 09-13 02:54 mid 1548，代码已在工作树未提交）。落点表 `docs/发版对账_v1.0.86_Q39.md`（新建）。
> 前置都已收工、本条不重做：Q-30（单人端 / 状态带真闸）、Q-31（筛选面板系统标签折叠）、Q-35（相册逐文件可见 + `note_album_miss` 坐席侧一行）、Q-36（`detect_offer_media`：客户「我发你图」不当索图）、Q-21（翻译目录 / lang-plan）、M-1 B（`translate_hold:<reason>` 纪律）。
> 分析画布：`C:\Users\Administrator\.cursor\projects\d-boundless\canvases\unfixed-bugs-three-angle-plan.canvas.tsx`。

## 事实（证据 `tmp_diag\{9PYWPG,THBSHN,XG3UZU,YS54YK}`；HEAD 代码只读核对）

- **#327 9PYWPG** WhatsApp Nori/19892968016 × Cameron：客户连发自拍，我方全自动**每次跟发一张** Mizuki 相册图，三次 caption 高度同构（Found this old one of me / still kinda cute right / hope you like it）。截图里识图只描述客户照片，无索图、无我方承诺。最像的链：入站图 → LLM 草稿自带「分享旧照」→ `autosend_helpers` L1752 起的**承诺兑现链**（`assume_intent="selfie"`）把它当已承诺 → 相册出图。Q-36 `detect_offer_media` 只挡「客户提议发图被拒」，不挡「客户发图我方跟发」。**先查该会话 `[album_match]` / `[commitment]` / `assume_intent` 日志定真闸，再动手**——与 Q-30 B 同一纪律。
- **#326 THBSHN** Telegram kj/7543790794 × Vince 03:56：草稿中文、`[lang-plan] reply=en`，`[xlate] provider=none err=ai:empty` → `outbound_translate` HOLD → `autosend_worker` `translate_hold:ai:empty`。03:53 同会话 deepseek `timeout 31.6s`；05:27 起翻译全部恢复。纪律（不发原文）正确，缺的是：① 空串 / 超时**重试一次**（换引擎或再打一次）；② 仍空 → 用 `reply_lang` 目标语**重起草**（草稿本该是 en，中文草稿是上游 persona_reply 没吃 lang-plan）；③ 状态带人话 + 「重试翻译」一键。
- **#323 XG3UZU** Coop 03:47:31「I try to take my time an enjoy the view」无任何要图意图，`image_autosend` 仍以整句为 query 跑 `album_match candidates=0`，同句 03:47:53 又跑一次，红条计「今天 2 次 · AI 已改口」。代码：`image_autosend.py` L1085 `_ask` 已有守卫、L1130 `note_album_miss` 也只在 `_ask` 时写，但 `album_miss_marker.mark` 与 `conv_state` 侧把 `candidates=0` 当索图未满足；`_ask` 里 `_scene_cls or _scene_kind` 对普通句子会误真（`scene_class_of` 泛匹配）。
- **#324 YS54YK** 全部账号视图列表顶 `dormant:ignored 4 / risk:medium 3 / 客户要求停联 1`：点击无反应、无法关闭、处理后不消。Q-31 只改了**筛选面板** `_renderPanelTags`（L5677，`_isSysTag` L5661 折叠）；列表顶那一排是另一处渲染（同读 `_tagStatsCache`），没走 `_isSysTag`、没 click-filter、没 dismiss。用户判定：单人自用版非必须。
- **#316 追加**：`admin.py` `is_persona_album_upload_path` + 中间件 OR + `tests/test_album_body_limit_q35.py`（5 passed）+ `docs/发版对账_v1.0.85_Q35.md` D 行，**全在工作树未提交**。先提交它，别让它被别的线卷走。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **0 先提交 #316** | `git add` 显式三个路径（`admin.py` / `tests/test_album_body_limit_q35.py` / `发版对账_v1.0.85_Q35.md`）→ `git diff --cached --name-only` 只这三个 → commit 带 #316。工作树其余脏文件（label-leak / ASR / `desktop/main.js` / Q-26 C–E）一字不碰 | 三个路径 |
| **A 跟图循环** | 先用 9PYWPG 会话回放定真闸（`[album_match]` `assume_intent` `_bind_reason` 三行）。修法（按真闸取其一或叠加）：① **入站是图且无索图意图** → 本轮 `run_autosend_image` 与承诺兑现链一律不出相册，LLM 草稿若写了「分享照片」承诺走既有**撤回改写**（L780 `_retract_unfulfilled`）而不是兑现；② 同会话**图片跟随冷却**（默认 30 min）+ **每日上限**（默认 6，`inbox.image_autosend.follow_daily_max`），超了只发文字；③ caption 近重复：与最近 3 张出图 caption 归一后相似 ≥0.8 → 换 `caption_registry` / 空配文，日志 `[album_send] caption_dedup`。每次出图落一行 `[album_send] trigger=<inbound_image\|ask\|commitment> intent= candidates= picked= caption_src=` | `image_autosend.py`、`autosend_helpers.py` L1723–1900（兑现链）、`commitment_guard.py`（只加「入站为图」判据） |
| **B 翻译空串可恢复** | `outbound_translate` 收到 `ok=0 err∈{ai:empty, timeout, provider_unavailable}`：① 同引擎重试 1 次（间隔 2s），仍失败换路由表下一引擎 1 次；② 仍空且 `mode=auto_ai` → 调 `generate_persona_reply(reply_lang=plan.reply)` **按目标语重起草一次**（`[xlate] redraft lang=en reason=ai:empty`），通过 Q-23 / 风控同门后投递；③ 三次都不行才 `translate_hold:<reason>`，且 `conv_state` 出一行人话「翻译引擎没回话 · 这条没发 · 重试翻译」+ 动作按钮打 `POST /api/unified-inbox/drafts/{id}/retranslate`（新，只对 hold 稿）。不改「不发原文」纪律；`lang_unknown` 不重试 | `outbound_translate.py`（`_mark_hold` 前加重试 / 重起草）、`translation_service.py`（暴露 `retry_once`）、`autosend_worker.py` L114 文案、`conv_state.py` 一态、`unified_inbox_send_routes.py` 一端点 |
| **C 要图意图闸** | `image_autosend` 进 `album_match` 前先算 `intent = detect_selfie_request ∨ requested_scene_kind(严格) ∨ 承诺兑现`；`intent` 为假 → **不跑匹配、不写 miss、不刷红条**，只留 `[album_match] skip=no_intent` 一行 DEBUG；`scene_class_of` 泛匹配不再单独把 `_ask` 置真（须与要图词共现）。同一入站 mid 只跑一次（`_album_match_seen` LRU 按 `conv:mid`）。`album_miss_marker` 计数只在 intent 真时增 | `image_autosend.py` L1050–1145、`album_miss_marker.py` |
| **D 全部账号筹码** | 找到列表顶那一排的渲染函数（同读 `_tagStatsCache`，非 `_renderPanelTags`）：① 系统族走 `_isSysTag` → **单人端（`seat_mode=single`）默认不渲染**；multi 或用户在筛选面板显式打开「系统标签」才出；② 出时每枚可点 = 应用该筛选（复用 `data-ptag` 路径），带 × 清筛选；③ 文案走 `_sysTagLabel` 人话（长期未回 / 需留意 / 别再联系），不出 `dormant:ignored` 原码；④ 处理后消：`dormant:ignored` 遇真入站已由 Q-31 摘，`risk:*` 摘标 / 释放 hold 时刷新统计（`_tagStatsCache` 重拉）。零新颜色，chip 用既有 `fp-chip` / `tk-*` | `unified_inbox.html`（列表顶渲染 + `_isSysTag` 复用）、`inbox_workspace.py` 词条如缺 |
| **E 门禁 + 回放** | `tests/test_send_truth_q39.py`：A ① 入站图 + 无索图 → 零出图、LLM 承诺被撤回改写；② 明确「send me a pic」→ 照常出图；③ 冷却 / 日上限；④ caption 去重。B ① `ai:empty` → 重试 → 成功投递；② 三次失败 → hold + conv_state 人话 + retranslate 端点；③ `lang_unknown` 不重试。C ① 「enjoy the view」→ `skip=no_intent` 零 miss；② 「May I see a pic」→ 照常 miss / 红条；③ 同 mid 不跑两次。D node 抽源：列表顶渲染调用 `_isSysTag`、single 下不出系统族、chip 带 `data-ptag`。Q-30 / Q-31 / Q-35 / Q-36 既有门禁零改动全绿 | tests |

## 红线

- 不发原文纪律不动：翻译三次失败仍 HOLD，不许回落发中文。
- 不改 Q-6 匹配打分、不改 `pick_media`、不改 `note_album_miss` / `album_miss_addendum` AI 改口行为；只在它们前面加闸。
- 不改 `detect_offer_media`（Q-36）语义；「客户提议发图」仍不当索图拒。
- 单人端不出任何系统筹码；多坐席行为不变。不进 Q-32 美化：不改字号 / 间距 / 颜色 / emoji。
- `.py` 须 zhiliao 重启才生效；本线**不许自己重启**（发版线统一装载）。模板 / 词条热更。
- 不碰工作树其他线的脏文件；每次 `git add` 显式路径，提交前 `git diff --cached --name-only` 必须只含本线文件。
- 台账：本线只把 #327 #326 #323 #324 状态改 confirmed 并写 fix_note 草稿，**不标 fixed**——fixed 由发版线在坐席包 + 117 重启都装载后打。

## 开新对话粘贴

```
读 docs/指令_Q-39_发送真相四闸_跟图循环与翻译空串与要图意图与全部账号筹码_2026-09-15.md，按它做 Q-39（#327 #326 #323 #324 + #316 追加，skuio 1.0.85.0，报告 9PYWPG / THBSHN / XG3UZU / YS54YK，证据在 D:\boundless\tmp_diag\<码>\）。先 0 段：把工作树里已写好的 #316 相册 2MB 豁免（engines/chengjie/src/web/admin.py is_persona_album_upload_path + tests/test_album_body_limit_q35.py + docs/发版对账_v1.0.85_Q35.md）单独 git add 显式三路径提交，其余脏文件一字不碰。然后 A #327：先回放 9PYWPG 会话的 [album_match] / assume_intent / _bind_reason 日志定真闸，再做「入站是图且无索图意图 → 不出相册、承诺走撤回改写不走兑现」+ 同会话图片跟随冷却 30min / 日上限 6 + caption 近重复换配文，每次出图落 [album_send] trigger= intent= candidates= picked= caption_src=。B #326：outbound_translate 收到 ai:empty / timeout / provider_unavailable 先同引擎重试 1 次再换引擎 1 次，仍空且 auto_ai 按 lang-plan 目标语重起草一次，三次不行才 translate_hold 并在 conv_state 出人话「翻译引擎没回话 · 这条没发 · 重试翻译」+ POST /api/unified-inbox/drafts/{id}/retranslate；不发原文纪律不动，lang_unknown 不重试。C #323：image_autosend 进 album_match 前先算 intent（detect_selfie_request ∨ 严格 requested_scene_kind ∨ 承诺兑现），假则 skip=no_intent 不匹配不写 miss 不刷红条，scene_class_of 泛匹配不再单独置真，同 mid 只跑一次。D #324：全部账号视图列表顶那排筹码（同读 _tagStatsCache、不是 _renderPanelTags）走 _isSysTag，seat_mode=single 默认不渲染，出时可点筛选带 ×、文案走 _sysTagLabel 人话、摘标后重拉统计，零新颜色。E tests/test_send_truth_q39.py 覆盖 A–D + Q-30/31/35/36 既有门禁零改动。预算 $160。开工先 scripts\agent_probe.ps1 -Intent 登记；不改 Q-6 匹配打分 / pick_media / note_album_miss / detect_offer_media；不自己重启 zhiliao；每个 commit 带单号并立刻补落点表 docs/发版对账_v1.0.86_Q39.md 一行；git add 显式路径且提交前 git diff --cached --name-only 只含本线文件；四单只改 confirmed + fix_note 草稿不标 fixed。收工只改 docs/Q批_进度总账.md 的 Q-39 行并 agent_probe -Done。
```
