# 实施 50：充值唯一化定价 + 新人 6U 互动海报（2026-08-21，P0 已上线）

老板拍板：官网价格去掉订阅方案（第一版），只留按充值计费（第二版），新增 5000U / 10000U /
企业合作档 + 企业级部署方案；新用户打开软件弹 6U 大礼包海报；海报要互动、官网与桌面端双面。
本批 = P0 全部落地并已部署生产（bd2026.cc 2026-08-21 06:39 部署，公网体检 + 线上标记全绿）。

## 一、定价体系变更（单源链已同批改）

| 档位 | 首充加赠 | 首充到账 | 有效期 | 备注 |
|---|---|---|---|---|
| 免费开始 | — | 1,000/月 + 注册送 10,000 | — | 非 SKU 档位，价格页顶部横条 |
| 6U 新人包 | 2 倍率 | 18,000 | 6 个月 | 注册 72h 内 · 每账号一次 · 不占首充资格 |
| 50U | 0% | 75,000 | 12 个月 | |
| 100U | +5% | 157,500 | 12 个月 | |
| 200U 🔥 | +10% | 330,000 | 12 个月 | 个人主推 |
| 500U | +20% | 900,000 | 24 个月 | |
| 1000U | +30% | 1,950,000 | 24 个月 | |
| **5000U 新** | **+35%** | **10,125,000** | 24 个月 | 专属客户经理 / 发票合同 / 优先支持 |
| **10000U 新** | **+40%** | **21,000,000** | 24 个月 | 上述全含 + 分账报表 + API 对接 |
| 企业合作（新） | 面议 | 年框协议价（建议 ≥2 万 U 起谈） | 合同期 | lead-based，`chatx-enterprise` 台账 |
| 企业级部署（新） | 面议 | 一次性实施 + 年授权维保 | — | 私有化/本地模型/数据不出网，`chatx-private-deploy`，承接旧旗舰私有化叙事 |

- **订阅三档（基础 39 / 专业 99 / 旗舰 598）2026-08-21 停售**：存量按期履约到期转充值；
  `product.yaml` + 注册表 + `lib/pricing.ts::legacyAutochatOffers` + `chatx-pricing.ts::LEGACY_CHATX_PLANS`
  四处 legacy 台账保历史订单反查与门禁形状断言，**勿删**。
- 老下单深链全部平移充值档（`order-lines.ts::LEGACY_PLAN_MAP`，单跳拍平）；
  云端托管交付选项随订阅一并下线（OrderPanel delivery 整链移除），托管需求由企业合作承接。

## 二、改动面清单

**事实源/注册表**：`products/zhiliao/product.yaml`（+4 SKU、订阅转停售）→ `tools/build_sku_registry.py`
重建（41→45 SKU）→ `npm run sync:facts`。桌面打包的 platform-staged 副本由 `build_backend.py` 下次构建自动重 stage。

**官网**（全部过 tsc + 三门禁 + next build 76/76 + 线上验证）：
- 单源 `lib/chatx-pricing.ts` 重写：RECHARGE_TIERS +2 档（perks 字段带权益）、ENTERPRISE_TRACKS、
  CHATX_FREE、计算器改「推荐充值档+续航月数」（`recommendRechargeTier`）；订阅相关导出移除。
- `/pricing` 整页重构：免费开始横条 → 6U 互动海报（3D 倾斜+光泽跟随，reduced-motion 降级）→
  八档充值区（加赠阶梯到 +40%、大额档权益行、企业锚点）→ 企业双卡 → 费率表 → 计算器 → FAQ 全改写。
- 首页 `Plans.tsx`/`content.ts`：月/年切换下线，五卡（免费开始/6U/200U/1000U/企业）。
- `OrderPanel`：chatx 订阅 Tab 移除，tokens Tab 改名「智聊 ChatX · 充值」挂帅；ROI 计算器改按费率折算。
- 全站残留订阅话术清零：`bot-knowledge.ts`（⚠ 曾按下标映射订阅 SKU，新卡片下会报错价，已改为
  充值 SKU 清单 + 卡片自带价文）、`chatxContent.ts`、`compare-content.ts`、order/pricing metadata、JSON-LD。
- 全站浮动海报 `NewbieFloatingPoster.tsx`（GlobalChrome 挂载）：md+ 专属、左下角、7 天频控、
  /pricing /order 不出、**刻意无倒计时**（匿名访客无注册时间，假倒计时=信任自杀）。
- 门禁强化：`assert-order-lines` mustMatch +recharge-5000/10000；`assert-offer-map` expect 同步；
  `offer-map.ts` ⟷ `ledger-lib.mjs` 双同步 +2 条。

**桌面端**（随下一次发版生效，v1.0.45+ 自动 ride）：
- 活动 feed 基建：`renderer/campaign-model.js`（纯函数：归一化/资格/频控/倒计时，37 断言单测进 npm test 链）
  + main.js（`campaigns.json` 拉取/缓存/状态落盘/IPC `campaign-poster`/`campaign-act`）+ preload 桥
  + `renderer/poster.js`（渲染+倒计时 tick 只更 span+交互回执）。
- 展示规则：仅托管版；audience=new_user 锚 `config.onboarding.completed_at`（首启向导完成才有 →
  向导天然优先；完成 reload 后即首个展示时机）；72h 真实倒计时、到点自动收（绝不留死倒计时）；
  频控 3 次/12h + 「不再提醒」永久静默；CTA 只放行 https 外链开浏览器下单。
- 埋点走既有 uiEvent 冷启动排队通道：`poster6u_view/click/dismiss/never`
  （读数 `/api/admin/ui-event-trend?prefix=poster6u_`）。
- feed 单一事实源：官网 `public/downloads/campaigns.json`（与 announcements 同域同模式）——
  **市场改 JSON 即上/下活动，无需发版**；未知版式/非 https CTA 整条丢弃（老客户端优雅降级）。

## 三、已知缺口（非本批引入，接线前 6U 订单人工履约）

- **引擎侧充值履约未接线**（前一晚 duty line 已点名）：token_ledger 无 recharge/首充语义
  （每人一次台账、赠送桶、12/24/6 月有效期、退款回收）。当前 recharge-* 付款后需人工发凭证。
- 桌面海报要随下一个安装包发出（feed 已就绪，老版本无海报是刻意的优雅降级）。

## 四、P1（2026-08-21 同日已落地并生产验证）

**充值履约自动化（缺口关闭）**——履约在厂商机计划任务（`ChatxFulfillWatch` 5min /
`ChatXTrialFulfill` 5min）每轮新进程直跑仓库脚本，**零实例重启即生效**：
- `chatx_fulfillment.py`：`RECHARGE_SKU_SPECS`（七档+首充加赠表，与官网
  `chatx-pricing.ts` 跨仓门禁钉死）+ `is_first_recharge`（**无状态**判定：官网订单
  历史 + contact_core/指纹双锚 + (时间,订单号) 确定性并列裁决——两笔同时到账只有
  一笔拿加赠，重放安全）+ `newbie_eligibility`（72h 窗锚=最早 trial claim，防重装
  刷窗；每账号一次同款裁决；claims 拉取失败按无 claim 放行=6U 客单价下的正确取舍）
  + `recharge_voucher_decision` / `plan_recharge_fulfillment`（issue/manual 双队列，
  manual 带具体原因）。**凭证载荷=chars（1 Token=100 字符并账口径）**：tokens 进的
  是没接消费点的钱包=客户付钱拿到花不动的余额；chars 立刻可用，钱包上线 1:1 并账，
  刻意不双发防将来双份到账。
- `fulfill_chatx_watch.py`：充值队列（拉 activated 历史 + trial-claims **peek=1**
  ——绝不污染试用履约端的心跳监控）。
- 门禁 `tests/test_recharge_fulfillment.py`（30 例，含跨仓价格交叉钉）；
  **生产实弹**：e2e 新人包单「下单→标 paid→自动签发 1,800,000 字符凭证→回填
  activated→payload 校验」全通；重复购买单被转人工 `newbie_already_claimed`。
- 已知边界（如实）：充值 12/24 个月有效期在字符池层无强制（客户只多不少）；
  `token_ledger.grant_bonus` 永不过期与「先赠后实付」口径相反——**钱包接线前必须
  改成 6 个月过期**；返利/首充判定的支付指纹（USDT 地址）未存储，现用
  contact+设备指纹双锚。

**新人包下单预检闸（官网，已部署）**：`lib/newbie-gate.ts`（fail-open，绝不挡结账）
→ `/api/order` 收钱前拦明确不合格单 → CheckoutModal 双语人话拒单文案（已领过/超窗，
导流 50U 档）。线上验证：重复新人包在下单时即拒 `newbie_already_claimed`。

**拉新升级（官网+引擎双端）**：
- 里程碑阶梯：邀满 3/5/10 个达标好友 → 额外 +20万/+50万/+100万字符（每档一次）；
  总上限 20→50。
- 首充返利：被邀请人首笔已付充值（含新人包）→ 邀请人得金额 **10%** 等值字符，
  **7 天观察窗**后发（订单状态机无 refund，延迟是唯一拒付防线）、单笔封顶 100U 等值；
  剔 e2e 测试单。
- 协议＝通用 bonus 通道：官网 `listBonusDue`（金额计算）→ `?due=1` 响应 `bonus_due`
  → 厂商机 `fulfill_trial.py` 第⑤腿签发（**REFERRAL_BONUS_MAX_CHARS=15,000,000
  单笔护栏——私钥不当官网的橡皮章**）→ POST `{bonus:[…]}` 先挂凭证后标记。
  未来任何新奖励类型走同一通道零协议改动。
- 会员中心邀请卡（模板热更新已生效）：里程碑阶梯条（含已入账态/下一档提示）+
  返利规则行 + **分享海报生成器**（canvas 1080×1350 PNG：大号邀请码+三步+链接；
  刻意无二维码——引码库成本>收益，机制核心是首启向导填码，P2 视传播数据再议）。
  i18n pack +16 键 zh/en 对齐。

**6U 漏斗看板（官网，已部署）**：`/console/funnel` 新增「🎁 新人 6U 海报」7/30 天
双窗卡——曝光/点击=官网口径（桌面弹窗曝光在引擎 `ui-event-trend?prefix=poster6u_`，
两套刻意不混算）、下单/已付=全渠道订单台账（剔 e2e）。

## 五、P2 进展（2026-08-21 上午，两线撞车合流实录）

本线 07:55 声明 P2 时，另一条线已于 07:25 声明同主题（读了本文档第五节清单）并在
07:32–07:47 完成大部分——按共享树撞车协议本线让行合流，只补不重叠缺口。

**兄弟线交付（引擎侧已随 ChatxFulfillWatch 计划任务生效；官网侧改动待其部署）**：
- **VIP 累充等级**：`chatx_fulfillment.VIP_REPEAT_BONUS_TIERS = [(500,3),(2000,5),(10000,8)]`
  ——复充按客户历史累计已付充值（严格早于本单）定档加赠，「复充回归基准价」自此
  升级为「复充按 VIP 等级加赠」；首充档不与 VIP 叠加。官网 `VIP_REPEAT_BONUS_TIERS`
  + 定价页文案 + 跨仓门禁钉住。
- `token_ledger.grant_bonus` 默认 **6 个月过期**（`BONUS_VALID_MONTHS`，到期序天然实现
  「先赠后实扣」）+ **独立 `grant_migration` 批次**（并账=客户已付真金白银，永不过期；
  kind 单列，审计一眼分清「并账」与「奖励」——比「bonus 加参数」更干净的方案）。
- 订单 **`refunded` 状态全链**：状态机 + 管理卡「↩️ 标记已退款」按钮 + TG webhook +
  守护对已履约退款单点名「凭证可能已兑换」转人工 + 首充/新人/VIP/返利资格判定
  天然排除（判定只认 paid/activated）。
- 同期另一条 quotawall v2 线交付：工作台额度墙四态 + 充值深链 + 凭证兑换 + 自动恢复
  （吃 12:30 重启窗装载）——消费面直接接上了本主线的充值下单链。

**本线补的缺口（凭证有效期协议；门禁 tests/test_voucher_validity_months.py 8 例）**：
- `issue_topup_voucher(valid_months=)` → 凭证 **`months` 字段**（Token 载荷专属，进
  Ed25519 签名契约；验签拒垃圾值，仅收 (0,120]）→ `redeem_topup_voucher` 透传 →
  `grant_pack(valid_months=)` 批次有效期。「充值 ≥500U 实付 24 个月有效」的官网承诺
  自此由**签发侧**写死进凭证、兑换端只执行；旧凭证缺省 12 个月，向后兼容零迁移。

## 六、P3 进展（2026-08-21 上午，影子期读数面 + 并账工具）

勘察再往下挖一层，「shadow 扣账接线」**整段不用做**：五个计费动作的观测计量
2026-08-19 已由灰度线全部接好（ai_reply 在 `ai_client.generate_reply` 出稿处并带
`_reply_free_path` 免费路径豁免；voice_clone 只计克隆引擎、预渲染/edge 免费；
pro/deepl_translate 按请求档；ai_image 只计现场生成——接线时序有 `test_token_p6`
源码钉），且 zhiliao overlay `token_ledger.enabled: true` 已生效运行，**账本里已有
两天真实扣账**。本线 P3 于是收敛为「读数面 + 切换日工具」两件：

- **影子读数 CLI** `tools/wallet_shadow_report.py`（只读、多实例根、`mode=ro`）：
  钱包面（余额=`allocate_spend` 同源核算 + 日燃/7天均燃/**跑道天数**/enforce 若开
  是否立即降级 + 折美元）、校准面（字符池 vs Token 账本同窗对照＝**付费路径占比**，
  免费路径承诺是否兑现的直接读数）、公平使用水表。`--json` 供周批管道。
- **并账 CLI** `scripts/wallet_migrate.py`（切换日一次性动作，**默认 dry-run 且真只读**
  ——账本不存在时不建库；`--apply` 强制显式单根）：剩余字符=included+topup−used，
  100 字符/Token **向上取整**，入 `grant_migration`（永不过期 + ref 幂等绝不双倍），
  钱包键=`wallet_id_for_status` 与消费点同源。体验档（local_trial）刻意不在范围：
  由试用凭证升级链自然换发。
- 门禁 `tests/test_wallet_tools.py` 11 例；Token 家族 66 例全绿。

**首份生产读数（2026-08-19/20 两天，zhiliao）**：钱包余额 7950/授予 10000，
`ai_reply` 日燃 850→1200 Token（≈120 条 AI 回复/天，$0.08/天），7 天均燃 293、
跑道 ≈27 天；voice_clone/pro_translate 零＝当窗真实零流量（app.log 交叉核实，
非接线缺口）；免费翻译水表 30→149 字符/日。**定价健全性对读**：此用量下新人 6U 包
（12k）够 ~40 天、50U 包（55万）够 ~5 年——单席位轻用量场景印证「充值制天然
匹配低频用户」，重席位多账号数据待坐席机实例接入后补。

**P3 剩余（按读数节奏走，无代码欠账）**：
1. 影子期攒 1-2 周读数 → 读数干净后：切换日跑 `wallet_migrate --apply` 并账 →
   overlay 开 `token_ledger.enforce: true` 灰度（降级轨/额度墙/凭证兑换均已在位）。
2. ASR（5 Token/分钟）是唯一未接观测的费率项——刻意不在影子期中途加点（污染两天
   基线），首个周读数后与灰度线合批补。
3. 桌面海报随 v1.0.45 发出后读 `poster6u_` 漏斗 → 第二波活动改 campaigns.json 零发版。

## 七、P4 进展（2026-08-21 上午，/enterprise 销售件上线 + 钱包周批）

**/enterprise 独立页（zh+en，已部署 bd2026.cc 并公网验证）**：
- 定位＝销售件 + 线索捕获：/pricing 企业双卡只是路标，资质叙事（两形态深卡带
  「适合谁/你会得到」）、形态对比表（部署位置/数据落点/模型 Token/结算/交付周期）、
  实施四步（勘察→PoC→部署→陪跑）、安全合规四点（链去 /compliance）、企业 FAQ 5 条、
  **站内留资表单**全在这里——企业买家不一定用 Telegram，外跳 TG 之外必须有
  零门槛留资通道（TG 直连降为次级链接）。
- 表单打**既有** `POST /api/lead`（蜜罐/去重/管理员 TG 卡片/console 台账全复用，
  后端零改动）；`interest` 前缀 `enterprise:` 供台账一眼分流；字段带公司/席位规模。
- 数据单源纪律：两形态取 `chatx-pricing.ts::ENTERPRISE_TRACKS`（与 /pricing 同份）；
  FAQ 在 `lib/enterpriseContent.ts` 供页面与 FAQPage JSON-LD 共源（growthContent 姿势）；
  另出 Service JSON-LD（面议刻意不带价格）。SEO：`lib/seo.ts` 注册 → sitemap/hreflang/
  IndexNow 自动覆盖（部署时已推 42 URL）。埋点 `enterprise_view/cta/tg_cta/lead_submit`。
- 接线：/pricing 企业卡主 CTA 改内链 `/enterprise`；页脚加「企业服务」。
- 验证：tsc + 三内容门禁 + `next build`（78 页）全绿；部署后线上 E2E——测试线索
  POST 200 → 落库 → 管理员 TG 卡片触发（卡片带处理按钮，标记 lost 即清）。

**钱包影子周批**：`wallet_shadow_report --out-jsonl`（紧凑趋势行：余额/窗口支出/
7天均燃/跑道/would_degrade + 校准面）+ `scripts/wallet_report_weekly.ps1`
（UTF-8 日志/轮转，注册命令在脚本头，**未自动建**属运营决策）。首条趋势行已落
`logs/eval/wallet_trend.jsonl`（burn7=304/天，跑道 25.9 天）。门禁扩到 12 例。

**P4 剩余**：邀请超限人审通道（REFERRAL_TOTAL_CAP=50 触顶后的提额审批）——推广者
体量尚零，**数据闸门未到刻意不建**；分享海报 QR 同理。
