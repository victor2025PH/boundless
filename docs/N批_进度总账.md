# N 批进度总账（跨对话 / 跨账号交接的唯一入口）

> 任何新对话、新账号、崩掉后重开，**第一件事就是读这里**。不依赖聊天记录。
> **谁改这里：** 每条指令的执行方在**收工时（或额度将尽时）**更新自己那一行，**只改自己那一行**。**每个 commit 落地后立刻补落点表一行**（M-7 教训）。
> 建立于 2026-09-07 23:0x。公共底座 `docs/修复指令_N批_并行_2026-09-07.md`（去向表 / 文件归属 / 决策 D-N1–D-N5 / 接手 M-7 话术）。

## 一句话现状

1.0.76 首日复验：skuio 25 份报告 → #237–#249（13 张 = 6 件事）；钧 #250（= #215③ 第二次，#251 重复）。两件 P1 是 1.0.76 自己带出来的：人设备份恢复首次真用即 `_esc is not defined`（他卸载重装后人设池 0、只能手工重建），主动关怀「立即发」85 分钟不发。M-7 A/B/C 已提交但 D 未做、落点表空、对话中断。目标版本 1.0.77。
**N-1 / N-2 先开，N-3 / N-4 / 接手 M-7 并行，N-5 收口。**

## 进度表

| 指令 | 主题 | 工单 | 预算 | 状态 | 已完成 | 剩余 | 落点表 | 已花 |
|---|---|---|---|---|---|---|---|---|
| **N-1** | 主动关怀「立即发」真投递 | #243（并 #244–#249） | $120 | **已收工**（09-08 02:0x） | F 答完（循环活着、无事不写日志；16:47 零出队真根因＝deferred 队列启动期关闸从未 start、懒建只建不起）/ A `e3a58eda` 同步直投六态回话 + 按钮防重复 / B `24240f04` bring_forward 兜底 due_at=now+fwd 备注+立刻补拍 / C `14087007` 原文行零错峰 + 卡片预计出手 / D `193a09f7` 队列随启动常备 + 每拍一行 + [care-deferred] 出队日志 + 灯 broken / E `87875dd3` 卡片状态机 + 排队中再点投那一行；39 例新测 + care 族 601 绿（4 红非本线） | 无（Messenger 无同步路径按排队中回话；300s 粒度改精确调度不做，理由见落点表 §四） | `发版对账_v1.0.77_N1.md` §F–§四齐 | ≈$70 |
| **N-2** | 人设工作室 1.0.76 回归三件 | #237 #239 #238 | $150 | **收工**（09-08 02:xx） | A `9c72cdd2`（#237：`_esc` 是 `_exportFilteredCSV` 体内 CSV 转义函数、全局够不着 → 24 处改 `_escHtml`（含 L-2 C JSON 导入预览同病）+ 失败人话落 renderer.log + 回执「已恢复 N 个人设…含相册引用 M 条」+ dry_run `name_clashes` 重名顾问 + 作用域门禁探测器）/ C `a1fb8d72`（#238：`album_ai.enabled` 默认 True + 官网网关端点放行（`_hosted_vision` 标记）+ `vision_probe` + 90s 熔断 + 上传/列表/补标回 `vision_ready/reason` + 五态引导条 + 进度 + 轮询续命 + 完整度纳入相册）/ B `2cd84c02`（#239：常驻「已登记 X · ▶ 试听 · 解绑」+ 播放器常驻 + toast 9s 可关去内部名 + 登记即生效明示 + 抽屉假脏守卫 + 体检钩子 `persona-voice-audition` 交 N-4） | 无；待 N-5：装载 4 个 `.py`、`config.example.yaml` `album_ai` 注释同步、回访口径见落点表 §二 | `发版对账_v1.0.77_N2.md` | ≈$95 |
| **N-3** | 域包错配与预置清理 | #241 #240 | $150 | **收工**（09-08 04:2x） | §2 答完（skuio 装 conversion ＝ clean 包 `config.desktop.min.yaml` 不写 domain + `effective_domain_name` 硬编码缺省；但 conversion 包在代码里**就是**陪伴包，十几处 `== "conversion"` 当陪伴判定——错配的是挂在包上的销售物料，故不换包名，新增 `business_domain` 单一真值）/ A `01837c2b`（business_domain.py 显式 > 桌面推导落盘 + 装配层按域选 CompanionDomainHook 与陪伴 KB 分类变体 + 启动一行 + 陪伴域不下发「转化成交」域级 + 出厂基线 + 开发者页下拉）/ B `4331c851`（陪伴槽位集 personal 轨 + 收入 / 资产敏感默认不勾 + 注入硬约束 + 自定义标签 + 存量不丢）/ C `e2faf064`（画像分组随域、商机区不渲染、「标成交」→「标记达成」达成结果 + 备注、摸底完成核对清单、报表去金额口径）/ D `1c93feba` + 修正 `63bac10d`（「营销目标」→「工作目标」界面 / 路由日志 / 词典；修正误夹带他人 hunk）/ E `d46b62a9`（KB 种子按域：非支付不播 19 条支付话术、陪伴默认停用、purge 打标不回灌、横幅一键清除、启动 WARNING）；新测 38 + 10 绿，相邻回归全绿（1 红 contacts 存量非本线） | 首装向导不建（基线 + 后台下拉替代）；待老板定「达成结果」文案；交 N-5：14 个 `.py` 装载、internal overlay 补 `business_domain` 一行、zh_hant 再生、回访口径（含「291 条是悬浮球语料」第三次） | `发版对账_v1.0.77_N3.md` §2 / 一~四 / 回访齐 | ≈$110 |
| **N-4** | 语音质量闸 | 钧 #250（#215③） | $120 | **已收工**（09-08 04:0x） | A 三问答完（两包全程 `speech=voiced`、零 send-voice；红条橙条从未被任何判定点亮——`.pv-note{display:flex}` 架空 `hidden`，0831 起恒常可见，#121 三进宫同根；Mizuki = 有录音 + 残留 `zh-CN-XiaoxiaoNeural`，克隆链健康；「过期」= 中文客户 identity 不译时前端把基准记成 'auto' 与 conv_lang 'zh' 比对）/ B `75848c5c` `[hidden]{display:none !important}` + 结果区唯一结论行 `panelVerdict` 四态 + `[tts] verdict=` 单行 + 人设页体检回填 / C `609763e5` `target_lang_resolved` + `xlBaseline`：只有目标语真变才过期，过期主按钮「重新生成」 / D `1a46c65f` 阈值实录校准 86 样本 0 误判不改 + `synth_gate` 合成前拦（克隆无录音等必拦；残留预置声名只提示）+ effective-config 预告 + `#profile=<id>&tab=voice` 深链 / E `42714553` `[tts]` 前缀统一 + AGENTS 关键词与时间窗；verify_cp_voice_ui 92/92、node 104、pytest 22 | 无；交 N-5：装载 5 个 `.py`、AGENTS 两份随 v1.3.1 重发、回访口径见落点表 §二 §四 | `发版对账_v1.0.77_N4.md` §〇–§四齐 | ≈$85 |
| **接手 M-7** | 目标引擎 D 项 + 落点表 + 总账 | #236 | $80 | 未接手 | A/B/C 已提交 | D 卡片说真话 + 单目标 stalled / 三次启动原因 / 落点表一~四节 / 总账 M-7 行 | `发版对账_v1.0.77_M7.md` | |
| **N-5** | 值守收口 | 并单 / #242 / zl_collect v1.3.1 / 两包定性 / 1.0.77 | $100 | **E 段发版完成（07:45）；A 并单 / 8 单 fixed / 回访 / 记账 = dry-run 备好等老板点头** | **C** 04:56：TAZ9QS 坐实 #215 ① = M-3 A 2MB 闸 a 支（13:37:38 WA 已 connected、13:39 后端在跑、全包零 `[send-media] request`）→ #215 `confirmed → fixed` + 816 字定性 note；#250 包 **TWJ7C8** 22:33 早到、台账漏记 → 补 `received` + note，`duty_evidence list` 无在途 / **B** 04:59：新 `tools/duty_send_file.py`（send-media 裸流 + 拨回 auto_ai + 台账）发 **mid 1331**（`zl_collect_jun.ps1.txt` v1.3.1）/ **1332**（`AGENTS_JUN.md` v1.3.1：+`[tts]` 关键词与时间窗头注）+ 说明文本（bot 链）明说覆盖，`add27c6d`；skuio 已是 v1.3.1 / **A** dry-run 05:03（并 7 / 关 1 / 改挂 1，`tmp/n5_a_merge_dryrun.txt`）**等老板点头 apply** / **D** `f3369b52`：90 分钟同人「同实体 → 同主题 → 引导段跟进词」挂最近单 + 关联码指向已修单时开放同题单优先 + 一轮按报告时间排序 + 挂单抬严重度；门禁 16 绿（#243 七连发 / #252→#253 / #254→#255 / MJGHKQ 四组回放）；**顺带修生产故障**：watch 水位 `sorted()[-200:]` 按字母序截，数字开头新码 3HNCJ7 / 47KNBV 被截 29 分钟、每 2 分钟重下 → 首见序截尾，05:19 tick 自愈回执 mid 1334 / **E** 05:24–07:45：preflight 四跑（N-3 五端点 + 微信 PC 线心跳端点代登记 `0e200e67` `55fe985f`）→ **05:58 zhiliao 装载**（20s，0 ERROR；N-1 队列 `drain_loop=running` 实证）→ 全量回归 **88 / 26040**（对 M-6 新红 12 → 串行 4 条稳定红全门禁 / 他线在途，0 功能回归）→ gate **8 / 796**（7 老债 + `--cp-ok-border` 代补 `105b384c`）→ bump 1.0.77 / 繁体 16762 / changelog 8 条 / ui-build 0651 / 缓存戳 20260908b → release **`aaaf983c`** → 三包 smart 591.4M / clean 520.2M（sha256 3912f951…）/ lite 523.6M 冒烟全过 → **三渠道全 1.0.77** + 官网 gate:content 6/6 部署 OK → **坐席三台 UP 1.077** → 播报群 **mid 1335** / 频道 **155**；症状族族 5 +7 层 + 新族 9；合并索引 `发版对账_v1.0.77.md`。⚠ 事故：自检误触 `purge-payment-seeds` 删 zhiliao 19 条**停用**系统预置支付话术（零对客影响、02:40 备份可恢复，落点表 §6） | **老板点头后**：A apply（`tmp/n5_a_merge.py --apply`）→ 8 单 fixed apply（`tmp/n5_mark_fixed_1077.json`）→ 回访 skuio 两条 + 钧一条（`.ops/duty_0908/`）→ 记账 → 对账表重跑 → 总账改收工；钧 selfcheck 回码待收；两机装 1.0.77 后核版本；交回：N-3 `DomainLoader` 根级 logger 让 `business_domain=` 启动行在生产不可见 / DY `douyin` 平台表 / 微信 PC 线 emoji 棘轮 / 新单 #252–#255 → O 批 | `发版对账_v1.0.77_N5.md` §0–§7 + 断点节 | ≈$80 |

## 老板拍板记录

（默认值见底座「老板决策」表；老板改口在这里追加一行：日期 / 决策号 / 改为）

## 全批收口（N-5 做）

> 注：「接手 M-7」那行「未接手」已过期——M-7 09-08 02:3x 在 `docs/M批_进度总账.md` 收工（D `eeec70f1` + 三启取证 §五），N-5 按已收工处理。

- [x] 五条落点表 + M-7 落点表齐 → 合并 `docs/发版对账_v1.0.77.md`（09-08 05:5x 建，发布事实收口时填实）
- [x] `restart_preflight` 05:56 GO → zhiliao 装载 05:58 → 全量回归 88 / 26040（0 功能回归）+ gate_sweep 8 / 796（7 老债 + 1 代补）
- [x] 1.0.77 三包 → publish 三渠道（07:30–07:36）→ 坐席三台 UP 1.077 → 播报 mid 1335 / 频道 155；两机核版本交下一班
- [ ] 台账：#236–#251 按落点表标 fixed（dry-run `tmp/n5_mark_fixed_1077_dryrun.txt` 8 单 + 并单 `tmp/n5_a_merge_dryrun.txt` 9 项——**等老板点头 apply**）；回访两份 + 钧一份（文案已过闸，`.ops/duty_0908/`）；对账表重跑
- [x] `docs/值守症状族对账单.md` 更新（族 5 +7 层「立即发」/ 新族 9「域包错配」）
