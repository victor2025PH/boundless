# 发版对账 v1.0.78 — O-2（记忆抽取基线：干净包首日即死 · 出站同抽 · 私事隔离 · 新增计数）

> 指令：`docs/指令_O-2_记忆抽取基线_2026-09-08.md`。底座决策 **D-O5**（`docs/修复指令_O批_并行_2026-09-08.md`）。总账：`docs/O批_进度总账.md` O-2 行。
> 报告 / 工单：WYNN22 → **#256**（P0，O-5 09-08 08:30 自 #201 摘出改立，reporter_version 1.0.76，证据已 received；A 段 commit `ad5c7dbd` 提交时写的 #201 即此单——后续 commit / 标 fixed 请用 #256）、584AUV（#183）。目标版本 1.0.78；**A 段止血已收工，N-5 打 1.0.77 时可带上**（见 §0）。
> 归属守住：只动 `config/config.desktop.min.yaml`、`src/utils/feature_registry.py`、`src/skills/skill_manager.py`（启动日志 + origin=manual 抽取入口两处）、记忆页模板 / 路由、tests；`config_manager.py` / `store.py` 一字未动。
> 执行：2026-09-08 08:20 起。仓库根 `D:\workspace\boundless`（`d:\boundless` 是 junction），引擎代码在 `engines/chengjie/`。

## 0. 先说结论（给 N-5 / O-5 / 老板）

| 项 | 结论 |
|---|---|
| **A 止血** | **已收工 `ad5c7dbd`**，可并入 1.0.77。`memory.extract.intents=[direct_chat, small_talk, greeting, complaint]`（与服务器同值）+ `memory.extract.enabled/use_llm=true` 三键进 **feature_registry A 类基线**（唯一能到存量 clean 升级机的载体，机制同 L-5）+ 桌面种子 `config.desktop.min.yaml` 首次带 `memory.extract` 块。skuio 两台、钧一台装上带此 commit 的包，首启 `_ensure_baseline` 把三键补进 `config.local.yaml`；用户显式写过 `intents: []` 的机器一个字不动。启动日志从无条件「情景记忆已启用」改为说真话：白名单非空 → INFO「情景记忆已启用 · 可抽取意图 N 个：…」；空 / 总闸关 → **WARNING「记忆不会新增」且不再出现「已启用」**。 |
| **B 出站同抽** | **已收工 `45b56b64`**。人工发送成功后的既有漏斗 `human_outbound_memory.on_human_outbound`（#177）多调一次新方法 `SkillManager.schedule_manual_outbound_extract`：把**客户这一轮对人工说的话**（上一条出站之后的连续入站）作 `user_msg`、坐席这条作 `reply`，走与 AI 回复后完全同一条抽取链、同一个记忆桶；`user=` 仍是客户号；日志 `[episodic] schedule run user=… source=manual_out`。两道去重（人审后手发不重抽；坐席连发只抽一次）。见 §2。 |
| **C 私事隔离** | **已收工 `6b42dc10`**。改前核实：所有 `add_fact` 写口都按客户键落库，会话→共享 KB 的**唯一**通道是 DailyLearner 审核通过——L-4 F 已在那里按 `private_kinds` 分流，但词表只有 J-10 六类 + 承诺 + 自报姓名，指令验收的「约会地点」「对方生日」两例**都不在词表里**，会被判成可进共享 KB。现新增单一来源 `src/utils/memory_scope.py`（生日 / 带日号日期 / 第一二人称属性为强锚点；星期 · 相对日 · 钟点 · 场所为弱信号必须合取——业务问题「你们周六营业吗」仍进共享 KB），DailyLearner 与抽取链去向日志同一口径：`[episodic] scope=customer reason=…` / `scope=shared reason=no_personal_anchor`。见 §3。 |
| **D 新增计数** | **已收工 `63daf5e3`**。记忆页「AI 记忆健康」卡顶部一行「自安装以来新增 N 条 · 今日 +T · 已转正 P · 记忆库建于 D · 已收客户消息 I 条」；**入站 ≥50 且 N=0 → 红字** `role=alert`「记忆没有在增长——已收到 I 条客户消息，记忆仍是 0 条。检查抽取白名单（memory.extract.intents）与模型是否可用」+ 链到 `/logs` 看 `[episodic]` 行。N 数的是**历史写过的每一行**（含后来删除 / 过时）——回答「抽取链有没有产出」，WYNN22 现场该读到 0；A 段修好后从 0 开始涨、红字自然消失。旧后端无 `growth` 字段两行隐藏。见 §4。 |
| 回访口径（交 O-5） | 给 skuio：装上带 A 段的包并重启后，`backend.log` 应有 `产品基线已补齐进 overlay: memory.extract.enabled, memory.extract.intents, memory.extract.use_llm`（仅首次）+ `[episodic] 情景记忆已启用 · 可抽取意图 4 个：complaint, direct_chat, greeting, small_talk`；此后客户一条 direct_chat 入站 → `[episodic] schedule run user=…` 后**不再**出现 `skip: intent=… not extractable`；AI 记忆页开始有新增（D 段做完后顶部直接显示「自安装以来新增 N 条」）。 |

## 〇. 为什么第三次「出厂默认没进基线」（给 N-5 / 未来发版的核对步骤）

**三次同根**：① 07-31 工作目标卡（演示机 overlay 开、包里关）→ 立 `feature_registry` A 类 + `_ensure_baseline`；② 09-06 L-5 D1/D7 三键（只写在 `cloud_light.yaml` 与内测种子，clean 升级机两条路都不经过）→ 补进 A 类；③ 本次 `memory.extract.intents`。机制早就有，**漏的是「哪些键该进表」这一步没有系统化核对**：L-5 只按老板决策 D1/D7 点名的三键补（`companion.goals.sprint.enabled` / `companion.wellbeing.crisis_audit` / `companion.wellbeing.crisis_escalation`），没有回头把「服务器 `config.yaml` 有、内测种子有、min 种子没有、且代码里**缺省即关**」的键全量扫一遍。`memory.extract.intents` 正是这种键：`should_extract_intent`（Phase D）为存量零回归刻意「空即不抽」，服务器 `config.yaml` L1770 有四值、内测种子 `config.desktop.internal.yaml` 用 `match_all: true`（只进 smart 包），而 `config.desktop.min.yaml` **没有任何 `memory` 块**，`config.example.yaml` 的 `memory` 块也**没有 `extract` 子块**——clean 包 / 照抄 example 的自建实例，记忆抽取从安装第一天起就是死的，卡片和日志还写「已启用」。

**「基线键清单核对」步骤（每次发版打包前，N-5 E 段或发版人跑一遍，5 分钟）**：

1. 在 `engines/chengjie/` 下跑一行：把 `config.desktop.internal.yaml`（内测种子）与本机 `config/config.yaml`（服务器真值，untracked）的叶子键逐个对 `config.desktop.min.yaml` 合并 `feature_registry.baseline_patch` 后的客户视图做 `dig(...) is None` 差集（本次实测差集 494 键，绝大多数是 LAN 地址 / 人设 / 音色等**本就不该随包**的内测专属）。
2. 只看其中**行为闸键**——判据：代码读它时「缺省 / 空 = 功能关」（`cfg.get(k)` 空即 return、`get("enabled", False)`、白名单空即不放行）。对每个这样的键回答一句：「客户机没有这个键，哪个功能会**静默**死掉？」答得出来的 → 三选一：进 A 类（纯软件零依赖）/ 登 `test_seed_switch_upgrade_coverage._PENDING` 待拍板 / 写明为什么客户机就该关。
3. 本次扫出的**同类候选（未拍板，本批不动，留老板）**：
   - `memory.consolidation.enabled`：代码 `ccfg.get("enabled", False)` **缺省关**——巩固 / 晋升 stable / J-10 A2 `auto_promote`（7 天无冲突且被召回 ≥1 → 自动转正）全在这个闸后面。服务器 `config.yaml` 的 `memory` 块与 min 种子都没有它，内测种子 `true`。这是「零 promoted」的另一半（抽取修好后事实会 `raw` 入库，但客户机上永远不会晋升 stable / 走自动转正）。建议下批与 W7ZTSB「记忆确认机制」一起拍。
   - `memory.vector.enabled`：已是 B 类（依赖 embedding 端点），托管版走官网网关时 `hosted_gateway` 运行时回放嵌入——不是缺口，列出只为说明它被有意排除。
4. 门禁现状：`test_desktop_seed_visibility`（A 类必须在种子按声明值落地）+ `test_seed_switch_upgrade_coverage`（种子里为 true 的开关必须有升级归宿）+ `test_packaged_capability_ledger`（客户视图不为 on 的能力必须登记处置）三张网都**只守「表里已有的键」**，守不住「该进表却没进表」。第 2 步是人工步骤，要机器化需要给每个行为闸键在代码里标注（例如统一走一个 `gate_default_off(key)` helper 再由测试反查）——工程量归下批评估，本批不做。

## 1. A —— `memory.extract.intents` 进产品基线 + 启动日志说真话（D-O5）

### 1.1 改前核实的机制链（与 L-5 §1.1 同一条）

- `ConfigManager.load()`（`src/utils/config_manager.py` L400–431）：读 `config.yaml` → `_merge_overlay`（`config.local.yaml`）→ **`_ensure_baseline`**（桌面态 `AITR_DESKTOP_MODE`；`feature_registry.baseline_patch` 算「合并视图缺键」→ `save_overlay_patch` 落 overlay + 即时合进内存）→ `_ensure_deploy_profile`（`cloud_light` 只在全新播种时）→ 迁移们。
- `SkillManager.__init__`（`skill_manager.py` L492–505）：`self._memory_cfg = config.config["memory"]` → `EpisodicMemoryStore` 起库 → **原来无条件 `info("情景记忆已启用: %s")`**。
- 抽取链：`_schedule_episodic_memory_extract`（L5436，只查 `memory.enabled` / `extract.enabled`，打 `[episodic] schedule run`）→ `_episodic_memory_extract_async`（L5549）→ 生日 / 命理 / 居住地三条「收到即解析」不吃 intent 门 → **`should_extract_intent(intent, ex)`**（L192：`match_all` 优先，否则 `intents` 白名单，**空即 False**）→ skip 日志 L5584。
- 所以 WYNN22 现场：库开着（「已启用」）、生日 / 居住地等启发式照抽、但 LLM 抽取与启发式正则事实全部被 intent 门挡掉 → 8 小时 30 次 skip、零抽取。

### 1.2 改动（commit `ad5c7dbd`，`#201`）

| 文件 | 改动 |
|---|---|
| `src/utils/feature_registry.py` | A 类 +3（`show=False`，位于 L-5 三键之后、B 类之前，独立 hunk）：`memory.extract.enabled=True` / `memory.extract.use_llm=True` / **`memory.extract.intents=["direct_chat","small_talk","greeting","complaint"]`**（首个列表型 baseline）。`baseline_patch` 对列表型 baseline **逐次 `list()` 拷贝**——patch 会被深合并进运行时 config 并落 overlay，共享同一个 list 对象＝谁改运行时谁就改了注册表。 |
| `config/config.desktop.min.yaml` | 在 `companion` 段后新增 `memory.extract` 块（`enabled: true` / `use_llm: true` / `intents: [direct_chat, small_talk, greeting, complaint]`）+ 注释块写明三次同根、两条旧路为何到不了 clean 机、显式空的语义。列表顺序与注册表一致（种子门禁按声明值逐字核）。 |
| `src/skills/skill_manager.py` | ① `import logging`；② 新增纯函数 **`episodic_startup_banner(memory_cfg, db_path) -> (level, msg)`**（紧随 `should_extract_intent`）：`extract.enabled=False` → WARNING「情景记忆库已就绪但抽取总闸已关…记忆不会新增」；`match_all` → INFO「已启用 · 可抽取意图：全部」；白名单非空 → INFO「已启用 · 可抽取意图 N 个：a, b, c」（排序去重剔空）；白名单空 → **WARNING「[episodic] 抽取白名单为空（memory.extract.intents=[] · match_all=False），记忆不会新增——每条入站都会 skip」**，四态都带 `[episodic]` 前缀；③ L505 原 `info("情景记忆已启用")` 改为 `self.logger.log(*episodic_startup_banner(...))`。**`_schedule_episodic_memory_extract` / `should_extract_intent` / skip 日志一字未动**（存量语义零变化）。 |
| `docs/功能矩阵_ChatX.md`（`engines/chengjie/docs/`，生成物） | `python -m scripts.feature_matrix --out` 重导出：尾注「另有 25 个内部/策略开关」→ 28（`test_feature_matrix::test_doc_in_sync_with_registry` 钉住；L-5 同做法） |
| `tests/test_feature_registry.py` | `test_baseline_patch_only_fills_missing` 期望键集 +3；断言 intents 声明值；**改 patch 里的列表不得污染注册表**；「全部已表态 → 零 patch」样例加 `memory.extract: {enabled: true, use_llm: false, intents: []}`（显式空与显式 false 同一条三态语义） |
| `tests/test_memory_extract_baseline_o2.py`（新，19 例） | 注册表三键 A 类 / 声明值 = 服务器值 / 列表副本；`baseline_patch`：缺 memory 块补、有 memory 无 extract 补、**显式 `intents: []` 不补且 `should_extract_intent` 仍 False**、服务器已有值零 patch、内测种子 `match_all` 合并后行为不变；**真 `ConfigManager`**（`AITR_CONFIG_PATH` 指 tmp）：干净包模拟（删 config 起 → 首启播种即有白名单、四意图可抽 `order_query` 不抽、banner INFO「4 个」）/ 升级安装无 memory 块（skuio 形态）→ 补进 overlay、主 config 一字不动、幂等 / 显式空 survive 启动且 banner WARNING 不含「已启用」/ **服务器实例（无桌面态）零变化不写 overlay**；banner 四态 + 纯函数不改入参。 |

`config.example.yaml` **未动**（`memory` 块无 `extract` 子块）：它是自建服务器档，服务器实例不走 `_ensure_baseline`，加了值等于改自建默认——照抄 example 的实例启动会打上面那行 WARNING，由部署者自己决定；归 O-5 / 下批评估是否补注释。

### 1.3 验收

- `pytest tests/test_memory_extract_baseline_o2.py tests/test_feature_registry.py tests/test_desktop_seed_visibility.py tests/test_memory_extract_gate.py tests/test_config_defaults_l5.py tests/test_seed_switch_upgrade_coverage.py tests/test_packaged_capability_ledger.py tests/test_baseline_reconcile.py tests/test_internal_seed.py tests/test_feature_matrix.py tests/test_feature_center_routes.py` → **87 passed / 3 failed**，3 红全部预存且已登记：`test_seed_switch_upgrade_coverage` ×2（孤儿键 `inbox.reply_style.bubbles.explicit_newline_only`，M-2 落点表 L27 登记，#210 / D-L3 遗留）+ `test_packaged_capability_ledger::test_every_not_on_capability_is_dispositioned`（`bubbles.enabled` 降 B 未登处置，1.0.75 落点表 ⑤ 归 L-1），与 O-2 无关、形态同前。
- episodic 全家 12 文件 + `test_memory_extract_gate` → **160 passed**（`skill_manager` 改动零回归）。
- 干净包模拟（`test_fresh_clean_install_boots_with_whitelist`）：删 config 起 `ConfigManager` → 播种 min 种子 → 合并视图 `memory.extract.intents` 四值 → `should_extract_intent("direct_chat")=True` → banner INFO「可抽取意图 4 个」。现场那 30 条 `skip: intent='direct_chat'` 在此配置下全部会走到抽取。
- 指令 ③「落一行 `[episodic] 基线补齐 intents=…`」：补齐本体在 `config_manager._ensure_baseline`（**不在 O-2 归属**，未动），它已有 `INFO 产品基线已补齐进 overlay: memory.extract.enabled, memory.extract.intents, memory.extract.use_llm`（仅首次、幂等）；紧随其后 SkillManager 的 `[episodic] 情景记忆已启用 · 可抽取意图 4 个：…` 给出生效后的白名单。两行合起来即「补了什么 + 现在是什么」，field-agent 现有关键词 `产品基线` / `情景记忆` 都能捞到；不另加第三行。

## 2. B —— `origin=manual` 出站同走抽取（DY534Y）

### 2.1 改前核实

- 抽取三个触发点全在「AI 回复之后」：A 线 `process_message` L2793、B 线 `generate_inbox_draft` L3638、`persona_reply.py` L883 写回。人工接管（会话 `manual`）不拟稿 → 三处都不跑 → 客户这段时间说的话零抽取。DY534Y 说的「出站 origin=manual 从不抽」表象是出站，**根因是客户那半也没人抽**。
- #177（K-1 D）`human_outbound_memory` 是手动发送成功后的唯一漏斗（`unified_inbox_send_routes.py` L1323 `record_human_outbound(app.state, …)`），它**刻意不把坐席的话写进客户事实库**（写了就是 Phase8「AI 自述被抽成用户事实」的镜像事故）——所以不能简单把坐席文本喂给抽取器当 `user_msg`。
- B 线记忆键派生（`generate_inbox_draft` L2945–2955）：`user_id=chat_key`、`chat_id=""`、`account_id` 缺省 / `default` 时从 `conversation_id=platform:acct:chat_key` 补——manual 路径必须同口径，否则落错桶。
- B 线拟稿会在 `user_context["user_msg_id"]` 记本轮入站 `message_id`（`autodraft_helpers` L106 取 store PK）——这是「人审后手发」形态去重的现成锚点。

### 2.2 改动（commit `45b56b64`，`#201`）

| 文件 | 改动 |
|---|---|
| `src/skills/skill_manager.py` | ① `_schedule_episodic_memory_extract(..., source="")`：`source` 非空时日志尾加 ` source=manual_out`，**入站行文案逐字不变**（测试钉住）；② 新方法 `schedule_manual_outbound_extract(platform, account_id, chat_key, operator_text, conversation_id, inbox_store, user_context)`：`inbox_store.list_recent_messages(cid, limit=30)` → 尾部跳过刚落库的出站行 → 收「上一条出站之后」的连续入站 → 过 `_episodic_manual_out_mark`（本方法消费过的最新入站 ts，随 ContextStore 持久）→ 若最新入站 `message_id == user_context["user_msg_id"]`（B 线已拟稿）→ skip `drafted` → 拼接原文（>1500 字截尾保留最近）→ `_recognize_intent`（关键词分类，缺省 direct_chat）→ `_schedule_episodic_memory_extract(chat_key, user_msg, 坐席句, intent, "", platform, acct, source="manual_out")` → 落 mark。绝不抛，返回 `{ok, reason, n_inbound, intent}`。**抽取本体 / 意图门 / 白名单 / 25s LLM 冷却 / 生日·居住地启发式全部复用**——启发式正则只吃 `user_msg`（客户句），坐席「我住在曼谷」绝不会被抽成客户事实；LLM 抽取拿坐席句作语境。 |
| `src/inbox/human_outbound_memory.py`（**归属外一处**：记忆侧漏斗，无其他线登记；改动仅一个 kwarg + 一段） | `on_human_outbound(..., inbox_store=None)`：文本出站在第 3 步（自述事实）后加第 4 步——`inbox_store` 非 None 时调 `sm.schedule_manual_outbound_extract(...)`，结果挂 `res["customer_extract"]`；`record_human_outbound` 从 `app.state.inbox_store` 取 store 传入。缺 store（老调用方 / 测试）＝旧行为；语音 / 图片人工出站只记占位不触发（不拿「[语音]」当 reply 去抽）。路由 `unified_inbox_send_routes.py` **一字未动**。 |
| `tests/test_manual_outbound_extract_o2.py`（新，15 例） | 纯人工接管恰好一次调度且参数与 B 线同口径（user=客户号 / user_msg 按时间序拼客户两句 / reply=坐席句 / chat_id="" / acct / source）；坐席连发第二条不重抽；mark 之后有新入站再抽；人审后手发（`user_msg_id` 相等）跳过且落 mark；陈旧 `user_msg_id` 不挡；纯媒体入站 `no_text`；无 store / 空文本；`account_id=default` 从 cid 补；`_recognize_intent` 生效 + 1600 字截尾保留「我要退款」；store 抛异常不外抛；**日志行入站逐字不变 / manual_out 带 `source=`**；端到端 `on_human_outbound` 触发 + mark 随 ContextStore 持久（重启不重抽）；缺 store 旧行为；语音出站不触发；`record_human_outbound` 从 `app.state` 取 store。 |

### 2.3 验收

- `pytest tests/test_manual_outbound_extract_o2.py tests/test_human_outbound_memory.py tests/test_memory_promises.py tests/test_episodic_self_log.py tests/test_memory_extract_gate.py tests/test_memory_extract_baseline_o2.py` → **94 passed**（#177 既有 22 例零回归）。
- 现场验收口径（交 O-5 回访）：人工接管中客户发「周六下午三点星巴克见吧」→ 坐席手发「好的」→ `backend.log` 出现 `[episodic] schedule run user=<客户号> intent=direct_chat msg_len=… source=manual_out`（不再只有入站触发的 run）→ 该客户记忆页新增约定条目。若出现 `schedule skip: source=manual_out inbound already drafted`＝人审后手发形态，抽取已在拟稿时走过，属预期。
- 不覆盖（写明）：坐席在**手机端**直接回的消息（TG `mirror_outgoing` 镜像行，非 `origin=manual` 路由）不经此漏斗——那是「人工插话」的另一形态，DY534Y 未点名，留 O-5 评估是否立单。

## 3. C —— 私事只进客户记忆（W7ZTSB / M-5 F 收口）

### 3.1 改前核实

- 抽取链 `_episodic_memory_extract_async` 两个 `add_fact`（启发式 `user_stated` / LLM `ai_inferred`）与全仓其余 `add_fact` 调用（生日 / 居住地 / 剧情 / 学习器私事写回 / 档案卡）**全部按客户键**落 `episodic_memory`，`episodic_memory_store` 无任何 shared / global 键概念——「私事永不进共享 KB」在抽取链上本就成立，缺的是**可见性**（日志）与 **DailyLearner 的判据覆盖面**。
- 会话 → 共享 KB 唯一通道：`DailyLearner.approve_draft`（L-4 F 2026-09-06 已做 `private_kinds` 分流，私事 → `set_memory_writer` 写该客户记忆，返回 `mem:<key>`；否则 `kb.add_entry(source=learner)`）。SV62XJ③ 说「三分流无实现」是因为它的词表**漏了时间 / 地点 / 生日 / 个人属性**：`private_kinds("周六下午三点星巴克见吧") == []`、`private_kinds("我生日是3月2号") == []`（改前实测）。
- 判据必须**宁严勿松**：误把业务问题判成私事 = 本该进共享 KB 的条目被改写进某个客户的记忆（学习器负面）。所以星期 / 钟点 / 场所 / 相对日单独不成锚，只有与彼此或约见动词合取才算。

### 3.2 改动（commit `6b42dc10`，`#201`）

| 文件 | 改动 |
|---|---|
| `src/utils/memory_scope.py`（新） | `personal_anchors(text) -> [kind…]`（`ANCHOR_KINDS` 12 类：name / birthday / time / place / meet / commitment / money / address / identity / family / health / personal）、`memory_scope(text) -> ("customer", "birthday,time") | ("shared", "no_personal_anchor")`、`is_customer_private`。强锚点单独成立：J-10 `high_impact_categories` 六类 + `is_commitment` + 自报姓名（L-4 F 原 `_NAME_HINT_RE` 迁入）+ 生日词 + **带「日 / 号」的完整日期 / ISO / 英文月名+日**（「五月到十月」「几天到账」「24/7」不算）+ **第一二人称属性**（我住在 / 你今年 30 岁 / I live in；「你们」「我们」排除）。弱信号合取：`time` = 日期 ∨ (钟点 ∧ (星期 ∨ 相对日 ∨ 约见动词)) ∨ ((星期 ∨ 相对日) ∧ (钟点 ∨ 约见动词))；`place` = 场所词 ∧ (time ∨ 约见动词)。纯函数、零框架依赖。 |
| `src/utils/daily_learner.py` | `private_kinds` 判定本体改为调 `memory_scope.personal_anchors`（异常 → 空，宁进人审共享侧不崩学习器）；`PRIVATE_KINDS` +4（birthday / time / place / personal，顺序即学习页标签序）；删本地 `_NAME_HINT_RE`（已迁）。`approve_draft` 两分支各加一行去向日志：`[episodic] scope=customer reason=<kinds> draft=<id> dest=memory:<key>` / `[episodic] scope=shared reason=no_personal_anchor draft=<id> dest=kb:<entry_id>`。分流逻辑本体（L-4 F）**未动**。 |
| `src/skills/skill_manager.py` | `_episodic_memory_extract_async`：启发式循环收集事实文本（`_scope_facts`），LLM 事实写完后**每轮一行** `[episodic] scope=customer reason=anchors:<kinds>|episodic_only facts=N user=…`。scope 恒 customer 是**事实**（抽取链只写客户库）；`reason` 给锚点，无锚点写 `episodic_only`——客户偏好（「喜欢吃辣」）没有人地时锚点但仍是客户记忆，**不因无锚点被丢**（零回归）。 |
| `src/web/i18n_packs/learner_page.py` | ZH / EN +4：`lr4_private_kind_{birthday,time,place,personal}`（学习页 `_privBadge` 按 `lr4_private_kind_<kind>` 动态取键，模板零改动）。zh_hant 随 N-5 发版 `i18n_hant generate` regen（覆盖率门禁 ≥99%，4 键不触红）。 |
| `tests/test_memory_scope_o2.py`（新，34 例） | **指令三例**：约会地点 → customer(time,place) / 对方生日 → customer(birthday,time)（中英）/ 通用天气常识 → shared；11 例强锚点单独成立（住在 / I live in / 你今年 30 岁 / ISO 日期 / 人名 / 金额 / 见面 / 家人 / 承诺 / 门牌地址）；11 例弱信号单独仍 shared（周六营业 / 周一到周五 / Sunday open / 附近有公园 / 24/7 / 三点建议 / 明天几点开门 / 你们住在哪 / 喜欢吃辣 / 充值 / 退款）；4 例合取成锚；reason 顺序与空输入；`private_kinds` 三例 + L-4 F 既有五正两负不回归 + 每个 `ANCHOR_KINDS` 都在 `PRIVATE_KINDS`；每个 `PRIVATE_KINDS` 都有 zh/en 标签；**三例经真 DailyLearner 落库**：两私事 → `mem:` 写客户记忆、KB 零新增，常识 → `kb-1`，去向日志各一行；源码级钉抽取链日志口径。 |

### 3.3 验收

- `pytest tests/test_memory_scope_o2.py tests/test_learner_triage_201.py tests/test_learner_lang_script_220.py tests/test_learner_traceability.py tests/test_fusion_phase3.py tests/test_memory_extract_gate.py tests/test_manual_outbound_extract_o2.py` → **152 passed**（L-4 F / M-5 E 学习器既有 118 例零回归）；`test_i18n_zh_hant` + `test_i18n_en_payload_gate` + `test_i18n_extra_langs` → 22 passed。
- 现场口径（交 O-5）：`backend.log` 抽取后每轮一行 `[episodic] scope=customer reason=anchors:time,place facts=2 user=…`；学习页通过私事条目 → `[episodic] scope=customer … dest=memory:`，通过常识 → `scope=shared … dest=kb:`。学习页私事徽章新增「生日 / 具体时间 / 具体地点 / 个人情况」四个标签。
- 边界（写明）：`memory_scope` 是规则不是 NER——人名只认自报姓名句式（「跟 Maria 一起」不算），城市名单独不算地点（「曼谷雨季」是常识；「我住在曼谷」靠第一人称属性）。要上 NER 归下批评估。

## 4. D —— 记忆页「自安装以来新增 N 条」

### 4.1 改前核实

- 页面既有 KPI「AI 记得的事」= `admin_summary.total_count`（库内 **active 且非 stale** 的行数）——删一条就少一条，看不出「抽取链到底有没有在产出」；`stable_count` 有但页面只在副标题里带过；没有「今日」，没有「入站多少条」。WYNN22 用户的验法「AI 记忆页 13:14 后无新增」需要肉眼比对时间戳。
- 入站累计只能从 `inbox.db`（`messages.direction='in'`）拿；本路由本就按路径（`resolve_inbox_db`）做身份富化，同口径只读一次 COUNT 即可，不引 InboxStore 实例。

### 4.2 改动（commit `63daf5e3`，`#201`）

| 文件 | 改动 |
|---|---|
| `src/utils/episodic_memory_store.py`（**归属外一处**：记忆库，读方法一枚，无写路径） | 新 `growth_counts(now=None)` → `{since_install（COUNT(*) 全部历史行，含软删 / stale / ignored）, today_new（created_at ≥ 本机当天 0 点）, promoted_total（tier=stable 且 active）, first_created_at（MIN(created_at) ≈ 建库 / 安装日）}`；异常全零不抛。`admin_summary` 多出 `"growth"` 键（向后兼容）。 |
| `src/web/routes/episodic_identity_routes.py` | 模块级常量 `MEMORY_STALL_MIN_INBOUND = 50`；纯函数 `inbound_message_total(inbox_db) -> Optional[int]`（`file://…?mode=ro` 只读 URI、2s 超时、缺库 / 无表 / 异常 → None）与 `memory_growth_status(growth, inbound_total, min_inbound)`（`stalled = inbound 已知 ∧ ≥50 ∧ since_install==0`；None 不判——宁不报勿误报）；`/api/episodic-memory/summary` 对 `out["growth"]` 合成后返回。 |
| `src/web/templates/episodic_memory.html` | 健康卡 `<h2>` 下加 `#cs-growth`（一行计数，`title` 解释口径）+ `#cs-stall`（`role=alert` 红底红字，默认 hidden）；JS `_emGrowthRender(g)` 由 `_emHealthLoad` 调用：无 `growth` 字段两行都 hidden；日期用 `window.wsFmtDate`（封板模板禁 `toLocale*` 门禁 `test_sealed_templates_no_legacy_date_formatters`）；红字末尾 `<a href="/logs">` 链到日志页。 |
| `src/web/i18n_packs/episodic_page.py` | ZH / EN +6：`em_growth_line`（{n}/{t}/{p}）/ `em_growth_since`（{d}）/ `em_growth_inbound`（{i}）/ `em_growth_stalled`（{i}）/ `em_growth_stalled_link` / `em_growth_tip`。zh_hant 随 N-5 发版 regen。 |
| `tests/test_memory_growth_o2.py`（新，8 例） | 空库全零；三条写入 → since 3 / today 3；一条转正 + 一条被忽略 → **since 仍 3、promoted 1、total_count 2**（全时计数不随删除消失）；昨日行不计今日、`first_created_at` 取最早；停滞规则四态（50 → 停 / 49 → 不停 / None → 不停 / since=1 → 不停）+ 自定义阈值；入站只读计数（60 行 40 入 / 缺库 None / 无表 None）；**端到端** `create_app` 起真管理台 → `GET /api/episodic-memory/summary` 带 `growth`（since 1 / stalled False / `stall_min_inbound` 50）；模板 id / `_emGrowthRender` / `/logs` 链接 / i18n 双语与占位符。 |

### 4.3 验收

- `pytest tests/test_memory_growth_o2.py tests/test_episodic_identity_display.py tests/test_episodic_review_queue.py tests/test_episodic_grounding_drops.py tests/test_episodic_recall_log.py tests/test_web_episodic_memory_api.py` → **87 passed**；`tests/test_i18n_coverage.py`（含封板模板门禁）+ `test_memory_growth_o2` → **333 passed**。
- 指令验收「干净包 + 修复后计数从 0 开始涨；未修复态显红字」：未修复态 = `since_install=0` 且 skuio 机入站早已 ≥50 → `stalled=True` 红字（`test_memory_growth_status_stall_rule` 第一例）；修复态 = A 段白名单生效后首条抽取写入 → `since_install=1` → 红字消失、计数开始涨（第四例）。真机口径见 §5 回访文案。
- 边界（写明）：`since_install` 以 `bot.db` 建库为起点——**重装但保留用户数据区**的机器（skuio 09-07 13:14 就是这种）会连带旧记忆一起数，红字条件因此更保守（只在真正 0 条时亮）；要按「本次安装」切分需要装机戳，归下批。

## 5. 收工

### 5.1 回访文案（交 O-5，给 skuio）

> 记忆抽取的根因已确认并修复：干净包的最小配置里从来没有「可抽取意图白名单」（`memory.extract.intents`），抽取闸对空白名单是「不抽」，所以 13:14 重装后每条客户消息都被 `skip`，而启动日志却写「已启用」。修复版（1.0.77 若 N-5 已带上，否则 1.0.78）装上重启后：
> 1. `backend.log` 首次启动会出现 `产品基线已补齐进 overlay: memory.extract.enabled, memory.extract.intents, memory.extract.use_llm`，随后每次启动一行 `[episodic] 情景记忆已启用 · 可抽取意图 4 个：complaint, direct_chat, greeting, small_talk`（若哪天又变成 WARNING「抽取白名单为空」，就是配置被改空了）；
> 2. 客户一条普通消息进来 → `[episodic] schedule run user=<客户号> intent=direct_chat …` 后**不再**出现 `skip: intent=… not extractable`；随后一行 `[episodic] scope=customer reason=… facts=N`；
> 3. 人工接管中你亲手回客户 → 多一行 `[episodic] schedule run user=<客户号> … source=manual_out`（客户对你说的约定也进记忆）；
> 4. **AI 记忆页顶部**新增「自安装以来新增 N 条 · 今日 +T · 已转正 P」，装上后 N 应从 0 开始涨；如果红字「记忆没有在增长」还在，把 `[episodic]` 行截给我们。

### 5.2 本批未做 / 交下批

- `memory.consolidation.enabled` 代码缺省关（服务器与 min 种子都没写）→ 客户机永不晋升 stable / 不走 J-10 自动转正，「已转正 P」会一直是 0——**待拍板**（§〇 第 3 条），建议与 W7ZTSB「记忆确认机制重设计」一起。
- 坐席**手机端**直接回的消息（TG `mirror_outgoing` 镜像行）不经 `origin=manual` 路由，B 段不覆盖（§2.3）。
- `memory_scope` 是规则不是 NER（§3.3）；`config.example.yaml` 未补 `extract` 块（§1.2 末）；zh_hant 词条随 N-5 regen。
- 「自安装」以建库为起点，重装保留数据区的机器连带旧记忆（§4.3）。

### 5.3 归属外触碰清单（都是记忆侧、无其他线登记；每处一个 hunk，agent_probe 全程无冲突）

`src/inbox/human_outbound_memory.py`（B 接线）/ `src/utils/daily_learner.py` + `src/web/i18n_packs/learner_page.py`（C 分流单一来源 + 4 标签）/ `src/utils/episodic_memory_store.py`（D 读方法）/ `src/web/routes/episodic_identity_routes.py`（D，本就是记忆页路由）/ `docs/功能矩阵_ChatX.md`（A 生成物重导出）。`config_manager.py` / `src/inbox/store.py` / `unified_inbox_send_routes.py` **一字未动**。

## 落点（每个 commit 落地后立刻补一行）

| commit | 段 | 工单 | 文件 | 说明 |
|---|---|---|---|---|
| `ad5c7dbd` | A | #201 | `feature_registry.py` / `config.desktop.min.yaml` / `skill_manager.py`（import + `episodic_startup_banner` + L505）/ `docs/功能矩阵_ChatX.md`（重导出）/ `tests/test_feature_registry.py` / `tests/test_memory_extract_baseline_o2.py`（新） | 三键进 A 类基线 + 种子带 memory.extract 块 + 启动日志四态说真话；87 passed（3 预存红）+ episodic 160 passed |
| `21a02beb` | A docs | #201 | `docs/发版对账_v1.0.78_O2.md`（新）/ `docs/O批_进度总账.md` O-2 行 | 落点表建档 + 总账「A 已收工可并入 1.0.77」 |
| `45b56b64` | B | #201 | `skill_manager.py`（`_schedule_episodic_memory_extract` +`source=` / 新 `schedule_manual_outbound_extract`）/ `human_outbound_memory.py`（第 4 步接线 + `inbox_store` 透传）/ `tests/test_manual_outbound_extract_o2.py`（新 15 例） | origin=manual 出站同走抽取：客户这一轮的话作 user_msg、坐席句作 reply、同桶同链、两道去重、`source=manual_out` 日志；94 passed |
| `1a9b558b` | B docs | #201 | `docs/发版对账_v1.0.78_O2.md` | §0 / §2 / 落点行 |
| `6b42dc10` | C | #201 | `src/utils/memory_scope.py`（新）/ `daily_learner.py`（`private_kinds` 切单一来源 + `PRIVATE_KINDS` +4 + 去向日志）/ `skill_manager.py`（抽取链 `[episodic] scope=` 一行）/ `i18n_packs/learner_page.py`（zh/en +4）/ `tests/test_memory_scope_o2.py`（新 34 例） | 私事只进客户记忆：三例验收 + 弱信号合取防误伤业务问题；152 passed + i18n 22 passed |
| `b32deec5` | C docs | #201 | `docs/发版对账_v1.0.78_O2.md` | §0 / §3 / 落点行 |
| `63daf5e3` | D | #201 | `episodic_memory_store.py`（`growth_counts` + `admin_summary.growth`）/ `episodic_identity_routes.py`（`inbound_message_total` / `memory_growth_status` / summary 合成）/ `episodic_memory.html`（`#cs-growth` + `#cs-stall` + `_emGrowthRender`）/ `i18n_packs/episodic_page.py`（zh/en +6）/ `tests/test_memory_growth_o2.py`（新 8 例） | 「自安装以来新增 N 条 · 今日 +T · 已转正 P」+ 入站 ≥50 且 N=0 红字链 /logs；87 passed + i18n 333 passed |
