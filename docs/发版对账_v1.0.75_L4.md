# 发版对账 v1.0.75 — L-4（后台可见性分层：用户版 / 代理商版 / 研发版 + 学习队列止血 · #197 #198 #212 #200 #201 #199 #211）

> 指令：`docs/指令_L-4_后台可见性分层_2026-09-06.md`。总账：`docs/L批_进度总账.md` L-4 行。决策：底座 D-L2 / D-L5 / D-L6 / D-L7 全部按默认执行（隐藏不删路由、止血不重做、代理商版才见商业面、用户版空态「功能升级中」）。
> **本条不发版、不重启生产实例。** `.py` 改动（`nav_schema.py` / `ui_visibility.py` / `admin.py` 两处 / `ui_visibility_routes.py` / `developer_page_routes.py` / `daily_learner.py` / `learner_routes.py` / `kb_store.py` 一行 / `demo_seeder.py` / `demo_routes.py` / `license_routes.py` / `feature_center_routes.py` / `feature_gate.py` / `singing_routes.py` / `page_routes.py` / `help_commands.py`）由值守在下个重启窗装载；模板 / i18n pack 已热更（**热更中间态自洽**：`ui_client_hide` 缺席一律按「不藏」，旧后端零变化）。
> 接手：先读落点表逐行核对，再 `git -C D:\boundless log --oneline -12 -- engines/chengjie/src/web/nav_schema.py engines/chengjie/src/utils/daily_learner.py engines/chengjie/src/web/templates/settings.html engines/chengjie/src/web/templates/reply_settings.html` 确认七个 commit 真在。
> ⛔ 未碰：`personas.html` / `persona_studio_core.js`（L-2）、`reply_pacing_settings.py`（L-1 bubbles 段；E 项不需要动它——holding 默认本就 False、voice 两键契约不变）、`feature_registry.py`（L-5 正在加 D1/D7 三键；B 项文案全在 i18n `feature_center.py`，不必碰表）、`skill_manager.py` / `store.py` / `drafts.py`（谁都不改）、`autosend_worker.py`（L-3）。`admin.py` 只动两处：A 的 `_enrich_context` 形态上下文注入（不动它导航拿不到开发者模式）、B 的档位徽标口径（指令点名的那一处）。
> 真机：未动任何账号。证据全部来自 `tmp_diag\{D736EJ,WBFYSR,AMTKXC,PF5QAF,SV62XJ,CMGJ9G,4Q3M5G}`（老板指令：不必再取证）+ 本地单测。

## 根因一句话

后台没有「给谁看」的维度：`nav_schema.CLIENT_HIDDEN_ITEM_IDS = ("logs","developer","bug_tickets")` 是唯一的形态门，清单三项就没再维护过，也没有代理商这一层——于是白标 / 演示数据 / 授权空壳、TG 时代斜杠命令帮助页、大模型调参面板、曲库 0 声库 0 的歌房、只有消费端的声音评测、全部对客户直出；学习队列则是 BM25 分数被 `min(score/15, 0.99)` 折算成恒 99%「疑似重复」+ 寒暄占位入队 + 「全部通过」166 条无确认进共享 KB。

## 开发者模式 flag（L-2 读，已写进总账 L-4 行）

| 层 | 名字 | 说明 |
|---|---|---|
| session | `developer_mode`（须与 `dev_unlocked` 同真） | `/developer` 页「开发者模式」卡开关 → `POST /api/developer/developer-mode {on}`；只写 session **不落 overlay**（客户机不会永久变研发面）；退出登录 / `POST /developer/logout` 即关 |
| 纯函数 | `src.web.ui_visibility.resolve_developer_mode(session)` / `client_hide_active(cfg, developer_mode)` | 后者＝client 形态且未开开发者模式＝「用户版隐藏」生效 |
| 模板全局 | `ui_flavor`（client / partner / internal）、`ui_developer_mode`（bool）、**`ui_client_hide`**（bool） | L-2 三处「开发者模式可见」写 `{% if not ui_client_hide %}`；`ui_client_flavor`（J-4 G）语义不变＝纯形态 |
| 导航 | `get_nav_context(cfg, developer_mode=)` | client + 开发者模式 → 被藏项回归并带 `tier="internal"/"partner"` 注解，侧栏渲染「研发 / 代理商」角标 |
| 形态 | `ui_visibility.flavor: client \| partner \| internal` | `partner` 只能显式配置（桌面信号只推出 client）；L-5 发版脚本定代理商机怎么落 |

## 落点表

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注/剩余 |
|---|---|---|---|---|---|
| **A 分层机制** | #197 #198 #212 #199 #211 | `395acee7` | `nav_schema.py`：`CLIENT_HIDDEN_ITEM_IDS` 加 `help / strategies / singing / voice_eval`（`learner` 按 D-L5 不藏，本就在简洁模式「更多」）；新增 `PARTNER_ONLY_ITEM_IDS = (settings_brand, settings_license, settings_demo)`（`CMD_EXTRA_ITEMS` 三条 `/settings#brand|#license|#demo` 深链，代理商 Ctrl+K 直达；与系统设置同 key，按 path 剔）；`_apply_ui_visibility(..., developer_mode)` client 形态剔两表、开发者模式改打 `tier` 注解 · `ui_visibility.py`：`FLAVOR_TIERS` 三层、`resolve_developer_mode` / `client_hide_active` · `ui_visibility_routes.py`：`POST /api/developer/developer-mode`（dev 解锁 + 只写 session） · `developer_page_routes.py`：logout 顺手清 · `admin.py` `_enrich_context` 注入 `ui_flavor / ui_developer_mode / ui_client_hide` 并传 `developer_mode` 给导航 · `base.html` nav_item 宏 + `_ws_sidebar.html` 渲染角标（各一 hunk）· `developer.html` 开发者模式卡 · i18n `nav.py` / `developer_page.py` · `tests/test_nav_client_hidden.py`（21 例）、台账 `test_ui_visibility._AUDIENCE_DECLARED` / `test_nav_regroup` 收录三条深链 | 新 21 例 + `test_ui_visibility` / `test_nav_regroup` / `test_nav_matrix_mode` / `test_nag_desktop_suppress_j4g` / `test_i18n_zh_hant` 99 passed；`test_i18n_coverage::test_base_chrome_i18n_keys_all_defined` 全绿 | partner 与 internal 的导航暂**无差别**（两表都只对 client 生效；bug_tickets 是否对代理商也藏 → 待老板）。`base.html` 是 L-2 文件（「7 ▾」一处），本项只加 nav_item 宏 3 行 + 4 行 CSS，与 L-2 的 `.user-btn` hunk 不相邻 |
| **F 学习队列止血四件** | #201（「全部通过」按 P1） | `b5d146bd` | `daily_learner.py`：`is_noise_query`（寒暄复用 `greeting_lexicon.is_greeting_message` / 任何「[」「【」开头 / 「语音消息-下载失败」类片段 / 纯表情标点）不入队，素材只收 `is_fact_question`；`private_kinds`（复用 J-10 `memory_review.high_impact_categories` 六类 + `is_commitment` + 自报姓名句式）→ 通过后经 `set_memory_writer` 写**该客户** AI 记忆（`source_ref=conv:…` 定位，路由注入 `sm._episodic_storage_key` + `estore.add_fact`）、**不进共享 KB**，无客户归属 → `LearnerApproveError(private_no_customer)` 保持 pending；进 KB 的条目 `source=learner`（`kb_store.KB_SOURCES` + `learner`，`knowledge.html` 来源下拉 + `kb2_src_learner`）；`approve_all_pending` 不传 ids 恒 0，`batch_action` approve 每批 ≤ `APPROVE_BATCH_LIMIT=20`（超出进 `skipped`）；`check_duplicate` BM25 层：`include_vendor=False` + `kb_gate.lexical_overlap_ok` 实词门 + **不再折算百分比**（`score=0 / method=bm25`）；`_triage_backfill_once`（kb_meta `learner_triage_backfill_l4` 幂等）：噪音 pending 自动 `rejected`（`reviewed_by=system:l4_noise`）/ 带查重标的重算 / 补 `private_kinds`；新列 `query_zh / private_kinds / dup_method` · `learner_routes.py`：approve-all 须 `ids` + `confirm=1`（无确认 428、超 20 400）、私事失败 400 转人话、新 `POST /api/learner/drafts/translate`（复用 `TranslationService` L1/L2 缓存，identity 不落库）· `learner.html`：「全部通过」→「通过已选（N）」+ 二次确认弹层列明去处 / 译文行 / 「客户私事」徽章 / 「疑似重复 · 相似度未计算（文本匹配）」/ 写入记忆的条目不再链 KB · i18n `learner_page.py` 34 键 · 路由清单 +2（含 A 的 developer-mode）· `tests/test_learner_triage_201.py`（27 例） | 新 27 例 + `test_learner_dedup` / `_enhancements` / `_funnel` / `_traceability` / `test_kb_source_isolation` 127 passed；`test_admin_route_inventory` 我方两条已入基线（剩两条 `channel-alert/*` 是 L-3 在途） | 「≥2 个不同客户重复」：`kb_miss_log` 只有 `(query, cnt, last_at)` 无客户列，`log_miss` 在 `skill_manager`（谁都不改）不带身份 → 暂以 cnt ≥ `min_miss_count`(2) 代替，**待 P3 案例学习一并做**。「缓冲语走人设口吻」同理（见 E）。skuio 机装载后：166 待审中噪音条自动进「已拒绝」页（可追溯），假 99% 重算清标，日志 `学习队列存量清标（L-4 #201）` 一行 |
| **B 系统设置口径** | #197 | `5512c0a6` | `settings.html`：品牌白标 / 授权 / 演示数据三卡按 `ui_client_hide` 挂 `set-tier-hide`（hash 深链**不**揭示——商业结构面不是被藏的功能开关）；授权卡删「已并入会员中心」空壳描述与提示框，措辞改读 `plan_source`：授权文件生效→「旗舰版 · 剩余 N 天」/ 厂商自营覆写→「旗舰版（厂商自营）· 未检测到授权文件」/ 其余→「社区模式」；演示卡按 `seed_allowed` 灰掉「铺示例数据」并写原因 · `feature_gate.badge_label_key` + `admin.py` 徽标改读 `gate_snapshot`（`plan_override` 显 `mb_plan_<plan>_override`）· `license_routes.py` `/api/admin/license` 加 `effective_plan / plan_source / gate_enabled` · `demo_seeder.real_workspace_footprint / seed_allowed`（真实会话 = 总会话 − demo；真实账号 = `AccountRegistry.list()`）+ `demo_routes` seed 409 `err.demo.workspace_not_empty`、状态回 `seed_allowed` · `feature_center.py`：「缺依赖 / 未开放」→「需要配套服务 / 即将开放」、`fc_hint` 去 `config.local.yaml`、C 类原因人话 · `feature_center_routes.py`：client 形态（未开开发者模式）`needs_dep` 的 extra 用 `fc_dep_client_generic`，不显 `ai.embedding_base_url` 一类配置键 · i18n `settings_page.py` / `membership.py` · `tests/test_settings_tier_197.py`（13 例） | 新 13 例 + `test_ui_visibility` / `test_feature_gate` 61 passed；`test_demo_seeder` / `test_feature_center_routes` / `test_feature_gate_routes` / `test_membership_page` / `test_settings_he_*` 全绿 | 矛盾根因：`config.desktop.internal.yaml` `licensing.feature_gate.plan_override: flagship`（内测种子）让右上读「旗舰版」而授权卡读文件「社区模式」。现两面同源；**「以授权文件为准」在无 override 的部署完全成立，有 override 的内测机显式标「厂商自营」**——是否让 override 干脆不影响徽标 → 待老板。「坐点数据」错字当前源码已是「坐席」（`set_s033`），无需改 |
| **G 歌房 / 声音评测空态** | #199 #211 | `a4f453da` | 新 partial `_client_tier_upgrading.html`（`ui_client_hide` 时渲染「功能升级中，如需开通请联系客服」横幅 + `.l4-locked` 样式）· `singing.html` / `voice_eval.html`：引入 partial、原操作面容器加 `l4-locked`、脚本锁定时不拉 API · `singing_routes.py`：`_assets_summary`（启用曲目 / 可用声库锚 / 备货段）进 `overview.assets`；`POST /api/singing/config enabled=true` 在 `ready=false` 时 409 `err.singing.assets_missing`（关闭与护栏参数不拦）· `singing.html` 资产不齐→能力位禁用并写原因，已开着→红字警示，409 detail 原样转告 · 新 i18n pack `client_tier.py` · `tests/test_client_tier_pages_199_211.py`（12 例） | 新 12 例 + `test_singing_*` / `test_song_*` 47 passed；模板 id 唯一性 / i18n 门禁全绿 | 运行时（客户求歌建单）仍读 `companion.singing.enabled`——已开着的存量机器不会被本单自动关（页面红字警示 + 可一键关）；是否让 `resolve_singing_cfg` 在资产不齐时视为关 → 待老板（改 `song_stock.py`，不在本单文件表） |
| **C 帮助 & 指令参考** | #198 | `f9edb52b` | `help.html`：client 形态横幅 + 整页 `l4-locked`；「话术管理(4)」→「机器人斜杠命令（旧）」+ 适用范围一句话（`help_commands` 章节 `scope_key`，只对 Telegram 主号 Bot 模式生效）；「客服培训演示」入口只在 `docs/training` 文件在场时渲染（`training_available`）· `page_routes.py`：`/help` 传存在性；`/training` 缺文件改渲染新 `_page_error.html`（extends base，404 HTML），不再吐 `{"detail": …}` JSON · i18n `help_page.py` · `tests/test_help_page_198.py`（6 例） | 新 6 例 + `test_help_commands` / `test_admin_pages_smoke` 全绿；`test_i18n_coverage` seal / chrome 240 passed | `_page_error.html` 可供其他页面路由复用（本单只接了 /training） |
| **D 回复策略** | #212 | `e180d1b1` | `strategies.html`：client 形态横幅 + 参数卡 / 批量条 / 意图→策略映射整体 `l4-locked`；partner / internal 与开发者模式原面板不变 · `tests/test_strategies_client_tier_212.py`（3 例） | 新 3 例 + 全部 `-k strateg` 70 passed | **待老板**：给用户版一个入口＝三档预设「稳重 / 均衡 / 活泼」×「简短 / 适中 / 详细」映射 temperature / max_tokens / 上下文轮数，改动留历史 + 一键恢复默认（1.0.74 已有 `/api/strategy-history` 可接）；本单默认只隐藏 |
| **E 自动回复高级三块 + 平台覆写表** | #200 | `d6942318` | `reply_settings.html`：语音「启用开关 + 触发方式」→ 单一下拉 `#rps-voice-mode`（关闭 / 总是 / 对方发语音才回 / 智能），底层 `voice.enabled` / `voice.trigger` 保留为隐藏控件（后端契约不变；「关闭」只翻 enabled → 日志仍能区分 disabled 与 never），旁显引擎在线/离线 chip（`/api/voice/effective-config` 的 `ready / hub_risk`）；缓冲语并入「AI 接管」卡（`#rps-holding-row`，只在拟稿人审 / 混合时出现，默认关本就是 `False`），提示改「按客户语言回一句安全缓冲语」；删「人设 ID + 秒数」表，既有覆写原样回传继续生效并显「N 个人设已有自定义节奏」，入口指向人设工作室；平台专家表默认折叠（`details` + 摘要）、「跟随全局」格灰字显示实际生效值（`rpsFollowLabel`）、LINE 打字格按产品事实硬禁（`RPS_PLAT_KNOWN_UNSUPPORTED`）· i18n `reply_settings_page.py` 16 键 · `tests/test_reply_settings_advanced_200.py`（5 例） | 新 5 例 + `test_reply_settings` / `test_autoreply_settings` 170 passed；`-k "reply_settings or rps_ or holding"` 189 passed；`test_i18n_coverage -k reply_settings` 全绿 | **交 L-2**：① 「总开关为所有语音路径硬门禁（主动关怀 / 自动问候 / 人设覆写只能收窄）」的**运行时**强制在 `tts_pipeline.py` / `avatar_voice.py` 回落链（L-2 A 语音三态正在动）——本单只把口径写进页面提示；② 人设工作室「快 / 中 / 慢」三档 UI（写 `deliver_delay.persona_overrides[pid].{min_sec,max_sec}`）。「缓冲语走人设口吻」：`holding_reply.py` 现按客户语言从安全短语库随机挑句，人设口吻需 LLM 改写，属 P3 |

## 交 L-7 验收口径（回访文案用）

| 单 | 口径 |
|---|---|
| #197 | 用户版（桌面包 client 形态）系统设置不见「品牌/白标」「试用/演示数据」「授权/激活」三卡；命令面板搜「白标 / 演示」无结果；右上档位徽标与授权卡说同一句话（无授权文件→「社区模式」；内测机 override→「旗舰版（厂商自营）」）；功能总览不再出现「缺依赖 / 未开放」与 `ai.embedding_base_url` 一类键名；演示数据在有真实会话/账号的工作区不可铺（按钮灰 + 原因） |
| #198 #212 #199 #211 | 用户版侧栏与命令面板不见帮助中心 / 回复策略 / 歌房 / 声音评测四页；直达 URL 显「功能升级中，如需开通请联系客服」且原操作面收起；内部人在 `/developer` 开「开发者模式」后四页带「研发」角标回到侧栏、页面恢复原样，退出登录自动关 |
| #199 补 | 歌房曲库 0 / 声库 0 时「唱歌能力」开关不可开启并写明原因（API 409）；已开着的显红字警示 |
| #198 补 | 帮助页章节名「机器人斜杠命令（旧）」并标注只对 Telegram 主号 Bot 模式生效；无 `docs/training` 时不出现「打开培训演示」按钮，直敲 `/training` 看到人话页面而不是 JSON |
| #200 | 语音回复只有一个下拉（关闭 / 总是 / 对方发语音才回 / 智能）+ 引擎在线/离线；缓冲语只在「拟稿人审」下出现且默认关；不再有「人设 ID + 秒数」表；平台专家覆写默认折叠，「跟随全局」显示实际值，LINE 打字格灰 |
| #201 | 「全部通过」按钮消失，只有「通过已选（N）」+ 二次确认弹层（每批 ≤20）；队列里不再有寒暄 / 「语音消息-下载失败」类条目（存量自动进「已拒绝」页）；「疑似重复」不再显 99%（触发词重叠显真百分比，文本匹配显「相似度未计算」）；外语条目有中文译文行；含人名/金额/地点/约定的条目标「客户私事」，通过后写该客户 AI 记忆不进知识库；进知识库的条目来源筛「学习队列学来的」 |

## 待老板 / 待办

- **待老板**：① D 项三档预设是否给用户版一个入口；② 有 `plan_override` 的内测机徽标是否干脆不受 override 影响（现标「厂商自营」）；③ 代理商版（partner）是否也藏 `bug_tickets`（现 partner 与 internal 导航无差别）；④ 歌房资产不齐时运行时是否直接视为关（改 `song_stock.py`）。
- **交 L-2**：`ui_client_hide` 已可用（见上表）；语音总闸运行时硬门禁；人设工作室「快 / 中 / 慢」三档写 `persona_overrides`。
- **交 L-5**：代理商机 `ui_visibility.flavor: partner` 怎么随发版脚本落（本单只认值）。
- **P3（案例学习批）**：`kb_miss_log` 加客户维度（「≥2 个不同客户」）；缓冲语人设口吻 LLM 改写；学习队列重做为案例学习（BX6ZY2）。
- **收口时**：`python -m scripts.i18n_hant generate` 再生繁体（本批各线都在加简体键，覆盖率门禁 ≥99% 目前仍绿，统一再生一次即可）。

## 已知既有失败（非本单引入）

- `tests/test_workspace_sidebar.py` 5 例（渠道中心侧栏矩阵项高亮）在 HEAD `81212d81` 干净工作树同样失败（`matrix_nav` 缺省隐藏 → fixture 里矩阵项不渲染），与 L-4 无关，留 L-7 收口时处理。
- `test_admin_route_inventory::test_no_unexpected_extra_routes` 剩两条 `/api/workspace/channel-alert/{disable,mute}` 是 L-3 在途未入基线。

## 预算

估算 ≈ $120（A/F/B/E 各约 $20–30，G/C/D 各约 $8–12）；未超 $150 上限。
