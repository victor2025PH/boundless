# 指令 Q-4：撤回错误默认 + 基线红线（班表 · 额度 · 关怀开关 · 升级说明 · 档位页）

> 预算 **$160**。工单 **#267 #265**（KYHGSZ / CSTCJT / GW9FZ2 / UW7CXF / 5YZG6V / 65UQRE / 7P24KN / HEEEN9 / R6P939；Q-0 拆单后以新单号为准）。目标版本 1.0.79（第一波）。决策 **D-Q1 撤回 D-O4、D-Q2 额度默认关**。
> 底座 `docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-4 行）。落点表 `docs/发版对账_v1.0.79_Q4.md`。

## 开工前必读

1. `docs/聊天核心能力_三视角方案_2026-09-09.md` §1 Q8–Q11、§2.1 第 6 条、§2.2 第 2 条、§3 Q-4。
2. 证据 `tmp_diag\KYHGSZ\ CSTCJT\ UW7CXF\ 65UQRE\ HEEEN9\`：班表 08:20–01:00 时区空＝服务器本地（上海）→ Mike 01:46 来信 01:47 稿已生成 risk=low 静默暂存无日志；复班回顾窗 2h 不补；额度闸门「300 · 安装包默认」运行中，各号折算 114–129，`自动 ok / 人工 ok` 两列（人工也拦）；23:16:33 升级后 `[care_dispatcher] 首拍 enabled=False`，而用户 9/7 15:27 已设 True 并热重载持久化；两条 verbatim 关怀 01:20 / 02:20 均「预计 08:00 出手（安静时段顺延）」同一分钟。
3. 代码事实（已核）：`src/utils/feature_registry.py` L281–298 `inbox.work_schedule.{enabled,default.start,default.end}` A 类 `baseline=True/"08:20"/"01:00"`（注释「timezone 空＝本机钟（桌面机就是运营者时区）」——**假设错误**）；额度：`reply_settings.html` L544 起 `rps_sg_*` 额度卡 + `i18n reply_settings_page.py` L548–550「安装包默认 300/天」，闸门实现见 `unified_inbox_send_routes.py` L2173 `api_unified_inbox_send_caps` 与 `official_api_worker.official_send_caps`；`config_manager.py` L289「功能 overlay 已存在时只补缺失键（显式 true/false 都尊重）」+ L330 `_seed_overlay_from`；`care_dispatcher.py` L207 `shift_out_of_quiet_hours`、L66 `goal_row_policy → ignore_quiet`；work_schedule 判定散在 `reply_pacing_settings.py` / `autodraft_helpers.py` / `autosend_helpers.py`（`off_hours` / `catch_up_regenerate_hours`）。
4. `scripts\agent_probe.ps1` 看 ACTIVE：你是 Q 批**唯一**动 `feature_registry.py` 与 `reply_settings.html` 的线（Q-5 的抽取两键、Q-8 的 proactive_topic 都经你 / 等你）；**不动 `autosend_worker.py`**（Q-3 的）——需要它加一行日志时写总账让 Q-3 代加。

## 红线

- 基线三条红线（写进 `feature_registry.py` 模块 docstring 并加 test）：① 只补缺席键、绝不覆盖显式值；② **会改变发送行为的开关不得 `baseline=True`**（work_schedule / 额度 / 拆条 / 关怀真发 / 全自动）；③ 时区类键不得默认服务器本机。
- 存量机器：已显式写过的值不动；本次撤回只对「由基线补进去的」值生效（有 `baseline` 标记来源的才回滚，落日志）。
- `git add <显式路径>`；每 commit 带 `#267`。

## A 班表出厂关 + 时区显式 + 拦截可见 + 过夜作废（D-Q1）

- `inbox.work_schedule.enabled` 改 `baseline=False`（或降 B 类）；`default.start/end` 保留为「开启时的建议值」不再 baseline；升级迁移：由 1.0.78 基线补成 true 且用户未改过的 → 回滚 false 并落 `[baseline] rollback inbox.work_schedule.enabled reason=D-Q1`。
- 开启流程：时区**必填**（账号时区 / 客户时区 / 本机，默认空不允许保存），页面换算提示「上海 01:00 = 客户纽约 13:00」；按账号可配可关。
- 拦截可见：`work_schedule` 判定处（`autodraft_helpers` / `autosend_helpers`）命中 off_hours → 日志 `[work_schedule] hold=off_hours conv=… until=hh:mm tz=…` + 会话 meta 标注（工作台小标签「作息外 · 到点重新拟稿」）；若必须在 worker 落日志 → 写总账交 Q-3。
- 过夜积压：`catch_up_regenerate_hours` 默认 0（=全部作废重写，按「刚醒」语境），「补发原稿」不再是默认；回顾窗文案与行为统一。
- 验收：clean 装 → 班表关；开班表未选时区不能保存；夜间入站 → 日志 + 标签；tests 三例。

## B 额度闸门出厂关 + 永不限人工 + 80% 提示（D-Q2）

- 额度 feature 键 `baseline=False`；迁移同 A 回滚；闸门实现只作用于 `origin=auto`（人工发送路径绕过，`自动 ok / 人工 ok` 两列去掉人工列）；开启时卡片「本号今日 AI 已发 N / 上限 M」进度条 + 80% 橙 + 会话列表 / 会话内提示「AI 已达今日额度请手动接管」，禁静默停发；「300 · 安装包默认」→「300 · 推荐」，折算依据一句话（新号 / 养熟）；启动日志打印闸门状态与各账号上限。
- 验收：人工发送在额度用尽后仍成功；自动达 80% 有提示；tests。

## C 关怀开关被升级重置：根因 + 红线

- 复现：以 1.0.77 数据目录 + 用户显式 `proactive_care.enabled=true`（overlay / 页面持久化落在哪个文件）升到 1.0.78 → 观察是否变 false；嫌疑：① 基线补齐写到了更高优先级的层；② 页面持久化写在升级会替换的文件；③ 迁移脚本覆盖。修根 + 加 test「升级不得改变用户显式值」；升级后回读对照并落 `[upgrade] user_flags_preserved=N changed=[]`。

## D 关怀人工指定时间 + 安静时段可见

- `goal_row_policy` / `shift_out_of_quiet_hours`：`mode=verbatim` 且人工指定时间 → 默认 `ignore_quiet`（尊重人工）；关怀页排期时若落在安静时段弹「仍按 01:20 发 / 顺延到 08:00」；顺延多条 → 错峰 0–40 min 或合并成一条；时区按客户 / 人设（无则账号）；关怀页显示安静时段区间 + 开关 + 时区依据；「出手」→「发送」；顶部「到期会先拟稿给你过目」只在 dry_run / 预览模式显示。
- 验收：01:20 verbatim → 按时发；两条同顺延 → 间隔 ≥ 5 min；tests。

## E 抽取两键进基线（代 Q-5）

- `companion.goals.profile_llm.enabled` A 类 `baseline=True`（陪伴域）；核实 `memory.extract.use_llm` 现基线值，改 True；注意红线②不适用（不改变发送行为，只改抽取）；启动日志打印两键。

## F 「这版改变了什么」弹窗

- 数据文件 `release_defaults_changed.json`（每版发版线维护：键 / 旧默认 / 新默认 / 人话说明）；升级后首次打开工作台弹一次（desktop renderer 或 web 模板）：列出本版新开 / 新关的默认开关，每行「保持关闭 / 开启」+ 时区确认；关闭即写用户显式值。i18n 三语。
- 验收：模拟 1.0.78 → 1.0.79 首启弹出，选择落 overlay。

## G 档位页文案（HEEEN9）

- `reply_settings.html` 节奏区：标题「未设置账号的兜底档位」+「N 个账号已设全自动 / M 个跟随本页」；「新会话自动沿用此档位」开关人话重写、默认关、收进高级区（或移除，写落点表理由）；账号菜单「此账号全部会话 → 全自动」注明含以后新会话；全自动卡片说明补「高风险转人工审核」（Q-3 落地后为真）。三层示意图一行文字即可。

## 收工

- 落点表 `docs/发版对账_v1.0.79_Q4.md`（§〇 「为什么 1.0.78 把美英客户的下午关掉了」+ 三条红线正文）；`docs/Q批_进度总账.md` 只改 Q-4 行；`agent_probe.ps1 -Done`；回访文案交 Q-0 / R79；`release_defaults_changed.json` 的维护步骤写进 R79 粘贴块前置（总账）。

## 开新对话粘贴

```
读 docs/指令_Q-4_撤回错误默认与基线红线_2026-09-09.md，按它做 Q-4（#267 #265：inbox.work_schedule.enabled 出厂关 + 基线补进去的回滚 + 时区必填与换算提示 + off_hours 拦截落日志与会话标签 + 过夜积压作废重写 / 额度闸门出厂关 + 永不限制人工 + 80% 提示 + 文案 / 关怀开关被升级翻回 False 的根因与「不覆盖显式值」test / 关怀 verbatim 人工时间默认 ignore_quiet + 弹选择 + 顺延错峰 + 时区 + 页面可见 / companion.goals.profile_llm 与 memory.extract.use_llm 进基线 / 升级后「这版改变了什么」弹窗 + release_defaults_changed.json / 档位页三层文案）。预算 $160。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与 D-Q1 D-Q2、docs/发版对账_v1.0.78_O1.md D 行（在它之上撤），再 scripts\agent_probe.ps1 -Intent 登记；你是 Q 批唯一动 feature_registry.py / reply_settings.html 的线；不动 autosend_worker.py（要加日志写总账交 Q-3）。基线三条红线写进 feature_registry docstring 并加 test。每个 commit 带 #267 并立刻补落点表 docs/发版对账_v1.0.79_Q4.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-4 行并 agent_probe -Done。
```
