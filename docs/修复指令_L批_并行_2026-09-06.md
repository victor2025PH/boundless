# 修复指令 L 批（真未修 20 单 + 两条系统性缺口）—— 并行公共底座

> **本文件只是「索引 + 去向表 + 文件归属 + 老板决策」的公共底座，不要拿本文件开对话。**
> 每条指令各自成文，**一条指令 = 一个新对话**。开对话粘贴话术在各指令文件末尾，也汇总在 `docs/L批_进度总账.md`。
>
> 依据：`docs/未修复问题总盘_三视角_2026-09-06.md`（09-06 08:0x：台账 213 单，未关闭 69 = 49 张「代码已进 1.0.74 待验」+ 20 张真未修 #194–#213；两台内测机 Cursor 报告 76 份全部挂单；skuio 自查 C6EFRS）。
> **结论：20 张真未修没有一张是「修坏了」，全部是 09-05 漏立单没排进去；另有两条系统性缺口这次才看出来——（A）「出厂开」决策只写在内测包种子里，两台内测机通过应用内更新拿的是干净包，D1 自动推进 / D7 危机留痕一个都没落地；（B）桌面包带着办公室内网 GPU/STT 默认地址，客户机反复探不通→熔断→回落，是翻译元回复 / 学习队列假 99% / 转录返空 / 看门狗红噪音的共同上游。**
> 所以本批七条里 **L-5（渠道）和 L-6 的服务端两件（识图 num_ctx / 令牌）不动业务代码、当天见效、最先做**；L-1…L-4 是写代码；L-7 是流程 + 收口。目标版本 **1.0.75**。

| 指令 | 预算 | 类型 | 内容 | 文件 |
|---|---|---|---|---|
| **L-1** | $120 | 出站可信度（AI 别让客户识破 / 别说假话） | #210 拆条止血 + 工作台按实际条数 / #208 问候不编事实 + greeting 分类收紧 / #194 无响应文案与双提示合一 | `docs/指令_L-1_出站可信度_2026-09-06.md` |
| **L-2** | $180 | 人设与语音闸门（后台页 + 语音路由） | #205 语音三态 + 校验 + 兜底不换声 / #203 JSON 导入回滚加闸 / #204 绑定胶囊同源 / #202 备份与迁移 / #206 补齐向量人话 / #192 残「7 ▾」 | `docs/指令_L-2_人设与语音闸门_2026-09-06.md` |
| **L-3** | $150 | 工作台状态真相 | #207 需人工闭环 / #170 幽灵未读闭环（告警上屏 + 浮回核实）/ #196 断线弹窗出口 + 告警归拢 / #209 批量挂链弹层 | `docs/指令_L-3_工作台状态真相_2026-09-06.md` |
| **L-4** | $150 | 后台可见性分层 | #197 系统设置 / #198 帮助 / #212 回复策略 / #200 自动回复高级三块 / #201 学习队列止血 / #199 #211 用户版收起 | `docs/指令_L-4_后台可见性分层_2026-09-06.md` |
| **L-5** | $100 | 内测渠道与出厂默认（非业务代码） | 内测机能拿到 seed-data 的更新路径 + D1/D7 默认进基础配置 + 种子缺席可见 | `docs/指令_L-5_内测渠道与出厂默认_2026-09-06.md` |
| **L-6** | $120 | 通道与服务端 | #213 识图网关 num_ctx + 令牌（服务端）/ 桌面包剥离内网默认值 / #195 贴纸落盘 / 首启随机 secret_key | `docs/指令_L-6_通道与服务端_2026-09-06.md` |
| **L-7** | $100 | 值守登记链 + K-5 收口 | 报告到达自动立单 / 每日对账 / smalltalk / 序号哨兵豁免 / K-5 ④⑤（49 单标 fixed + 61+ 回访） | `docs/指令_L-7_值守登记链与收口_2026-09-06.md` |

**投放顺序**：**L-6 服务端两件 + L-5 先开**（当天见效；L-5 定「出厂默认怎么落」，L-1/L-2 的默认值改动要照它的口径落）→ L-1 / L-2 / L-3 / L-4 文件不重叠，可同时开 → L-7 在 L-5 出结论后开（回访文案要写清哪些默认已开）。
**跨对话 / 跨账号交接唯一入口**：`docs/L批_进度总账.md`。
**背景**：`docs/未修复问题总盘_三视角_2026-09-06.md`、`docs/K批_进度总账.md`、`docs/发版对账_v1.0.74_K5.md`（断点节 09-06 追加）、`docs/值守_报障群用户画像_2026-08.md`（09-06 更新）。

## 20 张真未修 + 2 条系统性缺口的去向

| 去向 | 单 / 项 | 说明 |
|---|---|---|
| L-1 | #210 #208 #194 | 出站链；#171 #175 #177 已在 1.0.74，只在 L-1 落点表留「验收口径」不重修 |
| L-2 | #205 #203 #204 #202 #206 #192残 | 全在 `personas.html` / 人设编辑器组件 / 语音路由；#199 #211 的「补齐生产入口」半边**默认不做**（D-L6 默认隐藏，归 L-4） |
| L-3 | #207 #170 #196 #209 | 工作台前端 + watchdog + notify-bus；#170 只做闭环（告警上屏 / 浮回核实 / 入口引导），J-4 A 入口本身不重做 |
| L-4 | #197 #198 #212 #200 #201 #199 #211 | 扩 `nav_schema.CLIENT_HIDDEN_ITEM_IDS` + 各页模板；#201 只做止血四件（全部通过确认 / 入队收窄 / 译文 / 私事分流），重做为案例学习属 P3 提案 |
| L-5 | #166残 #185残 + 渠道 | 不写业务代码：`config.yaml` 默认值 + 发版脚本 + 装机脚本 + 一行日志 |
| L-6 | #213 #195 + 内网地址外泄 + 默认 secret_key | #213 两件是 VPS / 中继机上的活，不发桌面包 |
| L-7 | 流程 + K-5 ④⑤ | 值守工具与台账；49 单标 fixed 按 D-L8 |
| **不排修** | 49 张「已进 1.0.74 待验」 | 见总盘 §5；复报即打回原线（J-1/J-2/J-3/J-4/J-6/J-8/J-9/J-10/K-2/K-3） |
| **等老板拍板** | BX6ZY2 案例学习 / 9SZ8DY 三档生命周期 / W7ZTSB 记忆例外确认 / ERS5QQ 三态迁移 | 总盘 §3 P3；拍板后另立批 |

## 文件归属矩阵（共享工作树；开工先 `scripts\agent_probe.ps1` 看 ACTIVE 再动）

| 文件 / 目录 | 归属 |
|---|---|
| `src/inbox/reply_split.py`、`src/inbox/reply_pacing_settings.py`（bubbles 段默认值）、`src/web/routes/unified_inbox_send_routes.py`（`_send_bubbles` 镜像落行）、`src/skills/skill_manager.py`（**只动 `GreetingSkill` 与 greeting 分类两处，先看意向板 J-10 是否静默**）、`src/utils/greeting_lexicon.py`、`unified_inbox.html`（**只动 `_doSendMedia` 附件条/toast 两处**）、`i18n_packs/inbox_workspace.py`（对应键）、对应 tests | **L-1** |
| `personas.html`、`static/js/persona_studio_core.js`、`src/utils/persona_manager.py`（`list_profiles_summary`）、`src/web/routes/persona_routes.py` / `persona_media_routes.py`、`src/companion/persona_stock.py`（`collect_binding_usage` 复用）、`src/ai/tts_pipeline.py`（回落链 L868 / L1664 / `safe_edge_voice` L717）、`src/ai/avatar_voice.py`（只动回落判定）、`i18n_packs/persona_studio.py`、`base.html`（仅「7 ▾」一处，定位后再动）、对应 tests | **L-2** |
| `unified_inbox.html`（**只动**：需人工原因条 / 被埋横幅 / 角标浮层引导三处）、`src/web/static/workspace/notify-bus.js`、`workspace_base.html`（chandown）、`src/inbox/health_watchdog.py`（`alert_on_warn` 上屏路径 + 告警归拢）、`src/integrations/protocol_autoreply.py`（`clear_needs_human` 触发面）、`src/inbox/autosend_worker.py`（**只加**成功后摘标调用）、`desktop/renderer/shared/copilot/ui-prompt.js`、`workflows.html`（bulkStart 预览）、`i18n_packs/inbox_workspace.py`（对应键）、对应 tests | **L-3** |
| `src/web/nav_schema.py`（`CLIENT_HIDDEN_ITEM_IDS` + 二层）、`settings.html`、`help.html`、`strategies.html`、`reply_settings.html`、`learner.html` + `src/web/routes/learner_routes.py` + `src/utils/daily_learner.py`（入队条件 / 相似度未计算态 / 私事分流）、`singing.html` / `voice_eval.html`（只做隐藏或「升级中」空态）、`src/utils/feature_registry.py`（功能总览术语）、`src/web/admin.py`（**只动**授权/版本口径一处）、对应 i18n packs、对应 tests | **L-4** |
| `engines/chengjie/config/config.yaml`（**只动** `companion.goals.sprint.enabled` / `companion.wellbeing.crisis_audit` / `crisis_escalation` 三键默认）、`config/config.desktop.internal.yaml`、`src/utils/config_manager.py`（**只加**种子缺席日志）、`website/scripts/publish_chatx.ps1`、`deploy/desktop/push_chatx.ps1` / `chatx_fleet_status.ps1`、`website/lib/chatxContent.ts`（内测渠道指针）、`desktop/backend-launcher.js`（**只读**）、`docs/**` | **L-5** |
| VPS `website/lib/ai-gateway.ts`（num_ctx）+ 中继机 Modelfile + `.env` 令牌、`src/vision_client.py`（失败原因人话）、`src/utils/gpu_watermark.py` / `src/voice_transcriber.py` / `src/ai/avatar_voice.py`（STT 段）/ `src/ai/spoken_style_bridge.py`（desktop_mode 下不注册 LAN 默认）、`src/inbox/health_watchdog.py`（**只动** LAN 探针 desktop 门）、`src/integrations/protocol_bridge.py`（`tg_media_meta` / `download_tg_media`）、`unified_inbox.html`（**只动** L9672 sticker 渲染分支）、`src/bootstrap/web_app.py`（首启随机 secret_key）、`desktop/backend-launcher.js`（如需传参）、对应 tests | **L-6** |
| `tools/duty_watch_loop.py`、`tools/duty_*.py`、`deploy/duty/**`、`src/ops/bug_intake.py`（**只动** smalltalk 白名单 + 序号哨兵自发消息豁免两处）、`bug_intake.db`（只经端点 / `duty_*` 工具）、`docs/**` | **L-7** |
| `src/inbox/store.py`、`src/inbox/drafts.py`、`skill_manager.py` 除 greeting 两处以外 | **谁都不改** |
| 「unlimited outbound mode」孤儿线 8 文件（K-D1） | **谁都不碰**（老板未拍） |

## 老板决策（默认按此执行；不同意的划掉，对应对话按划掉后执行）

| 决策 | 默认 | 影响 |
|---|---|---|
| **D-L1** 内测机更新渠道 | **C：两者**——内测机切到能拿 seed-data 的 smart 渠道（`latest-internal.yml` 或 push 直装），**同时** D1/D7 默认值进基础 `config.yaml`（客户版也开）；种子缺席时启动日志打一行 WARNING | L-5；L-1/L-2 默认值改动照此落 |
| **D-L2** 后台分层口径 | **client flavor 隐藏清单扩到总盘 §1-D 全部**；加「开发者模式」开关（复用 `/developer` 密码闸）给内部人在客户机打开被隐藏项 | L-4 / L-2 |
| **D-L3** 拆条默认值 | **出厂关；仅显式空行才拆；短回复永不拆**（82BF95 客户识破为据） | L-1 |
| **D-L4** 语音三态 | **新建人设默认「不发语音」；存量人设按现状迁移（有克隆录音→克隆声，其余→预置声并标注）；克隆失败一律降级文字 + 提示，禁回落默认声** | L-2 |
| **D-L5** 学习队列 | **先止血四件**（P1-7），重做为「案例学习」另立批 | L-4 |
| **D-L6** 歌房 / 声音评测 / 帮助&指令参考 | **用户版隐藏 + 「功能升级中，如需开通请联系客服」空态**；不补生产入口 | L-4 |
| **D-L7** 演示数据 / 白标 / 授权 | **代理商版才可见；演示数据仅对无真实账号的空工作区开放并打独立标记；「社区模式」与「旗舰版」口径以授权文件为准统一** | L-4 |
| **D-L8** 49 单标 fixed 时机 | **剔除 #170 #173 后照单标（A 案 `duty_ledger_fix --mark-fixed-file`），回访文案带各单验收口径**；#170 #173 等 L-3 收口 | L-7 |

## 共同纪律 / 省钱纪律 / 断点续传

与 J/K 批完全相同，见 `docs/修复指令_J批_并行_2026-09-05.md` 对应三节。三条硬红线再钉一次：

- 提交只用 `git add <明确文件>` 或 `python tools/stage_hunks.py --file … --mine … --apply`；⛔ `git add -A` / `-a` / `reset --hard` / `checkout .` / `restore .` / 裸 stash。
- 开工先 `powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "…"`，ACTIVE / HOT-ZONE 文件先避让；收工 `-Done`。模板 / i18n / 静态 JS 热更新即上生产，每次保存必须自洽。
- 每项一个 commit 带工单号；落点表写 `docs/发版对账_v1.0.75_L<N>.md`；总账只改自己那一行；**对外答复一律走 `tools/duty_reply.py`，不得教用户改配置文件**。
