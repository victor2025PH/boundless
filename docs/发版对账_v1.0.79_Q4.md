# 发版对账 v1.0.79 · Q-4 撤回错误默认 + 基线红线（#267 #265）

> 指令：`docs/指令_Q-4_撤回错误默认与基线红线_2026-09-09.md`；底座决策 **D-Q1 撤回 D-O4 / D-Q2 额度默认关**（`docs/修复指令_Q批_并行_2026-09-09.md`）；在 `docs/发版对账_v1.0.78_O1.md` **D④ `2ddbb3ec`** 之上撤。证据 `tmp_diag\{KYHGSZ,CSTCJT,UW7CXF,65UQRE,7P24KN,HEEEN9}`。
> 执行：2026-09-10 开工（`agent_probe -Intent` 已登记）。**每个 commit 落地后立刻补一行。** 预算 $160。Q-4 是 Q 批唯一动 `feature_registry.py` / `reply_settings.html` 的线；**不动 `autosend_worker.py`**（需要它改的写 §四 交 Q-3）。

## §〇 先答一问：为什么 1.0.78 把美英客户的下午关掉了？

**答：D④ `2ddbb3ec` 把 `inbox.work_schedule.enabled=true + 08:20–01:00` 升成 A 类基线，注释写着「timezone 空＝本机钟（桌面机就是运营者时区）」——这个假设在客户机上是错的，而基线机制把错误假设复制到了每一台升级机。** 三层：

1. **时区归属错**：`work_hours_gate._resolve_tz("")` 回落服务器本机钟。运营者在上海、客户在纽约 / 伦敦：上海 01:00–08:00 ＝ 纽约 13:00–20:00 / 伦敦 18:00–01:00——正是客户最活跃的下午和晚上。KYHGSZ：Mike 01:46（上海）来信、01:47 稿已生成 `risk=low`，被 worker 班表闸**静默**留 pending 到 08:00+，无一行日志（worker 扣留分支只 `total_skipped_off_hours++`）。
2. **机制放大**：`_ensure_baseline` 的三态语义（缺键才补）本身没错，但它只回答「用户表过态没有」，不回答「这个默认在客户机上对不对」。skuio 机原本**没有** work_schedule 块（§四 D④ 当时已写「落待老板知会」）→ 首启补成上海时区班表；额度闸门 `companion_send_gate` 也走种子显式 `enabled:true 300/100/3`，各号折算 114–129 条，且 `gate_decision` 对 `origin=manual` 仍比 `recommended_cap` → 人工也拦（CSTCJT「自动 ok / 人工 ok」两列）。
3. **决策层缺红线**：拆条（D-L3 `82BF95`）已经吃过一次「会改变发送行为的开关默认开」的亏并降 B，但没有沉淀成机器可判的规则，两天后班表 / 额度又以 A 类进了基线。

**修法 = 三条红线钉进 `feature_registry`（docstring + `_validate` fail-fast + 测试），班表 / 额度降 B 出厂关，存量只回滚「基线原样形态」，并给升级加回读日志。** 见下表。

## 基线三条红线（写进 `src/utils/feature_registry.py` 模块 docstring，`_validate` + `tests/test_baseline_redlines_q4.py` 钉住）

| # | 红线 | 机器判定 |
|---|---|---|
| ① | **只补缺席键、绝不覆盖显式值** | `baseline_patch` 判据 `dig()==None`；`ConfigManager.load()` 全部迁移后回读 overlay，升级前存在的叶子逐字相等，否则 WARNING `[upgrade] user_flags_preserved=N changed=[…]` |
| ② | **会改变发送行为的开关不得 `baseline=True`**（班表 / 额度 / 拆条 / 关怀真发 / 全自动） | `SEND_BEHAVIOR_KEY_PREFIXES` + `is_send_behavior_key()`；A 类命中 → `ValueError("红线②…")` 进程起不来 |
| ③ | **时区类键不得默认服务器本机** | `is_timezone_key()`：A 类时区键拒；任何时区键 baseline ∈ {"", local, server, system} 拒 |

存量机器：`ConfigManager.baseline_rollback_patch()` 按 1.0.78 **原样形态**（`{enabled:true, default:{08:20,01:00}}` / `{enabled:true,300,100,3}`，且无回滚标记）识别「由基线补进去的」，命中才写 `enabled:false + baseline_rollback:"1.0.79"` 并落 `[baseline] rollback <key> reason=D-Q1|D-Q2`；用户填了时区 / 改了班次 / 显式关 / 加账号覆写 / 改了额度 → 一字不动。

## 一、根因（每张单一句）

| 单 / 报告 | 症状 | 根因 |
|---|---|---|
| #267 KYHGSZ / 7P24KN | 客户下午来信被静默扣到上海 08:00；复班回顾窗 2h 不补 | 班表 A 类基线 + 时区空＝服务器本机（§〇）；worker 扣留无日志；`catch_up_regenerate_hours=2` 只重拟 >2h 陈稿 |
| #267 CSTCJT | 额度「300 · 安装包默认」运行中，各号 114–129，人工也拦 | 种子 `companion_send_gate.enabled:true`；`gate_decision` manual 仍比完整 cap；文案把「推荐」写成「默认」 |
| #265 65UQRE | 升级后 `[care_dispatcher] 首拍 enabled=False`；verbatim 关怀 01:20/02:20 被顺延到同一分钟 08:00；「出手」/ 时区不明 | 关怀开关：**config 层复现不出**（见 §四 C）；顺延：`shift_out_of_quiet_hours` 对 verbatim 人工时间无豁免、无错峰、按本机时区 |
| #267 HEEEN9 | 档位页三层关系读不懂；「新会话自动沿用」不知所云 | 文案层 |

## 二、落点（每 commit 一行，落地即写）

| 项 | commit | 落点 |
|---|---|---|
| **A/B/E 核心** 三条红线 + 班表·额度 A→B 出厂关 + 存量回滚 + 升级回读 + 人工永不限额 + profile_llm 进基线 + 启动横幅（#267 #265，D-Q1 D-Q2） | `c860436e` | `src/utils/feature_registry.py`：docstring 追加「基线三条红线」段（§〇 事故 + ①②③ + 存量语义）；新常量 `SEND_BEHAVIOR_KEY_PREFIXES`（work_schedule / companion_send_gate / bubbles / proactive_care / proactive_topic / l2_autosend.deliver / automation_mode / bootstrap_automation_mode）、`TIMEZONE_KEY_MARKERS`、`ROLLED_BACK_BASELINES`（两键的 1.0.78 原样形态 + 标记键）、`WORK_SCHEDULE_SUGGESTED_SHIFT{08:20,01:00}`（建议值≠基线）；`is_send_behavior_key / is_timezone_key`；`_validate` 加红线②③ fail-fast。**表变更**：`inbox.work_schedule.enabled` A→**B**（无 baseline）；`inbox.work_schedule.default.start/end` **出表**（建议值走常量）；新增 B `companion_send_gate.enabled`（D-Q2）；新增 A `companion.goals.profile_llm.enabled=True`（Q-5 代，只多学不多发，红线②不适用）；`memory.extract.use_llm` 已是 A/True 复核不动。`src/utils/config_manager.py`：`load()` 开头 `_overlay_leaves()` 快照 → 迁移链末新增 `_rollback_baselines_1079()`（桌面态；`baseline_rollback_patch()` 纯读判定，整块与 1.0.78 形态逐字相等且无标记才命中；写 overlay `enabled:false + baseline_rollback:"1.0.79"`；WARNING `[baseline] rollback <key> reason=D-Qn old→new`；幂等）→ `_log_upgrade_readback()`（INFO `[upgrade] user_flags_preserved=N changed=[] rolled_back=[…]`，changed 非空升 WARNING 并逐键打 before→after）；`load()` 成功尾 `log_factory_defaults_banner()` 一行 `[baseline] work_schedule.enabled= tz= \| send_gate.enabled= target_cap= auto_cap/cap per account: … \| goals.profile_llm= memory.extract.use_llm=`（额度开着时经 `send_gate_today` 只读 DB 列各号折算）。`src/skills/companion_send_gate.py` `gate_decision`：**`origin=manual` 恒放行**（banned 仍拦——号已封是物理事实；health_red 对人工也放：风控信号下坐席看着发是人的决定）；`reserve_for_manual` 语义保留给 auto_cap。`src/inbox/send_gate_today.py`：`manual_verdict` 开闸时恒 `ok`；每行新增 `auto_pct`（AI 已用 / auto_cap %）+ `warn80`（≥80）供卡片进度条。`config/config.desktop.min.yaml`：`inbox.work_schedule.enabled: false`（start/end 保留为建议值，注释改 D-Q1）、`companion_send_gate.enabled: false`（300/100/3 保留为推荐值，注释改 D-Q2）、`companion.goals.profile_llm.enabled: true`。测试：**新** `tests/test_baseline_redlines_q4.py` 14 例——1.0.77 数据目录 + 用户显式 `proactive_care.enabled=true` 升到当前代码所有显式叶子逐字不变且 `changed=[]`（65UQRE 复现基线）/ 回读能红 / `baseline_patch` 对全部 A 类显式非基线值零 patch / 红线②名单覆盖五类且不误伤 natural·inbound_merge / `_validate` 拒 A 类班表 / 红线③拒 A 类时区键与 local 哨兵 / 种子三键状态 / 1.0.78 升级机形态两键回滚 + 标记 + 日志 + 幂等 / 四种用户改过的班表形态零触碰（parametrize）/ 服务器实例不跑 / 纯函数 patch 形态 + 标记止步 / 启动横幅 / manual 用尽仍放行。`tests/test_feature_registry.py` 期望集：去班表三键、加 profile_llm、断言 B 类；`tests/test_pacing_profiles.py` 种子班表两例改为「出厂关、显式开 + 显式时区再验节奏」；`tests/test_send_gate_today.py` manual→ok + auto_pct；`tests/test_seed_switch_upgrade_coverage.py` `_EXEMPT` 去 `companion_send_gate.enabled`（种子已 false）、补 `bubbles.explicit_newline_only`（L-1 遗留孤儿：代码默认已 True，`reply_split.DEFAULT_EXPLICIT_NEWLINE_ONLY`）。`docs/功能矩阵_ChatX.md` 重导出无 diff（三键均 show=False）。装载：`.py` / 种子 **需重启**（回滚与回读在 `load()` 跑）。 |
| **A/B/G 页面** 班表时区必填 + 换算核对 / 额度卡去人工列 + 进度条 80% / 「300 · 推荐」+ 依据 / 档位卡三层文案（#267，D-Q1 D-Q2 CSTCJT HEEEN9） | `938fd1fb` | `src/inbox/reply_pacing_settings.py`：`cross_validate` 新增 `_validate_schedule_timezone`——合并视图 `enabled` 真 ∧ `timezone` 空 → `{"field": inbox.work_schedule.timezone, "code": "tz_required"}`；只在触碰 enabled/timezone 时校验（存量「已开 + 空时区」不拦无关保存，由启动横幅 `tz=''` 露出）。`src/web/routes/reply_settings_routes.py`：错误码表加 `tz_required → rps_err_tz_required`。`src/web/templates/reply_settings.html`：**A** 班表卡——开关 hint 改「出厂关（1.0.79 起）…必须填班表时区…留空不能保存」；时区行标签「（开启班表时必填）」、placeholder `America/New_York` + datalist 九个常用 IANA、三选一快捷「账号在上海 / 客户在纽约 / 客户在伦敦 / 本机时区」(`rpsWsTzSet`)、hint 删「留空 = 服务器本地时间」并写明 1.0.78 事故；新增换算核对行 `#rps-ws-tzconv`（`rpsWsTzConv`/`rpsWsConvHHMM`，浏览器 Intl 按今日偏移算，例「上海 01:00 ＝ 纽约 13:00(-1) · 伦敦 18:00(-1)」，跨日带 ±1；开关/时区/上下班/预设变更与回填后都刷新；开着且空时区红字「⚠ 未填时区：开启班表后不能保存」；非法时区名红字）；`rpsSave` 前置：开关勾 ∧ 时区空 ∧ 本次动了 enabled/timezone → toast `rps_err_tz_required` + 聚焦时区框不发请求。**B** 额度卡——表头三列「账号 / AI 已发·上限 / AI 额度进度」（**去「人工」列**），`rpsSgRow` 改渲染进度条 `.rps-sg-bar`（`rpsSgPct` 优先后端 `auto_pct`，无则本地算；≥80% 橙 `.warn` +「已用 N%，接近上限」，≥100% 或 verdict=BLOCK 红 +「AI 已达今日额度，请手动接管」）；快捷键「300 · 推荐」；`rps_sg_cap_hint` 重写含依据句「新号按起步额度在爬坡天数内线性爬到 300（出厂 100 → 300 / 3 天）…此额度只管 AI 自动发送，坐席人工发送永不受限」；表下一句 `rps_sg_manual_note`；`rps_sg_today`→「本号今日 AI 已发」。**G** 档位卡——标题「未设置账号的兜底档位」+ 标题旁 chip `#rps-mode-tiers`「N 个账号已设全自动 / M 个跟随本页」（`rpsModeTiers` 由 `/api/reply-settings/account-modes` 的 decided(mode=auto_ai)/pending 数出，`rpsAmRender` 顺手刷）；hint 重写为三层优先级大白话 +「本页即使选全自动，账号未在账号菜单确认前仍按拟稿人审落地」；一行三层图 `会话手选 ▶ 账号设定 ▶ 本页兜底 ｜ 前面有值就不再往后看`；全自动卡描述追加「高风险（危机 / 投诉 / 金额 / 政策类）转人工审核」；「新会话自动沿用此档位」→ 折进 `<details class="rps-guard-adv">`「高级：把兜底档位写死到新会话」，标签改「新会话第一条消息到时，把当时的兜底档位写死进这个会话」，hint「出厂关。关着：…立刻跟着变。开着：…只在希望老会话锁住当时档位时才开」（出厂关＝两份桌面种子 `bootstrap_automation_mode: false`，1.0.76 D-M1 已落；代码缺键回落 `automation_mode.bootstrap_enabled_from_config` 未改——非桌面形态语义不动）。`src/web/i18n_packs/reply_settings_page.py`：zh/en 各 +20 键（`rps_err_tz_required`、`rps_ws_tz_pick*`、`rps_ws_js_tz_*`、`rps_ws_js_city_*`、`rps_sg_manual_note`、`rps_sg_js_exhausted/warn80`、`rps_mode_tiers*`、`rps_mode_adv`）+ 12 键改文案（zh_hant 由 `scripts.i18n_hant generate` 下次 regen 补，缺键回落简体）。测试：**新** `tests/test_reply_settings_ui_q4.py` 13 例（新键 zh/en 齐且 EN 零 CJK / 占位符守恒 / 模板与词包不再有「留空 = 服务器本地时间」/ 换算行与三选一与触发接线 / `rpsSave` 前置校验 / 错误码映射 / 人工列已撤且 `rpsSgRow` 三格无 `manual_verdict` / 进度条 80·100 / 「推荐」+ 依据 + 永不限人工 / 三层文案与 chip / bootstrap 折进高级 + 大白话 / 两份种子 bootstrap=false）；`tests/test_reply_settings.py` 端到端开班表改为带时区 + **新** `test_post_enable_without_timezone_rejected`（tz_required、不落盘、message 无 `{field}`）+ `test_timezone_required_rule`（六种合并视图）。门禁：`test_i18n_coverage` reply_settings 裸 CJK 基线 0 保持（JS 回落不写中文）；模板渲染后 `node --check` 通过；`rpsWsConvHHMM` node 实测 上海 01:00→纽约 13:00(-1)、纽约 13:00→上海 01:00(+1)、伦敦 09:00→上海 16:00。装载：模板/词包**热更**；`reply_pacing_settings.py`/routes **需重启**（重启前旧后端不拦空时区，前端已拦）。 |
| **A 运行时** off_hours 拦截落日志 + 会话标签「作息外 · 到点重新拟稿」+ 过夜积压默认全部作废重写（#267，D-Q1） | `7d6be0ad` | `src/inbox/work_hours_gate.py`：docstring 配置块改「enabled 出厂关 / timezone 开启必填 / regen 0=全部重写」；`off_hours_cfg` 默认 `catch_up_regenerate_hours=0`、坏值按 0，新增 `catch_up_regenerate_all`（≤0）；**新** `catch_up_regen_due(oh, draft_ts, now_ts, shift_started_ts)`——0 档按「稿拟于本班次开始前」判（无班次锚点不作废：判不出宁发陈稿不空转），N>0 档按稿龄，catch_up 关恒 False（**worker 接线归 Q-3**，见 §四）；**新** `off_hours_hold_info(ws, plat, acct)`（`schedule_state` 同口径：gated ∧ !in_hours → `{until_hhmm, until_ts, tz, now_local}`，until 按班表时区墙钟）；**新** `log_off_hours_hold(conv, ws, plat, acct)` → INFO `[work_schedule] hold=off_hours conv=<cid> until=<hh:mm> tz=<tz>`，同会话同 until 只打一次（进程内 dict，5000 上限轮换）；**新** 标签常量 `OFF_HOURS_HOLD_TAG_KEY="inbox.conv.off_hours_hold"` / `OFF_HOURS_HOLD_TAG_ZH` + `off_hours_hold_tag_labels()`（遍历 web_i18n 全语种取词）+ `strip_off_hours_hold_tags()`。`src/inbox/autodraft_helpers.py`：休息期路径重排——先算 `_ws_hold`，`generate_drafts=false` 仍走「不拟稿」return，否则 `log_off_hours_hold(...)`（稿照拟、worker 会扣住；日志在拟稿侧打，worker 零改动）。`src/inbox/store.py`：**新** `InboxStore.conversations_with_pending_drafts(cids) -> set`（单次 IN，400 一批，异常空集）。`src/web/routes/unified_inbox_read_routes.py` `_enrich_chat_list`：tags 段后新增读侧计算——班表开 → 按 (platform, account) 记忆 `off_hours_hold_info`，命中账号的行再一次 IN 查 pending 稿，有稿的行挂 `off_hours_hold{until_hhmm,until_ts,tz}` 并把 `tr(request, OFF_HOURS_HOLD_TAG_KEY)` 追加进 `conv_tags`（**不落库**；现有 `tagChips` 渲染即可见，`unified_inbox.html` 零改动——该文件工作区被别线整文件改写，Q-4 不碰）；班表关（出厂）整段零开销。`src/web/routes/unified_inbox_workspace_tags_routes.py` PUT tags、`unified_inbox_batch_notif_routes.py` 批量 set/add：入参先 `strip_off_hours_hold_tags`（防前端整表回写把计算标签钉进库）。**新** `src/web/i18n_packs/work_schedule_q4.py`：`inbox.conv.off_hours_hold` zh「作息外 · 到点重新拟稿」/ en "Off hours · redrafts at shift start" + `_t` 悬停句（`{until}` `{tz}`；unified_inbox 前端接 tooltip 留给页面归属线）。`src/inbox/reply_pacing_settings.py` FIELDS `catch_up_regenerate_hours` default 2→0；`i18n_packs/reply_settings_page.py` `rps_ws_regenh`「重拟阈值（小时，0＝全部重拟）」+ `rps_ws_catchup_hint` 重写（0 出厂＝过夜积压全部作废按早上上下文重拟；N>0 只重拟超龄）zh/en。测试：**新** `tests/test_work_hours_hold_q4.py` 11 例（默认 0/all、负数坏值归 0；regen_due 三态 + 无锚点不作废 + 阈值档 + catch_up 关；hold_info 在班 None / 关闭 None / 纽约 03:00 → until=09:00 tz=NY，同 epoch 填上海则在班 None（红线③价值）；日志格式全匹配 + 同会话同到点去重 + 在班不打；zh/en 词条 + strip 任意语言；写标签两路由接线；enrich 接线且无 set_conv_tags；autodraft 先「不拟稿」再 hold 日志；store pending 集合 upsert→sent 变化）；`tests/test_work_hours_gate.py` `off_hours_cfg` 期望改 0/all。共享文件纪律：`unified_inbox_read_routes.py` 工作区另有别线两处未提交改动（L595 / L1536），本 commit 用 `git apply --cached` 只入我方 `_enrich_chat_list` 一个 hunk（staged 版 ast 校验通过）。装载：`.py` **需重启**；`config.example.yaml` 内 `catch_up_regenerate_hours: 2 # 0=不重拟直接发` 注释已过期但该文件被别线占用，R79 前由 Q-0 顺手改（§四）。 |
| **D 关怀** verbatim 人工时刻默认照发 + 排期落安静时段弹「仍按 01:20 发 / 顺延到 08:00」+ 顺延多条错峰 ≥5 min + 时区按客户→人设→账号 + 关怀页安静时段卡（区间 / 开关 / 时区依据）+「出手」→「发送」+ 试运行提示只在 dry_run（#265 65UQRE） | `a870e8ff` | `src/contacts/care_schedule.py`：**新** `VERBATIM_QUIET_DEFER_SUFFIX=":defer_quiet"` / `QUIET_POLICIES` / `verbatim_quiet_policy(item)`（非 verbatim `""`；topic_norm 带后缀 → `defer`，否则 `keep`）；`add_verbatim(..., quiet_policy="keep")` 选 defer 时在 `verbatim:<sha8>` 后追加后缀（≤32 列宽，**无新列零迁移**，`is_verbatim_care` 只看前缀不受影响）。`src/contacts/care_dispatcher.py`：**新** `TzFallbackProvider`、`QUIET_STAGGER_SLOT_SEC=300 / MAX=2400`、`quiet_stagger_offset(n)`（n·5 min 至 40 min，之后随机 0–40）；`__init__` 加 `tz_fallback_provider`；**新** `_resolve_clock(item) -> (clock, basis)`（客户 `user_clock_provider` 命中 → `customer`；否则回落 provider 给的 `{tz_name, basis}` 构 `UserClock(trust="replace")` → `persona` / `account`；皆无 → `(None,"")`＝服务器钟）；**新** `set_quiet_window(s,e)`、`quiet_window_info(item)`（`{start_hour,end_hour,enabled,tz_basis,tz_name,in_quiet_now}`）、`quiet_check_for_due(item, due_at)`（`{in_quiet,due_local,shifted_at,shifted_local,tz_name,tz_basis}` 按该行时钟算墙钟）、`_stagger_after_quiet(ts)`（同一「顺延到」分钟第 n 条加 `quiet_stagger_offset(n)`）；`_dispatch_one`：`_vq=verbatim_quiet_policy(item)`，`keep`（**verbatim 默认**）或 goal `ignore_quiet` → **不顺延** `defer_why=none`；`defer` → 顺延后再错峰；`_send` 的 `extra` 多带 `quiet_policy` + `ignore_quiet`（manual 立即发同样 True）；`projected_send_window` 同口径（keep 不 shift；defer 上界 +40 min；返回多两键 `quiet_policy` / `tz_basis`）；`deliver_text` 改用 `_resolve_clock`。`src/integrations/shared/deferred_outbox.py` 第 3 步：`extra.ignore_quiet` 真 → **跳过服务器钟二次顺延**（否则 01:20 照发的行到队列又被推到 08:00——原实现必然二次顺延，故这一行是验收「01:20 按时发」的必要条件）。`src/bootstrap/background_tasks.py`：**新** `_care_tz_fallback(item)`（`resolve_effective_persona_id` → `PersonaManager.get_persona_by_id` → `resolve_place_with_fallback(p).tz_name` → basis `persona`；否则 `work_hours_gate.resolve_entry(ws, plat, acct)["tz_name"]` → `account`；皆无 `{}`），注入 `CareDispatcher(tz_fallback_provider=…)`。`src/web/routes/care_routes.py`：`POST /api/care/schedule` verbatim 分支接 `quiet_policy`（`keep|defer`，非法 `bad_quiet_policy`）；未带策略 → `dispatcher.quiet_check_for_due(...)`，落安静时段 → **不入队**回 `{ok:false, reason:"in_quiet_hours", due_local, shifted_local, shifted_at, tz_name, tz_basis, message}`；入队日志加 `quiet_policy=`；`/api/care/plan` 响应新增顶层 `quiet`（窗口 + 按首条待办算的 `tz_basis/tz_name/in_quiet_now`）；**新** `POST /api/care/quiet {enabled, start_hour?, end_hour?}`（关＝写 `quiet_start_hour=quiet_end_hour=8`；开＝给定或 23→8；写 `companion.proactive_care.quiet_*_hour` overlay + 派发器 `set_quiet_window` 即时生效 + 审计行 `quiet_window:s-e`）；模块导入 `web_i18n.tr`。`src/web/templates/care_schedule.html`：关怀方案标题下**新卡 `#cs-quiet`**（`_csRenderQuiet`：「安静时段 · 23:00–08:00 不主动打扰客户」/「已关闭：全天可发」+ 此刻在安静时段徽标 + 「时区依据：客户所在地 / 人设所在地 / 账号班表时区 / 服务器本地时间」+ 一句说明 + 开/关按钮 `csQuietToggle`；plan 降级路径无 `quiet` 则隐藏）；`_csAddSubmit(mode, confirmed, quietPolicy)` 收到 `in_quiet_hours` → `_csQuietChoiceShow`（预览面板复用：「到期时刻落在安静时段 · 按 服务器本地时间，01:20 正处于 23:00–08:00」+ 按钮「仍按 01:20 发」/「顺延到 08:00」/ 取消 + 「多条同顺延会自动错开 5–40 分钟」），选完带 `quiet_policy` 重发同一 payload，defer 成功语 `cs6_add_ok_verbatim_defer`；`_csRenderSteward` 过滤 `waiting_due` 建议——`eng.dry_run` 假时不显「到期会先拟稿给你过目」（后端 `care_advisor` 本就只在 dry_run 产出，前端再钉一道）。`src/web/i18n_packs/care_page.py`：zh `cs8_fwd_done / cs8_eta / cs8_eta_t / cs8_state_forwarded`「出手」→「发送」（`cs8_eta_t` 加「人工指定的时刻默认照发（排期时若落在安静时段会先问你一次）」，en 同步）；zh/en 各 +21 键（`cs6_add_ok_verbatim_defer`、`cs_quiet_choice_msg`、`cs_quiet_title/range/off/toggle_on/toggle_off/basis/basis_{customer,persona,account,server}/verbatim_note/choice_title/choice_sub/keep/defer/defer_note/cancel/save_fail/now`）。`tests/test_admin_route_inventory.py` 基线 +`/api/care/quiet POST`（该文件工作区另有别线未提交 hunk，本 commit `git apply --cached` 只入我方一段）；`tests/test_care_send_now_243.py::test_projected_send_window_matches_dispatch_rules` 由整 dict 相等改为四原键相等 + `quiet_policy=="keep"`。测试：**新** `tests/test_care_quiet_verbatim_q4.py` 13 例——store 后缀 / 缺省 keep；**01:20 verbatim keep → `defer_until==now` 且 `extra.ignore_quiet`**；defer → 当日 08:00；**两条同顺延间隔 ∈ [5, 40] min**；stagger 槽位 / 随机上限；`quiet_check_for_due` 01:20→「01:20 / 08:00」、14:00 不问；`quiet_window_info` + `set_quiet_window` 非法保持；`_resolve_clock` 客户 → 人设 → 服务器三态；`projected_send_window` keep 不 shift / defer shift；路由：未带策略 → `in_quiet_hours` 不入队 + message 含两时刻 → keep/defer 各入队并可判 → 非法拒 → 白天直接入队；`/api/care/plan.quiet` + `/api/care/quiet` 关/开写 overlay 两键 + 派发器即时生效 + `bad_hours`；模板/词包契约 +「出手」零残留；**deferred 队列 01:20 drain：`ignore_quiet` 行真发、自动行仍 `quiet_hours` 顺延**。回归：`tests/test_care_*`、`test_deferred_outbox` 74 例通过；care_schedule.html 抽 JS `node --check` 通过；`test_i18n_coverage -k care_schedule` 4 例通过。**边界**：Messenger 走 RPA 队列有自己的账号级 `safety.quiet_hours`（`messenger_rpa/runner.py`），是平台安全闸不是关怀顺延，本次不动、不豁免；`deliver_text`（人工改稿点发出）仍按原口径顺延（面板文案本就写着）。装载：模板/词包**热更**；`.py` **需重启**（时区回落 provider 在 `background_tasks` 起 dispatcher 时注入）。 |
| **F 弹窗** `release_defaults_changed.json` + 升级后首开工作台「这版改变了什么」弹一次（每行「保持 / 改回」+ 班表时区确认，选完写显式值）+ 三语 + 随包（#267 F） | `3fab55c1` | **新** `config/release_defaults_changed.json`（schema 1；`releases.1.0.79.changes` 四行：`inbox.work_schedule.enabled` true→false `show_if=marker needs_timezone`、`companion_send_gate.enabled` true→false `marker`、`inbox.work_schedule.off_hours.catch_up_regenerate_hours` 2→0 `kind=value show_if=missing`、`companion.goals.profile_llm.enabled` false→true `always`；`_doc` 内写维护五步）。**新** `src/utils/release_defaults.py`：`load_release_defaults / release_changes`（字段归一化，marker 行默认 show_if=marker）、状态文件 `<config_dir>/release_notice_state.json {seen{v:{ts,reason}}, last_version}`（`read_state / mark_seen`）、**升级机判定** `is_upgrade_install`＝`last_version≠本版` ∨ 任一行 marker 在 overlay（**overlay 非空不能当升级信号**：`_ensure_baseline` / 向导 / secret_key 在 clean 装首启就写叶子——这是设计上最容易犯的错，测试 `test_clean_install_never_pops_and_marks_seen` 钉住）、`pending_rows`（`marker`：本机被回滚过才弹；`missing`：用户 overlay 已显式写过则不弹；`always`：升级机一律弹）、`pending_notice(cm, version)`（dev / 未登记版本不弹；已 seen 不弹；clean 装 / 无行 → 静默 mark_seen）、`apply_choices(cm, v, choices, timezone)`（每行 `keep`＝写**显式**新默认 / `flip`＝写旧值；未列出的行按 keep——关闭弹窗即接受新默认且从此是用户显式值；班表 flip 必须带合法 IANA 时区，`local/server/system/空` 一律 `tz_required`（红线③），失败行不 mark_seen 下次再弹；全部走 `ConfigManager.set_overlay_flag`＝与页面同一写入面）。**新** `src/web/routes/release_notice_routes.py`：`GET /api/release-notice`（`?v=` 可覆盖版本供联调；会话角色非 master/operator → pending=false；返回 rows 已本地化 `title/desc` + `labels`）、`POST /api/release-notice/apply {version, choices, timezone}`、`POST /api/release-notice/dismiss`（＝全 keep）；全部 try/except 永不 500。`src/web/admin.py` care 路由块后注册（该文件工作区另有别线 4 处未提交 hunk 且正被 stage，本 commit `git apply --cached` 只入我方一段）。**新** `src/web/static/workspace/release-notice.js`（自带 CSS 的模态：标题 / 说明 / 每行 title+desc+「之前：开 · 现在：关」+ radio「保持关闭 / 开启」；班表行选开启才展开时区框（datalist 九个常用 IANA，`Intl.DateTimeFormat` 前端先校验）；value 行只展示 `2 → 0`；「确定」提交 choices，「全部保持新默认」/ × ＝全 keep；**文件零语种字面量**，文案全来自 API labels；`data-ws-embed=1` 内嵌不装；GET 失败 / 不 pending 零痕迹）。`src/web/templates/workspace_base.html` 尾部 `{% if user_role in ('master','operator') %}<script src=/static/workspace/release-notice.js defer>`（`unified_inbox.html` 继承它，Q-4 不碰该文件）。**新** `src/web/i18n_packs/release_notice.py`：`ZH / EN / ZH_HANT` 三语同键集（21 通用键 + 每行 `<i18n>_title/_desc` 8 键；zh_hant 直接在 pack 内维护不等 regen）。随包：`desktop/build/build_backend.py` DATAS + `after-pack.js` REQUIRED + `refresh_backend_datas.py` config 区增量名单 + `desktop/test/package-layout.test.js` MUST_SHIP 各加 `release_defaults_changed.json`（`node test/package-layout.test.js` 81 passed；本机 backend-dist 陈旧只 WARN）。`tests/test_admin_route_inventory.py` 基线 +3 端点。测试：**新** `tests/test_release_notice_q4.py` 12 例——数据文件四行形态 + 与 `ROLLED_BACK_BASELINES` marker/old/new 同源；缺失 / 损坏安全；三语同键集 + EN 零 CJK；**1.0.78 升级机（真跑 `ConfigManager.load()` 产生 marker）首启 pending=upgrade 四行 → 全 keep 写显式 false/false/true + 状态文件 → 第二次不弹**；flip 无时区 `tz_required`（其余行照写、不 seen、还会再弹）→ 四种哨兵/非法时区拒 → `America/New_York` 通过且 overlay + 内存视图都开；value 行 flip 拒 `not_a_switch`；**clean 装 overlay 非空仍不弹并静默 seen**，其后模拟 1.0.80 由 `last_version` 判升级；用户自己配过（无 marker）两行不弹且整体不判升级；dev / 未登记版本不弹不落状态文件；路由 GET/apply/dismiss（`AITR_APP_VERSION` 注入、本地化 message）；dev 版路由静默；模板装载点 master/operator 守卫 + JS 零 CJK + 契约字符串；随包四处登记。装载：`.py`/模板/静态/词包**需重启**（路由注册在 create_app）；升级首开 = 装完 1.0.79 后第一次以 master/operator 打开任一工作台页。 |

## 三、验收对照

| 验收项 | 状态 |
|---|---|
| clean 安装 → 班表关、额度闸门关 | 种子两键 false（`test_seed_has_work_schedule_and_send_gate_off_and_profile_llm_on`） |
| 1.0.78 升级机 → 基线补进去的班表 / 额度回滚 + 日志；用户改过的不动 | `test_rollback_baseline_inserted_*` / `test_rollback_never_touches_user_configured_*` |
| 升级不得改变用户显式值 + `[upgrade]` 回读 | `test_upgrade_1077_to_1079_preserves_every_explicit_user_value` |
| 人工发送在额度用尽后仍成功 | `test_send_gate_manual_never_blocked_by_daily_cap` |
| 开班表未选时区不能保存 / 换算提示 | ✅ commit 2：后端 `tz_required` + 前端 `rpsSave` 前置 + 换算行「上海 01:00 ＝ 纽约 13:00(-1)」 |
| 夜间入站 → 日志 + 会话标签 | ✅ commit 3：拟稿侧 `[work_schedule] hold=off_hours conv=… until=hh:mm tz=…`；列表行 `conv_tags` 追加「作息外 · 到点重新拟稿」（读侧计算不落库）；过夜稿作废重写的 worker 接线一行归 Q-3（§四，`catch_up_regen_due` 已备好） |
| 自动达 80% 有提示 / 「300 · 推荐」 | ✅ commit 2：进度条 ≥80% 橙「已用 N%，接近上限」、≥100%「AI 已达今日额度，请手动接管」；「300 · 推荐」+ 依据句；人工列已撤 |
| 关怀 verbatim 人工时间 / 顺延错峰 / 时区 / 页面可见 | ✅ commit 4：01:20 verbatim → `defer_until==now`（派发器不顺延 + 队列 `ignore_quiet` 不二次顺延，`test_verbatim_keep_dispatches_on_time_in_quiet_hours` / `test_deferred_outbox_respects_ignore_quiet_extra`）；两条同顺延间隔 ≥5 min（`test_two_deferred_rows_are_staggered_at_least_5min`）；排期落安静时段弹「仍按 01:20 发 / 顺延到 08:00」（`in_quiet_hours` 路由 + `_csQuietChoiceShow`）；时区客户→人设→账号→服务器（`_resolve_clock` + `_care_tz_fallback`）；关怀页 `#cs-quiet` 卡显示区间 + 开关 + 时区依据；「出手」→「发送」；「到期会先拟稿给你过目」只在 dry_run |
| 「这版改变了什么」弹窗 + `release_defaults_changed.json` | ✅ commit 5：`test_upgrade_1078_to_1079_first_open_pending_then_once`（真跑 load() 的 1.0.78 机器首启弹四行、选择落 overlay 显式值、同版只弹一次）+ `test_upgrade_choice_flip_writes_old_value_and_requires_timezone`（开启班表必带时区）+ `test_clean_install_never_pops_and_marks_seen` |
| 档位页三层文案 | ✅ commit 2：标题「未设置账号的兜底档位」+「N 个账号已设全自动 / M 个跟随本页」+ 一行三层图 + 全自动卡「高风险转人工审核」+ bootstrap 折进高级改大白话 |

## 四、越界 / 交接 / 待老板知会

- **C 关怀开关被升级翻回 False（65UQRE）——config 层复现不出。** 用 1.0.77 种子 config.yaml + overlay 显式 `companion.proactive_care.enabled: true / dry_run: false` 跑当前 `ConfigManager.load()`（桌面态、含 `_ensure_baseline` / 两个迁移 / 本次回滚）：合并视图与 overlay 文件里该值**原样 True**（`test_upgrade_1077_to_1079_preserves_every_explicit_user_value` 钉住）。三个嫌疑逐一核：① 基线写到更高优先级层——否，`_ensure_baseline` 与 `set_overlay_flag`（care 页 `/api/care/engine`）写的是**同一个** `config.local.yaml`；② 页面持久化写在升级会替换的文件——否，desktop `backend-launcher.js` 只注入 `AITR_CONFIG_PATH=<dataDir>/config/config.yaml`，安装器不碰 dataDir；③ 迁移脚本覆盖——否，`_migrate_bubbles_1075` / `_migrate_login_default_semi_1076` 只写 `inbox.*`。报告只带升级后 70 分钟日志（23:16 起），**9/7 15:27「已设 True」这一笔无日志证据**。已做的兜底：`[upgrade] user_flags_preserved` 回读——下次再发生，日志会直接列出哪个键从什么变成什么。**交 Q-0 回访**：请用户提供 9/7 15:00–16:00 的 backend.log（找 `运营开关已更新` / `配置热重载完成` 行）与当前 `config.local.yaml` 的 `companion:` 段；若 15:27 那笔实际走的是关怀页「启用（灰度）」→ 那是 `enable_dry`＝`dry_run=true`，与「dry_run=False」的陈述不符，需要核对操作。
- **Q-3（autosend_worker.py）需代加（commit 3 已把两个纯函数备好，worker 侧各一行）**：
  ① 班表扣留分支（L2230–2267 `if _ws_hold:` 命中处、`total_skipped_off_hours += 1` 之前）加 `from src.inbox.work_hours_gate import log_off_hours_hold; log_off_hours_hold(_conv, _ws_cfg_hold, _ws_plat, _ws_acct)`——同会话同到点自动去重，格式 `[work_schedule] hold=off_hours conv=… until=hh:mm tz=…`（拟稿侧已打一份，worker 这份是「真扣住」的证据，两处都有才闭环）。
  ② 复班补觉分支（L2343–2349）把 `_regen_h = float(_oh.get("catch_up_regenerate_hours") or 0)` … `and _regen_h > 0 and _draft_ts > 0 and _peer_txt and time.time() - _draft_ts > _regen_h * 3600.0` 换成 `from src.inbox.work_hours_gate import catch_up_regen_due, schedule_state; _shift = float(schedule_state(_ws_cfg, plat, acct)["shift_started_ts"] or 0); … and _peer_txt and catch_up_regen_due(_oh, _draft_ts, shift_started_ts=_shift)`。语义：默认 0 ＝ 稿拟于本班次开始前（过夜积压）**全部作废重写**；N>0 ＝ 只重拟超龄；`shift_started_ts` 拿不到时不作废（判不出宁发陈稿）。**gate 侧默认已是 0：worker 不改则「0 → `_regen_h > 0` 为假 → 一律不重拟」，与 1.0.78 的 2 小时档相比是退化，R79 前必须落。** 验收：`tests/test_autosend_work_schedule.py` L223 把 `catch_up_regenerate_hours: 2` 案例保留 + 新增 0 档「凌晨稿复班被作废、复班后新稿不动」两例。
- **Q-0 / R79 顺手**：`config/config.example.yaml` L3595 `catch_up_regenerate_hours: 2 # …0=不重拟直接发` 注释已过期（Q-4 期间该文件被别线占用未改）→ 改为 `0 # 0（默认）=过夜积压全部作废重写；N>0=只重拟稿龄超 N 小时`；`unified_inbox.html` 若要给「作息外」标签加悬停句，词条 `inbox.conv.off_hours_hold_t`（`{until}` `{tz}`）与行字段 `off_hours_hold{until_hhmm,tz}` 已就位，只差 `_tagChipHtml` 里一行 title。
