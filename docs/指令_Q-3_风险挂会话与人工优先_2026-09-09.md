# 指令 Q-3：风险挂会话 + 人工优先（needs_human 从标签变闸 · 真发前二次复检 · 切手动取消在途）

> 预算 **$160**。工单 **#264**（GTCQS9 / XBGPBN / VAE6CJ / 3D293E / 7KXNHB④；Q-0 拆单后以新单号为准）。目标版本 1.0.79（第一波）。
> 底座 `docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-3 行、接口约定②）。落点表 `docs/发版对账_v1.0.79_Q3.md`。

## 开工前必读

1. `docs/聊天核心能力_三视角方案_2026-09-09.md` §1 Q3、§2.1 第 2 / 3 条、§3 Q-3。
2. 证据 `tmp_diag\GTCQS9\ XBGPBN\ VAE6CJ\`：23:49:13 客户「Just need your address」→ L1 `shadow=privacy risk=high` → needs_human；23:55:42 客户新消息重新 `auto_generate_draft level=L2 shadow=- risk=low`（风险未继承）→ 23:55:43 过闸进 `[pacing] 35.1s` → 用户在窗内切本会话手动并打字 → 23:56:20 仍发出 → 23:56:23 needs_human 摘标 `by=agent_send`。
3. 代码事实（已核）：`src/inbox/autosend_worker.py` L1896–1915 「Sprint1 统一出站闸门」——显式降级（manual / review / multi_choice）跳过投递，**但它在节奏等待之前**；L1545 只有「延迟窗内客户插话，放弃本条投递」；O-1 D 的 `[pacing]`（read / think / type / composing）把决定发与真发拉开 30–60s；`protocol_autoreply.tag_needs_human` L807 / `clear_needs_human` L783 只是标签；`drafts.py` 风险只算在当前稿上（`keyword_risk_level`）；`autosend_policy.decide` 是发送决策单点。
4. 接口：你提供 `src/inbox/risk_hold.py`：`set(conv, reason, hit, ttl_h=24)` / `active(conv) -> reason|None` / `clear(conv, by)`；Q-2 命中 commitment 会调 `set`；O-1 A `stop_contact` 冻结也应登记一行（只登记，不改它的语义）。
5. `scripts\agent_probe.ps1` 看 ACTIVE：你是 Q 批**唯一**动 `autosend_worker.py` / `autosend_policy.py` 的线；Q-4 若需要 work_schedule 拦截日志写进 worker，会在总账写给你，你代加一行。切档路由 `unified_inbox_stored_read_routes.py` / `unified_inbox_aggregate.py` 有他人在途 hunk → stage_hunks。

## 红线

- 只动归属矩阵 Q-3 行；`drafts.py` 不动（Q-2 的）；`store.py` 不动（risk_hold 落独立小表或 conversation meta 既有接口；若必须建表，独立迁移 + 写总账）。
- 人工优先级 ≥ 客户：坐席任何动作（切档 / 打字 / 发送）都等同「客户插话」放弃在途稿。
- `git add <显式路径>`；每 commit 带 `#264`。

## A `risk_hold` 会话级风险持有

- 新 `src/inbox/risk_hold.py`（持久化：conversation meta 或独立表 `conv_risk_hold(conv, reason, hit, set_ts, ttl, cleared_ts, cleared_by)`）；命中 privacy / commitment / stop_contact 即 `set`；人工「摘标 / 解冻 / 我来回」→ `clear(by=agent)`；TTL 24h 自动过期但日志一行。
- `autosend_policy.decide`：`risk_hold.active(conv)` → 强制 L1（review）并继承 `shadow`（重新起草不得归零）；日志 `[policy] conv=… risk_hold=<reason> forced=L1`。

## B needs_human 从标签升级为闸

- `protocol_autoreply.tag_needs_human` 同时 `risk_hold.set(conv, "needs_human", …)`（若无更具体 reason）；needs_human 挂着期间 **禁 L2 自动发**（`decide` 里查）；`clear_needs_human` 同步 `risk_hold.clear`。「查看≠接管」口径（#45）不变：只有人工动作才清。

## C 节奏等待后、真发前二次复检

- `autosend_worker` 投递路径：`[pacing]` 等待结束 → **重读**：会话模式 / 账号模式 / `risk_hold.active` / needs_human / 坐席是否在打字或 60s 内发过 → 任一不满足 → 放弃并落 `[autosend] abort=mode_changed|agent_typing|agent_sent|risk_hold|needs_human draft=… conv=…`；把 L1896 的闸复用到这里（同一函数两处调用），不复制逻辑。

## D 切手动 / 坐席打字 / 发送 → 取消在途稿

- 切档路由（会话级 / 账号级）：目标档 ∈ {manual, review, multi_choice} → `cancel_inflight(conv|account)`：取消队列中 + 节奏等待中的全部 L2 稿，返回 `cancelled=N`，前端 toast「已取消 N 条待发 AI 消息」（i18n 三语）。
- 坐席打字信号：新端点 `POST /api/unified-inbox/agent-typing`（composer 输入节流 3s 一次）→ worker 视同插话放弃该会话在途稿；坐席发送成功 → 同样取消（`by=agent_send` 已有摘标，加取消）。
- 日志 `[inflight] cancel scope=conv|account n=… by=mode_switch|agent_typing|agent_send`。

## E 重新起草落 reason

- `auto_generate_draft` 触发源必须带 `reason=user_ignore|review_timeout|takeover_rearm|new_inbound|catchup_regen`（drafts.py 是 Q-2 的——你只在**调用侧**传 reason，日志在 worker / autoreply 侧落）。

## F 门禁 + tests

- 「切手动 1 秒内在途稿取消」用例（节奏等待中切档 → 不发）；「needs_human 期间新入站 → L1」；「risk_hold 继承 shadow」；「坐席打字 → 放弃」；回放 XBGPBN 时间线为回归用例。

## 收工

- 落点表 `docs/发版对账_v1.0.79_Q3.md`（§〇 「为什么切手动了还发」：闸在等待之前 + 只认客户插话 + 标签非闸 三段）；`docs/Q批_进度总账.md` 只改 Q-3 行（写清 `risk_hold` API 与 Q-2 / O-1 A 的联动）；`agent_probe.ps1 -Done`；回访文案交 Q-0 / R79。

## 开新对话粘贴

```
读 docs/指令_Q-3_风险挂会话与人工优先_2026-09-09.md，按它做 Q-3（#264：新 risk_hold 会话级风险持有 set/active/clear + autosend_policy 强制 L1 并继承 shadow / needs_human 从标签升级为闸禁 L2 / autosend_worker 节奏等待后真发前二次复检 abort=mode_changed|agent_typing|agent_sent|risk_hold|needs_human / 切手动·坐席打字·坐席发送取消在途稿 + toast「已取消 N 条」+ 新 agent-typing 端点 / 重新起草调用侧带 reason / 门禁「切手动 1 秒取消」）。预算 $160。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与接口约定②，再 scripts\agent_probe.ps1 -Intent 登记；你是 Q 批唯一动 autosend_worker.py / autosend_policy.py 的线；drafts.py / store.py 不动；切档路由有他人在途 hunk 用 tools/stage_hunks.py --mine。每个 commit 带 #264 并立刻补落点表 docs/发版对账_v1.0.79_Q3.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-3 行并 agent_probe -Done。
```
