# 发版对账 v1.0.78 — O-4 引用回复 MVP（#254 · 47KNBV / 3HNCJ7）

> 指令：`docs/指令_O-4_引用回复MVP_2026-09-08.md`；底座 `docs/修复指令_O批_并行_2026-09-08.md` D-O6。
> 证据：`tmp_diag\47KNBV\20260908-022412_report_report.md`（规则集全文）、`tmp_diag\3HNCJ7\20260908-023020_report_report.md`（要排期 + MVP 切法）。
> 预算 $120（MVP ≤$100 / 评估 ≤$20）；执行方 O-4 对话，09-08 08:2x 开工。目标版本 **1.0.78**。
> 基线：#37（1.0.74，I-4 D2）`src/inbox/reply_quote_policy.py` 纯函数 + `autosend_helpers._autosend_deliver` 接线，规则只有「未回复入站 ≥2 且回其中一条」、默认关。

## §0 一句话

一期做完：在 #37 之上补成**确定性规则引擎**（必引 burst/stale · 基线 random 10–15% 会话级种子 · 禁引 short_quick/streak/hourly_cap/self≤5% · 能力位 TG/WA），`[quote]` 每条出站一行日志，开关默认开并进自动回复设置页拟人节奏区；二期 / 三期只评估（§评估）。

## 一、改动落点（每 commit 一行，均带 #254）

| 项 | commit | 落点 |
|---|---|---|
| **A/B 规则引擎 + 接线 + 能力位** | `1c4ced51` | `src/inbox/reply_quote_policy.py`（重写，仍是纯函数、不读库不发网、不走 LLM）：`DEFAULTS` 新增 `stale_sec=1800 / random_lo=0.10 / random_hi=0.15 / short_quick_sec=120 / short_answer_chars=40 / max_streak=2 / cooldown_after_streak=2 / hourly_cap=6 / self_quote_pct=0.05 / self_quote_window_sec=600`，**`enabled=True`**（D-O6 默认开——直接改代码缺省，不依赖种子 / 基线补齐，绕开「第三次出厂默认没进基线」那个坑）。决策顺序：`unanswered_burst` → 候选（有字、未删、≤max_age）→ **n≥2** 相关性最高分过地板 = `burst/burst_relevant`（#37 金标 4 组沿用）；**n==1** 目标就是它：距今 >30 min = `stale/stale_30m`（必引）→ 否则 2 min 内且回复 ≤40 字 = `none/short_quick`（禁引）→ 否则掷骰：`session_rate(conv)`∈[10%,15%]（sha256(会话 id) 定该会话习惯率）、`roll=seeded_unit(conv,"roll",目标 mid)` < rate = `random/random_baseline`，否则 `none/roll_miss`；**n==0**（客户没插话、我们接着自己上一条再发）= 「补充上一条」：距自己上一条 ≤10 min 且有 mid，`seeded_unit(conv,"self",mid)` < 5% = `self/self_supplement`（`reply_to.from_me=True`，WA quoted 的 fromMe 已支持）。禁引 `_forbid` **绝对压过必引与基线**：`hourly_cap`（近 1h 带引用出站 ≥6）/ `streak_cooldown`（`streak_blocked`：最近 `cooldown` 个位置里坐着 ≥`max_streak` 的连引），被压掉时 `suppressed=<本可命中的规则>`。**历史从消息行推导**：编排器只在 worker 回执 `quote_applied` 为真时才把 `source.reply_to` 镜像进出站行（`messages.reply_to_id`）→ `row_was_quoted` 即「真带引用发出去了」，重启不清零、坐席手动引用也算数、无进程内状态。能力位 `QUOTE_CAPABLE_PLATFORMS={telegram:True, whatsapp:True, line:False, messenger:False}`，`platform_allows_quote` = 能力位 ∧ 编排器拥有（RPA 回落不收 reply_to）∧ 配置白名单；门禁 `test_capability_bits_synced_with_surface_fusion_registry` 钉住与 `surface_fusion` 注册表 `quote_reply.workspace==ok` 一致。`unsupported_decision` + `format_log`：`[quote] conv=<cid> reply_to_mid=<mid|-> reason=burst\|stale\|random\|self\|none rule=<规则> platform= burst= [score=] [roll= rate=] [suppressed=] [hour_quotes=] [self=1]`。stats 新增 `quoted_by{burst,stale,random,self}`，`skipped` 按 rule 计（`/api/drafts/autosend-status.worker.quote_reply` 原样透出，ops_overview 卡通用渲染）；灰度账本 `reason` 沿用 I-4 口径（具体原因，`summarize_quote` 的 low_relevance 直方图不断）并加 `why/rule/roll/rate`。`src/inbox/autosend_helpers.py` **只动引用决策调用一处**（L1836–L1873）：`enabled` 关 → 一行 DEBUG `rule=disabled`；能力位 / RPA 回落 → `unsupported_decision` 照样 `record_decision` + INFO 日志；否则 `list_recent_messages(limit=40)`（12→40，够覆盖每小时 6 条 + 连引冷却的出站历史）→ `decide_quote(..., conv_key=cid)` → 一行 `[quote]` INFO；带引用发送抛异常 → 去引用重发一次（沿用，日志改 `[quote] … rule=fallback_plain`，与 M-3 A 出站结果口径一致：不丢消息）。`src/web/i18n_packs/ops_overview_page.py` `ov2_rq_note` zh/en 改 O-4 口径（旧文案写「single_inbound 不引用」）。`tests/test_reply_quote_policy.py` 重写 48 项：金标 4 组 / stale 31 min 必引 vs 29 min 不必引 / short_quick 50 个会话种子全不引、长答进基线 / 种子确定性（同输入三次同结果，命中与未命中各钉一个会话 id）/ 3000 会话人群命中率 ∈[9%,16%] / `streak_blocked` 语义矩阵 / 连引冷却压掉 stale / 每小时 6 条压掉 burst、掉窗放行、`hourly_cap=0` 不限 / self 补充上一条 命中·未命中·超窗·无 mid·pct=0 / 人群 self ≤7% / 日志行格式 / 能力位 TG·WA·LINE·Messenger·zalo·RPA / autosend 接线（burst 带 reply_to + 日志、未配置默认开、显式关只 DEBUG、单条短答不引、stale 引、LINE/Messenger unsupported 普通发送、失败回落、store 异常吞掉、分条只首条引）/ autosend-status 透出 `quoted_by` + 未配置 `enabled=True`。 |

| **C 设置开关（默认开）** | `63c66713` | `src/web/templates/reply_settings.html`：「回复节奏与拟人细节」卡（拟人节奏区）内新增一行「引用回复」开关 `#rps-quote-row / #rps-quote`（`data-hot-chip` 热生效标注；`feat:"quote"` 特性探测——`/api/reply-settings` 的 `meta` 没有该键（旧后端）时整行隐藏、不参与收集 / 回填，防假脏 / 保存被拒，与 bub / floors / ws 同款）；`RPS_FIELD_DEFS` + `RPS_I18N.fieldNames` 各一键（改动明细能显示中文名）。`src/inbox/reply_pacing_settings.py` **仅加一键** `FIELDS["inbox.l2_autosend.quote_reply.enabled"]={bool, default=True, hot=True}`（消费方 `parse_quote_cfg` 逐次投递现读 config → 写 overlay 即生效；default 与 `reply_quote_policy.DEFAULTS["enabled"]` 同值，门禁 `test_settings_switch_whitelisted` 钉住；预设档不碰它）——本文件其余归 O-1（「拟人程度」三档），已在 commit 信息注明。`src/web/i18n_packs/reply_settings_page.py` zh/en `rps_quote_label / rps_quote_hint`（hint 只写规则不手写平台名——能力清单会烂，与 `test_capability_hints_hold_no_platform_claims` 同纪律）。tests：`test_settings_switch_whitelisted`（sanitize / nested_patch / effective_values 缺省 True·显式 False / split_hot_pending 全 live / 预设不含）、`test_settings_switch_wired_in_template_and_i18n`（行·控件·chip·字段定义·两处特性门·探测语句·fieldNames·位置在 pace 卡内·i18n 双语·无平台名）。回归 `tests/test_reply_quote_policy.py + tests/test_reply_settings.py` 200 passed / 1 failed（`test_platforms_match_worker_registry`：DY 线既有债，见 §四）；`tools/template_compile_check.py` OK（91 templates）。 |

## 二、验收（一期）

- [x] 连发 ≥2 → 引最相关那条（`reason=burst`）——`test_burst_quotes_most_relevant[4]`
- [x] 回 >30 min 旧消息 → 必引（`reason=stale`）——`test_single_inbound_stale_over_30min_must_quote` / `test_autosend_stale_single_quotes`
- [x] 10–15% 随机基线、会话级种子可复现——`test_random_baseline_is_deterministic_per_session_and_target` / `..._population_rate_in_band`
- [x] 禁引四条——`test_short_quick_single_reply_never_quotes` / `test_streak_cooldown_suppresses_even_must_rules` / `test_hourly_cap_six_per_conversation` / `test_self_quote_only_as_supplement_and_capped`
- [x] LINE / Messenger → `rule=unsupported` 日志 + 普通发送不报错——`test_autosend_unsupported_platform_logs_and_sends_plain`
- [x] 带引用发送失败 → 去引用重发（不丢消息）——`test_autosend_quote_failure_falls_back_to_plain`
- [x] `[quote]` 日志格式——`test_format_log_contract` / `test_autosend_passes_reply_to_for_burst_and_logs`
- [x] 开关默认开——`test_parse_cfg_defaults_on_and_clamps` / `test_autosend_default_on_without_config`
- [x] 设置页开关默认开、旧后端隐藏、写 overlay 即生效——`test_settings_switch_whitelisted` / `test_settings_switch_wired_in_template_and_i18n`
- 回归：`tests/test_reply_quote_policy.py tests/test_region_quote_gray.py tests/test_ops_overview.py tests/test_surface_fusion.py` 153 passed；`+ tests/test_reply_settings.py` 200 passed（1 failed 非本线，§四）。
- **生产验证口径（给 skuio 回访 / N-5 E 段）**：1.0.78 装上后，`logs/backend.log` grep `[quote]`——每条全自动出站一行：`[quote] conv=telegram:<acct>:<chat> reply_to_mid=<mid|-> reason=burst|stale|random|self|none rule=burst_relevant|stale_30m|random_baseline|self_supplement|short_quick|roll_miss|streak_cooldown|hourly_cap|low_relevance|no_candidate|no_inbound|unsupported platform=… burst=N [score=] [roll= rate=] [suppressed=]`；`reason≠none` 的那条在客户端应看到引用条（TG 原生回复 / WA quoted）。看板「地区语气 / 引用回复」卡 `quoted / decided`、`quoted_by`、`skipped` 同口径（进程计数）；跨重启用 `python tools/region_quote_gray_report.py`。**关掉**：自动回复设置 → 回复节奏与拟人细节 → 「引用回复」开关（写 overlay `inbox.l2_autosend.quote_reply.enabled=false`，下条出站即生效，日志退为 DEBUG `rule=disabled`）。

## 三、边界与刻意不做（一期）

- **`answers_mid` 无雏形**（全仓无该字段）：让模型结构化标「答的是哪条」要改 `drafts.py` 拟稿提示词，那是 O-1 C 地界；一期目标选择沿用 #37 的词汇覆盖相关性打分（金标 4 组全过），`answers_mid` 与「多条待回各回一条并引用」（47KNBV 唯一允许的多气泡情形）一并归二期（§评估）。
- **禁引压过必引**：47KNBV 把 2 min 短答 / 连引冷却 / 每小时上限列为禁引（防「引得太勤像机器」），规则引擎按「禁引 > 必引 > 基线」定序；被压掉的决策日志带 `suppressed=stale_30m|burst_relevant` 可审。
- **LINE 协议 worker 其实有 `related_message_id`**（`account_orchestrator.LineProtocolWorker.send`），但 47KNBV / 指令 §B 明说一期只做 TG/WA、LINE 无能力位；能力位表 `line=False`，二期若要开只翻一格（门禁会要求 surface_fusion 同步策划 LINE 行）。
- `config/config.example.yaml` L3659 `quote_reply.enabled: false # 默认关` 注释已过期（文件在别的线的工作树改动里、不在 O-4 归属），产品实际走代码缺省 True；请 N-5 / 文档线下次碰该文件时把注释改成「默认开」。
- `zh_hant_auto.py` 未再生（i18n 热区，覆盖率仍 >99%），随 1.0.78 打包 regen。

## 四、交回（非本线、跑测试时撞见）

| 事 | 归属 | 说明 |
|---|---|---|
| `tests/test_reply_settings.py::TestPlatformExpert::test_platforms_match_worker_registry` 红 | **DY 线（实施96 P1-1）** | `88f51826` 把 `("douyin","official",…)` 加进 `platform_capabilities.WORKERS`，但 `reply_pacing_settings.PLATFORMS` 没跟（该门禁就是为「两表漂移」立的）。修法一行：`PLATFORMS` 加 `"douyin"`（连带 `_sanitize_platform_*` 键域放开，抖音无 mark_read/typing 会被 `validate_platform_flags_caps` 如实提示）。O-4 只加了一键白名单、刻意不替别人改平台键域；N-5 E 段装配门禁会红，先记这里。 |
| `config/config.example.yaml` L3659 `quote_reply.enabled: false # 默认关` 注释过期 | 文档 / 该文件当前持有线 | 见 §三第 4 条。 |
| `desktop/build/i18n-packs-staged/*` 未同步 | N-5 打包 | 打包时同步产物（N-5 惯例），O-4 不碰 build 目录。 |

## §评估 二期 / 三期（只评估不做；答 3HNCJ7「各阶段开发周期 + 能否拆最小版快速交付」，交 O-5 回复）

**先答 3HNCJ7 的两问**：① 能拆最小版——已拆并做完（本文件一期，1.0.78 随包，实做 ≈$95 / 一个对话）；② 各阶段周期：一期 **已交付**；二期 **1 个对话 ≈$150**（约半个工作日，可排 1.0.79）；三期 **1 个对话 ≈$120**（约半个工作日，依赖二期的 intent 钩子，可排 1.0.79 或 1.0.80）。以下是逐项拆解与依据。

### 二期：重要内容高概率 + 镜像客户习惯 + 少 / 中 / 多三档 —— 估 $150

| 项 | 做什么 | 落点 | 依据 / 风险 | 估 |
|---|---|---|---|---|
| 2a 镜像客户引用习惯 40–50% | 入站行本就带 `reply_to_id`（客户在 TG/WA 引用我们时边车/worker 已落库）→ 纯函数数最近 N 条入站的引用占比，≥阈值（如 20 条里 ≥3）则该会话 `session_rate` 从 10–15% 抬到 40–50%（会话级种子不变） | `reply_quote_policy`（+1 规则 `mirror`，日志 `reason=mirror`）+ tests | 数据已在行里，零新依赖；最便宜、收益最直观（47KNBV 截图样本正是「爱引用」型客户） | $25 |
| 2b 重要内容高概率 60–80% | 候选按类型给概率：明确问题（`is_question` 已有）60% / 约定（时间地点承诺：确定性正则 + 复用 `memory.extract.intents` 里的 intent 标签）70% / 媒体（照片语音视频）80% / 情感表达 60%；仍受禁引约束 | `reply_quote_policy`（`important` 规则）+ 取 intent：入站行 `source` 或拟稿链已算出的 intent 需带到投递处（`autosend_helpers` 一处）| **媒体候选今天被排除**（无文字算不出相关性）：要引一张照片，必须知道「这条回复答的是那张图」→ 依赖 2d `answers_mid`；情感 / 约定的 intent 标签当前只在记忆抽取链（`skill_manager.should_extract_intent`）里，投递处拿不到 → 要么再跑一次分类（成本 / 延迟）要么在拟稿时随草稿落 `meta`。这是二期最贵的一项 | $60 |
| 2c 少 / 中 / 多三档 | `quote_reply.level ∈ {low, mid, high}`：low＝随机 5–8% / 每小时 ≤3 / stale 60 min；mid＝一期缺省；high＝随机 20–25% / 每小时 ≤10 / stale 15 min / 允许 2a 上限 50%。设置页开关升级为三档下拉；与 O-1 D「拟人程度」自然 / 偏快 / 偏慢一键联动（自然→中，偏慢→多，偏快→少） | `reply_quote_policy.parse_quote_cfg`（level → 参数集）/ `reply_pacing_settings.FIELDS` 一键 enum / `reply_settings.html` 一行 / i18n | 机制齐全，就是参数表 + UI；联动要等 O-1 D 落地后接 | $25 |
| 2d `answers_mid` 结构化标注 + 多条待回各回一条 | 拟稿时多条待回 → 提示词要求模型输出 `answers_mid`（答的是哪条 mid，可多条）→ 规则引擎优先用它选目标（相关性打分退为兜底）；进一步允许「每条待回各出一条回复、各自引用」——47KNBV / L-1 A 唯一允许的多气泡情形 | `drafts.py` / `persona_reply.py` 拟稿提示词与草稿 meta（**O-1 C 地界**，需协调）+ `autosend_helpers` 分条接线（一条草稿 → N 条各带 reply_to） | 改提示词有回归风险（拆条曾被识破 #210），要 50 条盲评；这是二期唯一碰 LLM 的地方，规则引擎本身仍确定性 | $40 |

合计 ≈ $150。建议顺序 2a → 2c → 2d → 2b（2b 依赖 2d 的媒体目标）。

### 三期：表情回应 ❤️ / 😂 / 🔥 —— 估 $120

| 项 | 做什么 | 落点 | 依据 / 风险 | 估 |
|---|---|---|---|---|
| 3a 出站能力 | TG：pyrogram `send_reaction`（MTProto `messages.sendReaction`；Bot API 对应 `setMessageReaction`）→ `TelegramProtocolWorker.react(chat_key, mid, emoji)`，A 线 companion 客户端同法；WA：Baileys `sendMessage(jid, {react: {text, key}})` → 边车新端点 `/react`（Messenger 边车已有同名端点可照抄）；LINE 无 reaction API → 能力位 false | `account_orchestrator`（两 worker 各一方法 + `orch.react`）/ `services/whatsapp-baileys/server.js` / `surface_fusion` 注册表 `reaction_out` TG/WA workspace none→ok | 库能力现成；`messages.reactions` 列（P4-3）已在，镜像回工作台零 schema 改动 | $45 |
| 3b 触发规则（确定性） | 照片 / 视频 → ❤️ 15%；夸赞 / 感谢 → ❤️ 10%；笑点（哈哈 / lol / 😂 串）→ 😂 15%；成就 / 秀 → 🔥 10%；会话级种子；禁：`stop_contact` / risk 命中不回应、每小时 ≤3、连续两条不回应、夜间档不回应 | 新 `src/inbox/reaction_policy.py`（与 `reply_quote_policy` 同骨架）| 纯规则、可钉；夸赞 / 成就分类复用 2b 的 intent 钩子（三期依赖二期 2b 的 intent 到位） | $35 |
| 3c 代替 / 延后文字 | 命中时二选一：`replace`（只回应不回文字，草稿取消，记 `decided_by=reaction`）或 `delay`（先回应、文字延后 1–5 min）；默认 `delay`，`replace` 只对纯媒体 / 纯表情入站 | `autosend_worker` 投递前一处（**O-1 A 地界**：与 stop_contact 门禁同段，需协调）+ 设置页一行开关（默认关，观察一周再默认开） | 「只回应不回文字」会改回复率口径（`outreach_response_stats` / 回复时延 SLA）→ 要把 reaction 计成一次响应 | $40 |

合计 ≈ $120。三期默认关灰度一周（`reaction_out` 计数 + 客户回复率对照）再决定默认开。

### 说话指纹样本 → O-1 C

47KNBV 截图是理想样本（全小写 / 拼错 / 无标点 / 一句一条 / 爱引用）：建议 O-1 C 的陪伴域 few-shot 直接收录 5–8 条真实句作 `spoken_style` 样例（5BV4U4 / FPFCRE 同源），并把「爱引用」交给本模块 2a 镜像规则而不是写进提示词（提示词遵守率低——D-O2 同一教训）。

### 回访文案要点（交 O-5 → skuio）

1. 一期已做、随 1.0.78：规则清单＝必引（连发 ≥2 回其中一条 / 回 30 分钟前旧消息）+ 10–15% 随机（每个会话习惯不同）+ 禁引（2 分钟内短答不引 / 连引两条歇两条 / 每小时 ≤6 / 引自己仅补充上一条 ≤5%）；仅 Telegram / WhatsApp 协议号；LINE / Messenger 一期不做。
2. 开关位置：自动回复设置 → 「回复节奏与拟人细节」→ 「引用回复」，默认开，关了下条出站即生效。
3. 验证：后端日志 grep `[quote]`，`reason=burst|stale|random` 的那条客户端应见引用条；看板「引用回复」卡看计数。
4. 二期（高概率 / 镜像 / 三档）≈1 个对话 $150、三期（表情回应）≈$120，可分别排 1.0.79 / 1.0.80；老板点头即开。
