# 修复指令 J 批（09-04 夜批报障：钧 / 花无缺，1.0.73）—— 并行公共底座

> **本文件只是「共同纪律 + 文件归属 + 老板决策」的公共底座，不要拿本文件开对话。**
> 每条指令各自成文（自带背景、证据、修法、验收、预算、接力话术），**一条指令 = 一个新对话**：
>
> | 指令 | 预算 | 类型 | 工单 | 文件 |
> |---|---|---|---|---|
> | **J-1** | $190 | AI 出站内容正确性 + 记忆一致性 | #171 #175 #176 #177 | `docs/指令_J-1_出站内容与记忆_2026-09-05.md` |
> | **J-2** | $190 | 目标 / 工作链 / 自动化档位：UI 承诺 vs 引擎实际 | #166 #168 #167 | `docs/指令_J-2_目标与工作链引擎_2026-09-05.md` |
> | **J-3** | $190 | LINE 媒体链（视频上限 / 入站视频 404 / 语音 501 文案 / receiver 断连） | #169 #172 #180 | `docs/指令_J-3_LINE媒体链_2026-09-05.md` |
> | **J-4** | $150 | 工作台 UI 小件（幽灵未读 / 批量挂链 / 幽灵账号行 / 说明条 / 电话掩码 / 媒体红字→黄字 / 告警 nag） | #170 #173 #174 #178 #179 #164 | `docs/指令_J-4_工作台UI小件_2026-09-05.md` |
> | **J-5** | $120 | 登记链与值守工具（零业务代码风险） | 限频丢登记 / 分类漏报 / backfill 双写 / verify 归属 / #138 #140 #142 校正 | `docs/指令_J-5_登记链与值守_2026-09-05.md` |
> | **J-6** | $150 | WhatsApp 边车稳定性（Node） | #181 + 403 无限重连 + ENOTFOUND 分类 + Bad MAC | `docs/指令_J-6_WhatsApp边车稳定性_2026-09-05.md` |
> | **J-7** | $150 | 人设工作室体验与报错（等 J-4 释放 personas.html 后开） | #189 #190 #191 #192 #193 #186（+#178/#179 若 J-4 未做） | `docs/指令_J-7_人设工作室体验_2026-09-05.md` |
> | **J-8** | $190 | 陪伴安全（危机审计默认开/空态真相/升级徽标）+ 关怀语义 | #185（P1）#182 | `docs/指令_J-8_陪伴安全与关怀语义_2026-09-05.md` |
> | **J-9** | $150 | 知识库厂商预置隔离 + 检索接通自检 | #184（#187 已作误读关单） | `docs/指令_J-9_知识库隔离与检索_2026-09-05.md` |
>
> **06:5x 追加 J-6…J-9**（06:38 复核：J-1/J-2/J-3 代码侧已完成、569 例测试绿；J-2 有追加对话 D1b 仍在改 `goals/**` + `cp-goal.js`；J-4/J-5 进行中）。
> **跨对话 / 跨账号交接的唯一入口 = `docs/J批_进度总账.md`**（谁接手都先读它）。
> 背景对账（证据、包码、逐单根因）= `docs/报障对账_钧_花无缺_20260905.md`。
> 推荐投放顺序：**J-1 → J-2 → J-3 → J-4 → J-5**，五条互相独立可同时开；无硬依赖。
> 不在本批：#181 WA 扫码登录慢（等钧机远程包 `dr-mtnarhxg-o8apf7` 到手再开 J-6 边车稳定性）；#164 的「对方收到没」/「一条还是两条」等钧回答。
> 卡住判据：开工 10 分钟内 `engines/chengjie` 下无任何文件变动＝卡了，停掉重开，只粘剩余项。

## 文件归属矩阵（共享工作树，越界即撞车）

| 文件 / 目录 | 归属 |
|---|---|
| `src/skills/skill_manager.py`（A 线守卫段）、`src/inbox/autosend_worker.py`、`src/ai/outbound_promise_guard.py`、`src/ai/media_pending.py`、`src/utils/persona_guard.py`、`src/ai/translation_service.py`、`src/ai/translation_memory.py`、`src/ai/translation_confidence.py`、新模块 `src/inbox/human_outbound_memory.py`、`src/eval/media_consistency_eval.py`、`src/eval/persona_consistency_eval.py` | **J-1** |
| `src/companion/goals/**`、`src/inbox/workflow_runner.py`、`src/inbox/workflow_autorun.py`、`src/inbox/automation_mode.py`、`src/inbox/account_mode_onboarding.py`、`src/web/routes/goal_routes.py`、`src/web/routes/unified_inbox_workflow_routes.py`、`config/profiles/cloud_light.yaml`、`config/config.example.yaml`（goals/workflows 段）、`desktop/renderer/shared/copilot/components/cp-goal.js`（目标表单，含 `src/web/static/shared/copilot/**` 镜像） | **J-2** |
| `src/integrations/line_media.py`、`src/integrations/account_orchestrator.py`（line-worker 段）、`src/web/routes/unified_inbox_send_routes.py`、`src/web/i18n_packs/errors_stock.py`、`tools/probe_line_media.py`、`src/integrations/line_rpa/**` | **J-3**（J-1 的 #177 若要在 send 路由加一行调用，先看意向板；J-3 活跃则改为写进结束报告交 J-3 落地） |
| `src/web/templates/unified_inbox.html`、`src/web/templates/workflows.html`、`src/web/templates/personas.html`、`src/web/templates/_alertlink_connect.html`、`src/web/routes/persona_routes.py`、`src/web/routes/unified_inbox_read_routes.py`、`src/inbox/unread_aggregate.py`、`desktop/renderer/shared/copilot/components/cp-chain-exec.js` / `cp-rel-stage.js`（+ `src/web/static/shared/copilot/**` 镜像）、`src/web/static/workspace/*.css`、`ui-build.txt` | **J-4** |
| `src/ops/bug_intake.py`、`src/ops/bug_intake_backfill.py`、`tools/duty_*`、`deploy/field-agent/**`、`deploy/duty/**`、台账数据 `bug_intake.db` | **J-5** |
| `services/whatsapp-baileys/**`（Node 边车）、`src/integrations/platform_session_health.py` | **J-6** |
| `src/web/templates/personas.html`、`src/web/routes/persona_routes.py`（**J-4 做完 #178/#179 并 commit 后移交**）、`persona_media_routes.py`、用户管理页模板、`admin.py`（只动 voice-eval 路由与会话回收两处） | **J-7** |
| `src/web/templates/crisis_audit.html`、`admin.py::crisis_audit_page`、`src/companion/wellbeing*`、`src/contacts/care_*`、`src/web/routes/care_routes.py`、`care*.html`、`i18n_packs/care_page.py`、`cloud_light.yaml`/`config.desktop.internal.yaml` 的 **wellbeing 两键**（只加自己的 hunk） | **J-8** |
| `src/utils/kb_*.py`、`src/web/routes/kb*_routes.py`、`knowledge*.html`、`i18n_packs/kb_page.py`/`knowledge_page.py` | **J-9** |
| `src/web/routes/unified_inbox_account_routes.py`（fetch-media 重试路由，J-3 交办） | **J-4**（补进归属；改法见 `发版对账_v1.0.74_J3.md`「交 J-4」） |
| `src/inbox/store.py`、`src/inbox/drafts.py` | **谁都不改**（需要新查询就新建模块；drafts 的接线点写进结束报告交值守） |
| `docs/发版对账_v1.0.74.md` | J-4 建骨架；其余各写 `docs/发版对账_v1.0.74_J<N>.md` 待合并 |

`desktop/renderer/shared/copilot/**` 与 `src/web/static/shared/copilot/**` 是**双树镜像**（门禁钉同步）：谁改组件谁同步两份 + bump `?v=`（`unified_inbox.html` 与 `shared/copilot/app.html` 两处）+ `ui-build.txt`。J-2 只碰 `cp-goal.js`，J-4 只碰 `cp-chain-exec.js` / `cp-rel-stage.js`，互不重叠；`?v=` 在 `unified_inbox.html` 里，J-2 的 bump 写进结束报告交 J-4。

## 老板决策（先看这里再看指令；不同意的划掉，对应对话按划掉后执行）

| 决策 | 默认 | 影响 |
|---|---|---|
| D1 `companion.goals.sprint.enabled` 在桌面包出厂默认 | **开**（安全闸 silence/min_gap/危机/opt-out 全部保留） | J-2 ①；不开则只做「UI 不再兜售自动推进」 |
| D2 #174 WhatsApp 客户卡电话掩码 | **点击揭示**（坐席需要全号，WhatsApp 号即身份） | J-4 |
| D3 桌面包（`AITR_DESKTOP_MODE`）隐藏「告警通道未接通 / 达成推进未接通」nag | **隐藏** | J-4 |
| D4 报障群限频只管 AI 回复不管登记；已知报障人「图+字」一律立单 | **改** | J-5 |
| D5 verify 归属：文中带 `#N` 才归属；短句「可以了/好了」不再翻单 | **改** | J-5 |
| D6 #138 / #140 / #142 由假 verified 改回 fixed | **改** | J-5 |
| D7 `companion.wellbeing.crisis_audit` / `crisis_escalation` 桌面包出厂默认 | **开 —— 老板 2026-09-05 15:05 已拍**（审计页按角色门控；升级同时亮工作台徽标，不依赖 webhook） | J-8 |
| D1b（J-2 提出）冲刺行豁免 care 灰度门 / `sprint.platforms` 加 whatsapp+line / 自然档每日拍 | **已由 D1b 对话按「开」施工**（`d34666f3` `abe96ba3` `981790e3`，仍在收尾） | J-2 追加 |

## 共同纪律（每份都适用，与 I 批同）

- 工作区 `D:\boundless`，引擎根 `engines/chengjie`。两位报障人版本均为 **1.0.73**（`6b7f2836` 之后的 HEAD 即同源），代码分析钉在 HEAD 即可。**不发版、不重启生产实例**（`.py` 改动等下个重启窗，值守统一装载）。
- 提交只用 `git add <明确文件列表>`，**禁止 `git add -A` / `git add .` / `git commit -a`**；提交前 `git status` 核对没装别人的文件；`.git/index.lock` 等 5 秒重试。暂存后几分钟内必须提交或 `git reset` 释放。
- **绝对禁止** `git reset --hard` / `git checkout .` / `git restore .` / 裸 `git stash push`（树上常年 100+ 个 dirty 文件属多条并行线）。只回滚自己的：`git checkout -- <明确路径>`。
- `git status` 里 `deploy/desktop/*.md/.json`、`deploy/instances/net_probe_117.ps1`、`@AutomationLog.txt`、`pc_runner_audit.jsonl`、`*.reqseq`、`engines/chengjie/desktop/build/i18n-packs-staged/**` 是他线/值守侧改动，不要装载。
- 只跑与本项相关的测试文件，不跑全量 pytest（全量由值守在各对话完成后统一跑）。
- 前端改动必须双戳：`src/web/static/workspace/ui-build.txt` 首行 + 相关 CSS/JS 的 `?v=`。
- 每项一个 commit，信息带工单号。自动执行所有命令不用问，删数据、改生产配置除外。
- 结束时告诉我：做了什么、测试结果、还能再优化什么、下一阶段建议。

### 省钱纪律

- ⛔ **禁止整读** `unified_inbox.html`（26k 行）、`skill_manager.py`（11k 行）、`telegram_client.py`、`autosend_worker.py`、`goals/service.py`、`unified_inbox_send_routes.py`、`bug_intake.py`——一律 `Grep`/`rg` 定位行号再 `Read` 带 `offset`/`limit` 取 ±80 行窗口。
- ⛔ 禁止 `python -m pytest tests/`（全量）。只跑本项测试文件，加 `-q --tb=line`；输出超屏 `2>&1 | Select-Object -Last 40`。
- ⛔ 禁止无过滤的 `git log`、`git diff`（不带路径）、`Get-Content` 整个日志。
- 用户机日志已在本地：`D:\boundless\tmp_diag\88MP86\logs\app\backend.log`（skuio 1.0.73，09-04 17:09→09-05 02:40）、`tmp_diag\3PZ95W\logs\app\backend.log`（钧 1.0.73，→09-04 20:53）。**用 `Select-String -Pattern` 抓关键行，不要整读**（2MB）。
- 一次工具调用能解决的别拆三次；相互无依赖的检查放在同一批并行发出。

### 断点续传协议（额度用尽 / 换账号 / 对话崩了都按这个走）

交接介质是仓库里四样东西——**`docs/J批_进度总账.md`（索引）**、commit、落点表、意向板。不依赖聊天记录。

1. **每完成一项立刻写落点表** `docs/发版对账_v1.0.74_J<N>.md`，一项一行：`| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注/剩余 |`；未完成项也留一行写「进行中：已改 X，还差 Y」。
2. **每项一个 commit 且立即提交**，别攒到最后。
3. **收工清意向板**：`scripts\agent_probe.ps1 -Intent "<同主题串>" -Done`。
4. **更新 `docs/J批_进度总账.md` 里自己那一行**（状态 / 已完成 / 剩余 / 已花）。只改自己那一行。接手方**不要只信总账**：先 `git log --oneline -15 -- engines/chengjie` 核对 commit。

**新账号 / 新对话续传，粘这段：**

```
接手 J-<N>，前一轮因额度中断。按顺序做三件事：
1. 读 D:\boundless\docs\J批_进度总账.md 找 J-<N> 行，看已完成到哪
2. 读 D:\boundless\docs\发版对账_v1.0.74_J<N>.md —— 这是断点，已完成项不要重做
3. 跑 git -C D:\boundless log --oneline -15 -- engines/chengjie 核对第 2 步的 commit 真在
然后读 D:\boundless\docs\指令_J-<N>_*.md，从落点表第一个未完成项继续，不要重新分析已完成的项。
```
