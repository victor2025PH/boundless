# 发版对账 v1.0.79 — Q-3（风险挂会话 + 人工优先：#264）

> 指令：`docs/指令_Q-3_风险挂会话与人工优先_2026-09-09.md`。底座：`docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-3 行 / 接口约定②）。总账：`docs/Q批_进度总账.md` Q-3 行。预算 $160。
> 接手方：先 `git -C D:\boundless log --oneline -10` 核对本表 commit 真在。全部是客户端 `.py` / 模板改动随 1.0.79；`.py` 须实例重启（`deploy\instances\restart_instance.ps1`）才生效，模板 / i18n 热更即生效。
> 执行：2026-09-10 起。意向板 slug `q-3-risk-hold-human-priority-264-risk-hold-py-au`。

## 〇 为什么切手动了还发（XBGPBN 23:49 → 23:56，三段）

1. **闸在等待之前**：worker 的 Sprint1 档位闸（原 L1897）跑在 `resolve` 之前；`resolve` 之后进 `_deliver_one` → `[pacing] 35.1s` 拟人等待 → 等待**之后**只有 fresh_guard 一道闸，而它**只认客户插话**（`find_superseding_inbound` 看的是更晚的入站）。23:55:43 进等待、用户此间切手动 + 打字，23:56:20 等待结束照发——期间没有任何一处重读档位 / 看坐席。切档路由那侧的 `cancel_pending_l2_drafts` 只作废 `pending`，而在途稿已是 `approved`，够不着。
2. **只认客户插话**：「插话」的定义里没有坐席——坐席打字没有任何信号进 worker（`/api/workspace/typing` 只广播给其他坐席），坐席发送成功只摘「需人工」标（`clear_needs_human by=agent_send`）不取消在途稿。人工优先 ≥ 客户在代码里没有落点。
3. **标签非闸**：23:49:13 隐私词命中 → `tag_needs_human`，但那只是列表标签 + `handoff_meta`，`autosend_policy.decide` 与 worker 从不查它；风险也只挂在**那一条稿**上（`risk_reasons`），会话没有记忆——23:55:42 客户又来一句，重新起草只看新入站，`risk=low → L2 shadow=-`，六分钟前的隐私命中归零。

## 0. 开工基线（全只读）

| 项 | 事实 |
|---|---|
| 意向板 | Q-3 登记；HEAD `b6d9ea90`；索引空闲 |
| 共享文件 | 我范围内他人在途：`i18n_packs/inbox_workspace.py` / `routes/unified_inbox_aggregate.py` / `routes/unified_inbox_send_routes.py` / `templates/unified_inbox.html` / `tests/test_admin_route_inventory.py` / `web/admin.py` → 一律 `tools/stage_hunks.py --mine`；`unified_inbox_stored_read_routes.py` 干净 |
| 代码事实 | `drafts.py` 四处 `policy_decide(...)` 都**不传会话**（L607 / L1136 / L1415 / L1602）——decide 里查 `risk_hold` 必须有会话，故 drafts.py 侧由 Q-2 补 `conversation_id=conv_id, store=self._store`（本线在调用侧 `autodraft_helpers` 先封顶 review 兜住）；`store.py` 无「按 draft_id 改 pending 稿 autopilot_level」的方法（`finalize_draft_enrichment` 只改 enriching）→ worker 对被闸住的 L2 走「取消 + 按 L1 重拟一次」而非原地降级；`update_draft_status(expected_statuses=)` 可把 approved → cancelled |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| A risk_hold + decide 强制 L1 继承 shadow · B 需人工作闸 · C 真发前二次复检 · D（worker 侧）在途取消 API · E 触发源 reason | #264 | `c0154cdd` | 新 `src/inbox/risk_hold.py`：`set(store, conv, reason, hit, ttl_h=24, by)` / `active(store, conv) -> reason\|None` / `active_record` / `clear(store, conv, by, only_reasons=None)` / `all_active`；存 `InboxStore.app_settings` KV（键 `risk_hold:<cid>`，值 `{reason, hit, set_ts, ttl_h, by, cleared_ts, cleared_by}`，**不动 store.py / 不建表**，与 Q-14 B 同先例）；TTL 24h 到期 `active()` 落 `[risk_hold] expired conv= reason= age_h=` 并写 `cleared_by=ttl`；泛因 `needs_human` 不覆盖已活跃的更具体原因（privacy / commitment / stop_contact…）；`clear(by=system:*)` 只清泛因，人工 `by` 清一切；同因重复登记只刷 TTL（debug）。`src/inbox/autosend_policy.py`：`decide(..., conversation_id="", store=None, risk_hold_reason=None)`，原正文抽成 `_decide_base`（逐字）；持有活跃且非硬停 → `Decision(level=L1, hold_reason=risk_hold:<r>, shadow=原影子记录或按持有造 L3 记录, review_required=True, risk_hold=<r>)`，日志 `[policy] conv= risk_hold= forced=L1 was= mode= shadow=`；不传会话逐字节旧行为。`src/integrations/protocol_autoreply.py`：`tag_needs_human` 同时 `risk_hold.set(conv, "needs_human", hit=reason)`（老标补闸）；`clear_needs_human(actor)` 同步 `risk_hold.clear(by=actor)`（`auto_clear` 的 `system:*` 只清泛因）；A 线直发新 `_risk_hold_gate`（持有 / 需人工标在场 → `_result("risk_hold")` 不生成不发，`[autosend] abort=risk_hold\|needs_human stage=protocol`）。`src/inbox/stop_contact.py`：`freeze_conversation` 登记 `set(conv, stop_contact\|self_harm, hits)`（只登记，冻结语义不变）；`unfreeze_conversation` 兜底 `clear(by=unfreeze:<actor>)`。`src/inbox/autosend_worker.py`：Sprint1 档位闸抽成 **`_human_priority_gate(conv, draft_id, is_farewell)`**（顺序：在途取消信号 → 显式档位 manual/review/multi_choice → 坐席 60s 内发送 → 60s 内打字 → risk_hold（泛因报 needs_human）→ 需人工标），**两处调用**：捞稿（原位置，mode 仍记 `decided_by=mode_downgraded` / `total_skipped_mode`；risk_hold/needs_human → `abort:<code>` + `_risk_hold_regen_once` 走 catchup 同一重拟回调按 L1 重拟一次，同一次持有不循环）与 `_deliver_one` 拟人等待后真发前（撤出站登记、approved→cancelled `decided_by=abort:<code>`、不喂熔断），日志 `[autosend] abort=<code> stage=batch\|presend draft= conv=`；在途登记 `_inflight_register/_unregister`；`cancel_inflight(conversation_id\|platform+account_id\|全部, by=mode_switch\|agent_typing\|agent_send) -> n`（点名在途 + 会话范围 `cancel_pending_l2_drafts`，`[inflight] cancel scope=conv\|account\|all n= by=`）；`note_agent_typing(conv)` / `note_agent_send(conv)`；告别稿 `_farewell` 豁免；快照新增 `total_abort_recheck / total_inflight_cancelled / total_skipped_risk_hold / total_risk_hold_regen / inflight_now`；补觉重拟前 `draft_trigger.note(conv, catchup_regen)` + `[draft] regen conv= reason=catchup_regen`。新 `src/inbox/draft_trigger.py`（进程级注册表 `note/pop/peek`，reason ∈ new_inbound/catchup_regen/risk_hold_regen/takeover_rearm/review_timeout/user_ignore）。`src/inbox/autodraft_helpers.py`（调用侧）：`auto_generate_draft` 前 `pop` 触发源 → `[draft] trigger conv= reason= mode=`；持有 / 需人工标在场且 mode=auto_ai → **封顶 review**（`[policy] conv= risk_hold= forced=L1 stage=autodraft`）+ `l1_reason=risk_hold`。新 `tests/test_risk_hold_q3.py` 20 例 | 20 passed；邻居 `test_autosend_worker_translate` / `_perf` 56 passed；`test_autodraft_helpers` / `test_automation_mode_downgrade` / `test_autosend_shadow_observability` / `test_handoff_tag_meta` / `test_l1_reason_m2` / `test_needs_human_autoclear_207` / `test_protocol_autoreply` / `test_stop_contact_hard_stop` 151 passed（`test_autodraft_bootstrap` 1 例失败为**存量**：断言未带 `source="bootstrap"`，与本线无关）。门禁四例：切手动 1 秒内取消（`[pacing]` 等待中切 manual + `cancel_inflight` → 0 发送、行 `cancelled/abort:mode_switch`、耗时 < 1s）；needs_human 期间 L2 → 捞稿取消 + `risk_hold_regen` 重拟一次；risk_hold 继承 shadow（medium 原 L3 影子记录原样、low 造 `risk_hold:privacy`）；坐席打字 → `abort:agent_typing`；XBGPBN 回放通过 |
| D（路由 / 前端侧）切手动 · 坐席打字 · 坐席发送取消在途稿 + toast + agent-typing 端点 | #264 | `de60a4fd` | `routes/unified_inbox_aggregate.py`：`_write_automation_mode` 非直发档（manual/review/multi_choice）→ 新模块函数 `_cancel_inflight_q3(request, conversation_id\|platform+account_id, by)` 调 `app.state.autosend_worker.cancel_inflight`（worker 缺席 / 旧版 → 0，不抛），计数并入 `cancelled_l2`（原 `cancel_pending_l2_drafts` 只够 pending，在途 approved 稿这次才被取消）。`routes/unified_inbox_stored_read_routes.py`：账号级批量确认 / 全局降档同步走 `_cancel_inflight_q3`（`res["cancelled_inflight"]`）；新 **`POST /api/unified-inbox/agent-typing`**（body `{platform, account_id, chat_key}` 或 `{conversation_id}` → `worker.note_agent_typing(cid)` → `{ok, conversation_id, cancelled}`；缺参 400）。`routes/unified_inbox_send_routes.py`：`_mark_send` 成功后 `worker.note_agent_send(cid)`（原只摘需人工标）。模板 `unified_inbox.html`：`_publishTyping`（3s 节流）同时 fire-and-forget 打 agent-typing，`cancelled>0` 才 toast；`setMode` toast 换 `inbox.mode.cancelled_ai`。i18n 三语 `inbox.mode.cancelled_ai` = 「已取消 {n} 条待发 AI 消息」/ `Cancelled {n} pending AI message(s)` / 「已取消 {n} 條待發 AI 訊息」（zh/en 在 `inbox_workspace.py`，繁体 `zh_hant_auto.py`）。路由清单 `_ADDITIONS_2026_09_10_Q3_AGENT_TYPING`。`tests/test_risk_hold_q3.py` +3（TestClient 真 InboxStore + 假 worker：切 manual 计数 = pending 1 + 在途 2 = 3、切回 auto_ai 不取消；agent-typing 端点 / 无 worker → 0 / 缺参 400；i18n 三语键） | 23 passed；`test_admin_route_inventory` 4 passed、`test_workspace_template_js_syntax` 3 passed。**暂存方式**：五个他人在途文件（`unified_inbox.html` 已被他人整文件 CRLF→LF 归一，`tools/stage_hunks.py` 每行都是 diff 无法分 hunk）改用仓库外临时脚本按 LF 归一后 `-U0` 差分挑我的 hunk、回写 HEAD 的换行、`hash-object` + `update-index --cacheinfo` 只进索引，工作树一字不动；`git diff --cached` 逐文件核对只含我的行。**发现**：共享工作树里 `tests/test_admin_route_inventory.py` 已**丢失** Q-14 已提交的 `_ADDITIONS_2026_09_09_Q14_AI_FAIL` 块（HEAD 有、工作树无；mtime 09-10 11:00，非本线所改）——本线 index 版按 HEAD + 追加 Q-3 块提交，Q-14 块保留；工作树那份未动，谁下次整文件 `git add` 该文件会把 Q-14 块删掉，已写进总账提醒 |

## 2. 待 Q-2 / 他线接口（写进总账 Q-3 行）

- Q-2（`drafts.py`）：四处 `policy_decide(...)` 补 `conversation_id=conv_id, store=self._store`——补上后 decide 的 risk_hold 分支才会在 drafts.py 侧落 `_record_shadow`（影子台账继承）；未补前由 `autodraft_helpers` 封顶 review 兜住（L1 但台账无影子行）。承诺守卫命中 → `risk_hold.set(store, conv, "commitment", hit)`（接口约定②）。
- O-1 A：`freeze_conversation` 已登记 `stop_contact` 行（本线代做，只登记）。
- Q-4：若要 worker 在 `_process_batch` 落 work_schedule 一行，直接在总账 Q-3 行点名即可。
