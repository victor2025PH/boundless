# 指令 O-1 续做：D 拟人节奏默认档 + E 三小件（接手 10:30–11:20 停摆的半成品，收工交 R78 发 1.0.78）

> 原指令 `docs/指令_O-1_拟人与反识破_2026-09-08.md`（D / E 两节原文仍有效）；底座决策 **D-O4**（`docs/修复指令_O批_并行_2026-09-08.md`）；落点表 `docs/发版对账_v1.0.78_O1.md`（A / B / C 三行已在，**D / E 两行由你补**）。工单 **#252 #254**（#253 / #255 已并入）。预算 **$80**（到 $65 停手写落点表，未做的写明交下批）。
> **1.0.78 全链在等你**：R78 发版线（`docs/发版对账_v1.0.78_发版.md`）已把 changelog / 标 fixed / 回访 / 播报全部备好，只等 O-1 行变「收工」就从 `restart_preflight` 起跑。**你不重启实例、不 bump、不打包**——装载归 R78。

## 0. 现场（12:2x，R78 核过）

上一段 O-1 对话在 10:30–11:20 把 D 段写了一半就停了，五个文件**未提交**躺在共享工作树里，你要在它们之上继续，不要重写：

| 文件 | 已有（`git diff` 可见） | 缺 |
|---|---|---|
| `src/inbox/humanize.py`（+162） | `PACING_PROFILES{natural,fast,slow}`（读 / 想 / 打字 / stop / min_gap / min_residual 参数）、`PACING_PROFILE_NAMES`、`HumanPacing` dataclass、`resolve_profile(block)`、`_count_words`（CJK 2 字≈1 词）、`estimate_human_pacing(reply_text, inbound_text=, params=, rng=)` 纯函数；`resolve_deliver_delay` 加 `inbound_text=` 与 profile 分支（结果对象新增 `profile / read_sec / think_sec / type_sec / stop_sec / typing_lead` 字段，legacy 全 0）；`stop_before_send_sec` 参数已到 L552 附近 | 分支收尾核对（diff 在 `for k in list(params.keys()):` 处中断，确认块显式值覆盖档位缺省、已耗时照扣、`typing_lead` = 打字分量）；**夜间 / 首回延迟 / 连发合并**未写（见 §1 ③） |
| `src/inbox/autosend_worker.py`（67 行） | `_pick_deliver_delay` → 包 `_pick_deliver_pacing`（返回完整结果对象）；`deliver` 路径 composing 可观测：`nocb / off / pending → sent / unsupported / skipped`（回调返回值判真挂上了没）；**每条出站一行 `[pacing] conv= platform= profile= read= think= type= stop= elapsed= total= typing_lead= composing= typing_calls= words=`**；`stop_before_send_sec` 传入；调用点带 `inbound_text=item.peer_text` | 核对 `stop_before_send_sec` 在 humanize 侧真被用（发出前 1s 停 composing 的实现）；`typing_callback` 为什么此前零日志（D-O4 ②：TG `sendChatAction` / WA 边车 `presence composing` 是否真接、被 `typing_indicator` 覆写关了没；LINE / Messenger 写 `composing=unsupported`） |
| `src/inbox/reply_pacing_settings.py`（21 行） | `PACING_PROFILE_CHOICES=("custom","natural","fast","slow")`；`FIELDS["inbox.l2_autosend.deliver_delay.profile"]`（enum / default `custom` / hot=worker）；三预设 `cautious→slow / natural→natural / rapid→fast` 各带 profile | 注释里写「出厂基线换 natural 交 config 基线归属方（O-2 / N-5）」——**O-2 已收工、N-5 已收工，这件归你**（§1 ④） |
| `src/web/templates/reply_settings.html`（34 行） | 「场景预设档」改名「拟人程度」三档文案；新行 `#rps-profile-row` + `<select id="rps-profile">`（natural / fast / slow / custom）+ `feat:"profile"` 特性探测（旧后端隐藏整行）；`RPS_FIELD_DEFS` 一键 | **门禁红 ①**：`<script>` 块内有一段 Jinja 注释 `{# O-1 D（D-O4）「拟人程度」三档… #}`——门禁 `test_template_inline_js_syntax` 只占位 `{% %}` / `{{ }}` 不剥 `{# #}` → node 报 `Unexpected token '{'`。生产页面没坏（Jinja 渲染会剥），但**门禁不绿不能发版**：改成 JS `//` 注释即可。`RPS_I18N.fieldNames` 是否已加 profile 键名自查 |
| `src/web/i18n_packs/reply_settings_page.py`（34 行） | zh / en 三预设 `rps_preset_*` 文案改成三档口径 | **门禁红 ②**：`test_i18n_coverage::test_sealed_pages_en_no_cjk_leak[reply_settings.html]`——模板用到的 `rps_profile_label / rps_profile_natural / rps_profile_fast / rps_profile_slow / rps_profile_custom / rps_profile_hint` **六键 zh / en 都没进包**（EN 渲染残留「拟人程度档 / 自然 / 默认 / 读 / 想 / 打字 / 词 / 分 / 偏快 / 偏慢」） |

`holding_reply.py`（06:28）/ `persona_reply.py`（06:39）/ `reply_split.py`（02:18）也是脏的，**不是 O-1 的**，别碰。`unified_inbox.html` 是 HOT-ZONE 且微信 KF 线在途，D / E 不需要碰它。

## 1. D 拟人节奏默认档（D-O4）——按此顺序，每步可单独提交

① **先让两条门禁转绿**（10 分钟，先于一切）：`{# #}` → `//`；六个 `rps_profile_*` 键进 `reply_settings_page.py` zh / en；跑 `python -m pytest tests/test_template_inline_js_syntax.py "tests/test_i18n_coverage.py::test_sealed_pages_en_no_cjk_leak[reply_settings.html-full]" tests/test_reply_settings.py -q` 全绿 + `python tools/template_compile_check.py`。
② **三档模型收尾 + composing 真发**：核对 humanize 分支；`typing_callback` 链路查明（`autosend_worker.py` L144–L190 `typing_callback` / `typing_indicator`，`channel_adapters.py` 归 O-1 可动；边车归属外只读）；`[pacing]` 行每条出站都有；`composing=sent` 在 TG / WA 至少一条能实证（zhiliao 装载后 R78 会 grep，先用测试钉住回调调用与返回值判定）。
③ **D-O4 其余三件**（写在 humanize / worker 一处，默认开、可关，键放 `deliver_delay.*` 块同一热更路径）：客户连发合并等 8–15s 一次回（与 L-1 A 镜像口径合并，别做第二套）；首次接触 / 沉寂 >6h 首回延 1–5 min；夜间按账号时区 01–08 不回、08 后随机 0–40 min 补。**预算不够就按此顺序砍尾巴，砍掉的在落点表「未做」写明**，不要半写。
④ **出厂基线 = natural**（D-O4 原话「自然（默认）」）：`src/utils/feature_registry.py` A 类加 `inbox.l2_autosend.deliver_delay.profile = "natural"`（`show=False`，机制同 O-2 A `memory.extract.*`——clean 升级机首启 `_ensure_baseline` 补进 overlay）+ `config/config.desktop.min.yaml` 种子同值 + `docs/功能矩阵_ChatX.md` 重导出（`python -m scripts.feature_matrix --out`）。**存量机器语义**：块里已显式写过 `profile`（含 `custom`）的一字不动；只有缺键的补 natural——这正是「旧 min/max 被夹成 12–13 秒常数」的机器要换的行为，落点表写清。门禁：`test_feature_registry` / `test_desktop_seed_visibility` / `test_seed_switch_upgrade_coverage` / `test_packaged_capability_ledger` / `test_feature_matrix`（后两者各有 1–2 条预存红，O-2 落点表 §1.3 已登记，别算自己头上）。`FIELDS` 的 `default` 保持 `custom`（那是「配置里没写时 UI 回填什么」，与基线补齐不同层）——若你判断该改成 natural，落点表写理由。
⑤ **tests**：新 `tests/test_pacing_profiles.py`（humanize 注释里已点名这个门禁名）——三档参数表与 `PACING_PROFILE_CHOICES` 一致；`estimate_human_pacing` 纯函数：注入 rng 后读分量随入站词数单调、打字分量随出站长度单调、CJK 走 cpm、夹 cap、`stop` 含在 type 段；**验收回放**：20 条不同长度入站 → 发出间隔与字数相关系数 >0.6、无两条间隔到秒相同；legacy 块（无 profile）输出逐字节不变（既有 `test_humanize*` / `test_autosend_deliver_delay*` 全绿即是）；`[pacing]` 日志行格式与 `composing` 六态；夜间样例不发、08 后补发窗口；三预设各带 profile；模板 / i18n / feat 探测钉住。相邻回归：`-k "humanize or deliver_delay or pacing or reply_settings or typing or autosend_work"`。

## 2. E 三小件

- **账号显示名 ≠ 人设名**（Vanessa / Mizuki）：绑定页 / 人设页红条「账号名与人设名不一致，客户会察觉」+ 一键「按人设改账号名」（能力位有的平台；TG 有 `update_profile`，WA / LINE 无则只给「人设改名」）；日志一次 `[self-name] account=… persona=… mismatch=1`。落点：新纯函数模块（如 `src/utils/account_name_check.py`：账号显示名 × 人设名归一比对，昵称含人设名算一致）+ 人设页 `personas.html`（非 HOT-ZONE，先 `agent_probe` 看没人在动）红条 + 现有账号 / 人设信息路由多回一个 `name_mismatch` 字段；**不新增路由**（装配门禁清单不必动）。仓里没有现成 `self_name` 模块，`test_self_name_guard.py` 是「AI 不得自称域角色名」的另一件事，别混。
- **>72h 未回的入站禁编造理由**：`GreetingSkill` / 自动回复对 >72h 沉寂后的首回，走 L-2 A「不编事实」词表（buried in work / been so busy / phone died / 忙死了 / 手机坏了…）→ 命中即改「不解释」或如实「刚看到」；与 C 段底稿「不编造日常与迟回理由」同向，守卫落 `persona_guard`（你的文件）。tests：三句编造理由被剥 + 「刚看到」放行。
- **发版门禁脚本** `scripts/outbound_style_gate.py`：**B 段已做完**（200 样例 0 违规，R78 12:0x 实跑过），E 段这一件只需在落点表 E 行写「已随 B `583af62b`」。

## 3. 红线（与原指令同，加三条）

- `git add <显式路径>`；每 commit 带 `#252 #254`；提交前 `git diff --cached --name-only` 只有你的文件；不 `-A`、不 `reset --hard`、不 `stash` 别人的东西。`zh_hant_auto.py` / `unified_inbox.html` 若必须动，用 `tools/stage_hunks.py --mine … --theirs …` 只暂存自己的 hunk。
- **不重启 zhiliao、不 bump 版本、不打包**：装载 / 回归 / 三包归 R78。`.py` 需重启这句照写进落点表「装载要求」。
- 中间态自洽：模板 / i18n 保存即生产热更——每次存盘 `python tools/template_compile_check.py` 绿、没有半接的按钮。
- 开工先 `scripts\agent_probe.ps1 -Intent "O-1 humanize anti-detect #252 #253 #254 #255 (…)"`——**同主题同 slug 即刷新**上一段留下的意向，不要另起一条。

## 4. 收工（三件齐，R78 才动）

1. 落点表 `docs/发版对账_v1.0.78_O1.md`：§二 补 **D / E 两行**（commit / 落点 / 装载要求）；§三 补验收行（20 条回放相关系数实测值、夜间样例、composing 实证、门禁 ①② 转绿、`test_pacing_profiles` 例数）；§四 补未做 / 交下批；**§五 回访口径**：R78 已替你起草了 `D:\chengjie-instances\.ops\duty_0908\r78_252.txt`（含 D / E 各一句），**你只改那两句**让它对上实做（路径「自动回复设置 → 回复节奏与拟人细节 → 拟人程度」是否准确、账号名提醒在哪个页），别整段重写；同时给 R78 一条 changelog 句（D 一句 / E 一句，中英各一，直接写进 `engines/chengjie/tmp/_notes_1078.txt` / `_notes_1078_en.txt` 末尾）。
2. `docs/O批_进度总账.md` **只改 O-1 行**：状态改「**收工**」、已完成列补 B / C / D / E 四段 commit、剩余列写「无 / 交下批 …」、已花实数。写法照 O-2 的教训（`8c0dc832`）：取 HEAD 版本只替换 O-1 那一行再写 index，**别把工作树里别人未提交的行卷进去**；文件是 CRLF，别丢 `\r`。
3. `scripts\agent_probe.ps1 -Intent "O-1 …" -Done`。然后在对话末尾一句话告诉老板「O-1 收工，R78 可发」。

## 开新对话粘贴

```
读 docs/指令_O-1_DE续做_2026-09-08.md，接手 O-1 的 D 拟人节奏默认档 + E 三小件（#252 #254）。上一段 O-1 对话 10:30–11:20 把 D 写了一半停了：humanize.py / autosend_worker.py / reply_pacing_settings.py / reply_settings.html / reply_settings_page.py 五个文件未提交在工作树里，按指令 §0 表在其上继续，不要重写。第一步先把两条门禁转绿（reply_settings.html <script> 里的 Jinja 注释 {# #} 改 // 注释；rps_profile_* 六键进 i18n 包 zh/en），再按 §1 ②③④⑤ 收尾 D（含出厂基线 profile=natural 进 feature_registry A 类 + min 种子）、§2 做 E 前两件（第三件门禁脚本 B 段已做）。预算 $80。先 agent_probe -Intent 用同主题刷新原意向，只动 O-1 归属文件，不碰 holding_reply.py / persona_reply.py / reply_split.py / unified_inbox.html（别线在途）。不重启实例、不 bump、不打包——1.0.78 装载与发布归 R78，它在等你。每个 commit 带工单号并立刻补落点表 docs/发版对账_v1.0.78_O1.md 一行；git add 显式路径。收工三件：落点表 D/E 行 + 验收 + 改 r78_252.txt 里 D/E 两句 + changelog 两句写进 engines/chengjie/tmp/_notes_1078*.txt 末尾；总账只改 O-1 行为「收工」（HEAD 版本只替换该行写 index，CRLF）；agent_probe -Done。
```
