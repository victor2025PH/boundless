# 指令 Q-17：风控三级分级 + commitment 只评出站 + 摘标冷却 + 风控页公开（#277②）

> 预算 **$160**。工单 **#277（P0）②**（报告 BRGQ6T / FRF7Y9 / E2NMBE / GXRD67 / VUDAJP，09-11 04:39–05:53，skuio 1.0.79；同族 CF5SCB / 7M4QQZ / 9JP5SZ）。底座 `docs/修复指令_Q批_并行_2026-09-09.md`。落点表 `docs/发版对账_v1.0.81_Q17.md`。
> #277 ①（识图描述被裁）③（能力自曝硬拦）已由 `4fdd238f` 修并随 1.0.80 上架，**本线不碰**。前置：Q-2 / Q-3 / Q-15 均已收工，本线在它们之上改。

## 事实（证据 `tmp_diag\{FRF7Y9,BRGQ6T,GXRD67,CF5SCB}`；代码只读核对）

- 今夜日志反推的现行标准（页面无任何说明）：`privacy`（your address / phone number **提及即中**）、`adult`（sex）、`stop_contact`、`keyword`（scam / send money）、`commitment:media|money|contact|meetup`（Q-2 新，客户**提及**照片 / 钱 / 电话 / 见面即中）→ 一律 L1 + needs_human(`auto_clearable=False`) + risk_hold，保持期间每条入站强制 L1 并**重复打标**。
- 实证 Cameron(15635715247)：04:39:45 人工发送摘标（02:59 所打）→ 04:50:12 客户「Trying to show you the pictures they have sent me to find out if you in the picture」→ `shadow=keyword hits=commitment:media` **第三次打标**。
- **根因**：`drafts.py` `keyword_risk_hits()` 对**任何**文本调 `detect_commitment(t) or detect_commitment_claim(t)`，命中即 `high`——`commitment_guard` 本为约束我方出站承诺（PHBRBW / D6V29H），被套用到客户叙述；`_SENSITIVE_PATTERNS` 里 `your address / phone number` 也是提及即 high。摘标 `clear_needs_human` 已同步 `risk_hold.clear`（Q-3），但没有冷却，下一条入站同类命中立即重打。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 三级分级** | 新 `src/inbox/risk_grader.py`：`grade(text, direction: in|out, persona) -> {level: high|medium|low, category, hits, action}`。**高**（诈骗 / 索钱 / 威胁 / 自伤危机 / 未成年 / 露骨 + 施压 = Q-15 `pressure`）→ L1 + needs_human + risk_hold（现状）；**中**（露骨提及 = Q-15 `explicit`、客户**索要**见面 / 照片 / 电话 = `request:<kind>`、停联词）→ 走人设政策：Q-2 委婉延后 / Q-15 soft_reply / stop_contact 冻结，**只打会话标签 `risk:medium` 不进 needs_human**；**低**（隐私词、照片 / 钱 / 电话的叙述性提及）→ 仅 `[risk] low conv= category= hits=` 日志。`keyword_risk_hits(text)` 增 `direction` 参数并改为调 grader 取 level（旧签名保留兼容） | 新文件 + `drafts.py` 调用处 |
| **B commitment 只评出站** | `direction=out`（AI 稿 / 出站）才跑 `detect_commitment_claim`；`direction=in` 只跑客户侧**索要句式**（send me / give me / can I have / what's your / show me yours / 发我 / 给我 / 你的电话 / 你住哪）→ `request:<kind>` 中级；`_SENSITIVE_PATTERNS` 里 `your address / phone number / my address is` 三条降为 `low`（叙述）并由索要句式接管中级。Q-2 门禁 20 邀约 + 10 要照片**必须仍全过**（出站不回归） | `drafts.py`（只改 `keyword_risk_hits` 与三条正则级别）、`commitment_guard.py`（只加 `detect_request`） |
| **C 摘标冷却 + 幂等** | `risk_hold` 增 `cooldown(cid, category, minutes=30)`：人工发送 / 点击「我知道了」摘标时写 `risk_cooldown:<cid>={category, until}`；冷却内**同类别**不重打、不重发横幅，仅**更高级别的新命中**可重打；保持期间已在标 → 只更新 `last_hit` 不重复打标不重复横幅；横幅固定显示 原因 / 时间 / 摘标按钮；会话头 chip「风险保持 · <类别>」；`[needs_human] 打标` 行带 `level= category= cooldown_skipped=` | `risk_hold.py`、`protocol_autoreply.py`（打标处一段，stage_hunks）、`unified_inbox.html`（`rk-` 前缀） |
| **D 风控页公开** | 回复设置页新「风控分级」卡：全部类别 / 词表摘要 / 级别 / 动作只读表 + 人设级可调 `boundaries.risk_overrides: {category: level}`（adult 沿用 Q-15 `adult_policy`）；`reply_diagnosis` 的 `high_risk` finding 带 level / category / hits；i18n zh/en/zh-Hant | `reply_settings.html` + `i18n_packs/reply_settings_page.py`（**用量线在途，stage_hunks**）、`persona_manager.py`、`reply_diagnosis.py` |
| **E 门禁** | `tests/test_risk_grader_gate.py`：客户叙述 15 句（address / phone / pictures they sent / money he stole / weekend plans）= low 或 medium，**零** needs_human；索钱 / 威胁 / 露骨施压 5 句 = high；摘标后 30 分钟同类不重打、更高级别可重打；Q-2 `commitment_gate` 与 Q-15 `adult_grader_gate` 全过；纳入 R 批门禁清单 | tests |

## 红线

- 不动 `autosend_worker.py` / `autosend_policy.py`（Q-3 闸继续按 risk_hold 拦），不动 `store.py`（冷却走 `app_settings` KV 同 risk_hold）。
- 高级别的现状行为一字不变——本线只把「中 / 低」从 needs_human 里剥出来，不放松任何真高风险。
- 不引入任何罐头句；中级的自动回应全部复用 Q-2 / Q-15 的人设口吻话术库。

## 开新对话粘贴

```
读 docs/指令_Q-17_风控分级公开与摘标冷却_2026-09-11.md，按它做 Q-17（#277 P0 ②：新 risk_grader 三级 高→L1+需人工+risk_hold / 中→人设政策软拒或冻结只打 risk:medium 标签 / 低→仅日志；keyword_risk_hits 加 direction，commitment_claim 只评出站、客户侧只认索要句式 request:<kind>，your address / phone number 叙述降低级；risk_hold 加 30 分钟同类冷却 + 保持期间幂等不重打不重横幅 + 会话头「风险保持」chip + 打标日志带 level/category；回复设置页「风控分级」卡公开类别词表级别动作 + 人设级 risk_overrides + reply_diagnosis 带类别；门禁 15 句叙述零需人工 + 5 句真高风险 + 冷却 + Q-2/Q-15 门禁不回归）。预算 $160。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与 Q-2 / Q-3 / Q-15 三张落点表（在它们之上改，不重做），再 scripts\agent_probe.ps1 -Intent 登记；不动 autosend_worker.py / autosend_policy.py / store.py；reply_settings.html 与 protocol_autoreply.py 有他人在途 hunk 用 tools/stage_hunks.py --mine；高风险现状行为一字不变。每个 commit 带 #277 并立刻补落点表 docs/发版对账_v1.0.81_Q17.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-17 行并 agent_probe -Done。
```
