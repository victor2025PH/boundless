# M 批进度总账（跨对话 / 跨账号交接的唯一入口）

> 任何新对话、新账号、崩掉后重开，**第一件事就是读这里**。不依赖聊天记录。
> **谁改这里：** 每条指令的执行方在**收工时（或额度将尽时）**更新自己那一行，**只改自己那一行**。
> 建立于 2026-09-06 23:0x。公共底座 `docs/修复指令_M批_并行_2026-09-06.md`（去向表 / 文件归属 / 决策 D-M1–D-M10 + L-4 四问补答）。

## 一句话现状

1.0.75 首日复验：skuio 51 份报告 + 钧 3 条群提交 → 新单 #214–#235（22 张）+ 补证，登记链全部自动挂单/立单并回执，**内容此前无人分析**。核心结论：1.0.75 把关怀真发 / 目标冲刺真发 / Messenger 新号直接全自动三条链打开了，而内容与通道校验没跟上——「错但没发」变成「错且发到客户」（#218 关怀内容错误真发、#234 中文直投外国客户、#232 Messenger 零送达却显 ✓）。目标版本 1.0.76。
**M-1 / M-2 今晚先开**（对客事故面；两条都碰 `autosend_worker.py`，各动各的三处），M-3 / M-4 / M-5 并行，M-6 等前五条收工。

## 进度表

| 指令 | 主题 | 工单 | 预算 | 状态 | 已完成 | 剩余 | 落点表 | 已花 |
|---|---|---|---|---|---|---|---|---|
| **M-1** | 出站三闸止血 | #214 #218 #234 #219 | $150 | **收工**（09-07 01:2x；`.py` 待装载，`care_schedule.html`/`care_page.py` 已热更） | A `fe8f7a8a`（关怀默认 verbatim；AI 润色 add 无 `preview_confirmed` 不入队 → 预览稿 + 档案 + 校验；新 `care_profile.py` 档案组装 + `fabrication_guard.detect_profile_contradiction` 本地人当外国人/老客当新客 → 拦 skip；首次真发/地名/人名 → `hold:*` 待运营点头「就这样发」；`effective_dry_run()` 单一真值 + 翻转 INFO + 到期组真实状态条 + 模拟中「立即发」说清；`[care-gen]` 落日志；36 例）/ B `d7735f97`（`resolve_outbound_lang` 手动>档案>消息/文字系统>出站历史>人设，全无 → HOLD `translate_hold:lang_unknown` 转人工不投递；mismatch 等校验失败一律 HOLD 带原因；表情/单字 identity 不进引擎；`[xlate] outbound … decided_by=`；worker 只动两处 + 共用文案助手，M-2 hunk 未动；38 例）/ C `13b58e3b`（**根因**：pyrogram 同 group 只跑第一个命中 handler，删除同步 handler 被已读回执遮蔽从未执行 → 独立 group；出站行 `revoke_own_by_platform_msg_ids`（store 唯一新函数）灰显不消失 + `list_recent_messages` AI 口径剔 revoked（点名）；A 线 assistant 历史/last_reply/recent_replies/_human_said_log 剔除 + 己方撤回账本注入「不得再提」；轮询 top 对账兜底；WA/Messenger `message-op revoke` 补同一钩子；13 例） | 交 M-4/M-6：己方撤回气泡文案「你撤回了一条消息」（`unified_inbox.html` 1 行 + 1 键，热区未动）；LINE 己方 unsend 不同步 → 1.0.77；M-2 E 原因卡读 `translate_hold:` 前缀；J-8 遗留 2 红 + 跨午夜 1 flake 见落点表 | `发版对账_v1.0.76_M1.md` | ≈$95 |
| **M-2** | 通道真相与登录默认 | #232 #233 #235 #223 #156补 | $180 | **收工**（09-07 02:0x；`.py` 十一处 + 边车 `server.js` 待装载——**Python 与 Node 两个重启都要做**；模板/i18n/CSS 已热更 `v=20260906g`；⚠ 本机 gitignored `config/config.yaml` 已按 D-M1 改 `automation_mode: review` + `bootstrap: false` + `login_gate`，zhiliao 装载后未显式设档会话默认半自动） | A `ece8a0aa`（`channel_connection_state` 单一出口：注册表 offline / 健康表不健康 / worker 放弃 → disconnected，worker 退避 → reconnecting；`send_via_adapters` 入口**自动与手动同一闸**拒发 503 `channel_disconnected`；Messenger `healthy()` 探测回报健康表——边车无该账号会话 ≥90s → needs_login（restored 0 盲区）；RPA 入队 `delivered=None`；`/send` 带 `delivered/queued`，按钮 ✓ 只在 `delivered===true`、入队「已排队发送」；账号 chip 未连接/重连中/通道异常/冷静期四标签 + 会话横幅四分支；新模块 `account_channel_gate.py`；13 例）/ B `3fe64e53`+`8d272236`+`dc8c5e0a`（种子与 example 全局 review + bootstrap 关 + `login_gate`；桌面存量三态迁移；账号层「登录晚于按账号确认 → review」不写会话行＝老会话不动；封顶 ⑧ login_cooldown ⑨ channel_degraded ⑩ channel_disconnected；新模块 `account_bulk_mode.py` + `POST /automation/account-bulk` 干跑→确认「将对 N 个会话开启，覆盖 M 个个别设置」；账号总览每行「此账号全部会话 → 手动/半自动/全自动」；worker 冷静期/降级/退避/未连接 → L2 留 pending 当积压 + 失败记账（3 次真实失败降级红标，边车退避不计）；9 例）/ C `b085e4bf`（改期到点重投先过账号门禁不续命 `total_retry_gated`；contextvar 人工标 → 边车载荷 `manual:true`；authorized → reset_backoff；conftest 门禁隔离；4 例）/ D `573de634`（边车 `restoreAll summary` 四计数 + `/accounts.worker.restoring/pending` + 404 落日志 `not_logged_in` + 500 补 message/reason_code + `manual` 每退避窗放行一次探测 `manualProbeDecision` + 登录成功重置退避；node 136 passed）/ E `3443edf1`（`l1_reason.py` 原因码 + autodraft 登记/审计 + **drafts.py 仅一条语句** `reason=`（D-M10）+ `/api/drafts.l1_reason` + 草稿条/待审卡原因 chip；下拉「本会话：…」+「半自动（待确认 N）」+ 封顶时「全自动 → 实际半自动」；`chatFilter='auto'` + `wsAutoConvCount/wsFilterAutoConversations` 接口（M-4 B 已接点击）+ 胶囊文案「全自动会话 N」；切手动三选一直/30 分钟/离开接回，`takeover_opt_from:` 全局关也接回、桌面种子 `takeover_rearm.enabled=false`（D-M9）；5 例） | 交 M-4：`_l2AutoBump` 让位 + `.is-active` 样式；交 M-6：既有 5 处红（`explicit_newline_only` 孤儿 / `assistant/actions.py` 未登记 / `ghost_ttl` 两例 / M-1 在途未定义名 / kill_switch 串味）见落点表；边车回读 `verified` 误判若再现单开工单 | `发版对账_v1.0.76_M2.md` | ≈$150 |
| **M-3** | 媒体发送链 | #227 #228 #229 #231 钧#215 | $150 | **收工（后端待装载）** | A `7899cc9a`（真根因＝admin.py 全局 2MB body 闸拦 send-media，>2MB 一律 413+掐连接、后端零记录；豁免 + 裸流落盘 + Content-Length 预检 + 视频压 50MB 同源 + `[http]`/`[send-media]` 访问日志；前端半被 `ece8a0aa` 卷走）/ C `c943261e` / B `04ad5bbf`（.MOV 有随包 ffmpeg 自动换封装 MP4，D-M4 两步同批）/ D `b80a3907`（四分类 + 探活自动重试） | 真机四平台 12 次矩阵（本机上传层版已过、见落点表）/ 钧 #215 包未到，规则与先验在落点表 / D-M5 放回 50MB / 其它 8 个 multipart 上传口同吃 2MB 闸→立单 | `发版对账_v1.0.76_M3.md` | ≈$95 |
| **M-4** | 工作台第三轮 | #222 #221 #230 #206补 | $150 | **收工**（09-07 01:1x） | A `b9ea59f3`（角标 = 当前 scope 内能点开的未读 + 悬浮「另有 群/频道/归档」+ `/account-unread?scope=` 同 WHERE + 群/频道视图说明条 + 看门狗 `[buried] startup_sweep` 72h 存量清扫 + 横幅「全部标已读」+ 被埋计数口径统一）/ B `898715ac`+修正 `4358ca4b`（`wsNav`/`wsNavUrl` 带 ?theme=&lang=，30 处裸导航替换 + window 捕获相位链接改写 + `__wsGoHome` 补 url；胶囊页内筛选钩子 **`window.wsFilterAutoConversations()`**（接口名，行为归 M-2 E，缺席回落带 theme 跳收件箱）；徽标改回「旗舰版」override 进悬浮（闭 L-4 待老板②）；顶栏收缩四档；主题分段控件本就在头像菜单）/ D `198d1e17`（会话工具整块删除、状态卡迁 AI 状态弹层、认领回头部、manifest convops app-only）/ C `dfa46687`（多选工具条「已选 N ｜ 归档·标签·删除 ｜ ✕」+ 批量删除同门同端点）；新测 38 例绿。⚠ B 提交误卷 M-3 C 两处在途 hunk，`4358ca4b` 已只在 index 回退、工作树未动（M-3 随后自提）。.py 四处待装载（`unread_aggregate` / `health_watchdog` / `unified_inbox_read_routes` / `unified_inbox_account_routes`） | 无（三档顶栏截图装机后取；`unified_inbox.html` 剩 4 处裸 `location.href=` 在 M-2/M-3 热区，ratchet 天花板 4，谁动谁改） | `发版对账_v1.0.76_M4.md` | ≈$95 |
| **M-5** | 产品清理与上游 | #217 #224 #226 #225 #220 | $120 | **收工**（09-07 01:1x；`.py` 待装载，模板/i18n/copilot 组件已热更） | A `b5c24bc8` 转化成交类目 client 隐藏（`/templates` 不下发 + 新建 403 + 存量卡标下线）+ `goals/product_guard.py` 无产品禁报价/开户/支付（`goal_no_product` 拦截）+ 首拍进 L1（`reason=goal_first_send`）/ C `169bcb6e` 出图 client `dev_state` 开发中态 + `[image_gen] probe` 落日志 + 去「联系运维」/ D `40249ce5` 工具箱转录默认复用工作台 `voice_transcriber` + 收件核查 `received` + `err.asr.*` 六分类 + DATAS 补 `assets/probe` / E `1090c4cf` 学习队列 `query_lang` 按文字系统 + 原语真实标注 + 未译标「重试」+ 同题合并 / B `0a19204e` 养号 `summary` 判词与计数同口径 + 灰置原因就地 + 三阶段人话说明 / A 追问①② `d55f8ebb`（老板拍板）生命周期自建用户版也闸 + 回复链注入块加「产品边界」行（首拍预览不扩到回复链；出图措辞不分两套） | 交 M-6：三个宿主的 `?v=` bump（宿主文件归 M-2/3/4）、#225 夹具随 1.0.76 打包才生效、`zh_hant_auto.py` 再生 | `发版对账_v1.0.76_M5.md` | ≈$100 |
| **M-7** | 目标引擎可追溯与真出手（09-07 02:5x 追加） | #236（#166 族第五次） | $150 | **收工**（09-07 07:4x；随 **1.0.77**；`.py` 十四处待装载，`cp-goal.js` 双树/三宿主 `?v=20260907e`/i18n 已热更；09-08 02:3x 接手收尾：skuio 三启取证落 §五 + 意向板 `-Done`） | **§2 取证真因**：59 稿零 `[goal-inject]` 不是 disabled DEBUG 也不是没调注入——`goals/*` 全包 logger 用根级名（`GoalService` 等），`logging_setup` root 钉 WARNING 只放 `src.*`，INFO 在源头被丢，J-2 测试用 `caplog.at_level(logger="GoalService")` 显式抬级所以绿；`sent_24h=4` 实为 2 次（钩子每发写 `beat_sent`+`care_sent` 双计）、卡片「2 拍」是两次被动注入 `consumed`；自然档到期不进即时告警且 `goal_miss_alert` 只到 webhook；cap 全仓只在 inject 路径读＝只拦被动注入。A `d0933356`（事件表三追溯列 + `beat_blocked` 槽位去重 + `GET /api/goals/{id}/beats` 经 care#→deferred→inbox 反查投递真相/消息 id + 卡片「已推进 N 拍」可点开每一拍 + 看门狗/卡片同数 `beat_sent`）/ B `9ff2e90a`（十 logger 迁 `src.companion.goals.*`；disabled/no_goal 每稿 INFO 60s 节流 + `last_goal=` 尾注 + `goal_expired`；被动注入不受 cap、主动侧记 `beat_blocked(pace_cap)`；`[goal-state]` created/updated）/ C `fe6a7861`（终态当刻 `settlement` 事件 + `goal_settled_alert` 进 SSE/铃铛不经聚合；卡片「已到期·查看结算」7 天 + 重建时「上一个目标已结算」）/ D `eeec70f1`（`auto_state` 八态：「自动推进中」只在近 24h 真发 ≥1；等待首拍/今日上限/被 X 拦/红字 stalled；看门狗逐目标点名 goal_id+会话）；三份 #236 测试 35 例（12+8+15，09-08 在 HEAD 复跑绿；首版写 40 是笔误）+ 相邻回归 1217 绿；gate_sweep 8 红全是 20–48h 前的他线旧红。**活体核对**（09-08 02:0x，本机 zhiliao 18799 已热重载装载 A–D）：`tools/verify_goal_card_live.py` API/生产日志/真浏览器三段 **RESULT: OK**——`[goal-state] created/updated` 真出现在生产 `app.log`、`[goal-sweep]` 8 月起首次可见（问 1 真因实证）、卡片按真相说话、通知中心/i18n 热更在线；顺手对齐修正 `bf8523b8`（「查看消息」接宿主既有 `__wsFocusConv(conv, mid)` + iframe 桥）。**skuio 三启取证**（GTNG25 / X6JMGZ 04:17 / 04:19，落点表 §五）：04:01:37 / 04:12:13 / 04:12:57 三次皆**壳整体拉起**——Messenger 边车 `启动 bundled` 与新后端 pid 三次 1–3 s 成对、无 `边车退出` 行、`fatal_traceback` 三段空白、后端自退会走 `scheduleRespawn` 且边车不动（形态不符）→ **非后端自杀**；04:01 已是 **1.0.76 首启**（boot 段 `dry_run=True ← 启动快照` 尾注由 `fe8f7a8a` 09-07 00:32 加入，1.0.75 树 0 处 / 1.0.76 树 2 处；距 03:58 回访 3 分钟）；04:12 两次是同版本**有序关开**（`renderer.log` 显示后端先停 14–21 s 再随壳起，更新器 / 热补丁无自动 relaunch 路径）。取证缺口：打包态壳主进程 `[backend]/[updater]` 行无落盘、`zl_collect` 关键词过滤掉哨兵行且不收 `run_sentinel.json` | 交下一收口：真机五项（落点表 §二）；到期结算铃铛需等真到期；`sent_hook` 带正文可省反查；#216 装 1.0.77 见 `[goal-inject]` 行即可 verified；**新增**：壳主进程日志落盘 + `zl_collect` 收哨兵 / boot 窗不过滤（§四 ④）；X6JMGZ ③「推进节奏每日 1/2/3 次」是功能需求另立单；三启回访口径见 §四 | `发版对账_v1.0.77_M7.md` | ≈$145 |
| **M-6** | 值守收口 | 钧 #215 + 22 单 + 1.0.76 | $100 | **收工**（09-07 04:2x；1.0.76 三渠道已发、坐席三台 1.076、回访四条已发、20 单 fixed + 记账） | C：01:54 zhiliao 装载（16s）+ Messenger 边车重启（`restoreAll summary restored=1`）；全量回归 79/25637（新红 9 全门禁/旧断言/域外副作用，0 拦发版，归属落点表 §4.1）；gate_sweep 10/794（新红 3：M-2 E `takeover_rearm` 未登记 + **三哑按钮** `_acctBulkMode`/`_gateClearDegraded`/`batchDeletePrompt` 代补暴露 `527700d8`，同 commit 代登记 M-2 B 两端点路由清单 + M-5 交办 `cp-*.js?v=`）；release `6403463d`（bump / 五值 496 MB sha 82dec80a… / changelog 11 条 / 繁体 16539 / ui-build 0300）；三包 smart 590.8M / clean 519.6M / **lite 522.9M 首次实弹**；publish 公共 + internal + lite 全 OK、官网 gate:content 6/6 + deploy OK；坐席 **三台 UP 1.076 smart/internal**（yunsheng 后端随重装恢复）；播报群 mid 1289 / 频道 msg 153；`update_hint` 已设。A：钧日志包仍未到 → 不钉根因、钉「两支都在 1.0.76 关掉，装后重发即结」mid 1292，#215 保持 confirmed。B：对账表 1.0.76 版 + 21 单口径 skuio mid 1290/1291、钧 mid 1293；20 单 dry-run→apply fixed、`duty_notify_summary` 记账 20；#216 因 02:27 新单 #236 反证**不标 verified**（随 M-7）。症状族：新立族 8 通道真相/登录默认 + 族 2/4/5 补层；合并 `发版对账_v1.0.76.md` | 交下一班：两机装 1.0.76 后核六样；钧日志包到后补 a/b 并流转 #215；61 单 8 月老积压回访（`r_*_backlog_1075.txt`，今日配额已用）；`dist-lite\latest.yml` 残留可删。交回各线：回归新红 9 条（M-1 B ×5 含文档翻译单字不译副作用 / M-1 C / M-2 A 重发幂等裁决顺序 / M-2 E / M-4 B）；M-3 交办其它 8 上传口同吃 2MB 闸 → 1.0.77 立单 | `发版对账_v1.0.76_M6.md` | ≈$75 |

## 老板拍板记录

（默认值见底座「老板决策」表；老板改口在这里追加一行：日期 / 决策号 / 改为）

- 09-07 01:2x / D-M6 补三问（M-5 A）/ 按 M-5 建议：① `auto_create` / `retention_auto` / `winback_auto` / `reconvert_auto` 在用户版也不建「转化成交」类目模板（`lifecycle_template_blocked`，按部署形态判）；② 首拍预览**不**扩到客户来消息时的顺势推进，改把「无产品禁报价」以产品边界行补进回复链注入块（direct/close 降 soft）；③ 出图失败卡「客服」措辞不按 partner/internal 分两套。落 `d55f8ebb`。
- 09-07 07:4x / M-7 四项默认（待老板过目，不同意划掉）：**D-M7-1** 拍数上限只管主动出手，客户来消息时的被动注入不受限；**D-M7-2** 到期结算对 `done` 只记摘要不推铃铛（已有达成通知）；**D-M7-3** 人工标终态只记摘要不推；**D-M7-4** goals 包日志迁 `src.*` 命名空间（生产 INFO 可见，每稿一行、60s 节流）。落 `d0933356 / 9ff2e90a / fe6a7861 / eeec70f1`，详见 `发版对账_v1.0.77_M7.md`。

## 全批收口（M-6 做）

- [x] 六条落点表齐 → 合并 `docs/发版对账_v1.0.76.md`（09-07 04:1x）
- [x] `restart_preflight`（01:53 GO，首跑 NO-GO 两项已消化）→ zhiliao 装载 01:54:03 + Messenger 边车 01:54:47 → 全量回归 79/25637（0 拦发版）+ gate_sweep 10/794（三哑按钮代补 `527700d8`）
- [x] 1.0.76 三包（smart / clean / lite）→ publish 公共 + internal + lite 三渠道 + 官网 deploy → 坐席三台 1.076 smart/internal → 两机核版本 **交下一班**（两机在内测渠道自更或手装，六样核对项见 M-6 表）
- [x] 台账：20 单标 fixed（dry-run 过目 → apply；#215 保持 confirmed 等日志包，#216 随 #236 / M-7 不标 verified）；回访 skuio 2 条（mid 1290 / 1291）+ 钧 2 条（mid 1292 / 1293）；对账表 1.0.76 版 `tmp/mapping_c6efrs_1076.py`（三档：已验 ✓ / 1.0.76 再修 / 口径不变合并行）；`duty_notify_summary` 记账 20
- [x] `docs/值守症状族对账单.md` 更新：**新立族 8 通道真相 / 登录默认**（5 层）+ 族 2 +7（#234）/ 族 4 +19–21（#227 #229 #231 #228）/ 族 5 +5–6（#218 #214 / #217）
