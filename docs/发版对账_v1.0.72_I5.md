# 发版对账 v1.0.72 — I-5（老单清理：#145 残余 + #32 四小件）

> 指令：`docs/指令_I-5_老单清理_2026-09-04.md`。总账：`docs/I批_进度总账.md` I-5 行。
> **本条不发版。** `.py` 改动攒批等下个重启窗装载（E1 的 orchestrator 日志行、E4 的 inbound_enrich / peer_delete_purge、E3 的 persona_guard / site_catalog 均需重启才进生产进程）。
> 下一个账号接手：先读本表逐行核对，再跑 `git -C D:\boundless log --oneline -15 -- engines/chengjie` 确认 commit 真在。
> ⛔ 未碰：`store.py`、`unified_inbox.html`（属 I-2）；`planner.py` / `service.py` / `sprint_ticker.py` / `pace.py`（属 I-3）；drafts/autosend/chat_assistant（I-1）；`spoken_style_bridge.py`（I-4）。

顺序实际执行：E2 → E3 → E4 → E1 → E5。

## 落点表

| 件 | 工单 | 结论 | commit | 改了哪些文件 | 测试结果 |
|---|---|---|---|---|---|
| E2 工具箱克隆声 | #145① | **已被 B1 顺带解决，本条不施工**。文案已拆「疑似无声 / 疑似念错」；日语乱音不再凭能量放行；自动链成品判念错即作废。需 I-6 走用户验收 | （本条无 commit）覆盖 commit=`929ba165` `23cb7dba` | 未改。判定文案在 `shared/copilot/i18n/cp-i18n.js`（`cp.voice.silent_note` / `garbled_note`）与 `src/ai/speech_verdict.py` | 未复跑 B1 测试 |
| E3 人设产品目录串味 | #145③ | **修了**（注入层 + 出站守卫）。现网 ChatX/LingoX 刻意不填 `personas:` 绑定＝共享，行为与旧版一致 | `bdae979e` | `site_catalog.py` / `offers.py` / `persona_guard.py` / `site_catalog.yaml`（仅字段注释）/ **新** `tests/test_persona_catalog_isolation.py` / `test_persona_guard.py` | `test_persona_catalog_isolation` + `test_persona_guard` **68 passed** |
| E4 手机删消息 | #32② / #145⑤ | **修了后端记得但不主动提**。工作台同步删走既有 `deleted_by=peer` + SSE `messages_deleted`（`unified_inbox.html` 已听，本条未改）。情景记忆保留；A 线历史留槽位换成占位句。出站 `sanitize_outbound` **当库函数测过，未挂发送链**（挂必须带 inbound，否则误伤「对方又提了日本」；发送链属 I-1） | `60c2d428` | **新** `src/inbox/withdrawn_cite.py`；`peer_delete_purge.py`；`inbound_enrich.py`；`tests/test_peer_delete_memory_purge.py`；**新** `tests/test_withdrawn_cite.py` | 两文件 **45 passed**。金标「我下周去日本」五轮稿不得主动出现「日本」 |
| E1 LINE 媒体 | #145② | **部分完成**。探针改拣 online 号 + 入站落盘验 magic（JSON 信封即红字非零退出）；群聊跳过补 INFO。真机：指定在线号图 Keep memo 送达且落盘 `FFD8` JPEG。**语音 / 视频 / 贴纸双向未跑**（探针 CLI 目前只覆盖图）；`groups=false` 是默认，群里发图工作台当没图——不开闸（产品决策） | `96cf53cd` | `tools/probe_line_media.py`；`account_orchestrator.py`（群聊跳过 INFO 一行）；`tests/test_line_media.py` | `tests/test_line_media.py` 绿（与 E5 合计 105 passed） |
| E5① 无意义悬浮副驾 | #32 | **未删**。指令要求先列清单等确认。见下方清单 | — | 未改 | — |
| E5② 「需人工 6」点不进 | #32 | **已被 B2 覆盖，不重复施工**。B2 修账号栏未读口径 + `GET /api/unified-inbox/account-unread` 可点直达。chip「需人工」在 `unified_inbox.html`（`ftab-attn` / `setFilter('attn')`）属 I-2 | （本条无 commit）覆盖 commit=`2f8fc70f` | 未改 | 交 I-6 验收；前端仍坏交 I-2 |
| E5③ 群组自己消失 | #32 | **只加观测不猜修**。采集链记下群/频道条数，变化打 `[inbox-list] group count changed X -> Y total=Z` | `96b59d73` | `src/inbox/channel_adapters.py`；`tests/test_channel_adapters.py` | `test_group_count_watch_logs_on_change` 绿。前端折叠在 `unified_inbox.html`，交 I-2 |

## E5① 悬浮元素清单（等确认再删，一条都没动）

| # | 位置 | 是什么 | 建议 |
|---|---|---|---|
| 1 | `base.html` / `workspace_base.html` 的 `assistant-ball.js` | 小智：问答 / 报障 / 我的工单 / 术语 / 引导 | **留**（有功能；旧 `?` 帮助球 2026-08-21 已退役） |
| 2 | `unified_inbox.html` 业务助手撕出窗 `_cpTear*` / 「收回全部悬浮小窗」 | 右栏撕成小窗 | 有功能；要动交 I-2 |
| 3 | `unified_inbox.html` PiP `_cpPipToggle` | 系统置顶小窗装 `/copilot/app.html` | 有功能；要动交 I-2 |
| 4 | `unified_inbox.html` `.nav-tip` | 账号/后台/主题导航悬浮说明 | 有功能；要动交 I-2 |
| 5 | `unified_inbox.html` toast / 诊断遮罩 | 发送反馈、死按钮红条 | 有功能；要动交 I-2 |

## 交 I-2（`unified_inbox.html` / 壳，本条不能改）

1. **E4**：手机删消息后气泡是否按 `deleted_at` / SSE `messages_deleted` 隐藏。监听已在该模板（约 L26327），若工作台仍不同步，核这一段。
2. **E5②**：若「需人工」chip 点了列表不变，核 `ftab-attn` / `setFilter('attn')` 与账号栏 `account-unread` 是否同口径。
3. **E5③**：群组动态折叠 / `isGroupConv` / `filteredGroups` 都在该模板；后端只打数量变化日志。
4. **E5①**：上表 2–5 若确认要删，落点全在该模板。

## 交 I-3（`service.py` 本条不能改）

`pick_products` 已接受 `persona_id`，调用在 `service.py` ~L1278 **目前不传**（回落 `fields._persona_id`，现网目录未绑人设故行为不变）。接线：

```
prods = sc.pick_products(
    catalog, prof_fields, pinned=pinned, sold=sold,
    persona_id=_account_persona(cfg_root, platform, account_id))
```

同文件若调 `build_catalog_block` / `pick_offer`，一并传 `persona_id=`。出站守卫（`persona_guard.sanitize`）不依赖这一行也能剥他人设产品名。

## 交 I-6（用户验收）

- E2：工具箱生成 vs 人设池试听，同人设同文本 → 同一音色、同一判定（不再笼统「音频异常」）。
- E5②：「需人工 N」能否点进那 N 条。
- E1：钧侧 **私聊** 图是否仍挂（Keep memo 图链已通；群聊默认不收下媒体）。

## 结束报告（五件）

- **E2**：不施工。B1 已覆盖「疑似无声误标」最可能根因。
- **E3**：注入层按 `personas:` 过滤（空=共享）；出站守卫挂既有 `persona_guard.sanitize`。规划链接线交 I-3。
- **E4**：记忆保留 + 历史占位 + prompt 钉子「不得主动提」；对方本轮又说到同一件事则不注入、出站不剥。工作台删条交 I-2 核 SSE。`sanitize_outbound` 未挂 I-1 发送链。
- **E1**：探针认在线号 + magic 纪律落地。图真机过。语音/视频/贴纸双向**没做**，不要假装四类都过了。
- **E5**：① 清单待确认；② B2 已覆盖；③ 只加观测。
