# 指令 N-1：主动关怀「立即发」真投递——点了就发、发了就说（#243，并 #244–#249）

> **预算硬上限 $120。** 正常花费 $60–90。花到 $100 停下写落点表 + 总账。
> 类型：**关怀派发链（M-1 A 的收口缺口）。** 证据 `tmp_diag\{B5TAA3,G7KEUT,GGUNG4,NRTDJH,WZPMXF,8VGQDM,RGPTQF,TXP5G3,5CM8QB,FHSZTA}\`，**FHSZTA 已把三层根因写全，照它做，不必再取证。**
> 为什么 P1：1.0.76 新关怀页 15:27:39 切真发（dry_run=False，M-1 A 的单一真值生效了），15:28:22 原文直发入队（verbatim 生效了），用户 15:28:31/50/58 点「立即发」三次 → 后端只记 `decision=bring_forward` 且**没改 `due_at`**，派发循环 15:29–16:29 一次没捡；16:34:52 到期后首轮 tick（晚 5.5 分钟）捡到却**不发**，入 deferred 队列 `send_in_min=13`，同秒队列才懒初始化；预计 16:47:52 出队，到 16:53:57 **零出队 / 零投递日志**。界面全程只显「到点发」。三层各扣一段，用户等了 85 分钟。M-1 把「说什么」修对了，「什么时候发、发没发」没人管。
> 默认按底座 **D-N4** 执行。

---

## 0. 开工前必读

1. `docs/修复指令_N批_并行_2026-09-07.md`：文件归属 / D-N4。
2. `docs/发版对账_v1.0.76_M1.md` A（关怀 verbatim 默认 / 预览 / 档案校验 / dry_run 单一真值 / `[care-gen]` 日志——本条在其上做派发与立即发）；`docs/发版对账_v1.0.74_J8.md`（#182 两档）。
3. 代码位置：`src/contacts/care_dispatcher.py`（tick / 到期判定 / deferred 入队 / bring_forward 处理）、`src/integrations/companion_proactive.py`（派发循环 `interval=600s enabled=… dry_run=…` 启动行——**13:14 启动 `enabled=False`，15:27 运行时切真发后循环是否真在跑要先答**）、`src/web/routes/care_routes.py`（「立即发」端点 → `bring_forward`）、多平台 deferred 队列（grep `multiplatform_deferred` / `send_in_min` / `deferred=1`）、关怀页模板（运行中·真发 / 关怀方案卡）。

**读完直接从 F（一问）开工，再 A→E。**

## 1. 红线与边界

- 提交只用 `git add <明确文件>`；提交前 `git diff --cached --name-only` 为空再 add；⛔ `git add -A` / `-a` / `reset --hard` / `checkout .` / `restore .` / 裸 stash。
- 不动 `autosend_worker.py`（M 批刚收口）；关怀发送复用现有投递适配器，不新开发送面。
- 不改关怀内容生成（M-1 A 已定）；本条只管**时间与投递真相**。

开工登记意向：
```
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "N-1 care send-now: synchronous direct send + bring_forward due_at=now + no jitter on manual time + deferred queue init/logging + card state machine (#243)"
```

---

## 2. F —— 先答一问：派发循环到底跑没跑

13:14:52 启动行 `proactive_care 派发循环已常备(interval=600s, enabled=False, dry_run=…)`；15:27:39 运行时切「真发」。要答：`enabled=False` 时循环是**不启动**还是**空转**？运行时切开关后有没有 tick 日志（G7KEUT 问的）？答案决定 D 项是「补启动」还是「补日志」。写进落点表首行。

## 3. A —— 「立即发」= 同步直投（D-N4 ①）

`care_routes` 立即发端点改为：直接走投递（与派发循环同一 `send_care(item)`），**同步返回**结果；成功 → 事件 `care_sent` + 卡片「已发出 hh:mm」；失败 → 卡片显原因（通道未连接 / 冷静期 / 拦截原因），不静默。按钮点击后禁用直到返回（防重复 ×3）。

## 4. B —— bring_forward 至少置 due_at=now + 立即 tick（D-N4 ②）

若产品仍保留「提前到最近一拍」语义：`bring_forward` 必须把 `due_at` 改到 now 并立刻触发一次 tick；卡片显「已提前，预计 hh:mm 出手」。（A 做了同步直投后，本项只作为兜底路径。）

## 5. C —— 错峰不作用于人工指定时间（D-N4 ③）

原文直发 / 立即发 / 用户指定时刻的条目不加错峰偏移；自动生成的关怀才错峰。卡片对有偏移的条目明示「预计 hh:mm（错峰）」。

## 6. D —— deferred 队列：启动即初始化 + 出队落日志 + 查 16:47 未出队

- 队列随后端启动初始化（现在懒加载到首次入队，日志 `多平台 deferred 队列已就绪 interval=120s` 出现在 16:34:52 即证）。
- 出队 / 投递 / 失败每条落 `[care-deferred]` INFO；查 16:47:52 预计出队为何零记录（interval 120s 的 tick 没跑？条件不满足？异常被吞？）——写进落点表。
- 到期 tick 若决定「不发而入 deferred」必须给原因日志（为什么不当场发）。

## 7. E —— 卡片状态机 + 防重复（D-N4 ⑥）

已排（hh:mm）→ 已提前 → 排队中（预计 hh:mm）→ 已发出 hh:mm / 失败（原因）；每个状态有时间戳；「立即发」按钮在非「已排 / 排队中」状态不可点。

## 8. 验收

- `tests/test_care_send_now_243.py`：立即发同步投递并返回；bring_forward 置 due_at=now；人工时间无错峰；deferred 出队有日志；状态机迁移。
- 真机（交 N-5 回访）：skuio 机给 Kxhm 排一条原文直发 → 点立即发 → 1 分钟内客户端收到、卡片「已发出」；到点自动那条在到点 ±2 分钟内发出并有日志可查。

---

## 9. 收工

- 落点表 `docs/发版对账_v1.0.77_N1.md`（首行写 F 的答案）；总账 **N-1 行**。**每个 commit 落地后立刻补落点表一行。**
- **交 N-5**：#244–#249 并入 #243 的口径；回访口径「立即发点了就发、卡片会写已发出几点或为什么没发；到点的那条也会准点」。
- 收工清意向板 `-Done`。

**开对话粘这段：**

```
执行 N-1（主动关怀「立即发」真投递：#243，并 #244–#249）。先读 D:\boundless\docs\N批_进度总账.md 与 D:\boundless\docs\修复指令_N批_并行_2026-09-07.md（决策 D-N4），再读 D:\boundless\docs\指令_N-1_主动关怀立即发_2026-09-07.md，先答 F（派发循环 enabled=False 时跑不跑、运行时切真发后有没有 tick），再 A 立即发同步直投 → B bring_forward 置 due_at=now → C 人工时间不错峰 → D deferred 队列启动初始化 + 出队落日志 + 查 16:47 未出队 → E 卡片状态机 + 防重复。证据 tmp_diag\FHSZTA（三层根因已写全）5CM8QB G7KEUT，不必再取证。不动 autosend_worker.py。每项一个 commit 带 #243；每个 commit 后立刻补落点表一行 docs/发版对账_v1.0.77_N1.md；预算上限 $120。
```
