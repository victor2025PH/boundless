# 实施94 · 0901 凌晨/中午批修复 —— 值守群内验证引导（修复线 → 值守线交接）

修复线 2026-09-01 完成 #133/#134/#130/#131/#132 五单 + #121 返工 + #93 一批，
处置台已全部标 fixed（自动回访已发，notified=true）。**发版前请值守按本单在
群内做验证引导**；各单生效载体不同，引导时注意别让用户在旧版本上白测。

## 生效载体

| 工单 | 修复位置 | 到达方式 |
|---|---|---|
| #133 #134 #131 #93 #132 #130 | 引擎侧（backend） | 随下一版桌面包（1.0.67）；内部实例随重启窗生效 |
| #121 | 前端 cp-voice.js（?v=20260901a，ui-build 20260901-1617） | 随下一版桌面包；网页工作台随引擎侧热区推送 |

⚠ #121 教训回写：0831 那次修复实际落仓晚于 1.0.66 封包约 1.5 小时，fix_note
却写了「随 v1.0.66」——本批所有回访话术统一口径「随下一版」，发版后才引导复测。

## 各单验收口径（群内引导用）

### #133（P1 · skuio）主动消息发中文
- 验收：**英文会话的任何主动消息（关怀/SOP 步/目标推进）不得出中文**。
- 引导：请 skuio 升级后把 viva Mexico 会话右侧「跟进 SOP」自动档恢复，观察
  24h 内主动消息语言；如仍见中文直发，要截图+时间点（总出口有日志可对账：
  `[orchestrator] 出站语言已按铆定修正` / `lang_hint_conflict` 计数）。
- 机制一句话：发送总出口补了「客户语言画像」回落判定，不再依赖每条链自觉接翻译。

### #134（P2 · skuio）同链双实例
- 验收：**一会话一链至多一个运行中实例**；升级后存量重复自动清理（留最早那条）。
- 引导：升级后打开 viva Mexico 的「跟进 SOP」面板确认只剩一个实例；再手动
  对同一会话重复点「启动」应提示已在运行/不再新增。

### #130（P1 · 钧）语音时长 6 倍拉长
- 验收：**对端实收时长==工作台时长，且播放不变速**。
- 引导：请钧升级后重发一条 5-10 秒克隆语音给测试对端，对比两端时长；顺带
  把值守之前问的「35 秒那条是不是慢放、变低变粗」补上（是→佐证 48k/8k 定性）。
- 若仍复现：拉工作台日志搜 `#130 WAV 采样率标头失配已修复`（有=已拦截过）。

### #121（返工 · skuio/钧）疑似无声红条误报
- 验收：**有声可播的音频不再亮「疑似无声」红条**；真静音空壳照拦。
- 引导：升级后在工具箱用同样文本重新生成试听——红条不应出现；如出现，
  按 F12 查红条元素 `data-detector` 应为 `v2-dual`（不是=前端没更新成功）。

### #93（一批 · 钧）工具箱回落标准音色
- 验收：**克隆档人设在工具箱生成必用克隆声**；万一回落必亮黄条+发送前确认，
  绝不静默换声。
- 引导：请钧在女王会话工具箱不选人设直接「生成语音」，看「实际使用」应为
  「人设克隆声」；若显示标准语音则必须同时出现黄条说明（截图黄条文案即可归因）。

### #131（P2 · 钧）人称/角色错乱
- 验收：复述 967 图场景（客户以卖方身份说「给你爸买个车，再找我买车险」），
  AI 不再把自己代入卖方/报价方。属 LLM 行为改良，引导用户日常观察即可，
  单次复测不作硬验收；复发继续截图归 #109 家族养证据。

### #132（P2 · skuio）SOP 页裸配置键
- 验收：运营界面不再出现 `inbox.cta.public_base`/「反代」字样；提示变成
  「转化目标短链还没启用」+ 页面内网址填写入口，填 https 地址保存即启用。

## 已核未修（知会）
- 报障实例（zhiliao）app.log 里持续刷 `zh_hant_auto.py '{' was never closed`
  热重载警告：仓库文件已被并行线于 0901 15:38 重新生成修复，实例侧待下次
  推送/重启后消失，与本批工单无关。
