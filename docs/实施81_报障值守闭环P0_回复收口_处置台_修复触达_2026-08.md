# 实施81：报障值守闭环 P0——回复收口 / 工单处置台 / 修复触达 / 值守缺位安全网

> 日期：2026-08-28 ｜ 状态：**已实施（工具/计划任务已生效；引擎 .py 待重启窗装载）**
> 关联：实施74（报障闭环 P0，其 §6.1 推迟的「客服工单处置页」本批落地）、
> 实施49/68（值守台账）、值守交接_20260827（对接方式 SSOT）
> 缘起：老板问「173 监控群的回复按钮 / 解决方案 / 热更新提示做好了吗」——
> 五视角分析结论：监听与登记扎实，**回复停在手写一次性脚本、修复触达差最后
> 一根线、值守缺位无安全网**。本批按该分析的 P0-P1 优先级实施。

## 0. 一句话

值守回复从「每条现写一个 tmp 脚本」收口成 CLI + 处置台按钮；工单标 fixed 时
回访文案带「本次改动 + 获取方式」；hotpatch 推完提示补发回访；173 会话断掉时
有计划任务兜底告警——「回复、修复触达、缺位监护」三条断链本批全部接通。

## 1. 交付清单

### P0-1 值守回复 CLI（tools/duty_reply.py，立即可用）

- `python tools/duty_reply.py --group neice --text "..." [--ticket N] [--status confirmed] [--dry-run]`
- token 从实例配置读（不再散落 tmp 脚本）；`client_msg_id` 自动唯一（旧固定
  id 重跑被 send_dedup 静默吞的坑）；打错群哨兵（不在 `bug_intake.groups` 拒发）。
- **双路径**：带 `--ticket` 优先走产品端点 `/api/admin/bug-intake/{id}/reply`
  （@报障人 + 工单号 footer + note 回写全在服务端）；端点未装载（重启前）
  自动回落 `/api/unified-inbox/send` + 本地直写工单 note——重启前后同一条命令。
- send 回落路径发完自动拨回 `auto_ai`（接管即静音的反向操作，值守 SOP 机制化）。
- 每次发送落 JSONL 台账 `<数据根>/logs/duty_replies.jsonl`。

### P0-2 工单处置台（/admin/bug-tickets，重启后可用）

- 列表（状态/严重度徽章、状态筛选）+ 汇总 pills（开放/P0P1/24h/回访待发）
  + 详情（台账正文、截图内嵌预览——实施74 只做了取图端点没有消费面）
  + 动作：状态流转（带修复说明/更新引导输入）、一键回访、**群内回复按钮**
  （快捷模板 4 枚：收到排查/已登记/要信息/修复请验证——只填不发）。
- 新端点 `POST /api/admin/bug-intake/{ticket_id}/reply`（发送成功才回写台账，
  发送失败 502 不回写）；nav「安全合规」组新增「报障工单」（master_only，
  客户形态隐藏 `CLIENT_HIDDEN_ITEM_IDS`）；i18n 独立 pack
  `bug_tickets_page.py`（zh/en 齐平，状态标签与 `VALID_STATUSES` 门禁钉住）。

### P0-3 修复→更新引导联动（P1-4 修复说明字段一并落地）

- `bug_tickets.fix_note` 列（幂等迁移）：标 fixed 顺手写「本次改动」一句话；
  空值不抹已写说明（回访重试场景）。
- `bug_intake.update_hint` 配置（example 缺省空=行为不变）：回访文案按需带
  「📦 获取方式」段（如「已推送热补丁，重启智聊即生效」）；`/status`、
  `/notify` 均可按单覆写。**缺省不带更新段是刻意的**——服务端修复即时生效，
  硬塞「请更新」是误导。
- `push_chatx_hotpatch.ps1` 推送成功后自动列出「fixed 但回访未送达」积压
  （新 CLI `tools/duty_notify_pending.py`），`-NotifyPending` opt-in 直接冲刷
  ——「补丁到机」和「告诉报障人」连成一条线，默认不自动发（真发群消息）。

### P0-4 值守缺位安全网（已注册计划任务，立即生效）

- `tools/duty_watchdog.py` + 计划任务 **DutyGroupWatchdog**（每 10 分钟）：
  报障群任一客户消息超 30 分钟无人应答 → 告警 @Sousaun（复用坐席日志监控的
  bot→实例双通道）。判定口径：支持号出站 / 员工亲号入站都算「已应答」；
  每条消息只告警一次（state 48h TTL）；纯截图报障（无文字）也算提问。
  卸载：`schtasks /Delete /TN DutyGroupWatchdog /F`。
- 哨兵入库：`deploy/duty/group_sentinel.py`（正本；水位缺省
  `D:\chengjie-instances\.ops\group_sentinel.pos`，运行态不写脏工作树）。
  173 值守下次交接改挂仓库路径，`C:\Users\Administrator\tmp_group_sentinel.py`
  退役。B123 断线补拉不重做（他线 `bug_intake_backfill.py` 已上线）。

### P1-3 已知问题周公示（tools/duty_known_issues.py，立即可用）

- 从工单台账生成「🔧已修复（带修复说明）/🚧处理中/🆕新报待确认」群公示；
  标题净化（VLM 图片摘要多行压单行）、窗口过滤（陈年 new 不刷屏）、每节
  封顶 10 条 + 折叠；`--send neice` 显式 opt-in 真发。建议每周一值守跑。

## 2. 测试与验证

| 面 | 结果 |
|---|---|
| 新增门禁 | `test_bug_tickets_console`(13) `test_duty_reply`(4) `test_duty_watchdog`(11) `test_duty_known_issues`(6) 全绿 |
| 存量回归 | `test_bug_intake`(51) 全绿；nav/i18n/模板结构族 352 全绿 |
| 路由清单 | 新端点已登记 `_ADDITIONS_2026_08_28_BUG_TICKETS`；受众台账 `bug_tickets` 已声明（CLIENT_HIDDEN） |
| 实弹冒烟（只读） | duty_reply --dry-run（数据根/payload 正确）；duty_watchdog --dry-run 对生产两群判定通过；duty_notify_pending 读真台账；duty_known_issues 出真公示（并当场抓到 fix_note 未迁移的旧 schema 崩溃 → 已改 SELECT * 兼容） |
| 计划任务 | DutyGroupWatchdog 已注册（对照 SeatLogMonitor 先例），投递模块加载 + bot token 可读已验 |
| ps1 | push_chatx_hotpatch / run_duty_watchdog PSParser 零错 |
| 广域 sweep | **ALL GREEN 1177**（首轮 3 红全是本批新面触发的棘轮，已修：新页补 howto 条目 `bug-tickets-console`（缺口回 10，检索评测 20 例不受新关键词误伤）+ badge 蓝改 `color-mix(var(--p))`（tailwind 裸蓝台账）） |
| ui-build | 双 bump 至 `20260828-2322`（severity=silent，管理页坐席无感） |

### 生效时机

| 改动 | 生效 |
|---|---|
| duty_reply / duty_watchdog / duty_notify_pending / duty_known_issues / 计划任务 / 哨兵正本 / ps1 | **已生效**（CLI 走既有 API + 只读库） |
| bug_tickets.html + i18n pack | 模板热更新（页面路由装载后即整体可用） |
| bug_intake.py（fix_note/update_hint/reply 文案）+ bug_intake_routes.py（/reply + 页面路由）+ nav_schema + admin.py | **需重启装载**。本批刻意未自行重启：23:0x 已把新端点补进路由清单基线，解开了 xiaozhi 线预检 NO-GO（其意向板明说「blocked on bug-intake owner 的 inventory」且在等安静窗重启）——其重启或 04:00 窗自然装载本批，自行再重启只会撞 FLAP |

## 3. 存量红上报（非本批引入，未代改）

- `test_workspace_sidebar` 5 例：渠道中心页侧栏矩阵组未渲染——用 HEAD 版
  nav_schema 复现，与本批无关（疑与并行线 workspace 壳/多语言改造相关）。
- `test_ui_visibility::test_new_nav_items_declare_client_audience` 剩 `singing`
  未表态（昨日积压收口批引入，归 singing 所有线决策客户可见性）。

## 3.5 P2 批次（2026-08-28 深夜，同日续做）

上线确认：23:17 xiaozhi 线重启已把本批 P0 的 `.py` 全部装载（重启原因栏明写
「Riders: duty_* tools, bug_tickets console」；探针：list API 带 `update_hint`
字段、处置页路由 303 非 404）——**处置台已在生产**。

| # | 事项 | 落点 |
|---|---|---|
| P2-1 | **首响 SLA 报告** `tools/duty_sla_report.py`：burst 按首条计等待（客户视角最坏值）、未应答超龄也计超时、数据截断如实标 partial；`--out-jsonl` 趋势 + `--notify` 投递；计划任务 **DutySlaWeekly**（周六 07:20）。首跑基线（近 7 天）：内测群 131 问、p50 2.4m、p95 96.3m、最坏 245.8m、超 30m 11 条（8.4%）、未应答 0——长尾即值守缺位时段，看门狗上线后此数应收敛 | tools + deploy/duty/run_duty_sla_weekly.ps1 |
| P2-2 | **批量分诊**（处置台，模板热更即生效）：勾选 + 全选 + 批量流转 confirmed/in_progress/closed——**刻意不含 fixed**（标修复要写说明并决定回访，批量放开＝一次 @ 几十人刷屏；该不变量有门禁）。方案修正:原计划的服务端 `notify` 开关因此不再需要，零重启债 | bug_tickets.html + i18n pack |
| P2-3 | **update_hint 一条命令** `tools/duty_update_hint.py`（写 overlay 走 `set_yaml_key_preserving` 保注释，ruamel 不满足拒写不降级）+ `push_chatx_hotpatch.ps1 -UpdateHint "..."` 发版顺手更新（显式传参才写，绝不自动） | tools + deploy/desktop |
| P2-4 | **看门狗升级重提**：仍未应答超 240 分钟再吵一次（🔁 标记与首报区分），`--re-alert-min 0` 回旧行为；与 health_watchdog 家族 4h 重提同刻度 | tools/duty_watchdog.py |
| P2-5 | **值守交接指针**：值守交接_20260827.md 顶部插「工具链已产品化」块（哨兵/回复/处置台/安全网/周读数新入口），tmp_* 命令降为历史参考 | docs |
| 纪律 | 本主线 6 个门禁文件登记进 `gate_sweep.ps1`（实施74 教训「不进 sweep 等于不存在」——上批漏了，本批补） | scripts/gate_sweep.ps1 |

验证：新增 `test_duty_sla_report`(8) `test_duty_update_hint`(3)；`test_duty_watchdog`
11→13；`test_bug_tickets_console` 13→14；SLA/update_hint/watchdog 三工具对生产
只读冒烟全过；两个 ps1 PSParser 零错。

### P2 补（2026-08-29 00:30 老板拍板）：内部告警收件人 → @ai_zkw

角色澄清（老板指出报告表述混淆）：群里监听+回复的一直是支持号 6834964252
（@htqp456），@Sousaun(katie/8244899900) 只是沿用 SeatLogMonitor 的内部告警
信箱。老板拍板：**值守内部告警（缺位告警 + SLA 周报）投他本人 @ai_zkw
（5433982810）**。落点＝新单点 `tools/duty_alert.py`（bot 直投 → 支持号私聊
回落；两消费方必须走它，直连 monitor 的 deliver 会被门禁点名
`test_alert_recipient_is_boss`）；`monitor_seat_logs.deliver_bot` 参数化
chat_id（缺省不变，SeatLogMonitor 自身仍投 katie——要切另行拍板）。
**实弹验证：`bot->ai_zkw ok`（主通道直达，老板已 /start 过 bot）**。

## 3.6 L1/L2 批次（2026-08-29 拂晓：对账机器人 + 自动修复派单器）

老板定向：「本机自动读取需要修复的工单来修复，然后自动在群里回复已修复」。
三级闭环的前两级本批落地（「说已修复」的权力留给人，其余全自动）：

| # | 事项 | 落点 |
|---|---|---|
| L1 | **工单↔B案台账对账** `tools/duty_reconcile.py`：CJK bigram 词面匹配工单与实施49/64/68 的 B 案行；**台账无已修标记绝不建议 mark_fixed**（红线有门禁）；`--apply "41=B114"` 显式逐单批准 → 标 fixed → 既有自动回访。首跑真实结论：60 B 案 vs 44 开放工单，**可安全自动标 fixed 的存量＝0**（B 案✅时代早于现开放工单），6 条挂「跟进中」、38 条真待修 → 交给 L2 | tools + tests(4) |
| L2 | **自动修复派单器** `tools/duty_autofix_dispatch.py`：可派工单（bug 类 new/confirmed 未派过，P0>P1>P2）→ headless Cursor agent（`agent` CLI **plan 只读模式**）逐单产出「根因+建议补丁 diff+验证方案+回访用修复说明一句话+置信度」→ 报告落 `.ops/autofix/t<id>/report.md` + 工单 note 回写 + 状态 in_progress + 摘要推 @ai_zkw。v1 安全边界：agent 不改共享树（多人类 lane 并行，自动 agent 直接编辑=撞车之源）；state 防重派 | tools + tests(3) |
| 前置 | Cursor CLI 已装（`agent 2026.08.25`，`C:\Users\Administrator\AppData\Local\cursor-agent`）；**待一次性 `agent login`**（未登录时派单器明确拒绝并指路） | 117 |
| 门禁 | 两个新门禁文件登记进 gate_sweep；sweep 跑后唯一红＝**他线**今晚写脏的生产 overlay 重复键（`avatar_voice.hub_fish.profile_map` ×4，会卡重启闸门——已上报语音线，本线不代改生产配置） | scripts/gate_sweep.ps1 |

配套：报障群互动/按钮问题的五视角方案另立 **实施82**（三个并行分析 agent 产出
+ 主线合成；未动代码）。处置台错误态 colspan=7 漏改（本线昨夜批量分诊列引入）
被美术线分析 agent 抓到，已修。

## 3.7 对外答复红线（2026-08-29 07:19 老板指令，当日实录沉淀）

**事故**：值守答「账号防轰炸额度」时把【办法二】写成 YAML 教程发进群——
「打开智聊数据目录的 config 下 local 覆盖层 → 找到 companion_send_gate →
加一行 target_cap: 500 → 保存重启」。老板三条裁决：

1. **群内容不得给用户任何「修改配置文件」的方法**（本节+机器闸门）；
2. **配置文件今后要加密与隐藏**（路线见 `docs/实施85_配置收口_加密与隐藏路线_2026-08-29.md`）；
3. **用户可调的设置一律收进后台管理页**——防轰炸额度已收进
   `/reply-settings`「🚦 账号发送额度（防轰炸）」卡（companion_send_gate
   七键全量 + 白名单管理，随下次重启装载生效）。

**机器闸门**：`src/ops/support_reply_guard.py`（纯函数，识别配置文件名/
内部键名/「打开·修改…配置文件」短语/YAML 键值行）→ `tools/duty_reply.py`
发送前硬拦 exit 2（含 dry-run，**无 bypass 旗**）；门禁
`tests/test_duty_reply.py`（正例＝当日群实录逐段）。

**值守答法替换表**（拦下后照此改写）：

| 旧（禁止） | 新（正确） |
|---|---|
| 教改 config 下的 local 覆盖层 | 指路后台设置页：「管理后台 → 自动回复设置 → 账号发送额度」 |
| 贴 YAML 键值行 | 说人话：「把目标日发上限调到 500」 |
| 「重启客户端生效」+ 文件路径 | 设置页保存即热生效（该卡全键 hot），无需教任何文件操作 |
| 设置页还没有该项 | 登记产品缺口进工单，答「已登记，随下版本上线」，**不给临时 YAML 方案** |
| 劝用户休息/关心作息（「早点休息」「快去睡吧」，0830 老板指令） | **只描述工作**：登记结果、修复进展、验证方法；亲和感靠致谢与分享排查心得（「你这条 80 字的直觉和我们引擎分段阈值正好对上」），不靠关心私人生活 |

**语气基调（0830 老板补充）**：可以调皮、多用表情、有亲和力——「专业内容 + 轻松外壳」：
emoji 点缀小标题与结论（🎫✅🔍🛠️ 一类工作语义为主）、遇到用户敏锐的反馈大方夸
（「这条抓得漂亮👏」）、自嘲可以有（「这锅我们背」）；**不变的边界**＝内容全部
有据可查、不闲聊跑题、不碰用户私人生活、红线表（配置文件/时限承诺）照拦。

**接线备忘（bug-intake 线）**：服务端 `POST /api/admin/bug-intake/{id}/reply`
是第二条对外发送路径，建议同样接 `support_reply_guard.config_edit_hits`
（一行 import + 拦截返回 400）；本批因该文件正被 bug-tickets 线活跃编辑，
按共享树协议未代改，已留 .ops 便签。

## 4. 下一阶段建议（按价值）

1. **值守 SLA 指标化**：看门狗只有告警半边；「群消息→首次人工响应时延」进
   ops 卡 + 周报（复用 duty_replies.jsonl + 群线程，量化「30 分钟内响应」承诺）。
2. **状态流转习惯养成**：真实台账 43 单全是 new——处置台上线后第一周把存量
   工单批量归位（confirmed/fixed/closed），否则周公示与回访链都是空转。
3. **update_hint 随发版自动化**：`bump_ui_build --severity` 的同款思路，打包/
   hotpatch 脚本产出版本号时顺手写 overlay 的 update_hint（当前是手工改）。
4. **快捷模板配置化**：现为 i18n 四枚固定模板；值守用顺后按高频问题扩成
   可编辑模板库（复用 howto 语料）。
5. **173 值守交接更新**：下次交接文档把监听命令切到
   `deploy/duty/group_sentinel.py`、回复命令切到 `tools/duty_reply.py`，
   tmp_* 脚本族退役。
