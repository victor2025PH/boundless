# 发版对账 v1.0.83 · #304 十翼会话语音「突然说广东话」+ 克隆成品被判「念错」（GWJ2RZ · jun/B990）

> 来源：老板 09-12 04:3x 直问「钧的电脑为什么从普通话跳到粤语、为什么不能克隆」；钧机 04:27 自传报告 `tmp_diag\GWJ2RZ`（fp B990，app 1.0.82，TG 8538547216 ↔ 7332005191，人设 `lin_jiaxin`）→ `duty_auto_ticket` 新立 **#304**。
> 归属：语音线（`lang_voice_route.py` / `tts_pipeline.py` / `voice_transcriber.py`）。与 **Q-22**（09-12 修订版把 #304 列进 D 段「语音语种一致性」）的关系：**本单已把 #304 的两条根因修掉**，Q-22 D 段不必再为 #304 核「xlate target=yue 译成粤语」或「克隆链粤语口音」两条假设——都不成立（见 §一）；Q-22 的 `tts_lang == 文本语种` 一致性闸仍值得做，是第二道保险。
> 执行：09-12 04:58 开工（`agent_probe -Intent` 已登记）→ 05:07 一个 commit 收工。**不发版、不重启**（R83 打包由发版线统一做）。

## 一、根因（日志逐行，`tmp_diag\GWJ2RZ\20260912-042540_report_report.md`）

| 现象 | 证据 | 根因 |
|---|---|---|
| 04:21:55 两条语音（mid 89/90）是粤语腔 | `[lang_voice_route] 粤语文本 → 切 edge_tts (zh-HK-HiuMaanNeural) markers>=2` → `[tts] 合成生效 backend=edge_tts voice=zh-HK-HiuMaanNeural`；04:22:41 客户「你在说什么怎么说广东话」 | `is_cantonese_text` 特征字表 `_CANTO_CHARS` 混着普通话口语字（啵嘞喇咩咯噶埋）与繁体（靚曬囉），阈值 2；人设港味（频繁往返香港）+ 客户转写全繁体 → 普通话回复凑两个字即整段切香港声，同时 `voice_profile.enabled=False` 丢克隆。**不是**翻译成粤语（无 `xlate target=yue`），**不是**人设 `dialect_flavor`（lin_jiaxin 没配，Vancouver 不映射 HK 档）。 |
| 40 分钟 6 次克隆 5 次 `synth_verify CER 0.32–0.47 > 0.30 → 重合成`，2 次重合成后仍 `判「念错」→ 作废改兜底`，每轮 `合成超 40s 预算` | 同机客户语音转写**全是繁体**（幾乎/煮飯/廚房/擺盤/什麼）＝该机 ASR 对普通话吐繁体；回验用同一转写器 | `_cer_cjk` 逐字比对简体送稿 vs 繁体转写：「温哥华这边是真的贵」×「溫哥華這邊是真的貴」8/15 字不同 ＝ CER≈0.45。**字形不是发音错误**。误判 → 白付一次重合成 + 一次远端 STT（40s 预算一半在这）→ 再判 garbled → 桌面 `no_edge_fallback` 拒发改文字 / 回落整段重合成。 |
| 兜底声是 `en-US-AvaMultilingualNeural`？ | 读码 `_pick_fallback_edge_voice`：按文本语种 + 人设性别选声；桌面 `policy.no_edge_fallback: true` 由调用方拒发 edge | 04:3x 首轮判断**不成立**，无需改。会话覆盖钉英文声是钧机本地数据，建议他清掉（04:20 后他已改 zh-CN-Xiaoxiao）。 |
| 网关慢？ | 117 实测 `bd2026.cc/api/ai/hub/health` 0.08–0.24s，104:7865 直连 0.03s | 隧道不是 40s 来源；慢在 合成 → 远端 STT → 误判重合成 → 再 STT。 |

## 二、落点（一个 commit）

| 项 | commit | 落点 |
|---|---|---|
| **A 粤语路由误切** | `73c8ab6b` | `src/ai/lang_voice_route.py`：`_CANTO_CHARS` 拆 `_CANTO_CHARS_STRONG`（嘅咁咗唔哋喺嚟氹嗰乜嘢佢睇畀冇諗攞嗌啱噉嚿餸嗮喎，单独即信号）/ `_CANTO_CHARS_WEAK`（埋靚曬啵嘞喇咩囉噶咯，**只在有独占信号时各 +1**）；`_CANTO_BIGRAMS_WEAK`（宜家 / 得闲 / 几好）同弱；`count_cantonese_markers` 无独占信号恒 0。`_CANTO_CHARS` 合集仍导出（兼容）。三处调用方（`_route_cantonese` / `voice_colloquial_llm` 粤文跳过口语化 / `image_autosend` 粤语配文）同时受益。既有正例（「你识唔识讲粤语噶」= 唔 1 + 噶 1）全保留。 |
| **B 克隆回验繁简假阳性** | 同上 | `src/ai/lang_voice_route.py` 新增 `to_simplified_plain()`（无豁免 t2s，opencc 缺失恒等；`to_simplified_for_tts` 改为复用）。`src/ai/tts_pipeline.py::_cer_cjk` 两侧先 t2s 再比；`verify_and_retry_synth` 记 `stt_ms / resynth_ms`，重合成过或回验 ≥3s 落 `[tts] synth_verify 分账 cer= retried= stt_ms= resynth_ms= lang= text=`（INFO），正常 DEBUG。返回 dict 形状不变（`speech_verdict` 消费方零改动）。 |
| **C 入站转写统一简体** | 同上 | `src/voice_transcriber.py` 基类 `transcribe_voice_message`：幻觉守卫之后 `_normalize_zh_script()`（复用 `to_simplified_for_tts`：非中文主体 / 粤文原样）；配置 `voice_recognition.zh_simplified_output`（默认 **true**，`config.example.yaml` 有注释键）。级联 `FallbackTranscriber` 各子级自然覆盖；synth_verify 共享转写器同路径。日志 `[asr] 转写繁→简归一: … → …`。 |
| **D 桌面包 opencc 数据** | 同上 | `desktop/build/build_backend.py` `COLLECT_ALL += ["opencc"]`：`opencc-python-reimplemented` 词典 / 配置是包内 `dictionary/*.txt` + `config/*.json`，此前冻结包只带 .py → `OpenCC("t2s")` 抛 → 全部 t2s 软依赖处恒等（B / C 与既有克隆发音 t2s、zh-tw 出向 OpenCCEngine 在坐席机上一直没生效）。**需 `npm run build:backend` 全量**（.py 变更 + collect 变更，增量档必自拒）。 |
| **E 门禁** | 同上 | `tests/test_lang_voice_route.py` +2（普通话句尾词 / 繁体字 / 宜家得闲不触发；弱字只随独占字计分）、`tests/test_synth_verify.py` +2（繁体转写 vs 简体送稿 CER=0；归一后真错字仍计）、`tests/test_voice_transcriber_fallback.py` +3（繁→简 / 粤文与英文原样 / opt-out）。语音·语种 15 套件 **460 通过**；2 红为 HEAD 既有（干净 worktree 复跑同红）：`test_cosy_dialect::test_personas_html_dialect_select_only_cantonese`（`personas.html` 他线在途 WIP）、`test_tts_lang_gate::test_lang_engines_routes_ja_to_mapped_engine`。 |

## 三、验收对照

| 验收项 | 验法 | 状态 |
|---|---|---|
| 普通话回复不再被切 zh-HK 声 | 钧机装 1.0.83 后同会话继续聊；`backend.log` 不再出现 `[lang_voice_route] 粤语文本` 行（除非回复真含 唔/嘅/咁 等独占字） | 待 R83 |
| 克隆回验假阳性消失 | 同机 `[tts] synth_verify CER=` 警告频次显著下降；`[tts] synth_verify 分账` 行 `retried=0` 为主；`克隆成品判「念错」` 不再出现在普通话回复上 | 待 R83 |
| 转写字形 | 客户语音转录行 `[语音转录]` 简体；`[asr] 转写繁→简归一` 行可见 | 待 R83 |
| opencc 随包 | 1.0.83 装机 `resources\backend\_internal\opencc\dictionary\` 存在；或 `[asr] 转写繁→简归一` 在坐席机真出现 | 待 R83 打包核 |

## 四、交接

- **R83 打包线**：本 commit 含 `.py` + `build_backend.py` 变更 → 后端指纹必红、增量档 `refresh:datas` 会 exit 3 自拒，走全量 `build:backend`。发版帖一条：「语音：普通话回复不再被误判成粤语切香港声；克隆语音质检不再把繁简字形差当念错（钧 #304）」。
- **Q-22 线**：#304 两根因已修，D 段「语音语种一致性」按第二道保险做即可，不必复核 xlate/口音两条假设。
- **未做 / 下一步**：① 钧机会话级音色覆盖（en-US-Ava）是本地数据，请钧自清；② Whisper `initial_prompt` 简体引导（源头治繁体）本次未动——C 段归一已覆盖结果面；③ opencc t2s 保留「著」为合法简体（守著≠守着仍算 1 字），影响极小，可加小补丁表。
