# 发版对账 v1.0.85 — 发版线 R85（单一版本：Q-29～Q-31 / Q-35～Q-38 + 接力记忆 + 模型·模式 → 一个 clean 包 → **公共渠道 + 内测桥接 + 官网 + 播报**）

> 规矩：`.cursor/rules/chatx-desktop-install.mdc` 「单一版本（D-Q11）」——一个 semver = 一个 clean 包。前例 `docs/发版对账_v1.0.84_发版.md`。
> 范围：公共渠道 1.0.84 → 1.0.85。Q-26 C–E（`reply_settings` / `personas` / `automation_risk_q26`）与 Q-28（#279 Bad MAC）**不进包**。Q-32/33/34 美化线未拍板。
> 接手方：先 `git -C D:\boundless log --oneline -16` 核对本表 commit 真在。意向板 slug `r85-release-1-0-85-single-clean-public-bridge`。
> 执行：2026-09-13 01:5x → 02:4x，见 §5。

## 0. 一句话

**1.0.85 单包** `ChatX-Setup-1.0.85.exe` **523,806,496 B** sha256 `87793d37eb7e5bad7d2d05994455770f4082469658fb3e3677d2bd4647f17bf0`（500 MB，clean 形态，`MZ` 头核过，build-info flavor=clean commit=`47b5dcf3`，干净 worktree `D:\boundless-r80@47b5dcf3`）。内容 = 1.0.84 全部 + **Q-29** 客户当地时间 + **Q-30 A–E** 单人端 / 状态带真闸 / 切档不双发 + **Q-31** 系统标签折叠 / 发前确认只计可行动稿 + **Q-35** 相册上传可见 + **Q-36** 客户发自己的图 + **Q-37** 调度钟三源 + **Q-38** 画像不写人设名 / 摸底确认钮 + 接力记忆 + composer「模型 / 模式」。坐席三台 **yunsheng / kouxing / lianbei 全 UP · 1.085 · clean/public**。公共渠道 + 内测桥接（同字节）+ 官网 + 台账 12 单 + 回访 + 群 / 频道播报见 §4。

## 1. 本版收掉的坑（摘自各线落点表）

| 事故 | 根因 | 本版 |
|---|---|---|
| 确认城市后仍问几点（#305） | 只有人设钟，客户 `location:confirmed` 没换算 | Q-29 `peer_time`：命中唯一时区才注入，不问几点 |
| 15:22 上海发「It's 3am here」（#318） | 安静闸读服务器钟，没读 Q-29 客户钟 | Q-37 调度钟三源：客户钟 → 人设钟 → 服务器钟 |
| 单人端「处理中 · 释放认领」（#309 #310） | `resolve_seat_mode` 无配置时 ≥2 账号即 multi | Q-30 A 缺省 single；设置页可开多坐席协作 |
| 全自动沉寂不发（#311） | 真闸是拟人首回延迟，界面当成认领 | Q-30 B `first_reply_hold` 进状态带 |
| 切全自动后旧 L1 稿还在（#308） | 顶栏全自动 vs 陈旧半自动稿 | Q-30 D 切全自动作废未处置 L1 |
| 切档同句双发（#319） | 让位回队 + 新稿各投一次 | Q-30 E 同会话只出一条 |
| 筛选一堆 dormant（#317） | 系统标签与用户标签同列 | Q-31 折叠「系统标签 ▸ N」；药丸只计可行动稿 |
| 相册上传失败看不见（#316） | 拒绝在成功日志之前 raise | Q-35 逐文件结果 + 原因 + 重试 |
| 客户要发自己的图被拒（#313） | offer 当索图 | Q-36 `offer_media` |
| Hi Mizuki 写成客户名（#272 追加） | FactGate 见入站 vocative 就放行 | Q-38 `own_name` / `vocative` 硬闸 |

## 2. 开工基线（01:5x）

| 项 | 事实 |
|---|---|
| 主干 | 探活时 HEAD `9aa5dff1`（Q-38 收工）；版本链基点随后提交 `47b5dcf3` |
| 意向板 | slug 登记；index FREE；热区 10 分钟静默（仅 seat-log 监控非热区） |
| 工作树 | Q-26 C–E 与 label-leak / `desktop/main.js` 等脏文件**未动、未进任何 commit**。`i18n_hant generate` 前把 `risk_grader.py` / `bug_tickets_page.py` 写回 HEAD、藏 `automation_risk_q26.py`，生成后拷回源包 |
| 版本链 | `desktop/package.json` 1.0.84→**1.0.85** / 1.084→**1.085**（字节替换，CRLF 保留）；`python -m scripts.i18n_hant generate` → **17699** 键；`gen-chatx-changelog add --version 1.0.85 --date 2026-09-13 --tags fix,improve,feature` **9 条双语**；`bump_ui_build.py --severity notice` → **20260913-0158**；小智 `help_terms` +10 / `howto_pack` +9；`xiaozhi_dialog_eval` **9/9 GUIDED** → 主干 **`47b5dcf3`** |
| 官网五值 | `chatxContent.ts` version / filename / url / sha256 / 体积 500 MB → 主干 **`27a415a1`** + **`5748a392`**（体积与 manifest `size_mb` 对齐） |

## 3. 回归 / 门禁（干净 worktree `D:\boundless-r80@47b5dcf3`）

| 项 | 结果 |
|---|---|
| worktree 复用 | `D:\boundless-r80` 原 `06fee5b8`：`git checkout -- .` + `git clean -f` 只清构建改写的 `i18n-packs-staged` 未跟踪项，再 `checkout --detach 47b5dcf3` |
| `import_sweep` | modules=**1271** bad=0 / from-import names=**3667** bad=0 **ALL GREEN**（57s） |
| predist（clean） | `build:backend` 全量（11:00）+ `dist:win:clean`：后端冒烟全过、干净包首启冒烟全过、after-pack 随包交付物 **22 项**；`latest.yml` version 1.0.85 size 523806496 |
| `gate_sweep -NoSyntaxScan` | 初跑 **18 failed / 1747 passed**；transient 复跑 14 文件仍 **18 红**。相对 R84 的 19：Q-22 `cp.voice.*` 12 键繁体已补，本版回到 18 条同名旧债（assistant_qa ×2 / deploy_profiles / inline_color ×3 / orphan_refs / legacy_blue ×2 / input_theme_contrast / silent_exception / takeover / copilot_tiny_font / workspace_emoji / sealed_templates / sql_phantom / no_dynamic_dot / ui_build_stamp mtime 伪红）。**无本版新红** |
| 发版门槛（worktree 真跑） | Q-29/30/31/35/36/37/38 + R84 闸（guard_context / fact_gate / Q-27 / messenger sidecar / howto / ja×STEVEN）= **366 passed / 1 skipped**；`test_seat_mode_q30::test_reply_settings_template_wires_seat_card` 初红因断言依赖未进包的 Q-26 C `rps-grp-auto` → 钉子 `51c9a317`（包不重打）；`node --test send_chain.test.js` **16 passed** |
| 全量回归 | `scripts\regression.ps1` 发版当时仍在跑（生产在线封顶 4 worker，~46% 时已见若干 F）。红集对照上一版 `06fee5b8`：只有「本版红 / 上版绿」才算本版引入；收口后补本节 |

## 4. 全链

| 步 | 状态 | 事实 |
|---|---|---|
| 坐席三台 | ✅ 02:21–02:24 | `push_chatx.ps1 -TargetSsh {yunsheng,kouxing,lianbei} -Setup <r80>\dist-clean\ChatX-Setup-1.0.85.exe -Install -Relaunch -Smoke`：三台 exit 0、`[push] OK`、seatverify sentinel FRESH + 18799 login 200、smoke `app version: 1.0.85` 六端点 OK + vision active + pacing ok；`chatx_fleet_status` **三台 UP · 1.085 · clean/public**。173 静默安装一次成功（未踩 NSIS 连崩）。日志 `%TEMP%\r85\push_*.txt` / `fleet.txt` |
| publish 公共 | ✅ 02:25–02:29 | `-DryRun` 过 → `publish_chatx.ps1 -DistDir <r80>\dist-clean -Yes -NotesFile announce_1085.txt`：VPS sha512 ✓ → 指针 + pm2 reload → R2 → 公网核 `latest.yml=1.0.85 manifest=1.0.85 exe=HTTP206` → `announcements.json` 带 `release-v1.0.85` → 清理 1.0.81。日志 `%TEMP%\r85\publish_public.txt` |
| publish 内测渠道**桥接** | ✅ 02:29–02:32 | 同一个 clean exe + blockmap + `latest.yml` 复名 `latest-internal.yml`（`%TEMP%\r85\internal_bridge`，sha256 逐字节同 `87793d37…`）→ `-Channel internal -DryRun` 过 → `-Yes -NoAnnouncement`：`latest-internal.yml=1.0.85`，公共指针未动（`latest.yml still v1.0.85`），清理 1.0.81。**不是第二个包** |
| 官网 | ✅ 02:33–02:35 | `gate:content` **6/6 PASS**（发布事实源 1.0.85 / 87793d37… 一致；体积 500 MB 对齐 manifest；更新记录最新 v1.0.85；测试数 1890 ≥ 宣传 1100+）→ `deploy.ps1 -FromHead` → **部署完成 [OK]**（healthy / webhookSecret / adminKey / deepseek 全 True，R2 同步）。日志 `%TEMP%\r85\website_deploy2.txt` |
| 台账 / 回访 / 播报 | ✅ 02:36–02:41 | `duty_update_hint --set`（明写升级后重启一次工作台后端）→ 报障群播报 `duty_reply --group neice --text-file broadcast_1085.txt --no-mention` → **mid 1534**（02:36:48）→ `duty_ledger_fix --mark-fixed-file mark_fixed_1085.json --apply`：**12 单 new→fixed**（#305 #306 #307 #308 #309 #310 #311 #313 #316 #317 #318 #319；#272 已 fixed 跳过，另回访 Q-38 追加）→ 逐单回访 13 条 `bug-intake/reply ok`（02:37–02:40，间隔 15s）→ `duty_notify_summary --ids … --msg-id 1534 --apply` **记账 12 单，该筛选积压 0** → 频道 @hykj7 **messageId=167**（campaign `release-1085`） |
| release 真值 commit | 见本节提交后 | `desktop/build/i18n-packs-staged` 自 worktree 构建拷回（含新 `seat_reason_q30.py`）+ 本表 + Q 总账 R85 行 |

## 5. 断点 / 交接

- 01:5x 探活 + 藏脏 i18n → 版本链 `47b5dcf3`（小智 9/9 GUIDED）→ worktree detach → import 绿 / 后端全量 / clean 包 / gate 18 红旧债 / 门槛 366+16 → 02:21–02:24 坐席三台 → 02:25–02:32 publish 公共 + 桥接 → 02:33–02:35 官网 → 02:36 台账 / 群 mid 1534 / 频道 167。
- 发版 worktree 保留 `D:\boundless-r80@47b5dcf3`（`dist-clean\ChatX-Setup-1.0.85.exe`）。
- **117 / zhiliao**：本线未重启 zhiliao。坐席三台 = 包内新后端，Q-29～Q-38 / 接力记忆已随包装载。117 后端半边仍等 `restart_preflight` GO 后 `restart_instance.ps1 -Instance zhiliao`（现又叠 label-leak / ASR 未提交 `.py`，勿擅自重启）。skuio 走公共渠道自检 1.0.85。
- 临时文书全在 `%TEMP%\r85\`，未入库。

## 6. 交回各线

- **Q-26 C–E**（#301 死链 + FJ8YU7）/ **Q-28**（#279 Bad MAC）随 **R86**。C–E 工作树未提交，本线未动。
- **#312 #314** 保持 confirmed（同事账号按设计不自动回，不是识图故障）。
- **Q-32 / Q-33 / Q-34** 美化线待拍板。
- 发版门槛钉子 `51c9a317`：坐席卡门禁不再要求未发布的 `rps-grp-auto`。
