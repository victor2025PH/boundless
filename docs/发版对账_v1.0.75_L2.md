# 发版对账 v1.0.75 · L-2 人设与语音闸门（2026-09-06）

> 指令：`docs/指令_L-2_人设与语音闸门_2026-09-06.md`。预算 $180，实花约 $120–140。顺序 **B → A → C → D → E → F**（B 是对客事故先止血），**六件全部完成、全部 commit**（08:2x–11:1x，6 个业务 commit + 1 个收口 commit）。
> 报障人 skuio（DC8F，1.0.73/1.0.74），证据＝Cursor 报告 `tmp_diag\{8CE7U3,V85TY9,ERS5QQ,5QMZ5E,5EFS2A,WYGNJ2,QRHNGM,29BAG6,E42974,2VKZKC}\`，未再取证。
> 决策：**D-L4**（新建人设默认不发语音；克隆失败降级文字、禁回落默认声；存量按现状推导不迁移）/ **D-L2**（研发面进开发者模式——三处「开发者模式可见」全部读 L-4 的模板全局 `ui_client_hide`，L-4 A `395acee7` 已落地，flag 名不必另定）。
> 模板 / i18n / 静态 JS = 热更新即上生产；`.py`（`tts_pipeline` / `persona_voice` / `voice_enroll` / `voice_profile_guard` / `voice_autosend` / `persona_routes` / `voice_routes` / `persona_manager` / `persona_stock` / 新 `edge_voice_catalog` / 新 `voice_tristate`）**待装载**（`restart_preflight` → zhiliao）。
>
> **commit 清单**：B `a3216231` / A `3f31eeab` / C `a1e54876` / D `bda3bbce` / E `d0593c7b` / F `df2f5c2a` / ui-build 戳 `a2cbfb8a` / 收口（繁体 regen + 本表 + 总账）＝紧随其后一条。核对：`git -C D:\boundless log --oneline -12 -- engines/chengjie docs`。

## 开工前核查（指令 §0-4）

`git log -S "粘贴 PROFILE JSON" --oneline -- engines/chengjie/src/web/templates/personas.html` 只命中 monorepo 迁移 `51ac7fc6`——JSON 导入块**不是 1.0.74 / K-2 新加的**，自迁移前就在，模板里是无 `open` 的 `<details>`。「默认展开」的真因是 CSS 把 `<details>` 三角标记藏了（`summary{list-style:none}` + `::-webkit-details-marker{display:none}`），折叠态标题「📋 从 JSON 导入字段…」看着像常驻区块；K-2 A「备货 → 上线准备」改 tab 名与它无关。C 项据此改法：client 形态整块不渲染 + 内部形态加可见 ▸ 标记 + 闸门。

## 落点表

| 项 | 工单 | 修什么 | 落点 | commit | 测试 | 状态 |
|---|---|---|---|---|---|---|
| B | #205 后半 | steven 男人设试听三次「无反应 → **系统默认女声** → 克隆声」；ERS5QQ：运行时兜底 `avatar_clone 失败 → edge_tts 默认声` | **根因**：`tts_pipeline.synthesize` L1664 块「主后端失败 → 回落 edge」+ `safe_edge_voice(voice, fallback=zh-CN-XiaoxiaoNeural)` 不看人设性别/语种；卡片试听不带 `fast`，第二次的女声就是这条兜底。**修**：① 新 `src/ai/edge_voice_catalog.py`——语种 × 性别 Edge 目录（每语种首条与 `EDGE_VOICE_BY_LANG` 逐条对齐；B 兜底选声 / A 预置声选择器与保存校验 / 试听三处同一事实源）；② 回落块：主后端 ∈ `CLONE_BACKENDS` 且非语种闸（#161 保护性改标准声）非 Token 降级 → **不回落任何预置声**：`ok=False` + `extra.degrade_to_text` + `fallback_blocked=clone_engine_offline` + `error=clone_engine_offline:<原因>`，落 `[tts-fallback]` INFO；③ 预置态才允许回落，兜底音色＝同语种同性别（`persona_gender` 由 `persona_voice.resolve_voice_cfg` / `resolve_effective_voice_context` 注入），目录无同类 → 改发文字（`no_matching_voice`）；性别未知 → 语种缺省声（旧行为零变更）；语种闸改道同样按性别（ja × 男 → Keita）；④ `safe_edge_voice(voice, fallback, *, gender, lang)`，`_run_backend` / `_edge_tts` 两处映射同规则；⑤ `classify_voice_error` 新桶 `clone_engine_offline` / `no_matching_voice` → `voice_routes._voice_fail_message` 出人话（`errors_stock` 两键 zh/en）→ 卡片试听 `persona_studio_core.js` 优先显示 `message`，引擎离线只提示不放别的声音。自动链：`voice_autosend._synth_ogg` 拿到 `ok=False` → `record_voice_fallback(clone_engine_offline:…)` → `voice_outage` 台账 → 既有看门狗「语音断档」告警（账号旁提示走这条既有链） | `a3216231` | 新 `tests/test_tts_no_cross_gender_fallback.py` 14 例（目录对齐 / 双性别齐 / 旧契约不变 / 性别映射 / 克隆离线不出 edge / 性别未知同样拦 / 语种闸按性别 / 预置态同性别兜底 / 无同类改文字 / persona_voice 注入）；`test_tts_pipeline_smoke` 与 `test_minicpm_clone_tts` 各 1 例**旧契约「克隆失败回落 edge 出声」改新契约**；语音面 258 passed（`test_tts_lang_gate::test_lang_engines_routes_ja_to_mapped_engine` 在 HEAD 干净树同样红＝预存） | `.py` 待装载 |
| A | #205 前半 | 语音 tab「语音 BACKEND 五选一 + 手填 VOICE ID」研发面直出；Mizuki `avatar_clone + ja-JP-NanamiNeural`（克隆引擎 + 预置声名、无录音）保存成功、页面标「已绑定克隆音色: ja-JP-NanamiNeural」 | **后端** 新 `src/ai/voice_tristate.py`（纯函数）：`derive_voice_mode`（显式 `voice_mode` > 登记齐备克隆 > 克隆引擎无录音（Mizuki 形态）> 预置引擎/音色名 > 什么都没填＝沿用全局并标注 `inherit_global`，只读推导不写回）/ `validate_voice_profile`（clone 无录音身份 = error；克隆 + 预置声名 = error；preset 音色不在合法表 = error；参考音**本机文件不在 = warning**——托管/hub 登记的档参考音可能只在远端，硬拦会误伤存量，取舍写明）/ `apply_new_persona_voice_default`（新建 → `{voice_mode: off}`）。`PUT /api/personas/profiles/{id}`：落库前校验，非法 → **400** `{detail 人话, voice_problems, voice_tab}`（存量非法档只改名字也拦＝「提示修正、不静默改」）；新建无语音配置 → 默认 off；warning 随 200 `voice_warnings` 回传。`TTSPipeline.synthesize` 开头 + `voice_autosend._synth_ogg`：`voice_mode=off` 单点判「改发文字」不打引擎。`voice_enroll` 三个登记构造器写 `voice_mode=clone`；`voice_profile_guard.protect_registered_clone_patch` 允许显式 clone 补丁清掉残留预置声名；`persona_voice._merge_voice_profile` 把 `voice_mode` 视为真配置（off 盖过全局克隆档）；`list_profiles_summary.has_voice` 尊重 off + 新 `voice_mode`。新 `GET /api/voice/preset-catalog`。**前端** `personas.html` 语音 tab 改三态单选「用我上传的录音（克隆声）/ 用预置声 / 不发语音」，预置声按语言 · 性别筛选 + 试听（`voice_cfg_override` 显式 backend/voice，Edge 走 fast 档）；旧 backend/Voice ID 手填 + 「声音克隆(命令)」收进「🛠 开发者」`<details>`（`ui_client_hide` → `display:none`，元素保留供 JS 同步）；状态行按实际类型给绑定文案（「已绑定克隆声：<录音文件名>」/「预置声：日语 · 女 · 七海 Nanami」/「沿用全局语音设置…」/「此人设不发语音」），非法组合红字「配置需修正」；保存前客户端同规则校验并跳到语音页；服务端 400 → 跳语音页 + 状态行红字；登记完成事件 → 三态重载；`peVcSync` 状态改按录音文件名（不再显示音色 ID 文本）。i18n `persona_studio` 33 键 ×2、`errors_stock` 8 键 ×2 | `3f31eeab` | 新 `tests/test_persona_voice_tristate.py` 19 例（推导优先级 / Mizuki 形态拒绝 / 文件缺失 warning / 登记 + 残留声名拒绝 / preset 规则 / 新建默认 off / 绑定文案素材 / 登记构造器 / 守卫放行清声名 / merge off 盖全局 / 管线 off 不打引擎 / autosend 跳过 / 路由：Mizuki 400 + 不落库、新建默认 off、存量陈默零变化、存量非法档只改名也 400 且改选预置 → 200、未知预置声 400、显式 clone 清声名 + warning、目录端点）；`test_persona_profile_merge` 样例人设从非法组合改合法预置态（4 行）；`test_admin_route_inventory` 登记 `preset-catalog`；人设/语音面 459 passed；模板 Jinja + 17 段内联 JS `node --check` 通过 | 模板/JS/i18n 在树（热更新已生效）；`.py` 待装载——**装载前**：三态 UI 已显示、保存仍走旧后端（无 400 校验、新建不注入 off、`/api/voice/preset-catalog` 404 → 选择器空表回落 `_ADV_MANAGED_TOP`），装载后完整生效 |
| C | #203 | 基础 tab「粘贴 PROFILE JSON」+「解析并填充」一键整体覆盖表单与保存基底，无预览/确认/撤销/校验；「高级字段」JSON 直出 `voice_profile` / `capabilities.photos` | ① 导入块与「高级字段」面板：`ui_client_hide` → 不渲染 / 强制隐藏（`data-client-hidden`）；内部形态折叠常闭 + 可见 ▸ 标记 + 「开发者」角标；② 「解析并填充」→「解析并预览」：`_jiParse` 拒绝非法 JSON / 非对象 / 空 / **未知顶层键**（`GET /api/personas/schema-keys`，SSOT＝`persona_manager.known_persona_top_keys()` ← `PERSONA_SCHEMA_FIELDS` 首段 + `quirks/language/capabilities/emotion/temper/avatar/created_at/updated_at`）→ 逐字段对照表（当前值 → 导入后，勾选，无变化标注）→ 服务中人设（`binding_count>0`）红字警示 → `confirm` 二次确认 → `_jiApply` 只并入勾选字段（不动未勾选/未提及字段、不并 `id`）→ 30s 撤销快照（客户端 `_jiUndo`；落库后的撤销仍走既有 `/revert` 历史栈）；③ 高级字段面板不再直出 `voice_profile`（语音 tab 三态表单接管，整棵子树进 `_ADV_MANAGED_TOP`）与 `capabilities`（相册 tab「可发照片」开关 `pma-cap-photos` 已是接管口，不在基础 tab 重复放一个）；其余未知字段静默保留随保存原样回传（既有 deep-merge）。i18n 22 键 ×2 | `a1e54876` | 新 `tests/test_persona_json_import_guard.py` 7 例（client 不渲染 / 内部折叠 + 预览按钮 + ▸ / 纯函数核心 **node 直跑**（拒绝·预览·勾选并入·撤销）/ 填充必经 confirm 且预览阶段不碰表单与基底 / 面板不露 voice_profile / `known_persona_top_keys` 契约 / schema-keys 端点）；路由清单登记 `schema-keys` | 模板/JS/i18n 在树（热更新已生效）；`schema-keys` 端点待装载（装载前前端回落 `_ADV_MANAGED_TOP` 作合法键表） |
| D | #204 | steven 已绑 TG 账号，卡片仍「未启用」；筛选条只有 Mizuki 算服务中 | **根因**：`persona_manager.list_profiles_summary.binding_count` 只数会话级 `_chat_personas + _chat_bindings`，账号注册表 `persona_ids`（「应用到 → 账号默认」写的就是它）不算。**修**：`persona_stock` 新 `collect_binding_usage_map(pm, cfg)` 四源一次扫完（注册表 / config 各平台 `persona_ids` / PM 会话绑定 / 全局默认）→ `{pid: usage}`，`collect_binding_usage` 改为按 pid 取值（同源零漂移）；`list_profiles_summary(full_config)` 新 `binding_accounts / binding_chats / binding_default`，`binding_count = a + c` 保住旧筛选/排序/健康面板 `>0` 判据，聚合失败回落旧会话级计数；`GET /api/personas/profiles` 把 config 喂进去。`persona_studio_core.js` 胶囊「● 服务中 a 个账号 · c 个会话」+ tooltip 分项（含「同时是全局默认人设」），皆 0 才灰且文案「**未绑定**」（不是「未启用」——人设没有开关，只有绑没绑）；旧 summary 无分项字段时回落 `binding_count`。i18n 5 键 ×2 | `bda3bbce` | 新 `tests/test_persona_binding_pill_204.py` 4 例（map 与逐 pid 一致 / **只有账号默认绑定 → 2 · 0 且 binding_count>0** / config 平台级 persona_ids 也算 / 聚合失败回落 / 胶囊文案 + 词条双语）；`test_persona_stock` 15 passed | JS/i18n 在树（热更新已生效）；`persona_manager` / `persona_stock` / `persona_routes` 待装载——装载前胶囊仍按旧 `binding_count`（JS 已兼容） |
| E | #202 | 底部「导出 / 导入 Profiles（主账号专属）」只有「同步到 personas.yaml」；后端启动 `config=0 canonical=0 runtime=14`，yaml 是研发迁移残留 | 区块改名「**人设备份与迁移**」+ 一句说明（保存即生效并自动落盘，备份用于换机/找回/多机同步）；master 三入口：**备份全部人设**（下载 `chatx-personas-<时间>.json`，含 `album_refs` 相册引用清单）/ **从备份恢复…**（选文件 → `import?dry_run=1` 预览「新增 N · 覆盖 N」列出名字 + 无法识别/带提醒条数 → `confirm` → 恢复 → 刷新列表）/ 卡片菜单「**导出**」单个人设（`export?pid=`）；子账号进入只给灰字「备份与恢复由主账号操作」，不再整块消失；「同步到 personas.yaml」**移入开发者区**（选择：不删——canonical diff / `mark_profiles_canonical` 等后端链还在用，删按钮不删链条无意义；`ui_client_hide` 隐藏）；状态卡 yaml 未同步警示 / 概览「未同步草稿」格 / 横幅在用户版不再出现（`window.__PSN_DEV_FACE`），`_renderDashboard` 对可能不渲染的格判空。**后端** `GET /api/personas/profiles/export`：信封 `{format: chatx-personas-backup, version: 1, exported_at, count, profiles, album_refs}`（`profiles`/`count` 旧键不变），`?pid=` 单个（写权限，404 不存在）；**路由挪到 `/{profile_id}` 之前**——旧位置一直被 `id="export"` 吃掉返 404（此前没有任何入口调它，接按钮才暴露）。`POST /api/personas/profiles/import`：`?dry_run=1` / body `dry_run` 只算不写，回 `add/overwrite/add_ids/overwrite_ids/names/invalid/warnings(未知顶层键、语音三态问题)/would_delete`；真写走 `_persist_with_status` 回 `persisted` 真值；覆盖走 `upsert_profile`（历史栈仍可 `/revert`）。i18n 28 键 ×2 | `d0593c7b` | 新 `tests/test_persona_backup_202.py` 7 例（**备份 → 删一个 → dry_run 新增 1 · 覆盖 1 且不写 → 恢复 → 内容一致** / 预览 invalid + warnings / 单个导出 + 404 / 模板 master 内部形态 / master client 藏 yaml 与「未同步草稿」/ 子账号灰字 / 卡片导出键 + 概览判空）；人设路由回归 45 passed | 模板/JS/i18n 在树（热更新已生效）；导出/导入端点待装载（装载前「备份全部」返 404「Profile 'export' not found」——旧路由顺序缺陷；单个导出同）|
| F | #206 / #192 残 | #206：档案 tab「补齐向量」术语（向量/句向量/实体别名/检索自测）；提示被底部按钮栏遮住且 1-2s 消失；已 183/183 齐全仍可点。#192 残：页头右上孤立「7 ▾」 | **#206**：文案改人话——「让 AI 能检索这份传记」/「检索索引已就绪 {emb}/{chunks} 段 · 逐句 {sents} · 名字别名 {aliases}」/「还有 N 段 AI 查不到」/「试试 AI 能查到什么」；`emb ≥ chunks` → 按钮 `disabled` + tooltip/内联「索引齐全，无需补齐」；结果从 toast 改为按钮旁**内联状态**（`_bioReembedLastNote` 按人设记、行重绘后仍在、不自动消失、换人设即清）；Embedding 不可用（任务 `error=embed_unavailable`）→「检索服务暂不可用，本次没有补上；已建好的索引仍可用，稍后再点一次即可」——**后端没有自动重试**（`persona_import_routes` reembed 是一次性后台任务），指令写的「稍后自动重试」改为不承诺的诚实措辞。**#192 残**：`base.html` `.user-btn` = 头像首字符（`username[:1]`）+ 用户名（`≤860px` 媒体查询隐藏）+ 下拉箭头 → 窄窗且用户名以数字开头（如手机号）时只剩「7 ▾」，与 E42974 描述吻合但**未拿到放大图，不算定位实锤**；本次只做零风险的一处：按钮加 `title`（「<用户名> · 账号菜单：修改密码 / 切换语言 / 退出」）+ `aria-label`（`web_i18n` 新键 `user_menu_t` ×2）；头像换图标待 L-7 回访拿图确认后再做 | `df2f5c2a` | 新 `tests/test_persona_bio_index_copy_206.py` 3 例（文案无研发词 + 双语新键 / 齐全禁用 + 内联不 toast + 按人设记 / 页头按钮 title + aria）；`test_persona_header_192` + `test_i18n_coverage` 331 passed | 模板/i18n 在树（热更新已生效）|

## 验收（交 L-7 回访口径）

- **#205**：「语音改三态；克隆失败不再换声、改发文字并提示」。① 打开任一人设 → 语音 tab 只有三选一 + 预置声选择器，看不到「语音 BACKEND / VOICE ID」（开发者模式下在「🛠 开发者」折叠里）；② Mizuki 打开编辑器 → 语音 tab 红字「配置需修正：引擎是克隆声但没有录音，填的又是预置音色名 ja-JP-NanamiNeural…」，直接点保存 → 400 提示同句并跳到语音页；改选「用预置声」（日语 · 女 · 七海）保存 → 成功；③ 新建人设 → 语音默认「不发语音」，保存后卡片无 ▶ 试听键；④ 装载后把 7852 关掉再点 steven 卡片 ▶ → toast「语音引擎离线，本条未出声、已按文字处理…」，**不播任何声音**；自动链同一情形发文字，后台日志 `[tts-fallback] 克隆引擎失败不换声 → 改发文字 persona=steven`；⑤ 男人设用预置态且主引擎失败 → 兜底音色是男声（日志 `[tts-fallback] … edge 兜底音色 zh-CN-YunxiNeural gender=male`）。
- **#203**：「JSON 导入对用户不可见；填充必经预览确认可撤销」。客户机（client 形态）人设编辑器基础 tab 底部**没有**「从 JSON 导入字段」也没有「高级字段」；`/developer` 开开发者模式后出现，标题带 ▸ 与「开发者」角标、默认折叠；粘贴 `{"name":"x","foo":1}` → 拒绝并点名 `foo`；粘贴合法 JSON → 出对照表（当前值 / 导入后 / 勾选）→「确认填充勾选字段」→ 浏览器 confirm → 填充 → 绿 toast 带「撤销」按钮 30s。
- **#204**：「卡片显『服务中 N 个账号』」。steven 卡片胶囊「● 服务中 1 个账号 · 0 个会话」（装载后；装载前仍旧值），悬停显示分项；无任何绑定的人设显「○ 未绑定」；筛选条「已绑定」计数包含只有账号默认绑定的人设。
- **#202**：「人设备份与迁移三按钮」。主账号在人设池底部看到「人设备份与迁移」：点「备份全部人设」下载 JSON；删掉一个人设后「从备份恢复…」选该文件 → 预览「新增 1 · 覆盖 N」列出名字 → 确认 → 人设回来且内容一致；卡片右上多一个「导出」；子账号进入只见灰字；客户机看不到「同步到 personas.yaml」也看不到「未同步草稿」格。
- **#206**：档案 tab 长传记行按钮文案「让 AI 能检索这份传记」，183/183 时按钮灰、旁边「索引齐全，无需补齐」；点补齐后结果留在按钮旁不消失。
- **#192 残**：页头右上头像按钮悬停出「<用户名> · 账号菜单：修改密码 / 切换语言 / 退出」。**回访请 skuio 补一张放大图**确认「7 ▾」是否即此按钮（窄窗 + 数字开头用户名）；确认后另开小单换图标。

## 收工回归（HEAD `df2f5c2a` + ui-build 戳，11:0x）

`tests/test_persona*.py` + `tests/test_voice*.py` + `tests/test_tts*.py` + 克隆挂载守卫 / i18n 覆盖 / 繁体 ratchet / 路由清单 / 主题 token / Token P6 / minicpm：**2795 passed, 7 failed**（15m31s）。7 红逐条归属：
- 预存（HEAD 干净树 `D:\boundless_headcheck` 同红）：`test_tts_lang_gate::test_lang_engines_routes_ja_to_mapped_engine`、`test_persona_media_question_guard::test_info_question_hits[抹茶拿铁…]`；
- 他线：`test_template_inline_color_ratchet` 两例（L-4 新模板 `_client_tier_upgrading.html` 的 `--th-bg-amber06` / `--th-bd-amber3`）；
- 顺序抖动：`test_voice_pipeline_integration::TestVoiceReplyLangRoute` 两例——单跑 / 与本条全部语音测试合跑（68 passed）都绿，全量合跑时被别的模块残留状态带红，不是本条契约变化；
- **繁体 ratchet 98.67% < 99%**（L 批各线新键累积）：`zh_hant_auto.py` 树上已干净 → 本条顺手 `python -m scripts.i18n_hant generate` 再生（16377 键，+411/−30），`test_i18n_zh_hant` / `test_i18n_extra_langs` 13 passed，随收口 commit 入库。

## 刻意不做 / 交出去

- **交 L-7（收口）**：① 装载窗带上本条全部 `.py`（见页首）；② 繁体已再生（见上），后续他线再加键请再跑一次 generate；③ 回访 #192 残要放大图；④ 回访文案里 #205 请说明「新建人设默认不发语音，要发声去语音页选」（D-L4，客户可能问「为什么新人设不发语音了」）。
- **交 L-4**：三处「开发者模式可见」（A 手填区 / C 导入块与高级字段 / E yaml 同步与未同步草稿）全部读 L-4 的模板全局 `ui_client_hide`（`395acee7` 已落地，flag 名 `session.developer_mode`，入口 `POST /api/developer/developer-mode`）；模板用 `ui_client_hide|default(false)`，L-4 未注入时按内部形态显示。
- **不做（P3 / 另立批）**：删除全局默认音色 + 存量三态迁移（ERS5QQ 全案）——本条只做闸门 + 新建默认，存量档按现状**只读推导**（有克隆录音 → 克隆声；其余 → 预置声并标注「沿用全局语音设置」），不写回；用户为人设自选「备用声」（ERS5QQ 方案 2 后半）未做——D-L4 是「一律降级文字」；Token 钱包耗尽 → edge 兜底（P6 计费降级）保留，但兜底音色同样过同性别选声。
- **取舍写明**：① 克隆态「参考音文件必须存在」做成 **warning** 而非 400——托管 / hub 登记的档参考音可能只在远端（`_has_voice_identity` 接受 speaker_id 的既有语义），硬拦会让存量人设连改名字都保存不了；② `test_persona_profile_merge` 的样例人设本身就是 Mizuki 同款非法组合（`avatar_clone + zh-CN-XiaoxiaoNeural`），改成合法预置态（测试意图＝富字段经 merge 保留，与语音无关）；③ `capabilities.photos` 不在基础 tab 另放开关——相册 tab 的「可发照片」总闸（`pma-cap-photos`）已是接管口，两处开关会打架；④ `personas.yaml` 同步按钮不删只藏——后端 canonical 链条仍在。
- **预存红（HEAD 干净树复核，与本条无关）**：`test_tts_lang_gate::test_lang_engines_routes_ja_to_mapped_engine`、`test_cosy_dialect::test_personas_html_dialect_select_only_cantonese`、`test_input_theme_contrast::test_template_inline_styles_no_fallback_vars_resolve`、`test_legacy_blue_ratchet::test_judged_files_tailwind_ceiling`、`test_template_bare_fetch_ratchet` 两例、`test_theme_contrast_gate::test_copilot_tiny_font_ratchet`；`test_template_inline_color_ratchet` 两例红来自 L-4 新模板 `_client_tier_upgrading.html`。
- **工具**（随 B/A 入库）：`tools/normalize_eol_to_head.py`（编辑器把 LF 文件存成 CRLF 时按 HEAD 换行风格改回——本条第一次提交前 `test_tts_pipeline_smoke.py` 整文件 diff 就是这么来的）/ `tools/check_template_js.py`（Jinja 渲染 + 内联 `<script>` 逐段 `node --check`）/ `tools/check_i18n_dup_keys.py`（AST 查同一 dict 重复键——本条中途在 `errors_stock.py` 抓到一组同名重复键并清掉）。
