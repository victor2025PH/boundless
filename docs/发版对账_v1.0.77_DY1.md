# 发版对账 v1.0.77 · DY-1（抖音接入 · 公共层 + 官方通道骨架）

> 线：DY（抖音，`docs/实施96_…_2026-09-07.md`）｜跨对话交接入口：`docs/抖音TikTok渠道族_进度总账.md`
> 性质：**纯增量 + 默认关**。抖音演示 worker（`platform_login.douyin.mock_enabled`）与官方通道（`douyin.enabled`）都默认关，
> 未开时生产零痕迹；策略层与回复窗执行器对未登记平台无限制（既有平台逐字节旧行为）。

## 一句话

抖音在服务端已与其它平台走同一条链（进线 → 收件箱 → 拟稿/回复 → 策略层拒收 → 回复窗配额 → 出站镜像），
官方通道（小程序 IM）的 API / Webhook / 令牌生命周期骨架已按文档契约落地并全分支钉测，**只等企业主体小程序资质**；
UI 层是「降级可见」（注册表 JSON 回落给出品牌色与正名，图标仍是通用气泡）。

## commit 落点

| # | commit | 内容 | 门禁 |
|---|---|---|---|
| ① | `26347522` | `src/integrations/platform_registry.py` 平台注册表单一事实源（13 平台）+ `scripts/platform_registry_export.py` → `src/web/static/platform_registry.json` | `tests/test_platform_registry.py` |
| ② | `9080bdb8` | `src/inbox/channel_policy.py` 渠道出站策略层 + 编排器 `send/send_media` 拒收 + `reply_split.cap_max_parts_for_platform` + `autosend_policy.decide(platform=)` + 拦因 policy 四族 zh/en | `tests/test_channel_policy.py` |
| ③ | `fa0860d9` | `src/integrations/douyin_mock_worker.py` 抖音演示 worker（默认关） | `tests/test_douyin_mock_worker.py`、`tests/test_douyin_e2e_alignment.py` |
| ④ | `9b518303` | `src/inbox/window_guard.py` 回复窗/配额执行器（从收件箱事实现算）+ TikTok 首条禁链 + `send-caps` `reply_window` + `unified_inbox.html` platColor/platName 回落注册表 JSON | `tests/test_window_guard.py` |
| ⑤ | `3758ef04` | `config.example.yaml` 注释文档块（channel_policy / douyin mock） | `tests/test_config_check.py` |
| ⑥ | `88f51826` | `src/integrations/douyin_official.py` 官方通道骨架（API / Webhook / worker / 令牌）+ admin 挂载 + 能力矿阵进表 + 策略层媒体白名单收窄仅图片 | `tests/test_douyin_official.py`、`tests/test_platform_matrix.py` |
| ⑦ | `044713fd` | `normalizer.py` 末尾注册表兜底（散表收口第一步）+ 令牌到期 → 会话健康表 `expired`（账号 chip「需重新登录」）+ `test_platform_matrix` 还原工厂表（修跨文件顺序依赖） | 同上 |
| ⑯ | 本批 | **实施100 T4/T5 + 实施99 P0-2**：TikTok.me 引流链接（`tiktok_me_link` / `sanitize_ref`，面板按 username 生成、ref 实时拼接复制）；进线 `referral.short_link.ref` 落 `peer_ctx.ref`（首条固定）+ `ref_counts` 近 30 天来源统计；`biz_stats` 记每个 webhook 事件 → `webhook_silence`（>24h 静默黄标）；`reauth_days_left` 7 天预警（自动续期 / 手填令牌分文案）进会话健康 detail 与面板；**自检接口** `GET /api/onboarding/{slug}/status`（凭证 / 路由挂载 / worker 注册 / 公网 https / 账号令牌态 / 地区阻塞 / 首条进线 / 静默 → 灯色 grey·blue·amber·red·green + 当前步 + 提示）；教程页顶部状态灯卡（抖音 / TikTok）；i18n 中英 24 键；路由基线 +1 | `tests/test_tiktok_official.py`（+3）、`tests/test_tiktok_oauth_connect.py`（状态灯四态 / 引流链接 / 来源统计 / 静默）|
| ⑮ | `3224506d` | **实施100 T3 一键接入**：面板保存应用凭证（`POST /help/onboarding/tiktok/credentials`）→ 注册地 + `GET /help/onboarding/tiktok/authorize`（state 带 region）→ 公开回调 `GET /webhook/tiktok/oauth/callback`（换令牌、scope 校验、资料、落注册表、**自动 `ensure_webhook`**）→ `POST /help/onboarding/tiktok/webhook` 手动重注册；账号行显示「令牌自动续期 · N d」/「手填令牌」；成功态带「去发一条私信」；i18n 中英 19 键；路由基线 +4；教程第 4 步改为三步操作 | `tests/test_tiktok_oauth_connect.py`（全应用端到端）|
| ⑭ | `3224506d` | **实施100 T1/T2 契约对齐**：官方验签 `Tiktok-Signature: t=,s=`（HMAC-SHA256 over `t.body`，容忍 300s 可配）；官方载荷 `user_openid + content 字符串` 二次解析，方向按 `to_user.id == business_id`，`referral.short_link.ref` / 引用 / media_id / embed_url 进 source，`im_mark_read_msg` 记录；我方发送成功 message_id 入幂等表（回显不二次落库）；OAuth（`/v2/auth/authorize/`、`/tt_user/oauth2/token|refresh_token/`）、`token_state` 到期前 30 分钟自刷、鉴权码先刷再重试一次、refresh 被拒双清；`complete_oauth`（scope 齐全校验 / `business/get` 资料 / `tiktok/<open_id>` 落注册表）、`ensure_webhook`（`/business/webhook/list|update/` 幂等）、`capabilities` 探测；决策入总账 + `docs/实施100` + `docs/实施99` | `tests/test_tiktok_official.py`（13 例重写）|
| ⑬ | `e61b69d8` | 教程页 **TikTok 面板**（第 4 步）：`POST /help/onboarding/tiktok/account` 登记 Business Account（business_id + access_token + **注册地必填**），账号按 `tiktok_regions.capabilities` 标「私信可用/不可用/待核实」「不能发图」「Shop 站点」「缺令牌」与替代路径；可顺手存 app_id/secret 到 overlay 并开 `tiktok.enabled` | `tests/test_douyin_oauth_connect.py::test_tiktok_panel_*` |
| ⑫ | `7c9f48d4` | 抖音令牌 **7 天预警**：`reauth_days_left()`（续满 5 次按 refresh 到期、无 refresh 按 access 到期）→ 会话健康 `authorized` 带倒计时 detail（不禁发）+ 面板黄标「N 天后需重新授权」 | 同上（+2 例）|
| ⑪ | `51fae76f` | 教程页 **抖音接入面板**（第 6 步）：`POST /help/onboarding/douyin/credentials`（`save_overlay_patch` 落 `config.local.yaml`，密钥留空=保留，审计）→ `GET /help/onboarding/douyin/authorize`（HMAC state 10 分钟，302 去抖音扫码）→ `GET /webhook/douyin/oauth/callback`（公开回跳；`DouyinApi.exchange_code` `/oauth/access_token/` → `complete_oauth` 把 `douyin/<open_id>` mode=official + access/refresh 令牌 merge_meta 落注册表，不抹人设）；面板列已授权账号与令牌状态 | `tests/test_douyin_oauth_connect.py`（全应用端到端：表单→跳转→回调→登记→页面）|
| ⑩ | `eff6b74d` | TikTok 官方通道骨架（TK-1 A+B 由 DY 对话代做，老板拍板并行）：`tiktok_regions.py` 地区模型 + `tiktok_official.py`（Business Messaging API 客户端 / HMAC 验签 webhook / worker：conversation_id 门槛、48h 超窗、鉴权码→needs_reauth、注册地门控发送与图片、3MB）+ 编排器注册钩子 + admin 挂载 + 能力矩阵进表 + config 文档块 | `tests/test_tiktok_official.py`（8 例，假 transport）|
| ⑨ | `b3c37b8d` | 渠道接入教程页 `GET /help/onboarding/{douyin\|tiktok\|payment-cny}`（`onboarding_guides.py` 双语数据 + `help_onboarding.html`）+ 小智 howto 条目 `onboarding-*` 进 HelpKB + i18n 包 `onboarding_guide_ui` + 路由基线登记；配套 `docs/实施98` 人民币收款主体清单（`65090da1`） | `tests/test_onboarding_guides.py`（4 例：数据 / 小智检索 top-3 / 页面中英 / 404）|
| ⑧ | `651cb1cd` | 收件箱 composer 回复窗信息带（倒计时 / 本轮还可发 N/M 条 / 预留提示 / 超窗·未开口灰显）+ 禁链·超字数即时红条提示；`send-caps` 新增 `outbound_policy`；新 i18n 包 `reply_window_ui`（zh/en 10 键）。模板**热更即生效**，内联脚本已 `node --check` 通过 | `tests/test_douyin_e2e_alignment.py`（UI 接线与键齐全）|

## 验收

- 本线测试文件 6 个共 80+ 例全绿；预览树（HEAD + 暂存）逐 commit 复验绿后再提交；共享文件只提本线 hunk（`stage_hunks` 自证两向 OK）。
- 端到端（完整 admin app）：`POST /api/internal/protocol/ingest` 进线 → `GET /api/unified-inbox/chats`（`can_send` / `send_modes` / `automation_mode` 同形）→ `GET /thread` → `POST /send` 外链 409 `policy_link_denied` 出平台规则人话 → 6 条配额后第 7 条 409 `policy_window_exhausted` → 客户再发言重置 → `send-caps?chat_key=` 带 `reply_window`。
- 官方通道骨架 16 例（假 transport）：验签 / challenge / 幂等 / 进线落库 / 回显镜像 / 场景选择 / 错误码映射 / 图片上传与 client_token 复用 / 卡片 / 令牌矩阵 / 刷新+续期两步 / 进私问候 / 注册门控 / 路由 / 会话健康上报。

## 装载 / 重启要求

- `.py` 全部需 zhiliao 实例重启装载（编排器 / 策略层 / 路由）；`unified_inbox.html` 与 `errors.py` i18n 热更已生效。
- 生产**不需要**任何配置改动；`douyin:` / `platform_login.douyin.*` / `channel_policy:` 均缺省即旧行为。
- 新增静态产物 `src/web/static/platform_registry.json`（随包）。

## 既有红（非本线，HEAD 同样红）

- `tests/test_unified_inbox_stage1.py` 三例：模板字串「建议你接管」/ `lang==='auto'` HEAD 亦无；路由子域数 34→37 被其它线新增模块改变。
- `tests/test_admin_route_inventory.py::test_no_unexpected_extra_routes`：5 个基线外端点（developer/business-domain、goals/custom-slots、kb purge）属其它线。
- `tests/test_platform_registry_consistency.py` 4 例：实施97 线 `wechat_kf` 在途未同步（他们已在改该测试）。

## 越界与边界点名

- 与实施97 线共用文件（`account_orchestrator.py` / `reply_split.py` / `unified_inbox_send_routes.py` / `admin.py` / `config.example.yaml` / `normalizer.py` / `unified_inbox.html`）**只提本线 hunk**；他们的未提交 hunk 原样留在工作树。
- `platform_registry.py` 已被实施97 线用于登记 `qq`（implemented=True，含备注）——注册表方向被验证。
- 桌面图标副本相对 web 版滞后的键（qq/qqbot）门禁只打印不判红，交实施97 收工时同步。

## 交接

- **交老板**：企业主体小程序 + 能力实验室申请（骨架已就位，资质是唯一阻塞）；`drafts.py` 加一行 `decide(platform=)` 的禁改例外；人民币收款主体；TikTok 是否提前到与抖音 P1 并行（`指令_TK-1`）。
- **交实施97 收工后**：散表迁移第二步（`platform_login.DEFAULT_PLATFORM_MODES` / `platform_icons.js` 读注册表，删 `ACCEPTED_COLOR_DRIFT`）；接入向导抖音企业版 / TikTok 卡（`channel_setup.CHANNELS`，**只需指向教程页面板**——凭证表单、扫码授权、账号登记已在 ⑪⑬ 落地）；`kf_window_guard` 与 `window_guard` 合并（微信客服关窗事件作为额外输入）。
- **TikTok 剩余（TK-1）**：C 店铺客服 API（Partner Center 授权、`seller.customer_service`、HMAC-SHA256 验签、3 秒 200）；Business Account OAuth 端点核实后把面板的「手填 access_token」升级为一键授权（现阶段令牌来自开发者门户）；令牌刷新按控制台文档；E huoke `reply_engine=chengjie` 桥 opt-in。
- **交 UI 批**：composer 信息带已落（⑧）；剩会话列表行的回复窗小徽标 / 抖音·TikTok 图标进 `platform_icons.js`（他线文件，等收工）/ 真浏览器截图验收（本批只做了 node 语法检查与接线断言）。
- **交 P0-4**：拿到抖音号后录 `creator.douyin.com` 私信页 DOM 选择器采样，边车与桌面 assistOnly 以它为准。
