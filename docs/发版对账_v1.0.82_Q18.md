# 发版对账 v1.0.82 — Q-18（全自动不被机制掐停：#292 #291 #293 #285 + #278 追加 cum 误触，P0）

> 指令：`docs/指令_Q-18_全自动不被机制掐停_2026-09-11.md`。底座：`docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-3 行——本批唯一动 `autosend_worker.py / autosend_policy.py` 的线）。总账：`docs/Q批_进度总账.md` Q-18 行。预算 $160。
> 接手方：先 `git -C D:\boundless log --oneline -10` 核对本表 commit 真在。全部是客户端 `.py` / 模板 / i18n 改动随 1.0.82；`.py`（adult_grader / autosend_worker / autosend_policy / abort_ledger / reply_diagnosis / 切档路由 / reply_settings 路由）须实例重启（`deploy\instances\restart_instance.ps1`）才生效，模板 / i18n 包热更即生效。
> 执行：2026-09-11。意向板 slug `q-18-agent-yield-defer-adult-cum-disambig-autose`。在 Q-3（`_human_priority_gate` / `cancel_inflight` / agent-typing 端点）、Q-15（adult_grader 钩子）、Q-17（risk_grader / 回复设置页卡片 / i18n pack 模式）之上改，不重做。

## 〇 为什么全自动会被自己的机制掐停（Mizuki × Jeeo 15:45）

1. **cum 误触**：印式英语「finally got your loving message cum reply」（cum = and）被 `_EXPLICIT` 的 `cum(?:ming|shot)?` 判 explicit → 软回应 + 24h adult 持有 + 「需人工」；一个孤立歧义词就能把全自动会话掐成人工。
2. **让位 = 丢稿**：Q-3 `_human_priority_gate` 返回 `agent_sent / agent_typing` 时 stage=batch/presend 直接 `cancelled`，客户那句就永远没回；坐席手发一句 → 60s 内所有入站全部无回应，且界面上看不出「AI 在让位」。
3. **拦截不可见**：`[autosend] abort` / `[needs_human] 打标` 只在日志里，运营看不到今天拦了多少、为什么。

## 0. 开工基线（全只读）

| 项 | 事实 |
|---|---|
| 意向板 | Q-18 登记；HEAD `911abdff`（开工）；索引空闲 |
| 共享文件 | 他人在途：`templates/unified_inbox.html` [HOT-ZONE ACTIVE，xlate-pop 线] / `static/workspace/unified-inbox.css` / `tools/verify_xlate_ui.py`；`i18n_packs/inbox_workspace.py` [HOT-ZONE 近期]。`adult_grader.py / autosend_worker.py / autosend_policy.py / reply_diagnosis.py / reply_settings_routes.py / reply_settings.html / unified_inbox_aggregate.py / unified_inbox_stored_read_routes.py` 开工时干净（提交前再核） |
| 代码事实 | `_human_priority_gate` 顺序：inflight cancel by → `mode_changed` → `agent_sent`（60s）→ `agent_typing`（60s）→ `risk_hold / needs_human`；batch 与 presend 两处调用都 `update_draft_status(cancelled, decided_by=abort:<code>)`。`note_agent_send / note_agent_typing` 记 ts 并 `cancel_inflight(by=…)`。`adult_grader.grade` 只看单条入站，`_hits` 子串去重；`naked` 单词在既有门禁里钉为 explicit（不列歧义） |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| A 成人词表消歧 | #278 追加（RKEJYF / N5N6QB） | `6882f874` | **`src/inbox/adult_grader.py`**：`_EXPLICIT` 去掉 `cum(?:ming\|shot)?`，只认 `cumming / cum ?shot / make (me\|you\|her\|him) cum / wanna cum / want to cum / cum (on\|in\|inside\|for) (me\|you\|my\|your)`；新 `neutralize_cum(text)`（`grade` 匹配前调用）：`(summa\|magna)? cum laude` → 「with honours」、`X cum Y` → 「X and Y」（左词 me/you/her/him/us/to/wanna/gonna/i'm… 或右词 on/in/inside/for/all/over/hard/now… 为强信号上下文 → 不豁免）；`_AMBIGUOUS_EXPLICIT = {cock, pussy, anal, hard on, boner, nude}` + `is_ambiguous_hit()`：命中的露骨词**全是**歧义词且（露骨+提及）总信号 <2 且无施压词 → 不判 explicit，降 flirt（有调侃口吻）/ mention，返回 `ambiguous=True`；≥2 信号或 + 施压词 → explicit / pressure 不变；强词（nudes / porn / blow job / make me cum…）单词仍 explicit。**`tests/test_adult_grader_gate.py`** +8 例：6 句参数化（「message cum reply」原句 / 「summa cum laude」/ 「colleague cum friend」→ 不命中且 `regrade_inbound` 原样放行——零打标 / 零 hold / 零软回应；「make me cum tonight」/「i'm cumming」/「send a cumshot vid」→ explicit/pressure）+ `neutralize_cum` 强上下文保留 + 孤立歧义词（pussy cat / cock crowed → mention、boner lol → flirt；show me your pussy / pussy send it now / pussy and boobs → explicit/pressure；nudes / blow job 单词仍 explicit） | `test_adult_grader_gate` 29 passed；邻接 `test_risk_grader_gate` 37 passed |
| B 让位 = 延后不是丢弃 + C 切全自动明示接回 / ay- chip / 诊断 + E 门禁 | #292 #291 | `d9d7f6c6` | **`src/inbox/autosend_policy.py`**：`YIELD_DEFER_REASONS=("agent_sent","agent_typing")` / `AGENT_YIELD_WINDOW_SEC=60` / `AGENT_YIELD_MAX_DEFERRALS=10` + 纯函数 `agent_yield_state(sent_ts, typing_ts, now, window) -> {active, by, since, until, remaining}`（发送优先于打字，与复检顺序一致；worker / 诊断 / chip 三处同口径）。**`src/inbox/autosend_worker.py`**：`_human_priority_gate` 只判不处置（语义不变）；两处调用方对 ③④ 两码改 **defer**——batch：稿留 pending（不 resolve 不取消）、`_yield_mark_defer(conv, code, draft_id, stage)` 登记 `conv → {by, until=坐席最后活动+60s, since, draft_id, stage, deferrals, first_ts}`，同稿同窗只计一次 / 只落一行 `[autosend] defer=<code> stage=batch draft= conv= until= (in Ns, deferral k/10)`，`_yield_schedule_wake` 用 `loop.call_later` 在 until+0.5s 唤醒主循环（否则要等 min_interval 兜底），下一 tick 再过闸放行即 `_yield_resume_if_deferred` → `[autosend] resume=agent_window_passed stage= draft= conv= by= waited= deferrals=`；presend：撤出站登记、行保持 approved、载荷带 `_yield_defer / _yield_by / _yield_deferrals` 进 `_retry_queue`（`next_ts=until+0.5`，`_attempt=0` → 到点重走 `_deliver_one`：再过闸 + fresh_guard——坐席又活动再让位 / 客户又说了新稿覆盖 / 切手动 mode_changed 老路取消）；**点名取消信号**（`cancel_inflight` 已把行改 cancelled，`_hp_signalled`）来的原因码不 defer（Q-3 原语义保留）；`cancel_inflight` 同范围一并取消 `_retry_queue` 里的让位改期稿（`abort=<by> stage=deferred`——让位等待中坐席真发 = 客户那句被人接，防窗过后同问双答）；同稿连续让位 >10 → `yield_exhausted=` WARNING + 走老放弃路；`resume_agent_yield(cid, by)`：清 `_agent_sent_ts / _agent_typing_ts`、摘登记、改期项 `next_ts=0` + `notify_new_l2()`，`[autosend] resume by=<by> conv= released= draft=`；`agent_yield_state(cid)`（状态 + draft_id / stage / deferrals）；`status_snapshot` 多 `total_yield_deferred / total_yield_resumed / total_yield_exhausted / yield_now`。`mode_changed / risk_hold / needs_human` 下方逻辑一字未动（`test_other_abort_codes_unchanged` 钉住）。**`routes/unified_inbox_aggregate.py`**：`_agent_yield_state_q18(request, cid)`（+`window_sec`）/ `_resume_agent_yield_q18(request, cid, by)`（worker 缺席 → None / had_yield=False，绝不抛）。**`routes/unified_inbox_stored_read_routes.py`**（开工干净，整文件 add）：GET `/api/unified-inbox/automation` 多 `agent_yield`；POST 同路由 `mode=auto_ai` → `_resume_agent_yield_q18(by=mode_select)`，响应多 `agent_yield_resumed`（非 auto_ai → None，manual 旧 cancel 语义照走）；新 **POST `/api/unified-inbox/agent-yield/resume`**（body platform/account_id/chat_key 或 conversation_id，`by` 缺省 chip）→ `{ok, conversation_id, had_yield, released, by, agent_yield}`；诊断路由传 `worker=app.state.autosend_worker`。**`src/inbox/reply_diagnosis.py`**：`diagnose_conversation(..., worker=None)`，让位活跃 → `out["agent_yield"]` + finding `agent_yield`（warn；params by / until / remaining_sec / since / draft_id / stage / deferrals），码表补行。**`templates/unified_inbox.html`**（他人在途 xlate-pop 线；`stage_hunks --mine "ay-\|_ay\|_AY\|ay_chip\|agent[-_]yield\|Q-18\|inbox\.ay\.\|high_risk:1\}"` 6/7 切分）：`ay-` 块 `_ayState / _aySet / _ayNoteLocal / _ayRemain / _ayByLabel / _ayChipHtml / _renderAyChip / _ayResume`（chip「AI 让位中 · 坐席 60s 内发过 · 23s 后接回」蓝底、`role=button`、1s 倒计时、到点自消并回拉一次真值、点击 POST agent-yield/resume → toast）+ `_renderHdrTags` 钩子——**这两段随兄弟线 `7c4aae3b` 整文件 add 先入了库**（共享工作树实录，功能归本单）；本 commit 六处一行接线：`_refreshConvStatus` 读 `d.agent_yield` → `_aySet`（45s 周期 + 手发成功后立即刷）/ `_publishTyping` → `_ayNoteLocal('agent_typing')` 本地先亮 / `setMode` 响应 `agent_yield_resumed.had_yield` → toast 追加「AI 已接回」+ 清 chip / `loadModeState` 切会话 `_aySet(null)` 防串显 / `_diagFindingText` known 表 + `agent_yield` 的 by_label / until_hhmm / n 插值 / `_diagFixHtml` 「立即让 AI 接回」按钮。**新 i18n pack `i18n_packs/agent_yield.py`**（ZH / EN / ZH_HANT 同键集 8 键：`inbox.ay.by_sent / by_typing / chip / chip_t / resumed / resume_fail / fix_resume` + `inbox.diag.agent_yield`；繁体手写，不碰生成物）。**`tests/test_agent_yield_defer.py`** 17 例；**`tests/test_risk_hold_q3.py`** `test_agent_typed_recently_blocks_new_batch_within_window` 断言由 cancelled 改为 pending + defer（旧断言即华哥事故根因） | `test_agent_yield_defer` 17 passed（手发后 60s 内入站 → 稿留 pending、一行 defer 日志、零 abort；窗过 → 发出 + resume 日志；切全自动 → 立即发 + `resume by=mode_select`；再手发再让位（deferred 2 / resumed 2）；presend 让位 → approved 行 + `_retry_queue` 1 项 → 到点重投 1 条；让位中真发 → 改期稿 `abort:agent_send stage=deferred`、窗过不再发；点名信号仍 `abort:agent_typing`；超上限 → `yield_exhausted` + `abort:agent_sent`；manual / risk_hold / needs_human → `mode_downgraded / abort:risk_hold / abort:needs_human` 原样；GET automation 带 agent_yield、POST auto_ai 接回、chip 端点 by=chip / worker 缺席 / 400；诊断 finding warn + fail-open；i18n 三语同键；模板接线静态）；`test_risk_hold_q3` 23 passed；autosend 全域回归 63 文件 1403 passed，3 failed 均非本单（`test_admin_route_inventory::test_no_unexpected_extra_routes` = 本单新路由跑在基线补行之前，补行后单跑绿；`test_reply_settings::TestPlatformExpert::test_platforms_match_worker_registry` Q-17 已记录他人在途；`test_silent_exception_ratchet` inbox 322>198 开工前已红，本单新增 except 全带 logger.debug）；`test_i18n_coverage / test_i18n_extra_langs / test_i18n_zh_hant / test_inbox_inline_handlers_exported / test_workspace_template_js_syntax / test_reply_diag_customer_waiting` 392 passed，1 failed = `test_sealed_templates_no_legacy_date_formatters`（`toTimeString().slice` 在 HEAD 已存在，兄弟线） |
| D 拦截台账可见 | #293 | `40a4d8cd` | **新 `src/inbox/abort_ledger.py`**：`KEY="abort_ledger:v1"` app_settings KV 单键 JSON 数组**滚动 200 条**（不建表，线程锁）；行 `{ts, conv, code, stage, hit, source, draft_id, reason}`；`normalize_code(code, category)`（`agent_send`→`agent_sent`、`mode_switch / mode_downgraded`→`mode_changed`、`adult:*` / category=adult→`adult`、`risk:*`→`risk_hold`、其余打标→`needs_human`）；`record / rows / parse_rows / rows_for_conv(纯函数, CLI 用) / summary(window_h=24, last_n=5) → {window_h, since, total, counts{7 码含 0 + 出现过的其他码}, recent[新→旧], capacity, stored}`；写入 best-effort 绝不抛。**钩子（只挂既有日志点，不新造判定）**：`autosend_worker._ledger_abort` 挂 `[autosend] abort=` 三处（batch / presend / 我新增的 deferred；risk_hold / needs_human 顺带取 `risk_hold.active_record` 的 reason / hit）+ 班表扣留 `log_off_hours_hold` 之后按 `_ledger_ws_stamp[conv]=until_ts` 去重记一条 `work_schedule`；`protocol_autoreply.tag_needs_human` 落地行（`[needs_human] 打标 … cooldown_skipped=false`）→ `adult` / `needs_human`（stage=tag，hits 进 hit；「保持」/ 冷却跳过两条早退路径不计）。**让位（defer）不进台账**——它不是拦截。**`reply_settings_routes.py`** 新 `GET /api/reply-settings/abort-ledger?hours=24&last=5` → `{ok, enabled, reasons, …summary, recent[].name（store.get_conversation 补显示名）}`；store 缺席 → `enabled=False` 全零不报错；hours 夹 [1, 336]、last 夹 [1, 50]。**`reply_settings.html`**（开工干净、整文件 add；`rps-al-` 前缀）：新卡 `#rps-al-card` 置于「风控分级」之后：标题 + 「24h 共 N 次」chip、按原因码 chip 计数（0 的调灰）、最近 5 条表（会话（显示名 · cid）/ 时间 MM-DD HH:MM / 原因人话 / 命中词 + 阶段小字）、空态；`RPS_AL_I18N` 词条查表；`rpsAlLoad()` 由 `rpsLoad()` 异步装载，旧后端 404 / `enabled=false` 整卡隐藏。**新 i18n pack `i18n_packs/abort_ledger.py`**（ZH / EN / ZH_HANT 同键集 16 键：`rps_al_title / hint / col_conv / col_time / col_reason / col_hit / empty / total / stage` + `rps_al_r_<7 码>`）。**`tools/why_no_reply.py`**：ro SQLite 直读 `abort_ledger:v1` → 本会话 24h 记录 → `out["abort_ledger"]` + verdict `abort_ledger`（warn；让位类标注「窗过自动发」）。**`tests/test_admin_route_inventory.py`** `_ADDITIONS_2026_09_11_Q18_AGENT_YIELD` 两条（agent-yield/resume、abort-ledger） | `test_agent_yield_defer` 23 passed（+7：归一表 / 滚动 200 / 24h 窗 / worker 三处钩子进账 + defer 不进账 + deferred 阶段 / 打标 adult vs needs_human + 保持不重复计 / 路由含 name / store 缺席 / i18n 三语覆盖 7 码 / 模板接线 / CLI verdict）；`test_admin_route_inventory` 全绿；邻接 `test_risk_hold_q3 / test_handoff_tag_meta / test_protocol_autoreply / test_needs_human_autoclear_207 / test_risk_grader_gate` 129 passed；`test_reply_settings / test_i18n_*` 523 passed，2 failed 均他人在途（`test_platforms_match_worker_registry`、`test_sealed_templates_no_legacy_date_formatters`） |
| E 门禁纳入 R 批 | #292 | `c2f341db` | **`scripts/gate_sweep.ps1`** 固定清单 +3：`test_adult_grader_gate.py`（Q-15，本单 +8 例）/ `test_risk_grader_gate.py`（Q-17 落点表曾要求「R 批门禁清单加」但未落脚本，本单补）/ `test_agent_yield_defer.py`（本单，23 例）。指令表里的四条门禁全在 `test_agent_yield_defer.py`：手发后 60s 内入站 → defer 不丢（`test_inbound_within_60s_after_agent_send_is_deferred_not_dropped`）/ 窗过自动发（`test_window_passed_auto_resumes_and_sends`）/ 切全自动立即发（`test_mode_select_auto_clears_yield_and_sends_immediately` + 路由 `test_mode_select_auto_route_resumes_yield`）/ 再手发再让位（`test_agent_sends_again_yields_again`）；adult 6 句在 `test_adult_grader_gate::test_q18_cum_disambiguation` | 三文件合跑 `python -m pytest tests/test_adult_grader_gate.py tests/test_risk_grader_gate.py tests/test_agent_yield_defer.py` 89 passed（29 + 37 + 23） |

## 1.1 回访（重启后看）

- 收件箱：造一句「finally got your loving message cum reply」到全自动会话 → 正常出稿自动发，会话头不出「需人工」/「成人让位」，日志无 `[adult]` 行。
- 全自动会话里坐席手发一句 → 会话头立刻出蓝 chip「AI 让位中 · 坐席 60s 内发过 · 59s 后接回」倒数；此时客户来一句 → 日志 `[autosend] defer=agent_sent stage=batch … until=`（**没有** `abort=agent_sent`），草稿面板里稿仍 pending；倒数到 0 → chip 消、`[autosend] resume=agent_window_passed` → 稿发出。倒数中点 chip 或顶栏重选「全自动」→ `[autosend] resume by=chip|mode_select` → 稿立即发。体检面板在让位期间出黄行「AI 让位中：坐席 60s 内发过，稿留队等到 HH:MM…」+「立即让 AI 接回」按钮。
- 回复设置页（`/reply-settings`）滚到「风控分级」下面 → 新卡「今日拦截」：重启后先是全零 + 空态（台账从重启起记，历史不回填）；造一条真拦截（如高风险句让 `[needs_human] 打标`、或让位期间坐席真发一句把 deferred 稿顶掉 `abort=… stage=deferred`）→ 45s 内刷新页面，对应原因码 chip +1、最近表出一行（会话显示名 · 时间 · 原因人话 · 命中词）。`sqlite3 config/inbox.db "select value from app_settings where key='abort_ledger:v1'"` 能看到裸账。`python tools/why_no_reply.py <cid>` 输出多一段 `abort_ledger` + verdict「24h 内被拦 N 次」。让位（defer）**不**出现在台账里——它不是拦截。
- 打包 / 发版：`.py` 改动（adult_grader / autosend_policy / autosend_worker / abort_ledger / protocol_autoreply / reply_diagnosis / 两条路由）**须实例重启才生效**，本单未重启；模板 / i18n pack 热加载。会话头 `ay-` chip 主体是随兄弟线 `7c4aae3b` 整文件 add 进 HEAD 的，本单 `d9d7f6c6` 只补接线钩子——回归若看到 chip 不出，先核 `unified_inbox.html` 里 `_renderAyChip` 是否还在。
