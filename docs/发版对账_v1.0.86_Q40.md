# 发版对账 v1.0.86 — Q-40（大文件媒体线：相册裸流 25MB + HEIC 转码 + 聊天视频回平台表 · #316 追加 / #322）

> 指令：`docs/指令_Q-40_大文件媒体线_相册裸流25MB与HEIC与聊天视频回平台表_2026-09-15.md`。工单 **#316 追加**（钧 09-13 相册 2MB）/ **#322**（Q7HJDU / 4KV58W 相册 UX）+ 老板 09-15 19:52「相册可传大图、聊天可发大文件图片 / 视频」。总账：`docs/Q批_进度总账.md` Q-40 行。预算 $120。
> 红线：不突破平台官方上限；Messenger 不放开（仍表外 25）；相册不存两份 / 不接 RAW；不改 Q-6 匹配链；send-media 抽共用**行为零变化**；`unified_inbox.html` 只动 `_stageMedia / _xhrSendMedia / _mediaItemHtml`（+ 紧随 `_mediaItemHtml` 的 `_mediaShrinkItem` 同 hunk）；不自己重启 zhiliao；#316 / #322 只改 confirmed + fix_note 草稿。
> 执行：09-15 20:08–。

## 0. 开工基线

| 项 | 事实 |
|---|---|
| 意向板 | `agent_probe -Intent "Q-40 big-file media (persona_media_routes, media_stream.py, personas.html, media_limits.py, unified_inbox.html _stageMedia/_xhrSendMedia/_mediaItemHtml)"`；index FREE；同刻 Q-39 线刚登记（`image_autosend / autosend_helpers / outbound_translate / unified_inbox.html sys-chips`）。zhiliao 31.9 min 前重启过 → 本线 `.py` 不自行重启 |
| 前置 Q-39 段 0 | 20:09 核对 HEAD `fee598af` 未含 `is_persona_album_upload_path`；本线 `git add` 三路径后 `git commit` 报 no changes——**Q-39 线同刻已提交 `4fc8eee0`**（#316 相册 2MB 豁免 + `test_album_body_limit_q35` 4 passed + Q35 落点表 D 行）。前置满足，不重复提交 |
| git | HEAD `4fc8eee0`；`personas.html` 工作树 3 个他线 hunk（`@@ -1234` / `@@ -5341` Q-26 C `psn-ar-`，`@@ -9323` zh_hant `cp-i18n-ext ?v=20260912b`）；`unified_inbox_send_routes.py` 1 个他线 hunk（`clone_engine_offline` 文案分支）；`unified_inbox.html` 他线 31 行未提交（Q-39 D `sys-chips` 等）——全部 `stage_hunks --mine` 切开 |
| 现场（HEAD 核） | 与指令「事实」表一致：相册口 `request.form()` 整包 + `upload.read()` 全进内存，图 10MB / 视频 50MB / 3min 常量；`media_limits.DEFAULT_VIDEO_CAP_MB = 50`；`_xhrSendMedia` `timeout=300000` 固定；`pmaDeleteItem` 成功 `await pmaLoad()` 整页回顶；卡片缩略图 `<a target=_blank>`；`_alSel` 批量条只有「加到选中」。`test_media_send_stream_m3` 14 passed 基线（其中 3 处断言钉死 50MB 压顶） |
| 依赖 | 本机无 `pillow-heif`（`_heif_available()=False`）→ HEIC 后端兜底走假解码器门禁 + `skipif` 真解码；`requirements.txt` 不加硬依赖（可选，import 失败即跳过） |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| A+B 后端 | #316 | `49f7236b`（`unified_inbox_send_routes.py` 经 `stage_hunks --mine "Q-40" --theirs clone_engine_offline`，Q-39 B `retranslate` 路由 + 他线 `clone_engine_offline` 两 hunk 留工作树） | **新** `src/web/media_stream.py`：`DRAIN_MAX 256MB / WRITE_BUF 1MB / HEAD_BYTES 64KB`、`drain_stream`、`read_head(it, min_bytes)`、`stream_to_file(it, path, head, cap_bytes, write_buf) -> (size, overflow)`（I/O 异常包 `StreamRecvError(cause, received)`）、`upload_file_chunks(UploadFile)`、`declared_length(request)`——抽自 `_send_media_streamed` 的读头 / 写缓冲 / 吞流三段。`unified_inbox_send_routes.py` 只改 import：`_SEND_MEDIA_DRAIN_MAX / _SEND_MEDIA_WRITE_BUF / _drain_stream` 保留为别名，头部循环 → `_read_head`，落盘循环 → `_stream_to_file`，`except _StreamRecvError` 取 `cause / received` 还原原日志字段（`test_media_send_stream_m3` **零改动 14 passed**）。`persona_media_routes.py`：上传口两分支——`Content-Type` 非 multipart → body 即文件、元数据走 query（`name` 必填，`triggers / caption / tags / weight / min_bond_level / enabled`）；multipart 旧路 `upload_file_chunks` 喂同一落盘代码。流程：守卫（裸流被拒先 `drain`）→ ext / HEIC 判定 → `Content-Length` > 上限即 413（带 `bytes / limit_mb / over_mb / media_type`，不读 body）→ `read_head` 空＝`empty_file` → 落 `.up_<hex>.part` 临时件（超限即停 + 吞流 + 413；`StreamRecvError` → 502 `recv_error`）→ 照片整读（≤25MB）走原 sniff / sha 去重 / EXIF 剥离 / pHash 链后写正式名、视频按头 sniff + 文件流式 sha + `os.replace`，都回读 16 字节验写 → 元数据 / 封面 / 入库不变（ffprobe / 缩略图改 `run_in_threadpool`）。**上限** `_MAX_PHOTO_BYTES 25MB / _MAX_VIDEO_BYTES 100MB / _MAX_VIDEO_DURATION_MS 5min`，`resolve_persona_media_limits(config)` 读 `inbox.persona_media.limits.{photo_mb, video_mb, video_max_sec}` 夹紧 1–2048MB / 1–7200s，缺省回模块常量（测试 monkeypatch 契约不变）。**HEIC**：`_heif_available()` / `_heic_to_jpeg(data, quality=90)`（`pillow-heif` 可选，`exif_transpose` 后 JPEG 落库，`ext=.jpg`，响应 `converted:"heic->jpeg"`，相册只存一份）；不可用 → 400 `ext_not_allowed` + `hint:"export_jpeg"` + 新词条 `err.pmedia.heic_export_jpeg`（zh/en，「手机相册 分享 → 导出为 JPEG」）。**新** `GET /api/personas/media-caps` → `{album_stream_upload:true, photo_max_mb, video_max_mb, video_max_sec, image_exts, video_exts, heic_backend}`（前端能力键；旧后端 404 ＝仍 multipart）。日志：`_reject` 每条 `[pmedia] 上传拒绝 pid= name= status= reason= size= ms= mode=`；成功 `[pmedia] 上传 … reason=ok size= ms= mode= declared= … heic=`；去重 `reason=deduped size= ms=`。`test_admin_route_inventory` 基线 +1；`test_persona_media_routes` HEIC 用例钉 `_heif_available=False` 分支 + `hint` | `test_persona_media_routes` 30 + `test_persona_media_ai_routes` 6 + `test_album_body_limit_q35` 4 = 40 passed；`test_media_send_stream_m3` 14 passed（零改动）；`test_admin_route_inventory` 4 passed；`test_media_stream_q40` A/B 段（见 E 行） |
| A+B+D 前端 | #316 #322 | `912f32c9` | `personas.html`（`stage_hunks --mine` 12 hunk 全本线；Q-26 C `psn-ar-` ×2 + zh_hant `cp-i18n-ext ?v=` 留工作树）：**A** `_pmaLoadCaps()`（`GET /api/personas/media-caps`，404 → `{}` 走 multipart 老口径；`pmaLoad` 预取）、`_pmaXhrUpload(pid, file, meta, onprog)`（XHR 裸流 `POST /media?name=&triggers=`，`Content-Type=file.type`，`timeout=max(300s, MB×3s)`，`upload.onprogress`）、`pmaUpload` 逐文件 `_pmaPrepareFile → 体积预检（caps 同源，超限本地记 too_large 不发请求）→ 裸流 / multipart 回落`，`_pmaUpLast.live={i,total,name,pct,phase}` 由 `_pmaUpLive` 喂 `_pmaRenderUploadResult` 顶部 `.pma-upr-live` 进度行（`<progress>` + 预处理中 / 上传中 n% / 服务端处理中），每文件 `console.info('[pma] upload file= status= reason= size= sent= ms= prep= mode=')`；rec 新增 `ms / prep / sent_size / over_mb / hint`。**B** `_pmaPrepareFile`：HEIC/HEIF → `createImageBitmap` 解码 → canvas JPEG 0.9（Chromium 解不开 → 原样上送让后端 pillow-heif 兜底，note `heic_client_undecodable`）；静图长边 >4096 或 >8MB → 长边 4096 / JPEG 0.85；PNG **真有透明**（`_pmaHasAlpha` 128px 采样）才保 PNG，不透明 PNG 截图转 JPEG（重编 PNG 体积不降）；压完不更小且没缩边 → 原图；视频 / GIF 不碰。失败文案：`pma_fr_too_large_detail`（X MB 超过 Y MB 超出 Z）/ `pma_fr_heic`（导出 JPEG）。**D** `pmaDeleteItem` 成功 → `_pmaRemoveCards([mid])`（摘 DOM 卡 + `_pmaState.items/stats` 减 + `_alSel/_pmaEdits` 清 + `_pmaRefreshSummary()` 只重绘 `#pma-summary-wrap` / 徽标 / `#pma-count` / 引导条），失败 / 异常 → `_pmaReloadKeepScroll()`（记 `.dpane.scrollTop` → `pmaLoad` → 还原）；摘要块抽 `_pmaSummaryHtml(items, stats, dim)`；批量条新增「🗑 删除所选」`_alDeleteSelected()`（逐条 DELETE，404 视为已删，成功摘卡、失败整刷保滚动）；卡片 `<a target=_blank>` → `onclick=_pmaLightbox(mid)` 页内灯箱 `#pma-lightbox`（`img` / `video controls autoplay`，✕ / 点遮罩 / Esc 关，Esc 在 `document` **捕获相位** `stopPropagation` 不落到 `closeProfileEditor`，caption 触发词 · 宽高 · 体积）。CSS `.pma-upr-live` / `.pma-lightbox`（`--th-bg-scrim60/45` token，z 3000 > `.drawer` 901）。词条 `persona_apply_modal.py` zh/en ×9（`pma_fr_too_large_detail / pma_fr_heic / pma_up_live_prep / pma_up_live_server / al_del_sel{,_confirm,_ok,_fail} / pma_lb_close`）+ `zh_hant_auto.py` regen 只切本线 hunk（他线 `bt_tpl_fixed` 留工作树）；`ui-build.txt` bump `20260915-2047` notice | 浏览器行为 harness（IDE Chromium + 本地 echo 服务，`C:\tmp\q40`，不进库）：10.7MB 带透明噪点 PNG → 重编不更小保原图；10.3MB 不透明 PNG → `shot.jpg` 2.1MB；6000×600 JPEG → 4096×410；800×600 JPEG 原样；假 `.HEIC` → `heic_client_undecodable` 原样上送；3MB mp4 原样；`_pmaXhrUpload` 回环 echo：query `name / triggers` ✓ `Content-Type: image/png` ✓ body 10.7MB magic `89504e47` ✓ onprogress 100；150MB 超时公式 450s。Node `node --check` 内联 script 0 错（`test_template_inline_js_syntax`）；`test_media_stream_q40` A3/D 段静态契约；前端门禁红全是他线旧债（`personas.html` `--th-bd-red30` Q-38 / 内联色 1 处 `ca1deed8`、`unified_inbox.html loadConnectModes` / `t.cat_'+` / `toTimeString`、`reply_settings rps-po-empty`） |
| C 聊天视频回平台表 | #316（老板 09-15 需求） | `f5d14978` | `media_limits.py`：`DEFAULT_VIDEO_CAP_MB 50 → 0`（D-M5 临时压顶撤回；`video_limit_mb` 运营显式压仍生效；`PLATFORM_MEDIA_CAP_MB` **不加 messenger**，表外仍 25）→ `media_cap_mb_for(video)`：LINE 100 / TG 200 / WA 64，`send-caps.video_max_mb` 同源随之。`unified_inbox.html` 只动三函数（`stage_hunks --mine "Q-40"`，Q-39 D 等他线 hunk 留工作树）：`_stageMedia` 超限 toast 改 `inbox.media.too_large_over`（大小 / 本会话上限 / 平台 / **还差 Z MB**，console 带 `over=`），入队记 `capMb / canShrink(静图 jpeg·png·webp >8MB) / origSize`；`_mediaItemHtml` 第一行 `体积 · [已压缩 X→Y ·] 本会话最多 N MB（LINE）`（N＝`it.capMb || _mediaCapMb(file)`＝`send-caps.video_max_mb / media_max_mb` 按类别），queued 且 `canShrink` 出「压后发送」按钮（默认仍发原图）；**紧随其后**新增 `_mediaShrinkItem(id)`（同 hunk）：`createImageBitmap` → 长边 2560 / JPEG 0.82（PNG 保 PNG）→ 只换该件 `file`（cmid / 归属不变）+ 标 `shrunk`，解码 / 编码失败或不更小 → 保原图 + toast `inbox.media.shrink_fail`；`_xhrSendMedia` `timeout = max(300s, ceil(MB)×3s)`。词条 `inbox_workspace.py` zh/en ×6（`inbox.media.cap_hint / too_large_over / shrink_send / shrink_send_title / shrunk / shrink_fail`）+ `zh_hant_auto` 同批 hunk。`test_media_send_stream_m3` **3 处断言随语义改**（唯一改动：`== 50` → 平台表；98MB 用例显式 `video_limit_mb: 50` 压回验同一条 413 路；send-caps `video_max_mb == 200`）——A 段抽共用时该文件零改动 14 passed 已在 `49f7236b` 前验过 | `test_media_send_stream_m3` 14 + `test_media_limits_169` + `test_line_media` = 101 passed；`test_media_stream_q40` C 段 3 条；harness：9.1MB 3000² 噪点 JPEG → `_mediaShrinkItem` 3.4MB 2560²、`_renderMediaBar` 调 1 次、零 toast；`test_template_inline_js_syntax` / `test_template_inline_handler_arg_quoting` 绿，`test_inbox_inline_handlers_exported` 唯一红 `loadConnectModes` 他线旧债 |
| E 门禁 | #316 #322 | `0e8ed67e` | **新** `tests/test_media_stream_q40.py` 27 项（+1 `skipif` 无 pillow-heif）：A1 `media_stream` 单元（写头 + 余块 / 超限即停 + 余块可 drain / I/O 异常带 `received` / `read_head` 只读到阈值）+ send-media 只 import（`R._drain_stream is MS.drain_stream`、`_send_media_streamed` 源里无 `open(local,"wb")`）；A2 `/api/personas/media-caps` 默认 25/100/300 + 配置 40/9999→2048/60；25MB 裸流成功（query `triggers/caption` 入库、文件 == 上传字节、`.part` 不残留、日志 `reason=ok mode=stream size= ms=`）/ 26MB 413（`bytes=size limit_mb=25 over_mb=1.0`，目录空，日志 `reason=too_large size= ms=`）/ chunked 无 Content-Length 走流式闸 413 `bytes>25MB` / multipart 旧路仍通且与裸流按 sha 互去重 / 视频 4MB 通·4MB+1 413（同一条路压小上限验）/ 时长可配 2s 拒 + 文件回收 / bad magic·空文件·缺 name；B HEIC 无后端 → 400 `hint=export_jpeg` + caps `heic_backend=False`、有后端（假解码器）→ 只存一份 `.jpg` `converted=heic->jpeg`、解码失败 → `bad_content` 不留 `.part`、真 pillow-heif 解码（装了才跑）；limits 夹紧；`/media/test` 不豁免；A3 `pmaUpload` 契约（caps / XHR / prep / multipart 回落 / 进度行 / console reason·size·ms）+ `_pmaPrepareFile` 规则；C 平台表 + messenger 25 + 三函数契约（超时公式 / cap_hint / shrink 2560·0.82）+ 词条占位符齐平；D 删图成功分支无 `pmaLoad` / 灯箱无 `window.open` / Esc 捕获相位 / 删除所选 + 词条 / `err.pmedia.heic_export_jpeg` | 27 passed 1 skipped（装 pillow-heif 后 28 passed） |
| D 修补 + B 依赖 | #322 #316 | 待 commit | **e2e 真浏览器发现**：缩略图 `<a href=… onclick=preventDefault>` 仍被 `_loading_overlay.html` 的 **document 捕获相位 `a[href]` 拦截器**先接住（它在 onclick 之前跑，`defaultPrevented` 还是 false）→ 当跨页导航亮遮罩 + 「页面未能打开」横幅——这正是 4KV58W「大图弹独立窗口」在桌面壳里的来源。改 `<div class="pma-th-link" role="button" tabindex="0" data-url=…>`（Enter / 空格可开，`cursor:zoom-in`），彻底脱离 `a[href]` 语义；`_pmaSummaryHtml` 批量条 `style="display:none opacity:.55"` 少分号（沿旧代码）→ 补 `;`；`pmaUpload` 失败 rec 补 `hint / over_mb`（上一批漏切的 hunk）。`requirements.txt` 加 `pillow-heif>=0.16.0`（可选依赖语义不变；坐席包要吃到 HEIC 后端兜底须随 R86 打包进环境）。本机 `pip install pillow-heif` 1.7.0 → `_heif_available()=True`，`skipif` 真解码用例转绿 | `test_media_stream_q40` 28 passed；e2e（见 §2）灯箱真点击零横幅、Esc 只关灯箱抽屉仍开 |

## 2. 真机（09-15 21:0x–21:4x）

| 项 | 机 / 账号 | 文件 | 结果 | 证据 |
|---|---|---|---|---|
| LINE 60–90MB MP4 | 本机 zhiliao 实例 LINE `Uk4bhjJ4…kQOg`（online，Keep memo 发给自己，客户不可见） | ffmpeg 合成 1080p30 78s testsrc2 + 440Hz，**77.0MB**（80,733,075 B） | `tools/probe_line_media.py --send-self --kind video --file big75.mp4 --max-mb 120 --confirm` → **delivered=True** `message_id=631906959548744248` **113.3s**；即时回读 `object_info.encodeStatus=ing`（LINE 服务端还在转码，`empty_body` retryable）；`--verify-msg` found contentType=2 `DURATION=78000`；~3 min 后 `--verify-recent-media --max-mb 120` 回读落盘 **21.9MB MP4 magic OK**（LINE 服务端转码后的版本，与 J-3 C 段「LINE 服务端会重编大视频」一致） | `C:\tmp\q40\line_send.json` / `line_verify.json` / `line_verify2.json`（不进库）；`[2026-09-15 21:0x]` 一次性 client，与运行中 worker 分离 |
| TG 150MB | 本机 zhiliao 实例 TG `8244899900`（protocol online，`send-caps.can_media=true`），发到自己（Saved Messages） | 同法合成 170s **150.1MB**（157,346,906 B） | 经**运行中** zhiliao `POST /api/unified-inbox/send-media`（裸流 `mode=stream`）→ `200 {ok, delivered:true, message_id:4735, media_type:"document"}`，`[send-media] ok … size=150.1MB recv=2.37s dispatch=204.10s total=206.53s`；本地镜像 `protocol_media/telegram/out_8244899900_3915762f9d57.bin` **sha256 与源一致**、magic `ftypisom`。**口径说明**：运行中的后端仍是旧代码（`video_max_mb=50` 压顶，本线不许重启），150MB 以 `.mp4` 送会被旧 413 拦——故按 **document** 类送（TG 同一 `upload.saveBigFilePart` 链，150MB 上传耗时 204s ≈ 0.73MB/s），证明 150MB 文件可送达；**视频类 150MB** 待 R86 重启装载 `DEFAULT_VIDEO_CAP_MB=0` 后由发版线用同法补一条（`.mp4` 名）。0.73MB/s 也印证超时随体积：200MB 视频固定 300s 必超时，`max(300s, 200×3s)=600s` 才够 | `app.log` L66420 / L66434；`C:\tmp\q40\tg_send.py` 输出（不进库）。副作用：收件箱多一条 TG 自己→自己会话 + 150MB 镜像文件 |
| 相册 iPhone HEIC | **新代码** `create_app` 起在 `127.0.0.1:18866`（临时数据根 `C:\tmp\q40\e2e`，一次性 token），IDE Chromium 跑**真 `personas.html`**（登录 → Lin → 相册 → `pmaUpload(File[])`） | pillow-heif 编的 **12MP HEIF 12.15MB** `IMG_0001.HEIC`（本机无 iPhone；同容器同解码链） | 结果条 **✓ 3 uploaded**；逐文件 console `[pma] upload file=IMG_0001.HEIC status=200 reason=ok size=12153853 ms=4364 prep=heic_client_undecodable+heic->jpeg mode=stream`——Chromium 解不开 HEIC → 原样裸流上送 → 后端 pillow-heif 转 **4000×3000 JPEG 3.68MB 一份**（相册目录只有 `.jpg`，无 `.heic`）；灯箱真点击打开显示 4000×3000 · 3.5MB | 截图 `page-2026-09-15T13-37-08-390Z.png`（灯箱）；e2e 服务 access log `POST /api/personas/lin/media?name=IMG_0001.HEIC 200` |
| 相册 8MB PNG | 同上 | 不透明噪点 PNG **8.65MB** `shot8mb.png`（1600×1800） + 附带 `wide.jpg` 6000×900 5.26MB | `shot8mb.png` → 前端 `_pmaHasAlpha=false` → JPEG 0.85 **2.05MB** 上送（`prep=shrunk 1600x1800->1600x1800`，`sent=2051901`）200 ok 781ms；`wide.jpg` → `shrunk 6000x900->4096x614` 1.43MB 200 ok 511ms；结果条 ✓ 3 uploaded | 同上 console 4 行 + `_pmaUpLast.results` 三条 `ok:true` |
| #322 删图 / 灯箱 / 删除所选 | 同上 e2e 页 | — | `pmaDeleteItem`：5→4 卡、`pmaLoad` 调用 **0** 次、徽标 `(4)`、`Showing 4/4`、`.dpane.scrollTop` 718→471（内容变短夹紧，不回顶）；灯箱 Esc → 灯箱关、`profile-drawer.open` 仍 true；「🗑 删除所选」勾 2 张 → `2 selected` → 逐条 DELETE 200 → 4→2 卡、`_alSel` 清空、`pmaLoad` 0 次 | e2e access log 三条 `DELETE … 200` |

## 3. 收工

待。
