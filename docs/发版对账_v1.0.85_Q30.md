# 发版对账 v1.0.85 — Q-30（认领体系退出单人端 · 档位一个真相 · 原因码人话表 · #309 #310 #311 #307 + #308 复核）

> 指令：`docs/指令_Q-30_认领体系退出单人端与档位一个真相与原因码人话表_2026-09-12.md`。本线按老板粘贴的
> 开工语做 **A / B / C / D 四段**（#309 #310 #311 #307 + #308 复核，预算 $100）；指令文档 22:3x 追加的
> **E 段（#319 切档瞬间同句双发）不在本次开工语里，未做**——见文末「未做 / 留债」。
> 底座：Q-26 落点表 `docs/发版对账_v1.0.84_Q26.md`（状态带 `conv_state` 只加源不重做）。总账：`docs/Q批_进度总账.md` Q-30 行。
> 红线：显式 `ui_visibility.seat_mode: multi` 行为一字不改；B13 单坐席守卫不动；`peer_bot_guard` 判定不动（只可见化）；
> `_human_priority_gate` 60s / defer 语义不动；`conv_state` 零写 KV（新源只读 marker / worker 状态）。
> 执行：09-12 23:0x–。

## 0. 开工基线

| 项 | 事实 |
|---|---|
| 意向板 | `agent_probe -Intent "Q-30 seat_mode default single + #311 gate locate + reason codes … (ui_visibility.py, admin.py, store.py, reply_settings.html rps-ms-, reply_settings_routes.py, autosend_worker.py, unified_inbox.html, conv_state.py, autodraft_helpers.py, tests/test_seat_mode_q30.py)"`；index FREE。同刻 ACTIVE 他线：Q-31（`unified_inbox.html` panel tags / `store.py` stop_contact 迁移 / `inbox_workspace.py`）、Q-35（`personas.html` / `unified_inbox.html`）、Q-36、ASR P1、label-leak；`reply_settings.html` / `reply_settings_routes.py` 里 Q-26 C 未提交 hunk（`rps-ar-` 组 / `_rk_policy_defaults`）→ 本线只碰自己的行 |
| git | HEAD `10619433`；zhiliao 18 min 前刚重启（本线 `.py` 改动等下一窗搭车，不自己重启） |
| #311 真闸（B 段先定位再改） | `tmp_diag\W2CSGA` 报告 backend.log 按 `abort / agent_sent / claim` 关键词捞 40 min：该会话 **零 `[autosend] abort`、零 `defer=agent_typing/agent_sent`、零认领拦发**（后端从不按 claim 拦发送，claim 只被 sla_watcher / scheduled_reporter / context 读）。唯一闸行：`12:11:20 [pacing] first_reply hold conv=whatsapp:12084403608:917983336714 silence=9.9h hold=115s remain=104s`（O-1 D 拟人首回延迟）。时序：12:11:18 稿 ①（L2）→ 首回延迟 115s 留 pending；12:13:04 爆发合并新入站 → 稿 ② 覆盖 ①（同 draft_id）→ 12:13:11 `[xlate] outbound` 进投递 → 12:14:07 `[pacing] … total=51.6 composing=sent` **发出**；12:14:27 稿 ③ → 12:14:28 `[xlate] outbound` 进投递（报告 12:14:28 截止）。结论：**不是 claim、不是 agent_typing，是「首回延迟 + natural 节奏 ≈ 3 分钟」全程对坐席不可见**（状态带一直绿「AI 会自动回」，草稿条两分钟不动就报「全线不发」）。按指令分支「其他码 → 补状态带」：`first_reply_hold` 进 `conv_state`（此前根本不在带上），`/api/workspace/typing` **不改**（无证据） |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| A 单人端退出认领体系 | #309 #310 | `e13bd46f` | `ui_visibility.py`：`resolve_seat_mode(config, users, presence)` **缺省 single**——显式 `ui_visibility.seat_mode`（single/multi）一字不改；无显式时只看 `count_recent_agents(presence, 30min)`（distinct `agent_id`、`last_seen_at` 落窗；不看 status——「有几个人」不是「几个人空闲」）≥2 → multi，否则 single；`users` 参数保留仅兼容，**不再参与判定**；取不到 / 坏行 / 异常 → single；新 `seat_mode_verdict` → `{mode, source: explicit\|presence\|default, agents_recent, window_min}`。`admin.py` `_multi_seat_now`：presence 来自 `app.state.inbox_store.list_agent_presence(active_within_sec=1800)`，60s 快照缺省 `False`、异常 `False`（旧：`True`）。`store.py` `_MIGRATIONS` 末段前插 `DELETE FROM conversation_claims /* q30_seat_mode_default_single */`（内容哈希唯一、一次性；表无 source 列全清，租约本 15min 过期）。`reply_pacing_settings.py` `FIELDS["ui_visibility.seat_mode"]`＝enum `SEAT_MODE_CHOICES=("", "single", "multi")` default `""` hot；`reply_settings_routes.py` `_snapshot_extras` 加 `seat_mode_effective`（GET/POST 同源）。`reply_settings.html`（`rps-ms-`）：「👥 多坐席协作」卡落在 Q-26 C 的 `rps-grp-auto` 组内（今日拦截之后）——开关开＝写 `multi`、关＝回基线（基线 `single` 保持、否则 `""`＝自动判）；读数行「收件箱当前按「多坐席」显示 · 近 30 分钟有 2 个坐席登录过工作台」；`feat:"seat"` 探测（旧后端白名单缺键整卡隐藏、收集/回填不参与）；`rpsSeatDetect(d)` 挂 rpsLoad / rpsSave。新 i18n 包 `i18n_packs/seat_reason_q30.py`（zh/en/繁体手写，含 B/C/D 词条）。**提交手法**：`store.py` / `reply_settings_routes.py` 走 `stage_hunks --mine/--theirs`；`reply_settings.html` 我的卡与 Q-26 C 未提交块在同一 HEAD 插入点（al-card 之后），任何 context 宽度 git 都并成一 hunk → 用同思路的**行归属**版（HEAD + 我的块 → blob → index，工作树不动、Q-26 C 行留工作树），index-vs-worktree 差里我的行 0 条 | `tests/test_seat_mode_q30.py` **17 passed**：指令四例（1 账号 / 2 账号 1 在线 / 2 在线 / 显式 multi）+ 无数据·坏行·迭代抛异常 → single / 30min 窗口边界·同人多行·offline 仍算人·空 id 不算 / 显式 single 压 presence·未知值回落 / 真钟 / verdict 四来源 / 白名单 enum 与 `ui_visibility` 常量同源 + sanitize 拒 `team` / snapshot 读值 / 迁移一次性（撤记账重开库即清空、`migration_errors==0`）/ admin 接线（不读用户表、`return True` 不在）/ 模板接线（卡在 `rps-grp-auto` 与 `rps-grp-quota` 之间、两处 feat 分支、window 暴露）/ 三语词条。`tests/test_ui_visibility.py` 旧钉子改新语义（2 账号 ≠ multi、fails-closed）**全绿**。同轮回归：`test_reply_settings`（除他线 douyin/tiktok 平台注册表红）/ `test_i18n_zh_hant` / `test_i18n_extra_langs` / `test_i18n_coverage`（除他线 `toTimeString` 红）/ `test_template_unique_ids` / `test_inbox_inline_handlers_exported`（除他线 `loadConnectModes` 红）/ `invariants/test_migration_observability` 绿 |
| B 定 #311 真闸 → 首回延迟进状态带 | #311 | `6ae59bdb` | **真闸＝O-1 D 拟人首回延迟，不是 claim、不是 agent_typing**（证据见 §0 第三行）。`autosend_worker.py`：`_first_reply_remaining` 在判成 hold 时登记 `_first_reply_until[conv]={until, draft_id, silence_sec, first_contact, hold_sec}`（remain 归零 / cfg 关 / hold=0 / 危机穿透即 pop；>2048 条清过期）；新只读口 `first_reply_hold_state(cid)` → `{active, until, remaining, draft_id, silence_sec, first_contact, hold_sec}`（到点即 inactive）。`conv_state.py` 新源 `pacing_hold`（只在 auto 档采集）→ `deferred` / tone warn / will_send True / `reason_code=first_reply_hold` / `text_key=inbox.cs.deferred.first_reply`（沉寂）或 `.first_contact`（首次来信）/ `until_ts` 倒计时（前端 deferred 分支的 `{n}` 秒表零改动复用）/ **action none**（刻意节奏不给「立即接回」）/ params `silence_h · hold_sec · draft_id`；优先级排 `agent_yield` 之后。人话（zh/en/繁体）：「AI {n} 秒后发出 · 沉寂 {silence_h} 小时后的首条回复刻意慢一点」+ 悬浮「…这不是『全自动不发』」。`/api/workspace/typing` **不改**（指令条件分支：仅 agent_typing 才改；留观见 §2） | `tests/test_conv_state_q30.py` B 段 **7 passed**：琥珀倒计时无动作 / 首次来信变体 / inactive·旧 worker 无方法·到点 → 绿 / 人审档不出 / 让位压首回；**W2CSGA 回放**（真 `AutosendWorker._first_reply_remaining`：出站 T−9.9h + 入站 T + 稿 T+0.5，12:11:20 判定 → hold∈[60,300]（实录 115s）→ `first_reply_hold_state.active` → `compute` 出 `deferred/first_reply_hold`、`silence_h=9.9`、`until_ts` 对齐 → 到点 remain=0、登记 pop、带回绿）+ 报告包证据断言（该会话无 abort / defer，唯一 hold 行 hold=115s，≥3 稿生成、≥2 稿进投递） |
| C 两个原因码可见化 | #307 #312 #314 | `6ae59bdb` | ① `unified_inbox.html` 纯插一行 `_HANDOFF_REASON_KEYS.media_lie_caught_repeat='inbox.handoff.r_media_lie_caught_repeat'`（`stage_hunks --mine media_lie_caught_repeat` 切出，他线 11 个 hunk 全 SKIP）；词条「客户说没收到图 · 已连续 2 次 · 交给你」zh/en/繁体（`seat_reason_q30.py`）——码来自 `autosend_helpers` / `persona_reply` 两处 `_tag_needs_human(reason="media_lie_caught_repeat")`。② 新 `src/inbox/peer_guard_marker.py`（`app_settings` KV `peer_guard_skip:<cid>`＝`{ts, reason, soft}`，与 `ai_fail_marker` 同模式，不动 store / 不建表）：`autodraft_helpers.py` 守卫 **跳过处** `mark(reason)`、**放行处** `clear`（标在场才落盘）；`peer_bot_guard.py` **一字未碰**。`conv_state.py` 新源 `peer_guard` → `held` / danger / `reason_code` 原码 `colleague:<id>` `ops_group:<id>` 或守卫码 / `text_key` 按前缀 `inbox.cs.held.colleague` `…ops_group` `…peer_guard` / **action none**（出路是改名单）/ params `kind · id · code · settings_url=/reply-settings#rps-sec-guard`；软停（daily_budget soft）不算；需人工标优先于它、它优先于边车 / 让位。人话：「AI 不拟稿不发 · 对方在『永不自动回复』名单（同事 / 本租户账号 {id}）」+ 悬浮指路 设置 › 自动化与风控 › 单会话额度 · 防机器人互刷（never_auto_reply）。③ 门禁 `tests/test_reason_codes_q30.py`：`ABORT_REASONS` 每码归一进 `abort_ledger.REASONS` 且 `rps_al_r_<code>` 三语齐 / `conv_state` 会吐的每个 `inbox.cs.*` text_key（含 marker 前缀表）三语齐 + `_t` / `_HANDOFF_REASON_KEYS`（字面量 + 追加行）每键可解且含 media_lie / `_csHeldWhy` 三段回落 + `_l1ReasonText` → `inbox.l1r.other` 回落在场 / marker 前缀表与 `peer_bot_guard` 的 `colleague:` `ops_group:` 返回同源 | `test_reason_codes_q30.py` **7 passed**；`test_conv_state_q30.py` C 段 **7 passed**（marker 读写清 / colleague 红 held 无 ack 带 id·settings_url / ops_group + 通用码 / soft 不 held / 需人工 > peer_guard > 让位 / compute 零写 / autodraft 接线 mark 在 return 前·clear 在放行处·守卫文件无 marker 引用）；`test_conv_state_q26` 源集合钉子改 9 源后 **20 passed** |

## 2. 未做 / 留债

- **E 段（#319 UG6KJG 切档瞬间同句双发）**：指令文档 22:3x 追加，本次开工语（#309 #310 #311 #307 + #308，$100）未含 → 未做。落点在 `autosend_worker.py` `resume_agent_yield` 放行互斥 + `_deliver_once.claim` 键改 conv+归一化文本 + 让位回队载荷不算重试 + `tests/test_yield_resume_dup_q30.py`，需单独开工。
- `/api/workspace/typing` keydown 即 ping：`unified_inbox.html` 的 textarea `keydown` 任何键（含 Shift / 方向键 / 空 textarea）都触发 `_publishTyping` → `note_agent_typing` → 60s 让位。W2CSGA 无此证据，按指令分支不改；留观：若后续 `abort_ledger` 出现 `agent_typing` 但坐席没打字，按指令「只在 textarea 真有字符时 ping、切会话即 `resume_agent_yield(by=switch)`」再动。
- `unified_inbox.html` `const MULTI_SEAT = {{ 'true' if ws_multi_seat is not defined … }}` 的「未定义 → true」模板兜底未改（admin.py 恒注入 `ws_multi_seat`，且该文件为他线 ACTIVE 热区；改它会撞 `test_inbox_template_gates_claim_surfaces_on_seat_mode` 的计数断言）。
