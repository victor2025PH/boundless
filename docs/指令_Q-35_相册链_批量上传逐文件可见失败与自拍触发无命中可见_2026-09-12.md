# 指令 Q-35：相册批量上传逐文件可见失败 + 修 logger 格式串（#316）；#306 只补坐席侧一行提示

> 预算 **$60**（首版 $80，20:5x 核对后缩：#306 的「无命中 AI 不沉默」已在库，只剩坐席侧可见）。工单 **#316（4HK54G，jun，1.0.84，13:37）** / **#306（BPMEWX，Vanessa，1.0.84，11:43）**。落点表 `docs/发版对账_v1.0.85_Q35.md`。

## 已修 / 未修（HEAD `0b606199`）

| 项 | 状态 | 证据 |
|---|---|---|
| 相册无命中时 AI 不沉默、不空头承诺 | **已修** | `image_autosend.py` L920 `note_album_miss` → `persona_reply.py` L137–167 `consume_album_miss` + `prompt_addenda.album_miss_addendum`（AI 会按场景改口）；相册空认领 Q-20 D 已修（ZQ53RN） |
| 无命中时**坐席侧**可见（#306 用户问「后台传了很多图为什么不发」） | **未修** | `[album_match] … candidates=0`（L1123）只打日志；状态带 / 草稿条无一字 |
| 批量上传逐文件失败原因（#316） | **未修** | `personas.html` L3831–3846 `pmaUpload()` 逐文件串行 POST，失败只 `fail++`，不记文件名 / HTTP 状态 / 响应体；结尾「成功 N / 失败 M」（L3847） |
| 服务端 logging 格式串错误 | **未修** | 4HK54G：`--- Logging error --- TypeError: not enough arguments for format string`（无栈帧）；`persona_media_routes.py` L408 那条参数数对得上，错在别处——要扫 |
| 4HK54G 里到底几张失败、为什么 | **未知** | 后端只见 13:26:08 / :09 两张 2 MB 成功 `vision=ready`，之后无上传行 → 其余要么没发出（前端），要么被拒在路由前（体积 / 类型 / 413） |

## 要做的三段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 逐文件结果** | `pmaUpload()`：每文件记 `{name, ok, status, reason}`（读 `r.status` 与 JSON `error / reason`；网络异常 `network:<msg>`；超时 `timeout`）；结束显示成功数 + **可展开失败列表**（文件名 · 原因人话：太大 / 类型不支持 / 近重复已有（409 视为已存在，不算失败）/ 服务端 5xx / 超时）+ 「重试失败项」（DataTransfer 重建 FileList） | `personas.html` L3818–3870、词条 `pma_*` zh/en + 繁体 regen |
| **B 服务端入口 + logger** | 复现：一次选 ≥6 张 2 MB 图；核对上传路由前的体积 / 类型 / 并发限制，每个拒绝分支回 JSON `{ok:false, reason, detail}` 不裸 4xx / 5xx；全仓扫 `logger.*("…%s…", …)` 占位符与参数数不等的调用修掉（先从 `persona_media_*` / `auto_tag` / `album_semantic_recall` 找），加 `tests/test_logger_format_args.py` 静态门禁 | `persona_media_routes.py`、`persona_media_import.py`、tests |
| **C #306 坐席侧一行** | `image_autosend` `candidates=0` 时除 `note_album_miss` 外再写 `conv_state` 一条 note（`album_no_match`，带 query），状态带琥珀「相册没有匹配『自拍』的图 · AI 已改口 · 去相册补标签」+ 深链 `/personas?pid=<pid>&pma=1&filter=notrg`；门禁一例 | `image_autosend.py` L1123–1135、`conv_state.py`、`unified_inbox.html` 状态带、`personas.html` 深链参数 |

## 红线

- 不改匹配打分（Q-6）与 `album_miss_addendum` 行为。
- `personas.html` 工作树有 Q-26 C 未提交 +11 行（`psn-ar-`）——开工先 `git diff` 看归属，`stage_hunks.py --mine`。

## 开新对话粘贴

```
读 docs/指令_Q-35_相册链_批量上传逐文件可见失败与自拍触发无命中可见_2026-09-12.md，按它做 Q-35（#316 #306；先看「已修 / 未修」表，note_album_miss / album_miss_addendum 已在库不要重做：A personas.html pmaUpload 逐文件记 name/ok/status/reason，结束显示成功数 + 可展开失败列表（原因人话，409 近重复算已存在）+ 重试失败项 / B 复现一次选 ≥6 张 2MB 图，服务端每个拒绝分支回 JSON {ok:false, reason, detail}，全仓扫 logger 占位符与参数数不等的调用修掉并加 tests/test_logger_format_args.py 静态门禁 / C image_autosend candidates=0 时写 conv_state note album_no_match 带 query，状态带琥珀「相册没有匹配『自拍』的图 · AI 已改口 · 去相册补标签」+ 深链 /personas?pid=&pma=1&filter=notrg，门禁一例）。预算 $60。先 scripts\agent_probe.ps1 -Intent 登记；开工先 git diff engines/chengjie/src/web/templates/personas.html 看 Q-26 C 未提交 hunk 归属，提交用 python tools/stage_hunks.py --file <路径> --mine <regex> --apply；不改 Q-6 匹配打分与 album_miss_addendum。每个 commit 带 #316 或 #306 并立刻补落点表 docs/发版对账_v1.0.85_Q35.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-35 行并 agent_probe -Done。
```
