# 指令 Q-35：相册链两件——批量上传逐文件可见失败（#316）+ 自拍触发无命中可见并可跳转补标（#306）

> 预算 **$80**。工单 **#316（4HK54G，jun，1.0.84，09-12 13:37）** / **#306（BPMEWX，Vanessa，1.0.84，09-12 11:43）**。落点表 `docs/发版对账_v1.0.85_Q35.md`。
> 前置：Q-6（相册触发词 / 批量触发词 / 近重复）、#238（首传引导设锚脸 + vision 不可达提示）已在库。

## 事实（证据 `tmp_diag\4HK54G` / `BPMEWX`；代码只读核对）

- **#316** 4HK54G 后端 30 min 内只有两行 `[pmedia] 上传 pid=WARM29432 … bytes≈2.05MB dur=0ms vision=ready`（13:26:08 / :09），之后再无上传行——用户选了一批，成功的只有两张，其余没到后端或被拒在服务端入口前。前端 `personas.html` L3831–3846 `pmaUpload()` 逐文件串行 POST `/api/personas/{pid}/media`，失败只 `fail++`，**不记文件名、不读响应体、不读 HTTP 状态**；结尾只显示「成功 N / 失败 M」（L3847）——用户看到的就是「批量上传失败」，没有任何线索。同报告里另有一条 `--- Logging error --- TypeError: not enough arguments for format string`（未带栈帧），是某处 `logger.*("%s …", 少一个参数)`——要找出来修掉，它可能就吞掉了真正的失败原因。
- **#306** Vanessa 人设 Mizuki 相册「后台已传很多图」，试触发「自拍」无命中不发；`src/inbox/image_autosend.py` L1123 只打日志 `[album_match] conv=%s query=%s candidates=%s picked=%s fallback=%s`，`candidates=0` 时**界面零反馈**（草稿条 / 状态带都不说），用户以为发图坏了。五视角方案 P0-4。

## 要做的四段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 逐文件结果** | `pmaUpload()`：每个文件记 `{name, ok, status, reason}`（读 `r.status` 与响应 JSON 的 `error / reason`，网络异常记 `network:<msg>`，超时记 `timeout`）；结束后状态区显示成功数 + **可展开的失败列表**（文件名 · 原因人话：太大 / 类型不支持 / 近重复已有 / 服务端 5xx / 超时），并提供「重试失败项」按钮；失败项不清空 `inp.files` 对应条目（重建 FileList 用 DataTransfer） | `personas.html` L3818–3870、词条 `pma_*` zh/en + 繁体 regen |
| **B 服务端入口** | 复现 4HK54G：一次选 ≥6 张 2 MB 图；核对 `persona_media_routes.py` 上传端点前的体积 / 类型 / 并发限制（FastAPI 默认 body 限制、`_MAX_BYTES`、近重复 409 语义），每个拒绝分支必须回 JSON `{ok:false, reason:<code>, detail}` 而不是裸 4xx / 5xx；找出并修掉 `not enough arguments for format string` 那条 logger 调用（全仓 `logger.*("...%s..." , ...)` 参数计数扫一遍，加 `tests/test_logger_format_args.py` 静态门禁） | `persona_media_routes.py`、`persona_media_import.py`、tests |
| **C 无命中可见** | `image_autosend` 在 `candidates=0` 时回喂 `conv_state`（state `held` 不对——是 **`will_send=True` 的 note**：`note_code=album_no_match`，带 `query`）；状态带琥珀一行「相册没有匹配『自拍』的图 · 去人设相册补标签」+ 深链 `/personas?pid=<pid>&pma=1&filter=notrg`；草稿条同句（走 Q-30 D `inbox.reason.album_no_match`，Q-30 未落时先加这一个键）；`fallback=<某图>` 时也说明「按相近标签『日常』发了」 | `image_autosend.py` L1100–1130、`conv_state.py`、`unified_inbox.html` 状态带、`personas.html` 深链参数 |
| **D 复验 + 门禁** | BPMEWX 复验：Mizuki 相册里到底有几张带「自拍 / selfie」触发词；AI 建议词是否真参与匹配（`_auto_tag` 的 `suggested` 与用户 `triggers` 是否同一匹配集）——若只匹配用户手填词，改成两集并匹配并在卡上标明「AI 词也会用于匹配」。`tests/test_album_visibility_q35.py`：0 候选 → note 落 conv_state + 深链正确；上传 3 成 2 败 → 失败列表两行含原因 | tests |

## 红线

- 不改匹配算法的打分顺序（Q-6），只让「没匹配到」与「为什么」可见。
- `personas.html` 工作树里有别线未提交的 +11 行（09-12 晚）——开工先 `git diff engines/chengjie/src/web/templates/personas.html` 看归属，不带走别人的 hunk。
- 上传失败重试不重复入库：近重复 409 视为「已存在」不算失败。

## 开新对话粘贴

```
读 docs/指令_Q-35_相册链_批量上传逐文件可见失败与自拍触发无命中可见_2026-09-12.md，按它做 Q-35（#316 #306：A personas.html pmaUpload 逐文件记 name/ok/status/reason，结束显示成功数 + 可展开失败列表（原因人话）+ 重试失败项 / B 复现一次选 ≥6 张 2MB 图，服务端每个拒绝分支回 JSON {ok:false, reason, detail}，找出并修掉 4HK54G 里「not enough arguments for format string」的 logger 调用并加 logger 参数计数静态门禁 / C image_autosend candidates=0 回喂 conv_state note album_no_match 带 query，状态带琥珀「相册没有匹配『自拍』的图 · 去人设相册补标签」+ 深链 /personas?pid=&pma=1&filter=notrg，fallback 时说明按相近标签发了 / D BPMEWX 复验 AI 建议词是否参与匹配，不参与则两集并匹配并在卡上标明；门禁 tests/test_album_visibility_q35.py）。预算 $80。先读 Q-6 落点表与 #238 相关 commit，再 scripts\agent_probe.ps1 -Intent 登记；开工先 git diff engines/chengjie/src/web/templates/personas.html 看别线未提交 hunk 归属，提交用 python tools/stage_hunks.py --file <路径> --mine <regex> --apply；不改 Q-6 匹配打分顺序；近重复 409 算已存在不算失败。每个 commit 带 #316 或 #306 并立刻补落点表 docs/发版对账_v1.0.85_Q35.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-35 行并 agent_probe -Done。
```
