# 发版对账 v1.0.86 — Q-39（发送真相四闸：#327 跟图循环 / #326 翻译空串硬 HOLD / #323 要图意图闸 / #324 全部账号筹码 + #316 追加）

> 指令：`docs/指令_Q-39_发送真相四闸_跟图循环与翻译空串与要图意图与全部账号筹码_2026-09-15.md`。工单 **#327（9PYWPG）/ #326（THBSHN）/ #323（XG3UZU）/ #324（YS54YK）**，全部 skuio 1.0.85.0；**#316 追加**（09-13 钧 mid 1548，相册 2MB 豁免，代码已在工作树未提交）。总账：`docs/Q批_进度总账.md` Q-39 行。预算 $160。
> 红线：不发原文纪律不动；不改 Q-6 匹配打分 / `pick_media` / `note_album_miss` / `album_miss_addendum` / `detect_offer_media`（只在它们前面加闸）；单人端不出任何系统筹码、零新颜色；`.py` 须 zhiliao 重启才生效，**本线不自己重启**；共享树脏文件（label-leak / ASR / `desktop/main.js` / Q-26 C–E / Q-40 connect-modes）一字不碰，`stage_hunks --mine "Q-39 X"` 切自己的 hunk。
> 执行：09-15 20:08–。

## 0. 开工基线

| 项 | 事实 |
|---|---|
| 意向板 | `agent_probe -Intent "Q-39 send-truth four gates (image_autosend / autosend_helpers / outbound_translate / unified_inbox.html sys-chips / test_send_truth_q39)"` → slug `q-39-send-truth-four-gates-image-autosend-autose`；index FREE；ACTIVE 他线：`deploy/desktop/monitor_seat_logs.*`（坐席日志监控）。zhiliao 19:37 重启（31 min 前）→ 本线 `.py` 攒下一窗，不自行重启 |
| git | HEAD `912f32c9`（Q-40 A+B+D，`ui-build 20260915-2047`）。本线要碰的文件里**他线未提交 hunk**：`autosend_helpers.py`（label-leak：`_safe_voice_inbox_text` / 译文 `system_label` 剥除）、`persona_reply.py`（label-leak：`_OUT_MEDIA_WORD` / `strip_out_label` / `media_form_note` L273–423 + L850）、`unified_inbox.html`（Q-40 connect-modes：`_showConnectModesFail` / `loadConnectModes` + zh_hant `cp-i18n-ext ?v=20260912b`）、`unified_inbox_send_routes.py`（Q-40 A `media_stream` 抽取 + `clone_engine_offline`）、`store.py`（他线 +19）。`image_autosend.py` / `outbound_translate.py` / `translation_service.py` / `conv_state.py` / `autosend_worker.py` / `test_admin_route_inventory.py` 干净 |
| 0 段 #316 | `4fc8eee0`：`admin.py` `is_persona_album_upload_path`（恰好 5 段 `/api/personas/{pid}/media|face-ref`）+ 中间件 OR + `tests/test_album_body_limit_q35.py`（4 passed）+ `发版对账_v1.0.85_Q35.md` D 行——显式三路径，`git diff --cached --name-only` 只这三个 |
| #327 真闸回放 | 9PYWPG 报告包 `backend.log` 段为空（关键词窗 180 min 内无行），改用 **HEAD 代码回放**（`tmp/q39_replay.py`，HEAD 游离 worktree vs 工作树）：入站 `[图片内容] 一名年轻男子的自拍照，戴着帽子在户外微笑，背景是海边` + Mizuki 相册一行 `kind:selfie` 无触发词 → HEAD：`detect_selfie_request(desc)=True`、`requested_scene_kind(desc)=selfie` → `[album_match] … candidates=1 picked=mz1` → **真发** `/static/mizuki1.jpg` 配 LLM 旧照口径配文「Found this old one of me, thought you'd like it」（与截图三句同源：相册条目无运营配文 → `_llm_caption_safe(freshness="old")`）。即真闸＝**系统识图行被当客户的话喂进 `pick_registered_media`**，两道意图判定都被「自拍」二字点亮；不是承诺兑现链（`_mctx` 在 `_last_is_img` 时本就为 False）。工作树同输入 → `skip=inbound_image_no_intent` 零发 |
| #323 真闸 | `persona_reply._prompt_addenda` L142 `scene = requested_scene_kind(inbound) or …` → 「the view」→ outdoor → `ask=True` → 拟稿期自探一次 miss；投递期 `run_autosend_image` → `pick_registered_media` `_ask = … or bool(_scene_cls or _scene_kind)` 再跑一次 → `album_miss_marker.mark` n=2 → 红条「今天 2 次」。RVMWYH 报告同形（群里一条报告文本也跑了 `[album_match] query=skuio 花无缺 报告已收到…`） |
| 既有红（HEAD 同红，非本线） | `test_image_autosend` 7 红 + `test_caption_truthfulness` 3 红 + `test_persona_media_question_guard` 1 红（Q-6 B「随机兜底默认关」后 generic 无标池测试未更新）；`test_outbound_lang_gate::test_target_unknown_no_reference_blind_send_counted`（D-M3 后 lang_unknown 改 HOLD）；模板门禁 8 红全他线文件（`loadConnectModes` 未挂 window / `createImageBitmap` / 内联色 / bare fetch / 日期格式）。HEAD 游离 worktree `D:\_wt_q39` 同集合复核 |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| A+C 意图闸 / 跟随预算 / 配文防重 / 同 mid | #327 #323 | `8ee760d3`（首版 `b5e89fa6` 误用 `git commit -- <路径>`——带路径的 commit 提交的是**工作树**内容，把 label-leak 线 `autosend_helpers` / `persona_reply` 整文件卷入；未推送即 `reset --soft` 重做，index 只留本线 hunk 后**裸** `git commit`） | **新** `src/inbox/image_send_gate.py`：`compute_image_intent(peer_text, history, inbound_media_type, assume_intent, directive_override, trigger_terms)` → `IntentGate{intent, trigger∈ask|keyword|offer_accept|commitment|directive|none, reason, words, inbound_image, scene_kind}`——`inbound_is_image`（media_type ∈ image/photo/sticker/video… ∨ 文本含 `[图片内容]/[贴纸内容]/[视频内容]` ∨ 整串媒体占位）时只看 `customer_words`（`strip_media_desc` + 剥 `[语音转录]`；纯占位 → ""），**识图描述一字不算**；图入站且配文无索图 → 承诺兑现 / LLM 指令一律 `intent=False reason=inbound_image_no_intent`；文本入站 `intent = detect_selfie_request ∨ strict_requested_scene_kind（场景 kind 须与索图动词 / Q-6 _MEDIA_REQ_RE 共现）∨ 运营触发词子串命中 ∨ offer_accepted / 带主体 offer（wanted_media_subject）`，否则 `assume_intent` / `directive_override` 照旧放行。同 mid：`album_match_seen/mark_album_match`（`conv:mid` LRU 2000 · 6h）。跟随预算：`follow_budget_check(ck, config, trigger)`——`inbox.image_autosend.follow_cooldown_min`（默认 30，只拦 commitment / offer_accept / directive，客户亲口「再来一张」不拦）+ `follow_daily_max`（默认 6，全部 autosend 出图；0=关）按本地日计；配文防重 `dedup_caption(ck, cap, src, alternatives)` 与最近 3 张出图配文 `proactive_variety.similarity ≥0.8` → 换第一个不雷同备选（registry / fixed）→ 都雷同空配文 `dedup_empty`；`format_album_send_log`。`image_autosend.py`：`pick_registered_media(+intent_gate, inbound_mid, inbound_media_type)` 在 `_suggest_ok` 之后、Q-6 `pick_media` **之前**过闸——无意图 `[album_match] skip=no_intent` DEBUG 一行即返 None（不匹配 / 不 `note_album_miss` / 不 `album_miss_marker.mark`），同 mid `skip=dup_mid` INFO；图入站带索图配文用 `words` 做匹配文本；`_ask` 不再被 `_scene_cls or _scene_kind` 单独置真（须 `trigger==ask`）；`_note_last_match/last_match` 记 candidates/picked 供 `[album_send]` 回填；`run_autosend_image(+inbound_media_type, inbound_mid)` 在 0a 指令直通 / offer 桥 / 相册 / 生成**全部之前**算闸（commitment/directive 撞图入站 → `record_image_fallback("inbound_image_no_intent")` + INFO），再 `follow_budget_check`（`follow_cooldown` / `follow_daily_max` 归因 + INFO「只发文字」），三个真发点（指令 / 注册相册 / 生成）投递前 `_dedup_cap`（`[album_send] caption_dedup conv= sim= src=a->b`，计 `caption_dedup`），投递成功 `_album_sent` → `note_follow_sent` + `note_caption_sent` + `[album_send] conv= trigger= intent=1:ask candidates= picked= caption_src= src= mid=`。`autosend_helpers.py`（`stage_hunks --mine "Q-39 A"`，label-leak 两 hunk 留树）：`autosend_image` 顺带取最新入站 `media_type` / `message_id` 传闸；`_autosend_deliver` 兑现链 `_q39_img_no_ask = compute_image_intent(last_in.text, media_type, assume_intent="selfie").inbound_image ∧ ¬intent` → 跳过 `fulfill` 分支直接 `_media_promise_action` 撤回改写（`[media_promise] … action=retract reason=inbound_image_no_intent`）。`persona_reply.py`（`--mine "Q-39"`，label-leak 五 hunk 留树）：拟稿期自探 `ask = gate.intent ∧ trigger∈{ask, offer_accept}` ∨ pending，`scene = strict_requested_scene_kind(words) or (extract_requested_scene(words) if asking) or pending.scene`，`pick_registered_media(..., intent_gate=gate)`——旧 `requested_scene_kind(inbound) or extract_requested_scene(inbound)` 泛匹配置真下线。**未动**：`pick_media` 签名 / 打分、`note_album_miss`、`album_miss_addendum`、`detect_offer_media`、`select_media` | `tests/test_send_truth_q39.py` A/C 段：识图行 / 占位 / media_type 判图 + `customer_words` 剥离；VLM 描述 `intent=False`（承诺 / 指令同拦；带索图配文放行）；文本四路径（ask / commitment / keyword 子串同 Q-6 `_triggers_hit` 口径 / offer）；「enjoy the view」`requested_scene_kind` 仍 outdoor（Q-6 不动）但 strict 为 ""、「send me a landscape pic」「发张风景照」仍 outdoor；无意图 `pick_media` 零调用 + 零 `note_album_miss` + 零 marker，「May I see a pic?」照常 miss / 红条；同 mid 两跑 marker n 仍 1、新 mid 才 2；`run_autosend_image` 图入站 + `kind:selfie` 相册 → 零发（HEAD 回放同输入真发）、`assume_intent="selfie"` 同零发 + `inbound_image_no_intent` 归因；显式索图照常出图且 `[album_send]` 五键齐；冷却只拦三类非显式触发 / 显式放行 / 过窗 / 可关 / 日上限 6 / 隔日清零；端到端 `follow_daily_max=2` 第三张只发文字；`dedup_caption` 换备选 / 全雷同空配文 / 不像原样；注册相册配文近重复端到端换掉 + `caption_dedup` 日志；helpers / persona_reply 接线静态钉；红线：`pick_media` 五形参在 + `persona_media` / `detect_offer_media` / `note_album_miss` / `prompt_addenda` 源码零 `Q-39`。既有门禁：`test_album_match_q6` / `test_album_no_match_note_q35` / `test_prompt_addenda_q6` / `test_media_intent_q36` / `test_autosend_helpers` / `test_outbound_promise_guard` / `test_selfie_wiring` / `test_persona_media` / `test_scene_state` / `test_photo_capability` / `test_inbox_draft_engine` 全绿；`test_image_autosend` / `test_caption_truthfulness` 红集合与 HEAD **逐条相同**（见 §0） |
| B 翻译空串可恢复 | #326 | `d29bde26` | `translation_service.py`：`RETRYABLE_ERRORS` + `is_retryable_error(err)`（`ai:empty` / `ai:TimeoutError: …` / `provider_unavailable` / `translate_failed` / 连接族 → True；`target_lang_mismatch` / `engine_refusal` / `unsupported_target` / `lang_unknown` / `cjk_residue` → False），`next_engine_after(failed, target)`（按 `_engines_for(target)` 含 per_lang_order 找下一个可用且支持目标语的引擎），**新** `retry_once(text, target, source, style, engine)`——**绕过 L1 负缓存 / 翻译记忆**直打指定引擎（空 → 路由 failover），成功照常写 L1 + 记忆 + 成本 / 额度，失败不写缓存（`translate()` 把失败态写 60s 负缓存，2s 后原样重打只会命中它＝「重试」等于没试）。`outbound_translate.py`：`parse_xlate_retry_cfg`（`inbox.l2_autosend.translate.retry.{enabled, gap_sec=2, redraft}` 默认全开）、`is_retryable_xlate_error`、`redraft_output_ok`（非空 / 目标非 CJK 不得实质含 CJK / 可检语种须等于目标，中文变体家族互认）；`_translate_outbound_core(+redraft)`：首发异常不再直接 HOLD 而按 `translate_exception` 进同一 ladder → ① `retry_once(engine=首发 provider)` ② `retry_once(engine=next_engine_after)` ③ 仍空 ∧ `retry.redraft` ∧ 会话 `auto_ai` ∧ origin 非 manual/human/verbatim → `await redraft(item, target)` 通过 `redraft_output_ok` 即返（`[xlate] redraft lang= reason=`，`_xlate_action=redraft`，`item["_xlate_redraft"]`）→ 三步不行 `_mark_hold(attempts=N)`（`attempts` 只在 >1 时带，M-1 B 三键契约不动）+ **新** `xlate_hold_marker.mark`；任一路径翻译成功 → `xlate_hold_marker.clear`。**新** `src/inbox/xlate_hold_marker.py`：KV `xlate_hold:<cid>={ts, first_ts, n, reason, target, draft_id, attempts, text≤120}`（`app_settings`，与 `album_miss_marker` 同款不建表；24h 陈旧不显不删），`is_engine_silent(reason)`。`conv_state.py`：新状态 `xlate_hold`（`STATES`/`PRIORITY` 排 lang_unknown 之后 ai_fail 之前 / `TONE=danger` / `ACTIONS+=retranslate`），源 `_src_xlate_hold` **不进 `sources`**（Q-26 六源 + Q-30 两源契约不动）单列 `out["ext"]["xlate_hold"]`，`text_key = inbox.cs.xlate_hold`（引擎没回话族）/ `inbox.cs.xlate_hold.other`，`action=retranslate`（有 draft_id）带 `reason/target/hhmm/attempts/n/draft_id`。`autosend_worker.py`：`_translate_hold_message` 引擎没回话族 → 「翻译引擎没回话（target=en，已试 N 次）· 这条没发 · 会话头点「重试翻译」补投（不发原文）」（前缀 `translate_hold:<reason>:` 稳定）；**新** `deliver_retranslate(draft)` → `_deliver_now(stage="retranslate")`（不标 `origin=manual`——稿是 AI 中文原文**必须再译**，翻译链纪律原样；成功标 sent_at / 计 `total_retranslate_delivered`，失败原样回 `translate_hold:*`）。`unified_inbox_send_routes.py`（`stage_hunks --mine "Q-39 B"`）：**新** `POST /api/unified-inbox/drafts/{draft_id}/retranslate`——`store.get_draft` 404 / 会话无 `xlate_hold_marker` note 或 note 的 draft_id 不符 → **409** `err.inbox.retranslate_not_on_hold`（不是任意草稿的「再发一遍」口子）/ worker 缺 `deliver_retranslate` → 503 / 成功 `{ok, delivered, conv_state}`；路由基线 `test_admin_route_inventory` 登记。`autosend_helpers.build_autosend_translate_cb`（`--mine "Q-39 B"`）：**新** `_redraft_in_lang(item, target)`＝`persona_reply.generate_persona_reply(app, platform, chat_key, last_inbound, history=normalize_history(近 20 条), reply_lang=target, conversation_id, account_id)` 取 `reply`，任何异常回空串；`translate_outbound_text(..., redraft=_redraft_in_lang)`。`unified_inbox.html`（3 hunk `Q-39 B`，cs-）：`_CS_ACT_KEYS.retranslate`、`_csParams` xlate_hold 插值、`_csAction` `retranslate` → POST 端点（60s）→ 成功 toast `inbox.cs.retranslate_ok` + `loadThread(c,true)`，失败 toast + `detail`，响应带 `conv_state` 即刷。词条 **新** `i18n_packs/send_truth_q39.py`（zh/en/手写繁体）：`inbox.cs.xlate_hold{,_t,.other,.other_t}` / `inbox.cs.act.retranslate{,_t}` / `inbox.cs.retranslate_{ok,fail}` / `err.inbox.retranslate_{not_on_hold,worker_missing}`（+ D 段 `inbox.systag.*`）。**未动**：不发原文纪律（三步失败仍 HOLD）、`lang_unknown` 直接 HOLD 零引擎调用、`_output_lang_sane` 等校验 | `tests/test_send_truth_q39.py` B 段：`ai:empty` → 同引擎 `retry_once(engine=ai)` 成功投递、无 hold、无 marker；同引擎再空 → `next_engine_after` 换 hymt 救回（`retry_calls == ["ai","hymt"]`）；三次失败 + auto_ai → `redraft(target=en)` 产物投递（`_xlate_action=redraft`）；重起草产物仍中文 → 弃用照 HOLD（`attempts=3`）；人审档不重起草，三次即 HOLD：`_xlate_hold{reason=hymt:empty, attempts=3, target=en}`、worker 文案「翻译引擎没回话 · 已试 3 次」、marker `{draft_id, attempts, reason}`、`conv_state` `xlate_hold/danger/will_send=False/text_key/action=retranslate/params.draft_id`、不进 `sources`、`ext.xlate_hold`；同会话下一次翻译成功 → marker 清、状态不再 xlate_hold；`lang_unknown` 零引擎调用零重试、`target_lang_mismatch` 零重试且三键契约不带 `attempts`；分类器正反例；**真 `TranslationService`**：首发 empty 进负缓存（第二次 `translate` 不打引擎）→ `retry_once` 直打成功 → 缓存回填；`AutosendWorker.deliver_retranslate` 复用翻译回调（`origin` 不标 manual、`_retranslate=True`）成功计数 / HOLD 原样回 `translate_hold:ai:empty`；端点静态钉（409 / marker / deliver_retranslate / 词条）+ helpers `redraft=_redraft_in_lang` 接线。既有门禁：`test_outbound_translate` / `test_outbound_lang_gate_234` / `test_lang_gate_coverage` / `test_proactive_lang_gate` / `test_autosend_worker_translate` / `test_translation_service` / `test_translation_engines` / `test_outbound_humanize` / `test_conv_state_q26` / `test_conv_state_q30` / `test_reason_codes_q30` / `test_ai_fail_marker_q14` 全绿；`test_admin_route_inventory` 本线端点已登记（剩余 extra `/api/personas/profiles/parse` 属他线）；route CJK ratchet 0 |
