# 实施92 · 报障群未修总攻派工（全量清底）— 2026-08-31 晚

> **给新修复对话的开工输入（当前未修全集，一份打完）**。工单库
> `D:\chengjie-instances\zhiliao\data\config\bug_intake.db`，每单 body 尾部有
> 值守 note（根因/原图路径/修向/验收），**接单先读工单本体**。
> 修完标 fixed + 一句话 fix_note → 自动按钮回访；桌面包改动攒 **v1.0.66**。
> 前情：v1.0.64 已到测试机；#97/#96/#95/#102/#104/#105/#106/#108/#110/#13 等
> 9+ 单已修完标「随 v1.0.65」——**第 0 步先把 v1.0.65 build+发包**，别开新工。

## 0. 开工顺序（严格照此）

1. **发 v1.0.65**（若还没发）：里面压着两张 P1 击穿单的真修复（#97 混语收口、
   #106 图片轮铆定）+ #96/#95/#110 三张断言/语言锁 + #102/#104/#105/#108/#13。
   发包后群里的验证潮由值守组织，你不用管。
2. **二轮返工两单**（v1.0.64 实测打回，证据全在工单）：
   - **#75 滚动条**：样式修复到货但**容器高度计算错**——滑块占满全轨=可滚高度≈0
     而内容溢出（原图 915，WA 列表实锤，三平台共用组件）。查列表容器高度/overflow
     链，比对内容高度 vs scrollHeight。验收=滑块比例正常、拖拽/滚轮均可到底。
   - **#78 应用到弹窗**（两人两票）：① `default default` 伪账号行隐藏（真实账号
     在列时纯属噪音，其上「清除」语义不明）；② 主标签换**账号显示名**（TG 显示名/
     用户名优先、ID 副行；LINE 行禁止裸 token）。原图 889/929。#61 并入本单验收
     （多账号指定→绑定生效全链）。
3. **#88 黄条案收尾**（fixed 但复测存疑）：skuio 复测见黄条仍挂（原图 931，升级
   存量核销嫌疑未跑）；值守已布置三分支实验，**接单前先看工单最新 note**——若已
   定「击穿」按下述修：升级核销迁移补跑/lazy 化（首次打开会话时若「标记后有成功
   出站」即清），并核对解除钩子在 v1.0.64 客户端真实生效。
4. **今晚新立七单**（证据全为原图级，按序）：
   | 单 | 要点 | 修向锚点 |
   |---|---|---|
   | #101 | LINE 媒体全类型双向断（收：语音/图/视频不可见；发：语音0:00、图/视频发不出）两机确认 | 入站媒体拉取落盘链 + 出站 duration 参数与上传链；若已有修复对话在途先对进度，撂了就接 |
   | #115 | 翻译引擎报错『您没有提供需要翻译的消息内容』以气泡进会话流 | 翻译层错误吞掉落日志/灰字，绝不进消息流；顺查空输入源头 |
   | #121 | 翻译发声双守卫误报（疑似无声+试听过期常亮） | 试听指纹把翻译维度（开关/目标语/译稿hash）纳入基线；静音检测阈值回校 |
   | #120 | 导航徽标幽灵未读（全部账号⑩/TG⑦/WA③ vs 列表无未读） | 徽标口径统一「用户可见真未读」；读后清零；勿与「待回复」口径混用（报障人实验结果看工单 note） |
   | #113 | 临时行程状态自锁 7 天不可见不可清（马尼拉复读根因） | 单句幻觉不建行程态（需锚定）；会话侧行程徽标+一键清除回归档案常驻地 |
   | #116 | 语音面板裸键 cp.voice.tip_got 直显 + 系统音色带「克隆」标语义混乱 | 客户端 i18n 键包补齐 + 打包前键完整性检查；标签清理 |
   | #111 | 目标卡「标成交」点击无反应（与 #54「调整期限」同卡同族） | 哑按钮：handler 暴露/接线；依赖未接通时必须给引导绝不静默（同屏见「达成推送未接通」黄字） |
5. **遗留批清账**（0827-28 积压，实施91 §3 已列，未被接的继续）：
   - 复测前置组（先在 1.0.65 复现，复现不了直接标 fixed 走按钮验证）：
     #3/#4/#5/#6/#8/#9（媒体/语音发送失败旧案）、#10（护栏文案）、#11（显示不全）、
     #18（宿务译马尼拉单例）、#28（1.054 陈旧报告，核对后直接关）。
   - 真工作量组：#16 关怀预览失败、#25 生图无反应（出图落点公网到达性=设计定性）、
     #27 Messenger 二次验证码遮蔽、#39 输入框失灵（renderer 日志已可收，等复发取证）、
     #43 生日记忆、#52 克隆弹层误触丢试听、#54 调整期限（并 #111 一批）、
     #7 告警测试 HTTP 200 误判、#33 工作链 i18n。
   - Messenger 发送失败家族 #21/#23/#31/#34/#49：先对意向板（impl86 时代 bugfix-dev
     认领过），撂了就并成一批修（与 #27 同域）。
6. **#109 回复质量残量**：四病中混语（#97）/凭空断言（#110）/画像（#108）已随
   1.0.65 修，剩两个：摸底提问节奏机械（归 #38/#40/#65 目标家族，对 goal 线进度）
   + 第 4 病「目标话术渗味」（『that's the first step』类步骤感元话术进出站软校验
   词表，禁在对客户文本出现）。
7. **产品决策件不施工**（转产品）：#32 悬浮清理、#33 后半（工作链vs目标合并）、
   #37 引用回复能力、#36 繁体包（先核对实施89 交付状态，到货即标 fixed）。

## 1. 边界与纪律

- **P0 基建（老板专属，修复线绕开）**：VPS 22 端口不可达（fail2ban/白名单），
  影响值守取证链（UHCU29 诊断包待取）。通了之后隧道断连告警加固见实施91 §2.0。
- 在途归属先对意向板：goal sprint pace 线（#38/#40/#65）、duty-tools 线
  （backfill 自咬守卫，垃圾单已 18 例）、#101 修复对话（今晨开工）。
- 共享树纪律照旧：`agent_probe.ps1 -Intent` 登记；`.py` 攒批走重启窗；模板/i18n
  热更新直上生产每步自洽；**绝不用全树回退命令**；显式列文件提交。
- 验证闭环归值守：标 fixed 即触发按钮回访，值守盯复测与击穿定性，你只管修。

## 2. 快速对账口径

```
python C:\Users\Administrator\tmp_duty_query.py fixcheck "75,78,88,101,111,113,115,116,120,121"
```
（值守机 117 上；处置台浏览器口径 `/admin/bug-tickets`）
