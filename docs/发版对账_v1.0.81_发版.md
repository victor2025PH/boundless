# 发版对账 v1.0.81 — 发版线 R81（单一版本：Q-16 + Q-17 + 1.0.80 全部内容 → 一个 clean 包 → 公共渠道 + 内测渠道桥接 + 坐席三台）

> 老板 09-11 14:3x 原话：「我们不要内测版，只要一个版本，把所有的功能都进行提交。」
> 解读：从本版起**只出一个安装包**（clean 形态、公共渠道），不再另打 smart / 内测包；坐席机与内测机装同一个包；HEAD 上**已提交**的全部功能进包（未提交的别线半成品不进——共享树上 30 个脏文件属用量线 / 运维降噪线 / 合规线，由各自入库）。Q-12 情感引擎 / Q-13 口语层一行未开工，不在本版。
> 接手方：先 `git -C D:\boundless log --oneline -12` 核对本表 commit 真在。SOP `.cursor/rules/chatx-desktop-install.mdc`；前例 `docs/发版对账_v1.0.79_发版.md`。意向板 `R81 single public release …`（14:3x）。
> 执行：2026-09-11 14:3x → 见 §5 断点。

## 0. 一句话

**1.0.81 单包** `ChatX-Setup-1.0.81.exe` **522,854,426 B** sha256 `18d58ac9dd4b8a098cf6c2ddedadd56ebb9fa6047cf00703511692ed629c7f81`（499 MB，clean 形态，干净 worktree `D:\boundless-r80@06951ac4`）。内容 = 1.0.80 公共版全部 + **Q-16**（#276 发送语言按会话 + 手发确认）+ **Q-17**（#278 风控三级 + commitment 只评出站 + 摘标冷却 + 风控分级卡）+ 小智词条 / how-to 两组。坐席三台 **yunsheng / kouxing / lianbei 全 UP · 1.081 · clean/public**（15:03 / 15:05 / 15:07 smoke 全 OK，数据保留）。公共渠道 + 内测渠道桥接 + 官网 + 台账 + 回访见 §4。

## 1. 为什么 1.0.80 出了两个包（本版收掉的坑）

| 包 | 切点 | 内容 | 去了哪 |
|---|---|---|---|
| 09-10 22:13 内测 smart | `boundless-wx@3b721cd7` | WX 实施97 / QQ route-1 A+B / TK-1 C–E + Q-8 / Q-10 / Q-15 | 只推坐席三台，未上传 |
| 09-11 09:11 R80 三包 | `boundless-r80@84c9210f` | 上面 + `4fdd238f` 人设裁剪根因修复 + token 用量批 + TK-3 | internal（593,991,165 B）+ 公共 clean（522,800,035 B）+ lite；**坐席未重推**、无发版台账 |

同一版本号两种内容：坐席机上的 1.0.80 不含 P0 修复且自动更新按 semver 判无更新。**本版规矩**：一个 semver = 一个包；坐席机改装 clean 包（升级保数据），装完自动跟公共渠道（`push_chatx` 明示 `channel: public`）。

## 2. 开工基线（14:3x）

| 项 | 事实 |
|---|---|
| 主干 | HEAD `9811577a`（Q-17 收工 13:44）；Q-16 `c72a0aa2` / Q-17 `6ec6e0d6` `6adeebf8` 已入库；`4fdd238f` 等 1.0.80 内容已在 |
| 意向板 | 用量线（07:20 起，最后 commit 12:40，动 `ai_client.py` / `skill_manager.py` / `reply_settings`）；B-line 已 -Done；热区（desktop / shared / messenger-web）10 分钟内零改动 |
| 工作树 | 30 个脏 `.py`/`.js`（用量线 / 运维降噪 / 合规 / 微信 KF）**不进包**——干净 worktree 只取 commit |
| 版本链 | `desktop/package.json` 1.0.80→**1.0.81** / 1.080→**1.081**（字节替换保 CRLF）；`python -m scripts.i18n_hant generate` → **17513 键**；`gen-chatx-changelog add --version 1.0.81` **7 条双语**；`bump_ui_build.py --severity notice` → **20260911-1436**；小智 `help_terms.py` +2（`risk_grading` / `send_lang_per_conv`）+ `howto_pack.py` +2（`risk-grading` / `send-lang-per-conv`，`test_howto_coverage` 6 passed）→ 主干 **`06951ac4`**（14:37） |
| 官网五值 | `chatxContent.ts` version / filename / sha256 / 注释 → 主干 **`76efe139`**（15:00）；`gate:content` 发布前 5/6（manifest 仍 1.0.80，预期） |

## 3. 回归 / 门禁（干净 worktree `D:\boundless-r80@06951ac4`）

| 项 | 结果 |
|---|---|
| `import_sweep` | modules=1237 bad=0 / from-import names=3583 bad=0 **ALL GREEN**（43s） |
| predist（clean） | `renderer-boot-invariants` / `outbound-wiring` / `package-layout` / `uninstall-nsh` / shell-i18n / cp-i18n-keys / backend-dist fresh / `stage_clean` / `smoke_backend` / `smoke_clean` 全过；after-pack 随包交付物 **22 项**；`build:backend` 全量 + `dist:win:clean` 14:41–14:53 |
| `gate_sweep` | **18 failed / 1602 passed**，复跑仍 18（`%TEMP%\r81\gate_sweep.txt`）。归因：**14 项 R79 已登记旧债**（`assistant_qa_eval` ×2 / `deploy_profiles` / `inline_color_ratchet` ×3 / `orphan_refs` / `legacy_blue` ×2 / `input_theme_contrast` / `silent_exception` / `takeover_trigger_surface` / `copilot_tiny_font` / `workspace_emoji`）+ `sealed_templates_no_legacy_date_formatters`（Q-14 B 已知）+ **3 项本版核过**：① `test_ui_build_stamp_not_stale` = worktree checkout 重写 mtime（测试自述「git checkout 造成的 mtime 重写按同法消警」，主树 3 passed）；② `test_sql_phantom_columns` = TK-3 `tiktok_huoke_bridge.py` 的 `kind / last_seen_ts / health / last_out_ts` 四列走 L207–241 `ALTER TABLE ADD COLUMN` 迁移表，静态门禁不认 f-string 迁移，运行时无事且 `tiktok.huoke_bridge.enabled` 出厂关 → **交 TK-3 线登记迁移列**；③ `test_no_dynamic_dot_access_concat` = Q-15 `unified_inbox.html` L16254/16257 `'inbox.adult.cat_'+…` 是 i18n **字符串键**拼接不是属性访问，门禁正则误报 → **交 Q-15 线改写法消警**。**0 条功能性回归** |
| 全量回归（干净 worktree） | `scripts\regression.ps1` **148 failed / 27207 passed / 40 skipped，1:08:55**（与后端构建并跑，生产在线 → 并行度封顶 4）。串行复跑 148 → **103 仍红**（45 条 = 并行 flaky / 缺 gitignored `config.yaml`）。**同 103 条在 1.0.80 release commit `84c9210f` 的临时 worktree 上复跑：102 红 + 1 skip**（`%TEMP%\r81\cmp_r80.txt`）→ 唯一「1.0.81 红 / 1.0.80 绿」= `test_ui_build_stamp_not_stale`（mtime 伪红）。**结论：1.0.81 相对已发布的 1.0.80 零新增回归。** 那 102 条是 HEAD 既有债（主工作树上同一批全绿——依赖各线**未提交**文件：合规线 `compliance/*`（`test_aline_disclosure_wiring` ×4）、Q-8 / Q-1 目标族（`goal_inject_keys_166` ×8 / `goal_probe_active_leg_236` ×4 / `exposure_guard` ×3 / `excuse_budget_q8` / `exit_gate_q8` / `goal_default_stage_q8` ×4）、关怀（`care_send_now_243` ×4 / `care_verbatim_default_218` ×2）、`image_autosend` ×7 / `caption_truthfulness` ×3（Q-6 有意变更未更新断言）等），**1.0.80 公共包同样带着**——各线入库前这批测试对安装包永远是红的，见 §6 |
| Q-16 / Q-17 门禁 | `test_risk_grader_gate` / `test_commitment_gate` / `test_adult_grader_gate` / `test_workspace_template_js_syntax` / `test_howto_coverage` **72 passed** + `test_lang_pref_routes_q16` **12 passed**（worktree） |
| 未做 | zhiliao 装载 + 活体核对（zhiliao 跑的是共享工作树不是安装包；本版以坐席三台真装 + smoke 六端点 + fleet 作活体）；`outbound_style_gate` 真机样本 |

## 4. 全链

| 步 | 状态 | 事实 |
|---|---|---|
| 坐席三台 | ✅ 14:59–15:07 | `push_chatx.ps1 -TargetSsh {yunsheng,kouxing,lianbei} -Setup <r80>\dist-clean\ChatX-Setup-1.0.81.exe -Install -Relaunch -Smoke`：三台 `installer exit=0`、seatverify 新后端、smoke 六端点 OK + vision active；`chatx_fleet_status` **三台 UP · 1.081 · clean/public**（原 smart/internal → clean/public，数据保留）。日志 `%TEMP%\r81\push_all.txt` |
| publish 公共 | ✅ 16:0x | `publish_chatx.ps1 -DistDir <r80>\dist-clean -Yes -NotesFile _announce_1081.txt`：build valid sha512 ✓ → VPS exe 校验 ✓ → 指针 + pm2 reload (rolling) → R2 同步 → 公网核 `latest.yml=1.0.81 manifest=1.0.81 exe=HTTP206` → `announcements.json` 带 `release-v1.0.81`（20 条）→ 清理 1.0.78。日志 `%TEMP%\r81\publish_public.txt` |
| publish 内测渠道**桥接** | ✅ 16:0x | 同一个 clean exe + blockmap + `latest.yml` 复名 `latest-internal.yml`（`%TEMP%\r81\internal_bridge`）→ `-Channel internal -Yes`：`latest-internal.yml=1.0.81 manifest=1.0.81 exe=HTTP206`，公共指针未动，清理 1.0.78。两渠道 sha512 **完全相同**（`X37Cd1FT…`）——**不是第二个包**。目的：还在内测渠道上的机器（skuio 等）应用内自检能看到 1.0.81 并装到 clean 包；装完 `app-update.yml` 指公共渠道，此后内测渠道可停更 |
| 官网 | ✅ 16:1x | `gate:content` **6/6 PASS**（发布事实源一致 / 更新记录覆盖 1.0.81）→ `deploy.ps1 -FromHead`（website/ 有 console i18n 线脏文件，只打 HEAD）→ **部署完成 [OK]**（healthy / webhookSecret / adminKey / deepseek 全 True，R2 同步）。日志 `%TEMP%\r81\website_deploy.txt` |
| 台账 / 回访 / 播报 | ✅ 16:09–16:12 | `duty_update_hint --set "1.0.81 已发布（单一版本…）"`（热重载）→ 报障群播报 `duty_reply --group neice --no-mention` → **mid 1431**（16:09:38）→ `duty_ledger_fix --mark-fixed-file --apply`：**#276 #278 new→fixed** → 回访 #276 / #278 两条 `bug-intake/reply ok`（间隔 ≥14s，取 Q-16 / Q-17 落点表口径 + 验收三步）→ `duty_notify_summary --reporter 8942577244 --ids 276,278 --msg-id 1431 --apply` **记账 2 单，积压 0** → 频道 @hykj7 **messageId=162**（campaign `release-1081`） |
| release 真值 commit | ✅ | `desktop/build/i18n-packs-staged` 自 worktree 构建拷回（13 改 + 6 新）+ 本表 + Q 总账 R81 行（见 git log） |

## 5. 断点 / 交接

- 14:3x 读总账 / 意向板 → 14:36 版本链 → 14:37 `06951ac4` → 14:38 worktree checkout → 14:41 回归 + 后端全量 + 单包并跑 → 14:53 包出 → 14:5x import_sweep 绿 / gate_sweep 18 红归因 → 14:59–15:07 坐席三台 → 15:00 `76efe139` 官网五值 → 15:1x 两个 publish dry-run → 15:50 回归收口 → 串行复跑 + 1.0.80 对照（零新增）→ 16:0x publish 公共 + 桥接 → 16:1x 官网 → 16:09–16:12 台账 / 回访 / 播报 → 本表 + 总账 → 意向 -Done。
- worktree `D:\boundless-r80@06951ac4` 保留（`dist-clean\ChatX-Setup-1.0.81.exe`）；对照用 `D:\_r80cmp` 已 `worktree remove`；`D:\boundless-wx` / `D:\boundless-r79` / `D:\boundless-r78` 可回收磁盘。

## 6. 交回各线

- **单一版本规矩**（老板 14:3x）：以后 R 批只出 clean 包；`npm run dist:win`（smart/internal）不再作为发版产物；内测渠道指针只做桥接（同字节），等 fleet 与 skuio 都显示 `clean/public` 后可停更 `/downloads/internal/`。SOP 规则文件 `.cursor/rules/chatx-desktop-install.mdc` 的「内测约定」节需按此改写（本线未改规则文件）。
- **HEAD 既有 102 条红（安装包同样带着）**：合规线请入库 `src/compliance/{runtime,disclosure,__init__}.py`（`test_aline_disclosure_wiring` ×4 + R79 `platform_scope` 同根）；Q-8 / Q-1 / 关怀 / Q-6 各线请在干净 worktree 上跑自己的测试——主工作树全绿只是因为未提交文件在撑；`test_assistant_qa_eval` 拒答率 31% < 45% 与 top3 96% < 100% 是小智语料真实回退，值守跟。
- **TK-3**：`test_sql_phantom_columns` 把 `tiktok_huoke_bridge.py` 的 `ALTER TABLE ADD COLUMN` 迁移四列判为幽灵列——请在该测试的迁移允许表登记（或让迁移表可被静态扫描）。
- **Q-15**：`unified_inbox.html` L16254 / L16257 `'inbox.adult.cat_'+…` 是 i18n 字符串键拼接，被 `test_no_dynamic_dot_access_concat` 误判——改 `'inbox.adult.cat_'.concat(…)` 或在键与 `+` 间留空格即绿。
- **skuio 回访待收**：#276 三步 / #278 三步（见回访正文）；升级后请重启一次工作台后端（风控分级为 `.py`）；`target_lang_mismatch` 52 条排查结论按 Q-16 落点表 E 段另发。
- **Q-12 / Q-13**（情感引擎 / 口语层）未开工，不在 1.0.81；#279 Bad MAC 无线；#265 / #216 / #258 / #261 待 skuio 复验后关。
