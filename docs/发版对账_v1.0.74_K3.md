# 发版对账 v1.0.74 — K-3 工作台交办小件（WA DNS 提示 · 记忆 chips · ops 已发假声明行 · LINE 入站上限 · config.example 文档键 · 语音合并窗）

> 指令：`docs/指令_K-3_工作台交办小件_2026-09-05.md`。实际顺序 D → C → F（等 K-1 期间顺手做了可选项）→ A → B → E。
> 2026-09-05 19:29 开工 / 20:35 收工，**六件全部 commit**。开工时 K-1 行「未开工」、`unified_inbox.html` 带 154 行 K-1 在途 hunk → 先做不碰它的 D / C / F + A 后端半边；K-1 A4（`69e62aba`）落地后接管模板做 A 前端半边、B；K-1 D1b（`124dd921`）落地后做 E。
> ⛔ 未碰：`episodic_*` / `memory_*` / `skill_manager.py` / `personas.html` / `store.py` / `drafts.py` / `services/whatsapp-baileys/**`。所有接口按 J-6 / J-10 落点表契约消费，未改任何后端接口。
> 共享文件提交纪律：`unified_inbox.html` 两次、`config.example.yaml` 一次都走 `tools/stage_hunks.py --mine … --theirs …` 只挑本单 hunk（排除表情包线 `sticker-panel.js ?v=`、K-1 `cp-goal.js ?v=`、K-D1 孤儿线 `outbound.unlimited_mode`）。

## 落点表

| 件 | 工单 | commit | 文件 | 测试 | 备注 |
|---|---|---|---|---|---|
| D LINE 入站缺省上限 20→32MB | J-3 C 发现 | `88c442f9` | `src/integrations/line_media.py`（`DEFAULT_INBOUND_MAX_BYTES` 一行 + 注释：服务端把 100MB 源转码成 27.5MB 下发，20MB 拒下＝发得出收不到）；`tests/test_media_limits_169.py` +1 | `test_media_limits_169` + `test_line_media` **87 passed** | 现场覆写键 `platform_login.line.media.inbound_max_bytes` 不变；example 值随 E 改 |
| C ops 卡「已发假声明」行 | #171（J-1 A 备注「ops 卡未加行」） | `26298844` | `ops_overview.html::loadAutosendMedia` 详情区一行 `id=asmSentClaimRow`「AI 说「已发」但没发：检出 N · 补发 N · 撤回改口 N」（读 `autosend-status.worker.image.sent_claim_{detected,fulfilled,retracted}`；三数全零不出现；撤回>补发且有检出亮琥珀）；`i18n_packs/ops_overview_page.py` +4 键 zh/en；`ui-build` 20260905-1937；`tests/test_ops_overview.py` +3 | `test_ops_overview` **81 passed**；window.T 键解析 passed | 数据源钉：`record_sent_claim_event` → `metrics_snapshot()` 同一出口 |
| F 语音入站单独合并窗（可选项，做了） | J-2 顺手项（88MP86 BABY BEAR fresh-guard 30+ 次） | `569997d8` | `src/inbox/inbound_debounce.py`（`is_voice_piece`：ingest 给的 `[语音]` 占位 / conv 带 `media_type=voice`；`voice_window_sec` 默认 15s、不低于常规窗、仍受 `max_wait_sec` 硬上限；窗内**最新一条**是语音才用语音窗，随后文本回常规窗；`stats_snapshot.voice_windows`）；`src/inbox/autodraft_helpers.py` 接线一行 + 启动日志带 voice_window；`tests/test_inbound_debounce.py` +5 | `test_inbound_debounce` + `test_autosend_dup_guard_wiring` **28 passed** | 配置键按代码真实结构放 `inbox.auto_draft.inbound_merge.voice_window_sec`（指令写的 `inbox.inbound_debounce.*` 块不存在）；WA 边车对语音条推 `media_type="voice"` 正文空、LINE 同理 → ingest 回调拿到的是 `[语音]` 占位，识别路径已核实，未改 ingest |
| A WA 配对 DNS 重试提示坐席可见 | #181（J-6 B-2 交办） | `ba19ac50` | 后端：`src/integrations/whatsapp_baileys_login.py::_poll` 返回 `hint_code`（空值也落、不粘）+ `pairing_dns_fails`（>0 才带）；`src/web/routes/unified_inbox_login_routes.py` `_pout` 透传 `pairing_dns_fails`（一行，非 WA / 零次不带键——契约里的「透传进登录状态响应」落在路由，原路由只透 hint_code，缺这行前端拿不到 N）。前端：`unified_inbox.html` `_CONNECT_LIVE_HINT_KEYS.dns_retry`（`n:true`）；`_applyConnectLiveHint(code,n)` 计数变了也刷、带 `{n}` 的码走 `window.Tf`；`pollConnect` 传 `d.pairing_dns_fails`。`i18n_packs/inbox_workspace.py` +2 键（`inbox.wa.st_dns_retry` / `inbox.wa.hint_dns_retry`，`{n}` 占位）；`ui-build` 20260905-2004；`tests/test_whatsapp_baileys_login.py` +3 | WA 登录 + 登录契约 + 哑按钮/孤儿/重复 id 门禁 **73 passed** | 文案即指令给的「网络解析 WhatsApp 服务器失败 N 次，正在重试（DNS 问题，检查本机网络/代理）」 |
| B 草稿旁记忆 chips + 不再使用 + 来源原话定位 | #183 #177（J-10 A3 交办） | `5315830c` | `unified_inbox.html`：两个草稿面都挂——待审面板 `.draft-card-mini`（正文之后、发送按钮 `.draft-acts` 之前）与 composer 草稿条 `#cdraft-bar`（`#cdraft-mem`，slim 态隐藏）。取数 `GET /api/episodic-memory/used?conversation_id=&limit=1` 取 `batches[0]`，15s 缓存；两道对账：`batch.inbound_msg_id` ↔ 会话流最新入站 `.msg-row.in[data-mid]`（与 `record_recall` 的 inbound_msg_id 同源＝store message_id）、草稿 `created_ts ≥ 批次 ts − 5s`；不一致不渲染防串轮；`count==0` / 旧后端 404（本会话期不再请求）→ 容器留空不占位（CSS `:empty`）。`<details>` 折叠「本轮用了 N 条记忆 ▾」，展开每条 `content`：`impact=high` 红点 / `source=ai_inferred` 琥珀点 / 客户说的绿点；`review_reason` 非空加「需要你看」；右侧「不再使用」→ `POST /api/episodic-memory/{id}/ignore`，成功该 chip 即消、N 同步、空了整块收起，403 提示无权限；`source_quote` 可点 → 已加载会话流里按 `source_ts` ±60s 找正文含前 20 字的入站气泡，复用 `_flashMsgEl` 滚动+高亮 2s，没加载到 toast「这条原话在更早的历史里」（不做历史回拉）。全部容器事件委托、零内联 handler；6 函数挂 window。`unified-inbox.css` 新 `.mem-chips` 系（只用 `--green/--red/--tk-amber-ink/--bdg-*` 等工作台已定义 token），`?v=20260905c→d`；`i18n_packs/inbox_workspace.py` +12 键 zh/en；`ui-build` 20260905-2025；**新** `tests/test_memory_chips_template.py` 9 例 | `test_memory_chips_template` 9 passed；哑按钮/孤儿/重复 id/点拼接/free-capture/引号门禁 + i18n 覆盖 + 模板钉相关集 **493 passed** | 与 AI 记忆页人档抽屉「最近被用」分区同一接口，可互相自证；`--amber` 在工作台上下文（`workspace_base`）未定义，故琥珀用别名 `--tk-amber-ink` |
| E `config.example.yaml` 文档键 | J-1 / J-10 / K-3 收尾 | `ee7f8dda` | 只加不改：`companion.media_promise_guard.sent_claim.{enabled:true, window_min:30}`；`memory.consolidation.auto_promote.{days:7, min_recalls:1}` + `memory.review.low_confidence_threshold:0.6`；`inbox.auto_draft.inbound_merge.voice_window_sec:15`；`platform_login.line.media.inbound_max_bytes` 20971520→33554432 并注明原因。`inbox.workflows.auto_start.max_per_tick:5` 已在 L3126（J-2 B），跳过 | `yaml.safe_load` OK；config_check / key_contract_gates / config_manager / deploy_profiles / desktop_seed_visibility / compliance_mode / media_paths **117 passed** | 键路径与默认值均对照 `skill_manager.py` L504–514 / L5664–5682、L10431–10441 核过 |

### K-1 收工时交办给 K-3 的两件（总账 K-1 行 / `发版对账_v1.0.74_K1.md` §3 §4.2），一并收了

| 件 | 工单 | commit | 文件 | 测试 | 备注 |
|---|---|---|---|---|---|
| `health_watchdog.phantom_unread_remind` 文档键 | #120 / #159 族（K-1 C `35b224c4` 的配置面） | `28089c22` | `config.example.yaml` `health_watchdog.phantom_unread_remind.{enabled:true, min_phantom:5, interval_min:360}`，注释写口径（未读汇总 − 真实未读入站 ＝ 幽灵数）与告警别名 `phantom_unread` | yaml OK；config_check + key_contract_gates 36 passed | 默认值对照 `health_watchdog._check_phantom_unread` L3195–3229 |
| #164 sticker 尾巴（K-1 D 盘点：09-04 在途未提交、测试红） | #164 | `8b545d37` | `sticker-panel.js` 发送 catch 改 `inbox.media.send_fail_net_restart` 黄字（键其实早在 `2b022a22` 进了 `inbox_workspace.py`，K-1 盘点时读的是旧状态；模板 `_doSendMedia` 半边 J-4 C 已做）；`unified_inbox.html` `sticker-panel.js?v=20260817c→20260904a`；`tests/test_media_send_unknown_toast.py` 入库（断言改截到函数收尾——固定 2500 字符窗口被变长的注释切出去＝假红） | 2 passed；static_js 门禁 16 passed | 这是该尾巴的全部残余，`sticker-panel.js` 工作树已干净 |

## 顺手发现（不修，留给对应线）

- **`dns_retry` 不在 `login_funnel_stats._REASON_CODES`**：`unified_inbox_login_routes.py` L440 会话按 TTL 过期时 `reason = sess.hint_code or "session_timeout"` 落漏斗，`_san_reason` 会把 `dns_retry` 归成 `login_failed`——「配对期一直 DNS 失败直到超时」在漏斗里不单列。若要单列需三处同源：`_REASON_CODES` 加 `dns_retry` + 前端 `_CONNECT_FAIL_REASON_KEYS` + `inbox.connect.err_dns_retry`（`test_frontend_maps_every_reason_code` 从枚举推导会逼着补齐）。本单不动枚举。
- **既有红（非本单引入，HEAD 上已红）**：① `tests/test_css_token_refs.py` 两例——`unified-inbox.css` 的 `--th-bd-teal30/--th-bg-teal100/--th-ink-teal8/--warn-fg`（来自 `1eb74d4e` 积压收口）、`ops_overview.html` 的 `--tk-fg-muted/--tk-line`（J-9 E `d74f6c89`）、`episodic_memory.html --bg1`、`reply_settings.html --brand` 全站零定义；② `tests/test_legacy_blue_ratchet.py::test_judged_files_tailwind_ceiling` `unified_inbox.html` 裸 Tailwind 蓝 17 > 天花板 15（HEAD 已 17，本单未加任何蓝色字面）；③ `tests/test_unified_inbox_stage1.py` 7 例（`lang==='auto'` / 「建议你接管」旧中文字面钉 + 子域 34≠37 + `send_bubbles` 4 例）在今早基线 `1e8a8010` 就已红。④ `test_i18n_coverage::test_template_window_t_keys_resolve[personas.html]` 缺 `pma_*` 键＝K-2 中批状态。建议 K-5 ②段按红灯账本口径处理，别当 K-3 回归。
- 指令 §3/§5 写的 `tests/test_template_window_t_keys_resolve.py` 不是独立文件，在 `tests/test_i18n_coverage.py` 里。

## 值守读数口径（1.0.74 装机后，交 K-5 ⑤段）

- **B**：skuio 机随便开一条最近有 AI 草稿的会话 → 待审面板 / composer 草稿条底部应见「本轮用了 N 条记忆 ▾」；展开点「不再使用」后 AI 记忆页维护区「已不再使用」视图应多一条、chip 消失；点原话应滚到那条客户消息并高亮 2s。没有 chips 的判法：AI 记忆页 → 该客户档案抽屉「最近被用」分区为空＝这轮真没用记忆（正常）；抽屉有而工作台没有＝对账没过（看会话流最新入站是否就是触发这稿的那条）。
- **A**：钧机 3PZ95W 重扫 WA 配对时若 DNS 连败，连接弹窗黄条应出「网络解析 WhatsApp 服务器失败 N 次，正在重试」，N 随轮询涨；DNS 恢复后黄条自动回到等待扫码文案。
- **C**：ops-overview「🔊 B 线自动媒体」卡，J-1 A 守卫抓到过假声明时详情区多一行「AI 说「已发」但没发：检出 N · 补发 N · 撤回改口 N」；零命中不出现是正常的。
- **D**：LINE 长视频（服务端转码 20–32MB）能收下，`too_large_actual` 不再出现在这一档。
- **F**：`web_app.state.inbound_merger.stats_snapshot().voice_windows` 随语音条增长（需 `inbound_merge.enabled: true`）；BABY BEAR 类会话 `guard=fresh post_humanize` 放弃次数应明显下降。

## 装载提醒（交 K-5 ①段）

本单 5 个 `.py` 改动（`line_media.py` / `whatsapp_baileys_login.py` / `unified_inbox_login_routes.py` / `inbound_debounce.py` / `autodraft_helpers.py`）要等实例重启才生效；模板 / i18n / CSS / `config.example.yaml` 热更即生效。D 与 F 对客户可感知的效果（LINE 长视频收得下 / 连发语音少烧 LLM）都在 `.py` 侧，装载前不要回访这两项。

## 花费

约 $90（预算 $150）。
