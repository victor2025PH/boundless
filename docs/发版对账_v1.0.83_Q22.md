# 发版对账 v1.0.83 · Q-22 克隆语音语种不支持不静默换系统音（#287 #288 #239追加 #304）

- 指令：`docs/指令_Q-22_克隆语音语种不支持不静默换声_2026-09-11.md`（09-12 修订版）
- 证据：`tmp_diag\V9YAAX`（1.0.81 STEVEN×ja → Keita 系统声 `reuse=True provider=edge_tts fallback=avatar_clone`）/ `YNH6ZW`（1.0.82 复验 ja/ko/th/fr 仍静默换声）/ `GWJ2RZ`（#304 十翼会话普通话被切 zh-HK 声；根因 A/B 已由 `73c8ab6b` 修，本单做第二道保险）/ `4EXEEY`（#239 登记 toast 一闪、无试听、体检空）
- 预算：$130 · P0
- 红线：**不新增任何默认回落音**；系统音只在坐席显式二次确认（`confirm_system_voice=1`）后出；自动链永不出系统音；不动翻译链与 `unified_inbox.html` 的 xl-/lp- 区（Q-21）；`conv_lang_plan.tts_lang` 由 Q-21 提供、未合并前 `plan_tts_lang()` 返回空＝管线按出站文本检测语种兜底

## 0 开工基线

| 项 | 值 |
|---|---|
| HEAD（开工） | `29b3a71a`（读码期间他线推进到 `823893da`；`73c8ab6b` fix(voice) #304 已把粤语误切 + 繁简 CER 假阳性修掉） |
| index | 空（`git diff --cached --name-only` 为空） |
| 他线在途（未提交，同文件邻行） | `src/web/routes/unified_inbox_send_routes.py`（conv_route 让路 +17，conv-model-route 线）、`src/inbox/autosend_worker.py`（同线）、`src/web/templates/personas.html` / `unified_inbox.html` xl-/lp- hunk（Q-21）——同文件一律 `tools/stage_hunks.py --mine Q-22` 部分暂存 |
| agent_probe | `-Intent "Q-22 clone voice lang unsupported no silent fallback (tts_pipeline / voice-send / personas voice tab / cp-voice)"` 已登记（index FREE） |
| zhiliao 实例 | 本单 `.py` 改动**不由本线重启**，收工交接 |

## 1 落点

| 段 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|
| **A 阻断 + 二次确认（后端）**<br>**C 自动链改文字 + 冷启动重试**<br>**D 语种一致性（#304 第二道保险）** | （本 commit） | `src/ai/tts_pipeline.py`：`synthesize(confirm_system_voice=False, tts_lang="")`；`_synthesize_uncached` 回落决策处——克隆主链 × （语种闸 / 成品判念错 / Token 钱包耗尽 / 引擎不可达）且未二次确认 → `ok=False`，`error=clone_unavailable:<why>`（引擎离线沿用既有 `clone_engine_offline:` 码，D-L4 门禁不回归），统一 `extra.clone_unavailable=<why>` + `degrade_to_text` + `system_voice=<同语种同性别预置声或空>`；`confirm_system_voice=True` 才走既有 edge 兜底（`fallback_from` 照标，`system_voice_confirmed`）。无兜底链部署同样打 `clone_unavailable` 标。`[tts] blocked reason=clone_unavailable:… system_voice=…` 一行。<br>`_lang_consistency()`：文本语种（粤语特征字 → `yue`）/ 音色 locale（`zh-HK-*` → `yue`，multilingual 不算）/ 计划语种三方核对；`[tts] 合成生效 … text_lang= tts_lang= source=`；不一致 → `error=lang_mismatch` + `extra.skip_voice` + `[tts] skip_voice reason=lang_mismatch text_lang= tts_lang=`。<br>`_cold_start_wait()`：`_try_minicpm_clone` 探到「可达但模型未载入/正在载入」写 `extra.clone_cold_start` → 自动链等 `avatar_voice.cold_start_retry_sec`（缺省 20s，受总预算约束，interactive 不等）重试主链一次，仍不可用才阻断。<br>`_clone_lang_blocked`：`voice_profile.clone_text_prefix` 含 `yue` ＝ 声明会念粤语放行。`_pick_fallback_edge_voice`：性别未知 + 语种表无该语种 → 目录缺省声（yue 二次确认不落回普通话声）。`classify_voice_error` +`clone_unavailable` / `lang_mismatch`。模块级 `plan_tts_lang()`（`peek_conv_lang_plan` 读 `tts_lang`，无字段返回空）/ `skip_voice_reason()` / `log_skip_voice()`。<br>`src/ai/lang_voice_route.py`：`clone_lang_gate` 粤文按 `yue` 查能力表；`_route_cantonese` 克隆人设 × 专线只配 edge → **不切系统声**（保持克隆链 + `_yue_text`，tag=yue，交管线阻断）；专线配克隆节点 → `_lang_route_cleared=yue`。<br>`src/client/sender.py`（A 线 voice_reply 整段 + 分条）/ `src/inbox/voice_autosend.py`（B 线）：传 `tts_lang=plan_tts_lang(...)`；`not ok` 且阻断 → `[tts] skip_voice reason=clone_unavailable:… conv= lang=` 一行，改发文字（`record=False` 不算合成断档）。<br>`src/web/routes/unified_inbox_send_routes.py` voice-send：body `confirm_system_voice`；阻断 → `{ok:false, blocked:true, reason:"clone_unavailable:<why>", clone_unavailable:{why,lang,system_voice,system_voice_available}, message}`；`lang_mismatch` → `{blocked, text_lang, tts_lang, message}`；**路由级拒收**：产物 `provider∉CLONE` 且 `fallback_from∈CLONE`（试听复用 / #93 补标旁路）且无 confirm → 删产物、同形阻断响应。<br>`src/web/routes/voice_routes.py` tts-test：同参；阻断响应同形（sync + background job `blocked_fields` 透传）；#161 念错改派 Edge 只在 confirm 后做，否则作废乱音、`clone_unavailable:clone_lang_garbled:<lang>`；`clone_unavailable_message()` / `clone_unavailable_payload()` 单点（send-voice 复用）。<br>`src/web/i18n_packs/errors_stock.py` ZH/EN：`err.voice.clone_unavailable{,_lang,_quota,_offline,_garbled}` / `err.voice.lang_mismatch_skip`。 | `tests/test_tts_no_cross_gender_fallback.py` 两例改 Q-22 契约（缺省阻断 + `system_voice=Keita`；confirm 后 Keita/Nanami）；`tests/test_lang_voice_route.py` 粤语路由 3 例（预置声人设仍切 zh-HK / 克隆人设不切 / 专线克隆节点带 cleared）；`tests/test_minicpm_clone_tts.py` 粤语直构管线补 `lang_route_cleared`。语音 12 套件 **225 通过** |

## 2 交接

- R83 打包线：本单为 `.py` 变更 → 走全量 `build:backend`。
- 前端（语音行红条 / 人设语音卡支持语种 / 登记结果面板）见下方后续落点行。
