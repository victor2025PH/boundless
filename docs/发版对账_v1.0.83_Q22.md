# 发版对账 v1.0.83 · Q-22 克隆语音语种不支持不静默换系统音（#287 #288 #239追加 #304）

- 指令：`docs/指令_Q-22_克隆语音语种不支持不静默换声_2026-09-11.md`（09-12 修订版）
- 证据：`tmp_diag\V9YAAX`（1.0.81 STEVEN×ja → Keita 系统声 `reuse=True provider=edge_tts fallback=avatar_clone`）/ `YNH6ZW`（1.0.82 复验 ja/ko/th/fr 仍静默换声）/ `GWJ2RZ`（#304 十翼会话普通话被切 zh-HK 声；根因 A/B 已由 `73c8ab6b` 修，本单做第二道保险）/ `4EXEEY`（#239 登记 toast 一闪、无试听、体检空）
- 预算：$130 · P0
- 红线：**不新增任何默认回落音**；系统音只在坐席显式二次确认（`confirm_system_voice=1`）后出；自动链永不出系统音；不动翻译链与 `unified_inbox.html` 的 xl-/lp- 区（Q-21）；`conv_lang_plan.tts_lang` 由 Q-21 提供、未合并前 `plan_tts_lang()` 返回空＝管线按出站文本检测语种兜底

## 0 开工基线

| 项 | 值 |
|---|---|
| HEAD（开工） | `29b3a71a`（读码期间他线推进到 `823893da`；`73c8ab6b` fix(voice) #304 已把粤语误切 + 繁简 CER 假阳性修掉） |
| index | 空（`git diff --cached --name-only` 为空） |
| 他线在途（未提交，同文件邻行） | `src/web/routes/unified_inbox_send_routes.py`（conv_route 让路 +17，conv-model-route 线）、`src/inbox/autosend_worker.py`（同线）、`src/web/templates/personas.html` / `unified_inbox.html` xl-/lp- hunk（Q-21）——同文件一律 `tools/stage_hunks.py --mine Q-22` 部分暂存 |
| agent_probe | `-Intent "Q-22 clone voice lang unsupported no silent fallback (tts_pipeline / voice-send / personas voice tab / cp-voice)"` 已登记（index FREE） |
| zhiliao 实例 | 本单 `.py` 改动**不由本线重启**，收工交接 |

## 1 落点

| 段 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|
| **A 阻断 + 二次确认（后端）**<br>**C 自动链改文字 + 冷启动重试**<br>**D 语种一致性（#304 第二道保险）** | `b9b37262` | `src/ai/tts_pipeline.py`：`synthesize(confirm_system_voice=False, tts_lang="")`；`_synthesize_uncached` 回落决策处——克隆主链 × （语种闸 / 成品判念错 / Token 钱包耗尽 / 引擎不可达）且未二次确认 → `ok=False`，`error=clone_unavailable:<why>`（引擎离线沿用既有 `clone_engine_offline:` 码，D-L4 门禁不回归），统一 `extra.clone_unavailable=<why>` + `degrade_to_text` + `system_voice=<同语种同性别预置声或空>`；`confirm_system_voice=True` 才走既有 edge 兜底（`fallback_from` 照标，`system_voice_confirmed`）。无兜底链部署同样打 `clone_unavailable` 标。`[tts] blocked reason=clone_unavailable:… system_voice=…` 一行。<br>`_lang_consistency()`：文本语种（粤语特征字 → `yue`）/ 音色 locale（`zh-HK-*` → `yue`，multilingual 不算）/ 计划语种三方核对；`[tts] 合成生效 … text_lang= tts_lang= source=`；不一致 → `error=lang_mismatch` + `extra.skip_voice` + `[tts] skip_voice reason=lang_mismatch text_lang= tts_lang=`。<br>`_cold_start_wait()`：`_try_minicpm_clone` 探到「可达但模型未载入/正在载入」写 `extra.clone_cold_start` → 自动链等 `avatar_voice.cold_start_retry_sec`（缺省 20s，受总预算约束，interactive 不等）重试主链一次，仍不可用才阻断。<br>`_clone_lang_blocked`：`voice_profile.clone_text_prefix` 含 `yue` ＝ 声明会念粤语放行。`_pick_fallback_edge_voice`：性别未知 + 语种表无该语种 → 目录缺省声（yue 二次确认不落回普通话声）。`classify_voice_error` +`clone_unavailable` / `lang_mismatch`。模块级 `plan_tts_lang()`（`peek_conv_lang_plan` 读 `tts_lang`，无字段返回空）/ `skip_voice_reason()` / `log_skip_voice()`。<br>`src/ai/lang_voice_route.py`：`clone_lang_gate` 粤文按 `yue` 查能力表；`_route_cantonese` 克隆人设 × 专线只配 edge → **不切系统声**（保持克隆链 + `_yue_text`，tag=yue，交管线阻断）；专线配克隆节点 → `_lang_route_cleared=yue`。<br>`src/client/sender.py`（A 线 voice_reply 整段 + 分条）/ `src/inbox/voice_autosend.py`（B 线）：传 `tts_lang=plan_tts_lang(...)`；`not ok` 且阻断 → `[tts] skip_voice reason=clone_unavailable:… conv= lang=` 一行，改发文字（`record=False` 不算合成断档）。<br>`src/web/routes/unified_inbox_send_routes.py` voice-send：body `confirm_system_voice`；阻断 → `{ok:false, blocked:true, reason:"clone_unavailable:<why>", clone_unavailable:{why,lang,system_voice,system_voice_available}, message}`；`lang_mismatch` → `{blocked, text_lang, tts_lang, message}`；**路由级拒收**：产物 `provider∉CLONE` 且 `fallback_from∈CLONE`（试听复用 / #93 补标旁路）且无 confirm → 删产物、同形阻断响应。<br>`src/web/routes/voice_routes.py` tts-test：同参；阻断响应同形（sync + background job `blocked_fields` 透传）；#161 念错改派 Edge 只在 confirm 后做，否则作废乱音、`clone_unavailable:clone_lang_garbled:<lang>`；`clone_unavailable_message()` / `clone_unavailable_payload()` 单点（send-voice 复用）。<br>`src/web/i18n_packs/errors_stock.py` ZH/EN：`err.voice.clone_unavailable{,_lang,_quota,_offline,_garbled}` / `err.voice.lang_mismatch_skip`。 | `tests/test_tts_no_cross_gender_fallback.py` 两例改 Q-22 契约（缺省阻断 + `system_voice=Keita`；confirm 后 Keita/Nanami）；`tests/test_lang_voice_route.py` 粤语路由 3 例（预置声人设仍切 zh-HK / 克隆人设不切 / 专线克隆节点带 cleared）；`tests/test_minicpm_clone_tts.py` 粤语直构管线补 `lang_route_cleared`。语音 12 套件 **225 通过** |
| **A 语音行红条 + 「改发文字」「用系统音发」（右栏 cp-voice）** | `20b088d3` | `shared/copilot/components/cp-voice.js`（镜像 `desktop/renderer/shared/copilot/components/cp-voice.js` 同字节）：`static cloneBlocked(d)` / `langMismatch(d)` 识别 `{blocked, clone_unavailable}` / `reason=lang_mismatch`；`_renderBlocked()` 在预览区出 `pv-note err`（`data-role="clone-blocked" data-reason=<why>`）＝ 服务端本地化 `message` + 两键：`data-act="send-as-text"` → `_sendAsText()` 派 `cp-voice-send-as-text{text}`（宿主 preventDefault ＝接管）或 `root.__cpVoiceSendAsText`；`data-act="sys-voice"`（仅 `system_voice_available`）→ `_sysVoiceSend()` 原生 confirm 二次确认后 `_sendVoice({confirmSystemVoice:true})`，body `confirm_system_voice=1`；lang_mismatch 只给「改发文字」。`_genTts` / `_sendVoice` 的 `d.blocked` 分支都走红条（不再「发送失败请重试」）；试听请求缺省不带 confirm（`_genConfirmSys` 一次性位，签名不改保测试锚点）；`_previewSysConfirmed` 随预览清；确认发出后提示 `cp.voice.sent_sys`。<br>`shared/copilot/i18n/cp-i18n.js`（+镜像）ZH/EN 11 键 `cp.voice.blocked_generic / blocked_lang_mismatch / blocked_hint / send_as_text_btn / sys_voice_btn / sys_voice_confirm / sys_voice_none / as_text_filled / as_text_copy / sent_sys`。<br>透传：`shared/copilot/client/copilot-client.js`（+镜像）`voiceTts` 白名单加 `confirm_system_voice`；`desktop/main.js` `desktop:voice-tts` IPC 同加（send-voice 本就整包透传）。<br>宿主：`src/web/templates/unified_inbox.html` `cp-voice-send-as-text` → 填 `#reply-ta` 聚焦 + input 事件 + `cpv_send_as_text` 埋点（在 `__cpVoiceSentBound` 块内，不碰 xl-/lp- 区）；`shared/copilot/app.html` / `desktop/renderer/shared/copilot/app.html` 同事件桥 `post("cp-fill")`。缓存戳：三宿主 `cp-voice.js?v=20260912a`、`cp-i18n.js?v=20260912a`（`copilot-client.js` 戳沿用 Q-21 在途的 `20260912q21`，由其 commit 携带；本单新增字段为空即省略，旧戳亦不致错）。<br>`src/ai/tts_pipeline.py`：阻断日志前缀改 `[tts] fallback blocked reason=clone_unavailable:…`（`test_voice_verdict_single_250` 钉 `[tts] fallback` 行数=4）。 | `tests/test_cp_voice_ui_revamp.py` 15 / `test_voice_verdict_single_250.py` + `test_preview_speech_verdict.py` 合 59 通过；`node --check` 四文件过。同文件他线在途（Q-21 邻行戳 / conv_route）：`copilot-client.js` 用 `tools/stage_hunks.py --mine`，三宿主 HTML 用「HEAD + 本单精确替换」写 index（`git diff --cached` 只含本单行）。 |
| **B 能力前置可见（#288）**<br>**E 登记结果可停留面板（#239）**<br>**F 门禁 6 例** | `1867bac4` | `src/web/routes/voice_routes.py`：模块级 `clone_supported_langs(raw_cfg, backend, pid)`（= effective-config `voice_langs` 同口径：`clone_voice_langs` ∪ `clone_route_langs`，空=未知）+ `enroll_result_extras(raw_cfg, pid, vp, backend)` → `{supported_langs, preview:{kind:"tts_test", tts_test:{persona_id,format}, reference:{file,has_text}}, health:{probe_spawned,last}}`（**不在登记请求里同步合成**，面板「▶ 试听」按描述符调 tts-test）；`/api/voice/enroll` 三条成功出口（avatar / lan / qwen）都 `**extras`（avatar 出口回填 `health.probe_spawned`）；`/api/voice/profiles` 克隆行 +`voice_langs`。<br>`src/web/templates/personas.html` 语音卡（只动 `pe-vc-*` 区，Q-21/Q-26 hunk 不碰）：+`#pe-vc-langs`（`psn_vc_langs` 「克隆声支持语种：zh / en（其它语种发语音会被阻断…）」，`peVcSync` 从 profiles 行渲染，空=隐藏）+`#pe-vc-result` 可停留结果面板（`peVcRenderResult(pid, ok, det)`：成功＝录音名 + ▶ 试听 + 支持语种 + 体检（上次行 / 进行中 / 待夜间）+ 质检裁剪提示 + 试听终审回填槽 `#pe-vc-res-verdict`（听 `persona-voice-audition`）；失败＝原因 + 服务端原文 `<code>` + 质检建议；只在 × 关，换人设自动收）；`peVcAfterEnrolled(pid, announce, det)` 有面板元素就渲染面板、否则回落旧 toast；新听 `cp-voice-enroll-failed`。<br>`src/web/i18n_packs/persona_studio.py` ZH/EN 10 键 `psn_vc_langs / psn_vc_langs_unknown / psn_vc_res_{ok,fail,reason,health_last,health_running,health_pending,q_curated}`。<br>`shared/copilot/components/cp-voice.js`（+镜像）：`cp-voice-enrolled` detail +`supported_langs / preview / health / quality`；失败（含网络异常）新派 `cp-voice-enroll-failed{reason,message,quality,raw}`；右栏状态行克隆声 +「会念 zh / en」（`cp.voice.eff_langs`）；**目标语超出克隆能力 → 生成按钮置灰**（`_syncGenGate(code)`：`disabled` + `data-lang-blocked` + title=`cp.voice.gen_lang_blocked_t`「克隆声不支持 ja（会念 zh / en）——改发文字，或关掉跟随翻译发声」；`_setBusy/_syncCounter/_genTts` 同读闸位；预置声不置灰；粤语 × 克隆无 yue 同走置灰）。`cp-i18n.js`（+镜像）+2 键。<br>`tests/test_tts_clone_unavailable_q22.py` 7 例：① ja×STEVEN 阻断零音频零 edge + `system_voice=Keita` ② confirm → edge Keita + `system_voice_confirmed` ③ 自动链 `log_skip_voice` 落 `[tts] skip_voice reason=clone_unavailable:… conv=` + 两自动链源码接线、③b 冷启动恰重试一次（sleep 钉桩、等待 3–5s 受预算裁剪）④ 文本 zh × `tts_lang=yue` → `lang_mismatch`（不进合成）+ 一致时合成生效行三段字段 ⑤ `enroll_result_extras` 形状 + 三出口接线 + profiles `voice_langs` + 前端面板/事件 ⑥ send-voice 拒收 + errors_stock 6 键 ZH/EN + 红条两键/置灰闸 + `[tts] fallback` 行数=4 红线。 | 新门禁 7 通过；语音 10 套件（gold / placeholder_guard / cp_voice_ui_revamp / verdict_single_250 / preview_speech_verdict / enroll_hosted_linkage / toolbox_enroll_removed / no_cross_gender / lang_voice_route / minicpm_clone）**197 通过**。`personas.html` 用 `stage_hunks --theirs "psn-ar|psn_ar|Q-26|Q-21|_dpLoadLangCatalog"` 留他线 3 hunk。 |

## 2 交接

- 收工 09-12 06:4x：三 commit `b9b37262`（A/C/D 后端）→ `20b088d3`（A 前端红条 + 透传）→ `1867bac4`（B/E/F）。总账 Q-22 行已改，`agent_probe -Done` 已登。
- R83 打包线：本单 `.py` + `desktop/main.js` + 双树 copilot 静态 变更 → 走全量 `build:backend` + 桌面壳重打；**zhiliao 未重启**（`restart_preflight` 由值守走，与 #304 / Q-27 / Q-26 攒同批）。
- 缓存戳：`cp-voice.js` / `cp-i18n.js` → `20260912a`（三宿主已改）；`copilot-client.js` 戳沿用 Q-21 在途 `20260912q21`（其 commit 携带；本单新增字段为空即省略，旧戳不致错）。
- 与 Q-21 的接口：`plan_tts_lang()` 读 `peek_conv_lang_plan(cid).tts_lang`（dict / attr 双读，缺字段返回 ""）——Q-21 落地 `tts_lang` 后 D 段自动从「文本检测 vs 音色 locale」升级为三方核对，无需改本单代码。
- 未做 / 后续：登记面板「▶ 试听」复用 `peVcPreview()` 单句探针（多情绪四段试听不在本单）；`_langName` 在人设页回落语种码（`CopilotShared.langName` 存在时用显示名）。
- 既有红（非本线，收工时 HEAD 可复现）：`test_i18n_coverage` 2（conv_route 线 `inbox.mp.depth_/effort_` 缺 zh；Q-14 `38707c79` 的 `toTimeString().slice`）、`test_legacy_blue_ratchet` 2（`ops_act/ops_glance/reply_settings` Tailwind 蓝）、`test_template_orphan_refs`（`reply_settings.html#rps-po-empty`）。
- 验证纪律备忘：本单门禁用 `_spy_run_backend` 桩写 `ID3` 头假 mp3，**未**产出真实媒体；真机验收要按 media-artifact-validation 规则（Content-Type + magic bytes + 采样率指纹）另做。
