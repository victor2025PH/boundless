# chengjie → AvatarHub 对接单（2026-07-24）

> 发起方：chengjie（多平台 AI 聊天陪伴 / 客服，运行在 .117）
> 接收方：AvatarHub 侧（Hub 中枢 .176:9000 / IndexTTS-2 质量轨 .173:7865）
> 用途：回应「AvatarHub 语音底座对接指令 · 2026-07-24」，供 Hub 侧 cursor 读取后配合。

收到底座升级 IndexTTS-2 的指令。红线已理解并接受：
- 不自建 / 另训 TTS，所有语音走 Hub API；音色录入走 `/api/voice_clone`。
- 不绕过 Hub 直连 `.173:7865`。
- 不动 `.117` 的 `7852(CosyVoice)` / `7858(Qwen3-TTS)` 端口、契约、显存。
- 新增 GPU 服务先登记 `cluster_map.json` + 过 `tools/topology_lint.py`，再报 Hub 加 `SVC_*` 路由。

---

## 1. 产品 / 场景 / 语音能力需求

- **产品**：`chengjie` 多平台 AI 聊天陪伴 + 客服（Telegram / LINE / Messenger / WhatsApp），双实例（智聊 `18799` 陪伴 + 通译 `18899` 翻译）。
- **语音场景**：
  1. 好友消息**全自动语音回复**；
  2. **主动触达语音开场**（按聊天进度每隔几小时发问候语音）。
  - **非直播、非实时**——可容忍 2–4s/条，离线批量友好 → `best_of=2~4` 正合适。
- **能力需求**：
  - 语种：中文为主，**多语自适应**（en/ja/ko/th/vi/id/es…，按客户语言出声）。
  - 情绪：需要**真情感变化**（warm/empathetic/playful/sad/excited…）——IndexTTS-2 音色不变、情绪解耦正是所需。
  - 平均时长：**短句 4–80 字**，长回复在客户端**分条连发 2–3 条**。
  - 唱歌 / 变声：**不需要**。
  - 音色：7 个聊天人设共用 **2 个音色身份**（`lin_xiaoyu` 女声、`marcus_wei` / 刘德华系男声），**音色保真是硬需求**。

## 2. 调用量（灰度中，量不大）

- 峰值并发：约 **2–5 路**。
- 日均条数：**数百条**量级（随灰度放量增长）。
- 单条平均字数：**20–40 字**。
- 结论：**暂不需要** .173 开第二个 IndexTTS-2 副本；但有**夜间预渲染批量**（固定台词备货）会短时突发，见 §3 批量接口。

## 3. 需要 Hub 新增 / 确认的 API

- **ogg/opus 输出**：Telegram 语音条原生要 ogg(opus)。现在 `/api/tts_only` 返回 WAV，本地能转，但**若 Hub 直接出 ogg 更省一跳**（响度归一后再编码更好）。
- **音色录入 `/api/voice_clone`**：手头有**已降噪的高质量参考音**可直接录入（见 §6），走 voice_clone 入库，不本地私存。
- **批量接口 + profile 命名映射**：夜间预渲染希望批量提交；另外 curl 示例用中文 profile `林小玲`，本侧人设 id 是 `lin_xiaoyu` / `marcus_wei`——**请给 profile 名映射约定**（或允许用英文 id 建档）。

## 4. .117 服务清单（与 cluster_map 一致，不新增 GPU 服务）

| 服务 | 端口 | 显存 | 角色 |
|---|---|---|---|
| cosyvoice3 | 7852 | 3.5G | 集群公共兜底 TTS（**不动**） |
| qwen3_tts | 7858 | 2.5G | 批量兜底（懒加载，**不动**） |
| chengjie_zhiliao | 18799 | 0 | 陪伴主实例（CPU/API） |
| chengjie_tongyi | 18899 | 0 | 翻译实例（CPU/API） |

显存预算 6.5/11G 不变。**无新增 GPU 服务计划**。

## 5. 健康端点 + 日志（纳入 Hub 巡检）

- `cosyvoice3`: `http://127.0.0.1:7852/health`
- `chengjie_zhiliao`: `http://127.0.0.1:18799/login`（200=就绪）
- `chengjie_tongyi`: `http://127.0.0.1:18899/login`
- 日志：实例 `D:\chengjie-instances\<inst>\logs\`（app.log + boot_*.out.log）；ops 状态快照 `D:\chengjie-instances\.ops\last_status.json`。

## 6. 现状同步 + 需要 Hub 拍板的关键决策

**现状（避免重复建设）**：聊天语音**目前走本机 7852 直连**（`avatar_clone` backend），**不是** `/api/tts_only`。客户端已有一整套编排层：**预渲染命中缓存**（固定台词零延迟）、**分条连发节奏**、**副语言标记 / 情感变速**、逐字稿 sidecar。今天刚把男声参考音做了降噪优化——脏参考音噪底 count 896 → 干净版，声纹 cosine 0.576 → **0.65**（客观 A/B 实测）。该**干净参考音正好可作为 `/api/voice_clone` 的高质量录入素材**，不浪费。

**关键决策（请 Hub 侧回复）**：聊天陪伴要不要从本机 7852 直连 **迁到 Hub `/api/tts_only`（IndexTTS-2）**？本侧倾向迁（真人感质量轨 + 音色/情感解耦更强），但迁移前需 Hub 侧确认三点：

1. **音色身份**：IndexTTS-2 对这两个音色的克隆保真如何？本侧有 `clone_score` 客观工具，可对 IndexTTS-2 vs 现用 CosyVoice3 跑 A/B——**但需从 Hub 侧发起、走 `/api/tts_only`，本侧不直连 .173**。
2. **延迟**：IndexTTS-2 整段生成经 Hub 中转，单条端到端延迟大概多少？（短句 20–40 字为主，容忍 2–4s。）
3. **缓存**：迁 Hub 后本侧**预渲染零延迟命中层**怎么保留——Hub 侧有合成缓存吗？还是本侧在客户端缓存 Hub 输出的 ogg？

**本侧下一步（等 Hub 回复再动）**：先不改生产链，先经 Hub 发起对 IndexTTS-2 做两个音色的 A/B（音色保真 + 情感解耦真人感），数据裁决是否迁移；同时把干净参考音录入 Hub。Hub 回复延迟 / 缓存 / 批量能力后，再定迁移方案。

在 Hub 回复前本侧**不并发跑合成**（避免抢 7865/7852 队列），各做各的、不打扰。

---

## 回执请包含

1. §6 三点（音色身份 A/B 结论 / 单条延迟 / 缓存策略）的答复。
2. profile 命名映射约定（中文名 vs 英文 id）。
3. `/api/voice_clone` 录入方式（我把干净参考音 + 逐字稿怎么传给你）。
4. ogg 输出 / 批量接口是否可加、排期。
