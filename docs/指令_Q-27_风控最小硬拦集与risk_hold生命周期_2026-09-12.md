# 指令 Q-27：风控最小硬拦集 + risk_hold 生命周期——只有五类真高风险能停全自动，摘标 / 切全自动即释放

> 预算 **$140**。工单 **#301 追加（AFD2CD，09-12 04:23「请修·风控触发词掐死全自动」）/ #296（Q4D67X）/ #303 复验**（skuio 1.0.82）。前置：Q-17（三级）/ Q-18（cum 消歧 + 让位延后）/ Q-23（场景闸 + 软回应改道）已收工在 HEAD，本线在其上**收紧到产品原则**。落点表 `docs/发版对账_v1.0.83_Q27.md`。
> 用户原话即产品原则：**「全自动 = 客户来消息就该发出。触发词只保留必须项，其它禁止停自动。」**

## 事实（证据 `tmp_diag\{AFD2CD,UXBYZT,Q4D67X}`；代码只读核对）

- 1.0.82 上报障群会话 `telegram:7331682688:-1004345824259`：`[risk_hold] touch reason=adult n=26` → `[needs_human] 保持 reason=high_risk level=low category=- hits=-` → `auto_generate_draft L1 reason=risk_hold` → `[policy] risk_hold=adult forced=L1`。即：**一次 adult 命中留下的 24h risk_hold，之后每条入站哪怕 `level=low` 也被保持并强制 L1**，打标行不带类别命中词（`category=- hits=-`）。
- Q-17 的 30 分钟冷却只管「不重打」，不管「已存在的 risk_hold 何时释放」；Q-15 adult 的 `risk_hold ttl=24h` 与 `needs_human auto_clearable=False` 仍是默认。
- #296（Cameron 说 creepy）：`risk_hold clear … by=agent_ack held_min=5.5`，打标原因 `high_risk category=-`——坐席看不出为什么。
- 用户要求的最小硬拦集（可保持、不可调）：**未成年 / 自杀自伤 / 明确人身威胁 / 明确要钱要验证码 / 确认诈骗**。其余：成人内容默认**不转人工、不 24h risk_hold、不自动挡话**，人设政策 = 接住或软回；只有「露骨邀约 + 施压」才转人；禁止 keyword-only 停自动；`X cum Y` / `message cum reply` 白名单 L0；只提 phone / number / address 不转人工；无第二性暗示的单点词最多记日志；摘标或顶栏切全自动后禁止旧 risk_hold 再 forced=L1。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 硬拦集收紧** | `risk_grader.CATEGORIES` 高级只留 `self_harm / minor / threat / money_request（含验证码 / 凭据）/ scam`；`payment_keyword`（refund / password / OTP）降为中级「只标记 + 人审候选」不强制 L1；`adult`：`explicit` → 中级（人设政策接住 / 软回，**不 risk_hold、不 needs_human**），仅 `pressure`（露骨邀约 + 施压）→ 高；`privacy` / `request_*` 叙述与单点提及 → 低（仅日志）。**任何 keyword-only 命中不得单独把全自动改 L1**——高级必须是「类别 + 句式 / 施压第二信号」 | `risk_grader.py`、`adult_grader.py`、`drafts.py` 风控入口 |
| **B risk_hold 生命周期** | ① 只有高级命中才 `risk_hold.set`，TTL 从 24h 改 **2h**（高级）；中级不设；② **释放条件**：坐席摘标（我知道了）/ 坐席人工发送 / **顶栏切到或重选「全自动」** / TTL 到期——任一即 `clear` 并落 `[risk_hold] clear by=`；③ 已 clear 后新入站重新评估，不继承旧 shadow；`autosend_policy` 里 `forced=L1 was=L1 shadow=risk_hold:*` 只在 hold 仍 active 时出现 | `risk_hold.py`、`autosend_policy.py`（Q-23 收工，本线接手唯一动它）、切档路由（stage_hunks） |
| **C 打标行必带类别** | `[needs_human] 打标 / 保持` 一律带 `level= category= hits=`（`保持` 路径当前打 `-`）；会话头 chip 与横幅同源；`保持` 若 `level=low` → 不保持、直接 clear（低级不能续命一个 hold） | `protocol_autoreply.py`、`risk_hold.py` |
| **D 白名单与消歧** | `neutralize_cum`（Q-18 A）扩为通用 `phrase_whitelist`：`X cum Y` / `message cum reply` / `cum laude` / `phone number`（叙述）/ `your address`（叙述）→ L0；单点成人词无第二信号 → `[risk] low` 日志；配置可加白名单不可加黑名单（黑名单只在代码） | `adult_grader.py`、`risk_grader.py` |
| **E 回归 + 门禁** | 回放三条必过：Jeeo「finally got your loving message cum reply」→ L0 全自动正常回；华哥全自动会话「旧 adult hold 后」下一条入站 → 发出；「send me your bank card number」→ 仍 L1 + needs_human + 2h hold；`test_risk_grader_gate` / `test_adult_grader_gate` / `test_guard_context_gate` 更新到新级别表；纳入 R 批门禁 | tests |

## 红线

- 五类硬拦一字不放松；本线只**缩小**会停全自动的集合，不新增。
- 不引入罐头句；成人中级的「接住 / 软回」走 Q-23 的人设短生成，失败不发。
- 不动 `autosend_worker.py` 的 defer / resume（Q-18）与 GuardContext（Q-23）逻辑，只改级别表与 hold 生命周期。

## 开新对话粘贴

```
读 docs/指令_Q-27_风控最小硬拦集与risk_hold生命周期_2026-09-12.md，按它做 Q-27（#301追加 AFD2CD #296 #303复验：risk_grader 高级只留 self_harm/minor/threat/money_request(含验证码凭据)/scam，payment_keyword 降中级只标记，adult explicit 降中级人设接住或软回不 hold 不 needs_human、仅 pressure 高，privacy/request 叙述与单点提及低仅日志，keyword-only 永不单独改 L1 / risk_hold 只高级设、TTL 24h→2h，坐席摘标·人工发送·顶栏切或重选全自动·到期任一即 clear 落日志，clear 后新入站重评不继承旧 shadow / [needs_human] 打标与保持行必带 level category hits，level=low 不保持直接 clear / neutralize_cum 扩为 phrase_whitelist：X cum Y·message cum reply·cum laude·phone number 叙述·your address 叙述 L0，配置只许加白 / 回放三条：Jeeo message cum reply L0 全自动正常回、华哥旧 hold 后下一条发出、bank card number 仍 L1+needs_human+2h；三张门禁更新）。预算 $140。先读 Q-17 / Q-18 / Q-23 三张落点表（在其上收紧，不重做），再 scripts\agent_probe.ps1 -Intent 登记；你是本批唯一动 autosend_policy.py / risk_hold.py 的线；不动 autosend_worker.py 的 defer/resume 与 GuardContext；五类硬拦一字不放松；不引入罐头句。每个 commit 带 #301 并立刻补落点表 docs/发版对账_v1.0.83_Q27.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-27 行并 agent_probe -Done。
```
