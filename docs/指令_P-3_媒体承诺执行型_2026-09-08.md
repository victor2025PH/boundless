# 指令 P-3：媒体承诺执行型——「要么发，要么拒，不许说发了」

> 预算 **$160**。工单 **#259 #252**（报告 6SRA2B / BBRGMG；RRYKF6 第三次复现；CW6RP4 假成功待用户确认）。目标版本 1.0.79。
> 底座 `docs/修复指令_P批_并行_2026-09-08.md`。落点表 `docs/发版对账_v1.0.79_P3.md`。

## 开工前必读

1. `docs/全自动出站链_三视角方案_2026-09-08.md` §0 N4、§1.1 第 4 条、§2 P0-4。
2. 证据 `tmp_diag\6SRA2B\*_report.md` 时间线：14:49:16 `send_media dispatch origin=auto` → 14:49:20 `delivered=True mid=3EB0FB70…` → `[autosend image] 已发相册媒体 id=38197800…`（真发一张）；14:49:51 `near_duplicate 跨轮相近放行 matched='Just took this one for you—…'`（第二句照片文案放行但本轮无照片）；14:50:28 `[media_complaint] kind=lie_caught text='Where? I didn't get the other picture'`；14:50 AI「Ah, here it is—maybe it took a moment to load on your end…」无 send_media；14:51 客户「Where honey? No picture」**未被识别**；14:51 AI「Sorry love, I thought it went through—let me try sending it again now…」仍无 send_media。
3. 锚点（已核）：`src/inbox/image_autosend.py`（发图决策）；`src/inbox/autosend_helpers.py` `[autosend image]` 处（文案与发图的交汇点）；`src/ai/outbound_promise_guard.py`：`detect_media_promise` L333 / `strip_media_promises` L371 / `detect_media_claim` L455 / `detect_sent_claim` L627 / `strip_sent_claims` L645 / `build_promise_rewrite_instruction` L672——**识别与改写函数都在，缺的是接线成「动作」**；`src/ai/companion_selfie.py` L1236 `detect_media_complaint`（`lie_caught` 分支 L1192，hint 式）；`src/companion/photo_capability.py`（`capabilities.photos`）；M-2 D「发送 ✓ 以回执为准」口径用于「真发成功」判定。
4. `scripts\agent_probe.ps1` 看 ACTIVE；`persona_reply.py` 与 P-1 分段（你只动结构化 `send_photo` 输出段），先看 P-1 是否在改，冲突则等它提交后 rebase。

## 红线

- 只动归属矩阵 P-3 行；`autosend_worker.py` 不碰（需要它配合的接口，先在 `autosend_helpers.py` 做适配层）。
- 「真发成功」= 平台回执 mid（M-2 D 口径），不是 dispatch。
- `git add <显式路径>`；每 commit 带 `#259`；不 `-A`。

## A 文字绑动作

- 修法：生成侧（`persona_reply.py` 结构化段）允许模型输出 `{"send_photo": {"query": "...", "scene": "..."}}` 作为**请求**而非事实；管线：`image_autosend` 按 query 选图 → 真发（回执 mid）→ **成功后**才允许配文入队（配文模板来自模型的 caption 字段）；无匹配图 / 发送失败 → 把「无图可发 / 发送失败」回喂模型重生一句**不含承诺**的回复（或用固定诚实模板）。日志 `[media-bind] conv=… requested=1 sent=1|0 mid=… caption_allowed=1|0`。
- 验收：有图 → 先图后文；无图 → 回复无任何承诺词；tests 两例。

## B 承诺句无动作即改写

- 修法：出站前（起草层，P-1 净化之前或之后均可，先于落库）：`detect_media_promise` / `detect_sent_claim` 命中（here it is / sent you / took this for you / just took this / let me send / check your phone / it went through / should be there / 发你了 / 看手机 / 刚拍的 / 给你发了）且**本轮无已回执 send_media** → `strip_*` 改写；改写后为空 → 用 `build_promise_rewrite_instruction` 让模型重写一次；仍命中 → 转人审（与 O-1 C 客服腔守卫同结构）。日志 `[media_promise] conv=… claims_send=1 attached=0 action=strip|rewrite|review`。
- 验收：回放 14:49:51 第二句 → 改写为无承诺；14:50 / 14:51 两句 → 拦下；tests 覆盖词表每条 + 「客户原话引用」不误伤。

## C lie_caught 固定动作 + 复诉词

- 修法：`detect_media_complaint` 词表补 no picture / where? / didn't get / nothing came / still nothing / 没收到 / 哪呢 / 没看到；`lie_caught` 从「给 hint 让模型编」改为**固定动作**：① 本会话最近一次真发的媒体 id 已知 → 自动重发同一张 + 一句短话（模板，不经 LLM 承诺）；② 从未真发过 → 强制实话模板（「刚才没发出去，我这边看一下」类，禁「加载慢 / 再试 / 网络」）；③ 连续第二次 lie_caught → 转人工 + 标签。日志 `[media_complaint] kind=lie_caught action=resend|honest|handoff media_id=…`。
- 验收：回放 6SRA2B 三轮 → 第一轮真发、第二轮重发同图、无第三轮谎话；tests 三分支。

## D `capabilities.photos=false` 硬拒 + 相册空诚实拒

- 修法：人设无照片能力或相册为空 / 无匹配 → 生成侧不允许任何 `send_photo` 请求，承诺词一律 B 段改写为诚实拒绝（人设口吻模板三语）。
- 验收：关掉 photos → 索图 20 例零承诺。

## E 同会话 5 分钟内第二句照片文案必须真附图

- 修法：`near_duplicate 跨轮相近放行`（`autosend_helpers.py`）对「照片文案类」不放行——命中承诺词且 5 分钟内已有一次真发 → 必须再附图（复用 A 段）否则改写。日志一行。
- 验收：回放 14:49:51 → 附第二张或改写。

## 收工

- 落点表 `docs/发版对账_v1.0.79_P3.md`（§〇 「为什么守卫看见了却没拦」：识别 → 无动作权 三段）。
- `docs/P批_进度总账.md` 只改 P-3 行；`agent_probe.ps1 -Done`；回访文案交 P-5（给 skuio：三轮回放结果 + `[media-bind]` / `[media_promise]` 日志 + 请他确认 14:49 照片气泡是否存在以排除 CW6RP4 假成功）。

## 开新对话粘贴

```
读 docs/指令_P-3_媒体承诺执行型_2026-09-08.md，按它做 P-3（#259 #252：文字绑动作——模型只能输出结构化 send_photo 请求、真发回执后才配文 / 承诺句无 send_media 即 strip→rewrite→review / lie_caught 固定动作重发上一张或强制实话、复诉词补全 / photos=false 与相册空硬拒 / 5 分钟内第二句照片文案必须附图）。预算 $160。先读 docs/修复指令_P批_并行_2026-09-08.md 的归属矩阵，再 scripts\agent_probe.ps1 -Intent 登记；outbound_promise_guard 的识别/改写函数已在，重点是接线成动作；persona_reply.py 只动结构化 send_photo 段且与 P-1 分段；autosend_worker.py 不碰。每个 commit 带 #259 并立刻补落点表 docs/发版对账_v1.0.79_P3.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/P批_进度总账.md 的 P-3 行并 agent_probe -Done，回访文案交 P-5。
```
