# 修复指令 M 批（1.0.75 首日复验暴露的对客风险）—— 并行公共底座

> **本文件只是「索引 + 去向表 + 文件归属 + 老板决策」的公共底座，不要拿本文件开对话。**
> 每条指令各自成文，**一条指令 = 一个新对话**。开对话粘贴话术在各指令文件末尾，也汇总在 `docs/M批_进度总账.md`。
>
> 依据：1.0.75 于 09-06 13:30 发布（内测渠道 `downloads/internal/`，两台内测机 13:38 / 13:55 装上）。skuio 13:55–21:12 传 51 份 Cursor 报告、钧 13:39–13:52 群提交 3 条，L-7 A 自动登记链全部挂单/立单：**新单 #214–#235（22 张）+ 补证挂旧单**，回执已发，**但内容无人分析**（L-7 13:43 收工在前，之后无值守）。本文件是 09-06 22:5x 的通读结论。
> **结论：1.0.75 把三条链从「关着」打开了——关怀真发、目标冲刺真发、Messenger 新号直接全自动——而这三条链的内容/通道校验没跟上，于是「错但没发」变成「错且发到客户」。** 其余是 1.0.74/1.0.75 修法的第三轮复验缺口（幽灵未读口径、上传通道、顶栏）与上游未通（转录、出图）。
> 目标版本 **1.0.76**。**M-1 / M-2 是对客事故面，今晚就做；M-3 明确根因已给；M-4 / M-5 可并行；M-6 收口。**

| 指令 | 预算 | 类型 | 内容 | 文件 |
|---|---|---|---|---|
| **M-1** | $150 | 出站三闸止血（对客真发） | #214 #218 关怀真发内容错误 + dry_run 状态不一致 / #234 自动回复语言错误（翻译校验失败兜底发原文）/ #219 己方删除消息不同步进 AI 上下文 | `docs/指令_M-1_出站三闸止血_2026-09-06.md` |
| **M-2** | $180 | 通道真相 + 登录默认（Messenger 为实例，规则全平台） | #232 #233 边车零会话仍显 ✓ / 重登 30s 全自动批量投递触发熔断锁死手动 / 登录默认半自动 + 冷静期 + 账号级批量开关 + 通道失败自动降级 / #235 发前确认 L1 原因可见 / #223 作用域文案 + 手动粘性 | `docs/指令_M-2_通道真相与登录默认_2026-09-06.md` |
| **M-3** | $150 | 媒体发送链（四平台） | #227 大文件上传阻塞本地 Web 服务 20–30s（钧 #215 同源）/ #229 #231 .MOV 前端静默拦截复用「结果未知」/ #228 附件全局态跨会话串 / 错误四分类 + 上限同源 | `docs/指令_M-3_媒体发送链_2026-09-06.md` |
| **M-4** | $150 | 工作台第三轮 | #222 幽灵未读第三轮（角标口径 / 存量被埋清理 / 其余 5 条）/ #221 顶栏胶囊整页重载 + 主题重置 + 溢出 / #230 多选按钮 / #206 补证 会话工具整块删除 + 标签下拉遮挡 | `docs/指令_M-4_工作台第三轮_2026-09-06.md` |
| **M-5** | $120 | 产品清理 + 上游 | #217 「转化成交」类目用户版隐藏 + 目标无产品禁报价话术 + 真发首条预览 / #224 养号面板矛盾 / #226 出图空壳 / #225 工具箱转录链 / #220 学习队列日语译文 | `docs/指令_M-5_产品清理与上游_2026-09-06.md` |
| **M-6** | $100 | 值守收口 | 钧 #215 定性回访 / 22 张新单回访 + 46 行对账表 1.0.76 版 / 1.0.76 装载·两包·两渠道·装机 / 台账 | `docs/指令_M-6_值守收口_2026-09-06.md` |
| **M-7**（09-07 02:5x 追加） | $150 | 目标引擎可追溯与真出手 | #236（= #166 族第五次复报：4 天 3 版本 0 次可见推进）：每拍可追溯 / 注入不断档 + 日志无死角 / 到期结算不静默 / 卡片说真话 / 单目标 stalled | `docs/指令_M-7_目标引擎可追溯与真出手_2026-09-07.md` |

**投放顺序**：M-1 与 M-2 先开（两条都碰 `autosend_worker.py`——**M-1 只动出站翻译判定，M-2 只动登录/通道门禁与批量投递节流**，改前互看意向板）→ M-3 / M-4 / M-5 文件不重叠可并行 → M-6 等前五条收工。
**跨对话 / 跨账号交接唯一入口**：`docs/M批_进度总账.md`。
**背景**：`docs/L批_进度总账.md`、`docs/发版对账_v1.0.75.md`、`docs/未修复问题总盘_三视角_2026-09-06.md`、`docs/值守_报障群用户画像_2026-08.md`。证据全在 `D:\boundless\tmp_diag\<码>\`（每份目录有 `ticket.txt` 记归属）。

## 22 张新单 + 补证的去向

| 去向 | 单 / 报告 | 说明 |
|---|---|---|
| M-1 | #214 #218（M2SHYA XZFA3S WN4JXJ 9YYD44）/ #234（U5Y7TX 8DVDVC GSYXQ8）/ #219（4NHEYW J64GVX） | 三件都是「已发到客户的错」或「会让 AI 接着错下去的错」 |
| M-2 | #232 #233（VPVZUH JV83XM YZW2E9 ZGKVQB SAD7Q2 CW6RP4 UE7VM3）/ #156 补证 K9CY6R 8TV32T / #235（3K9ZF9）/ #223（9YXPFX Q6Y856） | UE7VM3「以此条为准」是 skuio 合并后的产品要求，D-M1 采纳后照做 |
| M-3 | #227（SHG5WS MKYD7D SPRZKS 8YNDKE）/ #228（CJDQ56 V9DZ5S）/ #229（843XQV DR2QWV）/ #231（B65E24 GAJ5T2）/ 钧 #215（图 1255 1256） | GAJ5T2 已把 A/B/C 三层与 12 次验收矩阵写好，照单做 |
| M-4 | #222（GQREHD FF5PGS）/ #221（Y8Z47B 4KEJ63 7E3XKR）/ #230（4A4FAQ）/ #206 补证（SJTRJY 52RAGZ） | 幽灵未读是第三轮：J-4 A 入口 + L-3 B 闭环之后，剩「口径统一 + 存量清理 + 群/频道堆积」 |
| M-5 | #217（2D26JS 48JB6U ETR3XA Q6Y856 6TGCPC）/ #224（9TJCFK）/ #226（WPHAD7 X6MDC5）/ #225（5FW4KK 5NXHUW）/ #220（RJF9N7 DMS45P） | #216 / AC5XP9 / 5JH5RJ 是**验收通过**（目标引擎真发、需人工启动清扫、危机留痕默认开），只记不修 |
| M-6 | 钧 #215 + 全部 | 回访口径由 M-1…M-5 落点表「交 M-6」节提供 |
| M-7（09-07 追加） | #236（KH9FEP H82N5Q 7FKNFV）+ #166 族前四轮（5NAR3T XJWZRG V76EPK AC5XP9 X8MYGP 5JH5RJ） | 与 M-1…M-5 文件不重叠（`companion/goals/*` + `cp-goal.js`），可立即开；落点表按 1.0.76 是否已发决定写 `_M7` 随哪版 |
| 只记不修 | 钧 #215 语音「已核有声 vs 疑似无声」矛盾 → 并 #161 族随 M-3 拉到的日志定性；#223 结论已由 Q6Y856 给出 → M-2 改文案即闭环 | |

## 文件归属矩阵（共享工作树；开工先 `scripts\agent_probe.ps1` 看 ACTIVE 再动）

| 文件 / 目录 | 归属 |
|---|---|
| `src/contacts/care_dispatcher.py`、`care_schedule.py`、`src/integrations/companion_proactive.py`（dry_run 状态口）、`src/web/routes/care_routes.py`、care 相关模板 / i18n；`src/inbox/autosend_worker.py` **只动出站翻译判定两处**（L885 / L1165 附近 translate_hold 的覆盖面 + target_lang_mismatch 分支）；`src/inbox/outbound_xlate*` / 语言决策；`src/client/telegram_client.py` L1131–1145 `_on_deleted` + `protocol_bridge.report_deleted_messages`（己方删除）；`src/inbox/store.py` **只准加一列/一函数标记己方撤回**（需在落点表点名，默认仍禁改） | **M-1** |
| `config/config.yaml` `inbox.auto_draft.bootstrap_automation_mode` + 全局 `automation_mode` 默认、`src/inbox/autodraft_helpers.py` `maybe_bootstrap_automation_mode`、`autosend_worker.py` **只动**「登录后冷静期 / 通道连续失败降级 / 退避锁手动优先」三处、`src/integrations/messenger_rpa/service.py` + `services/messenger-web/**`（会话持久化 / 健康探测 / 500 落日志）、`src/inbox/channel_adapters.py`（发送 ✓ 以回执为准）、`src/integrations/platform_session_health.py`、`unified_inbox.html` **只动**：会话头部下拉文案 / 顶栏「自动(N)」胶囊行为 / 发前确认原因条 / 账号菜单批量切换四处、`src/inbox/drafts.py` **只准加 reason 字段日志一行**（D-M10）、对应 i18n / tests | **M-2** |
| `src/web/routes/unified_inbox_send_routes.py`（send-media 上传：L1200–1260 读流 / 上限 / 访问日志）、`src/bootstrap/web_app.py`（仅上传体上限 / 线程池配置）、`unified_inbox.html` **只动**：`_mediaQueue`/`_pendingMedia` 按会话隔离（L13497 起）/ 选文件格式白名单 / 附件条文案四分类、`src/integrations/protocol_bridge.py` `_OUT_VIDEO_EXT` + 转码钩子、`desktop/`（若 ffmpeg 随包）、`static/workspace/sticker-panel.js` 同口径、对应 tests | **M-3** |
| `unified_inbox.html` **只动**：角标口径 / 被埋存量清理入口 / 多选工具条 / 会话工具面板删除四处、`workspace_base.html`（顶栏收缩规则 + 主题开关进头像菜单 + 站内导航带 `?theme=` 助手）、`src/inbox/health_watchdog.py`（被埋计数口径）、`src/web/routes/unified_inbox_read_routes.py`（`/account-unread` 与列表同 WHERE）、对应 CSS / i18n / tests | **M-4** |
| 目标模板种子（经 `i18n_packs/goals.py` 键名反查所在 `src/companion/goals/*templates*`）+ `goal_routes.py`（client flavor 过滤）+ 冲刺出站接 `persona_guard.service_frame`、`nurture` 面板模板、`image_gen` 路由 / 面板、`src/ai/audio_pipeline*`（工具箱转录链并入 `voice_transcriber`）、`src/utils/daily_learner.py` + 语种识别（文字系统判定）、对应 i18n / tests | **M-5** |
| `tools/duty_*`、`deploy/**`、发版脚本、台账（只经端点 / `duty_*`）、`docs/**` | **M-6** |
| `src/companion/goals/{service,sprint_ticker,liveness,notify,store}.py`（不改冲刺安全闸本身）、`src/web/routes/goal_routes.py`（新 `/beats` 端点）、`desktop/renderer/shared/copilot/components/cp-goal.js` 双树、`i18n_packs/goals.py`、对应 tests | **M-7** |
| `skill_manager.py`、`drafts.py`（除 D-M10 一行）、「unlimited outbound mode」孤儿线、assistant 线文件 | **谁都不碰** |

## 老板决策（默认按此执行；不同意的划掉，对应对话按划掉后执行）

| 决策 | 默认（= 我的建议） | 为什么 | 影响 |
|---|---|---|---|
| **D-M1** 登录/重登默认半自动 | **采纳 UE7VM3 ①②③⑦**：任何账号登录 / 重登 / 清数据重登 → 半自动（只起草）；全局默认 `auto_ai` → `semi`，`bootstrap_automation_mode` 关；全自动须按账号显式开启并确认「将对 N 个会话开启」；登录后冷静期 10 分钟只起草并列积压；同账号连续 3 次发送失败自动降半自动 + 红标。**已在全自动的老会话不动。** | 今天 Messenger 重登 30 秒内向 7 个客户批量投递、首条 500 起熔断锁死手动——通道没准备好就开火，是所有假成功/错语言事故的放大器；9SZ8DY 提案与 #167 首登人审都指向同一处 | M-2 |
| **D-M2** 关怀默认原文直发 | **采纳**：「补一条关怀」默认走 verbatim（`care_dispatcher.py` L629 已有的不经 LLM 路径），AI 润色改为可选且必须预览确认；含地名 / 人名 / 首次真发强制预览；生成注入客户档案（国籍 / 居住地 / 相识时长）并做事实校验；care 生成落日志 | 用户写的是要发的话，不是「客户说过的事」；今天已经把菲律宾本地人当外国人真发出去了 | M-1 |
| **D-M3** 出站语言兜底 | **采纳 8DVDVC**：目标语言优先级 = 会话手动翻译设置 > 客户档案语言 > 客户消息文字系统 > 人设/账号对外默认语言 > **全无则转半自动，禁止自动投递**；出站翻译校验失败（含 `target_lang_mismatch`）一律拦下标「翻译失败待确认」，**任何情况不得落到操作员界面语言 zh**；表情 / 单字符不进翻译器 | 现有「不发原文」纪律只盖住 provider 不可用，没盖住校验失败与目标未知两条路 | M-1 |
| **D-M4** .MOV 处理 | **两步**：本批先「选文件即拒绝并写明仅支持 MP4/…」；后台转码 MOV→MP4 若随包 ffmpeg 可用则同批做，否则排 1.0.77 | iPhone 默认 .MOV，用户高频；先止住「静默失败复用结果未知」的误导 | M-3 |
| **D-M5** 视频上限 | **先压后放**：前端声明上限改读后端实际值（同源），在流式 / 分片上传落地前把视频上限临时压到实测可用值（建议 50MB）；本批做流式上传 + Web 层访问日志；验收 GAJ5T2 的 12 次矩阵 | 现在写 100MB、98MB 静默丢，还把本地 Web 服务堵 20–30 秒 | M-3 |
| **D-M6** 「转化成交」类目 | **用户版隐藏整个类目**（含 4 张预置），partner / internal 保留；引擎硬规则：目标未绑定产品字段时禁止生成报价 / 开户 / 支付话术（接 `persona_guard.service_frame`）；目标真发首条强制预览 | 两张是厂商卖智聊的销售剧本、两张是无产品字段的空壳；冲刺引擎 1.0.75 刚真发，留着就是让 AI 向陪聊客户推销智聊 | M-5 |
| **D-M7** 空壳面板 | **用户版收起**：智能养号面板（保留默认关 + 人话说明）、AI 生成图片（去「联系运维」，改「开发中」态只留相册兜底）、右侧「会话工具」整块删除（状态卡迁顶部 AI 状态弹层）——全部复用 L-4 的 `ui_client_hide` 机制 | 与 L-4 同一原则：没准备好的不出现在用户版 | M-5 / M-4 |
| **D-M8** 顶栏徽标与主题 | **徽标改回「旗舰版」**，`plan_override`「厂商自营」进悬浮提示；主题开关进头像下拉菜单；顶栏右侧加收缩规则 | 徽标加长把主题开关挤出窗口；同时回答 L-4 待老板②「有 override 的内测机徽标是否不受影响」= 不受影响 | M-4 |
| **D-M9** 手动模式粘性 | **采纳 Q6Y856**：切手动默认「一直手动」，30 分钟自动接回改为可选（切换时三选：一直手动 / 30 分钟接回 / 离开时接回） | 13:11 实例：坐席明确切手动，30 分钟后被程序改回全自动 | M-2 |
| **D-M10** `drafts.py` 禁改例外 | **允许 M-2 只加一行**：`level=L1` 日志带 `reason=` 字段（no_persona / cooldown / lang_unknown / first_contact / weak_evidence），不改判定 | #235 卡片要显示原因，日志没有字段就只能猜 | M-2 |
| **L-4 四问补答** | ① 回复策略预设档：**不给用户版入口**；② `plan_override` 徽标：**不受 override 影响**（D-M8）；③ partner 是否藏 `bug_tickets`：**藏**；④ 歌房资产不齐：**运行时直接视为关** | 四条都是「用户版少即是多」 | M-4 / M-5 / L-4 回访 |

## 共同纪律 / 省钱纪律 / 断点续传

与 J/K/L 批完全相同，见 `docs/修复指令_J批_并行_2026-09-05.md` 对应三节。三条硬红线再钉一次：

- 提交只用 `git add <明确文件>` 或 `python tools/stage_hunks.py --file … --mine … --apply`；⛔ `git add -A` / `-a` / `reset --hard` / `checkout .` / `restore .` / 裸 stash。**L 批实录：共享 index 竞态让 L-3 C 八个文件被卷进 L-5 的 commit——提交前 `git diff --cached --name-only` 为空才 add。**
- 开工先 `powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "…"`；收工 `-Done`。模板 / i18n / 静态 JS 热更新即上生产，中间态必须自洽。
- 每项一个 commit 带工单号；落点表写 `docs/发版对账_v1.0.76_M<N>.md`；总账只改自己那一行；对外答复一律走 `tools/duty_reply.py`。
