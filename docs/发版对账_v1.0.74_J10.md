# 发版对账 v1.0.74 · J-10（AI 记忆机制一期 + 「AI 记忆」页重构 · #183 #177 #182）

> 指令：`docs/指令_J-10_AI记忆页与记忆机制一期_2026-09-05.md`；总账：`docs/J批_进度总账.md` J-10 行。
> 断点规则：A 组（机制）先于 B 组（页面）；一件一个 commit；接手方先 `git -C D:\boundless log --oneline -15 -- engines/chengjie` 核对下面的 commit 真在。
> 红线：`unified_inbox.html` / `drafts.py` / `store.py` 不改；`memory_grounding` 安全语义不弱化；不引入 Mem0/Zep（D8）。

## 落点表

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注 / 剩余 |
|---|---|---|---|---|---|
| **A1 跨语言接地** | #183 | `d03f32e4`（+ `c61c9131` 纯 EOL 归一） | `src/ai/memory_grounding.py`（新增引文级判据 `ground_fact_with_evidence` / `ground_fact_items` / `summarize_drop_reasons` / `normalize_for_match` / `evidence_matches_user_msg`；旧 `fact_grounded_in_user_msg` / `filter_grounded_facts` 原样保留）· `src/ai/ai_client.py`（新 `extract_memory_facts` → `{"facts":[{fact,evidence}], "dropped":[{fact,evidence,reason}], "candidates"}`，`extract_memory_bullets` 成兼容壳；prompt 加「引文铁律」；`_parse_memory_fact_items` 兼容旧 `["..."]` 格式；`_ground_extracted_fact_items`）· `src/skills/skill_manager.py`（**只动**抽取调用点 3 hunk：消费 `extract_memory_facts`、`source_quote=evidence`、`record_grounding_drops`、日志 `grounding_dropped=`）· `src/utils/episodic_memory_store.py`（新表 `episodic_grounding_drops` 30 天保留 + `record_grounding_drops` / `grounding_drop_summary` + `admin_summary.grounding_drops` + `delete_by_source_quotes` 加引文子串匹配）· `src/inbox/human_outbound_memory.py`（`_grounded` 同款切换）· `src/eval/memory_extract_eval.py`（`evaluate_evidence_grounding` / `load_grounding_samples` / `format_grounding_report`）· 新 `config/eval/memory_grounding_samples.yaml`（15 样本 25 候选）· 新 `config/eval/memory_extract_samples_xlang.yaml`（LLM 门禁专用）· tests：`test_memory_grounding.py` +11、新 `test_episodic_grounding_drops.py` 4、`test_human_outbound_memory.py` +1、`test_memory_extract_eval.py` +1、`test_episodic_memory.py` / `test_media_desc_guard.py` 源码钉改指向 `extract_memory_facts` | 旧 9 例全绿；A1 集（grounding / extract_eval / human_outbound / episodic_memory / grounding_drops / media_desc_guard）**123 passed**（基线 106）；邻接集（peer_delete_purge / episodic_backfill / memory_recall_eval / persona_hard_pins / inbox_draft_engine）64 passed；`python -m scripts.run_eval --memory-extract --json` **前后同为 召回 1.0（6/6）/ 误抽 0**；接地评测 keep 14/14 · drop 11/11 · 原因不符 0 | **判据**：evidence 归一（NFKC/casefold/去标点空白）后是 user_msg 子串，或 token 重叠 ≥60%；事实文本不参与 → 语言无关。**不弱化补钉**：同语种（中↔中 / 英↔英）照旧叠加旧词汇判据（`fact_unanchored`）；应答/填充词（yes / ok / 好呀）不算引文；事实里拉丁名字/≥2 位数字必须真在原话里（「客户自称Tom」↔「I'm Bob」丢）。丢弃原因三种：`no_evidence` / `evidence_mismatch` / `fact_unanchored`（指令列两种，第三种＝旧判据在同语种下仍在咬的量，单独可观测）。**88MP86 基线**（修前）：992 轮 `[episodic] extract done` 中 960 轮 `llm_count=0`，96 次护栏事件丢 151 条（全部英文客户中文事实）。**装 1.0.74 后由值守跑同口径**：`Select-String "extract done" | llm_count>0 占比` + `grounding_dropped=` 分布 + `/api/episodic-memory/summary` 的 `grounding_drops.by_reason`。`run_eval.py` 不在本单文件边界，接地评测未挂 CLI（走 `tests/test_memory_grounding.py::test_grounding_eval_gate`）。**并行提示**：15:57 有另一会话直接改写了 `memory_grounding.py`（未登意向板），设计与本线一致且更完整，本线采纳其 API 并接线，未再改该文件。 |
| **A2 例外队列 + 自动转正** | #183 · D8 | `d9677847`（+ 透传体补在 `1c7e5a73`） | `src/utils/episodic_memory_store.py`（幂等 ALTER：`review_reason` / `impact` / `status` / `conflict_group` / `recall_count` / `last_recalled_ts` + 索引；`add_fact(confidence=, author=, review_reason=, impact=)` 写入即打标 + 同槽 stable 冲突判定；`review_queue` / `review_counts` / `confirm_fact` / `ignore_fact` / `restore_fact` / `resolve_conflict(keep_id)`；`consolidate(auto_promote_days=7, auto_promote_min_recalls=1)` 第二路径；`list_rows(status=, review=)` + `_ROW_COLS/_row_to_dict` 统一行形状；`admin_summary` 加 `total_count / total_users / stable_count / review`；recall / profile / inferred_counts / dedup / contradiction / backfill 全部排除 `ignored`；prune 先裁 ignored；R11 系统推翻 stable 时清组内 conflict 标）· 新 `src/utils/memory_review.py`（`classify_fact` 四类 + `is_high_impact` 词表）· `src/web/routes/episodic_identity_routes.py`（`GET /api/episodic-memory/review-queue`、`POST {row_id}/ignore`、`POST {row_id}/restore`、`POST resolve-conflict`；列表 `status` / `review` 参数）· `skill_manager.py` 3 hunk（`auto_promote` 配置透传 / `memory.review.low_confidence_threshold` 透传 / `episodic_list_for_admin(status, review)` 透传）· tests：新 `test_episodic_review_queue.py` 11 例；路由基线 +4 | 11 新例全绿；episodic/memory 回归 154 + 55 passed；`test_memory_contradiction` 1 例先红后修（R11 推翻 stable 后新条仍带 conflict 标被挡在 prompt 外 → 清组标） | **语义**：客户事实自动记，`review_reason` 非空才进队列（`conflict` / `high_impact` / `low_confidence` / `self_fact` / `commitment`，单一原因，conflict 最高优先）；`impact=high` 标红不阻断记录；`conflict` 新条**不召回**直到人选（stable 留在 prompt 里，不出现互相矛盾的两条）；`status=ignored`＝「不再使用」软删（不召回/不算画像/不补向量/最先被裁/可恢复），硬删仍 `DELETE`；`confirm` 语义不变（转正 + 清标），对不在队列的 user_stated 仍不命中（旧测试钉）；自动转正两条路径都跳过 conflict / ignored。**配置**：`memory.consolidation.auto_promote.{days:7, min_recalls:1}`（`false` 关）、`memory.review.low_confidence_threshold`（默认 0.6）——`config.example.yaml` 是 D1b 在改的文件，文档键待 D1b 收工后由值守补。**未动**：`inferred_counts` / `profile_summary.pending_inferred` 仍是旧「raw ai_inferred」口径（R14/R15 测试钉；B 组 KPI 卡改读 `admin_summary.review`）。**踩坑**：`tools/stage_hunks.py` 按标记分块，无标记的 hunk 会被当他人丢弃——A2 首次只提交了 wrapper 签名没提交透传体，A3 补上并把路由测试改走真 wrapper；接手方改 `skill_manager.py` 每个 hunk 都要带 `J-10` 字样。 |
| **A3 召回记账** | #183 | `1c7e5a73` | `src/utils/episodic_memory_store.py`（新表 `episodic_recall_log(ts, batch_id, memory_key, row_id, conversation_id, chain, draft_or_reply_id, inbound_msg_id)` 90 天保留；`get_bullets_with_ids`（`get_bullets_for_prompt` 成其 `[0]`）；`record_recall` / `recent_recalls` / `recall_stats`；`admin_summary.recall`）· `skill_manager.py` 1 处（`_inject_episodic_into_context`：有 `get_bullets_with_ids` 的 store 取 id，注入成功后一行 `record_recall(chain=inbox|direct, inbound_msg_id=user_msg_id)`，旧/假 store 走旧口）· `episodic_identity_routes.py`（`GET /api/episodic-memory/used`）· `episodic_memory.html` 人档抽屉「最近被用」分区 · `i18n_packs/episodic_page.py` +7 键双语 · `ui-build.txt` 20260905-1706 · 新 `tests/test_episodic_recall_log.py` 5 例；路由基线 +1 | 5 新例全绿（含注入点接线真 store + 假 store 退路 + 路由）；回归 197 passed；模板三门禁 10 passed；i18n 374 passed | `draft_or_reply_id` 注入时尚不存在 → 留空；J-4 按 `conversation_id` 取最近一批即「本轮」。zh_hant 未再生（缺键回落简体；B 组统一再生）。`recall_count` 立即被 A2 自动转正消费。 |
| **A4 「AI 自身经历」入口** | #177 | `72e0df5a` | 新 `src/utils/memory_self_log.py`（`collect_self_experience` / `delete_self_experience_entry`：读 `_human_said_log` + `_self_state_log` + `_media_sent_log` + TTL 内 `_media_pending`，只读拼时间线、删一条只改那份日志）· `episodic_identity_routes.py`（`GET /api/episodic-memory/self-log?memory_key=|conversation_id=`、`POST /api/episodic-memory/self-log/delete`；记忆键 → ContextStore 键用 `peek` 只读解析：conversation_id 拆 acct/chat → identity → 剥平台前缀 → 原键）· `episodic_memory.html` 抽屉「人设说过 / 承诺过」分区 · `episodic_page.py` +9 键 · `ui-build.txt` 20260905-1726 · 新 `tests/test_episodic_self_log.py` 3 例；路由基线 +2 | 3 新例全绿（含端到端：`on_human_outbound("我有个女儿")` → 接口可见 → 删后 `human_said_note` 少那条且落盘）；路由/模板/i18n/记忆回归 411 passed | **不影响任何注入逻辑**（只读 + 删日志条目）。**承诺持久化归二期**：`outbound_promise_guard` 是纯函数守卫无持久面，一期只能显示 15 分钟 TTL 内的悬置承诺（`_media_pending`）；「答应过什么 / 兑现没兑现」的长期账本等一期读数后再定。 |
| **B1–B5 页面重构 + 人话化 + 美化** | #183 | `de1a9cd9` | `src/web/templates/episodic_memory.html`（顶部「AI 记忆健康」卡替换「记忆校正质量」卡；例外队列面板；行/抽屉动作重排；维护折叠区；隐私行；时间线分组；能力探测回退）· `src/web/i18n_packs/episodic_page.py`（改 30 键 + 新 70 键双语）· `src/utils/memory_review.py`（`timeline_group`：服务端人话分组，i18n 门禁不许脚本内 CJK）· `src/utils/episodic_memory_store.py`（`_row_to_dict` 加 `group`）· `ui-build.txt` 20260905-1756 · tests +2 | Node 语法核对 OK；模板三门禁 + i18n 覆盖/渲染 + 记忆回归 **445 passed**；最终 §7 集 **320 passed**；`--memory-extract` 前后同为 1.0 / 0 | **B1**：KPI 五格（今日需处理 M·点开队列 / AI 已记住 N·覆盖 U·近 7 天新增 X·已确认 S / 有 D 条因无法核对原话未记录·悬停原因分布 / 近 7 天回复用上 R 轮 / 抽检准确率·样本 <5 显 —）+ 漏斗一行；客户卡红点＝`review_reason`；抽屉时间线 basic/family/pref/event/other + 来源原话 + 用过 N 次·上次用在；空态改人话；首次进入自动加载（原有）。**B2**：队列卡按原因给「为什么要我看」+ 类型化动作（冲突并列「保留这条」→ `resolve-conflict`；高影响 确认/不再使用；低置信「像是这样吗？」；人设的事「是客户的事」；承诺「记住这个约定」）；处理完卡片即消；有待办自动展开；批量只对同类（抽屉按原因一键），全局「全部确认属实」已去。**B3**：词汇全换（见 commit）；删向量列；运维件收「🛠 维护」折叠区（master/admin）；「不再使用」软删默认，彻底删除进「⋯」二级确认；已不再使用视图 + 恢复。**B4**：色点语义（绿/琥珀/红/灰，沿用页内变量）；隐私行；桌面窄栏沿用页内既有 media 规则；虚拟滚动**未做**（沿用「加载更多」分页，≥200 条场景建议二期）。**B5**：健康卡数据全部来自 A 组字段（`/summary.{total_count,total_users,stable_count,new_count,review,grounding_drops,recall}` + `/correction-stats`）。**未做/交办**：左右两栏「客户列表 ⇄ 时间线」双栏布局未做——现有「按人分组卡片 + 抽屉」承担同一信息架构（卡片＝客户列表、抽屉＝时间线），双栏重排等一期读数后再定；「来源原话 → 定位到那条消息」需 `unified_inbox.html` 支持（J-4，现为「在收件箱打开会话」到会话级）；每客户「导出 / 清空」未加（抽屉「复制记忆键」+ 维护区批量删除覆盖，清空属危险操作不入日常层）；zh_hant 未再生（缺键回落简体，值守统一跑 `python scripts/i18n_hant.py generate`）；`help_terms.py` 的 nav 说明仍是「情景记忆」措辞（不在本单文件边界）。 |

## 值守：装 1.0.74 后的读数口径（skuio 机 A1 修复前后对照）
- **修前基线（88MP86，09-04 17:09 → 09-05 02:40）**：`Select-String "\[episodic\] extract done"` 992 行，其中 `llm_count=0` 960 行（96.8%）；`接地护栏丢弃` 96 次共 151 条（全部英文客户中文事实）。
- **修后同口径**：① `llm_count>0` 占比（预期显著上升）；② 新增字段 `grounding_dropped=` 分布；③ `GET /api/episodic-memory/summary` → `grounding_drops.by_reason`（`no_evidence` 高＝模型没按新格式给引文，`evidence_mismatch` 高＝模型编引文/抄助手，`fact_unanchored`＝同语种旧判据仍在咬）；④ `review.pending` 日均（目标 <5，超了先调 `memory_review` 词表）；⑤ `recall.injections` 与 `consolidate … auto_promoted=` 日志（自动转正比例）。
- 两周读数后再定二期（见下）。

## 二期建议（一期读数两周后再定）
1. 承诺持久化：`outbound_promise_guard` 命中 + 客户承诺类事实 → 独立账本（答应了什么 / 何时 / 兑现否），抽屉「承诺过」从只显示 TTL 内悬置升为完整层。
2. 实体关系（第三方人物：邻居 / 前夫 / 同事）与 `valid_from/valid_to` 时间有效期——#177/#171/#182 共同根的完整解。
3. 例外队列自动降噪：`high_impact` 词表按两周 `review.by_reason` 读数收窄；`low_confidence` 改读 LLM 数值置信（prompt 加 `confidence` 字段，解析已预留 `confidence` 入参）。
4. 页面双栏（客户列表 ⇄ 时间线）+ 虚拟滚动（≥200 条）+ 每客户导出。
5. `inferred_counts` / `profile_summary.pending_inferred` 口径统一到 `review_reason`（现为 R14/R15 旧钉，B 组 KPI 已改读 `review`）。
6. 抽取评测加 LLM 实跑基线（`--extract-llm` + `memory_extract_samples_xlang.yaml`），把「英文客户入库量」做成周批趋势。

## 不做（防越界）
- 不引入 Mem0 / Zep / Letta（D8）；不做实体关系图 / 完整「AI 自身经历」层 / `valid_from/valid_to`（二期，等一期读数两周）。
- 不改 `_apply_crisis_safety_net` / `persona_guard` / 危机逻辑。

## 交 J-4（草稿旁记忆 chips · 后端已备好，前端落点在 `unified_inbox.html`，本单不碰）
- **接口**：`GET /api/episodic-memory/used?conversation_id=<platform:acct:chat>&limit=1`（`limit`＝最近几轮；也可 `memory_key=`）。
- **返回体**：`{ok, count, batches: [{batch_id, ts(epoch 秒), chain: "inbox"|"direct", inbound_msg_id, conversation_id, items: [{id, content, source: user_stated|ai_inferred, tier: raw|stable|stale, review_reason, impact: high|normal, source_quote, source_ts, status, recall_count, last_recalled_ts, memory_key, …} | {id, deleted: true}]}]}`——最新批在前，`items` 顺序＝当轮 prompt 里的 bullet 顺序；`count == 0` ＝这轮没用记忆（不要渲染空 chips 条）。
- **动作**：「一键不再使用」→ `POST /api/episodic-memory/{id}/ignore`（软删，可在维护区恢复 `POST {id}/restore`）；「确认属实」→ `POST {id}/confirm`；冲突 → `POST /api/episodic-memory/resolve-conflict {keep_id}`。
- **建议渲染位置**：草稿卡底部（发送按钮上方）一行 chips「本轮用了 N 条记忆 ▾」，展开列 `content`（`impact=high` 红点、`source=ai_inferred` 琥珀点、`review_reason` 非空加「需要你看」小标），每条右侧「不再使用」；取数时机＝草稿生成完成后（`record_recall` 在生成前注入点写入，草稿到手时批次已在）。取 `batches[0]`，`inbound_msg_id` 可与草稿所回复的入站消息 id 对账（同一轮）。
- **自证**：AI 记忆页 → 打开任一客户档案抽屉 → 顶部「最近被用」分区（同一接口按 `memory_key`）。
