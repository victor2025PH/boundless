# 发版对账 v1.0.74 — K-5（值守：①装载 → ②全量回归 → ③发版 1.0.74 + 装机 → ④台账写入 → ⑤回访 + 真机验收）

> 指令：`docs/指令_K-5_值守装载发版回访_2026-09-05.md`。总账：`docs/K批_进度总账.md` K-5 行。
> 非代码；不改 `src/**`；台账只经 `/api/admin/bug-intake/*` 端点或 `tools/duty_*`；重启前必跑 `restart_preflight.ps1`。
> 五段硬依赖：① 等 K-1 提交 → ② 等 K-1/K-2/K-3 收工 → ③ 发版装机 → ④ 两机装上 1.0.74 之后标 fixed（K-D2）→ ⑤ 回访 + 真机验收。

## 开工前置状态（2026-09-05 19:30 盘点）

| 项 | 实况 |
|---|---|
| zhiliao 末次重启 | 2026-09-04 19:38:27（`last_restart_zhiliao.json`，reason=watchdog_start）；当时 engines/chengjie 最新 commit `680ddf81`（09-04 13:45） |
| 重启以来 engines/chengjie 提交数 | 59 条（19:30 计，含 J-1…J-10 全部 + I 批部分 + K-1 A1 `82ee11a5` + K-3 D `88c442f9`）——全部**未装载** |
| WA 边车（:8790） | node pid 16372，09-04 19:36:15 起，跑的是 `D:\workspace\boundless\...\server.js`（与 `D:\boundless` 同一工作树：HEAD/脏文件数一致）；`/health` 现只有 `{ok, svc, caps}`，**无** `forbidden/reason_10m/bad_mac`（J-6 未装载，符合预期） |
| 边车重启方式 | 计划任务 `\WhatsApp-Baileys-Service` → `autostart-run.ps1 -DataDir D:\chengjie-instances\zhiliao\data` → `start.ps1`（注入 `AITR_DATA_DIR`，token/媒体目录才对）。**不得裸跑 start.ps1**（会读仓库 config → 入站 401 静默丢）。杀进程只杀 cmdline 含 `whatsapp-baileys\server.js` 的 node，照 `watchdog_wa_baileys.ps1::Invoke-HardRestart` |
| 重启前基线 | `GET /api/kb/health` → **404**；`GET /api/goals/engine-status` → **404**（J-9 / J-2 `.py` 未装载）；`GET /api/admin/bug-intake` → 200，但 stats **无** `observe_dup / verify_ambiguous / reply_continue` 三键（J-5 未装载，符合预期） |
| K-1 | 意向板 19:30 登记「K-1 commit orphan hunks」；A1 `82ee11a5` 已提交；A2/A3/A4/B/C/D 在途；`发版对账_v1.0.74_K1.md` 尚未出 |
| K-2 / K-3 / K-4 | 意向板均已登记，进行中；K-3 D `88c442f9` 已提交；三份落点表均未出 |
| 他线 ACTIVE | J-10 三期（`skill_manager.py` HOT-ZONE，`33a0571a` 已提交一件）；installer brand round2（`desktop/build/**`、`package.json` 有未提交改动）——③ 段 bump 前须静默 ≥30 分钟 |
| 台账 | 193 单；`/status` 端点标 fixed 会**逐单自动群内 @报障人**（带 ✅/❌ 键盘）——④ 段一次标 ~45 单等于逐单轰炸，与 ⑤ 汇总回访口径冲突，见 ④ 段决策点 |

## 第①段 —— 装载 ✅（2026-09-05 20:50–20:55）

| 步 | 结果 |
|---|---|
| K-1 提交核对 | 六个 commit 全在 HEAD：A1 `82ee11a5` / A2 `71355568` / A3 `c468483a` / A4 `69e62aba` / B `124dd921` / C `35b224c4`；`发版对账_v1.0.74_K1.md` 20:3x 出，§7 明写「K-5 ①段现在可以重启装载」 |
| 剩余 dirty `.py` 处置 | 33 个脏 `.py` 预检 `ast` 全过（0 语法错）。按 mtime 切两组：**「old」组**（09-04 13:27–14:04，孤儿线 21 文件 + 打包 staged 产物）在 09-04 19:38 那次重启**早已装载**，本次不引入新风险，按 K-D1 不提交不回滚；**「new」组**（会被本次新装载的未提交件）：`admin.py` / `i18n_packs/errors.py` / `zh_hant_auto.py`（13:3x–13:45，K-1 §4.2 归 孤儿线 1 hunk / J-8 两键 / regen 产物）、`skill_manager.py`（19:45，J-10 三期 3 个在途 hunk，默认关的加法特性）、`assistant_pair_routes.py`（20:07，assistant-ball 线，diff 只是版本串 + 标题 SVG，自洽）。**无「语法自检失败」文件，未做任何 checkout 回滚** |
| restart_preflight | 20:38 首跑 NO-GO（`config.example.yaml` 3 分钟前 K-1 刚提交）；20:44 NO-GO（同上 9.7 min + K-2 正在改 `persona_studio.py`）；**20:50:43 GO**（五项全绿：active 静默 / 33 py 语法 0 错 / app 装配 4 passed / Advise 正常 / 冷却 1500 min）。advisory：在 22:30 批处理窗之外——按指令「K-1 提交后即可」执行 |
| 重启 zhiliao | `restart_instance.ps1 -Instance zhiliao` 20:50:56 stop → start，`/login` **20s 就绪**（SLA 180s）；soft-stop 拒绝后 hard-stop（headless 常态）；冷却记录 `last_restart_zhiliao.json` ts=20:51:09 reason=restart_instance；ops alert 已发。**HEAD=`b513ded9`**；装载范围 `680ddf81..HEAD` = **81 条** engines/chengjie 提交（I 批 09-04 下午 + J-1…J-10 + K-1 全部 + K-2/K-3/K-4 截至 20:50 的提交） |
| 重启 WA 边车 | 20:51:34 杀旧 node pid 16372（09-04 19:36 起，cmdline 核对含 `whatsapp-baileys\server.js`）→ `schtasks /End` + `/Run "WhatsApp-Baileys-Service"`（经 `autostart-run.ps1` 注入 `AITR_DATA_DIR`）→ 新 pid 30240；2 个 WA 账号（639270135480 calixa_lopez / 639649321471 Wisley）~2 分钟内重新上线 |

**装载后自检（指令 §2 步 4 六项）：**

| # | 项 | 结果 |
|---|---|---|
| 1 | `GET /api/kb/health`（J-9） | ✅ 200（重启前 404）：`entries_total=105, entries_user=4, entries_vendor=77, entries_system=24, embedded=1, vendor_excluded=false`。**注**：zhiliao 库厂商条目实为 77 不是 ≈110（110 是 skuio 机首装量）；`vendor_excluded=false` 是服务器部署预期。未点一键清空 |
| 2 | WA 边车 `/health`（J-6） | ✅ 见 `stability.forbidden=0 / forbidden_rounds_max=2 / reason_10m{dns,net,server,forbidden,logged_out,restart,other} / dns_retry{3000ms×10} / bad_mac{threshold=3,cooldown_min=30}` 三段全在（重启前只有 `{ok,svc,caps}`） |
| 3 | 启动日志 sprint 行（J-2 A2）/ `[goal-inject]`（J-2 A1） | ✅ `20:51:07 ✅ 冲刺推进器已常备（goals.sprint.enabled=True，platforms=telegram,whatsapp,line，natural_daily=True）` + 一条 WARNING「platforms 未显式配置按出厂默认」（信息性）。`[goal-inject]` 行**要等有目标的会话来一条入站**才会出现，20:55 前尚无（见 ⑤ 5b #166 继续盯）。另见 `20:52:08 危机升级桥已装载`（J-8 A） |
| 4 | `GET /api/goals/engine-status`（J-2） | ✅ 200（重启前 404）：`sprint_enabled=true, sprint_dry_run=false, sprint_platforms=[telegram,whatsapp,line], care_enabled=true, care_dry_run=false, sprint_blockers=[], sprint_effective=true, natural_auto_effective=true`——D1b 全链有效 |
| 5 | `GET /api/admin/bug-intake` stats（J-5） | ✅ `observe_dup / verify_ambiguous / reply_continue` 三键出现（重启前无）；`pending_notify=61` |
| 6 | `python tools/bug_intake_report.py --days 1` | ✅ 跑通：近 1 天工单 29（P2 25 / P1 4；new 18 / confirmed 9 / closed 2），fixed 家族 2、已送达回访 0 |
| + | 重启后 app.log（20:50:56–20:55） | 195 行，**0 ERROR / 0 Traceback**；WARNING 只有 avatar_voice 令牌 warn-once ×3 / health_watchdog ×3 / contacts.bootstrap ×2。boot err.log 0 字节 |
| + | `GET /api/crisis-events`（J-8） | 200，但 zhiliao 上 `state=audit_off, audit_on=false, escalation_on=false`——D7 出厂开只写进 `cloud_light.yaml` / `config.desktop.internal.yaml` 两个 profile，zhiliao 自己的 overlay 没这两键。**非缺陷、非 K-5 决定**：zhiliao 要不要开留痕/升级交老板 / J-8（一键开启按钮在页面上） |

未装载 / 顺延：`tools/xlate_memory_purge.py`（skuio 机，等装机 ③）；AGENTS v1.3 重发（③）；`python scripts/i18n_hant.py generate` 繁体再生（③ 发版前，K-1 §4.2 交 K-5）。

## 第②段 —— 全量回归 + 门禁（2026-09-05 21:06–22:1x）

前置：总账 K-1（20:3x）/ K-2（21:0x）/ K-3（20:40）/ K-4（20:1x）四行均「已完成」后开跑。HEAD `b513ded9`（K-2 F `547c3c8a` 之后无新 engines 提交）。

### 全量 pytest

`python -m pytest tests/ -n auto -q --timeout=90 --timeout-method=thread` → **68 failed / 24993 passed / 36 skipped，49 分 52 秒**（输出 `engines/chengjie/tmp/k5_pytest_full.txt`）。

68 个红的归类方法：① 68 个 id 在当前树**串行重跑**（排除 xdist 抖动）→ 66 稳定红 + 2 抖动；② 同一批 id 在两个干净 worktree 重跑：`680ddf81`（上次重启 HEAD，整批之前）红 **56**，`1e8a8010`（K 批开工前）红 **63**。三点相减：

| 类 | 数 | 明细 | 处置 |
|---|---|---|---|
| 抖动（全量红、串行绿） | 2 | `test_voice_pipeline_integration.py::TestVoiceReplyLangRoute::{test_edge_voice_follows_text_language, test_route_disabled_keeps_original_voice}` | 不算红 |
| **预存红**（`680ddf81` 就红） | **56** | 全部 68 中减去下面 10 个（含 J 总账记过的 `test_workspace_sidebar` ×5 渠道中心融合线 / `test_stale_update_prompt` ×3 I-5 app.html / `test_template_bare_fetch_ratchet::not_above_ceiling` / `test_unified_inbox_stage1` ×7 `_fake_send_via(origin)` 签名 / `test_aline_disclosure_wiring` ×4 `_presend_blocked(peer)` / `test_business_line_ceiling` ×3 / `test_baseline_reconcile` ×3 / `test_css_token_refs` ×2 / `test_legacy_blue_ratchet` / `test_alert_audience_catalog`（`autosend_gate`,`phantom_unread` 别名无受众）/ `test_takeover_trigger_surface` / `test_silent_exception_ratchet` / `test_openai_client_fastfail_ratchet` / `test_notify_unification` / `test_instance_restart_status` 三个 ps1 无 BOM / … 共 56） | 整批之前的债，**不阻塞发版**；清单在 `tmp/k5_rerun_wt_680.txt` |
| **J 批引入**（`680ddf81` 绿 → `1e8a8010` 红） | **7** | 见下表 | 交回对应 J 线 |
| **K 批引入**（`1e8a8010` 绿 → 现在红） | **3** | 见下表 | 交回对应 K 线 |

**10 个新红逐条（写文件 + 测试名 + 归属线）：**

| # | 测试 | 症状 | 归属（最后改该处的 commit/线） | 建议 |
|---|---|---|---|---|
| K1 | `tests/test_i18n_extra_langs.py::test_extra_lang_overrides_subset_of_zh` | zh_hant override 里 9 个 `psn_stock_*` 键在 ZH 合并视图已不存在 | **K-2 / J-7 A #189 `7c9e39d2`** 删了备货 tab 台词库词条，繁体包未再生 | **K-5 ③ 段** `python -m scripts.i18n_hant generate` 一并解决（K-1 §4.2 已交 K-5） |
| K2 | `tests/test_template_bare_fetch_ratchet.py::test_bare_fetch_ledger_not_stale` | `personas.html` 裸 fetch 实际 0 < 账本 1 | **K-2 / J-7**（personas.html 最后一个裸 fetch 被收掉，正向） | K-2 把账本降到 0（一行测试改动） |
| K3 | `tests/test_watchdog_check_audit.py::test_behavioral_test_coverage_ratchet` | 无真调用测试的巡检 +1（天花板 0）：`_check_phantom_unread` | **K-1 C `35b224c4`**（phantom_unread_alert 接线；原 对话 B 线 `health_watchdog.py`） | K-1 / 对话 B 补一条真调用 `_check_phantom_unread` 的行为测试（J-8 落点表 09-05 14:07 已预告此棘轮） |
| J1 | `tests/test_python_undefined_names.py::test_no_new_undefined_names` | `src/contacts/care_dispatcher.py` L822 `deliver_text(...) -> Dict[str, Any]` 的 `Dict` 未 import | **J-8 B `a4a5ab63`**（K-1 §5 已点名） | J-8 一词修复 `from typing import Dict`。**已核实非运行时隐患**：该文件 L16 有 `from __future__ import annotations`，注解惰性不求值；装载后 `import src.contacts.care_dispatcher` OK、app.log 零 care_dispatcher/NameError 行。只是 lint 门禁红 |
| J2 | `tests/test_care_prompt_context.py::test_build_prompt_with_extras_and_without_is_backward_compatible` | care prompt 的「说话风格」块里丢了人设风格文本（`沉稳大方`） | **J-8 B `a4a5ab63`**（`build_care_prompt` 记忆块相关性过滤改动；另一候选 D1b P0-1..3 `d34666f3`） | J-8 复核 extras 透传 |
| J3 | `tests/test_care_prompt_context.py::test_dispatcher_passes_extras_into_prompt` | 同上（`俏皮活泼`） | 同上 | 同上 |
| J4 | `tests/test_assistant_qa_eval.py::test_retrieval_topk_floor` | 45 条金标 top3 98%<100%：「客户是外国人我打中文行不行」被 `term:nav_crisis_audit` 抢答 | **J-8 A `256e3380`**（危机审计 → 「客户安全预警」人话化：help_terms/nav 词条含「客户」二字过度匹配泛问句） | J-8 给 `nav_crisis_audit` / `page:crisis_audit` 词条收窄 keywords 或加负向词 |
| J5 | `tests/test_assistant_qa_eval.py::test_refusal_rate_floor` | 诚实拒答率 31%<45%：「有没有安卓 App」等 4 条被 `page:crisis_audit` 等抢答 | 同上 | 同上 |
| J6 | `tests/test_input_theme_contrast.py::test_template_inline_styles_no_fallback_vars_resolve` | `care_schedule.html` / `episodic_memory.html` 用了全站未定义的 `var(--t1)` | **J-8 B `a4a5ab63`**（care_schedule）+ **J-10 B `de1a9cd9`**（episodic_memory） | 各自改 `--t` 或补 fallback |
| J7 | `tests/test_i18n_zh_hant.py::test_zh_hant_full_coverage_ratchet` | 繁体覆盖率 98.69% < 99% | 全部 J 线新增简体键、各自刻意不 regen（J-2 A2 / J-10 明写） | **K-5 ③ 段** regen 一并解决 |

**通过标准判定：0 新红未达**——10 个新红里 2 个（K1、J7）由 K-5 ③段 regen 繁体包自行消化；其余 8 个交回 J-8（5：J1–J5）、J-8+J-10（J6）、K-2（K2）、K-1/对话 B（K3）。**均不阻塞发版**（都是门禁 / 评测 / 棘轮 / 主题变量层，无功能性回归；J1 已核实为纯 lint 红）。老板若要「0 新红」再发版：J-8 五条（J1–J5）+ J-10 一条（J6 半边）+ K-2 一条（K2）+ K-1 一条（K3）各是分钟级改动。

### gate_sweep -Full

- `[0/2]` 脏 `.py` 语法预检 warn-only：0 错。
- `[1/2]` 门禁 40 文件：**1 failed / 801 passed（4 分 19 秒）**，红＝`test_python_undefined_names::test_no_new_undefined_names`（上表 J1，J-8）；transient 重跑仍红＝真红；red ledger first-seen 0.0 h（本轮新进）。上次 sweep 记录（12.3 h 前）的两条红 `test_alert_delivery_e2e::phantom_unread`（K-1 C 修）/ `test_copilot_theme_tokens`（J-4 `7b56f894` 修）**均已转绿**。
- `-Full` 后半（`scripts\regression.ps1` 整套回归，生产在线 → BelowNormal + 并行度封顶 4）22:1x 起在后台跑，只为把 verdict 写进共享 `.ops` 记录；结论以上面 `-n auto` 那次全量为准（同一 HEAD、同一工作树）。输出 `engines/chengjie/tmp/k5_gate_sweep.txt`。

### 值守症状族对账单（已更新 `docs/值守症状族对账单.md`）

族 2 出站切语言：第 5 层（#154）状态改「已修（1.0.72）未回访」+ **新第 6 层** #176 翻译记忆缓存引擎废话（对外口径改 6/6）；族 3 幽灵未读：第 3 层加 K-1 A4 UI 半边 + **新第 4 层** #170 归档未读入口 + 值守告警行（phantom_unread_alert）；族 4 静默失败：第 8/9/10 层改「已装载」、第 11 层（#164）填 J-4 C + 贴纸同口径、**新第 12–15 层**（#166 目标引擎兜售 / #168 链卡无解释 / #172 LINE 视频误判过期 / #180 灰按钮无因）；**新立族 6：LINE 媒体收发失败**六层（#101 / #172 / #169 / 入站 32MB / #180 / #145② 等证据），对外口径「修到 5/6，#145② 暂不说已修复」。族 1 第 5 层（#161）状态改 1.0.72 已装。

## 第③段 —— 发版 1.0.74 + 装机（**步 1–7 完成，步 8 等两机**；第二轮对话 09-05 22:56 – 09-06 00:3x）

老板三个拍板（22:5x，AskQuestion）：**K-D1 孤儿线允许进包**（工作树原样打包）/ **④ 段走 A 案** / **播报文案照发**（发布完成后立即）。

| 步 | 状态 | 事实 |
|---|---|---|
| 1 合并落点表 → `docs/发版对账_v1.0.74.md` | ✅（上轮） | 17 行 + 36 单一句话 + 发布 8 步 |
| 2 版本 bump | ✅ | installer brand 线 23:04:19 提交 `75cd4a65` 并清意向板，23:34 静默满 30 min 后动手。`package.json` **1.0.72 → 1.0.74 / 1.074**（HEAD 原 1.0.72；工作树 1.0.73 是 09-04 发版时从未提交的孤儿 hunk，本次一并钉真值）。繁体 regen `python -m scripts.i18n_hant generate` → 16160 键；`test_i18n_zh_hant` / `test_i18n_extra_langs` / `test_i18n_coverage` **335 passed**（②段两红消化） |
| 3 打包 | ✅ | `npm run dist:win:smart` → `dist\ChatX-Setup-1.0.74.exe` **563.0 MB**（rc=0 23:49:03，后端 PyInstaller 全量重建）。`npm run dist:win:clean` **首跑 rc=1**：`smoke_clean.py`「首启零数据」断言把 J-9 A 新播的 3 条停用格式示例（`source='system'`，`template_key` 空）当业务泄漏。修 `desktop/build/smoke_clean.py` 口径（`template_key` 空 **且** `source≠system` 才算泄漏；无 source 列的旧库仍按 user 算）→ `1822bbcf`；重打 rc=0 23:58:36 → `dist-clean\ChatX-Setup-1.0.74.exe` **495 MB（519,130,117 B）sha256 `25e580d68bb20596e4f6067cfa41fd5edc7d279410aff08ee471c2488d61cba5`**，`✓ 安装版后端冒烟全过 / ✓ 干净包产物冒烟全过（首启零数据）`。**交 J-9 知悉**：`smoke_internal` 改了、`smoke_clean` 漏了 |
| release commit | ✅ | `d1e207f0 release(chatx): v1.0.74 发版事实钉真值`——`package.json` + `zh_hant_auto.py` + `shell-i18n-ext.zh_hant.js` + `website/lib/chatxContent.ts`（五值 1.0.74 / 495 MB / 文件名 / url / sha256）+ `website/lib/chatx-release-notes.json`（`gen-chatx-changelog.mjs add --version 1.0.74 --date 2026-09-06 --tags fix,improve`，中英各 **14 条**要点，源文件 `engines/chengjie/tmp/_notes_1074{,_en}.txt`） |
| 4 smoke | ✅ | 随步 5 在三台目标机上跑 `smoke_chatx_node.ps1`（bundled backend 起在临时数据目录）：yunsheng ready 6.1s、kouxing / lianbei 同款全 OK（workbench / me / setup / media-capabilities / features / diag probe / vision active / pacing ok） |
| 5 坐席三台 | ✅ | 三台 SSH 可达、后端均 DOWN（深夜无人用，装机不打扰）。`push_chatx.ps1 -Install -Relaunch -Smoke`：yunsheng 00:05:26 OK（1.0.73→1.0.74）/ kouxing 00:18:14 OK（1.0.72→1.0.74）/ lianbei 00:20:15 OK（1.0.72→1.0.74）。`chatx_fleet_status` 00:21:47：**三台 UP / chengjie 1.074**；kouxing 后端已跑过 85s FD 崩溃窗口（`680ddf81` 修）。装的是 `dist\` 内测包（与 1.0.73 同做法）。注：首次用 `Start-Job` 并行推两台随会话丢失（无 scp 在途、版本未变），改顺序后台推成功 |
| 6 publish + 官网 | ✅ | `publish_chatx.ps1 -DistDir dist-clean -DryRun` → `build valid sha512 matches latest.yml`；`-Yes -Notes "1.0.74：…"`（应用内公告 feed release-v1.0.74）00:10:15 完成：`exe verified on VPS (sha512 matches)` / `pointers uploaded + pm2 restarted` / `R2 mirror synced (r2:avatarhub/downloads)` / `public verified: latest.yml=1.0.74 manifest=1.0.74 exe=HTTP206` / `announcements.json carries release-v1.0.74` / `pruned 1.0.70 (local+VPS+R2)`。官网 `website/scripts/deploy.ps1 -SkipAssets` 00:13:33 `部署完成 [OK]`（公网体检 healthy） |
| 7 播报 | ✅ | `duty_update_hint.py --set "1.0.74 已发布：看到「立即更新」提示点一下即可；更新后请按各单验收口径帮忙复验"`（生产 overlay，热重载）。**报障群** bot 链（`tmp/tmp_k5_broadcast.py` → `bug_bot.send_group_html`，文案＝过目稿逐字）00:1x **message_id=1203**。**频道 @hykj7**（对外，只发频道，群 @hykjz 自动转发；与报障群不是同一个群）经 VPS 本机 `POST /api/admin/broadcast target=channel`（key 取 VPS `.env.local`）**messageId=150**，文案对外版 9 行要点 + 官网按钮 |
| 8 等两机上报 1.0.74 | ⏳ | `duty_evidence.py probe 188`：钧机 fp `B990-0C62-FC1C-F668` **1.0.73**，最近活跃 09-04 16:24Z；`probe 193`：skuio 单无机器码（指令给的 fp 尾号 DC8F，按客户端日志探）。00:3x 两人未开软件，自动更新要等他们下次打开。**④ 段按 K-D2 等两机都上报 1.0.74** |

装机后值守动作（③ 步 6 / J 批交办，**均等两机装上后做**）：skuio 机 `xlate_memory_purge --apply`；AGENTS_JUN / AGENTS_SKUIO v1.3 重发两机并明说覆盖。

## 第④段 —— 台账写入（**已备好，等两机上报 1.0.74 后 `--apply`**）

老板已拍 **A 案**。工具：`tools/duty_ledger_fix.py --mark-fixed-file`（`60e02aea` + `33f6c844`，`tests/test_duty_ledger_fix.py` 11 passed）——与 `/status` 路由同写 `status / fix_note / updated_ts`，`notify_ts` 保持 0（进汇总回访池），落 `ledger_fix` 事件，只动 `new / confirmed`（幂等）；#145 另带 `body_append` 写入 K-4 五行表（K-D6 编号）。

- 计划文件：`engines/chengjie/tmp/k5_mark_fixed_1074.json`（**49 单**：去向表 35 + #32 + K-4 六单 #61 #63 #64 #67 #36 #145 + K-2 六单 #186 #189–#193；#170 按 K-D5；#67 fix_note 含「请打开人设工作室 → 相册，把它们重传一次」；#181 按指令定稿句）。
- dry-run 已跑（00:2x）：**49 项全部 `→ fixed`，0 跳过、0 缺单**，全文 `tmp/k5_mark_fixed_1074_dryrun.txt`（老板过目稿）。
- 执行命令（两机都上报 1.0.74 之后）：
  ```
  cd D:\boundless\engines\chengjie
  python tools\duty_ledger_fix.py --data-root D:\chengjie-instances\zhiliao\data --mark-fixed-file tmp\k5_mark_fixed_1074.json --apply
  python tools\duty_ledger_fix.py --data-root D:\chengjie-instances\zhiliao\data --count-fixed-unnotified     # 应 61 → 110
  python tools\bug_intake_report.py --days 30 --db D:\chengjie-instances\zhiliao\data\config\bug_intake.db     # 发版后基线，存本表
  ```
- 之后 ⑤ 5a：`duty_notify_summary.py` 两份汇总（skuio 47+33 / 钧 13+16 / 其他 1），文案先贴本表。

**原决策点记录（已由老板拍 A 案）**：指令写「逐单 POST `/status` fixed」，但该端点标 fixed 会当场逐单发群内回访（`bug_intake_routes.py` L255–264，bot 链带验证键盘）。本批 ~45 单一次标完 = skuio 收 ~30 条、钧收 ~10 条连发，正是 I-6 §2 说的「轰炸」。两个走法：
- **A（推荐）**：标 fixed 走**不触发回访**的路（`tools/duty_ledger_fix.py` 同款直写，J-5 E 已用过、红线允许的命名工具；需给它加一个 `--mark-fixed id:note` 模式，`tools/duty_*` 归 K-5），notify_ts 保持 0 → ⑤ 段两份汇总回访一次盖掉 61 + 本批，再 `duty_notify_summary --apply` 记账。与指令 ④「count-fixed-unnotified 从 61 涨到 61+本批」的口径**一致**。
- **B**：照字面逐单 POST `/status`，接受逐单 @（每条带 ✅/❌ 按钮，J-5 B verify 链直接可用），⑤ 段汇总只盖旧 61 单。

## 第⑤段 —— 回访 + 真机验收

（等 ④）

## 附 A：去向表 35 张单 + K-1/K-D5 单的文案草稿（③ 公告一句话 ≤20 字 / ④ fix_note ≤300 字人话，不带 commit 号）

> 从各落点表「备注」抄改。**老板过目后**才进 `tmp/tmp_mark_fixed_1074.py`。报障人按台账 19:4x 实值。fix_note 末尾统一不写「请更新」（`update_hint` 单独字段带）。

| 单 | 报障人 | 一句话公告 | fix_note 草稿 | 来源 |
|---|---|---|---|---|
| #37 | skuio | AI 自动回复会引用对方那条 | AI 自动链回复现在会像人一样「引用」客户的消息：客户连发两条以上、且回复明显对应其中某一条时，才带引用（单条不引用、对不上不引用）；只在协议号路径生效，引用发不出去会自动去引用重发一次，不会卡住 | I-4 D2 |
| #38 | 钧 | 摸底目标一轮只问一个空槽 | 摸底类目标（收集客户信息）改成硬约束：每轮只问一个还没填的槽位，问过没答的会轮换，不再一次列两项像查户口；AI 从对话里归纳出的画像值要过「原话里真有」的核对，核不出的标「待确认」不当事实 | I-3 C2/C3 |
| #40 | 钧 | 工作目标接线 + 台湾/香港语气档 | 两半：① 工作目标推进已接进主链并按节奏档执行（见 #65）；② 人设居住地或会话「发→繁体/粤语」为台湾/香港时，AI 口吻自动切台/港档（语气词、用词、繁体字形），大陆档一字不变 | I-3 / I-4 D1 |
| #65 | 钧 | 限时目标按真实出手拍算进度 | 3 小时这类限时目标此前被当成「按天」目标、进度只按时钟走，所以聊 16 分钟只有 9%。现在建目标时按时长自动落「本场/今日」节奏档，进度按 AI 真正出手推进的次数爬，且每条入站都有时刻锚点，不再整段空转 | I-3 C1 |
| #152 | skuio | AI 复读机化会被截断不发 | 两件：① AI 一句话里开始机械重复自己时，出站前就被截断，截空就不发，不再回落原文；② 工作目标没按计划推进时，目标卡英雄行会亮出「上轮未按目标推进：原因」，看得见为什么 | I-2 F1/F2 |
| #153 | skuio | 目标 3 天不推进：同 #152②/#65 | 与 #152② / #65 同一件：目标注入的判定现在落日志、目标卡显示原因；限时目标按出手拍算进度并有入站时刻锚点。装 1.0.74 后再看那条 WhatsApp 会话的目标卡是否有原因行 | I-3 C1 |
| #154 | skuio | 「发→X」铆定不再被悄悄清掉 | 会话设了「发→英文」就永远发英文，对方换日语也不动：铆定不再被程序清空（删除必须显式），铆定缺位时按本会话我方历史主语种保守回落；坐席看到的「我方消息对照行」按会话「收→」语言显示，不跟着对方漂 | I-2 A1-c/d |
| #155 | skuio | 「爱称」两格挪到客户信息面板 | 「你称呼对方 / 对方称呼你」描述的是这段客户关系而不是人设本身，已从人设档案挪到按联系人单独存；旧值自动迁移、生效爱称逐字不变；人设档案里两格与说明条已删（说明条见 #178） | I-2 B3-155 + K-1 A4 |
| #156 | skuio | 新登录账号不再自动套默认人设 | 新登录账号不再被自动绑默认人设：账号栏出「未选人设」引导，未选期间 AI 只拟稿不代发（人审）。身份和接管方式都必须由人显式决定——与 #63 / #167 同一结论 | I-2 B3-156 + J-2 C |
| #158 | skuio | 审批页有「← 返回聊天」 | 草稿审批页顶部多了「← 返回聊天」，点顶栏「聊天」也能回到收件箱（之前桌面版点了没反应只能关窗）。壳层三处修：webview 漂走自动导航回家、辅助窗交接后收窗、页级原生返回链接 | I-2 A2 |
| #159 | skuio | 账号栏未读数字＝点得开的条数 | 账号栏未读数字与清单不一致的三道口径差（删除墓碑 / 消息全被撤回 / 手机端未读残值）已收成同一份规则；数字可点，直达那 N 条并可一键清零，点进去的条数与数字恒等——对不上就是新证据 | I-2 B2 + K-1 A4 |
| #160 | skuio | 全自动不再因敏感词停手 | 「AI 不再因为客户提到敏感词就停手。全自动就是全自动，触发到风险只做后台记录，一个月后用真实数据再定规则。」子串误伤（sexy / bank holiday）已整词化；「填入并发送」的 guard-check 同样改影子档只记不拦。**对外只说这个口径** | I-1 + K-1 A1 |
| #161 | 钧 | 日语/韩语不再走克隆声 | 日语克隆念错却被当「有声」放行的根因已修：能力表只登记实测念对的语种（ja/ko 默认走标准声并在语音卡明示）；终审补「疑似念错」档，命中就地改派标准声重合成，绝不发乱音；文案分「疑似无声 / 疑似念错」 | B 批 B1 |
| #162 | 钧 | 入站日志带账号/会话 id | 观测补丁：入站日志补账号与会话 id，档位改动日志补来源——两号同聊同一人时可以从日志分清是哪个号、哪条链在动 | I-1 D |
| #164 | 钧 | 发图无响应显黄字「结果未知」不再红字 | 发图后后端没响应时不再红字「发送失败」误导你重发（造成客户收两条），改黄字「结果未知」并自动刷消息流让你亲眼确认；只有后端明确失败才红字。手机端一条＝之前就发成功了 | J-4 C |
| #166 | skuio | 目标注入判定可见 + 引擎真相口径 | 目标为什么没执行现在看得见：每条入站后日志必出「注入成功 / 未注入：原因 / 出错」三者之一；右栏目标卡按引擎真实状态显示——不能出手时灰掉并点名原因（哪道闸拦的），不再兜售「在推进」；限时冲刺出厂开，停摆有看门狗 | J-2 A1/A2 + D1b |
| #167 | skuio | 首登账号先进人审不再全自动 | 新登录账号在你确认之前一律先进人审（AI 拟稿不代发），补上「首登竞态 / 接入时刻未知 / 未确认默认人设」三条漏网；存量已在跑的号不受影响。与 #63 / #156 同一结论 | J-2 C |
| #168 | skuio | SOP/链卡有解释行和失败原因 | 工作链卡片改三态并给人话解释：运行中·下一步几点做什么 / 已让路暂停（客户几点回话、静默多久后续跑）/ 失败（原因）；步骤重试与失败原因进日志与卡片；SOP 推进步拟稿钉住人设第一人称口吻 | J-2 B + J-4 |
| #169 | skuio | LINE 视频上限 25MB → 100MB | LINE 发视频上限从 25MB 放到 100MB（按平台分别设：TG 200 / WA 64），真机 30/60/100MB 三档全部送达；60MB 之前失败是上传超时按体积放宽后解决。收 LINE 长视频的入站上限也从 20MB 提到 32MB | J-3 C + K-3 D |
| #170 | skuio | 归档里的未读有入口可清 | 复核结论：那几条未读是你归档后再没新消息的会话，不是缺陷——之前只是没有入口。现在紧凑条常驻「归档中还有 N 条未读」，点进去可「取消归档这 N 条」，账号角标可点直达并「全部标已读」 | J-4 A（K-D5） |
| #171 | skuio | AI 不再谎称「已发照片」 | AI 说「已发过 / 刚发了」之前要对照近 30 分钟真发账本：没真发过的「已发声明」会被改写成如实表达或补发；ops 卡加「已发假声明」检出/补发/撤回三数 | J-1 A + K-3 C |
| #172 | skuio | LINE 视频不再 4 分钟判过期 | 根因不是 LINE 保留期（实测 7 天都在），是对方开了 Letter Sealing 的加密媒体我们没走对路径、没解密。已补齐：收到即预取，404 不再当过期；「重试」按钮悬停显示具体原因，真过期才提示「请客户重发」 | J-3 A + J-4 |
| #173 | skuio | 批量挂链按钮能弹出输入层 | 桌面壳里原生输入框直接报错导致按钮「没反应」；改为页内输入层，批量挂链、成交金额、降阶原因三处都改了，并加了按钮说明提示 | J-4 B |
| #174 | skuio | 电话默认掩码，点一下看全号 | WhatsApp 客户卡电话默认掩码保护，点一下揭示全号、再点收起，旁边 ⧉ 一键复制全号（老板决策：点击揭示） | J-4 F |
| #175 | skuio | 陪聊人设不再漏客服/销售话术 | 出站守卫新增「服务框架」剥离：「my assistant will reach out / 开户资料 / 引导页 / 团队会联系你」这类客服腔一律拦下，整段都是就换成第一人称中性表达；SOP 推进步的拟稿源头也钉了人设口吻 | J-1 B + J-2 B |
| #176 | skuio | 坏译文不再从翻译记忆里复读 | 「好的，请发送需要翻译的内容」这类引擎废话曾被存进翻译记忆反复命中；现在命中先验是不是废话，是就删掉重译。**存量坏记忆**：你的机器装 1.0.74 后我们远程跑一次清理（或第一次撞到时自愈） | J-1 C |
| #177 | skuio | 人工接管时你发的话进 AI 记忆 | 人工接管期间坐席替人设说的第一人称事实（如「我有个女儿」）会进 AI 记忆并持久，下一轮 AI 拟稿带「你亲口说过」块不再自相矛盾；人设答应过的事也记进「承诺账本」，AI 记忆页可见 | J-1 D + J-3 接线 + J-10 |
| #178 | skuio | 「双向称呼」说明条已删 | 人设档案里那条常驻说明文字已删（功能已改按客户单独设置，见 #155） | J-4 E |
| #179 | 钧 | 「default —」幽灵行不再出现 | 人设「应用到 → 账号默认」TG 列表里那行「default —」是占位账号漏出，已按「真号才列、占位不列」过滤 | J-4 D |
| #180 | 钧 | 语音按钮灰态说明具体原因 | LINE 语音发不出的红字改成三种具体原因（账号未托管 / 该平台不支持 / 正在重连约 N 秒后重试），按钮悬停可读；重连中的退避跨重启保留 | J-3 B + J-4 |
| #181 | 钧 | WA 扫码慢：DNS 分因 + 配对计时 | 403 终态 + DNS 分类 + Bad MAC 自愈三层已上，配对时长看装机后 pairing_ms。扫码 4–5 分钟最像你机器 DNS 间歇失败反复重连：现在 DNS 失败短退避 3 秒重试不烧预算、配对时长与原因可见；被 WhatsApp 限制的账号不再无限重连刷日志 | J-6 A/B/C（观测项，指令定稿） |
| #182 | skuio | 补一条关怀分「客户的事 / 到点发这句」 | 手动加关怀改两档：「客户的事」（AI 自然关心）/「到点发这句话」（原文直发）；填成祈使句会提示并拦下，预览先回显「我理解为：客户将在几点做什么」再出稿，不再凭空造第三方 | J-8 B |
| #183 | skuio | AI 记忆页重做：待确认变例外队列 | 「待确认」积压改成例外队列：只有冲突 / 高影响 / 低置信 / 人设自身 / 承诺五类才需要你看一眼，普通推断 7 天后被用过自动转正；跨语言抽取漏检根因（英文原话对中文事实的接地判据）已修；页面重做为健康卡五格 + 人话，可导出每客户记忆 | J-10 A/B/二期 |
| #184 | skuio | 厂商预置知识不再参与对客检索 | 首装不再带 110 条厂商产品知识；存量自动打「厂商预置」标签并从对客检索硬排除，知识库页可筛选 / 一键清空；ops 加「知识库接通性」卡：你的机器预期显示「用户条目 0 + 用户知识库为空」——这是对「零贡献」的正面回答：不是检索坏了，是还没有你自己的知识 | J-9 |
| #185 | skuio | 「客户安全预警」默认开 + 会叫人 | AI 一直在识别并兜底，只是之前没有记录、也没叫人。装 1.0.74 后页面改名「客户安全预警」，留痕与人工升级默认开，严重事件会点亮工作台徽标并置顶会话；如仍看到红条「危机留痕未开启」点一键开启即可 | J-8 A |
| #188 | 钧 | 手动发送不再短暂双显 | 显示层竞态：发送中恰逢一轮刷新，服务端已镜像的真气泡与「发送中」乐观气泡同屏。已按文本归一 + 60 秒窗判回执并隐藏乐观稿，失败仍可见可重发 | J-4 #188 |
| #32 | 钧 | 无实际意义的悬浮清单已删 | 业务助手等处那个没有实际意义的悬浮撕窗清单已下线（**待 K-1 A4 提交确认**） | K-1 A4 |

K-4 结论单（#61 #63 #64 #67 #36 #145）与 K-2 单（#186 #189–#193）的文案等各自落点表出来再补。#67 **必须**含「请重传一次相册」。

## 附 B：报障人分组（⑤ 5a 两份汇总用）

- **钧**（16）：#32 #36 #38 #40 #61 #63 #65 #67 #145 #161 #162 #164 #179 #180 #181 #188
- **skuio**（33）：#37 #64 #152 #153 #154 #155 #156 #158 #159 #160 #166 #167 #168 #169 #170 #171 #172 #173 #174 #175 #176 #177 #178 #182 #183 #184 #185 #186 #189 #190 #191 #192 #193

## 断点 / 交接

- 当前做到：**③段步 1–7 完成（1.0.74 已发布、三台坐席已装、群 + 频道已播报），④ 段计划已备好 dry-run 通过**（09-06 00:3x）。gate_sweep -Full 后半整套回归已跑完（verdict RED＝已知 68 红 + 双栏融合验证 6/8，写进共享 `.ops`）。
- 下一对话从 **③ 步 8 / ④** 接：① `python tools\duty_evidence.py --data-root D:\chengjie-instances\zhiliao\data probe 188`（钧）/ 看 skuio 机（fp 尾 DC8F）客户端日志版本——**两机都 1.0.74** 才动 ④；② ④ 按上面命令 `--apply`（dry-run 已过目）；③ ⑤ 5a 两份汇总回访（`duty_notify_summary.py` 先 dry-run 看盖多少单，文案贴本表）→ `--apply` 记账 → `duty_notify_pending.py --alert` 应不再告警；④ ⑤ 5b 真机验收表逐项；⑤ 装机后动作：skuio 机 `xlate_memory_purge --apply`、AGENTS v1.3 重发两机。
- ②段新红交回清单未变（J-8 ×5 / J-10 半条 / K-2 ×1 / K-1 ×1；两条 i18n 红已由 regen 消化）。另一条交回：**J-9** `smoke_clean.py` 口径漏改（本轮已修 `1822bbcf`，知悉即可）。
- 临时产物：`engines/chengjie/tmp/k5_pytest_full.txt` / `k5_failed_ids.txt` / `k5_rerun_failed.txt` / `k5_rerun_wt_680.txt` / `k5_rerun_wt_1e8.txt` / `k5_gate_sweep.txt` / `k5_dist_1074.txt` / `k5_dist_1074_clean2.txt` / `k5_push_{yunsheng,kouxing,lianbei}.txt` / `k5_publish_1074.txt` / `k5_website_deploy.txt` / `_notes_1074{,_en}.txt` / `_broadcast_1074.txt` / `_channel_post_1074.py` / `tmp_k5_broadcast.py` / `k5_mark_fixed_1074.json` / `k5_mark_fixed_1074_dryrun.txt`。
- 意向板：本轮收工 `-Done`；下一对话重新登记。
