# 指令 TikTok 线（TK-1 续做 → TK-2 立项）：Business Messaging 真流量 · Shop CS · huoke 桥

> 底座：`docs/指令_TK-1_TikTok接入_官方通道与地区模型_2026-09-07.md`、`docs/抖音TikTok渠道族_进度总账.md`（TK-1 / TK-2 行）、`docs/实施96_…_2026-09-07.md` §2.3/§4/§5、实施100（T1–T5）、实施99（接入页迁壳）。抖音（DY）线 **09-08 老板拍板冻结**（三道资质门未过，不再投代码）——本指令只管 TikTok；DY 的公共层（`platform_registry` / `channel_policy` / `window_guard`）本线**只追加 `tiktok` 条目、绝不重建**。
> 预算 **$180**（真流量联调 $60 / C 段 Shop CS $60 / E 段 huoke 桥 $40 / 门禁 $20）。落点表 `docs/发版对账_v1.0.80_TK.md`（总账 TK-1 行指向的 `发版对账_v1.0.77_TK1.md` 从未建档，以本表为准）。

## 0. 现状（全部已入库、默认关、无 Node 边车；09-08 晚起无新改动）

| 项 | 状态 | commit |
|---|---|---|
| A 地区模型 `tiktok_regions.py`（EEA 30 国 + CH/GB/US/IN 私信不可用、媒体禁用表、店铺站点） | 已入库 | `eff6b74d` |
| B Business Messaging worker + webhook（`tiktok_official.py`：`/business/message/send/`、媒体上传、HMAC 验签、幂等、48h 超窗、注册地门控、3 MB） | 已入库，`tiktok.enabled` 才注册 | `eff6b74d` |
| 教程 / 接入面板 `/workspace/onboarding/tiktok`（登记 business_id / access_token / 注册地；OAuth 一键授权 + 令牌自刷 + webhook 编程注册 T1–T3；TikTok.me 引流 ref 统计 / webhook 静默检测 / 令牌 7 天预警 / `GET /api/onboarding/tiktok/status` 状态灯 T4–T5） | 已入库 | `e61b69d8` · `3224506d` · `6b7787f6` · `cf69240b` · `a098c072` |
| 注册表 `tiktok.implemented` | `False`（正确：没跑过真流量） | — |
| 测试 | `tests/test_tiktok_*.py`、`test_douyin_e2e_alignment`（含 TikTok 首条禁链）全绿；`platName` 断言 09-10 随微信线 `f00c4d00` 改为「注册表回落仍在」契约（此前红为 unified_inbox.html 改多行所致） | — |
| **待外部** | TikTok for Business 开发者账号 → **Business Messaging 权限获批**；Partner Center 应用（Customer Service 类目）+ 测试店铺 | — |

## 1. 事实

- 停止条件（TK-1 §3 已定）：Messaging 权限**两次被拒**，或客户主要在 US/EU（私信不可用区）→ 停 TikTok 官方线，转 TK-2 的网页托管路线评估。
- 地区决定能力：US/GB/EEA 等 **私信不可用**，只剩评论 / Shop 客服；面板已按注册地标能力与替代——真流量联调必须用**可用区**的 Business Account。
- `auto_message`（欢迎语 / 推荐问题）与 `capabilities` 枚举字段在文档里有、在权限未批前拿不到真值——**不许猜填**，留 `None` 并在面板显示「待权限」。
- 与 DY 共用：`channel_policy`（TikTok 48h/10、首条禁链）、`window_guard`、composer 回复窗信息带、`reply_window` send-caps。改参数先改 `channel_policy`，不在 `tiktok_official.py` 内写死。
- 与微信线共享文件：`platform_registry.*`、`unified_inbox.html`、`onboarding_guides.py`、`howto_pack.py`、`product_facts.py`——微信线 09-10 已入库 `f00c4d00`，本线在 HEAD 上追加自己的 hunk 即可，不再有「实施97 在途未同步」的 4 红。

## 2. 要做的五段

| 段 | 内容 | 验收 | 文件 |
|---|---|---|---|
| **A 真流量联调**（权限获批当天） | 可用区测试 Business Account 授权 → webhook 注册 → 客户从 TikTok 私信一句 → `im_receive_msg` 落库 → AI 起草 → 发送成功 `im_send_msg` 回显 → 第 11 条 409 `policy_window_quota` → 48h 后超窗 409 → 图片 3 MB 边界；令牌过期 → 账号 chip 需重新登录 | 端到端截图 6 张进落点表；`tests/test_tiktok_official.py` 用**真载荷样本**（脱敏）替换 3 个假载荷；`platform_registry.tiktok.implemented → True` 只在此段过后改 | `tiktok_official.py`、`tests/test_tiktok_official.py`、`platform_registry.py/.json` |
| **B 权限后补字段** | `auto_message`（欢迎语走人设 opening，不用罐头句）与 `capabilities` 枚举按真值填；面板「待权限」灯转绿 | `test_tiktok_onboarding` +2 | `tiktok_official.py`、`help_onboarding.html` / 工作台面板 |
| **C Shop CS API** | TikTok Shop 客服会话（Partner Center CS 类目）：新 `src/integrations/tiktok_shop_cs.py`，同一 `tiktok` 平台下 `source=shop`；会话键 `tiktok/shop/<conversation_id>`；订单卡片只读进线程侧栏；发送走同一 `channel_policy` | 假 transport 测试 8 例；e2e 1 例（进线 → 起草 → 送达 → 回显）；能力矩阵进表「TikTok Shop（official）」 | 新文件 + `account_orchestrator` 注册钩子 + `docs/平台能力矩阵.md` 再生 |
| **D 策略参数与回复窗字段收口** | `tiktok_policy.py` 若仍独立 → 并入 `channel_policy`；`reply_window_deadline` / `window_sent_count` 两个 source 字段名与 DY 线统一（总账「TK → DY」交接项） | `test_channel_policy` +2；`test_window_guard` TikTok 例仍绿 | `channel_policy.py`、`window_guard.py`（DY 文件，stage_hunks） |
| **E huoke 桥 opt-in** | huoke（获客）侧 `reply_engine=chengjie`：TikTok 评论高意向线索 → 智聊起草 → 经 huoke 回评论（评论回复 API 归 TK-2，本段只做线索进线 + 起草 + 回传接口）；默认关 `tiktok.huoke_bridge.enabled=false` | 假 huoke 测试 4 例；配置块进 `config.example.yaml`（stage_hunks） | `src/integrations/tiktok_huoke_bridge.py`、`config.example.yaml` |

顺序：A 等权限；**C / D / E 不依赖权限，可先做**（C → D → E）。B 紧跟 A。

## 3. TK-2 立项（本线收工时一并写）

写 `docs/指令_TK-2_…_2026-09.md`：网页托管边车（assistOnly，需真实 TikTok 网页 DOM 采样）/ 公开评论回复 API / Messaging Ads 归因 / Shop 订单连接器 / TK-1 溢出。只立项不开工；DY 的 P0-4 边车经验（未开工）不可借。

## 4. 红线

- 抖音（DY）代码**一行不动**（冻结）；公共层只追加 `tiktok` 条目。
- 权限未批前不模拟「已支持」：`implemented=False`、事实卡「TikTok：接入中，等平台权限」不变；小智不得答「已支持 TikTok 私信」。
- 地区门控不可绕：不用 VPN / 非可用区账号做联调。
- 共享文件只 `tools/stage_hunks.py --mine tiktok --apply`；提交前 `git diff --cached --name-only` 只含本线；动 `platform_registry` 前 `agent_probe` 看板确认 wx / qq 线不在同文件活动窗。
- 发版：本线代码已在包内（默认关）；A 段过后才算「1.0.8x 支持 TikTok 私信」，此前发版说明不提。

## 5. 开新对话粘贴

```
读 docs/指令_TikTok线_续做_2026-09-10.md，按它续做 TK-1（代码已全部入库、默认关；抖音线冻结一行不动）。Business Messaging 权限未批前先做不依赖权限的三段：C TikTok Shop CS API（新 tiktok_shop_cs.py，同 tiktok 平台 source=shop，会话键 tiktok/shop/<id>，订单卡只读侧栏，发送走 channel_policy；假 transport 8 例 + e2e 1 例 + 能力矩阵进表）→ D tiktok_policy 并入 channel_policy、reply_window_deadline/window_sent_count 字段名与 DY 统一 → E huoke 桥 opt-in（线索进线 + 起草 + 回传接口，默认关，config.example 配置块）。权限获批当天做 A 真流量联调（可用区账号，6 张截图进落点表，真载荷样本替换假载荷，过后才改 implemented=True），紧跟 B 补 auto_message/capabilities 真值。收工写 TK-2 立项指令。预算 $180。先 scripts\agent_probe.ps1 -Intent 登记；共享文件只用 tools/stage_hunks.py --mine tiktok，git add 显式路径，提交前 git diff --cached --name-only 只含本线；每个 commit 补 docs/发版对账_v1.0.80_TK.md 一行；收工改 docs/抖音TikTok渠道族_进度总账.md 的 TK-1 行 + 新增 TK-2 行并 agent_probe -Done。
```
