# 报障对账 · 钧 JUN / skuio 花无缺（值守线 0905 02:0x–03:1x）

> 接 `docs/报障对账_钧_花无缺_20260904.md`。窗口：09-04 20:34 → 09-05 02:40 报障群 + 两机 Cursor（field-agent）报告 + 两机远程诊断包。
> 台账 `bug_intake.db` 现 181 单（本批新立 #169–#181，13 单）。两人版本均已核实 **1.0.73**（skuio 上传头 `app=1.0.73.0`；钧 #164 报障按钮 `1.073`）。

## 0. 本轮动作（已做）

| 动作 | 结果 |
|---|---|
| 远程拉包 DC8F（skuio） | `dr-mtnarhx5-ikg3py` → **88MP86** 02:40 到手：config.local + backend.log 2MB（覆盖 09-04 17:09→09-05 02:40）+ renderer.log |
| 远程拉包 B990（钧） | `dr-mtnarhxg-o8apf7` **pending**（钧机最后心跳 00:24，已关；下次打开工作台自动上传，7 天有效） |
| Cursor 报告 | skuio **13 份**（v1.3.1，含结构化证据与修法建议）：5NAR3T XJWZRG AMKK5Z WQYPPX S79FHC XARQUH DSY7ST 698ENE JHS2NU NQSBCU MWX6QN 66SFWD 32HB5P；钧 **0 份**（v1.2.2 上传坏，v1.3.1 未装） |
| 拆单 | #166/#168 各塞 5–6 件 → 拆出 #169–#178；钧 00:11–00:50 被当闲聊未立单的 3 件 → #179–#181；#165 并入 #164（dup_of=164, closed） |
| 证据台账 | `duty_evidence.py received` 已登记 13 单的包码；有证据的 8 单置 confirmed |
| 群回复 | 02:58 两条（#166 @skuio / #164 @钧，`duty_reply --ticket`，产品端点），各含 ≤2 个只有本人能答的问题 |

**取证模式结论**：skuio 机 = Cursor 报告 + 远程拉包双通（他的 agent 已能自己给出证据链，值守只需发【追证任务】或什么都不发）；钧机 = 远程拉包为主（零打扰）+ 截图，**Cursor 通道在 v1.3.1 装好前不可用**。「群里提问」只留给日志答不了的事（对方手机收到几条 / 客户原话截图）。

## 1. 逐单修复方案

状态列：✅ 根因已钉（可直接施工）｜🟡 方向已定、缺一个数据点｜⏳ 等包/等回答。

### skuio 花无缺（1.0.73）

| 单 | 级 | 状态 | 根因 / 证据 | 修法 | 验收 |
|---|---|---|---|---|---|
| #166 工作目标不推进（BABY BEAR=Loki，`whatsapp:17345893506:13308422244`） | P2 | 🟡 | 88MP86 config：`companion.goals.enabled: true`；但 9.5h 日志 **零 `[goal-inject]`**（`service.py::_note_meta` 对 injected/NOT injected 皆 INFO，唯 `no_goal`/`disabled` 为 DEBUG）→ 拟稿链（skill_manager L3314 → `build_block_for_chat`）对该会话恒走 `no_goal`，或 `find_active_goal` 内部异常被吞（`store.py` L305 debug）。另：`goals.sprint.enabled=False` 出厂默认（config.example L2492），UI 却允许选「自动推进」；进度「第 1/1 天 89%」是时间口径。 | ① `_note_meta`：`no_goal` 在 `conversation_id` 非空时升 INFO（按会话 10 分钟节流）；`find_active_goal` 的 except 升 WARNING 带 SQL 错因。② 目标表单：`sprint.enabled=false` 时「自动推进」选项灰掉并提示；目标卡回显引擎状态；启动日志 `enabled=False` 打 WARNING 不打 ✅。③ 桌面种子 overlay 决策：`companion.goals.sprint.enabled: true`（安全闸不变）或保持关但 UI 不再兜售。④ 键核对：等 skuio 目标详情截图对 `goals` 表 `conversation_id/chat_key/status`。 | 该会话下一条入站 → 日志出现 `[goal-inject] injected` 或明确 reason；卡片进度=动作口径 |
| #169 LINE 视频 >25MB | P2 | ✅ | AMKK5Z/WQYPPX：40 min 零 send_media，前端预检拦。上限来自 `unified_inbox_send_routes._media_cap_mb`：`inbox.media.limits_mb.{platform}`→`default`→25；LINE 未单配。 | 种子 overlay 加 `inbox.media.limits_mb.line`（先用 117 LINE 号 `probe_line_media.py` 实测 LineProtocolWorker 视频上限再定数，LINE 官方 ≥200MB）；红字带实际大小与差值。 | 30MB 视频 LINE 发送成功 |
| #170 幽灵未读 1.0.73 复发 | P2 | ✅ | S79FHC/XARQUH/698ENE + 88MP86：`health_watchdog` 01:54「被埋会话 7 个已归档却有未读（7 条，64h）」；角标 1+3；点开 15 个归档会话不消。`unified_inbox_read_routes.py` L1144 `/account-unread` 明细端点在（#159），但用户点角标「只切账号」→ 浮层没接到他点的那个元素。 | ① 归档视图打开会话同样触发 mark-read；② 账号角标点击（含头像上的数字）直达 `/account-unread` 列表 + 「全部标已读」（`mark-account-read` 已有）；③ 被埋会话告警上屏（现只写日志）；④ 归档会话有新入站自动浮回。 | 点开归档未读 → 角标即减；点角标 → 列表 |
| #171 AI 谎称已发照片（P1） | P1 | ✅ | 88MP86：22:26 两句假声明期间 John 会话零出图动作；**22:49:17 才** `[autosend image] 已发相册媒体`（origin=auto）。`media_promise_guard` 只覆盖将来时承诺（Phase18），不覆盖过去时假声明。 | ① `outbound_promise_guard` 加 `claimed_sent`（EN：`I (just )?sent (it\|you\|the photo)`、`you should have it`、`sending it again`；ZH：已经发了/发过去了/刚发/再发一次）；② B 线 `_autosend_deliver` / A 线 `_apply_media_promise_guard`：命中且近 N 分钟 `_media_sent_log`/`persona_media_sends` 无记录 → 走既有 fulfil（`autosend_image(assume_intent="selfie")`）否则 `_depromise_autosend_text` 改写；③ `detect_selfie_request` EN 问法补齐——**等 skuio 截 John 原话**；④ `media_consistency_eval` 加该实录为金标。 | 实录句式喂 eval 必红；线上 `promise_retracted` 计数 |
| #172 LINE 入站视频 4 分钟判「已过期」 | P2 | ✅ | 88MP86：22:48:26/22:48:46 `[line_media] OBS 404 → 标已过期` id=630323359523275215；视频 22:44 才到。`line_media.py` 全类型走 `download_object("talk","m",msg_id)` 单一路径；图通、视频/语音未真机验（I-5 E1）。 | ① 404 且消息龄 < 1h 不得判「已过期」——改标「拉取失败（服务端 404）」并保留重试；② 117 LINE 号真机复现视频下载，核 OBS 路径/host（视频与图片是否同 service/sid）；③ 收到即预取（不等点击）；④ 拉取/重试写 INFO 日志。 | 手机发 10s 视频 → 工作台可播 |
| #168 SOP 工作链「执行失败」 | P2 | ✅ | 88MP86：22:53:28 十条链 `step=0 retry=1`（无失败原因日志）；22:56:28 `[reply-yield] 客户已回，链让路暂停 starter_postsale_care`；00:44:30 `条件自动启动 20 条`；截图步骤「创建任务 +10h → 9h56m 后」＝步长设计。 | ① `workflow_runner` 步骤重试记 WARNING 带原因；② 卡片显示「下一步 X 后」「已让路暂停（客户回话）」两种状态而非一律「运行中」；③ `auto_start` 加 `max_per_tick`（20 条齐射）；④ 「达成推进未接通」黄条属告警链 nag，见 §3。 | 卡片能读出「为什么现在没动」 |
| #173 批量挂链无反应 | P2 | ✅ | `workflows.html` L645 `WF.bulkStart` 用 `window.prompt()`；Electron 渲染进程 prompt() 抛「prompt() is and will not be supported」→ 点击静默失败（该页在独立窗口，renderer.log 不捕获其 console）。同模式 `cp-chain-exec.js` L782、`cp-rel-stage.js` L192（`typeof prompt==="function"` 守卫无效：函数存在但抛）。 | 三处换页内输入弹层（复用现有 modal 组件）；加 tooltip 解释「批量挂链＝按筛选结果给一批客户启动本链」。 | 桌面壳点击出弹层；dry-run 预览 |
| #174 电话掩码 | P2 | 决策 | 顶栏全号、客户卡 `171****61` 两口径。 | 建议：客户卡点击揭示（WhatsApp 号即身份，坐席需要全号）。老板拍。 | — |
| #175 陪聊人设漏客服话术（P1） | P1 | 🟡 | 88MP86：01:05:25 草稿 L2 `risk=low` 放行；该会话零 goal-inject → 非目标注入；同账号挂 SOP「积极推进产品」，`inbox.workflows.auto_advance.enabled: true`。来源二选一：SOP 话术步指令 or LLM 自由发挥。 | ① `persona_guard` 英文客服/销售腔词表：`my assistant will`、`account details`、`setup guide`、`our team will reach out`；② SOP 话术步 prompt 钉「以当前人设第一人称、不得出现助理/团队/开户」；③ 对照 32HB5P/88MP86 该时刻是否有 `workflow` 拟稿行定来源。 | 实录句喂 `persona_consistency_eval` 必召回 |
| #176 「Exactly」→ 元回复 | P2 | ✅ | `looks_like_engine_refusal`（#115，0831）对此句必命中（元词「翻译」+ 框架词「请发送」），却仍显示 → 只可能是 `TranslationService.translate` L484 前置检**之前**的命中路径：翻译记忆/L1 缓存存着 0831 前的坏译文（`_memory_put` 只在 ok 时写，历史脏值从未重验）。 | ① 记忆/缓存命中也过 `looks_like_engine_refusal`，命中即删并重译；② 一次性清洗脚本扫 translation_memory 命中拒绝模式的条目；③ 入站翻译结果记一行 INFO（provider/memory_hit）。 | 「Exactly」显示「没错」 |
| #177 人工接管消息不进记忆 | P2 | 🟡 | 66SFWD/32HB5P。`persona_reply.py` L133 把 `direction=out` 一律映射 assistant → 人工出站**在窗口内**是可见的；出窗后靠 episodic，而 episodic 抽取只挂在 AI 回复后（`_update_after_reply`），人工发的「有女儿/邀同住」这类**自述事实**永不落库。 | ① 手动发送路由成功后触发同一套 post-reply 钩子（last_reply 环 + episodic 抽取，标 `author=human`）；② 人设自述状态（`_inject_self_state`）吃人工出站；③ 对照 32HB5P 60 分钟全量核窗口大小。 | 人工说过的事实切回 auto 后被引用 |
| #178 双向称呼说明条 | P2 | ✅ | 人设工作室→档案→双向称呼下 #155 落地留的解释占位（mid 1192）。 | 删说明或改为一次性提示（i18n 键随删）。 | 图 |
| #167 首登默认全自动 | P2 | 🟡 | 88MP86 config：`inbox.auto_draft.bootstrap_automation_mode: true` 与 `onboarding.enabled: true` 并存。同族 #63（`account_mode_onboarding`）/#156（1.0.72 已修）。 | 核 `automation_mode._account_mode_layer` 是否优先于 bootstrap；若否 → 新登录账号在确认前一律 review；回访 #63/#156 时带上本单结论。 | 新号首条入站不真发 |

### 钧 JUN（1.0.73）

| 单 | 级 | 状态 | 根因 / 证据 | 修法 | 验收 |
|---|---|---|---|---|---|
| #164（含 #165）媒体发送失败红字·第 4 次 | P2 | 🟡 | 3PZ95W：09-04 20:10:57 / 20:18:55 / 20:53:25 三个 session，无 traceback、无 exit 78 → 20:10=1.0.73 安装重启；20:19 后后端在跑但**无任何 send-media 请求进后端**。族史：#139×2（1.067 投递悬死→超时兜底）、09-02 20:46（1.070，被限频未立单）、本单。 | ① 已在下个桌面包：拿不到响应显示「结果未知，恢复后自动核对」黄字（不再一律红）；② 前端发媒体请求失败时记 renderer 日志（现在前端侧无痕）；③ **等钧回答对方是否收到**定是否还有第二因。 | 断后端发媒体 → 黄字 + 恢复后自动核对 |
| #179 「应用到」TG default 幽灵行 | P2 | ✅ | 图 1186。#61 修法「合并运行时账号注册表」把 config 里的 `default` 占位账号当真账号列出（无 label）。 | `persona_routes.py` 账号合并处过滤 `default`/无 label 且非在册在线的条目。 | 列表只剩真登录账号 |
| #180 LINE 语音 501 + 媒体按钮灰 | P2 | ✅ | 图 1187/1188 = 路由 501 `err.inbox.voice_unsupported`（`unified_inbox_send_routes.py` L1349，`orch.owns_media()` False＝worker 非 running）。3PZ95W：同 LINE 号 09-03 12:06 `LineProtocolWorker send_media type=voice delivered=True` 成功过；同日志 LINE receiver 对 `line-chrome-gw` 连败 6 次→编排器重启循环多轮。文案「需 protocol 多开且在线」通用措辞对 LINE 误导。 | ① 文案按平台/状态分：`worker 未 running` → 「LINE 通道重连中，稍后再试」；② 媒体按钮灰态 tooltip 同源；③ LINE receiver 断连风暴单列稳定性项（`line-worker` 连败 6 次即重启，重启期能力全灰）；④ **等 B990 新包核 00:48 那一分钟 worker 状态**。 | 断线期红字说人话；恢复后自动点亮 |
| #181 WA 扫码登录 4–5 分钟 | P2 | ⏳ | mid 1180–1183。两机 wa-sidecar 均有 `WA connection closed unexpectedly → scheduling reconnect` 风暴，skuio 机还有 `Bad MAC` signal 会话错误。 | 等 B990 包看 00:20–00:25 边车；方向：Baileys 重连退避 + 配对超时提示；`Bad MAC` → 触发 app-state resync。 | 扫码 <60s 上线 |
| 「发了一条显示 2 条」 | — | ⏳ | 图 1172，00:14 自愈；乐观回显与镜像去重竞态。**等钧看手机端是一条还是两条**：两条＝双发（P1）；一条＝纯渲染去重（P3）。 | 视答案定级 | — |

## 2. 登记链自身缺陷（bug_intake，属业务逻辑，需老板拍）

本夜 17 个不同问题只成了 5 单、3 单形状错，且 3 张已修单被误翻 verified——这四条不修，台账会继续失真：

| 缺陷 | 实锤 | 修法 |
|---|---|---|
| 限频拍扁真报障 | `max_replies_per_user_hour=4` 且每条消息都 `_rate_mark`（L1208）；skuio 22:55/00:58/01:11 三条 `rate_capped_report` 不立单。AI 已全静默（`ai_silent`），限频现在只剩「丢登记」这一个效果 | `trigger_verdict` L1111：`ai_silent` 时限频只管回复不管登记；`_note_capped_report` 改为正常立单/补录 |
| 分类器漏钧式短句 | 「这个红色框框的要修复，隐藏」「文字转语音克隆成功了，不能发送」「媒体这里发不了任何东西了」「WHATSAPP登陆了几分钟，登陆不上」全 → `other`（复现脚本已验） | 报障群内已知报障人（`reporter_id` 在台账出现过）发「图+字」一律按报障立单；词表补「不能/发不了/登不上/要修复/隐藏/显示 N 条」 |
| backfill 二次观察 | `bug_intake_backfill` 只用自己的水位差集，实时链已处理的消息再回放：正文每句双写（`[skuio 花无缺] …` / `[skuio] …`）；00:29:24 `补登记 2 条（1180→1183）` 把 1182「已经可以了」再判一次 → #140 假 verified（00:25 实时链已翻 #142）；00:14:23 `补登记 5 条` 同秒 #138 假 verified | 回放前查 `note_group_msg_seq` 已见 mid 集合，已观察的 mid 跳过；#138/#140/#142 人工改回 fixed |
| verify 归属 | `detect_verify_intent` 子串命中 + `pending_verify_ticket` 取最大 id；I-6 结论 B 已记 | 命中且文中有 `#N` 才归属 N；否则要求是对回访消息的回复；短句「可以了」不再触发 |
| 收集窗 30 分钟 | #165（追问）被立成新单 | 对支持号消息的回复视为该工单续写，不看时间窗 |

## 3. 顺手看到、未立单的观测

- 内测桌面包上常驻「告警通道未接通…只能靠人盯页面」toast（图 1172/1188）与「达成推进未接通」黄条（图 1171）：运维 nag 漏到终端用户，他们没有 webhook 可接 → `AITR_DESKTOP_MODE` 下隐藏 `_alertlink_connect`。
- skuio 机 `[mutual_chat] AI 对聊提醒：4 个全自动会话双向高频`（01:54）——看门狗怀疑对端也是 AI；BABY BEAR 语音入站转录超时 25s ×4（21:21）→ 走 `media_degrade_reply` 诚实拟稿。
- skuio 机 09-05 只重启 1 次（01:52:43），MWX6QN 报告里「2 小时重启 4 次」是 agent 把 `fatal_traceback.log` 里 09-03/09-04 的 session 行读进了窗口——**agent 报告的时间归因要复核**。
- E2F5JB 的短码 21 小时后才进群且贴的是模板原文「E2F5JB + 一句话症状」→ AGENTS 剧本里那句给用户的提示要改成可直接复制的成品句。

## 4. 等待项（谁 / 什么 / 到了做什么）

| 等 | 内容 | 到手后 |
|---|---|---|
| 钧 | ① #164 那张图对方收到没 ② tisay 手机端一条还是两条 ③ 跑 v1.3.1 selfcheck 短码 | ①定 #164 是否有第二因 ②定双显等级 ③钧机 Cursor 通道恢复 |
| 钧机 | 远程包 `dr-mtnarhxg-o8apf7`（下次开工作台自动传，DutyWatchTick 自动拉） | 核 00:48 LINE worker 状态（#180）、00:20 WA 边车（#181） |
| skuio | ① John 要照片原话截图（#171）② BABY BEAR 目标详情截图（#166） | ①补 `detect_selfie_request` EN 问法 ②核 goals 表键 |
| 值守 | #138/#140/#142 状态改回 fixed；§2 四条提交老板拍板 | — |
