# 发版对账 v1.0.80 — TK（TikTok 线续做：Shop CS · 策略字段收口 · huoke 桥 · TK-2 立项）

> 指令：`docs/指令_TikTok线_续做_2026-09-10.md`。底座：`docs/指令_TK-1_TikTok接入_官方通道与地区模型_2026-09-07.md`、`docs/抖音TikTok渠道族_进度总账.md` TK-1 / TK-2 行。预算 $180（真流量 $60 / C $60 / E $40 / 门禁 $20）。
> 总账 TK-1 行此前指向的 `发版对账_v1.0.77_TK1.md` 从未建档——**TikTok 线落点以本表为准**（TK-1 A/B/T1–T5/实施99 的 commit 见总账行）。
> 接手方：先 `git -C D:\boundless log --oneline -12` 核对本表 commit 真在。全部代码**默认关**（`tiktok.enabled` / `tiktok.shop.enabled` / `tiktok.huoke_bridge.enabled` 皆 false），已在包内但零痕迹；A 段真流量联调过后才算「支持 TikTok」。
> 执行：2026-09-10 20:30 起。抖音（DY）代码一行未动；公共层只追加 `tiktok` 条目。

## 0. 开工基线（20:30，全只读）

| 项 | 事实 |
|---|---|
| 意向板 | `tiktok TK-1 cont: shop_cs + policy merge + huoke bridge` 20:30 登记；同板：QQ route-1（platform_registry qq 活动中 → 本线**不碰 `platform_registry.py`**，Shop CS 归同一 `tiktok` 平台无需新条目）、ops-group alert noise（pre-restart）。git 索引空闲；HEAD `6ed9dfe3` |
| 共享文件实况 | `config.example.yaml`：HEAD CRLF / 工作树 LF + 他线 5 行在途（Q-6 album_ai apply、实施95 voice rate）→ 整文件 diff 逐行皆异，`stage_hunks` 切不动，E 段按 Q-14 同法手工造 index blob（HEAD 字节 + 我的块）；`platform_registry.py`：仅换行符差异（`git diff -w` 空）；`channel_policy.py` / `window_guard.py` / `account_orchestrator.py` / `tiktok_official.py` / `platform_capabilities.py` 工作树干净 |
| 现状核对 | `tiktok_policy.py` **不存在**（TK-1 参数 09-08 已直接落 `channel_policy` 的 `tiktok` 条目：6000 字 / 首条禁链 / 48h·10·预留 1 / 3 气泡 / 仅图 / enforce）→ D 段「并入」无事可做，只做字段名收口；TK-1 §7 规划的 `source.reply_window_deadline / window_sent_count` **从未写进代码**（DY 09-08 `window_guard` 改为从收件箱事实现算，UI 契约字段 `deadline_ts / sent / remaining / remaining_sec`） |

## 1. 落点

| 段 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|
| C TikTok Shop 店铺客服（Customer Service API） | `16fe54f6` | 新 `src/integrations/tiktok_shop_cs.py`：同 `tiktok` 平台 / 同 `official` mode，按账号 `meta.source == "shop"` 分流；`account_id = shop_id`，会话键 **`tiktok:shop:<conversation_id>`**（文档写法 `tiktok/shop/<id>`；代码与 `tiktok:user:<id>` 同用冒号分层），消息 `source.source="shop"` + `conversation_id / server_message_id / index / shop_id`。`TikTokShopApi`：开放平台签名（`sign = HMAC-SHA256(app_secret, app_secret + path + Σ(sorted k+v) + body + app_secret)`，query `app_key/timestamp/shop_cipher/sign`，头 `x-tts-access-token`）；`POST /customer_service/202309/conversations/{id}/messages`（`{"type":"TEXT"‖"IMAGE","content":"<JSON 字符串>"}`）/ `…/messages/read` / `GET …/messages`（`page_size ≤10` + `page_token`，拉取不置已读）/ `POST …/images/upload` / `GET /order/202309/orders?ids=` 只读 / `auth.tiktok-shops.com /api/v2/token/refresh`。Webhook `handle_shop_webhook`：`Authorization` 头 `HMAC-SHA256(app_secret, app_key + raw_body)`（容忍 Bearer 前缀 / 大小写）；`NEW_MESSAGE`(14) → 两把幂等键 `tts_notification_id` + `message_id` → 买家 in（`record_inbound` 记 buyer / index）+ `maybe_auto_reply`；SELLER/系统 → out 回显；`NEW_CONVERSATION`(15) 只记日志；坏签 401 / 坏 JSON 400 / 缺 id 400；路由回**空 200**（3 秒契约）。**订单卡只读侧栏**：`ORDER_CARD` 消息 → `source.order_card={order_id}` + 状态库 `order_cards`；`worker.order_card()` 拉一次订单摘要（状态 / 金额 / 币种 / 行项目 / 运单）10 分钟缓存；`GET /api/tiktok/shop/order-card?account_id&chat_key[&order_id]`（`app.state.api_auth`）——模块不暴露任何订单写操作。`TikTokShopCSWorker`：`start/stop/healthy/status/send/send_media/mark_read/backfill/order_card`；令牌 `refresh_due` 自刷 / 鉴权码强刷重试一次 / 刷新也败 → `needs_reauth` 清令牌 + 会话健康 expired；文本 >6000 拒、图片 ≤3MB 仅 image；`backfill()` 分页补拉按 message_id 去重、`source.backfill=1`。`register_tiktok_shop_cs_worker(config)`：`tiktok.shop.enabled` 才把 `(tiktok, official)` 工厂包成分流器（shop → 本 worker；其余 → `tiktok_official`；幂等不重包）；`account_orchestrator.ensure_builtin_workers` 紧随 TikTok 官方注册加一钩子。`tiktok_official.register_tiktok_routes` 成为 TikTok 路由唯一挂载口：先按各自开关挂 shop / huoke 子模块路由再挂私信 webhook（加幂等：同路径不重挂）。能力矩阵：`platform_capabilities.WORKERS` + `INBOUND_SITES["tiktok:official(shop)"] → _ingest_message`；`scripts/platform_matrix.py` 新 `ROW_LABEL` 覆写行标「**TikTok Shop（official）**」；`docs/平台能力矩阵.md` 再生（+1 行：发文本 Y / 发图 Y / 已读 Y / typing - / 收媒体 Y / 群 -）；`test_platform_matrix` 两张真相表各 +1。**已知保守项**：Shop 会话按事实表无 48h 窗，但发送走同一 `channel_policy(tiktok)` → `window_guard` 仍按 48h/10 判定（买家先发、无入站不许发本就成立）；按 `source` 放宽归 TK-2 | 新 `tests/test_tiktok_shop_cs.py` **9 例**（假 transport 8：验签 + 请求签名 / 进线·幂等·回显·发图 / 订单卡只读 / 发送形状 + 回显去重 / 图片 / 已读 + 能力位 `{send_text, send_media, mark_read}` / 补拉分页去重不置已读 / 令牌三路 + 分流注册门控 + 路由挂载空 200；**e2e 1**：conftest 完整 admin app——webhook 进线 → 起草钩子 → `/chats` 可见 → `/thread` → `/send` 首条外链 **409 policy_link_denied**（同一道 channel_policy）→ 正常送达假 transport 收到 Shop 发送形状 → 回显幂等 → `send-caps.reply_window` cap 10 / sent 1 / remaining 9）。邻居门禁 `test_platform_matrix / test_tiktok_official / test_douyin_e2e_alignment / test_channel_policy / test_window_guard / test_platform_registry(_consistency)` **97 passed**；`test_admin_route_inventory` 4 passed（默认关 → 无新路由） |
| D 回复窗字段名与 DY 统一（`tiktok_policy` 并入） | 本 commit | **并入无事可做**：`tiktok_policy.py` 不存在，TK-1 参数 09-08 已直接落 `channel_policy["tiktok"]`（§0 现状核对）。**字段名收口**：TK-1 §7 规划的 `source.reply_window_deadline` / `source.window_sent_count` 从未写进代码；DY 09-08 `window_guard` 已改为从收件箱事实现算并以 `deadline_ts / sent / remaining / remaining_sec` 出快照（`send-caps.reply_window`）→ 决议：**TK 采 DY 名，窗口状态不进消息 `source`**（再记一处就多一处会漂移的状态）。`src/inbox/channel_policy.py`（声明层）新增 `WINDOW_FIELD_DEADLINE/SENT/REMAINING/REMAINING_SEC`、`WINDOW_UI_FIELDS`、`LEGACY_WINDOW_FIELDS`（旧名 → 新名只读映射）、纯函数 `normalize_window_fields()`（旧名归一、新旧同在以新名为准、非 dict → `{}`；供 E 段 huoke 回传 / 第三方载荷用）；`tiktok` 条目 `note` 补「Shop 客服会话同此声明；回复窗 UI 字段随 WINDOW_UI_FIELDS」。`window_guard.py`（执行器）**零改动**：快照键本就是 DY 名，由门禁钉住。抖音代码未动 | `tests/test_channel_policy.py` **+2**：`test_tiktok_window_ui_fields_follow_dy_executor`（`WindowState.as_dict()` ⊇ `WINDOW_UI_FIELDS`、数值同源 `window_rule("tiktok")`、旧名不出现在快照、`tiktok_official.py` / `tiktok_shop_cs.py` / `window_guard.py` 源码不再产出旧名）、`test_legacy_window_field_names_normalize_to_dy_names`；`test_channel_policy` + `test_window_guard`（含 `test_tiktok_first_message_link_rule`）**24 passed** |
