# 发版对账 v1.0.84 — 发版线 R84（单一版本：1.0.83 定格内容 + Q-27 + Q-21 + Q-22 + #304 + Q-26 A/B + conv-model-route 已提交部分 → 一个 clean 包 → **公共渠道 + 内测桥接 + 官网 + 播报**）

> 规矩：`.cursor/rules/chatx-desktop-install.mdc` 「单一版本（D-Q11）」——一个 semver = 一个 clean 包。前例 `docs/发版对账_v1.0.82_发版.md`（全链发公共）。1.0.83 定格 `29b3a71a` 只装四台未发（`docs/发版对账_v1.0.83_发版.md` 断点①老板定其余随 1.0.84）。
> 范围：公共渠道仍 1.0.82，changelog / 播报覆盖 1.0.82→1.0.84 全部用户可见能力。Q-26 C–E（`reply_settings` / `personas` / `automation_risk_q26`）与 conv-model-route 未提交的 `unified_inbox_send_routes.py` **不进包**。Q-28 / Q-29 未进本包（Q-29 主干已收工 `6239faaf`，随下一版装载）。
> 接手方：先 `git -C D:\boundless log --oneline -16` 核对本表 commit 真在。意向板 slug `r84-release-1-0-84-single-clean-public-bridge-si`。
> 执行：2026-09-12 08:4x → 09:3x，见 §5。

## 0. 一句话

**1.0.84 单包** `ChatX-Setup-1.0.84.exe` **523,501,804 B** sha256 `cffef4fb7736564fc2838a4752cf44cb4383c717a004db294c2fa3ccd40454db`（499 MB，clean 形态，`MZ` 头核过，build-info flavor=clean commit=`06fee5b8`，干净 worktree `D:\boundless-r80@06fee5b8`）。内容 = 1.0.83 全部（Q-20/23/24/25 + 切语言，此前未发公共）+ **Q-27**（五类硬拦 / hold 2h / 摘标或切全自动即释放）+ **Q-21**（34 码含粤语 / lang-plan / 对方语言未知按人设回）+ **Q-22**（克隆声不静默换系统音 / 语种一致 / 登记结果面板）+ **#304**（普通话不再误切粤语声、克隆回验繁简归一）+ **Q-26 A/B**（会话头一条状态带）+ conv-model-route 已提交部分。坐席三台 **yunsheng / kouxing / lianbei 全 UP · 1.084 · clean/public**。公共渠道 + 内测桥接（同字节）+ 官网 + 台账 16 单 + 回访 19 条 + 群 / 频道播报见 §4。发版门槛在 worktree 真跑全绿（§3）。

## 1. 本版收掉的坑（摘自各线落点表）

| 事故 | 根因 | 本版 |
|---|---|---|
| 报障群 4 条成人软回应（#303） | 软回应绕过档位与场景闸 | GuardContext 单一闸门；群 / 人审 / 同事永不自动发；失败不发、无固定套话 |
| Messenger 四连败静默（#298 #297） | composer detached → 500 无码 + 40s backoff | 同线程重查 / 七码红字 / 同文案自动重试 / 铃铛；DXAPAX 回放零静默失败 |
| PIN 窗不消失（#299）/ 反应丢失（#300） | PIN 分支一次性 return；aria 正则过期 | 自重武装 + 黄条；REACTION_ARIA_RE；手发图不当客户图 |
| couple months → 职业 AI work（#295） | 旧 LLM 回填轨 token 重合问句实体 | FactGate：只写客户原话；确认钮有值才发、必有打勾或红字 |
| Alicia 被否认 / 刚发图不认领（#178） | 空值套用人设爱称；手发图不在相册台账 | 对方怎么叫我优先；30 分钟内刚发图遇「是你吗」认领不发第二张 |
| 全自动被成人 hold 掐死后摘标仍 forced=L1（#301 AFD2CD #296） | adult explicit 与 pressure 同路 24h；切全自动不释放 | 只五类硬拦；TTL 2h；摘标 / 切全自动 / 低级入站即释放 |
| 无粤语 / Hi 像人设没选 / 译文挡原文（#290 #302 #286） | 五处短表；lang_unknown 静默；字号 -1px | 目录 34 码含粤语；按人设语言回并标注；译文 0.9× 块流 |
| 日语发出微软男声（#287 #288 #239） | 语种闸被豁免静默回落 edge；登记 toast 一闪 | 阻断改发文字；红条二次确认；结果面板停留 |
| 普通话切香港声 / 克隆判念错（#304） | 弱粤语字阈值 2；CER 繁简假阳性 | 独占/共用拆表；两侧 t2s；opencc 随包 |
| #289 弹层裁切 | 已随 1.0.82 `7c4aae3b` | 本版不宣称新修，台账引该 commit |

## 2. 开工基线（08:4x）

| 项 | 事实 |
|---|---|
| 主干 | 探活时 HEAD `cb1c57b9`（#305 立 Q-29 + 总账 08:4x）；版本链基点已提交。切包后主干继续落 Q-29 A–D（`4614c5d5`…`6239faaf`），**不在本包** |
| 意向板 | slug 登记；index FREE；热区 10 分钟静默（仅 `deploy/desktop` 监控非热区） |
| 工作树 | Q-26 C–E 与 `unified_inbox_send_routes.py` 等脏文件**未动、未进任何 commit**。`i18n_hant generate` 前把 `risk_grader.py` / `bug_tickets_page.py` 写回 HEAD、藏 `automation_risk_q26.py`，生成后拷回 |
| 版本链 | `desktop/package.json` 1.0.83→**1.0.84** / 1.083→**1.084**（字节替换，CRLF 保留）；`python -m scripts.i18n_hant generate` → **17593 键**；`gen-chatx-changelog add --version 1.0.84 --date 2026-09-12 --tags fix,improve,feature` **9 条双语**（群聊永不自动软回 / 五类硬拦 / Messenger 失败可见 / 画像确认必反应 / 刚发图认领 / 会话语言含粤语 / 克隆声不换系统音 / 会话头状态带 / 界面语言切换）；`bump_ui_build.py --severity notice` → **20260912-1515**；小智 `help_terms` +8 / `howto_pack` +8；`test_howto_coverage` 6 passed → 主干 **`06fee5b8`** |
| 官网五值 | `chatxContent.ts` version / filename / url / sha256 / 注释 → 主干 **`53b4010a`**（push 坐席后、publish 前） |

## 3. 回归 / 门禁（干净 worktree `D:\boundless-r80@06fee5b8`，四项并跑 08:55 起）

| 项 | 结果 |
|---|---|
| worktree 复用 | `D:\boundless-r80` 原 `29b3a71a`：`git checkout -- .` + `git clean -f` 只清构建改写的 `i18n-packs-staged`，再 `checkout --detach 06fee5b8` |
| `import_sweep` | modules=**1259** bad=0 / from-import names=**3655** bad=0 **ALL GREEN**（32s） |
| predist（clean） | `build:backend` 全量 + `dist:win:clean` 08:55–09:11：后端冒烟全过、干净包首启冒烟全过、after-pack 随包交付物 **22 项**；`latest.yml` version 1.0.84 size 523501804 |
| `gate_sweep -NoSyntaxScan` | 初跑 **19 failed / 1746 passed**（13:11），transient 复跑 15 文件仍 19 红。相对 R83 的 25（含 worker 崩连坐）：本版稳定 19。**本版新红 1 条**：`test_cp_i18n_overlay_coverage::test_zh_hant_overlay_covers_every_key` 缺 Q-22 `cp.voice.*` 12 键繁体（1.0.83 无这些键故绿）。其余 18 同名旧债（assistant_qa ×2 / deploy_profiles / inline_color ×3 / orphan_refs / legacy_blue ×2 / input_theme_contrast / silent_exception / takeover / copilot_tiny_font / workspace_emoji / sealed_templates / sql_phantom / no_dynamic_dot / ui_build_stamp mtime 伪红）。发版后在 r80 跑 `i18n_desktop_ext generate --langs zh_hant` 补 overlay，**包不重打**（繁体副驾这 12 键本包回落简中） |
| 发版门槛（worktree 真跑） | `test_guard_context_gate` + `test_fact_gate_q25` + Q-27 三条回放 + `test_case1_ja_x_steven_blocked` + `test_messenger_sidecar_q24` + `test_howto_coverage` = **88 passed**；`node --test send_chain.test.js` **16 passed**（含 DXAPAX 四连败）。不引入罐头句（闸门 gen 失败零发送） |
| 全量回归 | `scripts\regression.ps1` **139 failed / 27656 passed / 43 skipped，1:16:40**。串行复跑 139 → **109 仍红 / 30 绿**。同 139 条在 1.0.83 release `29b3a71a`（`D:\_r84cmp`）串行：**98 红 / 40 绿 / 1 skip**。差集「1.0.84 红 / 1.0.83 绿」= **11 条**，全非功能/有意变更：① `test_ui_build_stamp_not_stale` = worktree 09:36 拷回 overlay 的 mtime 伪红（戳 08:55；安装包不含 tests）；②–⑦ Q-22 旧钉仍预期「静默换系统音」（`test_autosend_garbled_falls_back_to_edge` / `test_gate_falls_back_to_lang_aligned_edge_voice` / garbled 改派 4 条）→ 跟新为阻断 + confirm 才 edge；⑧ `test_region_suffix_stripped` zh-TW 现为一等码 `zh-tw`（Q-21 `_LANG_NAMES`）；⑨ `test_tts_blocked_and_metered` 英文稿撞 lang_mismatch → 改中文对账；⑩ `test_remote_cdn_ref_does_not_wait` Q-21 lang-plan 多读 store，CDN 仍立即生成；⑪ `test_stage_voice_file_routes_by_tier` mock 未收 Q-22 `tts_lang`。钉子另 commit，**包不重打** |

## 4. 全链

| 步 | 状态 | 事实 |
|---|---|---|
| 坐席三台 | ✅ 09:11–09:19 | `push_chatx.ps1 -TargetSsh {yunsheng,kouxing,lianbei} -Setup <r80>\dist-clean\ChatX-Setup-1.0.84.exe -Install -Relaunch -Smoke`：三台 exit 0、`[push] OK`、seatverify sentinel FRESH + 18799 login 200、smoke `app version: 1.0.84` 六端点 OK + vision active + pacing ok；`chatx_fleet_status` **三台 UP · 1.084 · clean/public**。日志 `%TEMP%\r84\push_{yunsheng,kouxing,lianbei}.txt` / `fleet.txt` |
| publish 公共 | ✅ 09:19–09:22 | `-DryRun` 过 → `publish_chatx.ps1 -DistDir <r80>\dist-clean -Yes -NotesFile announce_1084.txt`：VPS sha512 ✓ → 指针 + pm2 reload → R2 → 公网核 `latest.yml=1.0.84 manifest=1.0.84 exe=HTTP206` → `announcements.json` 带 `release-v1.0.84` → 清理 1.0.80。日志 `%TEMP%\r84\publish_public.txt` |
| publish 内测渠道**桥接** | ✅ 09:23–09:25 | 同一个 clean exe + blockmap + `latest.yml` 复名 `latest-internal.yml`（`%TEMP%\r84\internal_bridge`，sha256 逐字节同 `cffef4fb…`）→ `-Channel internal -DryRun` 过 → `-Yes`：`latest-internal.yml=1.0.84`，公共指针未动（`latest.yml still v1.0.84`），清理 1.0.80。**不是第二个包** |
| 官网 | ✅ 09:26–09:29 | `gate:content` **6/6 PASS**（发布事实源 1.0.84 / cffef4fb… 一致；更新记录最新 v1.0.84；测试数 1863 ≥ 宣传 1100+）→ `deploy.ps1 -FromHead` → **部署完成 [OK]**（healthy / webhookSecret / adminKey / deepseek 全 True，R2 同步）。日志 `%TEMP%\r84\website_deploy.txt` |
| 台账 / 回访 / 播报 | ✅ 09:30–09:36 | `duty_update_hint --set`（明写升级后重启一次工作台后端，热重载）→ 报障群播报 `duty_reply --group neice --text-file broadcast_1084.txt --no-mention` → **mid 1488**（09:30:04）→ `duty_ledger_fix --mark-fixed-file mark_fixed_1084.json --apply`：**16 单 new→fixed**（#272 #286 #287 #288 #289 #290 #295 #296 #297 #298 #299 #300 #301 #302 #303 #304；#178 #239 已 fixed 跳过；#289 fix_note 引 `7c4aae3b`）→ 逐单回访 19 条 `bug-intake/reply ok`（09:31–09:36，间隔 15s，每条带工单号 + 只给版本号 + 验收三步；#305 单独说明已立 Q-29 **不标修复**）→ `duty_notify_summary --ids … --msg-id 1488 --apply` **记账 16 单，该筛选积压 0** → 频道 @hykj7 **messageId=165**（campaign `release-1084`） |
| release 真值 commit | ✅ `c15f27f9` | `desktop/build/i18n-packs-staged` 自 worktree 构建拷回（6 改 + 2 新：`conv_state_q26.py` / `zh_hant_lang_plan_q21.py`）+ Q-22 繁体 overlay 12 键补 `cp-i18n-ext.zh_hant.js`（双树）/ `shell-i18n-ext.zh_hant.js` + 本表 + Q 总账 R84 行 |

## 5. 断点 / 交接

- 08:4x 读总账 / R83 对账 / SOP → 意向登记 + 热区静默核 → 08:5x 版本链 `06fee5b8`（藏脏 i18n 后 generate）→ 08:55 worktree 回滚 + detach → import_sweep / build+clean 包 / gate_sweep / regression / 发版门槛 五项并跑 → 08:56 门槛 88+16 绿、import 绿 → 09:11 包出（`MZ` / sha256 / latest.yml / build-info 核）→ 09:11–09:19 坐席三台 + fleet → 09:19 `53b4010a` 官网五值 → 09:19–09:25 publish 公共 + 桥接 → 09:26–09:29 gate:content 6/6 + deploy → 09:30–09:36 hint / 播报 / 台账 / 回访 / 频道 → 09:4x 真值 `c15f27f9` + 意向 -Done → 10:1x 回归收口 139 红 → 串行 109 vs `29b3a71a` 98，差 11 条钉子跟新（包不重打）。
- **对照 worktree** `D:\_r84cmp@29b3a71a` 用完即拆。发版 worktree 保留 `D:\boundless-r80@06fee5b8`（`dist-clean\ChatX-Setup-1.0.84.exe`）。
- **117 / zhiliao**：本线未重启 zhiliao。坐席三台 = 包内新后端，Q-27 / Q-21 / Q-22 / #304 / Q-23 闸门已随包装载。117 壳仍 1.0.83、后端半边仍等 `restart_preflight` GO 后 `restart_instance.ps1 -Instance zhiliao`（现又叠 Q-29 `.py`）。skuio 走公共渠道自检 1.0.84。
- 临时文书全在 `%TEMP%\r84\`（notes 双语 / announce / broadcast / channel_post / mark_fixed json / reply\*.txt / 各步日志），未入库。

## 6. 交回各线

- **#301 死链**（回复设置 → 人设工坊成人政策）属 **Q-26 C**，本版只把 AFD2CD 五类硬拦标进 #301；设置中心收口随 Q-26 C–E。
- **Q-26 C–E** / **Q-28** / **Q-29**：C–E 工作树未提交；Q-28 未开工；Q-29 主干已收工但不在 1.0.84 包、须 zhiliao 重启才进 117。#305 已回访「已立 Q-29 不标修复」。
- **繁体 `cp.voice.*` 12 键**：1.0.84 包回落简中；主干 overlay 已补，`ui-build` silent 戳 `20260912-1516`，下版随包。
- **未进本版**：Q-12 / Q-13；conv-model-route 未提交 send_routes hunk。
- **skuio / 钧 验收**：各单三步见 `%TEMP%\r84\reply\*.txt`；升级后请重启一次工作台后端。
