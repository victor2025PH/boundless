# 指令 P-4：出厂态与安全（后台默认回环 · 陪伴域首装 KB 空 · 打包清单 · 文案）

> 预算 **$150**。报告 **MTRCH2 ①②③④**（P-5 会从 #106 摘出新立单，commit 先带 `#254`，立单后落点表补号）、**32PTMK**（#254）。目标版本 1.0.79。决策 **D-P2 局域网默认关、D-P6 陪伴域首装 KB 空**。
> 底座 `docs/修复指令_P批_并行_2026-09-08.md`。落点表 `docs/发版对账_v1.0.79_P4.md`。

## 开工前必读

1. `docs/全自动出站链_三视角方案_2026-09-08.md` §0 N6–N9、§1.1 第 6 / 7 条、§2 P0-6 / P1-7 / P1-9 / P2-12。
2. 证据：`tmp_diag\MTRCH2\*_report.md`（Web 监听 0.0.0.0；clean 包缺 `handoff_scripts.yaml` / `handoff_compliance.yaml`，Handoff 两组件 init skipped；`fatex.db` 随用户版打包；「首启体验档 100000 字符 / 0.0 小时窗口（剩 None 小时）」；日志标签仍写 `from domain 'conversion'`）；`32PTMK`（首装 KB 剩 8 条：3 条【示例】价格 / 营业时间 / 退款 + `complaint_fallback` + 全局 / 问候 / 闲聊兜底 / 测试回复；类型标签「常规咨询 / 退款投诉」不在新分类里）。
3. 锚点（已核）：`desktop/backend-launcher.js` L77–88 `lanServeHost`：`strong && base_url 回环 → "0.0.0.0"`（`b.lan_access !== true` 已有一个开关键，但强令牌分支绕过了它）+ `desktop/test/backend-launcher.test.js`；`src/utils/kb_store.py` L3255 `seed_kb_format_examples`（J-9 #184 首装 3 条示例）/ L2694 `seed_default_data` / L3101 `system_seed_plan(cfg, business_domain)`（N-3 E 只挡 PAYMENT）/ L3131 `seed_system_replies`；`desktop/build/build_backend.py` L60–64 显式 config 清单（无 handoff 两个 yaml）；源文件 `engines/chengjie/config/handoff_scripts.yaml` / `handoff_compliance.yaml` 存在；`engines/chengjie/config/fatex.db` 存在——查 `stage_clean_assets.py` / `stage_internal_assets.py` 哪一步把它带进包；`desktop/test/package-layout.test.js`（打包门禁挂这里）；`src/web/i18n_packs/membership.py` L89 `mb_trial_line: "首启体验档 · 还剩 {h} 小时"`。
4. `scripts\agent_probe.ps1` 看 ACTIVE：R78 在动 `package.json` / 发版脚本——`build_backend.py` 与 `package-layout.test.js` **等 R78 收工再提交**（先做好放本地，落点表注明），其余不冲突。

## 红线

- 只动归属矩阵 P-4 行；不改 `package.json` 版本号；不碰 R78 的发版脚本。
- 销售域（`business_domain=sales`）保留现有示例与兜底，只改陪伴域行为。
- `git add <显式路径>`；每 commit 带 `#254`；不 `-A`。

## A 后台默认回环 + 「允许局域网设备连接」开关（D-P2）

- 修法：`lanServeHost` 去掉「强令牌即 0.0.0.0」分支，只在 `backend.lan_access === true` 时返回 `"0.0.0.0"`；设置页（系统设置 / 连接）新增开关「允许局域网设备连接（手机扫码操控需要）」默认关，开启时：写 `lan_access=true` + 提示需重启后端 + 顶部常驻小横幅「后台对局域网开放 · 192.168.x.x:18799」+ 二维码入口；Windows 网络类别为 Public（`Get-NetConnectionProfile` 或 Node 侧等价）时开启需二次确认；#57 扫码操控入口若检测到 `lan_access=false` → 引导去开开关（而不是静默失败）。`backend-launcher.test.js` 改断言。
- 验收：clean 装 → `netstat -ano | findstr 18799` 只见 127.0.0.1；开开关重启 → 0.0.0.0 + 横幅；扫码页在关时有引导。

## B 陪伴域首装 KB 为空 + 预填模板 + 系统话术移位（D-P6）

- 修法：① `seed_kb_format_examples` / `seed_default_data` / `seed_system_replies` 全部经 `system_seed_plan(business_domain)`：陪伴域 = 不播示例、不播任何直发兜底（complaint_fallback / 全局 / 问候 / 闲聊 / 测试回复）；② 「格式示例」改为 KB 页「新建条目」按钮的预填模板（陪伴域三例：称呼偏好 / 忌聊话题 / 常聊话题；销售域沿用原三例）；③ 「系统话术」整类从用户 KB 页移出 → 系统设置新页「兜底策略」（开发者模式可见，列出各兜底开关与文案；陪伴域全部默认关，AI 失败 = 静默 + 转人工提示）；④ 类型标签随域（陪伴：人设背景 / 日常话题 / 情感回应 / 关系推进 / 边界与安全 / 其他）；⑤ 存量清理：一键「清除客服域残留」（只删 `used=0 且 disabled` 的种子行，先 dry-run 列清单，与 N-5 误删事故同教训——**不要在自检里自动跑**）。
- 验收：clean 装陪伴域 → KB 0 条；「新建条目」预填三例；系统设置有「兜底策略」；销售域不变；tests：seed plan 两域各一。

## C 打包清单两张 + handoff 进包 + fatex 出包

- 修法：`build_backend.py` L60–64 加 `handoff_scripts.yaml` / `handoff_compliance.yaml`；查 `fatex.db` 进包路径并移出（命理产品库只随 FateX 产品线，用户版不带）；`package-layout.test.js` 加两张清单：**必须在**（config.desktop.min.yaml / handoff 两 yaml / domains manifest / seed 按 profile）与 **必须不在**（fatex.db / *.key / credpool db / 内网地址 / 演示数据），不过不 dist。
- 验收：`npm run predist` 门禁绿；装包后 `handoff_scripts loaded: N scripts` 出现在启动日志；包内无 fatex.db。**提交时机等 R78 收工。**

## D 文案 + 日志标签

- `mb_trial_line`：`{h}` 为 None / 0.0 / 窗口未开始 → 分支文案「首启体验档 · 未开始计时」/「不限时」；数据源为什么 None 一并查（`membership` 试用窗口未初始化即渲染）。
- 域包日志「System prompt loaded from domain 'conversion'」→ 打印 `business_domain` 名（陪伴 / 销售）；MTRCH2 提到的「care interval 300→600s 与 FHSZTA⑤ 精确调度相反」「帮助语料 292 条未播」只**核实写进落点表**，不改。

## 收工

- 落点表 `docs/发版对账_v1.0.79_P4.md`；`docs/P批_进度总账.md` 只改 P-4 行；`agent_probe.ps1 -Done`；回访文案交 P-5（给 skuio：`netstat` 验证 / 开关位置 / 首装 KB 空 + 模板在哪 / 包内清单门禁）。

## 开新对话粘贴

```
读 docs/指令_P-4_出厂态与安全_2026-09-08.md，按它做 P-4（MTRCH2 / 32PTMK / #254：后台默认绑 127.0.0.1 + 设置页「允许局域网设备连接」开关默认关 + 公共网络二次确认 / 陪伴域首装 KB 为空 + 「新建条目」预填模板 + 系统话术整类移到系统设置「兜底策略」+ 存量清理 dry-run / build_backend.py 补 handoff 两 yaml + fatex.db 出包 + package-layout 门禁「必须在·必须不在」/ mb_trial_line None 分支 + 域包日志标签）。预算 $150。先读 docs/修复指令_P批_并行_2026-09-08.md 的归属矩阵与决策 D-P2 D-P6，再 scripts\agent_probe.ps1 -Intent 登记；build_backend.py 与 package-layout.test.js 等 R78 收工再提交；销售域行为不变；清理只 dry-run 不自动跑。每个 commit 带 #254 并立刻补落点表 docs/发版对账_v1.0.79_P4.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/P批_进度总账.md 的 P-4 行并 agent_probe -Done，回访文案交 P-5。
```
