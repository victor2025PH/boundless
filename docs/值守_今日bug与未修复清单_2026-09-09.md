# 值守日清：今日 bug · 未修复清单 · 修复指令去向 —— 2026-09-09 19:0x

> 只读盘点，不改代码。数据源：`bug_intake.db`（zhiliao 数据根，只读连接）、`tmp_diag/*`（24 份今日报告，全部已回执）、`duty_watch_loop.log` / `duty_auto_ticket.log`、`git log --since 09-09`、意向板、`net_probe_117` / `uplink_watchdog` / `prod_edge_watchdog`。
> 修复指令**已全部成文**（`docs/指令_Q-*.md`，底座 `docs/修复指令_Q批_并行_2026-09-09.md`），本文只回答三件事：今天出了什么 / 哪些还没修 / 下一个对话该粘哪段。

## 一、今天（09-09 00:00–19:00）发生了什么

| 时间 | 事 | 处置 |
|---|---|---|
| 00:00–03:39 | skuio 1.0.78 首夜后半段报告继续进来（K24YJ2 / FCQFKF / AEMAMJ / 57S9XD …）→ 自动立 **#261–#269** | 已回执；07:09 三视角方案 → 07:45 Q 批底座 + 10 条指令成文 |
| 07:31 | 成本对账线 P0–P2 装载（llm_cost purpose+CNY / cost_ledger.db / `/workspace/cost`）；另 4 个小修 AWAITS RESTART（意向板 08:10） | 等 22:30 窗 |
| 07:58–08:07 | 运维群 `tg-ywqz` 上线，三条告警；老板点卡片链接打不开 | 08:42 值守分析：服务端正常、点击未到服务器＝设备侧网络；顺带三处真缺陷 → **Q-14 D/E** |
| 09:19 | 追加 **Q-14**（#262：ai.timeout 30→60 / `[ai] fail` 灰标 / 网关报表 / katie 三件 / magic link） | D-Q9 D-Q10 生效 |
| 11:28–11:35 | **#270**：Mike 十小时被问 8 次「做什么工作」后「Goodbye」（PMCRXG，T9KN8X 挂单） | 11:35 自动回执；14:12 值守代发回访（暂停摸底目标 / Mike 切手动 / 手补画像 / 1.0.79 修法）；14:13 Q-1 升第一优先、**D-Q6 修正：R79 = Q-1 + Q-3 收工即发** |
| 14:11–15:02 | **Q-14 A / B / C 三 commit 落地**（`2dcdaeab` / `38707c79` / `c32de531`）：timeout 60 + 读超时不重试 + prompt 6k 预算 + `[ai] fail` 一行；会话灰标「AI 本轮未生成」+ 重试起草 + reply_diagnosis；官网网关慢模型冷却切备用 + 24h p50/p95/499 报表 + pm2 reload 零停机 | A/B 随 1.0.79 装机；C 官网**已提交未部署**（DryRun 过）；**D katie / E magic link 未落**（意向板 Q-14 仍在册，落点表只有 A/B/C 三行） |
| 16:08→17:09 | 117 探针断 61 分钟（PROBE-GAP：机器关机 / 重启 / 任务未跑）；17:13 TCP176 曾 FAIL；17:18–18:27 办公室出网 **抖了 6 次**（每次 22–38s）；VPS 探针 15:21–18:31 五次超时（18:00 一次连重试也失败） | 19:48 `prod_edge` tunnel=200 public=200 已稳；宽带线 27.126.158.220（09-07 17:32 起 DOWN）19:52 恢复。**不是产品 bug**，但正是 Q-14 D「隧道判别日志」要抓的那类窗口 |
| 14:17 之后 | bug 群零新报告、零新消息；23:18 / 23:40 两份 selfcheck（H3BZKW / NQNTVZ）是自检包非报障，不立单 | 值守循环每 2 分钟一拍，回执 64/64 |

## 二、未修复清单（`bug_intake.db` 开放态 18 张）

### P0（6 张，全部去 Q 批第一波）

| 单 | 现象（一句） | 去向 | 状态 |
|---|---|---|---|
| **#269 / #270**（#264 族） | 同一客户十小时被问职业 8 次 → 客户「Goodbye」；根因＝目标引擎只判「问了没」不判「知道没」+ 画像抽取关着槽位永空 → missed 无界重试 | **Q-1**（第一优先） | 未开工。#270 在库里仍是 **P2/confirmed**，与回访口径「P0 客户流失」不一致 → Q-0 C 归位时改 |
| **#264** | 夸赞中 AI 主动退场 / needs_human 挂着仍 L2 直发 / 切手动 35s 内仍发出 / 「you are a bot」 | **Q-3**（人工优先）+ Q-1（识破族）+ Q-8（退场闸，第二波）+ Q-12 | 未开工 |
| **#263** | 答应周六上门喝茶 / 假发图四轮 / 「采集不可用：画像 AI 抽取未开」 | **Q-2**（承诺守卫）+ **Q-6**（假发图）+ **Q-5**（抽取合一） | 未开工 |
| **#267** | 班表 / 日发额度 1.0.78 升级后出厂开且按上海时区；额度限了人工 | **Q-4**（撤回 D-O4） | 未开工 |
| **#266** | 相册 91 张「缺触发词」但每张已有 AI 建议词——建议词不生效 | **Q-6 E** | 未开工 |
| **#259** | 1.0.77 WA hana 草稿事故（P 批已修，等装机） | R79 | 等发版 |

### P1 / P2（12 张）

| 单 | 现象 | 去向 |
|---|---|---|
| #260 P1 | 出厂态四件：0.0.0.0 / clean 包缺 handoff yaml / fatex.db 进包 / … | P 批已修 → R79 |
| #265 P2 | 关怀「立即发」到点被安静时段顺延 | Q-4 D |
| #268 P2 | 主题配色浅色 / 深色点了无反应（定时模式占着） | Q-10 B |
| #262 P2 | 1.0.78 运行时核验 + AI 沉默 2.4% | **Q-14**（A/B/C 已落，D/E 未） |
| #258 / #261 P2 | 1.0.77 / 1.0.78 启动全日志（对照用，无动作） | 随 R79 关单 |
| #216 P2 | 目标卡片阶段显示复验（09-03 遗留） | Q-1 收工时顺带复验后关 |
| #44 / #68 / #74 / #82（verified） | LINE 收不到对方消息 / 语音识别失败 / 图后漂中文 / 雨景照 | 已修待「released」态归位（0830 批），R79 台账一并清 |

### 今天新增但**不立单**的观察

- Q-14 A 的 `prompt_budget_tokens=6000` 与 1.0.78 3940 字底稿 + 目标注入 + few-shot 的实际长度未在真会话上量过——装机后看 `[ai] prompt_tokens=… trimmed=` 是否常态裁 few-shot（常态裁＝预算定低了，或底稿该瘦身）。
- 官网 C 段 `DEEPSEEK_BACKUP_API_KEY` 未配 → 慢模型冷却「只记不切」；要真切备用需运营给备用 Key（凭据，代码侧不能造）。

## 三、修复指令去向（按老板 14:1x 定的顺序，一条指令 = 一个新对话）

**顺序**：Q-1 → Q-3 → **（Q-1 + Q-3 收工即开 R79）** → Q-2 → Q-4 → Q-6 → Q-5 → Q-14（D/E 收尾）→ Q-0（B–E）。Q-8 / Q-10 第二波；Q-12 / Q-13 = 1.0.80。

每条粘贴块在各 `docs/指令_Q-*.md` 文末，也汇总在 `docs/Q批_进度总账.md`。开工三件事：`scripts\agent_probe.ps1` 看 ACTIVE + 意向板 → `-Intent` 登记 → 读归属矩阵（共享文件 `tools/stage_hunks.py --mine`）。当前意向板在册：**成本对账线（AWAITS RESTART，`ai_client.py` / `webhook_notifier.py` 整文件 LF 重存，stage_hunks 切不动——Q-1/Q-14 碰这两个文件要先看它）**、**Q-14（14:11 起，D/E 在途）**、QQ route-1。

### 3.1 Q-1（现在就开，#270 客户流失）

```
读 docs/指令_Q-1_目标引擎已知即不问_2026-09-09.md，按它做 Q-1（#264 #269：profile_slots 槽位三态 unknown/mentioned/confirmed + 注入前扫最近 30 轮、decide_probe_target 只选 unknown 且修掉「问过的加回末尾」/ 每槽每日 retry ≤2 + covered 判定 / 新 repeat_question_guard 24h 内相似问句拦 / 新 exposure_guard 识破词→停目标 24h + needs_human + 一句不辩解挽回 + 自证句黑名单 / 目标页「暂停全部摸底目标」总开关 + 卡片「已提及（未确认）」+ 确认 / prompt_addenda.identity_addendum 函数交 Q-6 接线）。预算 $180。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与接口约定①③、docs/发版对账_v1.0.78_O3.md C 行（在它之上改，不重做），再 scripts\agent_probe.ps1 -Intent 登记；不动 persona_reply.py / autosend_worker.py；判不准就不问。每个 commit 带 #264 并立刻补落点表 docs/发版对账_v1.0.79_Q1.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-1 行并 agent_probe -Done。
```

### 3.2 Q-3（与 Q-1 并行）

```
读 docs/指令_Q-3_风险挂会话与人工优先_2026-09-09.md，按它做 Q-3（#264：新 risk_hold 会话级风险持有 set/active/clear + autosend_policy 强制 L1 并继承 shadow / needs_human 从标签升级为闸禁 L2 / autosend_worker 节奏等待后真发前二次复检 abort=mode_changed|agent_typing|agent_sent|risk_hold|needs_human / 切手动·坐席打字·坐席发送取消在途稿 + toast「已取消 N 条」+ 新 agent-typing 端点 / 重新起草调用侧带 reason / 门禁「切手动 1 秒取消」）。预算 $160。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与接口约定②，再 scripts\agent_probe.ps1 -Intent 登记；你是 Q 批唯一动 autosend_worker.py / autosend_policy.py 的线；drafts.py / store.py 不动；切档路由有他人在途 hunk 用 tools/stage_hunks.py --mine。每个 commit 带 #264 并立刻补落点表 docs/发版对账_v1.0.79_Q3.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-3 行并 agent_probe -Done。
```

### 3.3 Q-2 / Q-4 / Q-6 / Q-5 / Q-0（B–E）/ Q-8 / Q-10

粘贴块分别见 `docs/指令_Q-2_现实承诺守卫五类_2026-09-09.md`、`指令_Q-4_撤回错误默认与基线红线`、`指令_Q-6_假发图闭环与相册建议词`、`指令_Q-5_抽取两链合一与昵称解析`、`指令_Q-0_值守止血与归位`、`指令_Q-8_自主推进与退场闸`、`指令_Q-10_工作台小件` 文末。Q-0 的 A 之一（#270 回访）已由值守代发，**B（我方 zhiliao 实例班表关 + 摸底注入停）今天仍未做**——这是配置层一分钟的事，且 D-Q4 明写「今天」，建议 Q-0 对话先做 B 再做 C/D/E。

### 3.4 Q-14 收尾（D katie 三件 / E magic link）

Q-14 线仍在册（14:11 起）。若该对话已断，接手方粘贴文末原话术并加一句：「A/B/C 已落（`2dcdaeab` / `38707c79` / `c32de531`），只做 D / E；C 段官网代码已提交未部署，随下次 `publish_chatx.ps1` 一并 reload」。D 的验证材料：今天 15:21–18:31 VPS 探针五次超时 + 17:18–18:27 办公室出网六次抖动（`net_probe_117_events.log`）——隧道判别日志上线后应能把这类窗口标成「办公室侧断」而非「后端挂」。

### 3.5 R79（Q-1 + Q-3 两行收工后立刻开）

粘贴块在 `docs/Q批_进度总账.md`「R79 开新对话粘贴」；按 D-Q6 修正，前提从「Q-1～Q-6 六行」改为「**Q-1 + Q-3 两行收工**，届时已收工的 Q-2/4/5/6/14 一并带上」。

## 四、值守在盯什么（持续）

- **bug 群 / 现场 agent 报告**：`duty_watch_loop` 每 2 分钟一拍（19:51 仍在跳），新包 → 解包到 `tmp_diag/<码>` → `duty_auto_ticket` 立单或挂单 → 群内回执单号；今天 24 份全部回执，无积压。新单出现即按「去向表」标 Q 线，P0 立刻在总账拍板记录加一行并回访。
- **Cursor 现场 agent 的错误**：报告里的 `fatal_traceback.log`（skuio 机 169B，09-08 23:16 起未变＝无新崩溃）、`backend.log tail` 的 `[ai] fail`（Q-14 A 装机后才有）、`renderer.log`。
- **网络 / 隧道**：`prod_edge_watchdog` 5 分钟一拍（tunnel + public 双 200 才算健康）、`net_probe_117_events`、`uplink_watchdog.alert`；今天下午那种抖动若复发且 tunnel≠200，先看办公室出网再看实例。
- **重启窗口 22:30**：成本对账线 4 个小修等着搭车；Q-14 A/B 是客户端代码不需要我方实例重启；Q-1/Q-3 落地后同窗装载验活体（同会话已答不再问回放 / 切手动 1 秒取消）。
