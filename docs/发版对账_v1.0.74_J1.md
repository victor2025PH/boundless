# 发版对账 v1.0.74 —— J-1（AI 出站内容正确性 + 记忆一致性：#171 #175 #176 #177）

> 断点续传介质。接手方先 `git -C D:\boundless log --oneline -15 -- engines/chengjie` 核对 commit 列真在，再从第一个未完成项继续。
> 指令：`docs/指令_J-1_出站内容与记忆_2026-09-05.md`；总账：`docs/J批_进度总账.md`。
> `.py` 改动**未装载生产**（不发版不重启，等值守下个重启窗统一装载）。

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注 / 剩余 |
|---|---|---|---|---|---|
| A 已发假声明守卫 | #171 | `7a3fc6c9` | `src/ai/outbound_promise_guard.py`（`detect_sent_claim`/`strip_sent_claims`、英文句点切句、sent_claim 重写指令+如实兜底）、`src/ai/media_pending.py`（`media_sent_within`/`last_media_sent_ts`）、`src/inbox/autosend_helpers.py`（B 线 `_autosend_deliver` 接线 + `_media_sent_recently` + `_depromise_autosend_text(sent_claim=)`）、`src/skills/skill_manager.py`（**只动** `_apply_media_promise_guard`，stage_hunks 部分暂存；同文件另 4 个他线 dirty hunk 未装载）、`src/inbox/image_autosend.py`（`record_sent_claim_event`）、`src/ai/companion_selfie.py`（EN 索图形态 + EN 物体图护栏）、`src/eval/media_consistency_eval.py`（英文过去时假声明金标）、tests：`test_outbound_promise_guard.py` / `test_companion_selfie.py` / `test_media_consistency_eval.py` / 新 `test_sent_claim_guard.py` | `pytest tests/test_sent_claim_guard.py tests/test_outbound_promise_guard.py tests/test_media_consistency_eval.py tests/test_companion_selfie.py tests/test_media_claim_guard.py tests/test_media_pending.py tests/test_photo_capability.py tests/test_selfie_wiring.py tests/test_scene_state.py` 473 passed；`test_image_autosend.py test_media_promise_trend.py test_autosend_helpers.py` 74 passed | **三层全做（含 A 线半边，skill_manager 当时非 ACTIVE）**。真伪判据＝近 30min 媒体真发账本（不是 media_context），键 `companion.media_promise_guard.sent_claim.{enabled:true, window_min:30}`（未进 config.example——companion 段无人认领但本批不动它，需要时值守补）。观测键 `sent_claim_detected/fulfilled/retracted`（`image_autosend.metrics_snapshot` → autosend-status/metrics）；ops 卡未加行（看板属 J-4/值守）。John 索图原话截图到手后按需再补一条 EN marker（现补的是 send me a photo / show me a pic / can I see you / got any pics / lemme see you 等常见形态）。 |
| B 客服/销售话术漏出 | #175 | `9d6426f3` | `src/utils/persona_guard.py`（`_SERVICE_FRAME_PATTERNS` + `matches_service_frame`；`collect_forbidden/find_violations/sanitize` 增 `service_frame` 开关；整段剥空 → 第一人称中性兜底；英文句点切句）、`src/skills/skill_manager.py`（**只动** `_enforce_persona_consistency` 一处：conversion 域一律开 + 子开关 `companion.persona_guard.service_frame.enabled`）、`config/eval/persona_samples.yaml`（实录原句 + 3 同形态必召回、3 条误伤面）、`tests/test_persona_guard.py` | 12 个 persona_guard 消费测试文件 297 passed；`test_sendpoint_guard.py` 30 passed | **守卫层全做**。来源核实：88MP86 00:50–01:07 **零** `workflow`/`chain`/`goal-inject` 行，01:05:25 是普通 `auto_generate_draft L2` → 企业话术是 LLM 拿账号 SOP（「积极推进产品」）自由发挥，**不是**链话术步指令。第 2 层「SOP 话术步 prompt 钉人设口吻」属 J-2（`workflow_autorun.py`）——钉子文案见下方「交 J-2」。 |
| C 翻译记忆坏译文重验 | #176 | `24229010` | `src/ai/translation_service.py`（`_memory_get`/`_cache_get` 命中先过 `looks_like_engine_refusal`：删记忆行/弹 L1 → 按 miss 重译；`_log_xlate` 出口 INFO；`_refusal_purged` 计数）、`src/ai/translation_memory.py`（`delete`/`delete_many`/`iter_rows`）、新 `tools/xlate_memory_purge.py`（默认 dry-run 只读；`--apply` 才删）、`tests/test_translation_service.py`（7 例） | `test_translation_service.py test_translation_memory.py test_translation_confidence.py test_translation_engines.py test_outbound_translate.py` 136 passed；CLI dry-run 在 tmp 假库跑通（测试内） | **全做**。本机 zhiliao / zhiliao_pilot 两库 dry-run 命中 0 条；skuio 机的记忆库在其 `%APPDATA%\telegram-ai-desktop\data\config\translation_memory.db`，1.0.74 装载后**值守需在该机跑一次** `python tools/xlate_memory_purge.py --db <该路径> --apply`（或等命中路径自愈——每条坏译文第一次被撞到即删+重译）。 |
| D 人工出站进记忆 | #177 | `5d46c4ee` | 新 `src/inbox/human_outbound_memory.py`（第一人称自述抽取 zh/en：`extract_self_facts`，否定/问句/转述/第三人称一律弃权；`filter_grounded_facts` 接地；写 `user_context._human_said_log`（author=human，**持久无 TTL**，cap 12 + 去重）+ `last_reply`/`recent_replies` 按客户**实际看到**的 `sent_text` 更新；媒体只记占位不抽事实；`record_human_outbound(app_state, …)` 路由一行接入，自解析 SkillManager，绝不抛）、`src/skills/skill_manager.py`（**只动** `_inject_self_state` 一处：消费 `human_said_note` 注入「你亲口说过」块，stage_hunks 部分暂存；同文件另 4 个他线 dirty hunk 未装载）、新 `tests/test_human_outbound_memory.py`（40 例：抽取正反例 / 接地 / 持久跨 ContextStore 重载 / sent_text 与原文分工 / 媒体占位 / account_id 归一 / 无 SkillManager 静默 / 静态钉 skill_manager 消费点） | `test_human_outbound_memory.py` 40 passed | **模块+prompt 消费全做，接线未落地**：`unified_inbox_send_routes.py` 属 J-3，意向板 J-3 活跃（1.5h 前登记 LINE media #172/#169/#180）→ 按指令把「插在哪 + 那一行」交 J-3（见下方「交 J-3」）。**接线前该功能零流量**（模块被 import 但没人调用）。窗口口径：`persona_reply` 历史按条数 `limit=30` 取（不看时间），32HB5P/66SFWD 原始 Cursor 报告本地 `tmp_diag/` 目录为空，人工出站条数未能从 backend.log 核出（手动 send 成功路径无 INFO 行）。 |

## 交 J-2：SOP / 链话术步拟稿 prompt 的人设口吻钉子（#175 第 2 层）

建议插入位置：`src/inbox/workflow_autorun.py` 的话术步拟稿指令组装处（`auto_advance` 到点拟稿把 SOP 步骤文案喂 LLM 的那段 system/instruction 尾部；链步 `advance` 同源复用）。钉子文案（zh，随会话语言由 LLM 自行对齐；不要另写英文版避免双源）：

```
【人设口吻硬约束（SOP 推进步专用）】
你是当前人设本人，以第一人称、朋友聊天的口吻自然推进这一步；推进的内容用「我」来做、来讲、来发，不得把动作交给任何第三方。
禁止出现以下组织框架：助理/团队/同事/客服/专员/顾问/客户经理「会联系你/跟进/对接」、「我让人/安排人联系你」、
「开户资料/引导页/设置指南/操作手册/入门流程/几分钟搞定」、「my assistant / our team will reach out / account details /
setup guide / onboarding / your account manager / I'll have someone」。
要给资料就说「我发你」；要走流程就说「我带你一步步来」；一句话说不完就先聊感受再说下一步。
```

守卫层（本批 `9d6426f3`）已在出站兜底剥离这类句子；prompt 钉子是源头治理，两层都要——只靠剥离会把 SOP 步骤的实际内容一起剥掉（整条都是框架腔时只剩中性兜底，推进步等于白跑一轮）。

## 交 J-3：手动 send 成功分支的一行调用（#177 接线）

文件 `src/web/routes/unified_inbox_send_routes.py`，文本发送路由 `_mark_send(cid)` 调用之后（当前 L999，`# #72 终局：sent（…）` 注释之前）。同作用域已有 `platform` / `account_id` / `chat_key` / `cid` / `original_text`（坐席敲的原文）/ `text`（出站翻译后实际发出的文本）：

```python
        _mark_send(cid)
        # #177：人工替人设说的自述事实进 AI 记忆（best-effort，模块内吞异常，零阻断发送）
        from src.inbox.human_outbound_memory import record_human_outbound
        record_human_outbound(
            request.app.state, platform, account_id, chat_key, original_text,
            conversation_id=cid, sent_text=text)
```

媒体（L1297 `record_agent_send(media)`）/ 语音（L1842）成功分支可选同款调用加 `media_type="image"|"video"|"voice"`、`text=""`——只记 `[图片]`/`[语音]` 占位进 `last_reply` 防复读，不抽事实；不加也不影响 #177 主诉。`account_id` 传路由原值即可（模块内按 `generate_inbox_draft` 同口径从 `conversation_id` 归一 `default`）。落地后单独 commit，验证：`python -m pytest tests/test_human_outbound_memory.py -q` + 真机手动发一句「我有个女儿」，下一轮 AI 草稿 prompt 应含「你亲口说过」块（`skill_manager._inject_self_state`）。

## 事故根因备忘（A）

88MP86 22:20–22:27 该会话只有 `auto_generate_draft … level=L2`，零 `[promise_guard]`/`send_media`；22:48:48 一条 64 字入站才触发图链（22:49:17 `send_media origin=auto`）。两道既有防线沉默的原因：`detect_media_claim` 吃 media_context 门控（客户近几条须含媒体名词索图，John 原话不在词表、「I never got it」也无名词）；promise 词表要求 photo/pic 名词，「let me try sending it again」是代词宾语。新守卫不吃语境，真伪对照近窗真发账本。
