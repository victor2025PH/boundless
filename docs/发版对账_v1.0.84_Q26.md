# 发版对账 v1.0.84 — Q-26（会话状态带与自动化风控设置收口 · #301 #302 #293 后续）

> 指令：`docs/指令_Q-26_会话状态带与自动化风控设置收口_2026-09-12.md`。分析底座：`docs/分析_1.0.81首日问题归类与工作台三视角方案_2026-09-12.md` §三 P1-7（conv_state 状态机 + 一条状态带）/ P1-8（设置 IA 收口）/ §四。状态源来自 Q-14B（`ai_last_fail`）/ Q-17（`risk_hold` + handoff_meta）/ Q-18（`agent_yield`）/ Q-24（边车 `inbox_hint` + 七码留痕）/ Q-4（班表 `off_hours_hold_info`）——**只读不写**。总账：`docs/Q批_进度总账.md` Q-26 行。预算 $160。
> 红线：`conv_state` 零写 KV；动作按钮全走既有端点（摘标 / 重试起草 / 接回 / 边车重试 / 托管 PIN）；`unified_inbox.html` 只动 `cs-` 前缀 + 旧 chip 挂接（`stage_hunks --mine`）；不新增内联色（全 `--tk-*` / `--th-*`）；不动守卫与发送逻辑（Q-23 / Q-27）。
> 执行：09-12 下午–。

## 0. 开工基线

| 项 | 事实 |
|---|---|
| 意向板 | `agent_probe -Intent "Q-26 conv_state band + automation-risk settings hub #301 #302 (…)"` → slug `q-26-conv-state-band-automation-risk-settings-hu`；index FREE。同刻 ACTIVE 他线：Q-21（`unified_inbox.html` `xl-`/`lp-`、`reply_settings.html`、`personas.html`、`unified-inbox.css`、`drafts.py`、`lang_catalog.py`）、Q-22（`tts_pipeline`、人设语音页签）、Q-27（`risk_grader`/`adult_grader`/`risk_hold`/`autosend_policy`/`protocol_autoreply`/`drafts`）、conv-model-route SLICE B（`admin.py` / `test_admin_route_inventory.py`）、i18n lang-switch —— 本线只碰自己的 hunk |
| git | HEAD `510a9570`；本线目标文件里 `admin.py` / `test_admin_route_inventory.py` 为他线 dirty → `stage_hunks --mine` |
| 现场 | 会话头五 chip 各说各话：`ay-`（让位）/ `rk-`（风控持有）/ `aif-`（起草失败）/ `ms-`（边车 PIN 黄条 + 失败气泡）；`lp-` 语言 chip **尚不存在**（只有列表侧 `#lp-opt-*`）。`_renderHdrTags` 写到 `#hdr-tags-row` —— 该容器已被 #206 删除（`test_conv_tools_panel_removed_206` 断言不存在）→ **ay / rk 会话头 chip 今天是死渲染**。`/thread` 响应不带 `ai_last_fail`（前端另拉）。`reply_settings.html` 无让位窗控件（60s 写死）、无成人 / 见面默认；暂停摸底在目标 UI（`inbox.goal.dpause.*`）。`personas.html` 成人 `#ad-*` / 见面 `#mp-*` 卡在人设工坊；#301 死链文案 = `rps_rk_ov_adult`「在人设工坊「成人内容政策」调」/ `rps_rk_act_adult_policy` |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| A 服务端 | #301 #302 | （本行随 commit 补 hash） | **新** `src/inbox/conv_state.py`：`compute(store, cid, *, platform, account_id, worker, config, health, now)` 六源只读聚合 → `{will_send, state, tone(ok/warn/danger/muted), reason_code, reason_text_key(inbox.cs.*), until_ts, action(resume/ack/retry/confirm_pin/none), params, sources}`。源：`mode`（`get_automation_mode` + `effective_automation` 封顶）/ `handoff`（「需人工」标 + `get_handoff_meta` level·category·hits + `risk_hold.active_record`；缺 category 走 `risk_grader.classify_reason`）/ `sidecar`（`PlatformSessionHealth.inbox_health` `e2ee_pin_required` → `pin`·`confirm_pin`；messenger 近 24h `recent_failed_outbound` 七码 → `send_fail`·`retry`(message_id)；`stall_since` → `stalled`；`UNHEALTHY_STATUSES` → `offline`）/ `off_hours`（`work_schedule_cfg` + `off_hours_hold_info`）/ `agent_yield`（`worker.agent_yield_state`）/ `lang_plan`（Q-21 `conv_lang_plan:<cid>` KV 若已落地 → `fallback=True`「按人设语言回」会发；否则 `l1_reason.peek==lang_unknown` → `lang_unknown_l1` 稿转人审不发）/ `ai_last_fail`（`ai_fail_marker.get` → `retry`(draft_id)）。优先级 manual > held > sidecar_down > off_hours > deferred > lang_unknown > ai_fail > human > auto；人审 / 多选档不算让位 / 作息外 / 语言（全自动专属噪音）。每源独立 try、整体绝不抛。`unified_inbox_read_routes.py`：`_conv_state_q26(request,cid,platform,account_id)` + **新** `GET /api/unified-inbox/conv-state?cid=`（或 platform+chat_key；cid 反推 platform/account）+ `/thread` 响应顺带 `conv_state`。`test_admin_route_inventory.py` `_ADDITIONS_2026_09_12_Q26_CONV_STATE`（stage_hunks --mine 单 hunk） | **新** `tests/test_conv_state_q26.py` **20 passed**：契约形状 / 枚举闭合；六源各一例（held-tag·ack / held-hold 无标 / pin·confirm_pin / send_fail·retry+message_id / 非七码不算 / off_hours until 09:00 / deferred·resume 23s / lang_unknown L1 变体 / lang_plan 回落变体 / ai_fail·retry(draft) / ai_fail 无稿无动作）；manual 压倒一切；全源同在按链逐级剥离；人审档跳过三源但仍出 ai_fail；只读（写计数不变）；坏 store 不抛；路由接线静态断言。`test_admin_route_inventory` 4 passed |

### 1.1 回访 / 证据

- **#302**（AI 没回原因隐形）：`compute` 把「为什么没回」折成一个状态 + 一个动作；`/thread` 首屏零额外请求可得。
- **#301**（成人政策死链）：本段只建状态源；文案与互链在 C 段。
