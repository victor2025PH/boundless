# 发版对账 v1.0.72 — I-4（地区语气自适应 #40 + AI 自动链引用回复 #37）

> 指令：`docs/指令_I-4_地区语气与引用回复_2026-09-04.md`。总账：`docs/I批_进度总账.md` I-4 行。
> **本条不发版、不重启实例。** `.py` 改动攒批等下个重启窗装载（zhiliao 上次重启 ~11:20 前 4h，冷却 clear）。
> 每完成一项立刻追加一行；未完成项也留一行写「进行中：已改 X，还差 Y，下一步 Z」。
> 下一个账号接手：先读本表逐行核对，再跑 `git -C D:\boundless log --oneline -15 -- engines/chengjie` 确认 commit 真在。
> ⛔ 未碰：翻译引擎栈（#36 前半已交付）、`platform/spoken_style/data/speech_prints.json`、`unified_inbox.html`、`telegram_client.py`；`unified_inbox_send_routes.py` 只 Grep+Read 窗口未改。

## 落点表

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注/剩余 |
|---|---|---|---|---|---|
| D1 口语层地区档 | #40 | `907faa83` | **新** `src/ai/persona_region.py`（SSOT）：zh-CN/zh-TW/zh-HK 三档 profile——TW 语气词 啦/耶/喔/欸/嘛/齁…、句式偏软「有沒有/要不要」、台灣用詞「蠻…的/真的假的/機車」、禁用词 咋/整/牛逼/老铁/贼/忒/得劲/哥们/啥/咱/俺/666/yyds/绝绝子/给力/土豆/西红柿/视频…；HK 檔繁體 + 粵文書寫 係/唔/嘅/咩/點解 + 港式用詞 返工/食飯/巴士/屋企 + 禁普通话虚词 什么/为什么/怎么/没有/的话；**zh-CN 恒空块**。解析优先级 `resolve_region`：人设 `speaking.region` 显式（别名 tw/台湾/zh-hant/hk/香港/yue/cn/大陆 归一）→ `voice_profile.dialect_flavor=cantonese`→HK → 人设居住地 `location` 国家码 TW/HK（复用 `persona_location.resolve_place_with_fallback`，slug/dict/role+background 推断三形态都认）→ 会话「发→zh-tw/yue」铆定（context 已带 `_outbound_lang_pin` 直接消费，否则三元组齐时查 `sendpoint_guard.outbound_lang_pin`）→ 全局 `ai.spoken_style.region` → zh-CN；`_spoken_region` 会话级覆写位预留给前端选择器。禁用词**只观测**（`find_banned_words` 单字「贼/忒」走程度副词上下文正则防「海賊王」误报；`note_banned_hits` 计数+INFO，**不改文本**）。`src/ai/spoken_style_bridge.py`：`system_block(config, role, *, context=None, region="")` 在指纹段之后追加地区块（包缺席仍可注入）、`resolve_region`/`region_block`/`observe_region_output` 三个薄封装、`rewrite_reply(..., context=)` 只做出站观测、`_STATS.l1_region_inject`（TW/HK 才计）、`region_enabled:false` 恒 CN。`src/ai/ai_client.py` 两处调用透传 `context`/`enhanced_context`（L1 注入 + L4 观测）。`src/utils/persona_manager.py`：`speaking.region` 登记 `PROMPT_EXEMPT_FIELDS`（注入归桥接，防双源）。`config/config.example.yaml`：`ai.spoken_style.{region_enabled,region,region_from_location}` 带注释 | `tests/test_persona_region.py` **23 例** + `test_spoken_style_bridge.py` + `test_persona_prompt_chain.py` **56 passed**。验收：TW 人设 → 块含「【地區語氣｜台灣】」+ 語氣詞 + 禁用词清单；**CN 逐字回归**：`system_block(cfg)`（旧签名）== `system_block(cfg, context=CN人设)` == `""`，`l1_region_inject` 不动；`enabled:false` / `region_enabled:false` 全空；地道台灣句 `find_banned_words`==[]、大陸句命中 咋/牛逼/哥们/咱们 | **已装载**（watchdog 12:26:40 重启 zhiliao，两批 .py 均在盘上；启动日志无本线模块报错）。**交 I-2**（可选增强，后端不配也工作）：会话级选择器写 `_spoken_region`——见结束报告「前端落点」。禁用词命中率看 `persona_region.stats().banned_hit`，高了再决定要不要 L4 带负样本重写 |
| D2 AI 自动链引用回复 | #37 | `70af9595` | **新** `src/inbox/reply_quote_policy.py`（纯函数）：`parse_quote_cfg`（`inbox.l2_autosend.quote_reply`，`min_unanswered` 配 1 也钳回 2）/ `platform_allows_quote`（只在编排器拥有账号＝协议 worker 路径才引用，RPA 回落适配器不收 reply_to → 如实不引用；`platforms` 白名单可再收窄）/ `unanswered_burst`（最后一条出站之后的连续入站串）/ `decide_quote`：串 <2 → `single_inbound` 不引用；候选＝有文字、未删、`max_age_sec` 内的入站（媒体无文字不做候选）；相关性＝回复对候选的词元覆盖（CJK bigram / 拉丁词 / 数字），问句 +0.15，`text`×`translated_text`×`reply_alt`（人设原文）多路取最高（跨语会话也算得出）；最高分 <`min_relevance`(0.15) → `low_relevance` 不引用；同分取更早那条。输出 `reply_to={id,from_me,participant,text,sender}` 与坐席手动引用同形。进程级 `stats_snapshot()`（decided/quoted/skipped{reason}/applied/fallback_plain）。`src/inbox/autosend_helpers.py::_autosend_deliver`：`_send_one(_txt, _reply_to)` 首条带 reply_to（单发 + bubbles 分条仅首条，与坐席 `_deliver_bubble_parts` 同语义）；带引用发送抛异常 → 去引用重发一次（目标已删/id 无效不阻塞）；决策链任何异常＝不引用。`config/config.example.yaml`：`quote_reply` 块默认 `enabled:false`。**zhiliao overlay 已置 `quote_reply.enabled: true`**（config.local.yaml，注释保留；需下个重启窗装载 .py 才生效） | `tests/test_reply_quote_policy.py` **25 例** + `test_autosend_helpers.py` **59 passed**；`test_platform_matrix.py` 绿（未动 worker）。验收：① 单条入站 → `single_inbound` 不引用 ✅ ② 连发 3 条（「今天好累啊 / 周末要不要一起去爬山？/ 对了你上次说的那家火锅店叫什么」）：回「那家火锅店叫蜀大侠…」→ 引用第 3 条（id=22）；回「爬山好呀！周末我有空」→ 引用第 2 条；英文三连同判；「我到家了/你吃饭了没/晚上要不要视频」→ 回「还没吃呢…」引用第 2 条 ✅ ③ 编排器不拥有（RPA 路径）→ reply_to=None 普通发送 ✅ ④ 带引用抛 MESSAGE_ID_INVALID → 第二次 reply_to=None 重发成功、`fallback_plain=1` ✅；store 抛异常 → 普通发送 ✅ | **已装载**（同上 12:26 watchdog 重启）；overlay `quote_reply.enabled:true` 热重载生效。`unified_inbox_send_routes.py` 未改（坐席手动引用链本就有）。可优化：把 `stats_snapshot()` 挂进 `/api/drafts/autosend-status`（drafts_routes 归他线，本轮不动）；灰度后按 `skipped.low_relevance` 占比调 `min_relevance` |
| 优化① 观测收口（autosend-status / metrics / ops 卡）+ 优化④ HK 字形硬钉子 | #40 #37 | `d3a41597` | `src/web/routes/drafts_routes.py`：`/api/drafts/autosend-status` 新增 `quote_reply={stats:stats_snapshot(), cfg:parse_quote_cfg()}`；`/api/workspace/metrics` 新增 `persona_region=persona_region.stats()`（两处 try/except 零阻塞）。`src/web/templates/ops_overview.html`：新卡 `regionQuoteSection`「地区语气档 · 自动链引用回复」（`loadRegionQuote` 合并两端点：地区块注入数 / 非 CN 观测数 / 禁用词命中率 / 简体漏出率 / 引用决策·引用率·不引用原因分布·去引用重发；开关开或有数据即显示，零流量也能核「在役」；`CARDS` 登记 `rq` growth 组 + `_REASON_MAP`）。`src/web/i18n_packs/ops_overview_page.py`：`ov2_s_rq`/`ov2_rq_*` zh+en。`src/ai/persona_region.py`：HK `region_block` 追加**字形硬钉子**（逐字繁體 + 明列高频简体特征字 这/那/个/们/说/吗/没/为/什么… 为负样本，含「唔好寫簡體字形」）；`_SIMPLIFIED_MARKERS`/`find_simplified_chars`（仅 traditional 档判）+ `_STATS.script_hit/observed`（命中率分母）；`note_banned_hits` 同时观测简体漏出。`ui-build.txt` bump | `test_persona_region.py` +3（HK 钉子含负样本 / 简体检测只对繁體档 / observed·banned·script 计数）、`test_reply_quote_policy.py` +2（autosend-status 暴露 quote_reply stats+cfg / metrics 暴露 persona_region）、`test_ops_overview.py` 卡三件套；模板/i18n 横切门禁绿（`test_template_bare_fetch_ratchet` 存量红为他线模板，与本改无关） | 模板/i18n/JS 热更即生效；`drafts_routes.py`/`persona_region.py` .py 等下个重启窗（或 watchdog 顺路）装载 |
| 优化②③ 前置：灰度持久账本 + 周读 CLI | #40 #37 | `f1e324a7` | **新** `src/ops/region_quote_gray.py`：`append(kind, rec)` 追加 `logs/i4_gray/{quote,region}_YYYYMMDD.jsonl`（相对 CWD＝实例数据根，与 autosend_shadow_log 同口径；`AITR_I4_GRAY_DIR` 覆写/`off` 关闭；异常吞掉只 DEBUG）；**不落用户原文**（引用只记 quote/reason/score/burst_len/n_cand，地区只记 region/命中词/长度）。`iter_records(kind, days)` 只读遍历；`summarize_quote(cur_min_relevance)`＝引用率/原因分布/low_relevance 0.05 桶直方图/**建议 min_relevance**（low_relevance ≥20 条才给，按有候选决策 25% 引用率分位，只建议下调且 ≥0.1）；`summarize_region`＝每档 observed/禁用词/简体命中率/Top 词/**L4 判词**（observed ≥30 且合计命中率 ≥10% → `recommend_l4_rewrite`，否则 insufficient_sample/observe_only）。生产者接线：`reply_quote_policy.record_decision/record_applied/record_fallback_plain` + `persona_region.note_banned_hits` 各追加一行。**新** `tools/region_quote_gray_report.py`（只读，多实例数据根自动发现，`min_relevance` 现值从实例合并配置读，`--json`）。`tests/conftest.py`：`AITR_I4_GRAY_DIR` 指进程级 tmp（防测试把账本写进引擎根）。ops 卡 note 指路该 CLI | **新** `tests/test_region_quote_gray.py` 9 例（落盘/坏行跳过/off/天窗/阈值建议闸门与只下调/L4 三态判词/两生产者接线且不落原文/CLI 无账本与有账本渲染）；`test_static_asset_paths.py`+`test_eval_runtime_config.py` 17 绿（`logs/` 相对路径＝C 类无害）；I-4 三文件 56 passed | 为什么必须有：本机每天重启 ~8 次，进程计数寿命只有几小时，「灰度一周后调参」永远攒不出样本。实例 `logs/i4_gray/` 在下个重启窗装载 .py 后才开始写；**一周后跑** `python tools/region_quote_gray_report.py --days 7` 直接读「建议 min_relevance」与「L4 判词」。优化②③ 本身仍是数据闸口（未做，等读数） |
