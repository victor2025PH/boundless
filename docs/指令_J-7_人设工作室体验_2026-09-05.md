# 指令 J-7：人设工作室体验与报错（#189 备货 tab · #190 相册 tab · #191 历史质检脚本出错 · #192 页头 UI · #193 /admin/voice-eval 打不开 · #186 用户管理页僵尸会话 ·（#178/#179 若 J-4 未做））

> **预算硬上限 $150。** 正常花费 $60–100。花到 $120 停下写落点表 + 总账。
> 类型：**后台页 UX + 前端报错**，全部在 `personas.html` / 人设编辑器组件 / 后台管理页。报障人 skuio（DC8F，1.0.73），证据＝5 份 Cursor 报告 `tmp_diag\{5BV4U4,K5XHJ2,HKGZ7C,G9DACM,DQVF55}\` + 原图（他说「截图已在群」的那几张在 `D:\chengjie-instances\zhiliao\data\protocol_media\telegram\6834964252_11*.jpg`，按时间找）。
> **优先级：C（#191 报错）→ E（#193 路由失败）→ A（#189）→ B（#190）→ D（#192）→ F（#186）**——先修「坏的」，再修「难看的」。
>
> ⚠ **开工条件：`personas.html` / `persona_routes.py` 当前归 J-4（#178 说明条、#179 幽灵账号行）。** 开工前看 `docs/发版对账_v1.0.74_J4.md`：#178/#179 已 commit → 本条接管这两个文件；未 commit 且 J-4 意向板仍在 → 先做不碰这两个文件的 C/E（`admin.py` 路由 + `crisis`/`voice-eval` 页 + 用户管理页），等 J-4 收工再做 A/B/D。**J-4 没做的 #178/#179 由本条一并做**，落点表写明。

---

## 0. 开工前必读

1. `docs/J批_进度总账.md`（J-7 行 + J-4 行看 #178/#179 状态）。
2. `docs/修复指令_J批_并行_2026-09-05.md`：共同纪律 / 省钱纪律 / 断点续传。
3. `engines/chengjie/CLAUDE.md`「前端『哑按钮』门禁」段：内联 onclick 必挂 window；重复 id 门禁；孤儿引用门禁；`personas` 页历史上删过死代码——门禁 `tests/test_inbox_inline_handlers_exported.py tests/test_template_orphan_refs.py tests/test_template_unique_ids.py` 每次改完必跑。

**读完立即从 C 开工。**

## 1. 红线 + 文件边界

- 提交只用 `git add <明确文件路径>`；一件一个 commit。⛔ 禁 `reset --hard` / `checkout .` / `restore .` / 裸 stash。
- ✅ 你可以改（按开工条件接管后）：`src/web/templates/personas.html`、`src/web/templates/crisis_audit.html`（**只改** #193 相关的跳转，不动内容——内容归 J-8）、`src/web/templates/admin_users*.html`/用户管理页模板（先 `rg -n "活跃会话|admin_sessions|用户管理" src/web/templates`）、`src/web/routes/persona_routes.py`、`src/web/routes/persona_media_routes.py`、`src/web/admin.py`（**只动** voice-eval 路由注册/隐藏与用户会话回收那两处）、`src/web/i18n_packs/persona*.py` / `admin*.py`、`src/web/static/workspace/*.css`、`ui-build.txt`、`tests/**`、`docs/**`。
- ⛔ 不要碰：`unified_inbox.html`、`workflows.html`（J-4）；`goals/**`、`cp-goal.js`（J-2/D1b 仍在改）；`store.py`、`drafts.py`。
- 模板/i18n 热更新直上生产，每步自洽；前端改动双戳（`ui-build.txt` + `?v=`）。

**开工先登记意向板：**
```
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "J-7 persona studio UX: history-QA script error / voice-eval route / stock tab / album tab / header / admin users zombie sessions (#189-#193 #186)"
```

---

## 2. C —— #191 「历史质检」点击行弹「页面脚本出错」

- 原话（HKGZ7C）：人设编辑器 → 预览 tab → 「人设一致性考题」→「历史质检」列表，点某行（90 分 2026/9/4 01:11:32 9/10）→ 顶部红条「页面脚本出错，部分功能可能失效」。
- 这是 dead-click 守卫兜住的 **未捕获 JS 异常**。该页 console 不进 renderer.log；但守卫会 `sendBeacon` 到 `POST /api/telemetry/frontend-error`（`src/web/frontend_error_stats.py`）——**先看 skuio 机的这项计数**：让值守用 88MP86 之后的新包或直接读 ops「🖱️ 前端哑按钮错误」卡（要他机器的数据，值守 remote 一次即可）。同时本地复现：`rg -n "历史质检|qa_history|history-qa|consistency_quiz" src/web/templates/personas.html` 找渲染与点击 handler，看行数据里哪个字段为空/类型不符会抛（时间格式、`score/total` 缺失、`details` 非数组是常见点）。
- 修：handler 全路径 try/catch + 缺字段容错；行内 `onclick` 引用的函数确认挂 window；加 beacon 带 fn 名。
- 验收：构造一条缺字段的历史记录点击不抛；门禁三件绿。

## 3. E —— #193 「页面未能打开: /admin/voice-eval [尝试打开]」

- 原话（DQVF55）：人设工作室顶部弹出该提示。`admin.py` 里 `rg -n "voice-eval|voice_eval" src/web/admin.py src/web/routes` 看路由是否注册、是否被 `_require_role`/uivis（developer_page `dv_uiv_sub`「内部界面默认对坐席隐藏」）门挡住、还是模板缺失。
- 修：路由存在且被角色/可见性门拦 → 人设工作室里指向它的入口应随门一起隐藏（不要留一个必失败的链接）；路由真缺 → 补注册或删入口；`[尝试打开]` 弹层文案带原因（403/404/初始化错误）。
- 验收：从人设工作室点「声音评测」要么打开要么入口不存在，不再弹「未能打开」。

## 4. A —— #189 备货 tab：台词库 / 说话指纹 / 备货就绪度

- 原话（5BV4U4）：「台词库」（仅夜间预合成短句音频缓存，不教 AI 说话，专属 0/共享 11）与「说话指纹」（spoken_style 包未随部署，显示不适用）；「备货就绪度 3/4」因台词库未填卡 75%。
- 背景（别删错东西）：台词库＝`config/prerender_lines/`（CLAUDE.md AvatarHub Phase3：预渲染命中层，**运营/夜间任务在用**，缺口自动入库 `auto_stock` 也在跑）；说话指纹＝`platform/spoken_style/data/speech_prints.json`（Phase spoken_style，桌面包**未随包**）。两者都是研发/运营侧机制，不是用户功能。
- 修：① 台词库整块**从人设编辑器移出**（保留 ops 侧入口：ops-overview「🎙️ AvatarHub 语音」卡缺口行已有「入库」按钮），用户侧只在语音 tab 留一句「常用短句会自动预合成，无需维护」；② 说话指纹：`spoken_style` 包不可用（`spoken_style_bridge` 加载失败）时整行不渲染；③ 「备货就绪度」清单去掉台词库项，改名「上线准备」，只留 档案 / 声音（或选不发语音）/ 相册（仅开启照片能力时）/ 绑定账号。
- 验收：备货 tab 无台词库；就绪度分母随能力开关变化；ops 卡入库入口仍可用。

## 5. B —— #190 相册 tab：试触发在底部 / 325 套表单 / 触发词全空 / 两套保存

- 原话（K5XHJ2）四点。相册注册表逻辑见 CLAUDE.md「每人设相册/媒体主线」（`persona_media_routes.py`、`personas.html` 相册面板，护栏与审计别动）。
- 修：① 「试触发」吸顶（或固定在上传区下方），命中时滚动到并高亮命中卡；② 卡片默认只显缩略图 + 触发词，权重/关闭门槛/多语配文进「高级」展开；③ 上传完成引导填触发词（可选：调既有 VLM 描述接口 `media_probe`/`caption` 生成候选词供确认——若无现成接口就只做引导）；④ 保存统一：卡片改动标脏 → 底部一处保存，删卡片内保存按钮。
- 验收：325 张卡首屏渲染 <1s（虚拟列表或分页）；试触发命中可见；只有一处保存。

## 6. D —— #192 页头：截断控件 / 重复新建按钮 / 90 个标签

- 修：数字下拉给足宽度或改图标 + tooltip；两个「新建人设」保留一个（保留工具栏那个，页头右上删）；标签云默认显高频 10–15 个 + 「更多」展开（或分组下拉）。
- 验收：1024px 宽度无截断；门禁重复 id 绿。

## 7. F —— #186 用户管理页：8 条 admin·master/127.0.0.1 活跃会话 + 术语

- 原话（QEDG4X）：活跃会话 8 条全为 admin·master/127.0.0.1/Chrome，最后活跃 08-29→09-05 每天一条 ⇒ 客户端每次启动新建登录会话、旧会话不回收；「登录：从未」与 8 活跃矛盾；本月用量 5203 无单位；页面没说这是智聊操作员账号而非聊天平台账号。
- 修：① 会话表按 `(user, ua, ip)` 同键回收旧会话（保留最近 1 条）或过期回收（>7 天无活动）；② 「登录：从未」字段口径修正（取会话首次创建）；③ 用量带单位（字符/次）；④ 页头一句说明「这里是智聊操作员账号，不是你的聊天平台账号」；⑤ 技术标注（master/127.0.0.1）折进悬浮。
- 验收：重启客户端 3 次后活跃会话仍 1 条。

---

## 8. 交付 + 换账号怎么接

- 一件一个 commit，带工单号。落点表 `docs/发版对账_v1.0.74_J7.md`；更新总账 J-7 行；清意向板。
- 只跑：`tests/test_inbox_inline_handlers_exported.py tests/test_template_orphan_refs.py tests/test_template_unique_ids.py tests/test_template_window_t_keys_resolve.py tests/test_persona_media_routes.py` + 新建。
- 结束报告：六件逐件；#178/#179 是否由本条代做；#191 的异常字段结论。

**下一个账号的新对话粘这段：**
```
接手 J-7（人设工作室体验与报错：#189–#193 #186）。按顺序做三件事：
1. 读 D:\boundless\docs\J批_进度总账.md 找 J-7 行（并看 J-4 行 #178/#179 是否已做）
2. 读 D:\boundless\docs\发版对账_v1.0.74_J7.md —— 断点
3. 跑 git -C D:\boundless log --oneline -15 -- engines/chengjie 核对 commit
然后读 D:\boundless\docs\指令_J-7_人设工作室体验_2026-09-05.md，从第一件未完成的继续。
personas.html / persona_routes.py 需 J-4 已收工才能改。预算上限 $150（含已花）。
```
