# 发版对账 v1.0.80 — QQ 线（route-1 qq-personal Milky 边车 · 诚实标位 · 真驱动移植）

> 指令：`docs/指令_QQ线_续做_2026-09-10.md`。底座：`engines/chengjie/docs/qq-personal 边车维护手册.md`、`engines/chengjie/docs/QQ个人号接入协议与风险须知.md`、R78 §1/§5。总账：`docs/Q批_进度总账.md`「独立线」QQ 行。预算 $120（真驱动 $70 / 去风险 $30 / 门禁与包 $20）。
> 接手方：先 `git -C D:\workspace\boundless log --oneline -10` 核对本表 commit 真在。A 段随 1.0.80 进包；B 段代码入库但**不加** after-pack REQUIRED（C 段 72 h 挂机没过之前，包里如实是「演示态」）。
> 执行：2026-09-10 20:3x 起。

## 0. 开工基线（20:30，全只读）

| 项 | 事实 |
|---|---|
| 意向板 | QQ route-1 20:30 登记；同板：ops-group alert noise（4.8h，pre-restart）。git 索引空闲；HEAD `6ed9dfe3` |
| 共享文件实况 | 25 个文件 **index CRLF / 工作树 LF**（`git ls-files --eol`：`platform_registry.py` / `product_facts.py` / `unified_inbox.html` / `test_platform_registry_consistency.py` / `test_product_facts.py` 等）——`git diff` 整文件逐行皆异、`stage_hunks` 切不动。处置：五个本线要碰的文件先按字节把 LF 还原成 CRLF（与 index 同），四个 .py 随即 `git status` 干净（= 纯 EOL 漂移、无实质改动），只剩 `unified_inbox.html` 的他人 hunk |
| ⚠ 他线在途异常 | `unified_inbox.html` 工作树（09-10 17:05 写入）比 HEAD **少了 Q-14 #262 的 aif-* 灰标全部代码**（`38707c79` 09-09 已入库：`#aif-chip` / `_aifRender` / `aifRetryDraft` / 体检 `ai_last_fail` 码），4 个 hunk 全是删除——像是某线用旧缓冲整文件覆写。本线**未动**这 4 个 hunk（`stage_hunks --mine "(?i)qq"` SKIP），但热重载意味着 zhiliao 当前正在服务一份没有 Q-14 灰标的模板。归 Q-14 / 微信线自查 |
| 边车实况 | `services/qq-personal`：`server.js` / `ntq/{driver,mock-driver,locate,qq-download}.js` / `test/locate.test.js`；`ntq/qqnt-driver.js` **不存在**；`node_modules` 在位（node v22.16.0）；`NOTICE.md` 已写移植来源（LLOneBot v4.9.4 / LuckyLilliaBot milky），`SOURCE_OFFER.md` 就位 |
| 注册表实况 | `qq.implemented=True`、无驱动维度；`static/platform_registry.json` 同；`product_facts.SUPPORTED_CHANNELS` 含「QQ 个人号（协议登录）」且 caveat 仍写「用户自装 NapCat / LLOneBot / Lagrange」（route-2 口径，route-1 是内置边车） |
| `/help/onboarding/qq` | **不存在**（`onboarding_guides.GUIDES` 只有 douyin / tiktok / payment-cny / wechat_kf / wechat_pc）；指令 A 段所谓「教程面板」按连接弹窗的 QQ 风险须知面板（`_renderQqRiskConsent`）落，E 段建 qq 教程时再带「演示态」条 |

## 1. 落点

| 段 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|
| A 诚实标位 | `54af3f00` | `src/integrations/platform_registry.py`：`PlatformSpec` 加 `driver_state`（`real/mock/none`，空＝按 implemented 推）+ `driver_note`；常量 `DRIVER_REAL/MOCK/NONE`；`qq` 显式 `driver_state=mock` + note「边车已装 · 驱动未接入（演示态）：扫码与收发均为演示数据，尚不能真收发」，`note` 改写为 route-1 内置边车口径；访问器 `driver_state(p)` / `preview_ids()` / `facts_preview_labels()`；`validate()` 三条：三态合法、implemented⇄none 互斥、mock 必带 note；`as_dict` 带两字段 → `static/platform_registry.json` 再生（schema 不变，纯增字段）。`src/assistant/product_facts.py`：「QQ 个人号（协议登录）」从 `SUPPORTED_CHANNELS` 移入新 `PREVIEW_CHANNELS`，事实卡新增【预览中（尚不能真收发）】段（zh/en）并明确「被问能用吗答预览，不说能」；QQ 个人号 caveat 改为内置边车 + 演示态口径（原 NapCat 自装说法删）；`facts_fingerprint` 带 `preview`。`unified_inbox.html`（`stage_hunks --mine "(?i)qq"` 5 hunk / 他人 4 hunk 未卷入）：`_PREG_DRV` 随注册表产物取 `driver_state`，`_qqDriverDemoHtml(pl,cls)` 只在 `qq` 且 `mock` 时出琥珀标「边车已装 · 驱动未接入（演示态）」，挂三处——账号抽屉 QQ 平台组头、每张 QQ 账号卡、连接弹窗 QQ 风险须知面板顶部；产物晚到补一次 `renderDrawerAccounts()`；CSS `.acct-qq-demo*`。i18n `inbox_workspace.py` zh/en `inbox.acct.qq_driver_demo` / `_t`（zh-Hant 随 `zh_hant_auto` 回落简体，生成物未再生）。测试：`test_platform_registry.py` **+1** `test_driver_state_three_way_and_qq_is_honestly_mock`（三态推导 / qq=mock+note / preview_ids / 事实卡 PREVIEW=注册表 mock 叫法且与 SUPPORTED 不相交 / 中英事实卡含「预览」/ JSON 带字段 / 模板钩子 + i18n 键在）；`test_product_facts.py` 映射断言放行预览渠道；`test_qq_e2e_parity.py` 事实卡断言改为「driver_state=mock → 在 PREVIEW 不在 SUPPORTED，否则在 SUPPORTED」（驱动接真后自动切换口径） | `test_platform_registry` + `_consistency` + `test_product_facts` 29 passed；`test_qq_e2e_parity` / `test_wechat_e2e_parity` / `test_i18n_coverage` / `test_workspace_i18n_render` / `test_i18n_zh_hant` / `test_i18n_en_payload_gate` / `test_qq_milky` / `test_onboarding_guides` 444 例中 443 绿，1 红 = `test_copilot_shared_mirror_synced[cp-i18n.js]`（`shared/copilot/i18n/cp-i18n.js` 他线在途未提交，与本线无关）；`test_template_inline_js_syntax` + `test_workspace_template_js_syntax` 4 passed；zhiliao 热态 `GET /static/platform_registry.json` 已返回 `qq.driver_state="mock"`（静态产物即时生效；工作台登录态未取到，账号卡人眼验收待有会话时补） |
| B 真驱动移植 | `dbcc314f` | 全部在 `services/qq-personal/`（GPL 目录）。新 `ntq/qqnt-versions.js`：`SUPPORTED_QQNT`（build → `appRel/entryMain/wrapperRel/api/verified`，44343 / 39038 两行，`verified` 皆空）+ `UnsupportedQqVersion` + `resolveQqntTarget(loc)`（未装 / 表外 / **用户自装 QQ 拒改**三种 throw）。新 `ntq/qqnt-agent.cjs`（QQ 主进程内 agent，零依赖 CJS）：拦 `.node`/`dlopen` 拿 wrapper 导出 → 代理 `NodeIQQNTWrapperSession.create` 与 `NodeIKernelLoginService` 构造 → 包装 `addKernelMsg/Buddy/LoginListener` 旁路 `onRecvMsg / onAddSendMsg / onQRCodeGetPicture / onQRCodeLoginSucceed / onBuddyReqChange`；`API_PROFILES["nt-2025q3"]` 收敛全部内核签名（新 build 另开档不改旧档）；RPC 面 `ping / login_state / qr_start / quick_login(_list) / self_info / send / recall / mark_read / upload_file / file_url / kick / set_group_name / accept_friend`；uin⇄uid 缓存 + `getUidByUinV2` 回落 `getUidByUin`；seq→msgId LRU 5000；只送 slimRecord 出进程。新 `ntq/qqnt-ipc.js`：JSON Lines over 命名管道（win32 `\\.\pipe\zhiliao-qq-<rand>`），随机 32B token 经 **环境变量** 给 QQ 子进程、`timingSafeEqual` 比对，`waitHello` 45 s 超时；本层只记帧类型/方法名。新 `ntq/qqnt-elements.js`：Milky 段 ⇄ NT 元素（text/mention/mention_all/face/reply/image/file；record/video/forward 明确 unsupported），`recordToMilky`（群带 group/group_member、私聊带 friend、未知元素转 text 不丢），`imageMeta`（PNG/GIF/JPEG 头）。新 `ntq/qqnt-driver.js`：`createQqntDriver` = locate → `resolveQqntTarget` → `ensureInjected`（写 `zhiliao_launcher.js` + 复制 agent 为 `zhiliao_qqnt_agent.cjs`、`package.json.main` 改指 launcher、留 `.zhiliao.bak`、幂等、main 不等于表内 entryMain 即拒）→ `AgentServer.listen` → spawn `QQ.exe`（env 带 pipe/token/api）→ `waitHello` → 失败 kill + throw（`driver.js` 捕获回退 mock）；契约 20 方法全实现，未登录统一 `-403`、不支持段 `-400`、异常 `-1` 绝不抛；`stageUri` 把 `file:// / base64:// / http(s)://` 落到 `<runtime>/../zhiliao-qq-media` 再交 agent；`mediaFile(id)` 登记 NT 本地缓存路径。`server.js`：`x_quick_login_list` 先 `refreshQuickLoginList()`；新 `GET /media/:id`（同 Milky 鉴权，只回放驱动登记过的 id）。测试：新 `test/driver-contract.test.js` **17 例**——同一张 `CONTRACT` 表（13 方法 shape）+ `GUARDED`（12 方法 -403）+ 出码/登录/事件流形状，对 **mock 与 qqnt（假 agent）各跑一遍**；qqnt 独有：不支持段不打 agent、**日志不落正文**（正文 `SECRET_BODY_TEXT` 不出现在任何日志行）、版本表三种 throw、`ensureInjected` 幂等与备份、`driver.js` 无 QQ / 表外版本两种回退均带 `driver_reason`；新 `test/ipc.test.js` 2 例真连管道（坏 token 断连、好 token 握手、call/ret、ev、断连 `agent_gone`、握手超时）。`NOTICE.md` 补移植来源逐文件对照表（LLOneBot v4.9.4 `ntqqapi/wrapper.ts hook.ts api/* types/msg.ts` → 本目录哪个文件、保留什么、改了什么）+「当前形态」改为双实现口径；手册 §2 目录 +6 行、§4 改写为「两张表两道闸」+ 六步维护流程、§5 +7 行排障 | `node --test test/locate.test.js test/driver-contract.test.js test/ipc.test.js` **23/23**；`node --check qqnt-agent.cjs` 通过；边车真起一次（`QQ_DRIVER` 缺省 qqnt、`QQ_RUNTIME_DIR` 指空目录）：本机定位到用户自装 QQ 9.9.21-39038（registry）→ `/health` = `driver:"mock", driver_reason:"qqnt-driver unavailable: qq install is user-owned (source=registry); refusing to patch it - set QQ_RUNTIME_DIR or use on-demand download"`，`get_impl_info` 透出 `driver:"mock"`，`/media/nope` 404；Python `test_qq_milky / test_qq_e2e_parity / test_qq_official` 81 passed；`desktop/test/sidecar-launcher + package-layout` 2/2。**真机行为（扫码 / 收发 / 断网重连 / 72 h）一律未验证**——归 C 段，版本表 `verified` 留空即是记录 |

| C 去风险验证 | — | **未开始**：需要一个 QQ **测试号**（老板明示不用主号）+ 一台可装运行时 QQ 的验证机 + 72 h 挂机窗口，三者本轮都不在手上。开工清单已写进手册 §4 第 2–3 步；本机（ZHUJI-117）只有用户自装 QQ 9.9.21-39038，真驱动按红线拒注入，正是 C 段第一步要换成 `QQ_RUNTIME_DIR` 运行时的原因 | 落点：`/health` 三态截图（mock→qqnt→表外回退）+ 72 h 心跳曲线；风控提示即停 |
| D 包与门禁 | — | **按指令不做**（B 未过 C 之前不加 `after-pack` REQUIRED，否则三包全红）。当前包定位＝演示态：`resources/services/qq-personal/ntq/` 会随 `extraResources`（`**/*` 减 test/）带上 `qqnt-*.js/.cjs`，但真驱动在用户机上仍因「无运行时 QQ / 版本表未验证」回退 mock，账号卡如实标演示态 | C 过后：REQUIRED +`ntq/qqnt-driver.js`、核对 lite 包对 QQ 边车的期望 |
| E 对客口径 | — | 等 C | `/help/onboarding/qq` 建页（现不存在）+「QQ 升级后怎么办」+ `howto:onboarding-qq` |

## 2. 待办 / 交回

- Q-14 / 微信线：`unified_inbox.html` 工作树缺 Q-14 aif-* 代码（见 §0），本线未处置。
- E 段：`/help/onboarding/qq` 教程页尚不存在，建页时带「演示态 / QQ 升级后怎么办」两节，`test_onboarding_guides` 的 SLUGS 集合断言随之 +qq。
- C 段开工前提（老板拍板）：测试号 + 验证机；真机上第一件事是把 `SUPPORTED_QQNT` 的 `verified` 从空填成事实，或把该 build 删掉。
- 预算：本轮 A + B 用时约 1 h 10 min（$120 里的真驱动 $70 档内），C/D/E 未动。

## 3. 收工（09-10 21:4x）

- 共享文件：`unified_inbox.html` / `inbox_workspace.py` 走 `stage_hunks --mine "(?i)qq"`，其余 7 个文件动前 `git diff --ignore-cr-at-eol` 为空后整文件 add；两次提交前 `git diff --cached --name-only` 均为空。
- 意向板 `agent_probe -Done`；总账「独立线」QQ 行改写（只动该行）。
- 仍存在于工作树、非本线：25 文件 index CRLF / 工作树 LF（本线只把自己碰的 5 个还原成 CRLF）；`unified_inbox.html` 缺 Q-14 aif-* 4 hunk；`shared/copilot/i18n/cp-i18n.js` 镜像漂移（`test_copilot_shared_mirror_synced` 红）。
