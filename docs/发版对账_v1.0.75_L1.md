# 发版对账 v1.0.75 —— L-1（出站可信度：#210 拆条止血 / #208 问候不编事实 / #194 无响应文案合一）

> 断点续传介质。接手方先 `git -C D:\boundless log --oneline -12 -- engines/chengjie` 核对 commit 列真在，再从「剩余 / 交办」继续。
> 指令：`docs/指令_L-1_出站可信度_2026-09-06.md`；底座 D-L3：`docs/修复指令_L批_并行_2026-09-06.md`；总账：`docs/L批_进度总账.md`。
> `.py` 改动**未装载生产**（不发版不重启，等 `restart_preflight` → zhiliao 装载）；模板 / i18n / 静态 JS 保存即热更（`reply_settings.html` 新行按 meta 特性探测控显，旧进程不露）。
> 花费约 $80（读证据 + 四项 + 出厂关补 + 门禁 + 三次共享树排障）。

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 状态 / 备注 |
|---|---|---|---|---|---|
| A 拆条止血（D-L3：出厂关 · 仅显式换行才拆 · 短回复永不拆） | #210 | `2f1cca8b` + `630835d7`（出厂关补） | `src/inbox/reply_split.py`（`DEFAULT_EXPLICIT_NEWLINE_ONLY=True` / `DEFAULT_MIN_TOTAL_CHARS=80`；`split_reply_parts` 短回复门先于一切模式、显式换行模式行即是条、算法切句降为 `explicit_newline_only=False` 才进的兜底档；**每条出口折叠换行**——拆不出第二条 / 并入末条的多行不再以「一条消息带换行」漏出）、`src/inbox/reply_pacing_settings.py`（FIELDS 新增 `explicit_newline_only` / `min_total_chars`，镜像常量与门禁互钉；`max_parts` 仍不进平台覆写）、三链透传并改发折叠后的单条：`src/web/routes/unified_inbox_send_routes.py` / `src/inbox/autosend_helpers.py` / `src/client/telegram_client.py`（**stage_hunks 部分暂存**，同文件孤儿线「unlimited outbound mode」hunk 未动）、`src/integrations/line_rpa/human_pacing.py`（RPA sentence 模式显式保留算法档）、`src/web/routes/reply_settings_routes.py`（保存双落审计：旁路 jsonl + `audit_store.log("reply_settings_save")`）、`src/web/templates/reply_settings.html` + `i18n_packs/reply_settings_page.py`（开关旁风险提示；高级区「仅显式换行才拆」行，`feat:"bub_nl"` 按 meta 控显；逐句/开关 hint 改口径）、`src/utils/config_manager.py`（`_migrate_bubbles_1075`：合并视图 `per_sentence=true` 且未表态 `explicit_newline_only` → overlay 写 `per_sentence=false` + `explicit_newline_only=true` + WARNING「拆条已按 1.0.75 默认收紧」；**部分暂存**，同文件 L-5 种子缺席 WARNING hunk 未动）、tests：`test_reply_split.py`（D-L3 三件 / 事故原句整条 / 逐句也吃短回复门 / 显式空行与单换行都拆 / 无换行长段整条 / 超长行不重切 / 出口无换行 / 三链透传·消费静态钉）、`test_reply_settings.py`、新 `test_bubbles_migration_1075.py`（skuio 形态迁移 + 幂等 + 已表态不动 + 零 per_sentence 零写盘 + 服务器实例同迁）；顺手修好 `test_unified_inbox_stage1.py` 四个自 08-12 起就坏的 `send_bubbles` 用例（fake 未接 `origin`） | `test_reply_split test_reply_settings test_bubbles_migration_1075 test_bubble_pacing_wiring + stage1 四例` 237 passed（预览 worktree 按 index 态跑）；邻接 `test_reply_single_paragraph test_autosend_helpers test_draft_full_text test_reply_quote_policy test_feature_registry` 通过 | **在树，待装载**。事故原句「I guess you just bring out a different side of me here. But it's still me…」（加权 ≈26 < 80）任何模式下整条单发。**出厂关已落（`630835d7`）**：「谁把 skuio 机的 enabled 打开了无记录」的答案＝2026-07-31 `feature_registry` 把 `inbox.reply_style.bubbles.enabled` 升 A 类基线（`_ensure_baseline` 桌面首启补 `true`）+ `config.desktop.min.yaml` 种子 `enabled: true` + 内测种子 `config.desktop.internal.yaml` `enabled/per_sentence` 双开（播进 `config.local.yaml`）。现 bubbles 降 B 类（默认关、设置页可自助开）、两份种子 `enabled: false` + `explicit_newline_only: true`、内测种子逐句也关；门禁 `test_feature_registry` / `test_baseline_reconcile`（顺手修好 3 个自 8 月基线扩容起就红的既有用例）/ `test_desktop_seed_visibility` / `test_config_defaults_l5` / `test_feature_center_routes` / `test_bubbles_migration_1075`（新增出厂关三处同值门禁）109 passed。L-5 收工后三文件已提交且非 ACTIVE，故自落不再交办。存量机：已显式 `enabled: true` 的 overlay（skuio）**保留运营表态**不改——迁移只关 `per_sentence`，开着也只对 ≥80 加权且草稿显式多行的长回复按行拆、其余整条折叠；要彻底关走设置页一键。 |
| B 工作台按实际发出条数 | #210 | `ecf2a5e2`（前端提示 + i18n 两键随 L-3 `b3a34895`/`19d60567` 整文件提交入 HEAD） | `src/web/routes/unified_inbox_send_routes.py`（`_deliver_bubble_parts` 返回各条 `message_id`，响应 `bubbles.message_ids`，INFO「气泡分条已发 N/M 条」——代码同 hunk 随 A 提交）、`src/client/telegram_client.py`（A 线 INFO「A线已拆 N/M 条发出」，部分暂存）、`src/web/templates/unified_inbox.html`（发送成功 `showHint`「已拆 N 条发出——客户手机与工作台都是 N 条」，一处三行）、`i18n_packs/inbox_workspace.py`（`inbox.send.bubbles_sent` zh/en）、新 `tests/test_bubbles_mirror_per_part.py`（手动链 N 条→N 次发送/N 个 message_id、中途失败只报已发、分条函数不得自写镜像行、编排器 send 镜像文本==实发文本、A/B 线逐条发送+汇总日志、前端键位/词表） | 7 passed | **在树，待装载**。复核结论：三链镜像本就**逐条落行**（编排器 `send` 每次一行带 `message_id`；A 线 `_send_reply` 逐条 `_postsend_mirror_and_record`），store 主键 `conv:platform_msg_id` 不会合并两条不同文本；`store.py` 禁改也加不了 `bubble_group_id` 列，故本项不另造多行，而是钉死不变量 + 补对账面。**「工作台 1 条」未能从代码复现**：82BF95/WMY7A6 日志里那轮只有 1 行「已回复消息」+ 1 行「[轮询兜底] 镜像手机已发消息 mid=88726」，报告里的「第二句」在日志中不存在——可能是 `_send_reply` 第二条被守卫 HOLD 未发、或轮询兜底只看 `top_message` 漏镜像（`_mirror_inbox` 关时的唯一镜像面）。1.0.75 起两处汇总日志可直接对账「实发 N / 镜像 N」，复报即按此判。 |
| D 无响应文案与双提示合一 | #194 | `0448aebf` | `src/web/templates/unified_inbox.html`（**只动** `_doSendMedia` 两处：失败瞬间记 `item.unknownRestart=_mediaBackendUnstable()`；`res==null` 分支撤 toast，只留附件条内联，beacon `media_unknown_restart/net`）+ `_mediaItemHtml` 按 `unknownRestart` 二选一 + `sticker-panel.js?v=20260906a` 撬缓存、`i18n_packs/inbox_workspace.py`（`retry_hint_unknown`→「后台重启中，结果未知…」；新键 `send_result_unknown`「没拿到发送结果（可能是网络或文件较大超时）；已自动核对对话记录，确认没发出再点发送重试」zh/en）、`src/web/static/workspace/sticker-panel.js`（贴纸无附件条，toast 按 `window._mediaBackendUnstable()` 二选一）、`tests/test_media_send_unknown_toast.py` 重写 | `test_media_send_unknown_toast` 4 passed；`test_i18n_coverage` 322 passed | **模板/JS/i18n 已热更上生产**。验收：无响应只出一处提示；不在重启窗（`__wsRestartCool` 空且连接 ok）时文案不含「重启」。`unified_inbox.html` / `inbox_workspace.py` 与 L-3 同文件，均按 hunk 暂存自证通过。 |
| C 问候不编事实 + greeting 分类收紧 | #208 | 代码在 `a781e36b`（L-3 #209 整 index 提交带走）；标记提交 `556d1aec`（空，带完整说明） | `src/utils/proactive_fabrication_guard.py`（新增近况守卫：`is_self_status_claim` 天气/地点·行程/正在做的事三类词表 + 自指判定 + 疑问词开头问句排除；`build_status_evidence` 来源池＝天气源 / 场景状态 / `_persona_bio_block` / 记忆 / 摘要 / 客户本条·上一条·历史 **user 侧**，assistant 侧与 `last_reply` 刻意不收；`persona_status_facts` 取档案 background/role/hobbies/schedule/specific_memories/likes；`strip_status_claims` 句级剥离、类别级有据（天气源→天气、场景→地点活动、档案类别词跨语言）+ 内容 token 宽松重叠；英文句号切句；剥空回落原文 + `all_stripped`；`history_status_claim_note` 历史里 AI 自己有无来源近况 → prompt 说明行）、`src/skills/skill_manager.py`（新 `_apply_status_fabrication_guard`，A 线 5c2d / B 线 9b3 各接一次，子开关 `companion.fabrication_guard.status.enabled` 默认开；`_recognize_intent`：带交易/收据/购买/媒体请求语一律 `direct_chat`，≥20 字陈述句不再兜底 `greeting`；**stage_hunks 部分暂存**，孤儿线 3 hunk 未动）、`src/utils/greeting_lexicon.py`（`has_request_context` zh/en 否定表，`is_greeting_message` 据此拒判；换行符已归一回 LF）、`src/ai/ai_client.py`（`GREETING_NO_FABRICATION_LINE` 追加到陪伴/通用两条问候 prompt；`_build_context_prompt` 陪伴域注入 `history_status_claim_note`——与 FACT_LOCK 不冲突，只禁把随口一说滚雪球）、新 `tests/test_greeting_no_fabrication.py`（14 例） | `test_greeting_no_fabrication test_greeting_lexicon` 14 passed；邻接 `test_proactive_fabrication_guard test_hooks test_inbox_draft_engine test_channel_metrics_intent test_persona_reply test_time_context test_i1_conv_intel_i3_templates test_quick_templates_curation` 210 passed | **在树，待装载**（`skill_manager.py` 当时非 ACTIVE、无 J-10 意向，按指令最后动）。FTK6S7 原句「Just got back from a walk, the rain's light here. How's your day going?」→ 出口变「How's your day going?」；「Send me the receipt」「I want to buy vitamins」→ `direct_chat`。残留：整条回复都是无来源近况（如单句「Just got back from a walk 😊」）时守卫回落原文放行 + WARNING 计数（应答不失语；prompt 硬约束 + 历史说明行把这种形态压到极少）——若装载后仍见 `整条回复都是无来源近况陈述` 告警成规模，下一步做「剥空→按意图一次重生成」。 |

## 给 L-5 / L-4 的知会（不需动手）：D-L3「出厂关」已由 L-1 落在你们辖区文件

`630835d7` 动了 `feature_registry.py`（bubbles A→B，一条 Feature 挪到 B 类段）、`config.desktop.min.yaml` / `config.desktop.internal.yaml`（bubbles 段）。理由：拆条真正的「出厂开」不在 `parse_bubbles_cfg`（缺省本就 False），而在这三处默认值；L-5 收工后三文件已提交、非 ACTIVE，按底座「默认值照 L-5 口径落」直接用了同一载体（A/B 分级 + 种子同值 + 门禁互钉）。L-4 若在功能总览术语里提到「分条」，口径改为「B 类 · 默认关 · 可自助开」。`config.example.yaml` L3412 的 `per_sentence: true` 仅是注释示例，未动。

## 交 L-7：验收口径（回访用）

- #210：「拆条出厂关（新装机默认不拆；基线不再自动打开）；短回复整条发；开着拆条也只在长回复有显式换行时按行拆；工作台条数与手机一致，日志/响应带实发 N」。skuio 机装 1.0.75 首启日志应见一行 `拆条已按 1.0.75 默认收紧`，随后 Sceya 会话新出站为单条；若运营要彻底关，自动回复设置页「启用分条发送」一键。
- #208：「问候不再编天气/行程/正在做的事；『Send me the receipt』类不再当问候秒回」。装载后看 `[fabrication_guard] 无来源近况陈述` INFO 行数与 `整条回复都是无来源近况陈述` WARNING 是否成规模。
- #194：「发媒体无响应只出一处提示（附件条内联）；不在重启窗时文案不含『重启』」——模板已热更，可当天核。
- 顺带 1.0.74 口径照 `docs/发版对账_v1.0.74.md` 原文：#171「AI 不再谎称『已发照片』」/ #175「陪聊人设不再漏客服/销售话术」/ #177「人工接管时你发的话进 AI 记忆」。

## 共享树两次归属错位（流程备忘，给 L-7 / 底座）

1. B 前端提示 + i18n 两键：L-1 09:0x 编辑后未及提交，L-3 `b3a34895`（09:00）/`19d60567`（09:07）整文件 `git add` 带入 HEAD。
2. C 全部 5 文件：L-1 09:48 `stage_hunks --apply` + `git add`，L-3 09:49:12 `a781e36b` 整 index 提交带走（间隔 < 30 秒）。代码以 `a781e36b` 为准，`556d1aec` 为 #208 空标记。
   建议底座补一条：`git commit` 前先 `git diff --cached --name-only` 看有没有不是自己的文件，或统一用 `stage_hunks.py --apply` 的外来暂存拒绝检查（它只在 `--apply` 时查，`git add` 直连不查）。

## 事故根因备忘

- **A**：拆条形态被识破的三个叠加因子——`per_sentence=true`（skuio overlay，来源＝内测种子 `config.desktop.internal.yaml`）绕过 `min_total_chars`；`min_total_chars=24` 加权对英文只有 ≈96 字符；逐句成条使两句英文必成两拍。任何一个都能让 Sceya 那句被拆。
- **B**：三链逐条镜像成立；「工作台 1 条」当轮日志无第二条「已回复消息」，反证第二条要么未发要么走了别的路，报告口径有待 1.0.75 的汇总日志复核。
- **C**：问候 prompt 只有「简短活泼像朋友接话」，无来源约束；FACT_LOCK 把第一次编造钉成后续「事实」；`_recognize_intent` 末尾 ≥20 字无问号兜底 `greeting`。
- **D**：J-4 C 把「无响应」一律按「重启窗」措辞，附件条与 toast 各说一遍。
