# 指令 O-3：目标摸底主动腿（到期未发根因 · 第 1 天就摸 · 注入变硬 · 卡片三计数）

> 预算 **$150**。报告 **HM7XBA / 5XRQ7C**（现挂 #255，O-5 改挂 #236 族——commit 先带 `#236`）。目标版本 1.0.78。
> 底座 `docs/修复指令_O批_并行_2026-09-08.md`。落点表 `docs/发版对账_v1.0.78_O3.md`。**前置**：M-7（`docs/发版对账_v1.0.77_M7.md`）已做「每拍可追溯 / 注入不断档 / 到期结算 / 卡片说真话」，代码在 HEAD 未装机；本条是在它之上，不重做。

## 开工前必读

1. `tmp_diag\HM7XBA\*_report.md`：WhatsApp evelyn(12137839654)×Martin 9/7 14:45 建 3 天「客户摸底」目标；17:21:09 冲刺推进器 enabled=True natural_daily=True；18:42:48 与 22:43:24 `health_watchdog goal_sprint_liveness stalled: auto=3 sent_24h=0 oldest_h=4.0→8.0`；卡片却「已推进 2 拍·下一次≈10:00」；00:11–00:43 该会话 9 次起草零 `goal-inject`/`sprint` 日志，9 条回复全部跟随客户话题；右栏「第 1/3 天·今日：先把互动热起来；顺着 TA 的话回聊」；客户已透露时差与「昨天下雨」，画像坐标仍空、摸底 0/4。
2. M-7 落点表 §一–§五：A `d0933356`（每拍可追溯）B `9ff2e90a`（logger）C `fe6a7861`（到期结算）D 卡片说真话 + `bf8523b8` 对齐——先确认 D 是否已覆盖「主动出手 0 次」红字；没覆盖就在本条 D 段补。
3. 锚点：`src/companion/goals/planner.py`（每日计划生成——「第 1 天先暖场」在这里）、`service.py` L1080–1120 `[goal-inject]`、`sprint_ticker.py` L354/389 `record_*_beat_sent`、`liveness.py`（stalled 判定）、`cp-goal.js` 双树 / 三宿主（`?v=` 版本号要一起动）、`goal_routes.py`。
4. **依赖 O-2**：采集链走情景记忆抽取；O-2 A 未装机前，卡片必须明示「采集不可用（记忆抽取未开）」而不是 0/4 静默。
5. `scripts\agent_probe.ps1` 看 ACTIVE（O-2 在动 `skill_manager.py`，别碰）。

## 红线

- 只动 `src/companion/goals/**`、`cp-goal.js` 双树、`goal_routes.py`、`i18n_packs/goals.py`、tests。
- 每 commit 带 `#236`；`git add` 显式路径。
- 主动出手仍经 M-1 出站三闸与 O-1 A 停联冻结（冻结会话不推目标）。

## A `auto=3 sent_24h=0` 到期未发根因

- 现象：三条自动目标 8 小时零真发，watchdog 两次 stalled，引擎「能发」。
- 查法：按 M-7 A 的每拍日志 `[goal-beat] goal=… due=… decision=… reason=…` 逐拍看 8h 内到底是「没到期 / 到期被安静时段顺延 / 被出站闸 HOLD / 起草为空 / 派发器没跑」——五选一，写进落点表 §〇。常见嫌疑：① 冲刺推进器 `natural_daily` 把「今天已有客户对话」当作「今日已互动」跳过主动腿（客户活跃反而永不出手）；② 到期落在安静时段无限顺延；③ 出站语言五级决策（M-1）HOLD 无日志回写到目标。
- 修法：对症一处 + `liveness stalled` 升 WARNING + 卡片红字「主动出手 0 次（N 小时）」（若 M-7 D 未覆盖）。
- 验收：回放 HM7XBA 时间线 → 到期一拍真发或明确 HOLD 原因写进卡片。

## B 第 1 天就摸 + 客户活跃自适应

- 现象：3 天目标第 1 天计划「先暖场、顺着聊」= 1/3 时间零摸底。
- 修法：`planner.py` 摸底类目标每天 ≥1 个自然摸底问句（第 1 天也要），问句围绕未填槽（陪伴域槽位表 = N-3 B）；客户活跃（30 min ≥5 条入站）时计划自适应提前下一步；日志 `[goal-plan] day=1 probes=1 slots=[坐标]`。
- 验收：新建摸底目标 → 第 1 天计划含 ≥1 问句；tests 两例。

## C 注入变硬 + 线索追问

- 现象：注入是软建议，模型优先答客户；客户提天气 / 时差，AI 也聊天气，坐标槽仍空。
- 修法：`service.py` 注入带 `target_slot`：客户本轮出现线索词（天气 / 时间 / 早上 / 城市 / 工作 / 家人…映射到槽位）→ 本轮**必须**带一个对应追问（prompt 硬约束 + 出站后校验：回复无问号 / 无槽位关键词 → 记 `inject_miss` 并下轮重试）；日志 `[goal-inject] target=坐标 cue=昨天下雨 result=asked|missed`。
- 验收：回放 00:11–00:43 九条入站 → 至少 2 条带坐标追问；tests。

## D 卡片诚实三计数 + 采集状态

- 修法：卡片拆「注入 N 次 / 主动出手 N 次 / 已采集 N/4」三计数（M-7 D 若只有拍数则扩）；采集依赖记忆抽取：白名单空（O-2 A 未装）→ 卡片黄条「采集不可用：记忆抽取未开」；`cp-goal.js` 双树 + `?v=20260908a` + i18n 三语。
- 验收：真浏览器卡片三计数与日志一致；黄条在未修复态出现。

## E 「现在就问一个」

- 修法：卡片按钮 → 预览一条摸底问句（按未填槽）→ 点发即发（经 M-1 三闸 + O-1 A 冻结检查）→ 计入「主动出手」；日志 `[goal-probe] manual=1 slot=…`。
- 验收：点一次 → 会话出一条问句 → 计数 +1。

## 收工

- 落点表 `docs/发版对账_v1.0.78_O3.md`（§〇 五选一根因）；`docs/O批_进度总账.md` 只改 O-3 行；`agent_probe.ps1 -Done`；回访文案交 O-5（给 skuio：根因一句 + 卡片三计数在哪 + 验证：`[goal-beat]`/`[goal-inject] target=` 日志）。

## 开新对话粘贴

```
读 docs/指令_O-3_目标摸底主动腿_2026-09-08.md，按它做 O-3（HM7XBA / #236 族：auto=3 sent_24h=0 到期未发根因 / 第 1 天起每天 ≥1 摸底问句 + 客户活跃自适应 / 注入变硬带 target 槽 / 卡片三计数 + 采集不可用黄条 / 「现在就问一个」）。预算 $150。先读 docs/修复指令_O批_并行_2026-09-08.md 与 docs/发版对账_v1.0.77_M7.md（M-7 已做的不重做），再 scripts\agent_probe.ps1 -Intent 登记，只动 src/companion/goals/**、cp-goal.js 双树、goal_routes.py、i18n goals.py。每个 commit 带 #236 并立刻补落点表 docs/发版对账_v1.0.78_O3.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/O批_进度总账.md 的 O-3 行并 agent_probe -Done。
```
