# 发版对账 v1.0.85 · Q-38 画像禁写人设名 + 摸底进度卡确认钮不被裁（#272 追加 HPS7C3 / E2GXEP）

> 指令：`docs/指令_Q-38_画像禁写人设名与摸底进度卡确认钮不被裁_2026-09-13.md`。工单 **#272 追加**（未立新单；01:21 HPS7C3 / 01:23 E2GXEP 自动挂 #272）。skuio 1.0.84，WhatsApp Mizuki/12084403608 × Jeo/917983336714。总账：`docs/Q批_进度总账.md` Q-38 行。预算 $60。
> 红线：`confirmed` 不动；Q-19 41 + Q-25 40 零改动；不改 `decide_probe_target` / `persona_reply` / Q-20 出站方向；不进 Q-32 美化；本线不重启 zhiliao。

## 〇、事实

| 项 | 事实 |
|---|---|
| 报告 | `tmp_diag/HPS7C3`（摸底卡溢出 + 称呼=Mizuki）；`tmp_diag/E2GXEP`（称呼=Mizuki 专报） |
| 会话 | `whatsapp:12084403608:917983336714`；人设 Mizuki；对方显示名 G M S Jeo new |
| HEAD 核 | Q-19 `name` 短语过；Q-25 FactGate 因入站含 Mizuki 放行；Q-20 只管出站；提示词 L559–567 已写「hi X 不是客户名字」无硬闸。UI：`.gl-slotchk{overflow:hidden}` 裁确认钮 + `.gl-slots-row` 不限高叠探问钮 |

## 一、落点（每段一行）

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| **A 人设名硬闸** | #272 | `b833a0f8` | `reserved_self_names` + FactGate `own_name` / `vocative`；apply / nickname_prefill / LLM 回填 / `_NAME_RE` 同门 | ① Hi Mizuki 不写；② My name is Jeo 写 |
| **B 进度卡确认钮** | #272 | `cdf591a6` | `cp-goal.js` 双树：`.gl-slotchk` overflow:visible、值 `.gl-slot-val` ellipsis、钮 flex-shrink:0、`.gl-slots-row` max-height 滚动、probe 钉外 | 待确认 5 能点确认、不叠探问钮 |
| **C 门禁 + 清洗** | #272 | 本段 | `test_profile_own_name_q38.py` 五例 + E2GXEP 回放 + UI 抽源钉 + `--own-name` 默认 dry-run | Q-19/Q-25 81 绿 |

## 二、验收对照

| 验收项 | 状态 |
|---|---|
| 人设名 / peer_calls_you 不进客户 `name` | 门禁绿（Hi Mizuki / hey Alicia 丢） |
| 呼格 evidence 丢、自称句放行 | My name is Jeo / 我叫Jeo 写；无 persona 不误伤 |
| 确认钮可见、探问钮不重叠 | `.gl-slotchk` 无 overflow:hidden；`.gl-slots-row` 有 max-height；probe 在行外 |
| Q-19 41 + Q-25 40 零改动 | 88 passed（7 新 + 41 + 40） |

## 三、上线备注

- `.py` 须 zhiliao 重启；`cp-goal.js` 热更。本线不重启，攒发版窗。
- 清洗 `--own-name --apply` 待回访核清单。
