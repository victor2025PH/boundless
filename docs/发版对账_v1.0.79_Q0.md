# 发版对账 v1.0.79 — Q-0（值守止血与归位 · 第二轮：#268 #270 #271 #272 #273 #274 #275 #276 #277 → #278 #279）

> 指令：`docs/指令_Q-0_值守止血与归位_2026-09-09.md`（第一轮：#263 #264 #267 #269 #270 A 之一由值守代做）+ 总账 09-11 12:5x 值守行（第二轮口径）。底座：`docs/修复指令_Q批_并行_2026-09-09.md` 去向表 09-11 三行。总账：`docs/Q批_进度总账.md` Q-0 行。预算 $60（第二轮）。
> 本轮**零代码**：对外只经 `tools/duty_reply.py`；台账 `bug_intake.db` 写入先 dry-run 再 `--apply`；额度经官网 console 核销 + 本机 `fulfill_trial` 守护签发。
> 执行：09-11 13:0x–13:3x。

## 0. 开工基线（13:05，全只读）

| 项 | 事实 |
|---|---|
| 意向板 | Q-0 round2 13:0x 登记；**Q-16 / Q-17 两线同刻在途**（0.1h：`unified_inbox.html` xl- hunks / `risk_grader.py` 等）→ 本轮不碰任何 `.py` / 模板；其余 6 条 pre-restart 陈旧 |
| git | 索引空闲；HEAD `05ba6658`（Q-16 / Q-17 指令 + 总账值守行 + 去向表三行） |
| 台账 before | #268 new P2 · #270 fixed P2 · #271 new P2 · #272 new P0 · #273 new P2 · #274 new P2 · #275 new P0 · #276 new P0 · #277 new P0；bug_intake.db = `D:\chengjie-instances\zhiliao\data\config\bug_intake.db` |
| #272 额度事实 | 官网 `GET /api/admin/trial-claims`：skuio fp `DC8F-0935-5EE4-F3D9` 领取单 `96181c05…`（contact `@shiqn1817`，source desktop）status issued、license 1,000,000 字符不过期；**bind_code 从未核销、无 topup voucher**；`fulfill_trial.jsonl` 对该 fp 只有 `issue` 一行无 `topup`。客户端告警 `100,583 / 100,000` = 机上跑的是 100k 档，不是 1M 试用授权（激活状态待核，见 §3） |
| fulfill 线 | 意向板「console i18n + license ledger outbox hooks」15.0h pre-restart 陈旧；`fulfill_trial_service` 守护 `logs\fulfill_trial\trial_fulfill_20260911.log` 每分钟轮询 exit=0（13:04 / 13:05 在跑）→ 可直接走核销 → 守护签发，不手工 `--once` |
| Bad MAC 事实 | JS63M2（09-10 18:31 启动验收包）sidecar 段 `libsignal session_cipher.js:171` `Session error: Bad MAC` ×10 行、同一发送方 `227500391174370`（session 1.0），账号 19892968016；无任何工单 |

## 1. 落点

### A 回访（`duty_reply.py --group neice --receipt`，三条各先 `--dry-run` 守卫 rc=0 再发；product 端点不回 mid，以 `client_msg_id` 为凭，台账 `<数据根>/logs/duty_replies.jsonl`）

| 条 | 单 | 发出 | client_msg_id | 要点 |
|---|---|---|---|---|
| A1 | #275（并 #273 #274 #277①③） | 13:11:23 | `duty-20260911-131122-405a` | 根因（裁剪器 1 token=3 字符截尾 vs 估算器中文 1 字≈1 token → 超额放大 3 倍，人设 / 记忆 / 识图段先被砍 700→22）；已修四点 + 主链 deepseek-flash；**1.0.80 已在 internal 渠道升级即生效**；验法 `[ai] prompt_tokens` 行无 `trimmed` 或 `after ≥ 80%`；四单标 fixed(1.0.80) |
| A2 | #276（并 #277②→#278） | 13:11:27 | `duty-20260911-131126-3074` | Q-16 已立 + 根因（发→X 写进全局默认键、切窗口不回读）+ 修法四点 + **过渡：切窗口先看工具条「发→X」再手发**；#277② 拆为 **#278**（P0，Q-17）+ 修法四点 + 并入的 6 个证据码；原单①③随 1.0.80 fixed；只承诺「随版本号报」 |
| A3 | #272 | 13:11:32 | `duty-20260911-131131-0bbf` | 额度：台账查实赠量从未核销（我们漏了，道歉）→ 已核销 + 签发 100,000 字符凭证，客户端 2 分钟自动轮询入账、会员中心可见；10 分钟未变回一句；续额方式 = 会员中心 → 兑换加量包 / 群里说一声。Bad MAC 拆为 **#279**（P1），请核该号「对方说发了、工作台没收到」 |

守卫：三条均不含配置文件路径 / 不含日期承诺，版本只提 1.0.80；A1 提到 `backend 日志` 是验法不是配置。

### B 台账（`%TEMP%\q0_ledger.py`，dry-run 13:09 → `--apply` 13:10）

| 单 | before | after | fix_note / note 摘要 |
|---|---|---|---|
| #275 | new P0 | **fixed** P0 | 真因 1 token=3 字符截尾 ×3 放大；`4fdd238f` 随 1.0.80：按段裁 + 人设 / 年龄段永不砍 + 历史≥6 + 预算 6000→12000 + 能力自曝硬拦 + 主链 deepseek-flash；验 `[ai] prompt_tokens` 无 trimmed 或 after≥80% |
| #273 | new P2 | **fixed** P2 | 助理腔 = 人设段被裁后回落助手默认口吻 + 同真因 |
| #274 | new P2 | **fixed** P2 | 「不住纽约 / 西班牙日本长大」= 所在地与专属记忆段被裁 + 同真因 |
| #277 | new P0 | **fixed** P0 | ①③ 同 #275 真因随 1.0.80；② 拆出 #278（note 列 8 个证据码） |
| **#278**（新） | — | new **P0** 1.0.79.0 | 自 #277 拆出②：风控高风险判定需公开分级 / commitment:* 误评客户入站 / privacy 提及即中 / risk_hold 保持期每条重打且摘标无日志；要求四点；**去向 Q-17**；证据 `received --diag` ×8：BRGQ6T FRF7Y9 CF5SCB 7M4QQZ NAVHF6 BQ5HBK CC97JP 4C85ZK；事件 `duty_split` |
| #271 | new P2 | **fixed** P2 | Q-15 adult_grader 随 1.0.80：四级 mention/flirt/explicit/pressure，只 explicit（非只标记）/ pressure 才 high+转人工；陪聊缺省软回应+标记，转人工 3 分钟无人接手补软回应；卡片显类别 + 命中词；人设工坊四格 |
| #268 | new P2 | **fixed** P2 | Q-10 B `4bf1092e` 随 1.0.80：夜间 chip 走 setNightMode；手选主题落 `cp_theme_user_set`，`?theme=` 只当出厂默认；定时中手选自动接管可恢复；console `theme change source=` |
| #270 | fixed **P2** | fixed **P0** | 严重度改 P0（客户流失，与 #264 #269 同根）；note 一行；状态不动 |
| **#279**（新） | — | new **P1** 1.0.79.0 | wa-sidecar libsignal Bad MAC 对发送方 227500391174370 反复，入站疑静默丢；核实三点（近 24h 落库 / sidecar 是否重建会话 / 与 #277 E2NMBE 图片是否相关）；证据 JS63M2；事件 `duty_split` |
| #272 | new P0 | new P0 | note：台账查实从未核销 → 13:06 核销 100,000 → 13:07 守护签发；Bad MAC 拆出 #279。**状态留 new**：额度到账要等客户端轮询 + skuio 回，且 JS63M2 本体是 1.0.79 启动验收对照单，未逐项核 |
| 事件 | | | `duty_ledger` 一行汇总（bug_events） |

`record_bug_ticket` 自动判的 severity（#278 P2 / #279 P0）随后按口径 UPDATE 为 P0 / P1；title 取首行；`reporter_version=1.0.79.0`。

### #272 额度动作（官网 console + 本机守护）

| 步 | 动作 | 结果 |
|---|---|---|
| 1 | 本机 admin key（`vendor\website_admin_key.txt`，不入日志）`GET /api/admin/trial-claims` 三查 | 见 §0：issued / bind 未核销 / 无 voucher |
| 2 | console 通道 `x-console-key` 本机无此 key（`CONSOLE_KEY` 只在 VPS `.env.local`，与 ADMIN_KEY 严格分离）→ **经 `ssh vps-bd2026` 在 VPS 本地读 env 调 `127.0.0.1:3000`，key 不离机** | `GET /api/console/trial-redeem` 200：`default_chars=100000`，该单 `bind_redeemed_at=""` |
| 3 | `POST /api/console/trial-redeem {code: BC-X8MB-AWW8, chars: 100000}` 13:06 | `{ok:true, already_redeemed:false, contact:@shiqn1817, chars:100000, pending_voucher:true}`；console 同时把 `@shiqn1817` 归并进集团客户体系（route 内建档逻辑） |
| 4 | 守护下一拍 13:07:02 | `✓ 96181c05… 加量 100000 字符 ref=trial-topup-96181c05…`，`fulfill_trial.jsonl` 新 `topup` 行 fp DC8F；console GET 复核 `topup_ready: true`；admin stats `topup_issued 3→4`、`bind_redeemed 2→3` |
| 5 | 客户端侧 | `health_watchdog._check_trial_claim` 每 120s 轮询 `claim-status` → `_consume_trial_payload` 自动入账（`[trial] 后台轮询已入账客服赠量 100000 字符`）；前提本机 `trial_claim` 状态有 `claim_id` 且未 `exhausted` → 若 skuio 10 分钟后额度未变，转人工路径 = 会员中心 → 兑换加量包（凭证只在服务端，不进群） |

### C 去向表核对（`docs/修复指令_Q批_并行_2026-09-09.md` 09-11 三行 vs DB 附证 + 报告正文）

| 行 | 原文 | 核对结论 | 处置 |
|---|---|---|---|
| Q-16 | KAZ964 / G229PJ / SFFR2H / Z4NFJ7 / D2A7SM（#276） | 五码正文全是手发翻译目标跨会话；DB：Z4NFJ7 附在 #275 下（自动并单按时间就近），其余在 #276 | **不改**（DB 附错不影响去向） |
| Q-17 | FRF7Y9 / BRGQ6T / GXRD67 / CF5SCB / E2NMBE（#277②） | FRF7Y9 / BRGQ6T / CF5SCB 对；**GXRD67 / E2NMBE 是 ③ 识图描述被裁 → 已由 4fdd238f 修，不属 Q-17**；**7M4QQZ / NAVHF6 / BQ5HBK / CC97JP / 4C85ZK 全是 risk_hold 摘标无日志 / 保持期重打 / hits=- 仍打标，属 Q-17 却被列到「已修」行** | **改**：本行 = FRF7Y9 BRGQ6T CF5SCB 7M4QQZ NAVHF6 BQ5HBK CC97JP 4C85ZK → **#278**；GXRD67 / E2NMBE 移「已修」；注明核对来由 |
| 已修不立项 | 223XQN / 7M4QQZ / NAVHF6 / BQ5HBK / CC97JP / CF5SCB / M3GVED / XVQPWF / ST5HRJ / VUDAJP | 223XQN / M3GVED / XVQPWF / ST5HRJ / VUDAJP 对（预算裁剪定量证据，DB 有三码附 #276 下不影响）；五个 risk_hold 码误列（见上）；CF5SCB 原两行重复 | **改**：去五码 + 去重；补 GXRD67 / E2NMBE；补 #275 其余同根附证（TVMYMW TSKFTR PPP8WA F7JXJ2 HUZPCG NEVFPQ）；标回访 13:11 + 台账 fixed(1.0.80) |
| 三行之外 | JECKCD / BNNJWQ（切回全自动不补回未答消息，与 XBGPBN 成对）、3A7ZE5（对表情 / 贴图逐条作答） | DB 附在 #276 下，与发送语言无关，**去向表无归属** | 写进「已修」行尾「未归位（待派）」；本轮不立单（超出三行口径，交总账拍板） |

## 2. 回访清单（本轮已发，后续由 Q-16 / Q-17 / R81 线接）

| 单 | 已发 | 待发（谁） |
|---|---|---|
| #275 #273 #274 #277①③ | A1 13:11 | skuio 升级 1.0.80 后若报 `trimmed`/`after<80%` → R81 线看 `[ai] prompt_tokens` 行 |
| #276 | A2 13:11（立项 + 过渡） | Q-16 收工随版本号报 |
| #278 | A2 13:11（拆单回声） | Q-17 收工随版本号报 |
| #272 | A3 13:11（额度 + 续额方式） | 10 分钟后 skuio 若回「未变」→ 值守核客户端 `trial_claim` 状态；JS63M2 启动验收对照本体未核 |
| #279 | A3 13:11（立单回声） | 待派：wa-sidecar Bad MAC 处置（重建 sender session / 告警） |
| #271 #268 #270 | 本轮不回访（#271 #268 回访文案在 Q-15 / Q-10 落点表 §2 交 R79/R81 线；#270 已 14:12 回访） | — |

## 3. 断点 / 交接

- **admin 列表 `has_topup_voucher` 显示 false 而 console `topup_ready` true、stats `topup_issued` +1**（同一 store 同一时刻）——`/api/admin/trial-claims` 行级字段疑有别的读路径 / 缓存；不影响履约（守护按 `needs_topup=1` 队列工作且已签），交网站线核 `listClaims` 与 admin route 映射。
- **skuio 机跑的是 100k 档而台账 1M 试用授权已签**：`has_license:false` 是否等于「客户端已领走」未核；若 skuio 回「额度仍 100k 级」→ 核 `trial_claim` 本地状态（activated_at）——可能一并把 1M 试用授权补激活，比 100k 赠量大得多。
- **#279 Bad MAC 处置未派**：libsignal Bad MAC 持续 = 双方 session 密钥不同步，重连不自愈；候选 = sidecar 捕获 Bad MAC → 删该 sender session 让对方重协商 + host_alert 一行；与 QQ/WA sidecar 线合并派工。
- JECKCD / BNNJWQ / 3A7ZE5 三码未归位（见 C 表末行）。
- 本轮临时脚本全在 `%TEMP%`（`q0_dump*.py` / `q0_claim.py` / `q0_console.py` / `q0_ledger.py` / `q0_reply_*.txt`），仓库无残留；`q0_console.py` 的 console 通道本机 401（无 CONSOLE_KEY），实际动作走的是 ssh 方案，脚本留作只读模板。
- 成本：≈$25（只读核查 + 三条回访 + 台账 + 一文两表），预算 $60 内。
