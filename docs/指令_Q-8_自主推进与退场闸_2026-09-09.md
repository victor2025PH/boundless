# 指令 Q-8：自主推进与退场闸（第二波：Q-1 / Q-2 / Q-4 收工后开）

> 预算 **$200**。工单 **#264 #263**（B9D8NW / 7C7SV2 / Y9JVHG / MTQG8S / R6YW5D / D5FJE8 / 7KXNHB③；Q-0 拆单后以新单号为准）。目标版本 1.0.79 能带就带，否则 1.0.80。决策 **D-Q5 proactive_topic 陪伴域 B 类默认开**。
> **前置**：`docs/Q批_进度总账.md` Q-1（`service.py` 注入判定）、Q-2（`outbound_humanize` claim 表）、Q-4（`feature_registry`）三行为「收工」。
> 底座 `docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-8 行）。落点表 `docs/发版对账_v1.0.79_Q8.md`。

## 开工前必读

1. `docs/聊天核心能力_三视角方案_2026-09-09.md` §1 Q5 / Q6、§2.1 第 5 / 8 条、§2.2 第 4 条、§3 Q-8 / Q-9。
2. 证据 `tmp_diag\B9D8NW\ 7C7SV2\ R6YW5D\`：客户连续 4 轮夸赞 → 23:26 AI「Alright, better get back to work… you have a good one, dear」，日志 `goal-inject NOT injected reason=no_goal`、无 work_schedule / 收尾注入 → 纯模型退场；今日手建 7 个 profile_discovery 目标，Enrique 不在内；已挂目标的会话 `push=soft → missed → retry` 屡让位；「现在就问一个 → 就这样问」→ `[goal-probe] manual=1 … decision=skipped text=''` 被 `contact_budget` 拦（人工动作走了自动关怀派发器）。
3. 代码事实：`feature_registry.py` L341 `companion.proactive_topic.enabled` C 类「AI 主动外呼=账号风险行为，不随安装包默认开」；`goals/planner.py`（O-3 B 第 1 天 ≥1 问句 / 活跃自适应）、`templates.py`、`signals.py`；O-3 E `goal_routes.py` `probe/send` 经 `care send_now`（`docs/发版对账_v1.0.78_O3.md` E 行）；Q-1 落点表（注入分级钩子位置——Q-1 收工时会写在总账）；Q-2 的 `CLAIM_KINDS`（你加 `exit_claim`）；`companion_proactive.py`、`sprint_ticker.py`（O-3 A `due_daily` 口径）。
4. `scripts\agent_probe.ps1` 看 ACTIVE。

## 红线

- 只动归属矩阵 Q-8 行；`autosend_worker.py` / `drafts.py` / `persona_reply.py` 不动（要注入的内容经 Q-6 的 addenda 接线点，写总账让其加或自己加一行调用——以 Q-6 落点表为准）。
- 主动出手仍受：停联冻结（O-1 A）/ risk_hold（Q-3）/ 节奏（O-1 D）/ 预算（只提醒不拦人工，见 G）。
- `git add <显式路径>`；每 commit 带 `#264`。

## A 账号级 / 人设级默认目标

- 目标页新区「新会话默认目标」：设一次「客户摸底（字段…）/ 关系推进」→ 自动挂到该账号（或该人设绑定的全部账号）新会话；存量一键批量挂（预览 N 个会话，默认不勾已停联 / 自聊 / 需人工）；日志 `[goal-default] account=… attached=n`。

## B 关系阶段自带推进计划

- `planner.py`：无手建目标的会话也有「阶段计划」（初识 = 摸底 + 话题投放；试探升温 = 回调细节 + 轻逗弄；稳定 = 仪式 + 倾听）——阶段来自 intimacy_engine 既有判定；计划只产出「今日意图」注入，不建目标行；卡片右栏显示「阶段 · 今日主线」。

## C 注入分级 must（在 Q-1 三态之上）

- 客户本轮无新信息（纯附和 / 夸赞 / 单表情）→ 本轮注入 `mode=must`：必须带出一个 unknown 槽问句或话题（受 Q-1「已知即不问」与每槽每日上限约束）；`missed` 连续 2 次自动升 must 一次；日志 `[goal-inject] level=soft|must reason=no_new_info|missed_x2`。

## D 退场闸 `exit_claim`

- `outbound_humanize` `CLAIM_KINDS` 加 `exit_claim`（gotta go / get back to work / have a good one / talk later / heading out / 我先忙了 / 回去工作了 / 下次聊）：除 ① work_schedule 到点 ② 客户先告别 ③ 用户开启「留白策略」外一律改写为 问问题 / 投话题（模板池）；合法退场必须留钩子（「等我忙完给你发 X」）并在关怀排一条真实后续（`care_dispatcher` 入队）；日志 `[exit] conv=… allowed=0|1 reason=…`。

## E `proactive_topic` 陪伴域 B 类默认开（D-Q5）

- `feature_registry` C → B（陪伴域 baseline=True；销售域仍 C）；触发：客户连续 2 轮无新信息即投人设话题库新话题（含摸底问句，受 Q-1 约束）；受节奏 / 预算 / 停联 / risk_hold 约束；日志 `[proactive_topic] conv=… topic=… reason=no_new_info_x2`。

## F 当地时间 + 日程 + 借口限额

- 生成侧注入人设当地时间与今日日程（经 Q-6 addenda 接线点）；「借口」只能从日程取；同客户「工作」类借口每日 ≤1（出站计数，超则改写）；夸赞处理策略「接住 + 反投问题」进底稿一句（与 Q-2 E 同文件，先看它已否加）。

## G 「现在就问一个」直投（原 Q-9）

- `goal_routes` `probe/send`：不进派发器、不受 `contact_budget`；预览即所发（`text` 不得为空且可编辑）；自动选路：客户 10 分钟内活跃 → 作为 `mode=must` 硬注入下一条回复，沉寂 → 直接单发（经 O-1 A 冻结 / Q-3 risk_hold / 节奏）；同步返回并卡片「已发出 hh:mm」；预算只提醒（按钮灰显 + 人话「今天已主动联系 TA N 次 · 仍要发」）；失败原因人话映射；与 N-1 A「立即发」共用直接投递路径。

## H 冲刺腿再验

- 回放 HM7XBA 时间线在 1.0.79 代码上：到期一拍真发或明确原因；watchdog 口径与 O-3 A 一致；写落点表一段。

## 收工

- 落点表 `docs/发版对账_v1.0.79_Q8.md`；`docs/Q批_进度总账.md` 只改 Q-8 行；`agent_probe.ps1 -Done`；回访文案交 R79；门禁「5 轮纯夸赞不退场且带出摸底问句」交 R79。

## 开新对话粘贴

```
读 docs/指令_Q-8_自主推进与退场闸_2026-09-09.md，按它做 Q-8（#264 #263：账号级/人设级默认目标自动挂新会话 + 存量批量挂 / 关系阶段自带推进计划产出今日意图 / 客户无新信息或 missed×2 → 注入 mode=must（受 Q-1 已知即不问约束）/ outbound_humanize 加 exit_claim 退场闸、合法退场留钩子 + 关怀排后续 / proactive_topic 陪伴域 B 类默认开 / 当地时间 + 日程注入 + 借口每日 ≤1 / 「现在就问一个」直投不进派发器、预览即所发、预算只提醒、与立即发同路 / 冲刺腿再验）。预算 $200。前置：docs/Q批_进度总账.md 的 Q-1 Q-2 Q-4 三行须已收工，先读它们的落点表与 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与 D-Q5，再 scripts\agent_probe.ps1 -Intent 登记；autosend_worker.py / drafts.py / persona_reply.py 不动（注入经 Q-6 的 addenda 接线点）。每个 commit 带 #264 并立刻补落点表 docs/发版对账_v1.0.79_Q8.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-8 行并 agent_probe -Done。
```
