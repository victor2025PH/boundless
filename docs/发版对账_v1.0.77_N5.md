# 发版对账 v1.0.77 — N-5（值守收口：并单 · 钧机取证脚本升级 · 两包定性 · 自动立单去重 · 1.0.77 装载→两包三渠道→装机→台账→回访）

> 指令：`docs/指令_N-5_值守收口_2026-09-07.md`。底座：`docs/修复指令_N批_并行_2026-09-07.md`。总账：`docs/N批_进度总账.md` N-5 行。预算上限 $100。
> 接手方：先 `git -C D:\boundless log --oneline -20` 核对本表 commit 真在；再看「断点 / 交接」节从未勾的一步继续。任何写台账先 dry-run；对外只走 `tools/duty_reply.py`（文件走 `tools/duty_send_file.py`）；不改业务代码。
> 执行：2026-09-08 04:45 起。

## 0. 一句话

1.0.77 已于 09-08 07:30–07:36 三渠道发布（公共 clean 496 MB / 内测 smart 591 MB / lite 524 MB）+ 官网部署 + 坐席三台 1.077 + 群播报 mid 1335 / 频道 msg 155；zhiliao 05:58 装载（20s 就绪，0 ERROR）；全量回归 88 红 / 26040 绿（对 M-6 新红 12 → 串行 4 条稳定红全是门禁 / 他线在途，0 功能性回归），门禁 8 红 = 7 老债 + 1 代补已绿。C 段：TAZ9QS 坐实 #215 = 2MB 闸 → fixed；#250 新包 TWJ7C8 补登记。B 段：钧机 zl_collect v1.3.1 + AGENTS v1.3.1 文件 mid 1331 / 1332 明说覆盖（selfcheck 回码未收）。D 段：去重三档规则 + 抬严重度 + 按报告时间排序，顺带修掉 watch 水位字母序截断的生产故障（两份报告卡 29 分钟）。**A 段老板点头后 11:26–11:31 落地：并 7 / 关 1 / 改挂 1，8 单标 fixed，回访 skuio mid 1340 / 1341 + 钧 mid 1342，记账 9 单，#236–#251 全部收口（§4.1）。** 事故一起：自检误触 `purge-payment-seeds` 删了 zhiliao 19 条停用的系统预置支付话术（零对客影响、02:40 备份可恢复，§6）。

## 1. 开工基线（04:45–04:58，全只读）

| 项 | 事实 |
|---|---|
| 意向板 | N-5 04:45 登记；同板 ACTIVE：DY P1 骨架（0.8h）/ QQ round 3（1.7h）/ DY P0 round 2（1.9h）/ 实施97 微信线（文件 15–30 min 前刚存盘，**未登记意向**）；N-1…N-4 意向未 -Done 但总账行均「收工」。git 索引空闲；HEAD `b5eea0ae`；工作树 38 个改动 + 20 余个未跟踪（全是 DY / QQ / 微信三线在途，N 批文件零脏） |
| **接手 M-7 已收工** | N 批总账「接手 M-7」行仍写「未接手」是**过期信息**：`docs/M批_进度总账.md` M-7 行 09-08 02:3x 已「收工」，落点表 `发版对账_v1.0.77_M7.md` §2 / 一~五齐（D `eeec70f1` + 对齐修正 `bf8523b8` + skuio 三启取证 §五），意向板已 -Done。**E 段前置条件（五线收工）实际已满足** |
| 台账 #236–#255 | #236–#249 全 `new`（#242 版本确认 / #244–#249 = #243 七连发）；#250 `confirmed`；#251 `new` P1（钧 22:07 贴文本重复）；**新到 #252–#255**（skuio 09-08 00:51–02:05，四单：#252 Vanessa×Sinue 客服腔 / #253「补证」同实体 2 分钟后又立一单 / #254 #255 同分钟 em dash 两单，#255 P0）——不在 N 批范围，见 §七；库 max id 255，fixed 186 / closed 43 / new 20 / confirmed 2 / verified 4 |
| 报告归属（`tmp_diag/<码>/ticket.txt`） | B5TAA3→#243 new；G7KEUT→#244、GGUNG4→#245、NRTDJH→#246、WZPMXF→#247、8VGQDM→#248、RGPTQF→#249 全部「无 #N / 无已知码 / 30 分钟内无同主题单 → 新立单」；TXP5G3 / 5CM8QB→#245、FHSZTA→#249（关联码挂到了错的子单）；MJGHKQ→#232（自动链按「关联 UE7VM3」误挂，其 note 首句就是 #237 的 `_esc is not defined`）；VC9ERR = `selfcheck` 报告（无 note，非报障，自动链本就 skip）；TAZ9QS / TWJ7C8 无 ticket.txt（远程 diag 包） |
| 两包 | `TAZ9QS` 09-07 17:41:58 = `remote-request:dr-mtpwnp9h-wac8zc`（M-6 等了一夜的钧 #215 包，17:41 就到了）；**`TWJ7C8` 09-07 22:33:57 = `remote-request:dr-mtrcb0iy-svzop9`**（#250 包，请求 22:32 后 2 分钟即到）——取证台账都没记 `received`，`duty_evidence list` 一直把 #250 报成「在途 2h+ 第二次追问」。N-4 落点表 §〇 已用两包定性 |
| 钧机脚本 | 群 mid 1319（22:07）报告头 `zl_collect v1.2.2`；仓库 `deploy/field-agent/zl_collect_jun.ps1` 头注 v1.3.1（09-04）；skuio 机报告头全是 `v1.3.1`（VC9ERR / MJGHKQ …）→ **skuio 已升，不用管** |
| 群水位 | 09-07 22:34 mid 1320 钧 #250 受理回复后钧无回话；09-08 00:51–02:13 自动回执 mid 1321–1330（12 份报告 → #252–#255 + 挂 #183 / #201）；**04:51 新到 3HNCJ7 / 47KNBV 两份**（自动链在跑，回执待核） |

## 2. C —— 两包定性（04:56 落地）

### #215（TAZ9QS）：坐实 a 支 = M-3 A「全局 2MB 闸」，`confirmed → fixed`

判定按 `发版对账_v1.0.76_M3.md` §钧 #215「包到手判两处」：

| 判据 | TAZ9QS 事实 |
|---|---|
| 日志窗口是否盖住 09-06 13:39 | `logs/app/backend.log` 1957 行覆盖 09-05 00:18 → 09-07 17:41；13:37:28 1.0.75 首启（pid 18684）→ 13:38:11–13:44:17 连续有 gap-probe / `[xlate]` / 拟稿 / `[tts]` 行，**13:39:16–18 三条 `[xlate]`** 即用户当刻在工作台操作；窗口完整无断 |
| b 支（WA 重连窗） | `wa-sidecar.log`：05:37:26Z 边车起 → **05:37:38Z `WA connected` 66803566865** → 05:38–05:45Z 零行（无 closed / reconnect）。13:39 时 WA 已连上 1.5 分钟且稳定 → **b 支排除** |
| a 支（请求没进路由） | 全包**零 `[send-media] request`**、零 413（旧闸在中间件层掐连接，路由零记录是它的指纹）；对照 09-05 00:53:12 同机同号 `[orchestrator] send_media dispatch whatsapp:66803566865 … type=image` → 00:53:17 `delivered=True mid=3EB0741A…`（<2MB 图片走得通） → **a 支坐实** |
| 文件大小 | 包里看不到（群图 1255/1256 附件条只有文件名）；但只有 >2MB 才会走「零记录 + 结果未知」这条路，手机原图 + `测试视频.mp4` 符合 |

落地：`tools/duty_ledger_fix.py --mark-fixed-file tmp/n5_mark_fixed_215.json`（dry-run `tmp/n5_mark_fixed_215_dryrun.txt` → apply `…_apply.txt`）：**#215 confirmed → fixed**，fix_note 300 字内（原文见计划 JSON）+ body 追加 816 字定性 note；`notify_ts` 保持 0 进汇总回访池（钧那份 1.0.77 回访一并说，见 §六）；`ledger_fix` 事件 1 条；「fixed 且未回访」61 → 62。`tmp_diag/TAZ9QS/ticket.txt` 补一行 → #215（09-07 22:3x 取证台账已记 received，本次只补索引）。

#215 ② 语音矛盾归 #250 / N-4：TAZ9QS 13:43:16 / 13:44:00 / 13:48:57 三次 `[tts] 合成生效 backend=avatar_clone voice=zh-CN-XiaoxiaoNeural 来源=人设mizuki` + `[voice/tts-test] 有声终审 speech=voiced basis=transcript+energy` ×3，与 N-4 §〇「两包全程 voiced、红条是 CSS 架空 hidden」一致。

### #250（TWJ7C8）：登记「包已到，N-4 已用」

`tmp/n5_c_250_received.py --apply`（默认 dry-run）：#250 note 追加 348 字（包 22:33:57 已到、台账漏记致误报在途、N-4 §〇 定性结论、修复随 1.0.77）；`duty_evidence received 250 --diag TWJ7C8` → `check 250`＝「证据：已到（TWJ7C8）」；`tmp_diag/TWJ7C8/ticket.txt` 一行 → #250。`duty_evidence list` 现「无在途证据请求」。#250 状态保持 `confirmed`（E 段随 N-4 落点表口径标 fixed）。

## 3. B —— 钧机取证脚本升级（04:59 发出；等 selfcheck 回码）

- 发文件走的路：**`POST /api/unified-inbox/send-media`（M-3 A 裸流入口，body=文件本体、元数据走 query）**，09-04 mid 1157/1158 同路（当时 `.ps1` 改 `.txt` 发，caption 写「下载后改名」——`.ps1` 在出站内容守卫 `media_guard.DANGEROUS_EXTS` 黑名单里，改名是既定做法）。本次收口成工具 **`tools/duty_send_file.py`**（新）：token / 群别名 / 幂等键 / 台账与 `duty_reply` 同源 import；发完按值守 SOP 拨回 `auto_ai`（send-media 同样触发「接管即静音」，报障群留在 manual 自动回执链就哑了——发后 GET 核 `mode=auto_ai` ✓）；每次落 `duty_replies.jsonl` 一行 `path=unified-inbox/send-media`。
- **mid 1331** `zl_collect_jun.ps1.txt`（12,336 B，= 仓库 `zl_collect_jun.ps1` v1.3.1 逐字节；caption 明说「覆盖 C:\zhiliao-agent\zl_collect.ps1，你现在跑的是旧版 v1.2.2，直接盖掉不要两份并存」）；**mid 1332** `AGENTS_JUN.md`（5,948 B；caption 明说「覆盖 C:\zhiliao-agent\AGENTS.md」）；说明文本 `duty_reply --ticket 250 --receipt`（`bug-intake/reply` ok，bot 链 mid 待镜像核；原文 `engines/chengjie/tmp/n5_jun_files_text.txt`：三步覆盖 + 「对 Cursor 说跑一次 selfcheck、把 6 位码发群、报告头写 v1.3.1 就算接通」+ #250 已定性随 1.0.77）。
- `AGENTS_JUN.md` 改动（本条，随发）：标题 v1.3 → **v1.3.1（2026-09-08）** + 头注「v1.3.1 变化」三条（`[tts]` 一词抓全 / `-Since` 必须盖住出问题那一刻 / 脚本重试与 `reupload`）+ 「本手册与脚本都是覆盖旧文件，不是新增」；§3.2 的 `[tts]` 关键词与 §3.3 时间窗规则是 N-4 E `42714553` 已写好的，本次只加版本头。`zl_collect_jun.ps1` 本体**未改**（仍 09-04 v1.3.1；M-6 / M-7 交办的 renderer 过滤词 `[send-media]`、收 `run_sentinel.json`、boot 窗不过滤 → v1.3.2 交下一班，见 §七）。
- 钧 selfcheck 回码：**未回**（05:0x 发出，他大概率在睡；收工前再核一次群水位，回了就在本节补码 + `duty_evidence` 记）。

## 4. A —— 并单与关单（dry-run 05:03 已出；**apply 等老板点头**）

脚本 `tmp/n5_a_merge.py`（默认 dry-run，`--apply` 才写；形态照 bug_intake 自己的并单口径 `dup_of=主单 + status='closed'` + `bug_dup` 事件，与 `test_bug_intake::test_e2_ticket_for_reply_follows_dup_of` 同形；主单 `report_count` 累加；被并单 body 追加去向；报告码 `ticket.txt` 追加新归属行）。dry-run 全文 `engines/chengjie/tmp/n5_a_merge_dryrun.txt`：

| 动作 | 单 | 说明 |
|---|---|---|
| 并单 ×7 | #244 #245 #246 #247 #248 #249 → `dup_of=243` closed | 七张全是 skuio 15:37–16:55 对同一件事的「核实 / 复查 / 第二次复查 / id=1 核实」跟进；主单 #243 note 一行列出九个报告码（含曾挂 #245 的 TXP5G3 / 5CM8QB、曾挂 #249 的 FHSZTA），这九个码的 `ticket.txt` 各追加一行 → #243 |
| | #251 → `dup_of=250` closed | 钧 22:07 贴回群的 v1.2.2 报告文本（mid 1319）被重复登记 |
| 关单 ×1 | #242 new → closed | 6435AY「新版本更新后确认版本号与启动阶段」= 版本确认，非缺陷；事件 `ledger_fix` |
| 改挂 ×1 | MJGHKQ：#232 → #237 | #237 追加 `[报告 MJGHKQ 09-07 13:22 …]` note（截 380）+ 改挂说明；#232 追加「上一条是 #237 的证据、误挂已改、本单结论不变」；事件 `duty_reattach`；`duty_evidence received 237 --diag MJGHKQ`；`ticket.txt` → #237 |
| 不立 | VC9ERR | `selfcheck` 报告（无 note、非报障），自动链本就 skip，无动作 |

apply 后 #243 / #250 各成主单，`_resolve_dup` 链保证旧回执里的 #244–#249 / #251 仍能被 `ticket_for_reply` 跟到主单。「建议追加」#253 → #252、#255 → #254 抬 P0 已由 **O-5 08:30 落地**（`bug_dup` ×2 + `#252 P2→P0`，并另立 #256 / #257），本条不再重复。

### 4.1 A 段 apply 落点（09-08 11:26–11:31，老板点头后）

| 步 | 时刻 | 事实 |
|---|---|---|
| 并单 dry-run 复跑 | 11:25 | `python tmp/n5_a_merge.py` → `engines/chengjie/tmp/n5_a_merge_dryrun2.txt`，与 05:03 存档 **sha256 逐字节一致**（`57DB717C…`）；`status_check_0908.py` 复核 #236–#251 状态与 05:03 基线一致（全 `new` / #250 `confirmed` / 零 dup / 零通知；#236 08:30 被 O-5 追加过 #257 关联 note，状态未变） |
| 并单 apply | 11:26:44 | `--apply` → `n5_a_merge_apply.txt`：**并 7**（#244–#249 → `dup_of=243 closed`；#251 → `dup_of=250 closed`；`bug_dup` 事件 7 条；#243 `report_count` 1 → **7**、#250 1 → **2**；两主单 body 各追加去向 note）/ **关 1**（#242 `new → closed`，`ledger_fix` 事件）/ **改挂 1**（MJGHKQ：#237 body += 报告 note 截 380 + 改挂说明、#232 body += 误挂说明、`duty_reattach` 事件、`duty_evidence received 237 --diag MJGHKQ` → 「#237 证据到手」）；9 个报告码 `ticket.txt` 各追加一行 → #243（G7KEUT GGUNG4 TXP5G3 5CM8QB NRTDJH WZPMXF 8VGQDM RGPTQF FHSZTA）、MJGHKQ → #237；VC9ERR 无动作 |
| 标 fixed dry-run 复跑 | 11:27 | `duty_ledger_fix --mark-fixed-file tmp/n5_mark_fixed_1077.json` → `n5_mark_fixed_1077_dryrun2.txt`，与 06:06 存档 **sha256 一致**（`EA98084B…`） |
| 标 fixed apply | 11:27:15 | `--apply` → `n5_mark_fixed_1077_apply.txt`：**8 单**（#236 #237 #238 #239 #240 #241 #243 `new → fixed`、#250 `confirmed → fixed`），`ledger_fix` 事件 8 条 `(no-notify, summary pending)`，`notify_ts` 保持 0；「fixed 且未回访」62 → **70** |
| 回访 dry-run | 11:27:54 | 三条 `duty_reply --dry-run` 全过红线闸（skuio p1 / p2 `--no-mention`；钧 `--ticket 250 --receipt`）。**p2 改了一句**（本条改，未另请示）：原「#252–#255 … 已登记排下一批，定性后按单回」——O-5 已于 08:34 按单回过 #252 / #254 / #256 / #257 去向，照发会与群里已有回复打架 → 改为「#252–#257 … 不在这版，去向与验法见 08:34 四条按单回复，不重复」；其余两条原文未动 |
| 回访真发 | 11:28–11:30 | skuio p1 → **mid 1340**（`unified-inbox/send`，11:28:14，1110 字）；skuio p2 → **mid 1341**（11:29:47）；钧 → **mid 1342**（`bug-intake/reply` bot 链，11:29:52；bot 链不镜像进 inbox.db，mid 由 11:30:13 `seq_gap_own own=1340,1341 last=1342` 事件推得——期间群内只有这一条出站）。#250 body 已回写 `[值守回复] …` 全文；`duty_replies.jsonl` 三行 `ok=true`（`n5_reply_skuio_p1.txt` / `_p2.txt` / `n5_reply_jun.txt`）。配额：skuio 2 / 钧 1，与计划同 |
| 记账 | 11:31 | `duty_notify_summary --reporter 8942577244 --msg-id 1340 --ids 236,237,238,239,240,241,243 --apply` → **已记账 7 单**；`--reporter 8852939166 --msg-id 1342 --ids 215,250 --apply` → **已记账 2 单**（`notify_note=summary:20260908 mid=…`）；「fixed 且未回访」70 → **61**（`n5_notify_summary_skuio.txt` / `_jun.txt`） |
| 核对 | 11:31 | `tmp/status_check_0908.py` → `engines/chengjie/tmp/n5_status_check_after.txt`：#236 #237 #238 #239 #240 #241 #243 #250 = `fixed` + 通知=有；#242 `closed`；#244–#249 `closed dup=243`；#251 `closed dup=250`；库总计 new 18 → **3**（#256 #257 + 1 张 O 批）/ fixed 187 → **195** / closed 45 → **53** / confirmed 3 → 2。#252–#257 与 08:30 O-5 落点一致，本条零触碰 |
| 未做 | — | 断点 ⑤「对账表重跑」（`mapping_c6efrs_1076.py` → `_1077.py`）：该脚本产出的是给 skuio 的 C6EFRS 总表**新回访文案**，不在本次点头范围（老板只点了 `.ops/duty_0908/` 三条），且本批 8 单口径已在 mid 1340 / 1341 按单说清；要重跑总表另请示 |

## 5. D —— 自动立单去重加强（05:05–05:20 落地，门禁 16 绿）

**改了什么（全在 `tools/duty_*`，`bug_intake.py` 未动）：**

| 文件 | 改动 |
|---|---|
| `tools/duty_auto_ticket.py` | 新规则 ③「同报障人 90 分钟内（建单**或**最近补录）挂最近单」三档：**a 同实体**（`note_entities`：会话 id `platform:acct:peer` / ≥8 位数字 id（排除 20260907 类日期）/ `id=N` / 客户名 `Name(digits)`·`Name telegram(digits)`·`Vanessa(…)×Sinue(…)`，平台词不算名）→ **b 同主题**（L-7 原相似度 ≥0.45，窗口 30 → 90 分钟）→ **c 引导段跟进词**（`followup_marker`：note 前 40 字到第一个冒号 / 】为止含 复查 / 核实 / 补证 / 复核 / 补〈码〉/ 第 N 次 → 挂该人窗口内最近开放单；正文里的「核实：…」不算）。规则 ② 补一条：**关联码指向的单已 fixed / closed 而同人 90 分钟内有同实体 / 同主题开放单 → 开放单优先**（MJGHKQ 病根之一；c 档不参与抢占）。挂单时 note 严重度高于主单 → `_lift_severity` 抬主单 + `auto_severity` 事件（P0 不能埋进 P2 的补录）。`order_by_report_time`：一轮多份按报告 `time` 头升序（MJGHKQ 病根之二：它比它关联的 RN2SKR 早被处理，#237 当时还不存在）。旧 `reporter_recent_tickets` 删除 |
| `tools/duty_channel_reminder.py` | `check_new_reports` 的 pending 先过 `_order_pending`（同上排序） |
| `tests/test_duty_watch_autoticket.py` | +6 例（共 16）：**#243 七连发回放**（十份原文按时间回放 → 只立一单、九份全挂 #243，三档各有命中）/ **#252→#253、#254→#255 回放**（「补证」同实体 2 分钟挂 #252；em dash 同分钟同实体只立一单）/ **MJGHKQ 回放**（码指向已修 #232 但同人开放同题 #237 优先；#237 不在时仍按旧口径挂 #232）/ 排序 / 跟进词与实体抽取边界（日期·版本·时刻不是实体；不同会话不相交）/ 水位门禁（见下） |

生产库 `--force --dry-run` 回放：K9F7PU → 同实体 17345893728；KT992X → 同实体 12137839654；G7KEUT / GGUNG4 → 同主题 1.00；WZPMXF → 同实体 6088992099；MJGHKQ → 关联 RN2SKR → #237。相邻 `test_duty_reply / ledger_fix / evidence` 41 绿。

**顺带抓到并修掉的生产故障（05:13 发现，05:19 tick 已自愈）：** `tools/duty_watch_loop.py` 水位 `sorted(seen)` 取尾 200 是**按字母序截**——码总数 09-08 02:1x 过 200 后，以数字开头的新码 **3HNCJ7 / 47KNBV（04:51 到）** 排最前被截掉：每 2 分钟被当 NEW PACK 重下重解（`rmtree` 顺带抹 ticket.txt）、永远进不了回执 / 立单链（watch_loop.log 04:51–05:15 十三轮「NEW PACK 47KNBV/3HNCJ7」实录；31 个 tmp_diag 目录不在水位里，其余 29 个是早已回执的旧码）。改为按首见顺序截尾 600（`order[-600:]`）；`duty_channel_reminder` 的 `receipted` 与 `duty_auto_ticket` 的 `handled` 同款字母序 `[-400:]`（超 400 后早字母旧码被挤出 → 重复回执 + 重复立单）改为跟随 watch 水位 `[c for c in codes if c in …]`；门禁 `test_watermarks_never_truncate_alphabetically` 钉住三处。tick 是每 2 分钟的新进程，改完即生效：05:19:19 tick → 两码入水位（202）→ 回执 **mid 1334**（47KNBV 自述「关联 78W8DN」→ 挂 #254；3HNCJ7 关联 47KNBV → 挂 #254；事件 1020 / 1021）。

## 6. E —— 1.0.77 收口（05:24 起）

| 步 | 状态 | 事实 |
|---|---|---|
| 五线收工核对 | ✅ 04:5x | N-1 / N-2 / N-3 / N-4 总账行「收工」；M-7 见 M 批总账（N 批总账那行过期，见 §1）。六份落点表齐 → 合并 `docs/发版对账_v1.0.77.md`（已建，发布事实收口时填） |
| `restart_preflight` 首跑 | ⚠ 05:24 | **NO-GO** 两项：① 装配门禁 `test_admin_route_inventory::test_no_unexpected_extra_routes` 红——N-3 新增 5 端点（`/api/developer/business-domain` GET/POST · `/api/goals/custom-slots` GET/POST · `/api/kb/entries/purge-payment-seeds` POST）未登记路由清单，N-3 已收工 → **代登记**（`_ADDITIONS_2026_09_07_N3_BUSINESS_DOMAIN`，清单是门禁台账非业务代码，M-6 先例）→ 4 passed；② active-files：DY UI 线 3 分钟前存 `unified_inbox_send_routes.py` / `reply_window_ui.py`。53 脏 py 语法 0 错 |
| 收口代办 | ✅ `0e200e67` | 路由清单 + N-3 交办 `config.desktop.internal.yaml` `business_domain: companion` 一行 + N-2 交办 `config.example.yaml` `album_ai` 注释改「1.0.77 默认开」+ N-3 交办 `business_domain` 注释块；两 yaml 经 `stage_hunks.py` 只暂存本线 hunk（DY / QQ / 微信 / 小智线 4 个在途块未卷，自证 ①② 均 OK） |
| `restart_preflight` 二跑 | ⚠ 05:33 | 仍 NO-GO：装配门禁又红一个**新端点 `POST /api/desktop/heartbeat`**——`desktop_bridge_presence.py`（新文件，0.1 分钟前）/ `unified_inbox_desktop_routes.py`（0.5 分钟前）= **实施97 微信 PC 副驾线正在实时写代码**（意向板未登记）。对方连清单都还没登，装配门禁在它登记前不可能绿；按共享树纪律**不载别人的中途存盘**，等安静。装载与打包解耦：先跑全量回归 |
| 全量回归 | ⏳ 05:37 起 | `scripts\regression.ps1`（生产在线 → BelowNormal + 4 worker）→ `tmp/n5_pytest_full.txt`；05:52 走到 11% |
| 路由清单代登记 ② | ✅ `55fe985f` | 实施97 微信 PC 线 `POST /api/desktop/heartbeat`（路由已落盘、该线仍在写、清单未登 → 装配门禁红挡住发版重启；同 08-30 小智线三端点先例，归属注明，改名请一并改） |
| `restart_preflight` 三 / 四跑 | ✅ 05:56 **GO** | 三跑 05:53：装配 4 passed，只剩 `unified_inbox_send_routes.py` 9.8 min（DY UI 线）；四跑 05:56 **GO**（57 脏 py 语法 0 错 / 装配 4 passed / 冷却 750 min；`unified_inbox.html` 1.5 min 是热更模板不计）。批处理窗 advisory：外（下一窗 12:30），N-1 / N-2 是对客 P1 面，照 M-6 先例不等窗 |
| **zhiliao 装载** | ✅ 05:57:56 | `restart_instance.ps1 -Instance zhiliao -Reason "N-5 E: load N batch … piggyback: DY P0/P1 + QQ round 3 + impl97 wechat_kf/pc in-flight .py"`：hard-stop（headless 常态）→ `/login` **20s** 就绪（SLA 180s）；boot 日志 `boot_20260908_055756.out.log`；ops alert 已发。N-1 未动边车 → Node **不**重启 |
| 装载后自检 | ✅ 06:00（`tmp/n5_postload_check.py`）| app.log 201 行 **0 ERROR / 0 Traceback**，WARNING 9 全是既有巡检类（credpool 池不可达回落 / Handoff 脚本缺 / 待审积压 / 案例积压 / avatar_voice 计划任务 / sprint platforms 缺省）+ **N-3 E 新 WARNING**「KB 存量含 19 条系统预置支付话术（GXP 等），本机业务域=sales 用不上：知识库页横幅一键清除，或 POST …purge-payment-seeds」。启动行：N-3 `business_domain 缺省 → 按部署形态推导为 sales 并写入 overlay`（+ ConfigManager `运营开关已更新: business_domain = 'sales'`；**不是** N-3 落点表写的那句 `business_domain=sales（hook=…）` 形态，但 `Domain hook registered: ConversionDomainHook` / `KB categories set from domain 'conversion': [产品价值, …]` / `工作目标路由已注册（/api/goals*）` 三行均在、生产零变化）；N-1 `✅ 多平台 deferred 队列已就绪（… interval=120s drain_loop=running accepting=True）` + `[care_dispatcher] 首拍 enabled=True dry_run=False interval=300.0s`（D 项「随启动常备 + 每拍一行」实证）；`冲刺推进器已常备`；`boot phases` 总 <5s。端点：`/api/goals/custom-slots` 200、`/api/developer/business-domain` 403（dev 未解锁，非 404）、`/api/care/health` 200 `dispatch.running=true`、`/api/voice/effective-config` 200、`/api/goals/1/beats` 404（无 id=1 目标，路由在清单门禁里已验）|
| 全量回归 | ✅ 05:37–06:41 | **88 failed / 26040 passed / 36 skipped，1:03:41**（`tmp/n5_pytest_full.txt`；M-6 基线 79 / 25637，本次并行度封顶 4 且三线同时编辑故慢 20 分钟）。对 M-6 失败集：**3 转绿**（M-6 代补的两条哑按钮门禁 + `test_python_undefined_names`），**12 新红 → 串行复跑只剩 4 条稳定红、0 功能性回归**（`tmp/n5_newred_serial.txt`）：8 条并行抖动（N-1 `test_care_send_now_243` ×4 与 M-1 `test_care_verbatim_default_218` ×2 的 caplog 日志行断言、`test_channel_adapters` 日志断言、路由清单红＝起跑时心跳端点尚未登记）串行全绿；稳定 4 条归属见 §6.1 |
| `gate_sweep` | ✅ 06:44 | **8 failed / 796 passed（2:42）**，复跑仍红＝真红：**7 条老债**（27.9–42h：`deploy_profiles` M-2 E / `seed_switch` ×2 / `inline_color_ratchet` ×3 / `orphan_refs`，与 M-6 / M-7 记录一致，非本批）+ **1 新（0.0h）**：`test_copilot_theme_tokens` `--cp-ok-border` ← N-4 B `cp-voice.js` 结论行引用、两主题未定义 → **代补** `105b384c`（亮 `#a7f3d0` / 暗 `rgba(56,211,159,.38)`，与 `--cp-warn-border` 同档；镜像同步；10 passed）。本次**没有**哑按钮红 |
| bump / 繁体 / changelog / ui-build / 缓存戳 | ✅ 06:47–06:51 | `desktop/package.json` 1.0.76→**1.0.77** / 1.076→**1.077**；`python -m scripts.i18n_hant generate` → **16762 键**（+223，`test_i18n_zh_hant` 6 passed）；`gen-chatx-changelog add --version 1.0.77 --date 2026-09-08 --tags fix,improve`（**8 条双语**，源 `tmp/_notes_1077{,_en}.txt`）；`cp-i18n / cp-i18n-ext / cp-goal / cp-image ?v=` → **`20260908b`** 三宿主（cp-goal.js 在 M-7 `20260907e` 戳后又被 N-3 B/C 改过两次，戳没跟上）+ `sync_copilot_mirror`；`bump_ui_build.py --severity notice` → **20260908-0651**（要在模板改完之后最后 bump）。⚠ `tmp/m6_bump_cp_v.py` 又把两模板 CRLF 写成 LF（M-6 同坑），`tmp/m6_fix_eol.py` 按 HEAD 形态恢复 → 模板 diff 只剩戳行（+ 微信 PC 线在途 `wechat_kf` / `bridge` hunk，提交时 `stage_hunks` 隔开） |
| ⚠ **事故（本条造成，已核清、可恢复）** | 06:00 | 自检脚本对 `POST /api/kb/entries/purge-payment-seeds` 发了一个带 `{"dry_run": true}` 的探测请求，**该路由不认 dry_run，真删了 zhiliao 生产 KB 的 19 条系统预置支付话术**（响应 200）。影响核清（`tmp/n5_kb_diff.py` 对比 02:40 夜间备份 `instance-backup-zhiliao-nightly-20260908-024002.zip` 抽出的 `knowledge_base.db`）：被删的**正好且只有**那 19 条 `template_key ∈ PAYMENT_SEED_KEYS` 的 `source=system` 条目，**全部 `enabled=0`（停用）、07-19 播种后从未被编辑（0 个版本记录）、14 条标题即「（停用占位）GXP …」**；总数 105 → 86，其余一条未动；KB 近 7 天 `queries_7d=0 / hits_7d=0`，启用条目仅 1 → **零对客影响、零用户内容损失**；且这正是 N-3 E 启动 WARNING 要运营在本机做的动作（`kb_payment_seeds_purged_v1` 已打标，1.0.77 的种子计划对 conversion 包本就不再灌这 19 条）。**恢复路径**（老板要就做）：从备份库把 19 行连同 `kb_translations / kb_entry_versions / kb_entry_images` 复制回去（备份已抽到 `tmp/n5_kb_backup_0240/knowledge_base.db`），或清 `kb_payment_seeds_purged_v1` 标记（但 conversion 包不会重播）。**教训**：值守自检只许 GET / 明确幂等的探针；不核路由参数就 POST 生产端点是本条的错，自检脚本已删掉该行并留注 |
| 发版文书（等待期做完） | ✅ 05:40–05:55 | changelog 8 条双语 `tmp/_notes_1077{,_en}.txt`；标 fixed 计划 8 单 `tmp/n5_mark_fixed_1077.json`（fix_note 全 ≤300 字，dry-run `…_dryrun.txt`：#236 #237 #238 #239 #240 #241 #243 new→fixed、#250 confirmed→fixed）；回访 skuio 两条 `.ops/duty_0908/r_skuio_1077_p{1,2}.txt`（1110 / 1072 字）+ 钧一条 `r_jun_1077.txt`（588 字）+ 播报 `tmp/_broadcast_1077.txt`（710 字）+ 频道稿 `tmp/_channel_post_1077.txt`——四条 `duty_reply --dry-run` 全过红线闸；`docs/值守症状族对账单.md` 族 5 +7 层「立即发」+ **新族 9「域包错配」**；合并索引 `docs/发版对账_v1.0.77.md` |

| release commit | ✅ 07:29 | **`aaaf983c`**（package.json / zh_hant_auto.py / ui-build.txt / chatx-release-notes.json / chatxContent.ts 五值 / personas.html 与 unified_inbox.html 缓存戳（后者 `stage_hunks` 只带 3 行，微信 PC 线 9 个在途 hunk 未卷）/ app.html 双树 / `i18n-packs-staged` 11 文件含 DY 线在途 `reply_window_ui.py` 随包；20 文件）。前置 gate 提交：`0e200e67` 路由清单 + config 交办、`55fe985f` 心跳端点代登记、`105b384c` 主题 token |
| 三包 | ✅ 06:52–07:20 | smart **591,417,887 B** + `latest-internal.yml`（`tmp/n5_dist_1077_smart.txt`，「内测包产物冒烟全过」+ 随包交付物 14 项）；clean **520,227,125 B**（496.1 MB → 官网 496 MB）sha256 **`3912f951061604ff0d0a4765af89d4cb9a5cc186c720d1f6e700d268a0c257a4`** + `latest.yml`（「干净包产物冒烟全过（首启零数据）」）；lite **523,555,643 B** + `latest-lite.yml`（「lite 定制包产物冒烟全过」）。`dist-lite\latest.yml`（08-24 残留）已删 |
| publish 三渠道 | ✅ 07:30–07:36 | 各先 `-DryRun`（全 publishable）再 `-Yes`。**公共**：VPS sha512 ✓ / 指针 + pm2 / R2 / 公网核 `latest.yml=1.0.77 manifest=1.0.77 exe=HTTP206` / `announcements.json` 带 `release-v1.0.77`（19 条）/ 清理 **1.0.74**。**内测**：`latest-internal.yml=1.0.77`，公共指针未动，清理 1.0.74。**lite**：`latest-lite.yml=1.0.77`。日志 `tmp/n5_publish_1077_{public,internal,lite}.txt` |
| 官网 | ✅ 07:34 | `npm run gate:content` **PASS 6/6**（chatxContent 1.0.77 / 496 MB / sha256 3912f951… ⇄ manifest 1.0.77；changelog 最新 v1.0.77）→ `scripts/deploy.ps1` **部署完成 [OK]**（healthy / webhookSecret / adminKey / deepseek 全 True；R2 同步；indexnow 48）。日志 `tmp/n5_website_deploy.txt` |
| 坐席三台 | ✅ 07:36–07:42 | `push_chatx.ps1 -TargetSsh {kouxing,lianbei,yunsheng} -Install -Relaunch -Smoke` 三台 exit 0（`channel: internal` 564 MB；smoke 全 OK：channel wizard / media selfcheck / feature center / diag probe / vision active / pacing）；`chatx_fleet_status`：**yunsheng / kouxing / lianbei 全 UP · 1.077 · smart/internal**。日志 `tmp/n5_push_*.txt` / `n5_fleet_1077.txt` |
| 播报 | ✅ 07:43 | `duty_update_hint --set "1.0.77 已发布：…"`（热重载）；报障群 `duty_reply --group neice --text-file tmp/_broadcast_1077.txt --no-mention` → **mid 1335**；频道 @hykj7 `tmp/n5_channel_post.py --apply`（campaign `release-1077`）→ **messageId=155** |
| 台账 / 回访 | ✅ 11:26–11:31（老板点头后） | A 并单 9 项 apply → 8 单标 fixed apply → 回访 skuio **mid 1340 / 1341** + 钧 **mid 1342** → 记账 7 + 2 单；每步 dry-run 复跑与 05:03 / 06:06 存档哈希一致再 apply；详见 §4.1 |
| 两机核版本 | ⏳ 交下一班 | 无 SSH；两机在内测渠道，壳自检会拿到 1.0.77；播报与回访都带手装链接。装完核清单见 §七 |

### 6.1 回归 / 门禁稳定红归属（交回各线，不拦发版）

| 测试 | 断言 | 归属 / 性质 |
|---|---|---|
| `test_copilot_theme_tokens::test_every_referenced_token_is_defined` | `--cp-ok-border <- cp-voice.js` 未定义 | **N-4 B**（纯 token）→ 本条代补 `105b384c` 已绿 |
| `test_reply_settings::TestPlatformExpert::test_platforms_match_worker_registry` | worker 注册表多出 `douyin`，回复设置平台表没跟 | **DY 线**（演示 worker 注册钩子 `fa0860d9`，在途） |
| `test_workspace_emoji_ratchet::test_workspace_emoji_not_increasing` | `unified_inbox.html` emoji 58 > 天花板 54 | **实施97 微信 PC 线**（在途 `PI` 表新增 `wechat_kf:'💬', wechat:'💬'` 等；未提交） |
| `test_ui_build_freshness::test_ui_build_stamp_not_stale` | 前端文件比构建戳新 | 收口方 bump → 06:51 已绿 |
| 并行抖动 8 条（串行绿） | `test_care_send_now_243` ×4（N-1）/ `test_care_verbatim_default_218` ×2（M-1）/ `test_channel_adapters::test_group_count_watch_logs_on_change` / 路由清单 | caplog 日志行断言在 `-n 4` 下抖；建议各线给这些用例加 `caplog.at_level(..., logger=…)` 显式抬级或 `@pytest.mark.serial`（不拦发版） |

## 七、交回各线 / 交下一班（本条发现但不该本条动的）

**新报障（09-08 00:49–02:30，skuio 16 份 → #252–#255 + 挂旧单；不在 N 批，O 批候选——05:4x 看到老板已在写 `指令_O-1_拟人与反识破` / `指令_O-2_记忆抽取基线`）**

| 单 / 码 | 内容 | 去向建议 |
|---|---|---|
| #252（EC7QZM）+ #253（K9F7PU XAM4KV FW78ZP Q9H2HM RYE8Y8 8FJDUK） | **客户明说「stop writing to me / Never write me again」后系统仍全自动连发 3 条**（XAM4KV：`shadow=stop_contact hits='please stop' risk=high` 却 `AutosendWorker 发出`——影子命中不拦真发，**封号风险，P0 级**）；客服腔（Q9H2HM：conversion 域包 system prompt 1868 字销售 / 客服底稿——N-3 A 只换了 hook / 分类 / 模板，**域包 system prompt 本体未动**）；拟人节奏（FW78ZP：typing / pacing 全局零日志） | O-1；#253 → #252 并单（同实体 2 分钟「补证」，A 段建议追加）；8FJDUK 实为 #241 域包话题（自述「与 TN736F/3NVM7Q 同一问题」）→ 改挂 #241 |
| #254（KT992X 78W8DN 47KNBV 3HNCJ7）+ #255（TDNJHQ 5XRQ7C HM7XBA） | em dash「—」与总结式句式高频（一天 5 例跨 3 号同人设 Mizuki）；引用回复 / 表情回应需求 + **用户点名要值守回开发周期评估**（3HNCJ7，未回）；5XRQ7C / HM7XBA 是**摸底目标 100% 被动跟随**（冲刺腿未发、采集链已死）——是 #236 / M-7 族，被同会话实体挂到了 em dash 单 | O-1（em dash / 句式）；#255 → #254 并单并抬 P0（A 段建议追加）；5XRQ7C / HM7XBA 改挂 #236 或新立（M-7 D 的 `auto_state` / `stalled` 上线后再看真机）；3HNCJ7 的周期评估问题需老板给口径 |
| WYNN22 → #201 / 584AUV → #183 | **1.0.76 全新安装 AI 记忆完全不抽取**：`[episodic] schedule run` 30 次全 `skip: intent not extractable (match_all=False intents=[])`，6 客户零抽取零 promoted（P0 退化） | O-2；#201 已 fixed，该证据应新立单而非挂旧 fixed 单 |

**值守工具 / 取证（N-5 自己的域，预算内未排）**：① `zl_collect_*.ps1` **v1.3.2**：renderer 过滤词加 `[send-media]` / `[media]`（M-6 交办）、收 `run_sentinel.json`、boot 窗（`boot phases` 前后 60s）不过关键词过滤（M-7 §四 ④）；② 打包态壳主进程 `console.log`（`[backend]` 拉起 / 就绪 / respawn、`[updater]`、`[hotpatch]`）无落盘去处 → 接进 `renderer.log` 或独立 `main.log` 并纳入 `diag_upload`（M-7 取证缺口，归桌面壳）；③ 去重规则已知边界：同实体不同题（HM7XBA 摸底目标 vs #255 em dash）按老板规则「挂最近单」，值守日对账时人工改挂；关联码规则先于实体规则（8FJDUK 因引 Q9H2HM 挂 #253）——码是用户显式意图，保留；④ `duty_watch_loop` 有 31 个 tmp_diag 目录不在水位里（29 个是旧已回执码 + 已修的两个），无需处理。

**交回 N-3（业务代码，本条不动）**：`src/utils/domain_loader.py` L? `logger = logging.getLogger("DomainLoader")` 是**根级名**——生产 `logging_setup` root 钉 WARNING、只给 `ai_chat_assistant.*` / `src.*` 放 INFO → 该模块所有 INFO（含 N-3 A 的验收行 `business_domain=…（hook=…，pack=…）` 与 `Domain 'conversion' loaded: … hook=…`）**在任何生产 / 客户机日志里都不出现**（06:00 zhiliao 装载后 app.log 零行实证；与 M-7 §2 问 1 goals 包同病）。一行修法：改 `logging.getLogger("src.utils.domain_loader")`（或 `__name__`）。已把 #241 的 fix_note / 回访验收句改成真能看到的 `Domain hook registered: CompanionDomainHook`（`ai_chat_assistant.SkillManager`）+ `KB categories set from domain 'conversion': [人设背景, …]`。

**M-6 交办仍未收**：其它 8 个 multipart 上传口同吃 2MB 闸（`voice_routes` 声音样本 / `persona_media_routes` 相册头像 / `kb_routes` 文档 / `sticker_routes` / 联系人导入 / `telegram_routes` / `developer_page_routes` / `admin.py`）——**仍待立单**（内部单，老板定版本）。

**总账 / 交接**：N 批总账「接手 M-7」行仍写「未接手」是过期信息（M-7 在 M 批总账收工），本条未改他人那行，只在全批收口节加注。两台内测机装 1.0.77 后核：M-6「两机核版本」六样 + N-3 三行启动日志（`business_domain=companion` / KB 分类陪伴集 / 工作目标路由）+ N-1 `send_now` 链 + M-7 `[goal-inject]` 每稿一行（出现即 #216 可标 verified）+ N-4 `[tts] verdict=` 单行。钧 selfcheck 回码（v1.3.1）待收。

## 断点 / 交接

- 04:45 意向登记；04:45–04:58 基线（§1）；04:56 C 段落地（#215 fixed / #250 received）；04:59 B 段三条发出（mid 1331 / 1332 / bot 链文本）；05:03 A dry-run 存档；05:05–05:20 D 段 + 水位故障修复（`f3369b52`），05:19 tick 自愈回执 mid 1334；05:24 / 05:33 preflight 两跑 NO-GO（微信 PC 线实时编辑）；05:37 全量回归起跑；05:40–05:55 发版文书全部备好（§6）。
- 05:56 preflight GO → 05:58 zhiliao 装载 → 06:00 自检（+ purge 事故）→ 06:41 回归完 → 06:44 gate_sweep → 06:47–06:51 bump 链（+ CRLF 坑）→ 06:52–07:20 三包 → 07:29 release `aaaf983c` → 07:30–07:36 三渠道 → 07:34 官网 → 07:36–07:42 坐席三台 → 07:43 播报 mid 1335 / 频道 155。**E 段技术链收工 07:45**；`agent_probe -Done` 已清（代码面无在途）。
- **11:26–11:33 老板点头后 A 段收口（§4.1）**：① 并单 apply ✅ → ② 8 单 fixed apply ✅ → ③ 回访 mid 1340 / 1341 / 1342 ✅ → ④ 记账 7 + 2 ✅ → ⑤ 对账表重跑 **未做**（会产新对客文案，不在点头范围）→ ⑥ 总账 N-5 行「全部收工」+ 全批收口勾选 ✅；`agent_probe -Done` 11:3x 清（07:45 那次未清干净，意向板 11:33 仍见 N-5 行）。#253→#252 / #255→#254 抬 P0 由 O-5 08:30 做了。**N-5 全部收工。**
- **老板若要恢复被删的 19 条 KB 停用条目**：`tmp/n5_kb_backup_0240/knowledge_base.db` 是 02:40 备份抽出的旧库，按 `template_key ∈ PAYMENT_SEED_KEYS` 把 `kb_entries`（+ `kb_translations / kb_entry_versions / kb_entry_images` 无关联行）复制回 `D:\chengjie-instances\zhiliao\data\config\knowledge_base.db` 并删 `kb_payment_seeds_purged_v1` 标记；不恢复也符合 N-3 E 对本机的预期。
- 钧 selfcheck 回码：07:45 仍未回（群最后一条入站是他 22:07 的 mid 1319）；回了在 §3 补码并 `duty_evidence` 记。
