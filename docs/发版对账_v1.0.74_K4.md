# 发版对账 v1.0.74 · K-4 返工单与复合单复核（2026-09-05）

> 指令：`docs/指令_K-4_返工单复核_2026-09-05.md`。预算 $100。类型：**代码只读 + 每单回归钉 + 二次结论**，不修 bug、不写台账。
> 复核基线：HEAD `71355568`（09-05 19:38，K-1 A2 已提交）。台账只读 `bug_intake.db?mode=ro`（`bug_tickets` + `bug_events`）。
> 一句话结论：**四张返工单一张都不是修复失效，无 D。** #63 / #67 的「验证未过」是同一句 LINE 媒体反馈被旧 verify 逻辑误归属；#64 的「还是不行」点在修复入包前；#61 的「还是不行」点在残余层 #78 上（本体已被用户自己的截图实测生效）。#64 族里 #97 是修复后的**真复发**，已在 1.0.65 补齐收口点，回访必须承认。
> 本文件交 K-5 ④段台账写入 + ⑤段回访；回访文案按 §「回访必带的一句」原样用，别缩写。

## 0. 版本时间线（本次复核用的事实源：`release(chatx)` commit 时刻 + changelog）

| 版本 | release commit | 时刻 | 与本批相关的修复 |
|---|---|---|---|
| 1.0.62 | `0d0da2ce` | 08-30 06:56 | #61 `ab866775`（05:56）· #63 `efa24317`（05:55）· #12 档位 |
| 1.0.63 | `1224b9a3` | 08-30 16:47 | #64 `cf1a3c6c`（13:26，三翻译出口守卫）· #67 `a24a6738`（15:00，相册三层）· #71 · #74 |
| 1.0.64 | `339fdb1e` | 08-30 22:59 | #78 一轮（LINE/WA 账号进「应用到」列表；钧 0831 13:34 确认到货） |
| 1.0.65 | `bce2cbc4` | 08-31 15:02 | #97 `9991a6f9`（13:24，混语收口点 `sendpoint_lang_mix_pass`）桌面半 |
| 1.0.66 | `e4ef285e` | 09-01 00:31 | #78 二轮（default 伪行隐藏 + 显示名链）· 34 语目录 `4390a469`（繁体/粤语「发→」） |
| 1.0.68 | `d808f261` | 09-02 11:17 | #93 #137 #140 克隆声黄条 · #142 `ae772df1`（归 #63 族）· #133 |
| 1.0.69 / 1.0.70 | `721c14d5` / `c76aa4aa` | 09-02 12:25 / 14:06 | #143 · #143 C-补 |
| 1.0.71 | `a6bfaf98` | 09-03 05:50 | `cfe9ad47` #149 克隆声挂载 · `0cdf9ada` #147 #146 · `2a9bf45a` A2 LINE E2EE |
| 1.0.72 | — | 09-03/04 | `929ba165` `23cb7dba` #161 · `2f8fc70f` `26794d2e` #159 |
| 1.0.73 | — | 09-04 14:3x | `bdae979e` #145③ · `60c2d428` #32② · `907faa83` #40 D1（地区档） |
| **1.0.74 待发** | — | — | `17230974` #179 · `45261c78` #167 · `09bd0294` #170 · `c9d19638` #172 · `71355568` K-1 A2 · **K-1 A3 待提交** |

注：#78 两轮代码都在 `b7161370`（09-02 06:04「0901修复批」批量提交），源码注释日期 08-30 / 08-31——先随包出、后补 commit，是这棵树的老习惯，版本归属以台账里的到货确认为准。

## 1. 四张返工单

| 单 | 证据链（原报 → 修复 commit → 进哪个版本 → 用户反馈时刻 + 原话 → 反馈时用户机版本 → 结论） | 结论 | 回访必带的一句 | 钉 |
|---|---|---|---|---|
| **#61** 人设「应用到」只认第一个 TG（钧 0830 00:59，P2） | 原报 00:59 → `ab866775` 08-30 05:56（`persona_routes.py` TG 枚举合并运行时注册表）→ **1.0.62**（06:56）→ 钧 08:03:46 群里原话「我登录了2个TELEGRAM和1个LINE，为什么指定时找不到LINE，多了2个DEFAULT」→ 值守 08:03 立 **#78**（`reporter_version=1.0.62`，body 明写「**#61 的 TG 部分用户已实测生效**，本单为范围扩展」）→ 08:07 点「还是不行」（`updated_ts`）→ 反馈时已在 1.0.62（截图能看到两个 TG 号只可能是修复后的枚举）→ 按钮说的是 #78 那层。残余三层全在 HEAD：#78-① LINE/WA/Messenger 注册表合并（L2160，1.0.64 到货）· #78-② Messenger `default` 占位门（L2093）· #78 二轮「有注册表账号即丢 config default」+ 显示名链 label→self_name→id（L2060/L2078，1.0.66）· #179 `17230974` 注册表占位行 `registry_row_is_placeholder` + `config_default_account_is_live`（L143/L176/L2025/L2054，**1.0.74 待发**）。#78 `fixed` 未回访；#179 `new` | **B**（按钮反馈实为 #78 层；#61 本体 A）。与指令预期 A 的差别：不是「入包前」，是「点在残余层」——定性一样：非修复失效 | 「#61（人设应用到多个 TG 号）你 8-30 早上 8:03 那张截图已经验证生效了；你点『还是不行』说的『找不到 LINE、多了两个 default』是 #78（1.0.64 / 1.0.66 两轮已修）和 #179（1.0.74 修最后那行『default —』）。装完 1.0.74 打开『应用到 → 账号默认』：**行数 = 你真实登录的账号数、每行一眼认出是哪个号**，就算过。#61 + #78 + #179 三个号一起关。」 | `test_61_status_enum_merges_runtime_registry_and_never_renders_placeholder` · `test_61_placeholder_judgement_keeps_real_accounts` |
| **#63** 新登录账号默认人审后发（钧 0830 02:35，feature P2） | 原报 02:35 → `efa24317` 08-30 05:55（新模块 `account_mode_onboarding.py` 账号级档位）→ **1.0.62** changelog「新登录账号先确认接管方式（全自动/拟稿人审/关闭）再放行」→ `ae772df1` 09-02 #142 真发总闸静默拦截可见（1.0.68，归 #63 族）→ `45261c78` 09-05 04:40 #167（**1.0.74 待发**：`FIRST_LOGIN_GRACE_SEC=7d` 首登竞态 / `created_at` 未知按待确认 / `cap_unconfirmed_default_persona` / `maybe_bootstrap` 未确认不落盘）。用户反馈：`bug_events` **verify_no 09-01 01:23:14** 原话「LINE还是不能收发图片语音视频」→ 与 #63 无关（旧 verify 逻辑打到该报障人最新 fixed 单，01:19:50 同一句先打到 #67）→ 反馈时用户机 1.0.65/1.0.66 窗口 → 误归属。HEAD 三处全在：`PENDING_DEFAULT_MODE="review"` / `is_new_account` 宽限判据 / `automation_mode.maybe_bootstrap_automation_mode` 账号层不落盘 + `decided_mode(...) is None → return mode` | **B**（误归属 → #101 族 / #145②）**+ A**（本体 1.0.62 起在包；1.0.74 补三条漏网） | 「#63 从 1.0.62 起：新登录的号在你到设置页确认『全自动 / 拟稿人审 / 关闭』之前一律只写稿不自动发，老号不受影响；1.0.74 再补了三条漏网（刚登进来的号被当成老号直接写成全自动的竞态）——**#63 / #156 / #167 是同一个结论**：接管方式和以谁的身份接管都必须是人的显式决定。另外 9-1 凌晨 1:23 你那句『LINE还是不能收发图片语音视频』被我们记错到这张单上了，那件事在 #101 / #145② 那边跟，不算这单没修好。」 | `test_63_account_level_takeover_entry_still_exists` · `test_63_bootstrap_never_persists_unconfirmed_account`（+ 既有 `test_onboarding_bootstrap_167.py` 50 例） |
| **#64** 出站中英混语「I'm 我, and I'm not going anywhere.」（skuio 0830 03:53，P2，复报 3 次） | 0827 B121 首报 → #43 → 本单 03:53 → 12:05 三报（1.0.62 未带）→ `cf1a3c6c` 08-30 13:26（`_reply_lang_mismatch` 拉丁夹 CJK 判据 + `_guard_reply_language` 单次重写 + deferred 出口补挂 `_guard_translated_lang_mix`）→ notify 13:22 → **skuio 14:37 点「还是不行」**（`updated_ts`）→ **1.0.63 16:47 才发**→ 反馈时用户机 1.0.62 = 修复入包前。**之后 #97（skuio 08-30 21:47，`reporter_version=1.0.63`）是真复发**：英文会话直接生成路径不经翻译出口，守卫罩不到 → `9991a6f9` 08-31 13:24 混语守卫上移到全链收口点 `sendpoint_lang_mix_pass`（`account_orchestrator.send` 正文+配文 / A 线 `outbound_quality_pass` / `sendpoint_guard`）→ **1.0.65**；#97 `fixed` 未回访，fix_note「发版后请在同会话复测 24h」；0831 10:55 第五例发生在 1.0.64（修复空窗内，非新击穿）。同族后续层（「切中文」方向，非混语）：#106（同 commit）/ #133（1.0.68）/ #143（1.0.69–70）/ #154 `2afc4e0f`（1.0.72）。HEAD 四层齐：生成端 `ai_client._reply_lang_mismatch` L2284 + 4 处 `_guard_reply_language` 调用 · 出稿口 `apply_outbound_text_guard` · 三翻译出口 `_guard_translated_lang_mix`（`autosend_helpers.build_autosend_translate_cb` 罩自动链+人工通过链，`main.py` L639 罩 deferred）· 收口点 `sendpoint_lang_mix_pass` | **A**（14:37 反馈在 1.0.63 入包前）。**#97 复发是真的**，已随 1.0.65 补层。三出口 + 收口点全挂，无 D | 「你 8-30 下午 2:37 点『还是不行』时机器还是早上那版 1.0.62，那版确实没带这个修，下午 4:47 的 1.0.63 才带——这条不怪你。**之后 8-30 晚 9:47 那条『I'm 我 the one…』是我们真的又漏了一层**：守卫只装在翻译出口，英文会话直接生成的那条路没罩住；8-31 把它补到了所有消息出门的最后一道口上，随 1.0.65 到你机器，之后到今天台账里没有新的『I'm 我』实例。装完 1.0.74 请在同一个英文会话跑 24 小时，零中文残字就 #64 + #97 一起关；出现一条就截图，那就是新层。」 | `test_64_mixed_script_is_detected_and_stripped`（#64 原句 / #97 两原句 / B125 译文实录 ×4）· `test_64_clean_english_is_not_false_positive` · `test_64_generation_side_rewrite_rule_and_hook` · `test_64_all_three_translation_exits_and_sendpoint_are_guarded` |
| **#67** 相册上传失败（钧 0830 04:19，P1） | 原报 04:19 → 值守 05:4x 三层根因（DC8F 包：落盘进安装目录 / 文件写坏 / 误报 album_empty）→ `a24a6738` 08-30 15:00 → **1.0.63**（16:47）→ notify 15:00（fix_note 尾句「修复上线后请按值守指引一次性重传相册」）。**无用户验收记录**：#94（closed）是值守机器人 21:14 发给钧的「可以用了✅ 相册那单随 v1.0.63 修了三层…」被误抓成工单，**不是钧说的**。用户反馈：`bug_events` **verify_no 09-01 01:19:50** 同一句「LINE还是不能收发图片语音视频」→ 误归属（#101 族）；body 末尾另有 1.066 上「❌ 媒体发送失败（网络/服务异常）⤴️ 报障」按钮回调 → 那是 #148 / #164 红横幅族（#164 `345241a6` J-4 C，随 1.0.74 桌面包），也不是相册。HEAD 三层全在：① `media_paths.resolve_album_root`（`AITR_CONFIG_PATH`/`AITR_DATA_DIR` 数据根）+ `migrate_legacy_album_tree`，`persona_media_routes` L46–58 消费 ② 上传 `sniff_media_bytes` magic 门禁在 `write_bytes` 之前 + 落盘回读验证 `_head != data[:16]`，`err.pmedia.bad_content` zh+en ③ `image_autosend` L1156 `album_file_missing` 独立归因 | **B**（误归属）**+ C**（需用户重传相册一次才看得到效果） | 「相册那单（#67）三层修复从 1.0.63 起就在你机器上：照片改存数据目录（升级不再清空）、上传当场校验（坏文件立刻提示，不再假装成功）、发送失败分清『文件缺失』和『相册是空的』。但 **8-30 之前传的那批是坏文件、又落在旧目录——请打开人设工作室 → 相册，把它们重传一次**，再用触发词试发一张，这单才算真的验过。9-1 凌晨 1:19 那句 LINE 的反馈、和 1.066 上那条『媒体发送失败』红横幅都不是相册问题，分别在 #101 / #145② 和 #164 那边跟。」 | `test_67_upload_route_validates_content_before_write_and_uses_data_root` · `test_67_sniff_and_album_root_pure_functions` · `test_67_send_failure_attribution_and_error_copy` |

## 2. #145 复合单五行表（按 K-D6 拆；钧 0902，`reporter_version=1.0.70`，`confirmed`）

| 件 | 钧原话 | 修复 commit（版本） | HEAD | 结论 | K-5 动作 |
|---|---|---|---|---|---|
| ① 克隆语音后工具箱没克隆成功 | 「克隆语音后，工具箱还是没有克隆成功，这个问题为什么一直没有解决」 | #93/#137/#140 黄条（1.0.68）· `cfe9ad47` #149 挂载断链 `voice_profile_guard` + 选声必达（1.0.71）· `929ba165` `23cb7dba` #161 `speech_verdict` 「疑似无声 / 念错」拆档 + 念错改派 Edge（1.0.72）| 在（`heal_clone_profile` 接 `persona_manager`；`_redispatch_garbled_to_edge`；`_discard_garbled_take`） | **A**。钧机 5GZHWT 是重装后的冷启动包、无案发现场（症状族 族1 层4）；1.0.70+ 「应克隆未克隆」必亮黄条；ja/ko 默认不走克隆声 | 标 fixed。回访：再克隆一次试听，若还是标准声看有没有黄条 → 截图 + 一键上报 |
| ② 收发图片 / 表情有问题（LINE） | 「收发图片功能有问题，还有收发表情也有问题」 | #101 族（`fixed` 未回访，fix_note 要求 1.0.65 双向复测）· `2a9bf45a` A2 LINE E2EE 自愈 + OBS 404 终局提示（1.0.71）· `c9d19638` #172 Letter Sealing 媒体定位 + 404 按龄分档（**1.0.74 待发**）。值守 0902 两次定性：QTCYNC 19:50 「发图失败」= 重装后 WA 零登录、通道未接；SY8TGE 20:46 「图片还是发送不了」在日志里 20:45–20:51 全部 `delivered=True`，红横幅是重启窗残留 | 在（`classify_missing` 龄未知恒 NOT_FOUND / `rebuild_e2ee_from_session`） | **随 1.0.74 真机验收**（I-5 E1 剩余），不下 A | **不标 fixed**；进 K-5 ⑤段真机清单（LINE 双向 语音+图+视频+贴纸）；**把钧 09-01 01:19 / 01:23 那两句「LINE还是不能收发图片语音视频」补记到本件 / #101**（见 §4） |
| ③ AI 说起其他人设推荐的产品并贬低 | 「为什么AI会说起其他人设的推荐的产品，并贬低笑话产品本身」 | `0cdf9ada` #147 贬损自家推广双层守卫 `claim_guard.sanitize_camp_disparagement`（1.0.71）· `bdae979e` #145③ 目录按人设过滤 `site_catalog.foreign_product_names` + `persona_guard._foreign_products_for`（1.0.73）· `71355568` K-1 A2 `pick_products(..., persona_id=catalog_persona_id)` 选品同源（**09-05 19:38 已提交，1.0.74 待发**） | 在（三处全在 HEAD） | **A** | 标 fixed（1.0.74 装机后） |
| ④ 手机端删除消息智聊不删 | 「手机删除消息了，智聊工作条要同步删除，避免产生错误的记忆」 | `0cdf9ada` #146 对端删除同步 + 记忆清理 `peer_delete_purge.purge_for_deleted_rows`（1.0.71）· `60c2d428` #32② 「记得但不主动提」`withdrawn_cite` 账本 / 历史占位 / 入站 hint（1.0.73）· **K-1 A3 出站接线**（`withdrawn_cite.apply_to_reply` 入口 + `skill_manager` L5017 + `drafts.py` L1229，**HEAD 未提交**，工作树在） | 入站半边在；出站半边等 K-1 A3 | **A（K-1 A3 提交后）** | 标 fixed 前确认总账 K-1 行 A3 已 commit；否则 body 只写「入站已修、出站接线在途」 |
| ⑤ 虚拟未读数字 | 「这些虚拟未读消息数字还没修复，要安排进工单」 | `2f8fc70f` `26794d2e` #159 徽标口径与清单同 WHERE `unread_aggregate`（1.0.72）· `09bd0294` #170 被埋会话入口紧凑态可见 + 一键取消归档（**1.0.74 待发**，模板热更新已在 zhiliao 生效） | 在 | **并族**（#120 / #159 / #170），随 #170 回访 | body 标「并 #120/#159/#170」，不单独回访 |

产出口径：K-5 把这张表写进 #145 body，**②除外的四件已修（④待 K-1 A3）**，② 随真机验收；#145 状态按 K-D6 标 fixed。
⚠ 编号不一致：`docs/值守症状族对账单.md` 族3 层3 把「账号栏徽标虚高」记为 **#145件④**，K-D6 记为 **件⑤**（件④是对端删除）。本表按 K-D6；K-5 写 body 用 K-D6 编号，顺手在症状族对账单那行加注「= K-D6 件⑤」。

## 3. 两张顺手单

| 单 | 证据链 | 结论 | 回访必带的一句 | 钉 |
|---|---|---|---|---|
| **#36** AI 回复繁体 / 粤语语言包（钧 0826，`new`） | 两半：**(a) AI 出向繁体 / 粤语** = 翻译目标语 `zh-tw` / `yue` 一等公民（`translation_service.LANG_NAMES` + `zh-hant`/`zh-hk`→`zh-tw` 归一，`outbound_translate._CJK_LANGS` / `_ZH_FAMILY_TARGETS` / `normalize_target` 不折叠繁体变体，源码注释 2026-08-29；批量提交 `b7161370`）+ 会话「发→繁体中文 / 粤语」34 语目录 `4390a469`（09-01 00:31 断电重建，随 **1.0.66**）。**(b) 地区语气自适应** = I-4 D1 `907faa83` #40（09-04，**1.0.73**）：`persona_region` zh-CN / zh-TW / zh-HK 三档语气词 + 句式，粤语人设 → HK 档，禁用词只观测；CN 档恒空（存量 prompt 不变）。台账状态 `new` 是错的（I-6 F3 ①） | **A**（两半都在包） | 「#36 两半都到了：**AI 回客户用繁体 / 粤语**——在那个会话顶部把『发→』设成繁体中文或粤语就生效（1.0.66 起）；**界面本身仍是简体，那是另一件事**。港台口吻（呀 / 啦 / 囉 这类语气词和句式）1.0.73 起按人设地区档自动带，粤语人设默认走香港档。」（fix_note 按 I-6 F3 ① 原话写：「会话里设『发→繁体中文 / 粤语』即生效；界面本身仍是简体（那是另一件事）」） | `test_36_traditional_and_cantonese_are_first_class_outbound_targets` · `test_36_region_tone_profiles_in_head` |
| **#170** 幽灵未读 1.0.73 复发（skuio 0905，`confirmed`） | 决策 K-D5 = fixed。`09bd0294` 09-05 07:06 在 HEAD：`unified_inbox.html` `_renderCompactBar` 紧凑态「归档中还有 N 条未读」pill（源码注 `#170`）+ `_renderBuriedHint` 「取消归档这 N 条」→ `_unarchiveBuried`（`/api/admin/buried-conversations` 权威清单，可撤）；i18n `inbox.buried.unarch*` zh+en。commit 核对结论：归档视图打开会话同样走 mark-read；新入站自动浮回默认视图；88MP86 的 7 条 64h 被埋 = 人工归档后再无入站，非缺陷。模板热更新已在 zhiliao 生效，桌面包随 1.0.74 | **A**（K-D5） | 「#170：你机器上那 7 条『被埋 64 小时』的未读，是你手动归档后对方再没发过消息——数字没错、只是入口藏在展开态里。1.0.74 起紧凑态也能看到『归档中还有 N 条未读』，旁边一键『取消归档这 N 条』就清掉；角标点开直接定位那几条的事归 #159，同版。」 | `test_145_5_and_170_unread_aggregate_and_buried_entry` |

## 4. 交 K-5 的台账写入清单（④段，**两台内测机装上 1.0.74 之后**——K-D2）

| 单 | 状态 | body 追加 | 回访分组 |
|---|---|---|---|
| #61 | `confirmed` → `fixed` | 「K-4 复核：08:07『还是不行』= #78 层（用户 08:03 截图已验 TG 多账号生效）；#78 1.0.64/1.0.66、#179 1.0.74」 | 钧：#61 + #78 + #179 合一条 |
| #63 | `confirmed` → `fixed` | 「K-4 复核：09-01 01:23 verify_no『LINE还是不能收发图片语音视频』误归属 → #101/#145②；#167 1.0.74 补三条漏网」 | 钧：#63 + #156 + #167 合一条（引 J-2 落点表 C 行） |
| #64 | `confirmed` → `fixed` | 「K-4 复核：14:37『还是不行』在 1.0.62，修复随 1.0.63；#97 21:47 真复发已随 1.0.65 收口点补层」 | skuio：#64 + #97 合一条，**明说 #97 是真复发** |
| #67 | `confirmed` → `fixed` | 「K-4 复核：09-01 01:19 verify_no 误归属 → #101/#145②；1.066『媒体发送失败』按钮 → #164；#94 是机器人消息误抓，非用户验收」 | 钧：**必带「请重传一次相册」步骤**（I-6 §4 ⚠ / K-D3） |
| #145 | `confirmed` → `fixed` | 写 §2 五行表；②「随 1.0.74 真机验收」；④ 写明 K-1 A3 是否已 commit | 钧：五件逐条 |
| #36 | `new` → `fixed` + fix_note（I-6 F3 ① 原话） | 「K-4 复核：(a) 出向繁体/粤语 1.0.66 (b) 地区档 907faa83 1.0.73」 | 钧 |
| #170 | `confirmed` → `fixed`（K-D5） | 「K-4 复核：09bd0294 在 HEAD；7 条 64h 被埋 = 人工归档后无入站」 | skuio：#170 + #159 合一条 |
| **#101（或 #145②）** | 不改状态 | **补记**：「钧 09-01 01:19 / 01:23 两次『LINE还是不能收发图片语音视频』（当时 1.0.65/1.0.66），原被 verify 逻辑记到 #67/#63」——这两句反馈目前不在任何 LINE 单里，不补就丢了 | 随 ② 真机验收 |
| #94 | 已 `closed`，不动 | — | 别当 #67 验收证据引用 |

## 5. 有没有 D（转修复）

**没有。** 四张返工单 + #145 四件 + #36 + #170 在 HEAD 均无缺层；#145④ 出站半边是「在途未提交」不是「缺层」，归 K-1 A3。
发现的都是**流程 / 台账层**问题，不开修复指令，写在这里给值守线：
1. 旧 verify 逻辑「用户一句话 → 打到该报障人最新 fixed 单」——#63 / #67 两例（相隔 3.5 分钟、同一句、先后打到两张不同的单）是实锤。J 批的 verify 改造是否已覆盖「用户消息与 fixed 单内容无关」的情形，K-5 装载后盯 `bug_events.verify_no` 新增行核一次。
2. 同一机制的另一面：#67 body 里 1.066 的红横幅「报障」按钮回调、#63 body 里 0902 的克隆声 / 语音时长 / 「不能分辨我和你」四条截图，都被 append 到该报障人当时最新的 open 单——**#63 body 里混了至少四件与它无关的事**。K-5 写 #63 时别把那些当成 #63 的内容。
3. 症状族对账单 #145 件号与 K-D6 不一致（§2 ⚠）。

## 6. 钉

`engines/chengjie/tests/test_rework_pins_k4.py` **21 例**（每单一到两条，静态切片 + 纯函数，不起服务不碰 store）：
- 工作树：`python -m pytest tests/test_rework_pins_k4.py -q --tb=line` → **21 passed 6.5s**
- 纯 HEAD（`git archive HEAD engines/chengjie` 导出到临时目录再跑，排除他线未提交 hunk 撑场）→ **21 passed 9.8s**
- 刻意不钉：`withdrawn_cite.apply_to_reply` / `skill_manager` L5017 / `drafts.py` L1229（K-1 A3 待提交，提交后由 K-1 的 `test_withdrawn_cite.py` 新例钉）
- commit：见总账 K-4 行

## 7. 边界 / 未做

- 不改任何 `src/**`；不写台账（K-5）；不发群。
- 用户机安装时刻没有直接证据，全部用台账里的 `reporter_version` / body 版本行推断：#78 1.0.62（08-30 08:03）· #94 1.0.63（08-30 21:14）· #97 1.0.63（08-30 21:47）· #101 1.0.64（08-31 07:44）· #67 追加行 1.066 · #145 1.0.70 · #170 / #179 1.0.73。
- #145④ 出站半边、#61 中 #179、#63 中 #167、#170、#145② #172 都要 **1.0.74 装机**才对用户可见——回访文案里「1.0.74 起」四个字不能删。
- 提交时 index 里有他线暂存的 `unified_inbox.html` / `inbox_workspace.py`，本线只用 pathspec 提交自己三个文件，未动它们。
