# 指令 Q-36：入站图描述不夸大 + 「客户提议发图」意图不误答成拒发 + Q-23 群聊拟稿复验

> 预算 **$80**。工单 **#313（2JK95C，jun，1.0.84，09-12 12:49）**；顺带 Q-23 复验（skuio 账号仍在报障群拟稿）。落点表 `docs/发版对账_v1.0.85_Q36.md`。
> 前置：Q-25（FactGate 二期）、Q-20 D（刚发图认领 + 自曝黑名单）、Q-23（自动出站单一闸门 + 群聊永不自动软回，1.0.84）已在库。

## 事实（证据 `tmp_diag\2JK95C` / `W2CSGA`；代码只读核对）

- **#313 ①** 客户发的图是「几个菜」，AI 说成「一桌菜」——入站图 caption 进 prompt 后模型自由夸张；`inbound_enrich` 的图片描述没有「只说看到的、不加量词 / 程度词」约束，FactGate 也不把「入站图里有什么」当事实锚。
- **#313 ②** 客户说「有你的照片吗，这么多…」/ 提议**发自己的照片**给人设看，AI 按「客户索图」走了拒发 / 挡话模板（`claim_guard` / `media_promise` / `request_media` 链）。现有意图集只有 `request_media`（对方要我们的图），**没有 `offer_media`**（对方要发图给我们）——两种意图落同一条处置。`risk_grader.py` / `photo_capability.py` / `group_show/runtime.py` 各有一处 `request_media`，无 offer 分支。
- **Q-23 复验** W2CSGA / VYY22Q 报告里 skuio 账号 `7331682688` 对**报障群** `-1004345824259` 在 11:41–12:03 连续 `auto_generate_draft L1 … shadow=risk_hold:adult`（`autosend_policy` `mode=review forced=L1`）：不发是对的（Q-23 闸门生效），但**群聊 / 运维群根本不该拟稿**——每条群消息都跑一遍 LLM 拟稿 + 成人分级，白烧额度还污染草稿队列。

## 要做的四段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 入站图描述约束** | 入站图 caption 注入 prompt 时加系统句「图里只描述看到的具体物件与数量，不加『一桌 / 满桌 / 好多』类量词或程度词；不确定就不说」；`inbound_enrich` 的图片描述结果经 FactGate 落 `kind=observation`（TTL 24 h，含原 caption），出站里与它冲突的量词（数字 / 一桌 / 好几 / tons of）由 `outbound_text_guard` 软改成 caption 的原词 | `inbound_enrich.py`、`fact_gate.py`（只加 kind）、`outbound_text_guard.py` |
| **B offer_media 意图** | 意图分类加 `offer_media`（客户提议发自己的图 / 已发图问「看到了吗」/「要不要看我的」：中英日韩西葡词干各 ≥6）；处置 = 接受 + 期待（「发来看看」），**不走** `claim_guard` / `media_promise` / 拒发模板；`request_media` 判定加排除：句中主语是客户自己（我 / 我的 / I / my）时不算索图。`repeat_question_guard` 不把「发来看看」当重复 | `inbox/intent_*`（现有意图表所在文件）、`claim_guard.py`、`risk_grader.py` L(request_media)、`photo_capability.py` |
| **C 群聊不拟稿** | `drafts.auto_generate_draft` 入口：`is_group` 或 `peer_bot_guard.never_auto_reply_reason` 命中 `ops_group:*` → **直接 return，不拟稿、不分级**（只落一行 `[AutoDraft] skip reason=group`），与 Q-23 单一闸门同位；报障群 / 运维群 / 通知群一律；普通群（非 ops）保持 Q-23 现状 | `drafts.py` L200–260 入口、`protocol_autoreply.py` 钩子 |
| **D 门禁 + 回放** | `tests/test_media_intent_q36.py`：「几个菜」caption → 出站「一桌菜」被改回；「我发张我的照片给你看？」→ intent=offer_media，回复不含拒发模板词；「有你的照片吗」→ 仍 request_media；2JK95C 回放。`tests/test_group_no_draft_q36.py`：ops_group 消息 → 不调用 LLM 拟稿（mock 计数 0） | tests |

## 红线

- `request_media` 的风控语义（Q-17 分级 / Q-20 D 认领）不弱化——只是新增一个更精确的兄弟意图。
- A 段是软改写 + 事实锚，不是硬拦；改写必须落 `abort_ledger` 同源的可观测行。
- C 段只对 `is_group` / `ops_group` 生效；私聊拟稿路径一行不改。

## 开新对话粘贴

```
读 docs/指令_Q-36_入站图描述不夸大与客户提议发图意图与群聊拟稿复验_2026-09-12.md，按它做 Q-36（#313 + Q-23 复验：A 入站图 caption 注入加「只描述看到的具体物件与数量，不加一桌/满桌/好多类量词」系统句，caption 经 FactGate kind=observation TTL 24h 落事实，出站冲突量词由 outbound_text_guard 软改回原词 / B 意图集加 offer_media（客户提议发自己的图，六语词干各 ≥6），处置接受 + 期待不走 claim_guard / media_promise / 拒发模板，request_media 排除主语是客户自己 / C drafts.auto_generate_draft 入口 is_group 或 ops_group 直接 return 不拟稿不分级只落 skip 日志，普通群保持 Q-23 现状 / D 门禁 tests/test_media_intent_q36.py + tests/test_group_no_draft_q36.py + 2JK95C 回放）。预算 $80。先读 Q-25 / Q-20 / Q-23 落点表（在其上加，不重做），再 scripts\agent_probe.ps1 -Intent 登记；request_media 风控语义不弱化；A 段软改写不硬拦且落可观测行；C 段私聊路径一行不改。每个 commit 带 #313 并立刻补落点表 docs/发版对账_v1.0.85_Q36.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-36 行并 agent_probe -Done。
```
