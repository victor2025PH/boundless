# 指令 Q-36：客户「提议发图」不被当索图拒 + 入站图描述不夸大（#313）；群拟稿只复核不立项

> 预算 **$60**（首版 $80，20:5x 核对后缩：群聊拟稿一节降为复核）。工单 **#313（2JK95C，jun，1.0.84，12:49）**。落点表 `docs/发版对账_v1.0.85_Q36.md`。

## 已修 / 未修（HEAD `0b606199`）

| 项 | 状态 | 证据 |
|---|---|---|
| AI **自己**提议发图 → 客户短肯定 = 要图（offer-accept 桥） | **已修** | `src/ai/outbound_promise_guard.py` L932–960 `detect_media_offer` / `offer_accepted` |
| **客户**提议发自己的图（「我发张我的给你看？」/「有你的照片吗，这么多…」）被当 `request_media` 走拒发 / 挡话模板 | **未修** | 意图集无 `offer_media`；`risk_grader.py` / `photo_capability.py` / `group_show/runtime.py` 各一处 `request_media`，无客户侧 offer 分支 |
| 入站图 caption 进 prompt 后「几个菜 → 一桌菜」 | **未修** | `inbound_enrich` 图片描述无「只说看到的、不加量词」约束；FactGate 无 `observation` 类 |
| 群聊 / 报障群自动**发送** | **已修**（Q-23，1.0.84） | 日志 `mode=review forced=L1`，只拟稿不发 |
| 群聊 / 报障群自动**拟稿**（skuio `7331682688` 对 `-1004345824259` 11:41–12:03 连续 L1 稿 + 成人分级） | **待复核，不立项** | `autodraft_helpers.py` L1236 `guard_auto_draft_action` 在 LLM 前判，但 `ops_group` 名单来自**本实例**配置——skuio 实例没把我们的报障群配进去，所以照拟；这是配置 / 产品口径问题：要么「群聊默认不拟稿」当开关（老板拍板），要么 skuio 侧配名单 |

## 要做的三段

| 段 | 内容 | 文件 |
|---|---|---|
| **A `offer_media` 意图** | 现有意图表加 `offer_media`（客户提议发自己的图 / 已发图问「看到了吗」/「要不要看我的」：中英日韩西葡词干各 ≥6）；处置 = 接受 + 期待（「发来看看」），**不走** `claim_guard` / `media_promise` / 拒发模板；`request_media` 判定加排除：句中领属是客户自己（我的 / my / 私の）时不算索图；`repeat_question_guard` 不把「发来看看」当重复 | 意图表所在文件、`claim_guard.py`、`risk_grader.py`、`photo_capability.py` |
| **B caption 约束** | 入站图 caption 注入时加系统句「只描述看到的具体物件与数量，不加『一桌 / 满桌 / 好多』类量词或程度词；不确定就不说」；caption 经 FactGate `kind=observation`（TTL 24 h，含原 caption）；出站与它冲突的量词由 `outbound_text_guard` **软改**回 caption 原词并落可观测行 | `inbound_enrich.py`、`fact_gate.py`（只加 kind）、`outbound_text_guard.py` |
| **C 门禁 + 复核** | `tests/test_media_intent_q36.py`：「我发张我的照片给你看？」→ `offer_media`，回复不含拒发模板词；「有你的照片吗」→ 仍 `request_media`；「几个菜」caption → 出站「一桌菜」被改回；2JK95C 回放。群拟稿：只写一段复核结论进落点表（skuio 配置里 `never_auto_reply` 群名单是否含该群；产品口径待老板拍板），**不改代码** | tests、落点表 |

## 红线

- `request_media` 的风控语义（Q-17 分级 / Q-20 D 认领）不弱化——只是新增一个更精确的兄弟意图。
- B 段是软改写 + 事实锚，不是硬拦。
- 群聊拟稿路径本条一行不改。

## 开新对话粘贴

```
读 docs/指令_Q-36_入站图描述不夸大与客户提议发图意图与群聊拟稿复验_2026-09-12.md，按它做 Q-36（#313；先看「已修 / 未修」表，AI 自提议的 offer-accept 桥已在库不要重做：A 意图表加 offer_media（客户提议发自己的图，六语词干各 ≥6），处置接受 + 期待不走 claim_guard / media_promise / 拒发模板，request_media 排除领属是客户自己，repeat_question_guard 不把「发来看看」当重复 / B 入站图 caption 注入加「只描述看到的具体物件与数量，不加一桌/满桌/好多类量词」系统句，caption 经 FactGate kind=observation TTL 24h 落事实，出站冲突量词由 outbound_text_guard 软改回原词并落可观测行 / C 门禁 tests/test_media_intent_q36.py + 2JK95C 回放；群拟稿只把复核结论写进落点表不改代码）。预算 $60。先读 Q-25 / Q-20 / Q-23 落点表（在其上加，不重做），再 scripts\agent_probe.ps1 -Intent 登记；request_media 风控语义不弱化；B 段软改写不硬拦。每个 commit 带 #313 并立刻补落点表 docs/发版对账_v1.0.85_Q36.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-36 行并 agent_probe -Done。
```
