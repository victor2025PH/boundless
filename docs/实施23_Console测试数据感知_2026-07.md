# 实施23 · Console 测试数据感知（徽章 + 默认隐藏开关 + 统计分离）

> 日期：2026-07-19 ｜ 上承：实施22（归并实时化 / is_test schema v6 / KPI 排除测试）
> 主题：把 is_test 数据层能力透出到集团后台 UI——运营默认只看真实数据，测试数据可辨识可选看。

---

## 一、数据层：读取口径测试感知（我直接做）

- `ListOptions` 加 `includeTest?`（默认 false）；`runList` 统一注入
  `COALESCE(is_test,0)=0` 过滤——`listCustomers/Orders/Leads/Licenses` 一处生效、全部默认排除测试；
- `getStats`：headline 计数（customers/orders/leads/licenses/ordersByStatus/到期）**全部排除测试**，
  新增 `test:{customers,leads,orders,licenses}` 子对象供 UI 显示存在感；
- 行类型补 `is_test?`（UI 徽章用）；
- **列存在性按连接缓存**（`tableHasIsTest`）：迁移前旧库/只读快照无列时安全降级为全量，不误伤、不报错。
- **TS 自测 10 项全 PASS**（tsx）：stats 排除测试+test 子对象、各 list 默认排除、includeTest 带全、行携带 is_test。

## 二、Console UI：徽章 + 开关 + 统计分离（并行 agent）

- `TestBadge` 组件（低调石板灰「测试」，title 提示不计入 KPI/商机）；
- 客户/订单/留资/授权四列表：行内测试徽章 + 「显示测试数据」开关（URL `?test=1` 驱动，
  服务端读 searchParams → list 传 includeTest；默认只显示真实数据）；对应 4 个 API route 支持 `?test=1`；
- 总览统计卡：headline 是真实数，`test.X>0` 时副文案追加「+N 测试」（存在但不混入主数）；
- 客户 360：该客户 is_test 时页头显示徽章。

**生产实证**：`/api/console/customers` 默认 total=0（2 个测试客户已隐藏），`?test=1` → total=2。
运营现在打开控制台看到的就是干净的真实经营视图。

## 三、卫生整改：丢弃孤儿未提交改动

实施21 那个卡住的 agent 曾往 `ledger-lib.mjs` 写了一组 helper（isTestSignal/classifyContact），
但**从未被任何已提交代码引用**，且其 test-signal 正则与已上线的 `ledger-mark-testdata.mjs`
**口径分歧**（bare "test" 的词边界处理不同）。为避免 divergence 与困惑，已 `git checkout` 丢弃
该孤儿改动，只保留 HEAD 里已提交的 v6 迁移。教训：agent 半成品要么完成落地要么清理，不留悬挂。

## 四、相对方案的二次优化

1. **过滤下沉到 runList 单点**：不逐个 list 函数改 SQL，一处注入全部列表默认排除测试，不漏不重复；
2. **headline 排除 + 单列 test 计数并存**：既给干净主数，又不让运营"忘记还有测试数据"（避免以为数据丢了）；
3. **列存在性缓存 + COALESCE 双保险**：兼容迁移前旧库/只读快照，绝不因缺列崩溃；
4. **UI 交给 agent、数据层我自己把关**：正确性核心（过滤口径）自测后再交 UI，UI 与数据层零冲突并行。

## 五、扫码接入（等老板）

`tg_backend_scan.py` 本轮又弹了扫码窗口，等老板扫。

## 六、下一阶段（实施24 建议）

1. **老板扫码接总机** → 真机私聊冒烟 → **验证实时建档**（新客户即时出现在 Console，
   且不带测试徽章=真实客户）→ 渠道台账置 active；
2. **avatarhub `/api/refresh` 404**：限时授权业务出现前仍低优先，可决策端点归属；
3. **老板把 `D:\key-backups\` 复制到离线介质**；
4. 07-26 两节点：群聊开关评估 + enforce 灰度（enforce_readiness 每日 READY 留痕中）；
5. **可选**：cancelled 测试订单从 order-store JSON 主真相源侧清理（目前只在账本标 is_test，
   JSON 侧仍在——不影响展示，但彻底干净可下轮做）。
