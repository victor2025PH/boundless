# 问询：算力与模型全清单（智聊线 → 176 线）

**发起方**：117 智聊线（chengjie 引擎宿主）
**日期**：2026-08-29 00:30
**目的**：老板已把中枢职能交给智聊线，要求「让智聊用到所有的模型」。要做到这件事，
我需要一份**权威的能力清单**——现在我只能从端口探测和进程列表反推，已经因此误判过四次
（详见文末「我已经犯过的错」）。请按下面编号逐条回答，**能贴命令输出就贴原始输出**，
不必整理成文。

---

## 零、先说我为什么问：智聊当前实际在用的东西

这是从 `D:\chengjie-instances\zhiliao\data\config\config.local.yaml` 读出的生效值，
如果你发现哪条已经不对，请直接指出（这本身就是有价值的答案）：

| 能力 | 主 | 备 | 备2 |
|---|---|---|---|
| TTS 克隆 | `140:7852` CosyVoice3（`backend: avatar_clone`，全部人设） | `176:9000` hub（`tts_engine: index_tts` → 转发 `173:7865`） | edge_tts 通用声 |
| STT | `176:8765` large-v3-turbo | `140:7854` AvatarHub whisper | 本机 CPU funasr |
| 语音情绪 SER | `176:8765` | 本机 CPU | — |
| 识图 VLM | `176:11434` qwen3-vl（**当前探针超时**） | `198:11434` qwen3-vl（0.4s） | 硅基云 |
| 文本翻译 | `173:8001` vLLM chatx (qwen3.6-27b-abl-awq) | 云端 DeepSeek/硅基 | — |
| 嵌入 | `140:11434` bge-m3 | `176:11434` bge-m3 | — |
| 出图 | `176:8188` ComfyUI | **无**（零备胎） | 相册存货 |

**今晚我在你的机器上做过的事**（如实报备，都可逆）：

1. **173**：vLLM 从 20:57 死到 23:10（`utilization 0.93` 要 29.6 GB，但桌面常驻 8 GB + IndexTTS
   约 7.5 GB，永远起不来，systemd 在 `Restart=on-failure` 里空转了 2 小时）。我加了 drop-in
   `/etc/systemd/system/vllm.service.d/99-vram-fit.conf` 把 utilization 降到 **0.70**
   （`max-model-len 24576` / `max-num-seqs 32` 均未动），并把 `vllm_drain_wait.sh` 的阈值
   3000→12000。另外**重新启用了 `VLLM_Keepalive` 计划任务**——它此前处于「已禁用」状态、
   日志停在 08-14 23:36，而 vLLM 单靠 systemd 活不下来（WSL VM 会空闲回收）。
   **如果那个任务是你刻意禁的，请告诉我原因，我会改用别的方式维持。**
2. **198**：停了整套独立 AvatarHub（9000 主控 / 7855 fish / 7854 stt / 8000 换脸 / 7870 vcam /
   7878-7879 monitor_relay / 7910 live_director / AvatarHub.exe），并禁用了
   `AvatarHubDrill|Hub|Kick|Restart` 四个计划任务。依据是 **176:9000 的 `/api/engines`
   引擎表里没有任何 198 端点**，智聊配置也零引用。free 181 MiB → 5125 MiB。
   **文件一个没删**，`D:\AvatarHub\` 完整保留。
3. **104**：停了 `faceswap_api.py`(8000) + 禁用 `FaceSwap_Boot|Watchdog|AvatarHubKick`。
   free 6227 MiB → **11813 MiB（整机几乎全空）**。保留了往 `176:7913` 推屏的
   `desktop_agent.py` / `control_agent.py`。
4. **198 Ollama**：`OLLAMA_KEEP_ALIVE` 从 `-1`（永久驻留）改 `30m`、`CONTEXT_LENGTH` 8192→4096，
   并用 Modelfile 把 `qwen3-vl:8b-instruct` 的 `num_ctx` 钉成 4096——此前它在 `/v1` 兼容层
   下按原生 262144 上下文加载，实测吃 **26.59 GB**（一张 12 GB 卡装不下，必然溢出）。
   现在 5.2 GB。
5. **五台机器**共关了 166 个桌面进程（浏览器/网盘/灯效工具/动态壁纸等）并删了对应自启项。
   **实测总共只释放 994 MiB**——这条路我判断错了，收益微乎其微，已停止该方向。
   如果有人反馈「浏览器自己关了/开机不自启了」，是我做的，自启项可随时加回。

停完立刻跑了全域真探针，**智聊语音链完好**（`tts OK 1.8s index_tts@22050Hz`，
22050 Hz 是 IndexTTS-2 的采样率指纹，链路完整）。

---

## 一、TTS 引擎矩阵（**最高优先**，智聊要重选主力）

`176:9000/api/engines` 返回 8 个 TTS 引擎，5 个 `available=False`：

```
index_tts2   173:7865        available=True    <- 智聊现在经 hub 在用
fish_speech  127.0.0.1:7855  available=True
qwen3_tts    127.0.0.1:7858  available=True
cosyvoice    173:7852        available=False
gptsovits    127.0.0.1:7853  available=False
xtts         127.0.0.1:7851  available=False
moss_ttsd    173:7866        available=False
voxcpm2      127.0.0.1:7856  available=False
```

**1.1** 这 5 个 `False` 的，逐个说明：**模型文件在不在磁盘**（在哪）、**能不能拉起**
（怎么拉）、**拉起后占多少显存**。我要区分「没装」和「只是没起」——前者要下载，
后者我自己就能拉。

**1.2** 三个 `True` 的引擎，各自的**定位差异**：音质、单句延迟、支持语种、是否支持
零样本克隆、是否支持情绪标签。智聊现在无脑用 `index_tts`，我不知道这是不是最优选。

**1.3** `services` 里还有 `sbv2_tts: true`（Style-Bert-VITS2？），但引擎表里没有它。
它是什么、走哪个端口、和上面 8 个什么关系？

**1.4** **53 个音色 profile**（`/health` 的 `profile_count: 53`）：这些是什么？
智聊的人设声音能不能直接用这批 profile？如果能，怎么按 profile 名调用？

**1.5** `cluster_mode.parked` 里有 `emotion_tts`——智聊文档里记的「7852 CosyVoice3 情感克隆」
就是它吗？它被 parked 是不是意味着**智聊配的 `140:7852` 其实已经不在服务**？
（探针显示 140:7852 智聊侧可用，但 hub 认为 `173:7852` 不可用，我怀疑这里有两套东西。）

---

## 二、STT：为什么有四个，该用哪个

我在 176 上看到**三个 STT 服务并存**，加上 140 一共四个：

```
176  funasr_stt_server.py       (D:\projects\模仿音色\)
176  dolphin_stt_server.py      (D:\projects\模仿音色\)
176  C:\aitr_asr\asr_server.py  (8765, large-v3-turbo + SER)  <- 智聊主用
140  7854 AvatarHub stt_server.py                            <- 智聊备用
```

**2.1** 这四个的关系是什么？是历史遗留还是各有用途？
**2.2** 哪个准确率最高、哪个最快？智聊该用哪个当主、哪个当备？
**2.3** `cluster_mode.parked` 里的 `nemo_stt` 是第五个吗？值得拉起吗？
**2.4** `funasr_stt` 和 `dolphin_stt` 如果是冗余的，能停哪个？（176 现在跑 11 个服务，
CPU/IO 争抢已经让识图探针超时了。）

---

## 三、识图 / VLM

**3.1** 除了 Ollama 的 `qwen3-vl:8b-instruct`，集群里还有更强的 VLM 吗
（比如 72B 量化版、或者 InternVL/MiniCPM-V 系）？在哪台机器、装没装？
**3.2** `176:11434` 现在探针超时（模型驻留正常、GPU 利用率 2%，判断是机器过载）。
你那边看 Ollama 日志有别的线索吗？
**3.3** `aitr_ocr`（`C:\aitr_ocr\ocr_server.py`）是什么？智聊的图片翻译现在走 VLM，
如果有专用 OCR 可能更准更快——它支持哪些语种、走哪个端口？

---

## 四、出图（**这是智聊唯一零备胎的能力**）

**4.1** `176:8188` ComfyUI 装了哪些模型？逐个给**模型名 + 文件路径 + 大小**
（SDXL？FLUX？SD1.5？LoRA？）。
**4.2** 它是 `--lowvram` 启动的，实际推理时峰值显存多少？
**4.3** **104 现在整机空出 11.8 GB**，我想在那里装第二个 ComfyUI 做备胎
（同时给 176 减负）。请给：
   - 模型文件能不能直接拷（哪些必须拷、哪些可省）
   - Python 环境怎么建最省事（有没有现成的 requirements/conda env 导出）
   - 有没有踩过的坑（驱动版本、xformers、CUDA 版本要求）
**4.4** `cluster_mode.held` 里有一堆和视频/换脸相关的（`ditto` `echomimic` `latentsync`
`lipsync` `musetalk` `singing` `vcam` `faceswap/faceswap2`）——**智聊完全不用这些**。
它们是常驻占显存，还是按需拉起？如果常驻，能不能全部转为按需？

---

## 五、翻译

**5.1** `173:8001` vLLM 跑的是 `qwen36-27b-abl-awq`（24576 ctx）。集群里还有 `hy-mt2`
（我在 140/176 的 Ollama 里见过）——两者在**翻译**这个具体任务上哪个更好？有没有实测对比？
**5.2** vLLM 那个模型带 `--reasoning-parser qwen3`，智聊调用时必须显式传
`chat_template_kwargs.enable_thinking=false`，否则 `content` 返回 null（thinking 内容
吃光 max_tokens）。这是**已知行为**还是配置可以改掉？
**5.3** `wss://openspeech.bytedance.com/api/v4/ast/v2/translate` 出现在 hub 的引擎表里
（字节的同传接口）——那是什么用途？有 key 吗？智聊能不能用？

---

## 六、机器归属与「哪些能动」

**6.1** 请给一份**全集群服务归属表**：每台机器上每个常驻服务，标注
`必须常驻` / `按需拉起` / `已废弃可停`。这是我最需要的东西——今晚我停 198/104 之前
只能靠「hub 引擎表里没引用」来推断，风险很高。
**6.2** 176 现在跑 11 个服务（含我方的 `hud_server.py` / `xiaojie_server.py`）。
哪些可以迁到 104/198？迁移有什么依赖（比如必须和 hub 同机）？
**6.3** `sign_worker.py --watch 20` 是什么？在轮询什么？
**6.4** `boundless-hud`（`desktop_agent.py` / `control_agent.py` → `176:7913`）是老板的
监控看板吗？我保留了它，确认一下。

---

## 七、还有没有我不知道的算力

**7.1** 除 `117 / 140 / 173 / 176 / 198 / 104` 之外，还有别的机器/GPU 吗？
（配置文件里见过 `192.168.0.188:7855`、`192.168.1.49:7860`、`192.168.1.43` 这些历史地址，
现在还在吗？）
**7.2** 云端资源：硅基流动的 key 额度、字节同传、其他云 API——哪些还有效、额度多少？
**7.3** 有没有闲置/可征用的机器？（智聊要装 ComfyUI 备点、可能还要第二个 vLLM。）

---

## 八、最急的三条（如果时间有限只答这些）

1. **§六.1 全集群服务归属表**——没有它，我每一步都在猜。
2. **§四.3 ComfyUI 迁 104 的具体做法**——出图是智聊唯一零备胎的能力。
3. **§一.1 + §一.2 TTS 引擎的真实状态与定位**——语音是智聊的核心卖点，
   我需要知道现在用的 `index_tts` 是不是最优。

---

## 附：我已经犯过的错（说明为什么需要权威清单）

今晚这几个小时里，我靠端口探测和进程列表反推拓扑，错了四次：

1. 把 `173` 当成「只跑 vLLM 的单一服务机」，实际它还跑 IndexTTS(7865) + HUD 代理——
   而 IndexTTS 正是智聊 TTS 链的实际执行者，差点因为「给 vLLM 腾显存」把它停掉。
2. 把 `198` 当成「闲置的 4070」，实际是完整 AvatarHub 节点。
3. 把 `104` 当成「零 AI 负载」，实际跑着 faceswap。
4. 把进程的**内存占用**当成**显存占用**（nvidia-smi 在 Windows 上对图形进程只显示 `[N/A]`），
   于是判断「集群有 20 GB 显存被桌面软件浪费」，实测只有 994 MiB。

还读错过一次文件：`173:/root/start_vllm_nohup.sh` 是 08-15 就废弃的手动脚本（跑 qwen2.5），
真正生效的是 `vllm.service`（qwen3.6-27B），我据废弃脚本宣布「文档全错」，还照它启动了
错误的模型（幸好显存不足失败了）。

这些都是「没有权威清单只能反推」的必然产物。有了 §六.1 那张表，这类错误就不会再发生。
