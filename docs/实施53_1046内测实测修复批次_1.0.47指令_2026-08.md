# 实施53：1.0.46 内测实测修复批次 → 1.0.47 指令

（值守线汇总 2026-08-22 09:40。总账=《实施49》B14-B44 各行，本文档是可执行指令收敛；
证据截图在 `D:\chengjie-instances\zhiliao\data\protocol_media\telegram\`，
诊断包 3DTSV9 解包在 117 `C:\Users\Administrator\diag_3DTSV9\`。
施工纪律照旧：动手前 `scripts\agent_probe.ps1 -Intent` 登记；.py 攒批走重启窗；
模板/i18n 热更 bump `ui-build.txt`；收口 `gate_sweep.ps1`。）

---

## P0（客户实测致伤/功能死，先修）

### P0-1 B41 重复回复（两种形态并案）
**现象**：①同稿双投——给真实客户的三连回复整组原样发两遍（`_234`，03:09）；
②同问双答——同一条来消息被写两份不同措辞的回答发出（`_241`，03:24 起持续，
网关流水铁证：19:26:53.440 与 19:26:55.713 两次同规模生成相隔 2.3s）。
**根因面（诊断包 3DTSV9 实证）**：向导「全自动档」一键打开三类发送通道——
`inbox.l2_autosend`(worker+deliver)、`companion_send_gate`、跟进 SOP 工作链
（该会话挂着进行中的「新客破冰」链，workflows 页自述「自动推进档话术步自动拟稿
交自动发送管线」）——同一会话多通道叠加出稿。同稿双投另涉 03:04:36 应用重启后
投递队列重放（fatal log 佐证重启时刻）。
**修**：①同一会话同一时刻仅允许一条通道出稿（通道互斥仲裁：会话有活跃工作链→
抑制常规自动回复，或统一并入同一出稿队列做语义去重）；②投递幂等钉
（draft_id 为幂等键，投递前 CAS 置 sent，重启恢复不重放已 sent）；
③修复需 1.0.47 装包多通道场景实测验收。
**涉**：`src/inbox/autosend_worker.py`、companion/proactive 链、workflows 引擎、
`capability_presets.py` full_auto 语义。

### P0-2 B39 「跟进 SOP（工作链）管理」页整页崩
**现象**：页顶红横幅「页面脚本出错（_TYPE_LABELS）」，列表永卡「加载中」（`_231`）。
**根因**：`src/web/templates/workflows.html` 405/450/659 三处引用 `_TYPE_LABELS`，
定义已不存在（疑近期批次删除未同步）→ 初始化 ReferenceError。
**修**：补回定义或改走 i18n 词条表，三处同源；**加「模板 JS 未定义引用」门禁**防复发；
横幅亮内部变量名违反人话铁律，文案并入 B24-② 批。

### P0-3 B23 人设绑定状态打架（回炉，v=20260821a 批复测未过）
**现象**：绑定已生效（「当前生效 steven（旧绑定）」+绑定更新 toast），但 composer
橙条「未绑人设·账号1」+按钮下「请先在上方选择一个人设」红字同框矛盾；
跨会话均复现（Nicks/Hjv/钧，`_214`/`_217`/`_245`/`_246`/`_250` 共五图=验收用例）。
**漏点**：①「旧绑定生效+下拉未选」组合仍走「先选人设」分支；②composer 橙条这
第四个面根本没接同源。
**验收**：绑定生效后四个面（状态行/下拉预选/按钮取值/composer 条）全一致，
1.0.47 装包按五图场景实测。

---

## P1（流程死锁/产品安全/生效性）

### P1-4 B37 全自动开启流程重做（两位内测用户 100% 卡死，证据链完整）
1. 向导第 4 步撤「应用此档位」按钮、第 5 步撤「发送测试消息」，全向导纯图文教导
   （B35 续；`welcome.html`；档位应用逻辑保留给新入口）。
2. **点「全自动」档位时同步引导/询问打开真发**（skuio 验收原话：不许藏第二道开关
   让人猜）。
3. `capability_toggle.py:71` 等护栏文案去 `auto_ai` 黑话。
4. **客户包真发开关直达入口**：nav flavor 裁剪后矩阵总览无「高级管理区/能力看板」
   露出（`_260`/`_262`），设置页「上游总闸」卡的「去矩阵总览的能力看板调整」链接
   =断头链接（把用户引进不存在区域，`_253`→`_260` 绕圈实录）；且向导只在首装弹一次
   （重看仅 `--first-run` 参数）→ **用户完成向导后再无任何 UI 能碰真发开关=完全死锁**
   （08-22 06:38-09:30 实录）。断头链接按 flavor 隐藏或改指新入口。
5. 上游总闸的开关变更纳入「档位变更历史」（skuio 的开关昨夜被未知动作关回，不可追溯）。

### P1-5 B44 群聊会话默认排除自动回复（产品安全）
**现象**：钧客户端开全自动+「新会话自动沿用」后，其 AI 把报障群当客户会话
**以钧的身份自动代答**（05:59-06:53 五条+一条语音，秒回、逐句复述值守话术、
支持方口吻，警告后 5 秒仍接话）——用户 AI 在任意群里以假乱真代言。
**修**：群聊会话默认排除自动回复（需显式开启）；「新会话自动沿用」不得覆盖群聊。

### P1-6 B43 克隆语音客户包整体不可用（两位内测 100% 复现，08-22 晚定性升级）
**现象**：登记显示成功，试听/实际发声仍 edge_tts 通用音色（「由 avatar_clone 回落」
小字，`_248`）。诊断包实证 `avatar_voice.enabled: false`（hosted 自动配置关闭态），
登记流程未联动开启引擎。
**修**：①登记成功即联动 `avatar_voice.enabled` 翻转（hosted 形态）；②回落时显式
提示「克隆音色暂不可用，正用通用音色」而非小字；③并入 B25-① 一起验收；④**两登记入口行为统一**（工具箱入口先查开关直接拦「引擎未接入」（钧 `_278`，诊断包 83V85P 同款 enabled:false）、人设页入口不查假成功——修后同一语义）；⑤该报错话术重写（托管已开通却让「联系官方开通」，并入人话批）。

### P1-7 B42 身份混淆守卫
**现象**：AI 用自己人设名称呼客户——对客户 Nicks 说「you're not that old, Steven」
（`_236`，首例偶发）。
**修**：①prompt 注入处锚定 peer_name+写死「对方称呼只能来自客户资料，绝不用自己
名字称呼对方」；②出稿守卫扫「用自己人设名呼叫对方」样式，命中拦下重写
（与 world-clock/fabrication 守卫同族）。

---

## P2（收纳/性能/双语/排版）

- **P2-8 B25-① 克隆音色进人设页改版**：语音页签放「登记克隆音色/从已登记选」前置
  （`_212`/`_248`：现状 BACKEND/VOICE ID 技术填空+登记区藏底部）；`voice_profile`
  等技术字段收高级折叠；工具箱只留「生成语音」。**B26 浮层撤除同批**（决策材料在
  实施49 B26 行，等老板拍板后执行）。
- **P2-9 B38 首启加载慢**：量测首启各段耗时（后端 sidecar 冷启动主嫌疑）能压则压；
  压不掉的把「正在载入工作台…」换成「首次启动初始化中，仅此一次」进度提示（`_209`）。
- **P2-10 B25-③ 英文版三件**：预置 SOP 跟进链模板双语化（`_219`：框架已英、模板
  本体中文写死）；桌面壳原生菜单 SHELL_STR 迁完（`_223`）；提示气泡中英夹杂治理
  （「Stage upgrade pending: 初识→试探/升温」）。
- **P2-11 排版+额度可见性**：会话列表左上角状态控件文字截断挤竖条（`_193`/`_216`）；
  B15 额度耗尽显式徽章+续费提醒（昨晚用户 21%→2% 烧穿全程无感知实证）。
- **P1-8（08-22 晚补入）B46 回复引用陈旧图片话题**：客户发酒店图、AI 回几小时前的粉丝面（`_271`，识图面板本身描述正确=生成抢跑未消费当前图 enrich）；修=回复链等待/消费当前消息识图结果，超时不引用图片话题；验收=隔时发两张不同图各对各图。（归 P1：直接影响真实客户对话质量）
- **P1-15（08-23 上午补入）B43回炉+B55 并案：托管能力存量激活专项**：voice enabled:false 存量机未被自动激活（_305 登记死锁：拦检→登记不了→联动开不了）+ vision 全天网关零调用；查 ensure_hosted_* 在升级安装上的触发链（仅领取时跑？boot 不重跑？_hosted_auto 语义失效？），目标=存量/新装一致激活；**对照证据（10:47Z 网关）**：钧机 register_spk 200=其 1.048+ 托管语音已激活，skuio 机仍 enabled:false——复现差异收窄到安装/配置世代；报错文案「请联系官方开通」在已开通托管下为假话，并 B24-② 人话批。
- **P1-18（08-23 午间补入）B64 messenger 接入验证死锁**：登录窗机器人验证渲染一小块不可放大→FB 不下发 2FA 码→接入卡死（_316/_317）；修=①登录窗缩放/最大化 ②换浏览器逃生口 ③等码文案实情说明。
- **P1-17（08-23 午间补入）B63 messenger 发送失败**：RPA 发送失败且报错「messenger send failed:」空原因（_314）；修=①失败原因透传到 UI（人话）②等现场信息定位发送环节；其诊断通道坏（B53）取证受限。（12:33 补充：跨会话持续=会话过期嫌疑，已引导账号态自查；③失败消息零留痕→修=失败留痕+一键重发。）（14:47 定性：500=本机 messenger-web 边车内部失败（PIN/浮层/登出族），http_error_detail 已有但 /send 调用点未接→修①接上透传真因 ②浮层/登出态上卡提示。）
- **P1-16（08-23 上午补入）B61 生成语音无声**：clone 200/真字节/时长正常但内容疑静音（176 高压态）；修=①clone/ogg 路径静音检测拦下重试 ②hosted 客户 TTS 纳入 voice_outage 台账 ③高压态排查（voice-truth 专项衔接）。（10:56 复测仍无声=持续性；已发 .ops ASK_from_duty_20260823_hub_silent_audio 交语音线。）**附 B62**：skuio 机工具箱生成回落 edge_tts 透传克隆名报 ValueError 裸码——回落映射+人话报错并入 P1-15 一体修。
- **P1-14（08-23 上午补入）B59 紧急停发误冻+无出口**（值守建议 P0）：升级掉线的 auth 族错误被 ban_signal 判「被封」永久冻结外发；客户包无解除入口、文案称「运营手动冻结」误导；修=①登录成功自动解除 auth 族 auto_ban+banned 标记 ②登录变更窗口降档 ③客户包解除入口 ④文案按 actor 实情。（B57 同因并案）
- **P1-13（08-23 上午补入）B57 升级后 TG 会话全体失效**：嫌疑=升级时旧进程未退净、双进程抢会话触发 AuthKeyDuplicated 注销；复现=老版登录→原地升级；目标=升级绝不需要重登（并作为 B59 的输入源头一并修）。
- **P2-23（08-23 午后补入）B65 LINE 接入弹窗错标 Facebook**：共享表单占位键 `inbox.connect.relay_email_ph` 未按平台分化（_325）；修=占位平台参数化 zh+en，LINE 是否露账密表单交产品确认，IG/Zalo/WA 同款核查。
- **P2-22（08-23 上午补入）B60 会话列表滚动不达底**：底部「已归档」入口行被截断不可达（_307，单账号筛选视图）；修=滚动容器高度/底部留白计算；unified_inbox 热区注意 agent_probe 协调。
- **P2-21（08-23 上午补入）B58 标签不可改删**：补标签改名/删除/取消关联（_302）。
- **P1-12（08-23 晨补入）B56 译文镜像地名偷换**：出站英文 Cebu 正确、操作员中文译文变「马尼拉」（_297）；修=①翻译提示词专有名词保真铁律 ②译后地名/数字对账校验，不符重翻或括注原词；客户侧无影响（原文正确）。
- **P1-11（08-23 凌晨补入）B55 未经识图接图瞎猜**：人像照被回「你家猫」（_295，1.048 互聊实测）；实证=网关全天 vision 事件 0，hosted-vision _hosted_auto 存量机未生效嫌疑；修=①vision 通路装配核查+存量补齐 ②无识图结果硬闸/诚实降级（与 B46 并案）③称呼保守化（并 B42 族）。
- **P1-10（08-23 凌晨补入）B53 求助上传路径级故障**：持续报「连不上官网服务」而同机网关聊天 200 正常（昨晨还曾上传成功 PJ6ZVM）；查 support 面板→本地后端转发→官网这段链路；报错区分三类人话（本地未就绪/官网不可达/被拒）。（03:12 排查结论：两路同 urllib 同代理语义、非传输库差异；ZIP 大件官网入口零到达=线路掐大包嫌疑最大；修复补：失败自动重试、mini 包降级重传、远程诊断上传腿 hosted_gateway:993 同故障一并修。）
- **P1-9（08-23 凌晨补入）B51 全自动复读对方原话**：AI 把对方刚说的话一字不差回发（`_285`，两测试者 AI 互聊暴露）；修=查该请求生成记录定位+出稿检查「回复不得与对方上一条雷同」。（归 P1：直接损害对话质量与可信度）
- **P2-19（08-23 凌晨补入）B54 人设输入框偶发无光标**：人设填写时输入框有时无光标无法输入（_291 求助面板备注带出）；嫌疑 B47 状态机/B19 焦点族；先向用户要复现路径再修。
- **P2-18（08-23 凌晨补入）B52 人设自述状态矛盾**：刚说要睡觉、转头主动消息说刚健身完要洗澡（`_287`，proactive_opener）；修=人设近期自述入短期状态记忆，主动/后续生成须衔接，矛盾拦下重写（与 B50 同族）。
- **P2-17（08-23 凌晨补入）B50 客户档案记忆未消费**：已获取的客户年龄后续又复问（`_284`）；修=档案属性捕获即入画像+生成注入+禁复问已知项。（08:21 复报 `_299`：真实客户三次自报 38 岁仍被问年龄段——值守建议升 P1，该图为验收用例。）
- **P2-16（08-23 凌晨补入）B49 主动关怀预览超时**：点「看 AI 会说什么」报「AbortError: signal is aborted without reason」（`_282`）；修=预览异步化/等待加长+报错人话「生成超时请重试」；黑话报错并入 B24-② 批。
- **P2-15（08-22 晚补入）B48 通知归拢**：同一事件双重打扰（新消息提醒+草稿待审各弹一次；顶栏另有急需处理/发前确认多入口）；方向=一事件一提醒，消息与对应草稿合并单一待办入口（产品评估后实施）。
- **P2-14（08-22 晚补入）B47 人设编辑器丢弃后锁死**：新建人设→确认丢弃未保存修改→编辑器无响应（`_273`，草稿恢复条与丢弃弹窗状态机打架嫌疑）；修=丢弃后表单复位、两套未保存状态合一；验收=丢弃后立即可再编辑。
- **P2-13（08-22 晚补入）B45 未读角标已读后复活**：点开已读的会话过会儿又跳「1 条新消息」，多会话复现（`_269`）；已读状态持久化+同步不回退，验收=点开→退出→数分钟不复活。
- **P2-12 诊断包补 app.log**：`src/utils/diag_upload.py` 加收 app.log 尾部 2MB
  （3DTSV9 实战暴露缺口：只有 fatal/sidecar 无主日志）。

---

## 待老板拍板（不动手）
B26 浮层撤除定稿、B32 计划类主动消息免逐条确认、B34 额度上限自定义。

## 运行中事项（值守线自持，勿动）
B40 内测无限额度（至 08-29，网关 env 已生效，用量归档 `GwUsagePull` 每日 23:55）；
远程诊断请求 dr-mt3ch24q（钧机 B990）pending 待其开机自动上传。

---

## 施工状态（执行线回写 2026-08-22 12:35；代码已随 12:28 重启窗装载 zhiliao）

- **P0-1 B41 ✅落码**（autosend-simplification 线 0731 批交付，本线合流审计）：
  deliver_once 幂等（in-flight/dup_text/CAS sent_at）+ 通道互斥三层（链 hook 顺延/
  生成后复查丢弃/worker 批内取消）+ run() 双循环护栏；65 例绿；线上观测
  `autosend-status.worker.deliver_once`/`total_skipped_mutex` 在位。
  ⏳ 装包多通道实测＝下一个包（1.0.47 已于 0343 发布、不含本批——按包序应为 1.0.48）。
- **P0-2 B39 ✅落码**：`workflows.html` 补回 `_TYPE_LABELS`（三处渲染同源）；
  新门禁 `tests/test_template_undefined_identifiers.py`（B39 形态：读取型裸标识符
  全文件无声明=必然 ReferenceError；探测器自证+防过期表；已进 gate_sweep），
  首轮全站扫出并修掉 `ops_overview.fmtTs`、`knowledge.openEntryEditor` 两个同族隐病。
  横幅人话文案留 B24-② 批（未动）。
- **P0-3 B23 ✅落码**：cp-persona `_legacyResolved` 单一解析（下拉预选/来源行/按钮
  取值三面同源，服务端 legacy 层/生效者兜底——复测五图的「boundId 撞不上键位」
  病根收口）+ composer 身份条缓存先画不看 TTL（过期仍触发校正，消 30s 橙闪脉冲）；
  双树镜像 v=20260822a；门禁 `test_p1_copilot_wiring.py` ⑥ 段。⏳ 五图场景装包实测。
- **P1-4 B37 ✅落码**：向导④⑤撤实操按钮改纯图文（onbApplyTier/onbSendTest 移除，
  档位应用路由保留给新入口）；capability_toggle 拒因去黑话（父开关 label 反查、
  auto_ai/config 路径不再外泄）；设置页「高级微调」链接随侧栏单源探测显隐（断头
  链接收口）；feature-center 开关翻转纳入 companion_capability_audit.jsonl（B37-5
  最后一个不落账写入口）。真发引导/主控/standby 捆绑=sibling 0731 批已交付。
- **P1-5 B44 ✅落码**：群自动回复豁免改配置 `inbox.auto_draft.group_autopilot_platforms`
  **默认空**（任何平台的群未经坐席确认一律封 review；bootstrap/批量对齐原有「绝不
  代写群档」铁律不变）；zhiliao overlay 显式 `[telegram]` 保留内部 A 线群策略语义
  （生产零行为变化）；客户包（desktop.min 无此键）自动获得默认排除。
- **P1-6 B43 ✅落码**：enroll avatar_clone 成功即联动（hosted 形态 `_hosted_auto`
  且未 opt-out 时持久化翻开 `avatar_voice.enabled` + 当场重跑 ensure_hosted_voice）；
  cp-voice 回落警示人话化「克隆音色暂不可用，本条用的是通用音色…」（zh/en 双树，
  cp-i18n v=20260822f 四 host）。⏳ 与 B25-① 同批验收。
- **P1-7 B42 ✅落码**：prompt 身份锚补镜像方向（「你的名字绝不能用来称呼对方，
  对方称呼只能来自客户资料」）+ 出稿呼格守卫第三臂（persona_guard
  `strip_vocative_self_name`：只抹呼格名不动句子本体；对方名未知/同名不判；
  自介语境排除；`persona_guard.vocative.enabled` 可关）；_236 原话进金标（38 例绿）。
- **P2-9 B38 半件✅**：壳侧真·首启文案 `conn.first_boot_init`（localStorage 一次性
  判定，「仅此一次」绝不对老用户复读）；⏳ 首启分段耗时量测待专项。
- **P2-12 ✅落码**：诊断包收运行时 logger 树全部 file handler 落点（`logs/app/` 下
  每文件尾部 2MB，优先于边角日志写入）——3DTSV9「只有 fatal/sidecar 无主日志」缺口闭。
- **P2-8 B25-①/B26、P2-10 英文三件、P2-11 排版额度**：⏳ 排后续批次（B25-① 需
  unified_inbox+personas 热区专场+浏览器门禁；B26 等老板拍板同批）。
- 待老板拍板三项与运行中事项未动。

---

> **接续**：08-23 值守报障批次（B53-B65 及 B45-B52 遗留收敛）已另立《实施64_0823值守报障批次集中修复_1.0.52指令》，执行线从该文档接单。
