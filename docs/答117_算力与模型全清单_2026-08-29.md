# 答 117 智聊线 · 算力与模型全清单（中枢 176）

- 时间：2026-08-29 01:15 左右（中枢本机实测）
- 问询原文 `D:\boundless\docs\问询176_算力与模型全清单_2026-08-29.md` 在 176 上**不存在**（`docs\` 下无此文件）。按你贴的三条急问 + 顺手三问作答。
- 权威源：`cluster_map.json`（显存账）+ `app_config.py` `SERVICES`（端口/环境）+ `env_config.bat` `SVC_*`（路由）+ `D:\boundless\deploy\boundless-hud\modes.json`（三模式编制）。下面「今晚实测」是打端口/HTTP 的原始结果，不是账本。

---

## 0. 今晚先看这一段（否则会继续误判）

### 0.1 `/api/engines` 的地址栏是过期硬编码，不要当路由真相

你看到的 `index_tts2 173:7865 True` **一半对、一半错**：

- `available=True` 对：Hub 健康表 `index_tts: true`。
- `173:7865` 错：那是 `engine_registry.py` 写死的展示 URL。热改路由在 `hub_config.json`：

```json
"index_tts": "http://127.0.0.1:7865"
```

智聊经 Hub 打的 IndexTTS **实际落在 176:7865**，不是 173。今晚两边都活着（见 0.3）。

同理：`qwen3_tts` 展示 `127.0.0.1:7858`，本机 7858 **TCP CLOSED**，真身是 **117:7858**（HTTP 200）。`cosyvoice` 展示 173:7852，那是 Hub 的 `SVC_EMOTION_TTS`；智聊克隆语音主力其实是 **140:7852**（三模式不许泊，今晚 HTTP 200）。

### 0.2 算力模式现在是 `unknown`（不是 chatx）

`GET /health` → `cluster_mode.mode = "unknown"`。  
上次成功切到 chatx：2026-08-28 17:47。  
22:21 boot-restore 再切 chatx **失败回滚失败**：173 SSH 超时、198:9000 超时、140:7854 超时。姿态巡检 `checked=0`。

后果：编制是「chatx 残留 + 失败回滚」的混合态，不能用「现在是智聊模式」去推断谁该停。

### 0.3 今晚活体（原始）

176 本机 `nvidia-smi`：

```
NVIDIA GeForce RTX 5090, 22718 MiB / 32607 MiB, util 4%
可见 GPU 进程：fishspeech\python.exe、indextts\python.exe、Ollama llama-server.exe
（WDDM 下 per-process used_memory 全是 [N/A]——这就是你把进程内存当显存会错的根因）
```

TCP（OPEN/CLOSED）+ 关键 HTTP：

| 点 | 结果 |
|---|---|
| 176:9000 Hub | OPEN /health 200 |
| 176:7855 Fish | OPEN model_loaded=true |
| 176:7865 IndexTTS | OPEN model_loaded=true **（Hub 真路由）** |
| 176:7861 SBV2 | OPEN ready，权重未装进 GPU（lazy） |
| 176:7864 FunASR | OPEN CPU SenseVoice-Yue |
| 176:7867 Dolphin | OPEN CPU 22 方言 |
| 176:8765 aitr_asr | OPEN large-v3-turbo CUDA + emotion2vec |
| 176:8188 ComfyUI | OPEN `--lowvram`，torch 未占卡 |
| 176:7913 HUD | OPEN clients=0 |
| 176:7851/7853/7856/7866/8000/8090/7930 | CLOSED |
| 173:8001 vLLM chatx | OPEN served=`chatx` qwen36-27b-abl-awq max_model_len=24576 |
| 173:7865 IndexTTS | OPEN model_loaded=true **（不该和 :8001 同卡）** |
| 173:7852 Cosy | CLOSED |
| 173:8010 | TCP OPEN，HTTP 被 RST（portproxy 残体，不是活 coder） |
| 140:7852 Cosy | OPEN models_loaded=true **（智聊克隆主力）** |
| 140:7854 Whisper | OPEN large-v3-turbo，NLLB 仍 loaded=true（chatx 本应卸掉） |
| 140:7857 Nemo | CLOSED |
| 117:7858 Qwen3-TTS | OPEN |
| 198:9000 Lite Hub | CLOSED（你已经停了） |
| 198:11434 Ollama | OPEN，钉着 `qwen3-vl:8b-instruct` 5.5G（不是编制里的 qwen3:8b） |
| 104:8000 换脸热备 | CLOSED（你已经停了） |
| 104:8188 Comfy | CLOSED（还没拉起来） |
| 104:22 SSH | OPEN |

176 ollama `/api/ps` 此刻只有 `qwen3-vl:8b-instruct` 5.1G（hy-mt2 蒸发）。140 ollama：`bge-m3` keep_alive 钉到 2318 年 + 另有一条 qwen3-vl。

---

## 1. 全集群服务归属表

三选一口径（对 **chatx / 智聊要用全集群** 这一目标）：

- **必须常驻**：智聊链路此刻就要用，停了会断。
- **按需拉起**：face/出片/日语才要；chatx 下应泊或懒加载。
- **已废弃可停**：退役/封存/端口被占用，不要再当 TTS/算力。

显存是账本数（`cluster_map.json`），不是 `tasklist` 工作集。

### 117 声备 · RTX 3060 12G · `shengbei`

三模式 **untouchable**（modes.json）。本机 TTS 08-14 已退役为坐席机，只留一条现役。

| 服务 | 端口 | 模型 | 显存账 | 归属 |
|---|---|---|---|---|
| qwen3_tts `Qwen3TTS_Boot` | 7858 | Qwen3-TTS 0.6B Base（展示写 1.7B，117 实跑 0.6B） | 3.2G | **必须常驻**（Hub `SVC_QWEN3_TTS`，今晚 True） |
| emotion_tts `EmotionTTS_Boot` | 7852 | CosyVoice3 热备 | 3.3G | **已废弃可停**（任务已 Disabled；与 qwen3 同卡会 OOM 硬死） |
| 智聊坐席本身 | — | DeepSeek 云端主链（老板锁 cloud） | 不占 3060 给集群 TTS | 你们自己的，中枢不动 |

### 140 听写 · RTX 4070 12G · `tingxie`

| 服务 | 端口 | 模型 | 显存账 | 归属 |
|---|---|---|---|---|
| CosyVoice `emotion_tts` | 7852 | Fun-CosyVoice3-0.5B + **/v1/vc** | 3.3G | **必须常驻**。modes.json 硬拦：任何模式泊 140:7852 = 执行器 400。117 本机 TTS 退役后这是智聊克隆唯一主力。今晚活着。 |
| STT `STT_Boot` | 7854 | faster-whisper **large-v3-turbo** | 1.5G | **必须常驻**（Hub `SVC_STT`）。今晚 NLLB-600M 仍挂着（chatx 应卸，restore 失败留下的） |
| Nemo `NemoSTT_Boot` | 7857 | Nemotron 3.5 流式 | 2.5G | **按需**（face/同传）。chatx/code 泊。今晚 CLOSED |
| ollama bge-m3 | 11434 | bge-m3 | 0.7G | **必须常驻**（invariants，智聊嵌入主路） |
| ollama hy-mt2 | 11434 | hy-mt2-7b | 5.5G 满血 | **按需**（chatx 钉、face/code 卸）。今晚 **没钉上** |

### 173 韵声 · RTX 5090 32G · `yunsheng`

**不是「只跑 vLLM 的单一服务机」。** 三模式互斥整卡：

| 模式 | 应在跑 | 显存账 |
|---|---|---|
| chatx | 只 `vllm.service` :8001 served=chatx，qwen36-27b-abl-awq 0.9 池 | 29.4G |
| face | IndexTTS :7865 10G + Cosy :7852 3.5G + gemma4 7G | 20.5G |
| code | 只 `vllm-coder.service` :8010 | 28.0G |

| 服务 | 端口 | 模型 | 显存账 | 归属 |
|---|---|---|---|---|
| vLLM chatx | 8001 | qwen36-27b-abl-awq | 29.4G | chatx **必须常驻**；face/code 必须停。今晚活着 |
| vLLM coder | 8010 | Qwen3.6-27B-abl-awq | 28.0G | code **必须**；chatx 必须停。今晚端口有残、HTTP RST |
| IndexTTS `IndexTTS` | 7865 | IndexTTS-2 fp16 | 10.0G | **按需**：face 在 173；chatx 应泊、改由 176 跑。今晚 **173 仍 loaded=true，和 :8001 抢卡** |
| Cosy `EmotionTTS` | 7852 | CosyVoice3 | 3.5G | **按需**（face 质量轨）。chatx 泊。今晚 CLOSED。权重在 **173** `C:\models\cosyvoice`（176 无此目录） |
| MOSS-TTSD | 7866 | MOSS-TTSD-v1.0 8B | 26G | **已废弃可停**（任务 DISABLED，与 IndexTTS 互斥）。权重保留 `C:\models\moss-ttsd\weights\` |
| ollama | 11434 | gemma4 仅 face | 7G | chatx 应空。今晚 `/api/ps` = `[]` |

SSH 必须 `admin@173`（不是 Administrator）。

### 176 中枢 · RTX 5090 32G · `zhongshu`

底座（三模式都在）：Hub :9000、ollama bge-m3 0.7G、FunASR CPU 0、Dolphin CPU 0、live_director CPU 0。desktop_misc 记账 2G。

chatx 编制（底座 2.7 + 模式 24.6 = 27.3 ≤ 30）：Fish 2.5 + SBV2 1.5 + hy-mt2 4.9 + qwen3-vl 5.7 + **IndexTTS 本机 10.0**。

| 服务 | 端口 | 模型 | 显存账 | 归属 |
|---|---|---|---|---|
| avatar_hub | 9000 | — | CPU | **必须常驻** |
| fish_tts | 7855 | Fish-Speech 1.5 | 2.5G | chatx/face **必须**；code 泊 |
| sbv2_tts | 7861 | SBV2 JP-Extra 林小玲 | 1.5G | chatx/face **按需**（日语）；懒加载，空闲不占 GPU |
| index_tts | 7865 | IndexTTS-2（本机权重 `C:\models\index-tts\checkpoints`） | 10G | chatx **必须**（宿主在 176）；face 切回 173 |
| ollama qwen3-vl:8b | 11434 | 视觉 | 5.7G | chatx **必须** |
| ollama hy-mt2 | 11434 | 翻译 | 4.9G | chatx **必须**（今晚蒸发了） |
| ollama bge-m3 | 11434 | 嵌入 | 0.7G | **必须**（invariants） |
| ollama coder-30B | 11434 | qwen3-coder-abl-64k | 21G | 仅 code |
| funasr_stt | 7864 | SenseVoice-Yue CPU | 0 | **按需**（同传粤语）。零显存，可留 |
| dolphin_stt | 7867 | Dolphin 22 方言 CPU | 0 | **按需**（同传方言）。零显存，可留 |
| stt（本机副本） | 7854 | — | — | **不要在 176 拉**。`SVC_STT` 指向 140 |
| faceswap / faceswap2 | 8000/8003 | inswapper+增强 | 10+2.2 | 仅 face |
| lipsync / ditto / vcam | 8090/8096/7870 | MuseTalk/Ditto/OBS | 4+5+? | 仅 face |
| interpreter / live_director | 7900/7910 | 同传/声援 | CPU | 仅 face |
| voxcpm | 7856 | VoxCPM2 | ~4G+ | **按需**（chatx 编制停它） |
| singing :7853 | 7853 | YingMusic-SVC（**不是 GPT-SoVITS**） | GPU | **按需**出片 |
| xtts :7851 | 7851 | Coqui 退役 | — | **已废弃可停** |
| moss 本机 :7866 | 7866 | — | — | 不要在 176 起（权重在 173，26G） |
| ComfyUI | 8188 | FLUX fp8 | 空闲 0，出图时大 | **按需**。今晚还在 176 空转 |
| aitr_asr | 8765 | whisper turbo + emotion2vec | GPU（未入中枢账本） | **智聊自有件**，见 §4 |
| hud_server | 7913 | 集群实况 HUD | CPU | **必须常驻**（老板看板） |
| xiaojie | 7912 | 桌面小界 | CPU | 保留 |
| sign_worker | 无端口 | 官网签发队列 | CPU | **必须常驻**（见 §6） |
| gateway | 7930 | 对外 API | CPU | 今晚 CLOSED；对外才必须 |

### 198 口型 · RTX 4070 12G · `kouxing`

这台 **不是闲置 4070**。chatx/code 编制 = 泊 lipsync + 钉口语 LLM。

| 服务 | 端口 | 模型 | 显存账 | 归属 |
|---|---|---|---|---|
| AvatarHub Lite | 9000 | 控制面 | CPU | **必须常驻（控制面）**。cluster_mode 泊/唤 lipsync 走 `198:9000/api/engine/*`。你停掉它 → 今晚 k1 超时、模式卡死。**停 lipsync ≠ 停 Lite Hub** |
| lipsync | 8090 | MuseTalk 热备 | 7.7G 满载账 / 实测可低到 ~2.2G | **按需**（face 热备；chatx 应泊） |
| ollama | 11434 | 编制是 **qwen3:8b** 9.7G | 9.7G | chatx/code **必须**（口语化备点）。今晚实际钉的是 **qwen3-vl:8b-instruct 5.5G**，和 SSOT 不一致 |

`LipSync_Standby` 计划任务是 faceX 旧遗物、已 Disabled，别碰。

### 104 脸备 · RTX 4070 12G · `lianbei`

三模式 **untouchable**（动员成本高、无 Ollama）。

| 服务 | 端口 | 模型 | 显存账 | 归属 |
|---|---|---|---|---|
| faceswap `FaceSwap_Boot` | 8000 | inswapper+hyperswap+gfpgan | 5.5G | **按需热备**。主路由已是 176:8000。主引擎挂了才 PATCH 指回 104。你为 Comfy 清空 11.8G = 主动放弃热备，可以，但要 **Disable `FaceSwap_Boot`**，否则开机自启会抢卡 |
| AvatarHub（若有 Lite） | 9000 | — | — | **已废弃可停**。104 不在 Hub 编排里，今晚 9000 CLOSED |
| ComfyUI（你正在迁） | 8188 | 见 §3 | 12G 吃紧 | 迁完后这才是这台卡的主业 |

**结论（你问能不能停 198/104 的 AvatarHub）：**

- **104 的 Hub / 换脸热备**：可以停，好迁 Comfy。代价 = 换脸无局域网回退。把 `FaceSwap_Boot` 禁用，不要只杀进程。
- **198 的 Lite Hub :9000**：**不要停**。停的是编排入口，不是「闲置占用」。lipsync 可以停；Hub 留下。Ollama 按编制钉 qwen3:8b（或你们改 SSOT）。今晚 198 Hub 已死、模式切换已经炸过一次。

---

## 2. TTS 引擎真实状态

Hub 默认引擎是 **fish_speech**（`engine_registry.default_engine`），不是 IndexTTS。智聊无脑钉 index 是「质量轨」选择，不是全集群最优实时选择。

### 2.1 五个 `available=False`：没装 vs 没起

#### cosyvoice（展示 173:7852）—— **装了，chatx 编制故意没起**

- 权重：生产在 **173** `C:\models\cosyvoice`（176 `C:\models\cosyvoice` **不存在**）。140 另有一份正在跑的 Cosy（智聊主力）。
- 能不能拉：能。face 模式 `remote_wake`；或 SSH 173 `schtasks /Run /TN EmotionTTS`。**不要在 chatx 下和 :8001 同开**（3.5G 但 0.9 池几乎顶满）。
- 显存：173 账 3.5G / 140 账 3.3G。
- 你要的克隆：打 **140:7852**，别等 Hub 引擎表变 True。

#### gptsovits（7853）—— **已退役，不是「没起」**

- GPT-SoVITS 运行时 2026-06 清空。7853 现由 **YingMusic-SVC 翻唱**占用。registry 明确 `deprecated: true`。
- 拉起来也不会变回 TTS。唱歌走 Hub `/avatar/sing`。
- 不要当克隆引擎。

#### xtts（7851）—— **已退役（许可证 + Coqui 停运）**

- 不是「权重丢了起不来」。Coqui 权重在 TTS 库用户缓存，退役理由是 **CPML 禁商用 + 上游停运**。
- 7851 留给 `sapi_tts_server.py`（Windows SAPI 应急，同契约）。灾难时手动拉，Hub 有 `_tts_fallback_skip`。
- 不要当生产 TTS。

#### moss_ttsd（展示 173:7866）—— **装了，任务禁用，与 IndexTTS 互斥**

- 权重在 **176 也有一份**：`C:\models\moss-ttsd\weights\MOSS-TTSD-v1.0`（4×safetensors，约 15.6G）。生产任务在 173 `MOSS_TTSD` DISABLED。
- 拉起：173 启任务。占 **~26G**，和 IndexTTS-2、和 vLLM 都互斥。
- 对话感盲听曾胜出；现网质量轨已让位 IndexTTS。chatx 不要动。

#### voxcpm2（7856）—— **装了，chatx 编制停它**

- 权重：`D:\projects\模仿音色\models\VoxCPM2\model.safetensors` 4.27G + audiovae 360M。今晚 176:7856 CLOSED。
- 拉起：`POST http://127.0.0.1:9000/api/engine/start?name=voxcpm`（会走显存闸；chatx 下 176 已近满，大概率拒）。
- Apache-2.0 可商用、30 语、48k。要商用实时克隆时再排，别和 IndexTTS+Fish+VL 叠。

### 2.2 三个 True：怎么选（针对智聊）

| | IndexTTS-2（你在用） | Fish-Speech 1.5 | Qwen3-TTS |
|---|---|---|---|
| 真身今晚 | **176:7865**（表写 173） | 176:7855 | **117:7858**（表写 127.0.0.1） |
| 音质定位 | 整段质量天花板；音色/情感/时长三解耦 | 中文 WER 最好（标称 0.54%）；对话默认 | campplus 克隆相似度 **0.76 vs fish 0.37** |
| 单句延迟 | 放弃实时；旧标定 8–10s/轮。健康里 `stream:true` 是分段吐，不是 Fish 那种首包 | compile 后 5090 RTF ~0.3；对话首块 200–300ms/字 | 3060 上 RTF>1，**非实时**；定位离线批配音。官方 1.7B 首包可到 ~97ms，你们跑的是 0.6B |
| 语种 | 中文为主 + 情感参考 | 17 语 + instruct | 10 语 |
| 零样本克隆 | 是 | 是 | 是（3 秒极速克隆） |
| 情绪标签 | **最强**：8 维向量 / 情感参考音 / 文本描述，音色保持 | instruct + 自然语言情感 | **否**（Base 无 instruct） |
| 许可 | B站 IndexTTS，商用需授权 | CC-BY-NC-SA（内部可用，公开卖不行） | Apache-2.0 可商用 |
| 建议 | 口播、情绪戏、要「像本人且能换情绪」 | 对话 TTFA、同传兜底 | 要像、可商用、可等几秒 |

智聊还应该知道的第四条（引擎表里没有）：**140:7852 CosyVoice** — Apache-2.0、emotion/instruct、零样本、有 `/v1/vc`。Hub `/api/tts_only` 的 `instruct`（「用四川话说」）走的就是 Cosy instruct2，**绕过 IndexTTS**。

无脑 IndexTTS 的代价：每句数秒、占 176 10G、和 VL/Fish 抢 5090。对话请用 Fish 或 140 Cosy；情绪口播再钉 `tts_engine=index_tts`。

### 2.3 `profile_count=53` 是什么

**不是 TTS 音色包。** 是 AvatarHub 角色库（脸+声+档案），SQLite `avatar_profiles.db`。今晚 active=`001`。

全名：

```
001, 云帆, 何俊生, 侯三乐, 刘德华, 刘德华-净版, 刘德华-降噪, 夏晚荷, 夏阳, 安岚, 小帅,
张景光, 张景光-智聊, 彭思远, 无一, 晓桐, 晴子, 晴子-智聊, 暖乔, 林以东, 林佳欣, 林佳欣-智聊,
林小玲, 林小雨, 林小雨-智聊, 梁文轩, 江澈, 沈泉雪, 沈砚清, 温晏, 白鹭汀, 秦立, 秦震,
美月, 美月-智聊, 苏婉, 苏若初, 苏麦, 范铁林, 茵晴, 言知, 赵明德, 赵明德-智聊, 郝大山,
钟鼎年, 陈以谦, 陈美玲, 陈美玲-智聊, 陈默, 顾沉舟, 马跃, 魏卡斯, 魏卡斯-智聊
```

带 `-智聊` 的是给你们对人设的。人设声音 **可以直接用**，调用：

```http
POST http://192.168.0.176:9000/api/tts_only
{"profile":"林小雨-智聊","text":"你好","tts_engine":"index_tts","format":"ogg"}
```

- `profile` = 上表名字（404 = 拼错）。
- `tts_engine` 可空（走角色质量轨 / 默认 fish）；钉引擎用 `index_tts` / `index_tts2` / `fish_speech` / `qwen3_tts`。未知名 400，不静默换路。
- `instruct` 非空 → 改走 140/173 Cosy instruct2（方言/语气），不再走 IndexTTS。
- 列表：`GET /profiles?fields=name,has_voice,has_face,lib`。

### 2.4 `sbv2_tts:true` 但引擎表没有它

**Style-Bert-VITS2 JP-Extra**，日语情感 TTS，林小玲专训（JVNV 融合）。

- 端口 **7861**，`SVC_SBV2_TTS=http://127.0.0.1:7861`。
- 权重 `C:\SBV2\Data\LinXiaolingJVNV`（今晚 ready，GPU 未加载）。
- 不进 `/api/engines`：registry 没注册，只在 `app_config.SERVICES` + `/health.services`。
- 拉起：`POST /api/engine/start?name=sbv2_tts`。占约 1.5G。
- 智聊日语人设用它；中文别走它。

---

## 3. ComfyUI 迁 104

### 3.1 schnell fp8 是不是 all-in-one？还要不要 pulid / insightface / EVA02？

**对，无脸图这一条够。** 智聊 `comfy_infer.py` 用的是 `CheckpointLoaderSimple`，ckpt 名 `flux1-schnell-fp8.safetensors` / `flux1-dev-fp8.safetensors`，clip/vae 都从这一个文件出，**不是** UNETLoader + 分离 clip/vae。

**人设自拍不够。** `--face-ref` 会插入 PuLID 链，还要：

| 文件 | 176 现成 | 作用 |
|---|---|---|
| `flux1-dev-fp8.safetensors` 16.06G | `D:\ComfyUI\models\checkpoints\` | 锁脸必须走 **dev**（PuLID 在 dev 上训的）。schnell 只给物体/风景 |
| `pulid_flux_v0.9.1.safetensors` 1.06G | `models\pulid\` | PulidFluxModelLoader |
| insightface **antelopev2**（onnx 一套，zip 344M） | `models\insightface\models\antelopev2\` | PulidFluxInsightFaceLoader |
| `EVA02_CLIP_L_336_psz14_s6B.pt` 817M | `models\clip\` | PulidFluxEvaClipLoader |
| custom_node `ComfyUI_PuLID_Flux_ll` | 已装 | 缺了节点直接报 class_type 不存在 |

还要拷 **整个 custom_nodes 里 PuLID 那包**（或在 104 按同样依赖装），不是只拷权重。

### 3.2 workflow 节点

无脸：CheckpointLoaderSimple → CLIPTextEncode ×2 → EmptyLatentImage → KSampler → VAEDecode → SaveImage。schnell **不挂** FluxGuidance。

有脸：再加上 LoadImage + PulidFluxModelLoader + PulidFluxInsightFaceLoader + PulidFluxEvaClipLoader + ApplyPulidFlux，KSampler 的 model 改接 ApplyPulidFlux。

生产 URL 现在写死 `http://192.168.0.176:8188`。迁 104 后改 `COMFY_URL`。

### 3.3 12G 卡上的弱卡方案

集群 **没有** 现成 SDXL / SD1.5 出图编制。176 `checkpoints\` 只有两个 FLUX fp8，unet/vae/loras 空。没有「另一台机跑 SD1.5」这回事。

可选：

1. **无脸**：schnell + `--lowvram`，4 步。会从 5090 的数秒变成 4070 的几十秒，但这是库存里唯一的蒸馏弱卡路径。
2. **锁脸**：dev fp8 + PuLID 在 12G 上会很痛（InsightFace CUDA + EVA + UNet offload）。建议自拍仍打 176:8188，104 只接无脸；或接受极慢。
3. 不要幻想 176 账本里还有一张闲 4090 跑 SDXL。

今晚 176 Comfy **还在跑**（`--listen 0.0.0.0 --port 8188 --lowvram`，torch 未占卡）。迁走前先停 176 这份，避免双实例。

### 3.4 踩过的坑

- 176 启动就是 `--lowvram`（`D:\ComfyUI\_runcomfy.bat`），5090 都 offload，104 更必须。
- 环境：`D:\Miniconda3\envs\comfyui`，torch **2.11.0+cu128**（5090 = sm_120）。104 4070 用 cu128 也可以，别装旧 cu118。
- 首次 `--face-ref` 会下 antelopev2 ~360MB + EVA02 ~850MB；176 网络慢，**不要在 104 上现下**，从 176 拷。
- PuLID `start_at=0` 会把姿态钉死成同一张正面照；线上用 `COMFY_PULID_START_AT` ~0.1–0.2。
- 出图有跨进程锁 `comfy_infer.lock`，防和换脸栈峰值叠加 OOM。104 若只跑 Comfy 可忽略，但别和 FaceSwap_Boot 同开。
- xformers：这套 workflow 不依赖它；缺了也能跑，别为 xformers 折腾 CUDA。

---

## 4. 四个 STT 是什么关系（176 三个 + 140 一个）

| 谁 | 端口 | 模型 | 设备 | 主人 | 准/快 | 能不能停 |
|---|---|---|---|---|---|---|
| **140:7854** | Whisper **large-v3-turbo** | GPU | 中枢 `SVC_STT` | Hub 对话/提取/同传回落 | 准、较快 | **不能停** |
| **140:7857** | Nemotron 流式 | GPU | 同传主 ASR | 更快（块 320ms） | chatx 下本应泊，今晚已 CLOSED |
| **176:7864 FunASR** | SenseVoice-Yue | **CPU** | 同传粤语 | 粤语 CER 远好于 Dolphin（6.3% vs 26%） | 零显存。同传不用可停，不停也不抢 GPU |
| **176:7867 Dolphin** | small.cn 22 方言 | **CPU** | 同传方言 | 广谱方言；粤语别走它 | 同上 |
| **176:8765 aitr_asr** | **同一款** large-v3-turbo + emotion2vec | **CUDA** | **智聊自己的**（`C:\aitr_asr`，不在 Hub 编目） | 热 ~0.3–0.4s ASR / SER ~44ms | 中枢不用它。176 识图超时、11 服务抢 IO/GPU：**这是真占 5090 的那份 Whisper**。要腾 176 卡，优先评估能不能改打 140:7854（同模型）只留 SER 在 176，或把 aitr_asr 迁走 |

所以不是「四个并列 ASR」。Hub 主链只有 **140:7854**；176 两个方言是 CPU 旁路；**aitr_asr 是你们 07-11 部署的智聊专用 GPU ASR**，和 140 功能重叠、和 Fish/Index/VL 抢同一张 5090。

---

## 5. 173:8001 `--reasoning-parser qwen3` → content=null

**已知行为，不是坏了。** Qwen3 思考模型在 vLLM 里会把思维链拆到 `reasoning_content`，`content` 在 thinking 打开时可以是 null。chatx 底模是 qwen36-27b-abl-awq（思考型）。code 的 :8010 同样带这个 parser。

改法：

1. **调用方继续传** `chat_template_kwargs.enable_thinking=false`（你们现在的做法，正确）。
2. 不要为了省事拆掉 `--reasoning-parser`：thinking 开着时会把 `<think>` 泄漏进 content。
3. 服务端改默认要想清楚：unit 里写死 chat template 会让真要深度推理的请求变笨。智聊主链已锁 cloud，:8001 是嵌入/视觉/兜底，默认关 thinking 合理，但那是 173 unit 的事，改之前先备份。

今晚 :8001 `max_model_len=24576`（不是注释里的 64k）——上下文预算以 `/v1/models` 为准。

---

## 6. sign_worker 和 HUD

### sign_worker.py --watch 20

**不是模型、不是监控。** 官网授权签发机：每 20 秒拉 `bd2026.cc /api/console/sign/pull`，用本机 `secrets/license_vendor_sk.pem` 签 Ed25519，再 `complete` 回填。私钥永不上 VPS。计划任务 `AvatarHub_SignWorker`。

**保留。** 停了客户授权会停在「签发中」。

### boundless-hud :7913

**是。** 老板的集群实况看板（数字雨 HUD + 六机壁纸板同源数据）。

- `hud_server.py :7913` 只跑中枢，任务 `BoundlessHudServer`。今晚 200，`tv=yunsheng`，`tv_on_air=false`，clients=0。
- `desktop_agent`：各机只读截屏推到 7913，不是远控。
- `control_agent`：指挥舱手势/甩屏，T0 呈现层。

**保留。** 与算力编制无关，CPU 服务。

---

## 7. 给智聊「用到所有模型」的老实话

不是每台卡上每个进程都该常驻。三模式是互斥整卡：chatx 要 173 的 27B vLLM，就 **不能** 同时要 173 的 IndexTTS+Cosy；要 176 的 IndexTTS+VL+Fish，就 **不能** 同时要换脸直播栈。

智聊现在真正该咬住的活件：

1. 173:8001 vLLM chatx  
2. 140:7852 Cosy + 140:7854 Whisper + 140 bge-m3  
3. 176:9000 Hub + Fish +（质量轨）IndexTTS 本机 + qwen3-vl  
4. 117:7858 Qwen3-TTS  
5. 198 ollama 口语备点（Hub 控制面留着）  
6. 出图：176:8188 或迁完的 104:8188  

今晚已经偏了的：173 IndexTTS 没泊、198 Hub 被停、104 换脸热备被停、176 hy-mt2 蒸发、140 NLLB 没卸、aitr_asr 和 140 各跑一份 turbo、Comfy 还在 176。要归位走中枢 `POST /api/cluster/repair`（force 重放当前目标）；目标请先和老板确认是 chatx 还是继续 `unknown` 混跑。

---

## 附录 · 原始探活（节选）

```
GET /health cluster_mode.mode=unknown  held=[ditto,echomimic,faceswap,faceswap2,interpreter,latentsync,lipsync,live_director,singing,vcam,voxcpm]
parked=[ace_studio,emotion_tts,hair,nemo_stt]
services true: fish_tts,funasr_stt,index_tts,sbv2_tts,stt,qwen3_tts

173:8001/v1/models id=chatx root=/root/models/qwen36-27b-abl-awq max_model_len=24576
176 /api/ps: qwen3-vl:8b-instruct size_vram=5.51G
140 /api/ps: bge-m3 keep_alive→2318 + qwen3-vl…
198 /api/ps: qwen3-vl:8b-instruct 5.51G
104:8000 timeout  198:9000 timeout
```
