# 指令 微信线（实施97 续做）：企微客服 · 个人微信 PC 副驾 · 企微 SSO · relay 中继

> 底座 `docs/实施97_微信接入智聊_企微客服与个人微信PC副驾_开发方案与实施_2026-09.md`（八轮实施记录 + 「接入生产操作单」）。本指令是它的**续做单**：代码已于 2026-09-10 18:5x 以 **`f00c4d00`** 全部入库（96 文件 +12842/−70，全部开关默认关），此后的目标是**联调真账号、进安装包、生产开启**，不是再写功能。
> 预算 **$150**（真企微账号联调 $80 / 副驾生产化 $40 / 中继运维 $30）。落点表新开 `docs/发版对账_v1.0.80_WX.md`（每个 commit 一行）；收工只改 `docs/Q批_进度总账.md` 末尾「独立线」区加一行 WX。
> **发版归属**：1.0.79 已于 09-10 18:0x 发出，**不含**本线；本线随 **1.0.80** 进包（见 §4 进包条件）。

## 0. 入库现状（f00c4d00 带了什么 / 没带什么）

| 入库 | 内容 |
|---|---|
| 线 A 企微客服 | `src/integrations/wechat_kf{,_worker,_webhook,_setup}.py`、`src/inbox/kf_window_guard.py`（48h·5 条）、`compliance/{__init__,runtime,disclosure}.py` 的 `platform_scope` / `platform_of_conversation_key` 强制披露、`official_api_worker.OFFICIAL_PLATFORMS += wechat_kf`、转人工 / 接待状态（`unified_inbox_send_routes` kf/{close,session-state,transfer} + 工作台 `_syncKfTransferBtn` `_renderKfSession`）、`routes/wechat_kf_setup_routes.py`（`/api/setup/wechat_kf/*` ×7） |
| 线 B 个人微信 PC 副驾 | `src/integrations/wechat_pc/`（13 文件：capture / identity / policy / risk_screens / send_guard / service / uia_backend / win32_windows / env_check / backend / `__main__`）、`src/web/desktop_bridge_presence.py` 心跳、`channel_adapters._enqueue_desktop_bridge`、`health_watchdog._bridge_driver_problems`（桥接掉线并入同一告警链）、`routes/wechat_pc_setup_routes.py`（`/api/setup/wechat_pc/*` ×5）、`tools/wechat_pc_{autostart,devlink}.ps1`、`scripts/wechat_pc_probe*.py` |
| 入口 / 引导 | `templates/connect_guide.html` + `i18n_packs/connect_guide.py`（`/workspace/connect/{wechat_kf,wechat_pc}`）、`login.html` + `auth_user_routes` 企微扫码入口、`routes/wecom_login_routes.py`（`/login/wecom`, `/login/wecom/callback`, `/api/auth/wecom/{status,verify-file}`）、`src/integrations/wecom_sso.py` |
| 中继 | `relay/`（VPS 服务 app.py + systemd + nginx 模板 + deploy 脚本 + 5 测）、`src/integrations/relay_client.py`（设备端 WS 出站） |
| 共享文件 | 只带本线 hunk（index-only、保 HEAD 换行）：`unified_inbox.html` 26/31 块（排除工作树因基线旧于 HEAD 而「删掉」的 Q-14 aif ×4 + `ai_last_fail` 1 块）、`health_watchdog.py` 4/11（ops 告警线 7 块未带，仍在其活动窗）、`config.example.yaml` 2/4（`wechat_kf:` 52 行 + `platform_login.wechat_kf/wechat_pc` 20 行）、`config.desktop.internal.yaml` 渠道边界一句、`test_admin_route_inventory.py` 只带 `/api/auth/wecom/verify-file`（DY 代登记 6 行未带） |
| 测试 | 11 个本线测试文件 136 例 + `test_douyin_e2e_alignment` platName 断言改为「注册表回落仍在」契约（实施97 起 platName 先查两个微信平台 i18n 名） |
| **没带** | `config/ops_events.db`、`pc_runner_audit.jsonl`、`companion_capability_audit.jsonl`、`@AutomationLog.txt`（运行产物，永不入库）；`tests/test_line_sync_bootstrap.py.reqseq`（8 字节残片，可删）；cost 线 / ops 告警线 / 小智线 / Q 线其余脏改 |

入库树验证（`D:\boundless-wx` = f00c4d00 干净 worktree）：导入扫雷 1220 模块 0 坏；本线 136 + 相关门禁 214 绿；既有红仅 `test_i18n_extra_langs::test_extra_lang_overrides_subset_of_zh`（HEAD 的 `zh_hant_auto` 含 cost/小智未入库键，R79 已归因）、`test_group_show_console` ×5、`test_messenger_send_failures` ×2（M-2 C 未入库）——**均非本线**。

gate_sweep（入库树）21 红 vs R79 worktree 26 红：本线在 R79 表上的 9 项旧红**全消**（`test_admin_route_inventory` 20 端点 / `workspace_i18n_keys` kf.transfer 缺 zh / `kfTransferToHuman` layer1 / `python_undefined_names` / `template_undefined_identifiers` / `inline_js_syntax` 等）；新增 4 项中 2 项本线已由 **`183e2322`** 修（`wechat_kf_setup_routes` 字面直读 `orchestrator_enabled` → `resolve_login_switch`；`wechat_kf_webhook.py` 登记进 `_TAKEOVER_CALLER_ALLOWLIST`），`ui-build` 戳同提交 bump 到 `20260910-1908`，`t.cat_'+ / t.pol_'+` 点拼接归 Q-15 D（`inbox.adult.*`，非本线）。其余 17 项为 R79 已归因的旧债 / 他线棘轮。

## 1. 事实（联调前必知）

- 全部开关默认关：`wechat_kf.enabled=false`、`wecom_login.enabled=false`、`relay.enabled=false`、`platform_login.wechat_pc.tier=copilot`（只读建议不发）。未配则：worker 不注册、回调路由不挂、登录页无企微按钮、副驾账号卡不出现。**安装包带上它等于零行为变化**。
- 生产 zhiliao 17:31 被 Q-8 线重启时载入的是**主干工作树**（含本线脏树），所以生产上本线代码**已经在跑**（默认关）；`f00c4d00` 只是把同一份代码钉进 git，不需要为它再重启。
- 无真实企微账号 → §6「有凭证 E2E」从未做过；欢迎语人设化、`msgmenu↔multi_choice` 未闭环。
- 中继 `https://relay.bd2026.cc/healthz` 200（`devices_online:0`）；`prod_edge_watchdog` 未纳入巡检；设备表无过期清理。
- 联调残留：`192.168.0.149:18898` portproxy + 防火墙规则 + 18898 实例；后台 09:10/09:13 两条重复测试行。
- 线 B 已知上限：非文本气泡只占位；同文无时间条会合并；无逐条精确时间戳；风控屏真机未触发过（不能拿主人的号触发）。

## 2. 要做的六段（按序，前三段不依赖真账号）

| 段 | 内容 | 验收 | 文件 |
|---|---|---|---|
| **A 门禁进 R 批清单** | 把 11 个本线测试文件 + `tmp/r79_import_sweep.py` / `r79_import_names_sweep.py`（导入扫雷，25 s）纳入 `scripts/gate_sweep.ps1` 的固定清单；`test_workspace_template_js_syntax` 已是模板 JS 门禁——确认它在 `-Full` 之外也跑 | `gate_sweep.ps1` 输出出现 `wechat_*` 行；扫雷脚本从 `tmp/` 挪到 `scripts/import_sweep.py`（tmp 不入库） | `scripts/gate_sweep.ps1`、新 `scripts/import_sweep.py` |
| **B 中继运维闭环** | ① `prod_edge_watchdog` 加 `relay.bd2026.cc/healthz` 探活（同 katie 域名一档告警）；② `relay/app.py` 设备表 24h 无心跳清理 + `/healthz` 带 `devices_stale`；③ `relay/deploy_relay.ps1 -Status` 只读核对 | `relay/tests/test_relay.py` +2 例；VPS 上 `systemctl status chatx-relay` active | `src/ops/prod_edge_watchdog.py`（或其所在）、`relay/app.py`、`relay/tests` |
| **C 联调残留清理**（主人拍板后执行） | 撤 portproxy / 防火墙 / 18898 实例 / 联调计划任务；删 09:10/09:13 两条重复行（先 dry-run 列出主键，再删） | `netsh interface portproxy show all` 无 18898；`Get-ScheduledTask -TaskName 'ChatX WeChat PC Copilot*'` 只剩生产那条或没有 | 无代码改动，只记操作到落点表 |
| **D 企微真账号联调**（拿到 CorpID/Secret/open_kfid 后） | 按实施97 §6 有凭证清单：`/api/setup/wechat_kf/test` 凭证测试 → 选客服账号 → 客户扫码发一句 → 入站落库 → AI 起草 → 配额条 1/5 → 第 6 条 409 `policy_window_quota` → 转人工 → 接待状态 3 → 结束会话；欢迎语人设化（`wechat_kf_worker` 的 enter 事件走人设 opening，不用罐头句）；`msgmenu` ↔ `multi_choice` 双向 | 端到端截图 6 张进落点表；`tests/test_wechat_kf_worker.py` +3（welcome_persona / msgmenu_in / msgmenu_out） | `wechat_kf_worker.py`、`wechat_kf.py` |
| **E 副驾生产化** | 桌面壳内置「PC 副驾」启停（`desktop/` 主进程拉起 `python -m src.integrations.wechat_pc --tier …`，替代计划任务）；`tier` 三档（copilot / semi / auto_reply）在账号卡可切；掉线 → `health_watchdog` 桥接告警已接，确认 ops 群真收到一条 | `tests/test_wechat_pc_core.py` +2；`desktop/test/*.test.js` 加壳启停 1 例；真机：关微信 90 s 内 ops 群告警 | `desktop/main.js`（或 sidecar-launcher）、`wechat_pc/service.py` |
| **F 进包与对外口径** | 安装包默认配置三开关仍关（`config.desktop.internal.yaml` 勿开）；`product_facts` 已写「微信客服（企微官方通道）已支持；个人微信为 PC 副驾非官方渠道」——对外只说这两句；`docs/平台能力矩阵.md` 再生 | `npm run dist:win` 预发门禁全绿；包内 `resources/backend/_internal/src/integrations/wechat_pc/` 存在；小智被问「支持微信吗」按事实卡答 | `scripts/platform_capabilities*`、`docs/平台能力矩阵.md` |

## 3. 红线

- **不改生产配置**（`config.local.yaml`）——生产开启按实施97「接入生产操作单」由主人执行；本线只准备、只核对。对外答复不得包含改配置文件的方法。
- 个人微信仍是**准入区**：不做 Hook/DLL 注入、iPad 协议、ADB、加好友/群发/朋友圈；`tier=auto_reply` 只在主人明确要求时才给。
- 共享文件（`unified_inbox.html` / `admin.py` / `health_watchdog.py` / `config.example.yaml` / `test_admin_route_inventory.py`）只用 `tools/stage_hunks.py --mine 实施97 --apply`，提交前 `git diff --cached --name-only` 只含本线；**不用整文件 `git add`**。
- 工作树里 `unified_inbox.html` 是 LF 且基线旧于 HEAD：动它之前先 `git checkout -p -- engines/chengjie/src/web/templates/unified_inbox.html` 只留本线 hunk，或干脆 `git checkout --` 后从 HEAD 重做（HEAD 已含本线全部块）。
- `kf_window_guard` 数值与 `channel_policy.window_rule('wechat_kf')` 有同源门禁；改配额先改 `channel_policy`。

## 4. 进包条件（1.0.80）

1. §2 A 段完成（门禁进清单），gate_sweep 无本线红。
2. 安装包三开关默认关；`after-pack` 不新增 REQUIRED（本线无 Node 边车）。
3. 打包测试：干净 worktree `npm run build:backend` → `npm run dist:win`，预发门禁（renderer-boot / outbound-wiring / package-layout / uninstall-nsh / after-pack）全绿；`win-unpacked` 起后端 `GET /api/health` 200、`GET /workspace/connect/wechat_kf` 200（登录后）。
4. 落点表写「进包」一行；发版线按 `docs/发版对账_v1.0.79_发版.md` 全链带上。

## 5. 开新对话粘贴

```
读 docs/指令_微信线_续做_2026-09-10.md，按它续做实施97 微信线（代码已全部入库 f00c4d00，所有开关默认关）。先做不依赖真账号的三段：A 把 11 个 wechat 测试文件 + 导入扫雷纳入 scripts/gate_sweep.ps1 固定清单（扫雷脚本从 tmp 挪到 scripts/import_sweep.py）；B 中继运维闭环（prod_edge_watchdog 探 relay.bd2026.cc/healthz、relay 设备表 24h 清理 + healthz devices_stale、relay/tests +2）；C 列出联调残留（18898 portproxy/防火墙/实例/计划任务、两条重复测试行）的清理清单只 dry-run 不执行，等我拍板。D/E/F 段等我给企微凭证后再开。预算 $150。先 scripts\agent_probe.ps1 -Intent 登记；共享文件只用 tools/stage_hunks.py --mine 实施97，git add 显式路径，提交前 git diff --cached --name-only 只含本线；不改 config.local.yaml；每个 commit 补 docs/发版对账_v1.0.80_WX.md 一行；收工只在 docs/Q批_进度总账.md 末尾加 WX 一行并 agent_probe -Done。
```
