# 发版对账 v1.0.76 · M-2 通道真相与登录默认（2026-09-06）

> 指令：`docs/指令_M-2_通道真相与登录默认_2026-09-06.md`。预算 $180，实花 ≈ $150（含与 M-1/M-3/M-4 共享工作树的三次 index 等待与两次补装）。五件（A #232 / B D-M1 / C #233 / D #232 边车 / E #235 #223）全部完成，**一件一个 commit**（B 因 hunk 自证标记 `D-M1` ⊃ `M-1` 被工具拒装一次，补装两笔，见 §提交归属说明）。
> 模板 / i18n / CSS = 热更新即上生产（`unified-inbox.css?v=20260906g`，中间态已自洽）。`.py` 十一处 + `services/messenger-web/server.js` **须重启装载**（Python 实例 + Node 边车两个动作都要做，边车不重启 `/accounts.worker.restoring` 与 404 落日志不生效），装载前走 `scripts\restart_preflight.ps1`。
> ⚠ **本机 `config/config.yaml`（gitignored，zhiliao 实例活配置）已同步改**：`inbox.auto_draft.automation_mode: auto_ai → review`、`bootstrap_automation_mode: true → false`、新增 `login_gate{enabled,cooldown_sec=600,fail_streak_limit=3}`——zhiliao 装载后**未显式设档的会话**默认半自动（已显式写过 auto_ai 的会话行不变）。这是 D-M1 老板决策的直接后果，M-6 装载时请知会值守。

## 落点表

| 项 | 工单 | commit | 改了什么（文件 → 行为） | 门禁 / 测试 | 备注 |
|---|---|---|---|---|---|
| **A** 发送 ✓ 以回执为准 + 通道状态上屏 + 未连接禁投递 | #232 | `ece8a0aa` | ① `platform_session_health.py`：新增 **`channel_connection_state(platform, acct)`**（注册表 offline/removed → disconnected；健康表 needs_login/expired/logged_out/failed/blocked/forbidden → disconnected；编排器 worker error/starting → reconnecting，连败放弃 → disconnected；running / authorized → connected；无登记 → unknown 不拦）与 `channel_send_block_reason`（只在 disconnected 时给 `channel_disconnected`）；`session_status()` 读口；`report_session_transition` 在 authorized 时挂账号门禁 `note_login`（新 login_id / 从 needs_login·logged_out·expired 恢复才算真登录，failed/blocked 恢复与同 login_id 重推**不算**）+ `reset_backoff`。② `account_orchestrator.py`：`MessengerWebWorker.healthy()` 把探测结果**回报健康表**——边车列出该账号 → `authorized(login_id)`；`logged_in:false` → `needs_login`；边车在线但没有该账号会话且持续 ≥90s（`ABSENT_GRACE_SEC`，边车 `worker.restoring>0` 期间不判）→ `needs_login`（ZGKVQB「restored 0 界面仍已登录两小时」盲区）；边车不可达只记 detail 不上报（UI 显重连中）；新增 `managed_state()`。③ `channel_adapters.py`：`send_via_adapters` 入口按通道真相拒发 `ChannelSendError(503, reason_code=channel_disconnected)`——**autosend 与手动同一闸**（此前 worker error → `owns()=False` → 回落适配器直打边车 → 每条 500）；`_send_via_rpa_queue` 入队改 `delivered=None, queued=True`（入队 ≠ 回执）。④ `unified_inbox_send_routes.py`：`/send` 响应新增 `delivered / queued`；`channel_disconnected` → 结构化 detail `{code,platform,account_id,message}`；`_raise_if_account_blocked` 对媒体/贴纸/语音三链同闸。⑤ `unified_inbox_read_routes.py`：账号条目下沉 `channel_state / channel_reason / channel_detail / channel_since` + `gate` 快照。⑥ `unified_inbox.html`：账号 chip 标签「未连接（红）/ 重连中（转圈）/ 通道异常·已暂停自动（红）/ 冷静期 N 分（琥珀）」，头像绿点按「通道通不通」；会话横幅四分支（未连接 + 重新登录出路 / 重连中 / 降级 + 「恢复自动」/ 冷静期 + 「看积压草稿」）；发送前置闸（未连接不出乐观气泡、原文留框）；**发送按钮 ✓ 只在 `delivered===true`**，RPA 入队显「已排队发送」（琥珀），无回执语义显「已提交」。⑦ `stored_read_routes.py`：`POST /api/unified-inbox/automation/account-gate`（clear_degraded / end_cooldown / snapshot）。⑧ i18n `errors.py` `err.inbox.channel_disconnected`、`inbox_workspace.py` `inbox.chan.* / inbox.gate.* / inbox.msg.sent_queued / sent_submitted` zh+en；CSS 四个标签色。 | `tests/test_channel_truth_m2.py` **13 passed**；`test_platform_session_health` + `test_channel_adapters` + `test_send_gate_feedback` 99 passed；`check_template_js` 30 inline ok | 边车 15:48:46 那条 `send ok len=2 verified=true` 说明当天的 ✓ 其实**有**回执——边车回读锚定误判不在本条范围（不重做边车登录/发送流程），本条把「没回执也打勾」的入队路径与「通道没通还能点发」两条堵死 |
| **B** 登录默认半自动 + 冷静期 + 账号级批量 + 连续失败降级 | D-M1（#232 #233 UE7VM3） | `3fe64e53` + 补装 `8d272236`（autosend 两处）+ `dc8c5e0a`（i18n） | ① **新模块 `src/inbox/account_channel_gate.py`**（A 提交已装）：账号级四件事单一事实源——登录冷静期（`note_login` 判新 login_id / 真重登 → `cooldown_until=+600s`）、登录后账号默认半自动（`login_default_mode`：登录晚于用户上次按账号确认 → review）、连续失败降级（`note_send_fail`：真实失败 3 次 → `degraded_since` + 红标，**边车退避类 send_backoff/account_blocked 不计**；`clear_degraded` 需人确认）、退避水位（`backoff_remaining/reset_backoff`）；持久化 `config_dir()/account_channel_gate.json`。② 种子/示例：`config.desktop.min / .internal / config.example` 的 `automation_mode auto_ai→review`、`bootstrap_automation_mode→false`、新增 `login_gate` 段；本机活配置同改（见顶部 ⚠）。③ `config_manager.py`：`_migrate_login_default_semi_1076`（桌面态存量热修，三态：overlay 显式写过档位不动 / 已有 `login_default_semi` 标记不重迁 / 其余写 review+false+标记）。④ `automation_mode._account_mode_layer`：登录后默认 review 叠在 onboarding 层上（onboarding 更保守如 manual 则保留）；**不写会话行**，会话显式 auto_ai 仍胜出＝老会话不动。⑤ `effective_automation.compute_mode_caps` 追加 ⑧ `login_cooldown`（until_ts 可倒计时）/ ⑨ `channel_degraded` / ⑩ `channel_disconnected` 三层 review 封顶（胶囊、B 线拟稿、A 线闸、worker 同源）。⑥ **新模块 `account_bulk_mode.py`**：`plan/apply`「此账号全部会话 → 手动/半自动/全自动」——升 auto_ai 跳过群、覆盖个别设置如实计数、降档取消待投递 L2、落 `decide_account_mode` + `confirm_account_mode`（source=`account_bulk`，standby/onboarding 对齐不覆盖它）。⑦ `stored_read_routes.py`：`POST /api/unified-inbox/automation/account-bulk`（`confirm` 缺省干跑 → 前端确认「将对 N 个会话开启，覆盖 M 个个别设置（跳过 K 个群）」→ `confirm=true` 落地）。⑧ `autosend_worker.py`（M-2 三处之二）：`_process_batch` 账号门禁扣住（冷静期/降级/退避/未连接 → **L2 留 pending 当积压，不取消不 resolve**，同账号同原因 60s 一条 INFO）；`_deliver_one` 成功清连续失败/退避、失败记账（`translate_hold` 不计）；`status_snapshot` +`total_skipped_channel_gate / total_account_degraded`。⑨ `unified_inbox.html`：账号总览（普通 + dock）每行「此账号全部会话 → 手动 / 半自动 / 全自动」按钮 + 门禁提示（冷静期剩余 / 通道异常 / 登录后默认半自动），确认弹层带 N/M/K 与冷静期备注。⑩ `tests/test_seed_switch_upgrade_coverage.py` 移除 `bootstrap_automation_mode` 的 `_EXEMPT` 条目。 | `tests/test_login_default_semi_m2.py` **9 passed**（种子默认 / 三态迁移 / 重登→冷静期+账号默认半自动+老会话不动 / authorized 转移钩子 / 3 次失败降级·退避不计·确认解除 / 账号批量 plan+apply / worker 扣住·降级·退避）；档位/封顶/worker 相关 112 passed | 冷静期是**硬**的：账号菜单显式开全自动不结束冷静期（确认文案里明示「还剩 N 分钟，期间仍只起草」），`end_cooldown` 端点预留给「我已过目积压」。全自动老会话在冷静期内被封顶 review，冷静期过自动恢复 |
| **C** 退避锁手动优先 + 登录重置 backoff | #233 | `b085e4bf` | ① `autosend_worker.py`（M-2 三处之三）：`_tick` 重试队列到点项**先过账号门禁**——边车 `send_backoff` 水位未过 / 冷静期 / 降级 / 未连接 → 不重投、按剩余水位（`_account_gate_wait`，5–900s 带抖动）再排，计 `total_retry_gated`；退避只由真实成功 / 登录成功 / 健康探测通过解锁，不再由 13s/23s 改期一到点就撞边车续命（K9CY6R）。② **新模块 `send_context.py`**：contextvars 人工发送标记 `manual_send_scope / is_manual_send`。③ `channel_adapters.send_via_adapters`：`origin=manual` 的整条 await 链置位；`_send_web` 载荷带 `manual:true`。④ `account_orchestrator.MessengerWebWorker.send` 同上。⑤ `report_session_transition` authorized → `reset_backoff`（A 提交已装）。⑥ `tests/conftest.py` 新增 autouse `_isolated_account_channel_gate`（门禁状态按测试落 tmp——同进程串味曾让 `test_autosend_send_block` 误红）。⑦ `tests/test_messenger_send_failures.py` 两例按新语义补 `reset_backoff`（到点重投前提＝账号退避水位已过）。 | `tests/test_backoff_manual_first_m2.py` **4 passed**；autosend/deliver/send_failure 13 文件 227 passed | 边车侧半边在 D（`manual` 探测放行） |
| **D** Messenger 边车：会话恢复可解释 + 落日志 + 退避随登录重置 + 人工探测放行 | #232 #233 | `573de634` | `services/messenger-web/server.js`：① `restoreAll()` 每个 profile 目录去向各落一条（无 cookie 快照 / 拉起 / 失败），汇总行 `restoreAll summary {sessions_dir, dirs, restored, no_cookies, failed, already_live, read_error}`（restored=0 时 warn）；`_restoring` 计数 + `entry._isRestore`。② `GET /accounts` worker 段新增 `restoring / pending_count / pending[{login_id,status,account_id,is_restore}]`（Python 探测据此区分「还在恢复」与「根本没有该账号会话」）。③ `POST /accounts/:id/send` 404「account not connected」此前**静默** → warn 带 accountId / 已知会话 / restoring，响应 `reason_code=not_logged_in`（文案带 `not_logged_in` 标记 → Python `note_send_auth_failure` 立刻登记 needs_login，不必等探测）；catch-all 500 日志补 `message / jid / manual / loginId`（Playwright 错误对象序列化只剩 log 数组），响应 `reason_code=exception`。④ `body.manual=true` 在 `send_backoff` 窗内**每窗放行一次**探测性发送（`msg_ops.manualProbeDecision` 纯函数），`account_blocked` 不放行；成功 `noteSendSuccess` 解锁。⑤ `promoteIfLoggedIn`（重）登录成功 → 重置 `_sendFailStreak/_sendBackoffUntil/_manualProbeAt` 并落行。 | `node --check` ok；`node --test` **136 passed**（新增 `manualProbeDecision` 1 例） | 持久化本身（profile 目录 + `.cookies.json` 每 ~8s 快照 + `<userData>/data/messenger-sessions`）**已在**，「restored 0」的真根因当天不可归因；本条让下一次 restored 0 能从汇总行一眼判出是「目录空 / 全是空壳 / 拉起失败」，且 Python 侧 90s 内把账号翻成「需重新登录」并禁发 |
| **E** 发前确认原因可见 + 作用域文案 + 手动粘性 | #235 #223（D-M10 / D-M9） | `3443edf1` | ① **新模块 `l1_reason.py`**：原因码推导 `cooldown > no_persona > manual_review（会话显式人审）> 其它封顶层（channel_disconnected / channel_degraded / deliver_paused / warmup / platform_cap …）> lang_unknown > first_contact > weak_evidence > account_default / global_default` + 进程注册表 `note/peek`。② `autodraft_helpers.make_auto_draft_cb`：封顶后推导 → 按 conv/draft 登记 + `record_draft_audit(action=l1_reason)`（重启后仍可查）。③ **`drafts.py` 仅加一条语句**（D-M10）：`level=L1` 时再落一行 `auto_generate_draft L1 conv=… draft_id=… reason=<码>`，只读注册表、不改判定。④ `drafts_routes.py` `GET /api/drafts`：L1 草稿带 `l1_reason`（注册表优先，回落审计行）。⑤ `unified_inbox.html`：composer 草稿条 + 会话内待审卡显示原因 chip（「刚登录/重登冷静期」「账号未选人设」「客户语言未定」「客户首条来信」「消息太短·证据不足」…）；档位下拉改「🙋 本会话：手动 / 📝 本会话：半自动（待确认 N）/ 🚀 本会话：全自动」，全自动被封顶时显「本会话：全自动 → 实际半自动（待确认 N）」不再自相矛盾；封顶胶囊白名单 + 文案补三层；`chatFilter='auto'` 页内筛选 + **`window.wsAutoConvCount()` / `window.wsFilterAutoConversations()` 接口**（M-4 B 已在 `workspace_base.html` 把胶囊点击接到后者，本条不重复挂监听），本页把胶囊文案改写为「全自动会话 N」（N＝当前列表 auto_ai 会话数）；切手动三选「一直手动（默认）/ 30 分钟后接回 / 离开会话时接回」（`_appConfirm` 新增 `alt2` 第四键），离开会话时前端恢复接管前档位。⑥ `takeover_rearm.py`：`TAKEOVER_OPT_PREFIX=takeover_opt_from:`（坐席显式选 30 分钟接回）——全局开关关时 sweep **只**接回它，手动发送触发的接管与「一直手动」保持粘性；`rearm_state` 报 `sticky / opt_in`。⑦ `stored_read_routes.py` `POST /automation` 接 `body.rearm="30m"` 改写来源。⑧ 桌面种子 `takeover_rearm.enabled true→false`（D-M9）。⑨ i18n：`inbox_workspace.py`（`inbox.l1r.* / inbox.mode.scope_* / pending_n / sticky_* / fs.auto`）、`inbox_effective_mode.py` 三层、`workspace_shell.py` `base.pill.autoconv(_t)` zh+en。⑩ `tests/test_takeover_trigger_surface.py` 登记 `account_bulk_mode` / `account_mode_onboarding` 写点。 | `tests/test_l1_reason_m2.py` **5 passed**；接管/接回/封顶文案 66 passed；`check_template_js` ok；`check_i18n_dup_keys` ok | 「待确认草稿在会话内就地可批准/改/拒」复用既有会话内待审卡（发送 / 驳回 / 编辑）与 composer 草稿条（采用 / 面板 / 忽略），本条只补原因 chip |

## 提交归属说明（共享 index 实录）

- B 项首笔 `3fe64e53` 用 `stage_hunks --mine "M-2" --theirs "M-1"` 暂存 `autosend_worker.py` 时自证失败——我的注释里 `D-M1` 含子串 `M-1`，工具判「我的块里夹带他人标记」拒装；`inbox_workspace.py` 同一命令里因 index 已有我自己刚暂存的文件被判「外来暂存」拒装。两处分别补在 `8d272236`（autosend 两处，改 `--theirs "M-1 B"`）与 `dc8c5e0a`（i18n）。代码都在树上、内容完整，只是分了三笔。
- 与 M-1 / M-3 / M-4 同时改 `unified_inbox.html`、`inbox_workspace.py`、`autosend_worker.py`、`unified_inbox_send_routes.py`：每笔都按 hunk 暂存并自证；共享 index 被他人占用三次，各等 40–75s 释放后再装，**无一次卷入他人 hunk**（每次 `git diff --cached --name-only` 为空才开始）。
- 另：他人边写边保存曾把我 `inbox_workspace.py` 的 zh 块从工作树顶掉一次（同文件并发写），提交前复核发现并重新落上——热更新文件建议各线**改完立刻提交**。

## 既有失败（非本条引入，交 M-6 / 各线自查）

| 测试 | 现象 | 判断 |
|---|---|---|
| `tests/test_seed_switch_upgrade_coverage.py::test_every_seed_switch_is_upgrade_reachable` / `test_detector_catches_an_uncovered_switch` | 孤儿开关 `inbox.reply_style.bubbles.explicit_newline_only`（HEAD 桌面种子已 true，未登记 `_EXEMPT/_PENDING`） | #210 / D-L3 遗留，与 M-2 无关 |
| `tests/test_takeover_trigger_surface.py::test_mode_writer_files_are_allowlisted` | 余下 `src/assistant/actions.py` 未登记（我已登记 `account_bulk_mode` / `account_mode_onboarding` 两个） | assistant 线文件「谁都不碰」，交 M-6 转其归属 |
| `tests/test_platform_session_ghost_ttl.py::TestChannelSnapshotName` 两例 | 假注册表行无 `status` → L-3 C 的 `session_expected_online` 过滤掉 → `count==0` | L-3 C 引入过滤时未更新此测试；我未改 `_channel_health_snapshot` / `session_expected_online` |
| `tests/test_python_undefined_names.py` | `src/ai/translation_service.py: _no_translatable_body`、`src/contacts/care_dispatcher.py: Dict` | M-1 在途改动 |
| `tests/test_messenger_send_semantics.py` 三例（仅大批量同进程跑时） | `blocked:kill_switch:account:messenger:100` 状态串味；单跑 44 passed | kill-switch 进程级状态泄漏，既有 |

## 交 M-4（胶囊 DOM / 主题 / 溢出）

- 接口已就位：`window.wsAutoConvCount()`（当前列表 auto_ai 会话数，排除归档/已移除/已登出）、`window.wsFilterAutoConversations()`（切换 `chatFilter='auto'`，再点取消；筛选摘要 chip「全自动会话 N」）。M-4 B 的 `wsAutoPillClick` 已调用后者，**勿再在收件箱页挂第二个 click**（双绑等于没切）。
- 胶囊**文案**由收件箱页 `_syncAutoConvPill()` 在每次 `applyFilters` 后改写为「全自动会话 N」（i18n `base.pill.autoconv` / `_t`，并按 n=0 隐藏、筛选中加 `.is-active`）；壳层 `_l2AutoBump` 收到 L2 事件仍会写回「自动 N（本页打开以来）」——建议 M-4 B 在 `_l2AutoBump` 里 `if(typeof window.wsAutoConvCount==='function') return;` 让位，并给 `.ws-pill.l2auto.is-active` 一个选中态样式。
- 溢出 / 收缩规则 / 主题按 M-4 B 既定方案，本条未动 `workspace_base.html`。

## 交 M-6 回访口径

- **#232**：发送按钮只在拿到平台/边车回执（`delivered=true`）才闪「✓ 已发送」；LINE/WA RPA 入队显「已排队发送」（琥珀）；账号栏对边车没有会话的号标红「未连接」，会话上方红条「该账号 Messenger 会话未建立，消息不会发出——请先重新登录该账号」+ 重新登录按钮，此时**自动投递与手动发送都被同一闸拦下**（发送框原文保留）；worker 重连期间标「重连中」（不拦，发出去按真实结果留痕）。边车重启若 `restored 0`，边车日志现在有一行 `restoreAll summary`（目录数 / 无 cookie / 拉起 / 失败）可直接判因，Python 在 90 秒内把该号翻成「需重新登录」。装机后验：手动断网或删边车 sessions 目录 → 重启边车 → 90s 内账号栏红标 + 发送被拦。
- **#233**：边车 429 `send_backoff` 后自动链不再 13s/23s 反复重投续命（日志 `guard=channel_gate … backoff`，`autosend-status.total_retry_gated` 增长）；坐席手动发送在退避窗内**每窗放行一次**（边车日志 `manual probe allowed through send_backoff`），成功即解锁；重新登录成功 / 健康探测通过即清退避（日志 `自动投递退避已重置` / 边车 `send backoff reset on (re)authorization`）。
- **登录默认（D-M1）**：任何账号登录 / 重登 / 清数据重登 → 该账号所有会话 10 分钟内只起草不投递（账号栏琥珀「冷静期 N 分」+ 会话横幅 + 草稿卡「刚登录/重登冷静期」），账号默认半自动直到你在账号总览（顶栏账号数字按钮）该账号行点「全自动」并确认「将对 N 个会话开启，覆盖 M 个个别设置」；同账号连续 3 次真实发送失败 → 账号红标「通道异常·已暂停自动」+ 会话横幅「恢复自动」按钮，恢复要人点。**已在全自动的老会话档位不变**（冷静期内暂按半自动执行，到点恢复）。桌面机升级后全局默认自动迁成半自动（日志 `全局自动化档位已按 1.0.76 默认收成半自动`），设置页显式选过档位的机器不迁。
- **#235**：待确认（L1）草稿在 composer 草稿条与会话内待审卡上写明原因（刚登录/重登冷静期 / 账号未选人设 / 客户语言未定 / 客户首条来信 / 消息太短·证据不足 / 通道未连接 …），后端日志 `auto_generate_draft L1 … reason=<码>`；会话头部下拉在半自动时显「📝 本会话：半自动（待确认 N）」，全自动被封顶时显「本会话：全自动 → 实际半自动（待确认 N）」不再只亮全自动。
- **#223**：下拉全部带「本会话：」前缀（悬浮说明「整个账号切档请用账号菜单」）；顶栏胶囊改「全自动会话 N」，点击只看这些会话再点取消；切手动会问「一直手动（默认）/ 30 分钟后接回 / 离开会话时接回」，默认一直手动——30 分钟不再自动接回（仅显式选了 30 分钟的会话会接回，横幅倒计时如实显示）。

## 剩余 / 建议

- `account_channel_gate` 的 `hold_reason` 在 UI 里目前只展示冷静期 / 降级 / 未连接三种；`backoff` 水位（边车 429）只进 worker 日志与 `autosend-status`，未做账号栏标签——按 K9CY6R 的时长（十几秒到几分钟）不值得占 chip 位，若值守要看可在 ops 卡加读数。
- 边车「回读锚定 `verified` 误判」（15:48 send ok verified=true 客户未收到）不在本条范围（不重做发送流程）；若 M-6 回访再现，建议单开工单查 `readbackLastOutgoing` 的锚定判据。
