# 指令 Q-16：发送语言按会话保存 + 手发前语种确认（#276）

> 预算 **$120**。工单 **#276（P0）**（报告 KAZ964 / G229PJ / SFFR2H / Z4NFJ7 / D2A7SM，09-11 01:29–02:39，skuio 1.0.79）。底座 `docs/修复指令_Q批_并行_2026-09-09.md`。落点表 `docs/发版对账_v1.0.81_Q16.md`。
> 无前置。前端热更（模板）+ 路由日志；**不动 `store.py`**（`set_outbound_lang` 已存在，只在路由层加日志）。

## 事实（证据 `tmp_diag\{KAZ964,G229PJ,SFFR2H,Z4NFJ7}`）

- 01:29 Telegram steven(7331682688)×钧JUN 工具条切「收→中 · 发→日」发出日语；02:15:01 WhatsApp Nori(19892968016)×Cameron(15635715247) **手发**（`source=manual_out`）也被译成日语发给英语客户，02:16 用户撤回。两平台工具条状态一致；日志无任何语言切换记录。用户强调：多账号多语种是常态，**必然重复发生**。
- **根因已定位（代码只读核对）**：`unified_inbox.html`
  - `_persistXlatePrefs()`（L20928–）把每次会话选择整份写成 `prefs.__default__=entry`（注释原话「用户的最新选择成为新会话的默认」）→ `__default__.out='ja'`；
  - 切会话初值 `_xlateOut=(convEntry&&convEntry.out)||bd.out||''`（L20723）→ 没有会话级设置的 Cameron 会话继承浏览器默认 `ja`；
  - `_hydrateConvXlateOut` 服务端为空时「保留本地初值（含浏览器默认）」→ 不纠正；
  - 手发 `body.target_lang=_xlateOut`（L20204）→ 译成日语。
  - 服务端会话级事实源 `/api/unified-inbox/conv-xlate-out`（`set_outbound_lang`，B67/#154）**本身是对的**，AI 链 `resolve_outbound_lang` 逐条 `decided_by=message` 不受影响——问题只在「发→X」的浏览器全局默认继承。
- 附带（G229PJ ⑤）：01:20–02:15 `translation_service err=ai:target_lang_mismatch` 20+ 次，充值后仍现（DMS45P）。

## 要做的六段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 根因修（前端）** | `__default__` **不再保存 `out`**（只留 in / preview / back / tone）；切会话初值 `_xlateOut = convEntry.out || ''`，不继承 `bd.out`；存量 localStorage 里的 `__default__.out` 迁移时丢弃；服务端为空 = 「自动（跟对方语言）」，UI 显示「自动 · 跟对方：English」而非空白；`_hydrateConvXlateOut` 逻辑不变 | `unified_inbox.html`（`xl-` 前缀 hunk，stage_hunks） |
| **B 日志** | 路由 `POST /api/unified-inbox/conv-xlate-out` 落 INFO `[lang_pref] conv=<cid> scope=conv send=<lang|clear> by=<source>`；手发翻译路径（`/api/unified-inbox/translate` 且 `source=manual_out` / composer）落 `[manual_xlate] conv= from=<detected> to=<target> decided_by=<conv|auto>`；AI 链 `[xlate] outbound` 现有行不动 | `unified_inbox_translate_routes.py`、`outbound_translate.py`（只加一行） |
| **C 发送前确认** | 手发且 `target_lang` 非空且 ≠ 会话客户语言（`c.language`，缺则用最近入站检测）→ **阻断式确认**「将译成 日语 发给 English 客户，确定发送？」确认前不发；复用 D 批 `_langWarnCheck` 的语言判定，不新做检测；确认结果落 `[manual_xlate] … confirmed=1` | `unified_inbox.html` |
| **D 工具条文案** | 「发→X」旁常驻小字「本会话」；顶部常驻「译文语言」控件同步；「收→」保留全局默认语义并在 tooltip 写明「收→ 全局默认 / 发→ 仅本会话」；i18n zh/en/zh-Hant | `unified_inbox.html`、`i18n_packs/inbox_workspace.py`（或既有 `inbox.xl.*` 词包） |
| **E mismatch 排查** | 统计 skuio 报告与 zhiliao 近 24h `err=ai:target_lang_mismatch` 按 target / provider / src_len 分布；核对是否 prompt 预算裁剪（#275 根因，06:13 `4fdd238f` 已修）波及翻译 system 提示；仍复现则给根因与修法，修不了立单 | 只读 + 落点表 |
| **F 门禁** | 模板 JS 语法门禁 `test_workspace_template_js_syntax`；新 `tests/test_lang_pref_routes_q16.py`：set → `[lang_pref]` 落日志、clear 语义；前端回归（node 测试或 Playwright 冒烟）：会话 A 设 ja → 切无设置的会话 B → `_xlateOut===''` 且手发请求体**无** `target_lang` | tests |

## 红线

- 不动 `store.py`、不动 AI 链 `resolve_outbound_lang` 决策顺序（D-M3）。
- 「发→X」绝不再有任何形式的跨会话继承（浏览器默认 / 上次选择 / 账号级）——账号级默认要做也是**另立决策**，本线不做。
- 确认弹窗只在「目标语 ≠ 客户语言」出现，同语不弹（别把坐席逼疯）。

## 开新对话粘贴

```
读 docs/指令_Q-16_发送语言按会话与手发确认_2026-09-11.md，按它做 Q-16（#276 P0：unified_inbox.html 的 _persistXlatePrefs 不再把「发→X」写进 __default__、切会话初值不继承 bd.out、服务端空=自动跟对方语言并显示「自动 · 跟对方：X」/ 路由落 [lang_pref] conv scope send by 与手发 [manual_xlate] conv from to decided_by / 手发目标语≠客户语言阻断式确认 / 工具条「发→X 本会话」「收→ 全局默认」文案 + i18n / target_lang_mismatch 20+ 次排查 / JS 语法门禁 + 路由测试 + 会话切换回归）。预算 $120。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵，再 scripts\agent_probe.ps1 -Intent 登记；unified_inbox.html 他人在途 hunk 多，只动 xl- 前缀并用 tools/stage_hunks.py --mine；不动 store.py / outbound_translate.py 决策顺序。每个 commit 带 #276 并立刻补落点表 docs/发版对账_v1.0.81_Q16.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-16 行并 agent_probe -Done。
```
