# 发版对账 v1.0.85 · Q-37 主动触达调度钟三源同源 + 人设夜间不主动 + 文本时刻尺（#318 VV7BRY）

> 指令：`docs/指令_Q-37_主动触达调度钟三源同源与人设夜间不主动_2026-09-12.md`。工单 **#318（VV7BRY，skuio，1.0.84，09-12 21:23）**：WhatsApp Nori/19892968016 × Michael/46 New York，本地 **15:22** AI 主动发「It's 3am here and I'm wide awake wondering how the Marina's treating you lately」。总账：`docs/Q批_进度总账.md` Q-37 行。预算 $60。
> 红线：`schedule_clock` **trust=replace-only** 不放宽；不改 pacing / 冷却 / 回复率数字；不动 care 文案与预算，只换读钟；`_persona_clock_note` 继续注入；「Marina」归 Q-36 B，本条只记 skuio 记忆有无 marina 事实。本线**不重启** zhiliao。

## 〇、链路与 Marina（先坐实）

| 项 | 事实 |
|---|---|
| 会话 | 工单标题 `WhatsApp Nori/19892968016 x Michael/46 NewYork(15195****60)`；报告关键词窗 **18:23–21:23 没盖到 15:22**（`tmp_diag/VV7BRY/20260912-212305_report_report.md`） |
| outreach_log | **本机坐实不了**：skuio 数据在坐席机（报告 hostname `DESKTOP-IFTHJUI`），117 `D:\chengjie-instances\zhiliao\data\inbox.db` 不是事发库。回访时在 skuio 该会话 15:2x 的 `outreach_log.batch_id`（`care:*`＝关怀，否则 proactive）补一眼即可 |
| 机制（HEAD 核） | 15:22 上海＝03:22 纽约。「It's 3am here」是 `_persona_clock_note` 如实写人设美东钟，错在**发送时机**：安静闸没读 Q-29 `resolve_peer_tz`（New York confirmed）→ 回落服务器 15:22 放行。文案形态是主动开场不是关怀约定句——**按 proactive 修，care 读钟顺手对齐** |
| Marina | **本条不做**（归 Q-36 B observation）。本机无 skuio episodic；回访时让 skuio 在该会话记忆页搜 `marina` / `码头`。Q-36 C 已钉：`observation_line` 不出「Marina」、识图描述不进 `profile_slots` |

## 一、落点（每段一行）

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| **A 调度钟三源** | #318 | 本提交 | `user_clock.clock_from_tz_name`；`user_clock_resolver.resolve_schedule_user_clock` / `resolve_schedule_clock(cid) -> (hour, source∈peer_tz\|explicit\|persona_tz\|server)`：① Q-29 `resolve_peer_tz` → replace `peer_tz`；② 现有 `resolve_for_conversation` 仅 `trust==replace` → `explicit`（narrow/advisory **不升档**）；③ `persona_location.resolve_place_with_fallback` → `persona_tz`；④ server。人设钟不写 `conversation_meta.tz_*`。`plan_proactive_sends` 新 `schedule_clock_provider`（给了则安静闸与 `clock_source` 只读它）；`CareDispatcher._resolve_clock` 新 `schedule_clock_provider`（未接线走旧 customer/persona/account）。生产：`proactive_topic._schedule_clock_for_plan`、`background_tasks._care_schedule_clock` | ① NY confirmed + 15:22 → `peer_tz` hour=3 不发 |
| **B 人设夜间不主动** | #318 | 本提交 | `companion_proactive`：`persona_hour_provider`；人设当地落在 `quiet_start–quiet_end`（默认 23–8）且 `now-last_in_ts > 1800`（无入站视作无）→ diag `persona_quiet_hours` + 日志 `[proactive] skip=persona_quiet conv= persona_hour=`；≤30 min 有入站放行。plan 带 `persona_hour`。生产 `_persona_hour_for_plan` | ② 无人设外客户钟 + 人设美东 03 不发（A 的 persona_tz 安静闸）；③ 客户东京白天 + 人设 03 + 25 min 前入站放行；无入站 → `persona_quiet_hours` |
| **C 文本时刻尺** | #318 | 本提交 | `world_clock_guard.detect_daypart_conflict(text, local_hour, persona_hour=)`：客户时段词仍按调度钟；新 `detect_persona_self_time_conflict`（3am here / 半夜了 / 刚起床）按人设钟；**深夜自述出现在调度钟 8–22** → `persona_late`（VV7BRY 下午主动说 3am）。`proactive_topic` L1.09 **沿用**重写一次仍矛盾 → `mark_attempt` + 放弃，不新建分支 | ⑤ 事故原文 × 调度钟 15 + 人设钟 3 → `persona_late`；两边都是凌晨不误伤；`good morning`@15 仍拦 |
| **D 门禁** | #318 | 本提交 | 新 `tests/test_schedule_clock_q37.py`（10 例＝指令五例 + mentioned 也认 peer_tz + 无入站 skip + narrow 不升档 + care 读 `peer_tz` + 静态钉）。`test_peer_time_q29` **86 例零改动** | `test_schedule_clock_q37` + `test_peer_time_q29` **96 passed**；邻域 `user_clock_schedule` / `user_clock_resolver` / `world_clock_guard` / `care_quiet_verbatim_q4` / `care_user_clock` / `companion_proactive` / `seasonal_guard` 一并 **264 passed** |

## 二、验收对照

| 验收项 | 状态 |
|---|---|
| `resolve_schedule_clock` 三源，trust=replace-only 不放宽 | ✅ A（narrow monkeypatch → 仍 server） |
| proactive / care 安静时段同读它，diag 带 `clock_source` | ✅ A（plan `clock_source`；care `_resolve_clock` basis=`peer_tz`） |
| 人设 23–8 且 30 min 无入站不主动；日志 `skip=persona_quiet` | ✅ B |
| 人设自述 × 人设钟、客户时段词 × 调度钟；重写一次仍矛盾放弃 | ✅ C（既有 L1.09 分支） |
| 五例 + Q-29 86 零改动 | ✅ D |
| 不改 pacing / 冷却数字；care 文案预算不动；`_persona_clock_note` 仍注入 | ✅ 只换读钟 / 加闸 / 加尺 |

## 三、上线备注

- `.py` 须 zhiliao 重启才生效（`user_clock.py` / `user_clock_resolver.py` / `companion_proactive.py` / `care_dispatcher.py` / `background_tasks.py` / `proactive_topic.py` / `world_clock_guard.py`）。本线**未重启**，攒发版窗。
- 生效后盯：`clock_source=peer_tz` / `persona_tz`；`[proactive] skip=persona_quiet conv= persona_hour=`；问候时刻守卫拦截含 `persona_late`。
- 回访：skuio `outreach_log` 15:2x `batch_id`；记忆页搜 marina。
