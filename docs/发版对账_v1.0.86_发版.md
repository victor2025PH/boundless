# 发版对账 v1.0.86 — 发版线 R86（单一版本：Q-39 + Q-40 + 看门狗/工坊根修 → 一个 clean 包 → **公共渠道 + 内测桥接 + 官网 + 播报**）

> 规矩：`.cursor/rules/chatx-desktop-install.mdc` 「单一版本（D-Q11）」——一个 semver = 一个 clean 包。前例 `docs/发版对账_v1.0.85_发版.md`。
> 范围：公共渠道 1.0.85 → 1.0.86。Q-26 C–E / Q-28 / Q-32～Q-34 **不进包**。#312 #314 保持 confirmed。
> 接手方：先 `git -C D:\boundless log --oneline -8` 核对本表 commit 真在。意向板 slug `r86-release-1-0-86-single-clean-public-bridge`。
> 执行：2026-09-16 06:4x → 07:3x，见 §5。

## 0. 一句话

**1.0.86 单包** `ChatX-Setup-1.0.86.exe` **528,451,126 B** sha256 `e0687a161bcb425cd4578c501667b41ce3624199fe35f2dbcafdfc3bd0644aad`（504 MB，clean 形态，`MZ` 头核过，build-info flavor=clean commit=`2547b854`，干净 worktree `D:\boundless-r80@2547b854`）。内容 = 1.0.85 全部 + **Q-39** 跟图闸 / 翻译 HOLD+重试 / 全部账号系统筹码 + **Q-40** 相册裸流大图 HEIC / 聊天视频回平台表 + #316 相册 2MB 豁免 + 报障 outbox 看门狗 + 人设工坊根修。坐席三台 **yunsheng / kouxing / lianbei 全 UP · 1.086 · clean/public**。公共渠道 + 内测桥接（同字节）+ 官网 + 台账 6 单 + 回访 + 群 / 频道播报见 §4。

## 1. 本版收掉的坑

| 事故 | 根因 | 本版 |
|---|---|---|
| 客户发自拍又跟一张（#327） | 识图描述当客户话喂进相册匹配 | Q-39 A `image_send_gate` |
| enjoy the view 刷 miss（#323） | 场景 kind 无索图词也当要图 | Q-39 A strict 共现 |
| 翻译空串卡住（#326） | 引擎空串直接 HOLD 不可恢复 | Q-39 B 重试→换引擎→重起草→HOLD+重试翻译 |
| 全部账号顶 dormant 原码（#324） | 列表顶不认系统族 | Q-39 D `_isSysTag` |
| 相册大图 Failed to fetch（#316） | 全局 2MB body + 无裸流 | Q-40 + `is_persona_album_upload_path` |
| 删图跳顶 / 大图弹窗（#322） | `pmaLoad` 整刷 + `a[href]` 被遮罩拦截 | Q-40 D 摘卡 + lightbox |

## 2. 开工基线

| 项 | 事实 |
|---|---|
| 主干 | 版本链基点 `2547b854`（前含 Q-39/Q-40 收工 + HEIC 钉子 `6a7832fe`） |
| 意向板 | slug 登记；index FREE |
| 工作树 | Q-26 C–E / label-leak / ASR / `desktop/main.js` 等脏文件**未动、未进任何 commit** |
| 版本链 | `desktop/package.json` 1.0.85→**1.0.86** / 1.085→**1.086**；`i18n_hant generate` → **17719** 键；changelog **10 条双语**；`bump_ui_build` → **20260916-0652**；小智 help +5 / howto +5；dialog **5/5 GUIDED** → **`2547b854`** |
| 官网五值 | `chatxContent.ts` 1.0.86 / 504 MB / e0687a16… → **`1c2803d3`** |

## 3. 回归 / 门禁（干净 worktree `D:\boundless-r80@2547b854`）

| 项 | 结果 |
|---|---|
| `import_sweep` | modules=**1276** bad=0 / from-import names=**3689** bad=0 **ALL GREEN** |
| predist（clean） | `build:backend` 全量 + `dist:win:clean`：后端冒烟全过、干净包首启冒烟全过、after-pack **22 项**；`latest.yml` version 1.0.86 size 528451126 |
| 发版门槛 | **369 passed / 1 skipped / 1 failed**（仅 `test_ui_build_stamp_not_stale` checkout 同秒 mtime 伪红，与 R85 同形） |
| `gate_sweep -NoSyntaxScan` | 发版时仍在跑（生产在线封顶 4 worker）；红集事后对照 R85 18 旧债口径收口 |

## 4. 全链

| 步 | 状态 | 事实 |
|---|---|---|
| 坐席三台 | ✅ | `push_chatx.ps1 -Setup <r80>\dist-clean\ChatX-Setup-1.0.86.exe`：三台 exit 0、`[push] OK`、seatverify FRESH、smoke `app version: 1.0.86`；`chatx_fleet_status` **三台 UP · 1.086 · clean/public** |
| publish 公共 | ✅ | `publish_chatx.ps1 -DistDir <r80>\dist-clean -Yes`：VPS sha512 ✓ → 指针 + pm2 → R2 → 公网核 `latest.yml=1.0.86`；清理 1.0.82 |
| publish 内测桥接 | ✅ | 同字节 → `-Channel internal -Yes -NoAnnouncement`：`latest-internal.yml=1.0.86`，公共指针未动 |
| 官网 | ✅ | `gate:content` **6/6 PASS** → `deploy.ps1 -FromHead` → **部署完成 [OK]** |
| 台账 / 回访 / 播报 | ✅ 07:32–07:36 | `duty_update_hint` → 群播报 **mid 1575** → `mark-fixed` **6 单**（#316 #322 #323 #324 #326 #327）→ 逐单回访 6 条全过 → `notify_summary` **记账 5 单**（#316 已有 notify 跳过）积压 0 → 频道 @hykj7 **messageId=171**（campaign `release-1086`） |

## 5. 断点 / 交接

- 发版 worktree 保留 `D:\boundless-r80@2547b854`（`dist-clean\ChatX-Setup-1.0.86.exe`）。
- **117 / zhiliao**：本线未重启 zhiliao。坐席三台 = 包内新后端。117 后端半边仍等 `restart_preflight` GO（工作树仍有他线脏 `.py`，勿擅自重启）。
- 临时文书全在 `%TEMP%\r86\`，未入库。

## 6. 交回各线

- **Q-26 C–E** / **Q-28**（#279）仍不在包。
- **#312 #314** 保持 confirmed。
- **Q-32 / Q-33 / Q-34** 美化线待拍板。
