# 指令 Q-0：值守止血与归位（第 0 波，今天先开）

> 预算 **$80**。工单 **#263 #264 #267 #269**（+ #265 #266 #268 #254 相关）。决策 **D-Q4（今天暂停摸底自动注入）**。
> 底座 `docs/修复指令_Q批_并行_2026-09-09.md`。落点表 `docs/发版对账_v1.0.79_Q0.md`。

## 开工前必读

1. `docs/聊天核心能力_三视角方案_2026-09-09.md` §1 表（Q1–Q16）、§2.3 用户视角、§3 Q0。
2. 用户画像 `docs/值守_报障群用户画像_2026-08.md`（skuio 要结论 + 路径 + 验证步骤；只给版本号不给日期）。上一条回访口径见 `docs/发版对账_v1.0.79_P5.md` §回访。
3. 工具：`engines/chengjie/tools/duty_reply.py --group neice --ticket N --receipt --text-file F [--status confirmed] [--dry-run]`；`duty_evidence.py`；立单 / 并单模板 `tmp\duty_split_0906c.py`、`tmp\n5_a_merge.py`（dry-run → `--apply`）；只读总览 `tmp\new_since_0908.py "2026-09-09 03:40" 20260909-034000`。
4. 事实：#264 现挂 退场（7C7SV2 / B9D8NW / Y9JVHG / MTQG8S）+ 见面承诺绕审（GTCQS9 / 3D293E）+ 切手动仍发（XBGPBN / VAE6CJ）+ 四合一（7KXNHB）+ 情感引擎需求（SXKKPA / 3CS9MQ）+ 重复追问（K24YJ2 / FCQFKF / 57S9XD）；#263 挂 画像抽取（GAWRU5 / 9APUMX / T5PGMG）+ 假发图（D6V29H / J6RHBQ）+ 现在就问（R6YW5D / D5FJE8）+ 见面（PHBRBW / CBD4J5 / XNZR8R）；#267 挂 额度（UW7CXF / 5YZG6V）+ 档位 UX（HEEEN9 / R6P939）+ 草稿不消费（CV4E22 / 8FSXX2）+ 班表（CSTCJT / GW9FZ2 / KYHGSZ）。#269 = 重复追问第三次加急（AEMAMJ / 5GB37E）。
5. `tools/duty_auto_ticket.py` 规则③a「同实体（会话 id / 长数字 id / 客户名）」（N-5 D）是堆单的来源。

## 红线

- 台账写入先 dry-run；对外只经 `duty_reply.py`，每条带工单号；不教改配置文件（guard 会拦），只给页面路径。
- 我方 zhiliao 实例配置改动走既有页面 / API（`config.local.yaml` 由页面写），并落 `.ops` 一行记录；不动 `store.py`。

## A 回访三条（今天上午发）

- **#269 / #264（重复追问 + 识破）**：一件事一个单号；结论：引擎只判「问了没」不判「知道没」，抽取又关着 → 无界重试，是我们的错；**请他现在做两件事**：目标页逐个暂停全部 profile_discovery 目标（含 Enrique 新建的 2b15fe91）、Mike / Enrique 会话切人工；修复在 1.0.79（Q-1：已知即不问 + 每槽每日 ≤2 + 识破即停目标 + 「暂停全部摸底目标」总开关）。
- **#267（班表 / 额度 / 档位）**：承认两条默认是我们放进出厂的、且班表时区按了本机；**请他现在关掉**：自动回复设置 → 节奏 → 工作时间班表「启用」关；自动回复设置 → 额度 →「启用发送额度闸门」关；1.0.79 两者出厂关、额度永不限人工、班表时区显式选。
- **#263（见面承诺 / 假发图 / 画像 0）+ #265 / #266 / #268**：见面政策默认「永不」+ 委婉拒绝自动发 + 风险挂会话 + 切手动即取消在途（Q-2 / Q-3）；假发图闭环补全 + 相册建议词一键采纳（Q-6）；画像抽取开 + 昵称解析（Q-5）；关怀开关被升级重置我们在查、人工指定时间将弹选择（Q-4）；主题 / 草稿消费（Q-10）。**1.0.79 = P 批（登录不群发 / 停联持久 / 发图不说谎 / 首装 KB 空 / 后台回环）+ 上述行为止血；情感引擎（SXKKPA）立 1.0.80 专项，先接线 v1、盲评 ≥80% 才发。**

## B 我方 zhiliao 实例止血

- 自动回复设置 → 工作时间班表 关；额度闸门 关（页面操作）；目标页暂停全部 profile_discovery（或开发者模式下 `companion.goals.sprint` / 注入开关关）；记 `.ops\duty_0909\q0_instance_changes.txt`（时间 / 项 / 前后值）。

## C 三张单按主题拆（先 dry-run 列表给自己看再 apply）

- 从 #264 摘出：新单「AI 答应见面 / 承诺给地址 → 风险不继承重新起草 L2 直发 + 切手动仍发」（PHBRBW CBD4J5 XNZR8R GTCQS9 3D293E XBGPBN VAE6CJ，P0，去向 Q-2 / Q-3）；新单「客户夸赞中 AI 单方面退场 + 无自主推进」（7C7SV2 Y9JVHG B9D8NW MTQG8S，P1，去向 Q-8）；新单「需求：情感陪伴引擎」（SXKKPA 3CS9MQ，去向 Q-12，1.0.80）；#264 本体保留 重复追问族（K24YJ2 FCQFKF 57S9XD）并把 #269 并入或互链（一件事只看一个号）。7KXNHB 四合一 note 挂到对应四张单。
- 从 #263 摘出：新单「假发图死循环第四轮 + 相册建议词未生效」（D6V29H J6RHBQ GAWRU5② + #266 并入，去向 Q-6）；新单「现在就问一个被 contact_budget 拦」（R6YW5D D5FJE8，去向 Q-8 G）；#263 本体保留 画像抽取 0（GAWRU5 9APUMX T5PGMG，去向 Q-5）。
- 从 #267 摘出：新单「班表出厂开 + 上海时区 + 深夜静默」（CSTCJT GW9FZ2 KYHGSZ，P0，去向 Q-4 A）；新单「采用草稿不消费」（CV4E22 8FSXX2 RU89S6，去向 Q-10）；#267 本体保留 额度 + 档位 UX（去向 Q-4 B/G）。
- 每张新单 `duty_evidence received --diag <码>` 补登记；回声一条（多单合并成一条消息，每行一个单号 + 去向）。

## D 聚类规则双条件 + 上限（`tools/duty_auto_ticket.py`）

- 规则③a「同实体」改为「同实体 **且** 主题重叠 ≥0.3」；单票已挂 ≥6 份 → 不再挂，新立并在 note 互链「同实体前单 #N」；`auto_severity` 抬级只在开放单且首次；tests 用今夜 #264 的 15 份回放：应分成 ≥4 张。
- 顺手：汇总总表（`_SUMMARY_RE`）继续 skip，但要在 `.ops` 写「待人工对账」清单文件，Q-0 E 段消费。

## E 两份汇总总表逐项对账

- TZNW5H（1.0.77 未修清单）与 GRC9WW（1.0.78 索引）：逐项对到 单号 / 批次 / 版本（O 批 1.0.78 已发；P 批 + Q 批 1.0.79；Q-8 / Q-10 / Q-12 / Q-13 1.0.80；「不做 / 已澄清」单列），写进落点表 §E 一张表，并回 skuio 一条「总表逐项去向」（挂 #261 或 #262，1.0.78 验收单）。

## 收工

- 落点表 `docs/发版对账_v1.0.79_Q0.md`（A 三条回访 mid / B 实例改动 / C 拆单前后表 / D 规则 commit / E 对账表）；`docs/Q批_进度总账.md` 只改 Q-0 行；`agent_probe.ps1 -Done`。

## 开新对话粘贴

```
读 docs/指令_Q-0_值守止血与归位_2026-09-09.md，按它做 Q-0（#263 #264 #267 #269：三条回访——请 skuio 暂停全部摸底目标、关班表与额度两开关、告知 1.0.79 = P 批 + 行为止血、情感引擎 1.0.80 专项 / 我方 zhiliao 实例班表关 + 摸底注入停并记 .ops / #264 #263 #267 按主题拆单 + 7KXNHB 四合一分挂 / duty_auto_ticket 同实体规则加主题重叠 ≥0.3 与单票 ≥6 份上限 / TZNW5H 与 GRC9WW 两份总表逐项对账并回访）。预算 $80。先读 docs/修复指令_Q批_并行_2026-09-09.md 去向表与 D-Q4，再 scripts\agent_probe.ps1 -Intent 登记；台账写入先 dry-run；对外只经 engines/chengjie/tools/duty_reply.py，每条带工单号、只给版本号、只给页面路径不教配置文件。每个 commit 带工单号并立刻补落点表 docs/发版对账_v1.0.79_Q0.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-0 行并 agent_probe -Done。
```
