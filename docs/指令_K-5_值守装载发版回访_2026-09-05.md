# 指令 K-5：值守——装载 → 全量回归 → 发版 1.0.74 → 台账写入 → 61 单回访 → 真机验收

> **预算硬上限 $120。** 正常花费 $60–90。花到 $100 停下写落点表 + 总账。
> 类型：**非代码。** 这是 I 批与 J 批「全批收口」步骤 + I-6 F1② / F3 的继续。**用户能感知到的 35 张单的修复，只有走完本条才算修完**——服务端 zhiliao 末次重启 09-04 19:38（09-05 全部 `.py` 未装载），两台内测机 1.0.73。
> **分五段，段与段之间有硬依赖，不能跳：** ① 装载（K-1 提交后即可）→ ② 全量回归 + gate_sweep（K-1 / K-2 / K-3 都收工后）→ ③ 发版 1.0.74 + 装机 → ④ 台账写入（**装机之后**，决策 K-D2）→ ⑤ 回访 + 真机验收。
> 一个对话做不完是正常的：每段结束更新总账 K-5 行「已完成到第 N 段」，新对话从下一段接。

---

## 0. 开工前必读

1. `docs/修复指令_K批_并行_2026-09-05.md`：去向表（35 张单清单就在那）/ 决策 K-D2 K-D3 K-D5 K-D6。
2. `docs/J批_进度总账.md`「值守收尾清单」+「全批收口」+ 各行「值守：」备注（每条指令留给值守的动作，本条一次做完）。
3. `docs/I批_进度总账.md` I-6 行 + `docs/指令_I-6_值守流程与积压_2026-09-04.md` §3（回访文案三样必含）§4（F3）。
4. `docs/发版对账_v1.0.74_K4.md`（K-4 的四单结论 + #145 五行表 + 回访必带句）——④⑤ 段依赖它；没出就先做 ①②③。
5. `docs/值守症状族对账单.md`（收口时要更新）。

**开工先登记意向板：**
```
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "K-5 duty: restart zhiliao + WA sidecar / full regression / release 1.0.74 / ledger mark fixed / 61 notify flush / device acceptance"
```

## 1. 红线

- 台账**只经端点**改：`POST /api/admin/bug-intake/{id}/status`（body `{status, fix_note, update_hint}`，token 读法见 `tmp_mark_fixed_0901.py`）或 `tools/duty_ledger_fix.py` / `duty_notify_summary.py`（默认 dry-run，`--apply` 才写）。**不直接 UPDATE sqlite。**
- 重启生产实例前必跑 `engines\chengjie\scripts\restart_preflight.ps1`（意向板静默 + 语法 + Advise + 冷却），再 `deploy\instances\restart_instance.ps1 -Instance zhiliao`；**不双重启**（`agent_probe` 第 4 段看冷却）。
- 发群消息（回访 / 汇总）先把文案贴进落点表让老板看一眼（K-D3 已默认「发」，但文案本身要过目）；bot 链发送，不手打。
- 版本号 / `package.json` / `installer.nsh` 归 installer brand 线（ACTIVE 中）——③ 段开工前 `agent_probe` 看它是否静默 ≥30 分钟，否则等。
- 不改 `src/**`。回归红：先判「他线预存红」（J 批总账各行有记录）还是新红；新红写落点表交对应线，不自己修。

---

## 2. 第①段 —— 装载（K-1 提交后即可）

1. `git -C D:\boundless log --oneline -25 -- engines/chengjie` 确认 K-1 的 commit 已在；`git status --short -- engines/chengjie` 看还剩哪些 dirty（K-1 落点表 §D 列的孤儿文件——**它们也会随重启一起装载**，这是共享树的现实；若 §D 里有「语法自检失败」的文件必须先让原线处理或 `git checkout -- <该文件>`（只此一种回滚，且写落点表））。
2. `powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\restart_preflight.ps1` → 通过 → `powershell -ExecutionPolicy Bypass -File deploy\instances\restart_instance.ps1 -Instance zhiliao`。
3. 重启 117 的 WhatsApp 边车：`engines\chengjie\services\whatsapp-baileys\start.ps1`（J-6 值守①）。
4. 装载后 5 分钟自检（都在 J 批各落点表「值守」段，逐条打勾写落点表）：
   - `GET http://127.0.0.1:18799/api/kb/health` → `entries_vendor≈110, vendor_excluded=false`（J-9；**zhiliao 不要点一键清空厂商预置**）。
   - WA 边车 `GET /health` 见 `forbidden / reason_10m / bad_mac` 三段（J-6）。
   - backend.log 出现 `[goal-inject] injected|NOT injected reason=|ERROR`（J-2 A1，#166）；启动日志 sprint 一行是 ✅ 还是 WARNING（J-2 A2）。
   - `GET /api/goals/engine-status` 200（J-2）。
   - `GET /api/admin/bug-intake` stats（J-5：`observe_dup` / `verify_ambiguous` / `reply_continue` 计数键存在）。
   - `python tools/bug_intake_report.py --days 1` 跑通。
5. 落点表第①段：重启时刻、装载的 commit 范围（`git log --oneline <上次重启的 HEAD>..HEAD -- engines/chengjie | Measure-Object -Line`）、自检 6 项结果。

## 3. 第②段 —— 全量回归 + 门禁（K-1 / K-2 / K-3 收工后）

```
cd D:\boundless\engines\chengjie
python -m pytest tests/ -n auto -q --timeout=90 --timeout-method=thread 2>&1 | Select-Object -Last 40
powershell -ExecutionPolicy Bypass -File scripts\gate_sweep.ps1 -Full 2>&1 | Select-Object -Last 40
```
- 红的逐条归类：预存红（J 批总账记录过的：`test_alert_delivery_e2e::phantom_unread`——K-1 C 应已修；`test_stale_update_prompt` ×3 I-5 app.html）/ 新红（写文件 + 测试名 + 最后改该文件的 commit 作者线 → 交回）。
- 通过标准：0 新红。预存红若仍在，写明原因与归属，不阻塞发版（老板可划掉）。
- 更新 `docs/值守症状族对账单.md`：族 2（出站切语言）/ 族 4（风险扣稿）/ 幽灵未读族 / LINE 媒体族 各补 1.0.74 层。

## 4. 第③段 —— 发版 1.0.74 + 装机

1. 合并落点表：`docs/发版对账_v1.0.74.md`（J-4 建的骨架）← J1–J10 + K1–K4 各表按「工单 → commit → 验收口径」汇总（只汇总，不复制全文）。**用户可见的一句话公告口径**按 #号列 35 张单（去向表第一行），每单 ≤ 20 字人话。
2. 版本 bump：先 `agent_probe` 确认 installer brand 线静默 ≥30 分钟；按 `a6bfaf98`（v1.0.71）同款做 `release(chatx): v1.0.74 发版事实钉真值——版本 bump + 官网五值/changelog + 发版对账清单`（`git show a6bfaf98 --stat` 看要动哪些文件：`desktop/package.json` 版本、官网五值、changelog）。
3. 打包 + 冒烟：按 `docs/发版对账_v1.0.73.md` 的发版结果段记录的命令（`rg -n "dist|pack|smoke" docs/发版对账_v1.0.73.md`），产出内测包 + 干净包；`deploy/desktop/smoke_chatx_node.ps1`。
4. 装坐席机：`deploy/desktop/push_chatx.ps1`（yunsheng / kouxing / lianbei；`chatx_fleet_status.ps1` 核三台 UP / 1.074；kouxing 装完盯 3 分钟——I 批复查记的 85 秒 FD 崩溃已由 `680ddf81` 修，装完看 `renderer.log` / backend 心跳越过 85s）。
5. 内测机（钧 B990-0C62-FC1C-F668 / skuio DC8F）：切官网下载指针与 R2 到 1.0.74（按 I 批总账「官网下载指针与 R2 已切 1.0.73」同款）→ `python tools/duty_update_hint.py --set "1.0.74 已发布：看到「立即更新」提示点一下即可；更新后请按各单验收口径帮忙复验"` → 群里一条播报（bot 链）。**等两机版本上报 1.0.74**（`tools/duty_evidence.py` 探报障人版本）再进第④段。
6. 装机后值守动作（J 批各行「值守」）：skuio 机 `python tools/xlate_memory_purge.py --db %APPDATA%\telegram-ai-desktop\data\config\translation_memory.db --apply`（J-1 C；远程执行或让他的 Cursor 助手跑，不跑也会命中自愈）；AGENTS_JUN / AGENTS_SKUIO v1.3 重发两机并明说覆盖（J-5 交值守②）。

## 5. 第④段 —— 台账写入（**两机装上 1.0.74 之后**，决策 K-D2）

用 `tmp_mark_fixed_0901.py` 的形态写一个 `tmp/tmp_mark_fixed_1074.py`（只读 token → 逐单 POST `status=fixed` + `fix_note` + `update_hint`）。**每单 fix_note 一句人话**（从对应落点表「备注」抄改，≤ 300 字，不带 commit 号），按批：

| 批 | 单 | fix_note 来源 |
|---|---|---|
| I 批（09-04，1.0.72/1.0.73 已部分带、`.py` 本次装载） | #37 #38 #40 #65 #152 #153 #154 #155 #156 #158 #159 #160 #161 #162 | `发版对账_v1.0.72_I*.md` / `_A.md` / `_B.md` |
| J 批（09-05） | #164 #166 #167 #168 #169 #171 #172 #173 #174 #175 #176 #177 #178 #179 #180 #182 #183 #184 #185 #188 | `发版对账_v1.0.74_J*.md` |
| 观测项 | #181 | J-6 落点表「#181 待核」——fix_note 写「403 终态 + DNS 分类 + Bad MAC 自愈三层已上，配对时长看装机后 pairing_ms」 |
| K-1 提交的 | #32（悬浮清单）| K-1 落点表 A4 |
| K-D5 | #170 | K-4 落点表 §5 口径 |
| K-4 结论 | #61（合 #78 #179）#63 #64 #67 #36 #145（五行表进 body，②除外） | `发版对账_v1.0.74_K4.md`「回访必带的一句」——#67 **必须**含「请重传一次相册」 |
| K-2 / K-3 收工的 | #186 #189–#193 及 K-3 涉及的 | 各自落点表；K-2 未收工的单**不标** |

- 先 dry-run 打印全部 fix_note 让老板过目，再 `--apply`。
- 标完 `python tools/duty_ledger_fix.py --count-fixed-unnotified`：应从 61 涨到 61 + 本批数——这正是第⑤段要冲的。
- `tools/bug_intake_report.py --days 30` 一份存落点表（发版后基线）。

## 6. 第⑤段 —— 回访 + 真机验收

### 5a 61 单（+ 本批新标）汇总回访（I-6 F1② → K-D3「发」）
- 按 `指令_I-6` §3：两份汇总（skuio 47 + 本批 / 钧 13 + 本批 / 其他 1），每份三样必含：已修清单（#号 + 一句人话）/ **需用户操作才生效的单独标出**（#67 重传相册；#176 翻译记忆清理；#63/#156/#167 新登录账号会先进人审）/ 请优先验的 3–5 项（skuio：#171 #175 #166 #172 #183；钧：#164 #180 #161 #67 #61）。
- 文案先贴落点表；bot 链发出后拿 `msg_id` → `python tools/duty_notify_summary.py --reporter 8942577244 --msg-id <mid> --apply`、`--reporter 8852939166 --msg-id <mid> --apply`（默认 dry-run 先看一遍要盖多少单）。
- 之后 `python tools/duty_notify_pending.py --alert` 应不再告警；`duty_ledger_fix.py --count-fixed-unnotified` 归零或只剩 webuser/无 TG 会话的。

### 5b 真机验收清单（装机后 24h 内，能自己取的绝不问用户——`tools/duty_evidence.py` + 远程包）
| 单 | 看什么 | 来源 |
|---|---|---|
| #166 | BABY BEAR 下一条入站后 backend.log `[goal-inject] injected` 或 `NOT injected reason=…`；目标卡显示原因行 | J-2 A1/A2 |
| #177 | 手动发「我有个女儿」→ 下一轮草稿 prompt 含「你亲口说过」块 | J-1 D |
| #172 | LINE 收视频不再 4 分钟判过期；重试按钮分因 tooltip | J-3 A / J-4 |
| #169 | LINE 发 30/60MB 视频送达 | J-3 C |
| #158 | 打包态从两条路径进审批页 → 「← 返回聊天」可用，renderer.log 无 Uncaught | I-2 |
| #181 | `/login/:id/status.pairing_ms` < 60s；skuio 24h `/health.forbidden ≥ 1`、`giving up` 不再每 15 分钟 | J-6 |
| #160 | `logs/autosend_shadow/*.jsonl` 有 `policy_mode=shadow` 行 | I-1 |
| #154 | 铆定「发→英」会话对方切日语后仍英文出站 + ✎ 副行中文 | I-2 尾巴 d |
| #184 | ops「📚 知识库接通性」卡：用户条目 0 + 黄字「用户知识库为空」= 正面回答 | J-9 |
| #183 | AI 记忆页健康卡五格有数；88MP86 同口径五条 | J-10 落点表「值守读数口径」 |
| #185 | 后台「客户安全预警」三档空态正确；R8 升级点亮徽标 | J-8 |
| #191 | skuio 机 ops「🖱️ 前端哑按钮错误」计数不再增长 | J-7/K-2 |
| #145② / I-5 E1 | LINE 语音 / 视频 / 贴纸双向真机 | I-5 剩余 |
| K-3 B | 草稿卡底部记忆 chips 出现（有记忆的会话） | K-3 |
- 每项结果回写对应工单 body（`/api/admin/bug-intake/{id}/note` 若有，否则写落点表）；验收过的、报障人在群里带 `#N` 确认的，由 J-5 B 的 verify 逻辑自动 verified（只认 `#N` / 回复回访消息）。

---

## 7. 交付 + 换账号怎么接

- 落点表 `docs/发版对账_v1.0.74_K5.md`：五段各一节；②段红表；④段标单清单（单号 + fix_note 一句）；⑤段两份回访文案 + 验收表。
- 更新 `docs/K批_进度总账.md` K-5 行「已完成到第 N 段」；J 总账「值守收尾清单」逐项打勾；I 总账 I-6 行收口。
- 清意向板。
- 结束报告：装载了哪个 commit 范围 / 回归红表 / 1.0.74 装到哪几台 / 标了多少单 / 回访盖了多少单 / 验收表通过率 / 还剩什么。

**下一个账号的新对话粘这段：**
```
接手 K-5（值守：装载 → 回归 → 发版 1.0.74 → 台账 → 回访 → 真机验收）。按顺序做三件事：
1. 读 D:\boundless\docs\K批_进度总账.md 找 K-5 行，看做到第几段；同时看 K-1/K-2/K-3/K-4 行状态（②段要 K-1/K-2/K-3 收工，④段要 K-4 结论 + 两机已装 1.0.74）
2. 读 D:\boundless\docs\发版对账_v1.0.74_K5.md —— 断点
3. 跑 git -C D:\boundless log --oneline -25 -- engines/chengjie 核对最近提交
然后读 D:\boundless\docs\指令_K-5_值守装载发版回访_2026-09-05.md，从下一段继续。不改 src；台账只经端点；重启前必跑 restart_preflight。预算上限 $120（含已花）。
```
