# 发版对账 v1.0.98（2026-09-24）

主题：LINE 出站媒体公共包默认开（老板例外 2026-09-24，A 类基线 + 公共种子）。

| 项 | 状态 | 证据 |
|---|---|---|
| 源码 | ✅ | 功能 `9016a9d3`（feature_registry A 类 `platform_login.line.media.outbound=true` + `config.desktop.min.yaml` 种子 + 专项测试）；版本 `596071b9`（1.0.97→1.0.98） |
| 构建 | ✅ | 干净 worktree `D:\boundless-r98` @ `596071b9`：`npm run build:backend` → `dist:win:clean`；backend fingerprint `89300b99…e3f919`，2237 文件；安装版后端冒烟全过、干净包产物冒烟全过（`tmp/build_1098_backend.log` / `tmp/build_1098_dist.log`） |
| 产物 | ✅ | `ChatX-Setup-1.0.98.exe` 492,659,045 B（470 MB），sha256 `e7e629815ff3073966219fa91078e6f74116cb780c0b7fc751555f27a54e9c76`；build-info flavor=clean、commit `596071b9`；包内 `backend/_internal/config/config.desktop.min.yaml` 含 `media: outbound: true`；包内无 internal 种子 |
| 公共渠道 | ✅ | `publish_chatx.ps1 -Channel public -NotesFile tmp/notes_1098.txt -Yes`：VPS sha512 一致、R2 同步、announcements 带 release-v1.0.98、剪掉 1.0.95（`tmp/publish_1098.log`） |
| 内测桥接 | ✅ | 同字节 → `tmp/bridge_1098`（latest.yml 改名 latest-internal.yml）`-Channel internal -Yes -NoAnnouncement`；公共指针未动（`tmp/publish_1098_internal.log`） |
| 官网 | ✅ | `b1a79d11` 五值 1.0.97→1.0.98 + 更新记录条目（中英 3 条）；`gate:content` 6/6 PASS（`tmp/gate_1098.log`）；`deploy.ps1 -FromHead` 完成，公网体检 healthy（`tmp/deploy_1098.log`） |
| 频道 | ✅ | @hykj7 messageId=209，只发频道（`tmp/bc_1098.log`） |

公网复核（发布后）：`/downloads/latest.yml` 1.0.98；`/downloads/internal/latest-internal.yml` 1.0.98；`/downloads/manifest.json` 1.0.98 / sha256 e7e62981…；`/downloads/ChatX-Setup-1.0.98.exe` HTTP 200 Content-Length 492659045；`announcements.json` 含 release-v1.0.98；`/download/chatx` 指向 1.0.98 且 sha 一致；`/download/chatx/releases` 含 1.0.98。

测试：LINE 专项 + registry/种子/媒体 11 文件 181 passed / 3 failed；3 红均为既有 `ai.fallback.enabled=True`（C 类种子可达性），在 1.0.97 worktree 同样复现，非本次引入。ruff（F,E9,E7）改动文件全过。

已知：build-info dirty_count=12，均为打包时重新生成的 `i18n-packs-staged`，与 1.0.96/1.0.97 相同，不影响产物。
