# 值守症状族对账单（首版 2026-09-02，值守循环 v3 §H 的落地台账；09-05 K-5 ②段 / 09-06 L-7 D 收口更新：族 4 +16–18 层、族 5 +4 层、新立族 7）

> 用途：同一**用户可见症状**背后往往有多个独立根因，按工单号记账会导致「修一个根因就说已修复→用户撞上下一个根因→信任-1」。
> 本表按症状聚合全部已知根因，回访与群公告一律引用本表口径（「修到第 N 层，还剩 X」）。
> 更新纪律：每次新定性、每次发版对账、每次回访复验后随手更新；「敞口」列为空才允许对该症状说「已修复」。

## 族1：克隆声失败（用户可见：选了克隆声，发出来还是标准声/克隆不成功）

| 层 | 根因 | 工单 | 修复版本 | 状态 |
|---|---|---|---|---|
| 1 | 克隆档启用标志丢失 → 解析器静默回落标准声 | #93 | v1.0.68 | 已修（回落必亮黄条） |
| 2 | 音色占位串被当语种不匹配 → 改走标准声 | #137 #140 | v1.0.68 | 已修 |
| 3 | hub 超时导致克隆音色回落 | #114（B61 族） | — | 语音线在修，未随版 |
| 4 | 工具箱链路真机复验 | #145件① | — | 5GZHWT=冷启动包无现场（重装清数据）；转 1.0.70 复现取证（黄条+新日志） |
| 5 | **日语克隆乱音「有能量」即放行**——三次 CER>0.35、重合成仍「ゾオパパパ」，服务端终审只有有声/无声两档，喇叭在响就判有声发出去；还被标成「疑似无声」（归因也错：明明有声，是念错） | #161 | 929ba165（v1.0.72，两机已装 1.0.73）| 已修未回访（61 单积压内）：① 能力表以引擎真值为闸（ja/ko 从缺省表撤下，只登记实测念对的语种）；② 终审补第三档 garbled＋就地改派 Edge 标准声；③ 文案分「疑似无声/疑似念错」；④ STT 令牌三级解析（客户机不再因开发机路径缺失静默失败） |

**当前对外口径**：修到第 2/5 层；1.0.68 起「应克隆未克隆」不再静默、会亮黄条——升级后仍失败的，黄条文案+诊断包是新证据入口。1.0.72 起**日语/韩语默认不走克隆声**（实测念不对，直接用标准声并在语音卡明示），要开这些语种须先过 `tools/verify_clone_lang.py` 验收 + 运营显式登记。

## 族2：出站切中文（用户可见：外语会话里 AI 蹦中文）

| 层 | 根因 | 工单 | 修复版本 | 状态 |
|---|---|---|---|---|
| 1 | 识图中文描述被当客户语言证据（单行剥离） | #74 | v1.0.6x（0830 批A） | 已修 |
| 2 | 主动出站（SOP/关怀）绕过语言守卫直发 | #133 | v1.0.68 | 已修 |
| 3 | 贴纸轮/我方发图配文语言恒中文 | #143 | v1.0.69 | 已修（单行场景） |
| 4 | 多行识图描述只剥首行，续行中文当语言证据 | #143 C-补 | v1.0.70（f88055fb，写入压单行+读取剥续行） | 已修 |
| 5 | **会话「发→X」铆定被程序清空 + 坐席侧对照行随对方漂移**（本层是「切成对方新语言」，前四层是切中文）——① 前端任何翻译偏好变动都把可能为空的 `_xlateOut` POST 上去，服务端空串直接 DELETE 行、无审计 → 铆定丢失后出站按客户最新入站语言走；② 铆定缺位时收口点只认客户语言画像，日语客户 × 日语草稿零冲突 → 日语原文长驱直出；③ 上午形态（JV7MGQ 定性反转）：客户侧实收英文，是坐席侧 ✎ 对照行渲染了 LLM 日语草稿并压掉中文补译，观感像「AI 用日语回了」 | #154 | 2afc4e0f（服务端删除须显式 `clear:true` + 审计表 + 前端先 GET hydrate/`source` 分流 + `outbound_history_lang_pin` 我方出站主语种保守回落）· 8ff7f422（✎ 行 `_origReadable` 锁定会话「收→」）——v1.0.72 | 已修（1.0.72 已装两机；服务端 `.py` 随 09-04 19:38 重启装载）：金标「历史英文 + 四条日语稿全改写为英文」`test_154_japanese_lines_rewritten_via_history_pin`；✎ 行真值表 `test_outbound_orig_row_154.py`。**未回访**（在 61 单积压内）|
| 6 | **翻译记忆缓存了引擎废话**——「Exactly」译成「好的，请发送需要翻译的内容。」：引擎拒答/元回复被当正常译文写进翻译记忆，之后同句每次命中都复读中文废话（观感＝翻译坏了/蹦中文） | #176 | 24229010（`_memory_get`/`_cache_get` 命中先过 `looks_like_engine_refusal`，是废话即删行 + 按 miss 重译；`tools/xlate_memory_purge.py` 清存量）——服务端 `.py` 09-05 20:50 已装载；桌面包随 1.0.74 | 已修待发版：skuio 机存量坏记忆需装 1.0.74 后跑一次 `xlate_memory_purge --apply`（不跑也会在命中时自愈） |

**当前对外口径**：修到第 6/6 层（第 6 层 1.0.74 待发）：会话设了「发→X」就永远发 X，对方换什么语言都不动，铆定不再被程序悄悄清掉；坐席看到的「我方消息对照行」按会话「收→」语言显示、不随对方漂移；翻译记忆里的引擎废话会被自动清掉重译。#143 已标 fixed 并回访（0902 19:07）；复报按「修复失效」升级定性，不当新票。

## 族3：未读徽标虚数（用户可见：面板/账号栏未读数字与实际不符）

| 层 | 根因 | 工单 | 修复版本 | 状态 |
|---|---|---|---|---|
| 1 | 归档会话未读徽标口径 | #142件 | v1.0.68 | 已修 |
| 2 | SLA 徽标计 warn 档、待处理面板只列 crit 档（徽标 2 面板空） | #144件② | v1.0.70（60a0511f，徽标＝面板条数、分两段渲染） | 已修 |
| 3 | **顶部账号栏徽标虚高**——徽标口径（store 全库有效未读）比清单少了三道清单专属剔除：删除墓碑、消息全被软删、协议号手机端未读残值（纯占位会话）。三处显示 7、清单一条都没有 | #145件④（= K-D6 编号**件⑤**，件④是对端删除）→ #159（1.0.71 复现，证据包 G4BYWH：steven 号 tg 7092595256） | 2f8fc70f（v1.0.72，服务端口径）· 69e62aba（K-1 A4：账号栏数字**可点**直达浮层 + 一键清零，UI 半边，随 1.0.74） | 已修：口径收进 `src/inbox/unread_aggregate`，与「点数字直达那 N 条」共用同一份 WHERE（点进去的条数与徽标恒等）；1.0.72 时前端半边未 commit → 1.0.74 才有「点数字」入口 |
| 4 | **归档会话里的未读没有入口**——skuio 1.0.73 复报「角标 1 / 3 点开 15 个归档会话仍不消」：复核是**人工归档后再无入站**的会话（非缺陷），但入口只在展开态可见、列表一滚就折起，角标点了只切账号＝看得见数字摸不到条目 | #170（K-D5 定性 fixed） | 09bd0294（J-4 A：紧凑条常驻「归档中还有 N 条未读」pill + 「取消归档这 N 条」批量链；角标点击浮层场景 13 与徽标同 WHERE）——随 1.0.74 | 已修待发版 |
| 值守 | 幽灵未读**告警**（不是用户可见层）：被埋会话 / 幽灵未读进 `phantom_unread_alert` → webhook 格式器 + e2e 载荷清单，值守主动收到而不是等复报 | #120/#159 族 | 35b224c4（K-1 C；服务端 09-05 20:50 已装载）| 已装载；`test_watchdog_check_audit` 棘轮要补一条真调用测试（K-5 ②段红表 K3） |

**当前对外口径**：修到第 4/4 层（第 3 层 UI 半边与第 4 层随 1.0.74 待发）；账号栏数字自此＝清单里点得开的条数，**数字可点**直达并可一键清零；归档里的未读有常驻入口并可批量取消归档。若仍对不上：数字与直达列表的条数差就是新证据，直接发诊断包。

## 族4：静默失败（用户可见：什么都没发生/没下文——最大的「以为没修好」来源）

| 层 | 根因 | 工单 | 修复版本 | 状态 |
|---|---|---|---|---|
| 1 | 全自动总闸静默拦截（无提示无恢复入口） | #142 | v1.0.68 | 已修（会话内亮提示+一键恢复） |
| 2 | 出站语音静默投递失败（本地显示成功） | #101 | — | 验收口径已改「对端真机收到」，修复待核版本 |
| 3 | 克隆声静默换标准声 | #93 | v1.0.68 | 已修（黄条） |
| 4 | 出站近重复守卫误拦客户新消息的回复，静默无下文 | #144件① | v1.0.70（60a0511f，跨轮放行+拦截进待处理标原因） | 已修 |
| 7 | **LLM 退化循环（复读机化）漏网直发**——单条内部 token 循环，历史相似度守卫不看；截空后 `cleaned or reply` 回落照发 | #152件① | 30951d10（待 1.0.72） | 已修待发版：确定性 n-gram 连续重复检测截断 + 截空不回落 |
| 8 | **工作目标「看着在推进实际没执行」**——注入早退只写内存 meta，无日志无前端消费 | #152件② | e99a61c7（v1.0.72；服务端 09-04 19:38 装载） | 已修：注入判定落日志 + 英雄行亮「上轮未按目标推进：原因」（**观测层**；执行层见第 10 层，可见性补强见第 12 层） |
| 10 | **限时目标拿不到出手机会 / 进度卡在个位数**——① 建目标没给 pace 一律当 natural，`clamp_deadline_days` 把 60 分钟夹成 1 天，库里永远见不到子日跨度；② custom 进度只按时钟走，3 小时目标聊 16 分钟≈9%；③ inbox 读不到入站时刻时 session 整段只有 `s:0` | #65 #153 #152② | e95994c4（I-3 C1）——服务端 **09-05 20:50 已装载** | 已修已装载：**执行层**——创建口按跨度自动落 `session`/`today`（白名单外仍 natural）；结算按已出手拍爬进度（natural 零感知）；`inbound_turn` 锚本条开口。情绪/冷却/主动预算护栏未绕过。`beat_rejected` 仍让路 |
| 9 | **入站风险词扣稿静默**——全自动会话里客户一句话含 `stop`/`sex`/`bank`… 子串（`sexy`/`bank holiday`/`i didn't want to stop` 全误伤）→ 草稿被扣成 L3/L4 挂起等人审，坐席侧无提示、客户侧无下文；四条「硬底线」也一并静默扣稿 | #160 | da7a8787（分类器整词化）· 58aed354（`autosend_policy.decide` 单一入口，`policy_mode` 默认 `shadow`＝**一条不留全放行**，触发只写台账 `logs/autosend_shadow/*.jsonl`）· a667783c（ops 卡 + CLI + `stop_contact`/`self_harm` 即时告警不拦只报）· 067a0b62/28864be4（台账维度 + 放行后去向）· 82ee11a5（K-1 A1：「填入并发送」guard-check 同改影子档）——服务端 **09-05 20:50 已装载**，桌面半边随 1.0.74 | 已修已装载：老板 2026-09-04 v2 决策「全自动就是全自动，触发到风险只做后台记录，一个月后用真实数据再定规则」；坐席侧零改动（ops_only）。⚠ 对外口径按 v2，**不得**说「只在四种情况等人」 |
| 12 | **目标引擎「在推进」是兜售**——① 注入口 `no_goal` / 读库异常 / 注入链异常三条路径全 DEBUG，日志判不出走了哪条；② 只翻 `sprint.enabled` 不等于真出手：派发终点 care `dry_run`、`sprint.platforms` 出厂只 telegram、自然档走 bridge 出厂关，UI 却照常显示「自动推进」；③ 引擎停摆无看门狗 | #166 #153 | d0aa6560（J-2 A1：三条路径升 INFO/WARNING，`[goal-inject] injected / NOT injected reason= / ERROR` 必出一行）· e4a62ee5（J-2 A2：`sprint_engine_status` 单一出口 + `GET /api/goals/engine-status` + 目标卡按引擎真相灰掉点名阻塞闸）· d34666f3/abe96ba3/981790e3/124dd921（D1b P0-1..6：冲刺脱离 care 灰度门 / 三平台白名单 / 自然档每日拍 / 运行时闸单源 / 停摆 liveness 看门狗）——服务端 **09-05 20:50 已装载**，`engine-status` 回 `sprint_effective=true, blockers=[]`；UI 随 1.0.74 | 已修已装载：真机验收口径＝BABY BEAR 下一条入站后 backend.log 必出 `[goal-inject]` 行（K-5 ⑤ 5b） |
| 13 | **工作链 / SOP 让路或失败无解释**——卡上只有一个「已暂停/失败」徽标；步骤重试/失败原因不落日志；`auto_start` 一 tick 无上限 | #168 | 983e05ba（J-2 B：`format_step_fail_reason`、`card_state/card_label` 三态、`max_per_tick=5`、SOP 拟稿钉人设第一人称）· 3c1213e6（J-4：链卡 `cardExplain` 行消费三态）——服务端 09-05 20:50 已装载，卡片热更已生效 | 已修已装载 |
| 14 | **LINE 入站视频 4 分钟即判「已过期」拉取失败**——不是保留期（实测 7 天都在），是对方开了 Letter Sealing 的媒体走 `/r/talk/em*/<OID>` + X-Talk-Meta + 解密两层都缺，404 被当过期；重试路由传 fake 空壳永远拉不到 | #172 | c9d19638（J-3 A：`em*` 路径 + 解密 + 收到即预取 + 404≠过期、`expired_after_hours=168`）· 500d9b4d（J-4：重试路由传真消息 + `miss_reason` 分因 tooltip，真过期才「请客户重发」）——服务端 09-05 20:50 已装载，UI 随 1.0.74 | 已修已装载；真机 E2EE 视频回读逐字节一致 |
| 15 | **媒体/语音按钮灰着不说为什么**——LINE 语音 501 红字「该账号不支持从收件箱发送语音」一句话盖三种原因（账号未托管 / 平台不支持 / worker 重连中） | #180 | 28ce86ba（J-3 B：`caps_reason/worker_state/backoff_sec` 三分因 + receiver 连败跨重启退避）· c9759aea（J-4：灰态 tooltip 消费三分因）——服务端 09-05 20:50 已装载，UI 随 1.0.74 | 已修已装载 |
| 5 | 媒体发送失败（有红横幅=可见，属可见失败） | #145件② | — | 定性（QTCYNC 0902 20:06）：钧机重装后 WA 零登录（restored 0 sessions）＝通道未接，非 bug；转配置引导 |
| 6 | **断连窗误标「发送失败」→点重发→真双发** | #148 | 媒体件 v1.0.71（E2）；文本件未修 | 定性（64PY7D）：WA 重连窗界面提前标失败，重发新 id 绕过幂等锁；同族 #72 变体。**媒体半边 1.0.71 已修**（钧机 SY8TGE 复盘：幂等键随件固定→重试同键不双发；无响应失败标「结果未知」；后端恢复/线程刷新按对话记录对账自动清幽灵失败）。**文本件重发仍新 id**，指令 H 待交老板 |
| 11 | **桌面包后端自启后无响应仍标红字失败**——E2 的「未知」黄字只认 `__wsRestartCool`（117 实例重启冷却）；坐席机本地后端被杀/自启不走这条，`_mediaBackendUnstable` 判稳 → 红字「网络/服务异常」。3PZ95W：#164 报障窗本机零 send-media 请求，通道 9/2–9/3 全绿 | #164 | 345241a6（J-4 C：`res==null` 一律黄字「结果未知」+ 自刷消息流 + `[send-media] no-response` 日志；语音同口径）· 8b545d37（K-3 收 K-1 交办：贴纸发送同口径）——随 1.0.74 | 已修（1.0.74）：1.0.73 仍是旧红字。**1.0.75 补**（#194，0448aebf）：无响应只出一处提示（附件条内联，toast 撤）、不在重启窗时文案不含「重启」。文本重发双发仍是第 6 层敞口 |
| 16 | **出站形态失真（拆条）**——`per_sentence=true`（内测种子播进 overlay）绕过 `min_total_chars`，两句英文被拆成两拍让客户识破 AI；坐席侧只见 1 条、看不出实发几条；「出厂开」真源＝feature_registry A 类基线 + 两份桌面种子 `enabled:true` | #210 | 2f1cca8b（L-1 A：仅显式换行才拆 / 短回复门 80 / 逐句存量迁移「拆条已按 1.0.75 默认收紧」/ 出口折叠）· 630835d7（出厂关：降 B 类 + 种子 false）· ecf2a5e2（L-1 B：逐条镜像不变量 + 响应 `message_ids` + 「已拆 N 条发出」）——服务端 **09-06 11:45 已装载**，桌面随 1.0.75 | 已修已装载：对外口径「拆条出厂关；短回复整条发；开着也只按显式换行拆；工作台条数与手机一致」。skuio 机 overlay 显式 `enabled:true` 保留运营表态、只关 `per_sentence` |
| 17 | **「需人工」标只在人工发消息时清**——机器打的标（dup_guard_blocked 等）AI 回上不摘；点进会话无原因无入口；红标与 AI 蓝徽标同亮 | #207 | b3a34895（L-3 A：`HANDOFF_AUTO_CLEAR_REASONS` + AI 回上即摘 + 启动扫描消化陈旧标 + 会话头原因条 + 徽标互斥）——服务端 09-06 11:45 已装载，模板已热更 | 已修已装载：crisis / high_risk / 人工手打的标不会被机器摘 |
| 18 | **断线提醒只有「1 小时内别再提醒」一个出口且每小时重弹**；看门狗各类告警逐条 toast 不归拢 | #196 | a924426f（L-3 C，卷入 L-5 提交：六出口 + 按账号静音落注册表 + `标为已停用` + notify-bus 同 key 原地更新 / 折汇总卡 / 四级分层 infra 默认不弹）——服务端已装载，模板已热更 | 已修已装载 |

**当前对外口径（族4）**：媒体「假发送失败 / 重发双发」修到 v1.0.71，桌面包本地后端自启这一条（#164 第 11 层）**1.0.74 起改黄字「结果未知」**（1.0.75 起只出一处提示、不乱说「重启中」）。文本重发双发仍是敞口。第 9 层（#160）对外只说 v2：「AI 不再因为客户提到敏感词就停手，全自动就是全自动，触发到风险只做后台记录，一个月后用真实数据再定规则」。第 12–15 层（1.0.74）：目标 / 工作链 / LINE 媒体 / 灰按钮四处「没下文」现在都会把原因写在脸上。**第 16–18 层（1.0.75）**：拆条出厂关且工作台条数与手机一致；「需人工」AI 回上即摘并说明原因；断线卡六个出口、可静音可停用、告警不重弹。

**专项方向（给老板的长期指令）**：静默回落清零——盘点全链所有「失败但用户不可见」的回落点，一律加可见提示或事件日志；#93 的黄条是模板。

## 族5：AI 出站内容越界（用户可见：AI 在客户面前说错话/拆自家台）

| 层 | 根因 | 工单 | 修复版本 | 状态 |
|---|---|---|---|---|
| 1 | 人称错乱 | #131 | — | 见各自工单 |
| 2 | 配置泄漏 | #132 | — | 见各自工单 |
| 3 | **跨人设商业事实不共享 → 贬损自家阵营推广**（佳士得充值活动被斥「silly top-up bonuses / flashy ad / beg for deposits」） | #147 | v1.0.71（D1） | 已修两层：prompt 白名单块 + 出站贬损守卫。**生效前提＝运营在 `config/site_catalog.yaml: camp_promotions` 登记活动**（没登记＝守卫不知道谁是自家，只兜「产品名」那一层） |
| 4 | **问候编造近况**——问候 prompt 只有「像朋友接话」无来源约束；FACT_LOCK 把第一次编造钉成后续「事实」；`_recognize_intent` ≥20 字陈述句兜底 greeting（「Send me the receipt」被当问候秒回） | #208 | a781e36b（L-1 C 代码，卷入 L-3 提交；标记 556d1aec）：近况守卫 `is_self_status_claim` 三类词表 + 来源池只收 user 侧 / 档案 / 天气源 → 句级剥离；问候 prompt 硬约束；历史随口一说不延续；请求语一律 `direct_chat`——服务端 09-06 11:45 已装载 | 已修已装载：整条都是无来源近况时回落原文 + WARNING 计数（`整条回复都是无来源近况陈述`），成规模再做「剥空→重生成」 |

**当前对外口径（族5·第 3–4 层）**：1.0.71 起 AI 不再嘲讽登记过的自家活动/产品；请运营把在推活动登记进目录（值守可代填），未登记的活动仍可能被当陌生推广评价。1.0.75 起问候不再编天气 / 行程 / 正在做的事，「发我收据」类请求不再被当问候。

## 族6：LINE 媒体收发失败（用户可见：LINE 会话里图 / 语音 / 视频收不到、发不出、或显示已过期）——2026-09-05 立族（K-5 ②段）

| 层 | 根因 | 工单 | 修复版本 | 状态 |
|---|---|---|---|---|
| 1 | 出站语音静默投递失败（本地显示成功，对端没收到） | #101（族4 第 2 层同件） | — | 验收口径已改「对端真机收到」，随 1.0.74 真机复验（#145② 并入） |
| 2 | **收**：Letter Sealing（e2ee）媒体走错对象路径 + 未解密 → 404 被当「已过期」；重试路由传空壳永远拉不到 | #172 | c9d19638 · 500d9b4d（服务端 09-05 20:50 已装载；重试按钮 tooltip 随 1.0.74） | 已修已装载；真机 E2EE image/video/voice 回读逐字节一致 |
| 3 | **发**：前端 25MB 一刀切 + okline 30s socket 写超时 → 60MB 视频 91s 后报 `obs_upload_failed`；编排器 `send_timeout` 按固定 45s 判悬死 → 假 502 / 双发 | #169 | e3573a66 + 65e122a2（按平台上限单源 line 100 / tg 200 / wa 64；OBS 上传与编排器超时按体积放宽）——服务端已装载，前端预检自动跟 `send-caps.media_max_mb` | 已修已装载；真机 30/60/100MB 三档送达 |
| 4 | **收**：入站缺省上限 20MB，LINE 服务端把 100MB 源转码成 27.5MB 分发 → 收长视频 `too_large_actual` 拒下 | （J-3 C 发现，K-3 D 收） | 88c442f9（20→32MB，服务端已装载） | 已修已装载 |
| 5 | 语音发送 501 一句红字盖三种原因（未托管 / 平台不支持 / worker 重连中）→ 用户以为「LINE 不能发语音」 | #180 | 28ce86ba · c9759aea（随 1.0.74） | 已修待发版 |
| 6 | 图 / 表情双向真机未跑（探针只覆盖图；群聊入站默认关） | #145② / I-5 E1 | — | **等证据**：1.0.74 装机后 LINE 语音 / 视频 / 贴纸双向真机（K-5 ⑤ 5b） |

**当前对外口径（族6）**：修到第 5/6 层（第 2–4 层服务端已生效，第 5 层随 1.0.74）：LINE 收视频不再 4 分钟就说过期、真过期才提示请客户重发；发视频上限 100MB；收长视频不再被 20MB 拒下。第 6 层（贴纸 / 语音双向）等真机复验，**对 #145② 暂不说「已修复」**。（TG 侧贴纸破图 #195 属 L-6 C 2d804c5f、随 1.0.75，不在本族。）

## 族7：后台可见性——研发面 / 商业面直出终端用户（用户可见：设置里有一堆看不懂、不该我碰、点了没用的入口）——2026-09-06 立族（L-7 D 收口）

根因一句话：后台没有「给谁看」的维度，`nav_schema.CLIENT_HIDDEN_ITEM_IDS` 三项自建以来没再维护，也没有代理商这一层（L-4 落点表）。D-L2 / D-L5 / D-L6 / D-L7 定：client 形态隐藏清单扩到总盘 §1-D 全部 + 开发者模式复用 `/developer` 密码闸；歌房 / 声音评测 / 帮助 用「功能升级中，如需开通请联系客服」空态，不补生产入口。

| 层 | 根因 | 工单 | 修复版本 | 状态 |
|---|---|---|---|---|
| 1 | **分层机制缺失**：client 隐藏清单只有 logs/developer/bug_tickets；无 partner 层；无「内部人在客户机看全」的开关 | #197 #198 #212 #199 #211 | 395acee7（L-4 A：`CLIENT_HIDDEN_ITEM_IDS` + help/strategies/singing/voice_eval，`PARTNER_ONLY_ITEM_IDS` 三条设置深链，开发者模式 session 键 + `POST /api/developer/developer-mode`，模板全局 `ui_client_hide`，侧栏「研发/代理商」角标）——服务端 09-06 11:45 已装载，模板已热更 | 已修已装载 |
| 2 | 系统设置直出品牌白标 / 演示数据 / 授权三卡；档位徽标（内测种子 `plan_override: flagship`）与授权卡（读文件「社区模式」）两面矛盾；功能总览「缺依赖 / 未开放」+ 配置键名 | #197 | 5512c0a6（L-4 B）| 已修已装载：无授权文件→「社区模式」；有 override→「旗舰版（厂商自营）」；是否让 override 不影响徽标 → 待老板 |
| 3 | 帮助页＝TG 机器人时代斜杠命令表 + 培训演示 404 JSON | #198 | f9edb52b（L-4 C）| 已修已装载 |
| 4 | 回复策略页＝大模型调参面板直出 | #212 | e180d1b1（L-4 D）| 已修已装载：默认只隐藏；三档预设入口 → 待老板 |
| 5 | 歌房曲库 0 / 声库 0 开关虚开；声音评测只有消费端 | #199 #211 | a4f453da（L-4 G：空态横幅 + 唱歌能力位资产门禁 409）| 已修已装载：运行时资产不齐是否视为关 → 待老板 |
| 6 | 自动回复高级三块语义冲突（语音开关+触发方式双层 / 缓冲语常驻 / 人设 ID 秒数表）| #200 | d6942318（L-4 E）| 已修已装载 |
| 7 | 学习队列把研发缓存当用户功能：寒暄入队 / 假 99% / 「全部通过」166 条无确认 / 私事进共享 KB | #201 | b5d146bd（L-4 F：止血四件 + 存量清标，zhiliao 装载日志 `noise_rejected 2 / dup_recalced 7`）| 已修已装载：重做为「案例学习」属 P3（BX6ZY2 等老板） |
| 8 | 人设编辑器研发面直出：JSON 导入 / 高级字段 / 语音 BACKEND 五选一 / personas.yaml 同步 | #203 #205 #202 | a1e54876 · 3f31eeab · d0593c7b（L-2 C/A/E：`ui_client_hide` 不渲染 + 开发者区折叠）| 已修已装载（`.py` 随 11:45 装载，模板已热更） |

**当前对外口径（族7）**：1.0.75 起用户版后台只见该用的：系统设置无三卡、帮助 / 回复策略 / 歌房 / 声音评测显「功能升级中，如需开通请联系客服」、人设编辑器无 JSON 导入与 BACKEND 手填；内部人在「开发者」页开「开发者模式」临时看全（退出登录自动关）。四条待老板拍板项不影响用户版口径。

## 使用备注

- 新增症状族的门槛：同一症状出现**第二个独立根因**时立族，不等第三个。
- 「状态」列只允许：已修（vX）/ 未修（指令已交/未交）/ 等证据（缺什么写什么）。
- 回访引用本表时对用户只说层数和症状语言；工单号是内部索引。
