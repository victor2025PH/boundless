# 发版对账 v1.0.83 — Q-24（Messenger 网页代发链止血 · #298 P0 / #297 / #299 / #300）

> 指令：`docs/指令_Q-24_Messenger网页代发链止血_2026-09-12.md`。底座：`docs/修复指令_Q批_并行_2026-09-09.md`（归属矛阵：本线不动 `autosend_worker.py` / `autosend_policy.py`（Q-23）、不动 WA / TG 边车、不动翻译区；`inbox_workspace.py` / `zh_hant_auto.py` / `workspace_base.html` 为他线热区，本线词条独立成包）。总账：`docs/Q批_进度总账.md` Q-24 行。预算 $200。
> 执行：09-12 11:0x–。

## 〇 事故根因（一句话）

Messenger 网页边车 `/send` 在 `composer.click()/type()` 瞬间被 Messenger 重绘换掉 ElementHandle → `Element is not attached to the DOM` → 直接 500 + 立刻 re-navigate 再撞 → 同会话 4 连败进 40s backoff **静默**（DXAPAX / FJM9ER 同型）；Python 只见「发送失败」无码、工作台泛红字、坐席零感知，客户被晾。同期：授权后 PIN 浮层在场时 `maybeAutoHideAfterLogin` 一次性 return 永不再武装 → 窗口永久露着 + 每次导航抢前台 + `--no-sandbox` 黄条（TKV3HB）；反应 aria 正则 `/回应了|reacted with/` 早已匹不到当前胶囊 → 反应从未出边车（P7P8FY）。

## 0. 开工基线（11:0x）

| 项 | 事实 |
|---|---|
| 意向板 | `agent_probe -Intent "Q-24 messenger sidecar send chain #298 (…)"` 已登记 slug `q-24-messenger-sidecar-send-chain-298-services-m`；同刻 ACTIVE 他线：i18n 线（`inbox_workspace.py` / `zh_hant_auto.py` / `__init__.py` HOT-ZONE / `workspace_base.html` / `admin.py`）、Q-25 / Q-20 / Q-23——本线不碰其文件 |
| git | 索引空闲；HEAD `1ba8133f`（Q-20 总账收工） |
| 诊断包 | `tmp_diag/DXAPAX`（07:20–07:22 同会话 4×500 detached）/ `FJM9ER`（同型）/ `P7P8FY`（对方点赞零事件）/ `TKV3HB`（登录后窗口不隐藏 + 抢前台 + 黄条）；`tmp_diag/2J4H2V` = #297 值守自动单 |
| 现场代码 | `server.js` L4183–4271 `/send`：`box.click()` 前无重查、异常分支统一 500 无码、`recoverComposerOnce` 立即 re-navigate；`maybeAutoHideAfterLogin` PIN 分支 `return` 不再武装；`launchOptions` 无 `chromiumSandbox` → Playwright 默认加 `--no-sandbox`；`bringToFront` 仅登录三阶段（L2760/2837/3671），导航 / backfill / 来电路径无 |
| 测试基线 | `services/messenger-web` `node --test` 136 passed；Python 相关 36 套（i18n / sidecar 契约 / persona_reply / reply_diag / inbox_store…）基线里 `test_i18n_coverage::test_sealed_templates_no_legacy_date_formatters` **已在 HEAD 失败**（`unified_inbox.html` L16299 `toTimeString().slice`，非本线，见 §2） |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| A/B/C/D/E 边车 | #298 #297 #299 #300 | `8a1c1555` | **新** `services/messenger-web/send_chain.js`（纯函数层）：七码 `SEND_FAIL_CODES`、`isDetachedError`、`normalizeSendFailCode`（legacy reason / 异常文本 → 七码；detached 族先于 attach 判据，防「not attached」被吞成 upload_failed）、`sendFailBody`（`{ok:false, delivered:false, code, reason, detail, retry_after_ms, retries, reason_code, error}`）、`sendFailHttpStatus`、**`inputWithRebind`**（可注入回调编排：input → detached? clear+requery+input（同线程 1 次）→ detached? renavigate+input（1 次）→ 抛 `sendCode=composer_detached`；非 detached 错误原样上抛）、连败 / 待重试队列（`bumpJidFailStreak` / `shouldQueueRetry`（阈值 2，只排 composer_detached\|thread_not_found\|call_overlay\|upload_failed）/ `retryQueueUpsert`（同会话覆盖最新文案）/ `retryQueueDue` / `retryQueueSettle`（60s×3 终局））、`REACTION_ARIA_RE` + `parseReactionPill`。`server.js`：`resolveComposerStable`（waitForSelector → MutationObserver 300ms 静默 ≤2s（`MSG_COMPOSER_STABLE_MS`）→ 新 handle → focus）；`detectCallOverlay`（来电 / 通话 dialog 自动点 关闭/拒绝/Escape）；`performTextSend`（nav → PIN 自愈 / `e2ee_pin_pending` → 来电 → retry 模式先回读防双发 → 接受请求 → `resolveComposerStable` → `inputWithRebind` → Enter → `waitComposerCleared` → 回读）；`failSend`（noteSendFailure + per-jid 连败 + 排队 + `postStatus send_stuck "<code>\|jid=\|streak=\|preview="` + 15s `retryTick`：成功 → `postIngest origin=retry_queue` + `send_recovered`；3 次仍败 → `send_stuck_final`）；`/send` `/send-media` 全部失败分支走 `sendFailBody`（login_expired 404 / send_backoff 429 / e2ee_pin_pending 503 / thread_not_found 502 / call_overlay 409 / upload_failed 502 / composer_detached 500）；`mirrorManualOutbound` 回抄带 `direction:"out", origin:"external", sender_id:"external"`；`readThreadTail` 反应元素改按 `REACTION_ARIA_RE` 收 `{aria,text}` → `parseReactionFromAria \|\| parseReactionPill`；`maybeAutoHideAfterLogin` → 自重武装 `hideTick`（PIN 在场：`_pinState=missing` + `postInboxHealth` + 每 15s 复查 + `[msg-sidecar] window visible reason=e2ee_pin_prompt` 日志；`HIDE_PIN_GRACE_MS`（默认 2min）到 → **强制隐藏**；浮层消失 → `scheduleBackfillAfterPin`）；心跳里 e2ee 页上浮层消失 → `_pinState=ok` + 回填；`tryAutoE2eePin` 成功 → 回填；`pollInbound` 消费 `_backfillRebuildPending` 重建队列（只碰已读会话）；`launchOptions.chromiumSandbox = !MSG_NO_SANDBOX` | `node --check` ✓；`node --test` **152 passed**（新 `send_chain.test.js` 16 例：七码冻结 / detached 判定 / 归一表 / 体形状 / 重绑 6 序列 / 连败阈值 / 队列 upsert-due-settle / **DXAPAX 四连败回放**：4 次全部 `code=composer_detached` 结构化、第 2 次起 `queued=true` + 铃铛 3 响、队列 1 条、60s 后重试成功出队清零 / 反应 aria 与 pill 解析） |
| B/C/F Python | #298 | `6ce57f75` | `messenger_web_login.py`：`SIDECAR_SEND_FAIL_CODES` + `sidecar_fail_code(reason, detail)`（镜像 Node 归一）+ `http_error_fields` 新增 `code / retries / sidecar_detail`（老边车无 code → 归一；旧四键不变）。`account_orchestrator.MessengerWebWorker.send`：失败返回体加 `sidecar_code / sidecar_reason / sidecar_detail / sidecar_retries`（`error_kind` 优先取七码）；2xx `ok:false` 同样归码；`send_media` 由裸抛改 try/except 同契约。`send_failure_class`：`_SESSION_MARKS += login_expired`、`_CHANNEL_MARKS += thread_not_found/call_overlay/upload_failed`、新 `sidecar_fail_fields(result)`。`unified_inbox_send_routes`：文本 / 媒体两条 `delivered=False` 分支 → 有七码时 `HTTPException(502, {code:"sidecar_send_fail", sidecar_code, retries, retry_after_ms, retry_after_sec, sidecar_detail, message, platform, account_id, chat_key})`；留痕 `fail_reason` 即七码。**新** `src/inbox/sidecar_send_alert.py`：`parse_send_stuck_detail` + `push_send_stuck_bell`（`app.state.notif_queue` `sys_status`，id `ms_send_stuck:<plat>:<acct>:<jid>` 同会话合并，文案 `inbox.ms.bell.*`）；`unified_inbox_account_routes` session-status 入口先拦 `send_stuck / send_recovered / send_stuck_final` → 铃铛并返回（不进 `PlatformSessionHealth`）；ingest 路由读 `origin`：`retry_queue` → `store.mark_failed_outbound_resent_by_text`（同文案 failed → resent）；`external` 只落库留日志（起草只在 `direction=="in"`；让位 `note_agent_send` 只在工作台手发路由——外部出站天然不触发）。`store.py`：`mark_failed_outbound_resent_by_text` / `recent_failed_outbound`；`update_message_text` 按 `media_ref` 定位时加 `direction != 'out'`（识图描述不落我方图）。`reply_diagnosis`：新 finding **`sidecar_send_fail`**（messenger 近 24h `status=failed` 且 `fail_reason ∈ 七码` 或 detached 文本；params code/n/ts/ago_sec/preview/message_id）+ 码表。`unified_inbox_read_routes`：`hint_code=e2ee_pin_required` 不等 `stall_since` 即透 `inbox_hint`。`persona_reply.normalize_history`：出站媒体占位 → `[我方发出的图片]` / `[我方发出的<type>]`（入站仍 `media_placeholder`） | **新** `tests/test_messenger_sidecar_q24.py` **23 passed**（归一表 11 例 / http_error_fields 新旧体 / worker send 失败带码、2xx ok:false 归码、send_media 结构化不抛、成功体不变 / 路由字段形状 + 五类人话不掉兜底 / 铃铛解析与合并 / 留痕改标与读取 / 诊断 finding / media_ref 方向闸 / 出站媒体标签 / 三语词条齐全）；`test_messenger_send_failures.py` 契约随新三键更新（其余 36 套 731 passed） |
| B/C/D 工作台 | #298 | `8fc8d4c6` | `unified_inbox.html`：`_MS_FAIL_CODES / _msFailCodeLabel / _msFailThreeParts(det)` → `sendMsg` / `resendFailed` 识别 `detail.code==="sidecar_send_fail"` → 失败气泡红字三段式「<七码人话> · 已自动重试 N 次（或：未自动重试）· 你可以手机发，或 X 秒后再试（PIN / 登录 / backoff 各有第三段）」+「重试」按钮（走既有 `resendFailed` 同文案重放，**不换文案**）+ 埋点 `send_ms_<code>`；pin / login 码顺带 `loadChats()` 刷会话头；`_failReasonText` 七码留痕人话（失败行刷新后仍显示原因）；`_paintAcctBanner` 新增 messenger `inbox_hint==='e2ee_pin_required'` 黄条「Messenger 需在手机确认 PIN 后才能同步」+ 副标 + 「托管 PIN」按钮（`setMessengerPin`）；出站 `sender_id==='external'` → 时间戳旁「手机发出」角标（`_extChip`，title 说明不由工作台发出）；体检面板 `known += sidecar_send_fail`（`code_label / hhmm / n / preview`）。**新** `i18n_packs/messenger_sidecar_q24.py`（ZH/EN/ZH_HANT 同键：`inbox.ms.fail.*` 七码 + retried / next_* / retry_btn、`inbox.ms.bell.*` 三态、`inbox.ms.pin_bar(_sub)`、`inbox.ms.external_out(_t)`、`inbox.ms.media_out_label`、`inbox.ms.reaction_in`、`inbox.diag.sidecar_send_fail`；**不碰** `inbox_workspace.py` / `zh_hant_auto.py` 热区）。`unified-inbox.css`：`.ms-fail-tri` / `.ms-ext-chip` | `test_template_inline_js_syntax` ✓、`test_i18n_zh_hant`（≥99%）✓、`test_i18n_coverage` 除 §2 既有一红外绿；`test_messenger_sidecar_q24::test_i18n_three_lang` 三语键齐 |

### 1.1 回访 / 证据

- **#298**（DXAPAX / FJM9ER 同型）：根因 = `box.click()` 拿到的 ElementHandle 在 Messenger 重绘后失效 → detached → 直接 500 无码 + 立刻 re-navigate 再撞 → 同会话第 4 次进 40s backoff 静默。修后：detached 先同线程重查（DOM 300ms 静默）再输 1 次，仍 detached 才 re-navigate 1 次；连败 ≥2 进待重试队列（60s×3，只重放同文案、重试前回读防双发）+ 坐席铃铛 `send_stuck / send_recovered / send_stuck_final`；工作台三段式红字 + 重试。回放门禁 `send_chain.test.js` 「DXAPAX 四连败」零静默失败。
- **#297**（2J4H2V 值守自动单）：随 #298 同根因，同修。
- **#299**（TKV3HB）：`maybeAutoHideAfterLogin` PIN 分支一次性 return → 永不再武装。修后自重武装，PIN 在场 ≤2min 宽限后强制隐藏；黄条治本走 `launchOptions.chromiumSandbox`（`MSG_NO_SANDBOX=1` 才关沙箱）；`bringToFront` 核实只在登录三阶段（L2760 / L2837 / L3671），导航 / backfill / 来电路径零调用，来电 / 通话 overlay `detectCallOverlay` 自动关。
- **#300**（P7P8FY）：反应从未出边车 = `readThreadTail` 反应 aria 正则 `/回应了|reacted with/` 已匹不到当前 Messenger 胶囊 aria（`X 以 ❤️ 回应`/`Reactions: ❤️ 1`/`😂 1`）；修后 `REACTION_ARIA_RE` + `parseReactionPill` 双解析；入站反应经既有 `kind=reaction` ingest 落库 → 工作台角标（`inbox.ms.reaction_in`）；`reaction_followup` 默认关且 whatsapp-only → 不起草。
- **Y82GWM（归 Q-21 扩，本线只记证据）**：客户只发英文「Hi」，全自动起草中文（顶栏译文 = 中文）——`tmp_diag/Y82GWM` 目录为空（打包失败），证据仅工单描述；属 `conv_lang_plan` 语言判定，本线未动。
- **重启**：Node 边车（`services/messenger-web`）+ zhiliao 实例（`.py` 十件）须重启才生效，**本线未重启**，交值守（`deploy\instances\restart_instance.ps1` + 边车 watchdog 自拉）。

## 2. 收工核对

| 项 | 结果 |
|---|---|
| 红线 | `autosend_worker.py` / `autosend_policy.py` **零改动**（`git log --stat` 三 commit 无此二文件）；WA / TG 边车零改动；翻译区零改动；失败重试只重放同文案（`_retryBody` / `retryQueueUpsert` 覆盖最新原文案），**无任何自动换文案** |
| 他线热区 | `inbox_workspace.py` / `zh_hant_auto.py` / `i18n_packs/__init__.py` / `workspace_base.html` / `admin.py` / `persona_manager.py` / `contact_names.py` / goals / `bug_intake.py` / `deploy/desktop/*` 未带；工作树剩余 M 文件（`bug_intake.py` / `admin.py` / i18n 热区 / `login_redirect.py` / 五模板 / 六测试）全属他线 |
| 门禁 | `node --test` 152 passed；Python `test_messenger_sidecar_q24 (23) + test_messenger_send_failures + test_send_fail_reason_p5 + test_template_inline_js_syntax + test_i18n_zh_hant` = 71 passed；宽跑 36 套 731 passed |
| 既有红（非本线） | `test_i18n_coverage::test_sealed_templates_no_legacy_date_formatters`：`unified_inbox.html` L≈16299 `toTimeString().slice` 在 HEAD `1ba8133f` 已存在（`git show HEAD:… \| rg -c toTimeString` = 1），本线未引入亦未修（属他线模板债） |
| git | 三 commit 均带 #298，每次 `git add` 显式路径，提交前 `git diff --cached --name-only` 为空；落点表随 commit 补行 |
| 总账 | 只改 `docs/Q批_进度总账.md` Q-24 行；`agent_probe -Done` |
