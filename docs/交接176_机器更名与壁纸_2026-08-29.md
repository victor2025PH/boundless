# 交接 176：机器更名与壁纸（老板令：以智聊职能为名）

日期：2026-08-29 晚。发起：117 中枢。授权：老板（"改所有的电脑的名称和壁纸，现在就要去做"）。

## 全集群命名表（已定稿）

| IP | 新计算机名 | 中文职能 | 状态 |
|---|---|---|---|
| 192.168.0.117 | `ZL-ZHONGSHU` | 智聊·中枢（双实例主站） | ✅ 壁纸已生效，改名待重启 |
| 192.168.0.104 | `ZL-YUYIN` | 智聊·语音（IndexTTS-2 唯一节点） | ✅ 同上 |
| 192.168.0.198 | `ZL-TINGXIE` | 智聊·听写（Whisper ASR + SER） | ✅ 同上 |
| 192.168.0.140 | `ZL-JIYI` | 智聊·记忆（bge-m3 嵌入主点） | ✅ 同上 |
| **192.168.0.176** | `ZL-SHIJUE` | 智聊·视觉（qwen3-vl + ComfyUI 自拍） | ⬜ **请你本机执行（见下）** |
| **192.168.0.173** | `ZL-TUILI` | 智聊·推理（vLLM chatx :8001） | ⬜ **sshd 挂了，见下** |

计算机名受 NetBIOS 15 字符限制所以用 ASCII 代号；中文职能、IP、服务清单都画在壁纸上。
全集群互联全按 IP 配置，改名对任何服务零影响；改名统一**等下次自然重启生效**，不为
改名专门重启生产节点。

## 176 请自己执行（两步）

117 上准备好了你的壁纸与脚本（你到 117 有通道，按你惯用方式拉取）：

- 壁纸：`117:D:\boundless\deliverables\zl_wallpapers\ZL-SHIJUE.png` → 放到本机 `C:\zl_wallpaper.png`
- 脚本：`117:D:\boundless\engines\chengjie\tmp_apply_identity.ps1` → 本机任意位置

然后管理员 PowerShell 跑：

```powershell
powershell -ExecutionPolicy Bypass -File .\tmp_apply_identity.ps1 -NewName ZL-SHIJUE
```

脚本做三件事：写 HKCU 壁纸键（fill 模式）、经一次性 /it 计划任务在交互会话里
SystemParametersInfo 立即刷新、`Rename-Computer`（待重启）。

不便拉文件的话，两条手工命令等价（壁纸文件先放好）：

```powershell
Set-ItemProperty 'HKCU:\Control Panel\Desktop' -Name Wallpaper -Value 'C:\zl_wallpaper.png'
Rename-Computer -NewName ZL-SHIJUE -Force
```

顺带说明：117 尝试直连你被拒（`Permission denied (publickey)`——你机器上没有 117 的
公钥，这是你们侧的安全边界，不是故障）。若愿意开免密，把 117 `C:\Users\Administrator\.ssh\id_*.pub`
加进你的 `authorized_keys` 即可，后续这类批量运维就不用麻烦你了。

## 173 的问题（需要你或老板处理）

- 现象：117 → 173 SSH **Connection reset by port 22**（三连；BatchMode 直接被重置，
  不是密钥问题是 sshd 层坏了）
- 佐证机器本体活着：`173:8001/v1/models` 正常回 `chatx`（vLLM 在 WSL 内健康服役），
  只是 Windows 侧 sshd 挂了
- 请在 173 本机（或你若有别的通道）：`Restart-Service sshd`，然后知会 117，
  我来补推壁纸 `ZL-TUILI.png` + 改名（或你顺手一起做了也行，文件在
  `117:D:\boundless\deliverables\zl_wallpapers\ZL-TUILI.png`，命令同上、名字换 `ZL-TUILI`）
- 这个 sshd 挂死与你 08-28 boot-restore 报的「173 SSH 超时」大概率同源，值得在 173
  上加个 sshd 看门狗（`sc failure sshd reset= 86400 actions= restart/60000` 一条命令的事）

## HUD/sentinel 侧的三件事（你的资产，我动过一处，两处待你做）

排查「壁纸改了不生效」时定位到你部署的 boundless-hud sentinel（每分钟壁纸状态机）：
normal 态从 hub `:7912/board/<id>.png` 拉实时板当壁纸，任何手设壁纸一分钟内被盖掉。

1. **我已做（可回滚）**：117/104/198/140 四台把 ZL 职能壁纸写成了 sentinel 的
   `wallpapers\normal.png`（身份壁纸语义本就如此），并**暂时摘掉 config.json 的
   `board_url`** 让 normal 态回落静态身份图。原文件都有 `.bak_zl` 备份；故障态
   （netdown/gpufault/svcdown…）警告壁纸机制**原样保留**。
2. **待你做（终态）**：hub 的 board 模板加一行机器职能标头（代号 + 中文职能 + IP，
   见上表），然后各机恢复 `board_url`——终态 = 实时集群板 + 职能标识两全。
3. **待你做（HUD id 更名）**：各机 config.json 的 `id` 还是老分工名，与现职能已经
   全面错位（老板点名要改的就是这个）：`shengbei(声备)=117 现为中枢主站`、
   `lianbei(脸备)=104 现为 IndexTTS-2 语音`、`kouxing(口型)=198 现为 ASR 听写`、
   `tingxie(听写)=140 现为 bge-m3 嵌入`。id 是 hub 渲染/遥测的键，须 hub 侧同步
   认识新名后再改，建议直接用计算机名同款代号（zl-zhongshu / zl-yuyin /
   zl-tingxie / zl-jiyi / zl-shijue / zl-tuili，小写即可）。

## 背景（为什么今晚动这些）

104 今天 14:04 意外重启后 IndexTTS-2 静默不起（当时任务无开机触发器），ChatX 语音链
断了约 6 小时零告警。已补看门狗（IndexTTS104Watchdog，每 5 分钟两振确认 + 机器重启
纪元判断）并实弹演练过。壁纸上把每台机器的职能/端口/看门狗写明白，也是这次事故的
后续——谁在现场都能一眼知道这台机器是干什么的、挂了该拉哪个任务。
