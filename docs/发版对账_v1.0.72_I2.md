# 发版对账 v1.0.72 — I-2（#158 审批页返回 + #154 铆定收尾 + 1.0.72 发版对账建档）

> 指令：`docs/指令_I-2_审批页与铆定收尾_2026-09-04.md`。总账：`docs/I批_进度总账.md` I-2 行。
> **本条不发版、不重启实例。** 模板/i18n 热更新即上生产；`desktop/**` 改动随 1.0.72 包出货。
> 每完成一项立刻追加一行；未完成项也留一行写「进行中：已改 X，还差 Y，下一步 Z」。
> 下一个账号接手：先读本表逐行核对，再跑 `git -C D:\boundless log --oneline -15 -- engines/chengjie` 确认 commit 真在。

## 落点表

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注/剩余 |
|---|---|---|---|---|---|
| A2 审批页进得去出不来（定层＝**壳层**，嫌疑 1 的变体，非页级脚本错） | #158 | `c0412669` | **根因**：`unified_inbox.html` 的「去审批」（`toggleDraftQueue` L21840 / `diagOpenDrafts` L14724）是**同窗** `location.href='/workspace/drafts'` → 壳内主窗收件箱 webview **自己**漂到子页；再点「聊天」：`_win_unique`(新壳 `wsRoute`) → `window.open('/workspace')` → `main.js focusMainInbox` → `renderer onOpenWorkspace` 只 `Inbox.activate`（标签本来就激活）＝**静默吞点击**。证据包 `tmp_diag/U7WQES` renderer.log 无 `Uncaught`（排除嫌疑 2）。**修三层**：① 新 `desktop/renderer/ws-home-route.js`（纯函数双模式）`resolveWorkspaceHome(cur, home, req)`：webview 同源但不在收件箱主路径 → `navigate` 回 `Inbox.homeUrl`（保 `?lang/&theme`，深链 conv/focus/mid/flag/case 并入 URL，页面自带 `?conv=` 解析）；在家 → `deliver`/`none` 旧行为；`/login`、`about:blank`、跨源、无 home 口径刻意不动；`renderer.js` 登记 `Inbox.homeUrl = fullUrl` + `onOpenWorkspace` 经 `routeWorkspaceOpen` 真 `loadURL`；`index.html` 挂载模块（早于 renderer.js）。② `main.js`：`makeBackendPopupHandler(from)` 带弹窗 `{win, slot}`，`openBackendPopup` 主窗接手后 `closeHandedOffPopup(from)` **只收 wsub 辅助窗**（admin 后台弹窗刻意不关）——弹窗内点 shell 导航不再静默 deny。③ `draft_review.html` 顶部原生 `<a href="/workspace" id="dr-back-chat">`（**无 target/无 onclick**：页级 IIFE 崩了也能出去；去重语义由 `_win_unique` 捕获相位拦截器统一走 `__wsGoHome`，未另写一套）+ `.dr-back` 样式 + `i18n_packs/draft_review_page.py` `draft.back_chat`/`draft.back_chat_title` zh+en；`ui-build.txt` bump `20260904-1036` | `desktop/test/ws-home-route.test.js` 44 断言（真值表 ×8 形态 + renderer/main/draft_review/_win_unique 四处静态契约）接进 `npm test`，**`cd desktop && npm test` 全绿**；`backend-popup-unique.test.js` 放宽 handler 参数正则；python 侧 `test_i18n_coverage`/`test_workspace_i18n_render`/`test_template_unique_ids`/`test_template_orphan_refs`/`test_inbox_inline_handlers_exported`/`test_template_dynamic_dot_access` 387 passed；`test_ui_build_freshness` 3 passed | 验收 §2：③ desktop 用例 ✅；④ 返回入口不依赖页级脚本 ✅（静态契约钉死无 onclick）；①② 两条进入路径 + renderer.log 无 Uncaught 需 **1.0.72 打包态**在坐席机实跑（本机无包，代码层三处契约已钉）——交 I-6/值守随包验收。他线存量红（HEAD 即红，未动）：`test_stale_update_prompt.py` 3 例要求 `shared/copilot/app.html` 读 `severity`/复制按钮文案同步，`shared/copilot/**` 属 I-5 |
| §5 I-1 转交：`unified_inbox.html` 两处 `?v=` → `20260904a` | （I-1 #160 词典 + 他线存量红） | `69316cc3` | `unified_inbox.html` L91 `cp-i18n.js?v=20260902a→20260904a`、L103 `cp-voice.js?v=20260902a→20260904a`（Grep 定位 + 精确替换，未整读） | 与 `shared/copilot/app.html` 的 `20260904a` 对齐；`ui-build.txt` 已在 `c0412669` bump | I-1 落点表备注的「缓存戳 app.html=20260904a vs unified_inbox.html=20260902a」存量红应随之转绿（收口 gate_sweep 复核） |
| A1 尾巴 c）`sendpoint_guard.py` 无 pin 时按我方出站主语种保守回落 | #154 | **已在 HEAD `2afc4e0f`，复核通过，未重做** | `src/ai/sendpoint_guard.py` L263 `outbound_history_lang_pin`（窗口 12 条出站、可判样本 ≥3、主语种占比 ≥2/3、且**最后一条**也是该语种——刚切语言不被历史拽回；纯媒体占位不算证据）；L410-418 `sendpoint_lang_pin_fix` 顺序＝显式 pin → **hist** → peer hint；`lang_hist_conflict` 计数 | `tests/test_sendpoint_guard.py` 含金标 `test_154_japanese_lines_rewritten_via_history_pin`（一路英文历史 + 四条日语稿全改写）+ 样本不足/刚切语言两反例；本轮实跑 `test_sendpoint_guard.py` + `test_tg_gap_probe.py` 全绿（62 passed 含模板门禁） | 金标「历史 90% 英文、对方突发日语 → 仍出英文」＝hist 判据的直接覆盖；无需补代码 |
| A1 尾巴 d）坐席侧出站译文（✎ 行）语言锁定会话「收→」 | #154（上午形态，JV7MGQ 包定性） | `8ff7f422` | **机制**：✎ 行＝`.msg-orig-sub`（css L786），数据＝`outbound_translations.original_text`；自动链（`outbound_translate.py` L619 / sendpoint 守卫）记的是 **LLM 草稿**——钧发日语 → 草稿日语 → 守卫 pin=en 译英真发英（客户侧正确），✎ 行却渲染日语草稿，且 `_outHasOrig` 把它当「已双语」压掉中文补译。服务端不知道「收→」（坐席端逐会话 localStorage），锁定落前端：`unified_inbox.html` 新顶层 `_origReadable(meta)`（记录 `source_lang` × `_xlateIn` 不同语 → 不算对照原文；未知源/未设收→ 保持旧行为）；`_outHasOrig` 两分支 + `renderMessages` ✎ 行两来源同一判据 → 不可读时不出 ✎ 行、不抑制补译，出站气泡交常规补译按「收→」出译文行；`ui-build.txt` bump `20260904-1037` | 新 `tests/test_outbound_orig_row_154.py` 5 passed（静态契约 ×4：helper 单定义/两消费点同判据/aggregate 带 `source_lang` 证据/自动链记 `source_lang=eff_source`；node 真值表：ja×zh 不可读、zh-CN=zh、zh-tw 不折叠、未知源与未设收→ 旧行为、英文坐席×中文原文走补译）；`test_inbox_inline_handlers_exported` / `test_template_free_capture` / `test_ui_build_freshness` 绿 | 未动 `store.py`（谁都不改）与 `unified_inbox_send_routes.py`（I-4）；手动链 `source_lang` 取请求体（可空 → 旧行为），自动链恒有 `eff_source`。**未做**：服务端 `_enrich_outbound_originals` 侧不改（无「收→」可依），前端锁定已闭环 |
| A5 建 `docs/发版对账_v1.0.72.md` 骨架 + 症状族对账单补两层 | — | — | 未开始 | — | 最后做 |
