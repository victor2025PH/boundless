# 指令 O-5：值守（O 批归位与答复——不与 N-5 重叠）

> 预算 **$60**。工单 #252–#255 + WYNN22 / HM7XBA 归位。**N-5 正在做**：并单（#244–#249→#243、#251→#250、#253？）、钧机脚本、TAZ9QS 判读、自动立单去重、1.0.77 打包——**先读 `docs/N批_进度总账.md` N-5 行，N-5 已做的不重做**。
> 底座 `docs/修复指令_O批_并行_2026-09-08.md`。落点表 `docs/发版对账_v1.0.78_O5.md`。

## 开工前必读

1. `docs/N批_进度总账.md` N-5 行 + `docs/O批_进度总账.md` 各行状态。
2. 工具：`engines/chengjie/tools/duty_reply.py --group neice --ticket N --receipt --text-file F [--status confirmed] [--dry-run]`（每条回复带工单号；配置文件改法被 guard 拦——只教界面路径）；`duty_evidence.py --data-root D:\chengjie-instances\zhiliao\data {received --diag CODE|probe|set-version} N`；立单模板 `tmp\duty_split_0906c.py`（dry-run → `--apply`）；`tmp\three_day_overview_0908.py`（只读总览）。
3. 用户画像 `docs/值守_报障群用户画像_2026-08.md`：skuio 要结论 + 路径 + 验证步骤；每份报告都要有工单号回声。

## 红线

- 所有台账写入先 dry-run；对外只经 `duty_reply.py`；不改 `duty_auto_ticket.py`（N-5 在动）。
- 每 commit 带工单号；`git add` 显式路径。

## A 回 3HNCJ7（用户在等排期答复）

- 一条回复挂 #254，内容：① 今晚 P0 批次排期：O-1 A 停联硬停 + O-2 A 记忆基线 = 止血、并入下一版；O-1 B/C/D（去 AI 标点 / 陪伴底稿 / 拟人节奏）、O-3 摸底、O-4 引用一期 = 1.0.78；② 引用回复：#37（1.0.74）已有「连发 ≥2 带引用」；一期 MVP 按他的切法做（规则清单五条）；二期 / 三期估算各一个对话，排 1.0.79；③ 他要的「出站文本落日志」在 O-1 B（`[outbound] punct_fix= style_fix=`）；④ 验证步骤留待 1.0.78 回访。**不承诺日期，只承诺版本号。**

## B 归位（先 dry-run）

- WYNN22（记忆抽取 intents=[]）：从 #201 摘出改立新单（reporter skuio，severity P0，title「1.0.76 干净安装 AI 记忆完全不抽取：intents=[]」，reporter_version 1.0.76）→ `duty_evidence received --diag WYNN22`；回声一条：「#N 已立，去向 O-2」；O-2 落点表补新号（通知 O-2 或自己补一行）。
- HM7XBA / 5XRQ7C（摸底 100% 被动）：改挂 #236 族（`dup_of` 不用——新立 #N「目标摸底主动腿：auto=3 sent_24h=0 + 第 1 天不摸 + 采集链死」，note 关联 #236 #216），回声「#N 已立，去向 O-3，前置 M-7 随 1.0.77」。
- #253 → 并入 #252（`dup_of=252`，note 保留六份报告码）；#255（TDNJHQ em dash 统计）→ 并入 #254；若 N-5 已并则跳过。
- #252 / #254 `--status confirmed` + 回声：「#252 客户识破 AI + 停联仍连发 → O-1（A 止血并入下一版）」「#254 em dash / 客服句式 / 引用回复 → O-1 B/C + O-4」。

## C 回访口径汇总（交 N-5 E 或 1.0.78 发版）

- 收 O-1 / O-2 / O-3 / O-4 收工时留下的回访文案 → `docs/发版对账_v1.0.78_O5.md` §回访清单（工单号 / 收件人 / 文案 / 验证步骤）；1.0.77 若带了 O-1 A / O-2 A，则把两条止血的回访交给 N-5 E 一起发。
- 三日复盘一句话进群（可选，老板拍）：三天 89 张 / 87 张有 commit / 1.0.77 待装 / 今天新族 O 批。

## 收工

- `docs/O批_进度总账.md` 只改 O-5 行；`agent_probe.ps1 -Done`。

## 开新对话粘贴

```
读 docs/指令_O-5_值守_2026-09-08.md，按它做 O-5（回 3HNCJ7 排期答复 / WYNN22 改立 P0 新单去向 O-2 / HM7XBA 新立单挂 #236 族去向 O-3 / #253→#252、#255→#254 并单 / #252 #254 confirmed 回声 / 回访口径汇总）。预算 $60。先读 docs/N批_进度总账.md N-5 行（N-5 已做的并单不重做，不碰 tools/duty_auto_ticket.py），再读 docs/修复指令_O批_并行_2026-09-08.md 去向表。所有台账写入先 dry-run；对外只经 engines/chengjie/tools/duty_reply.py 且每条带工单号；不承诺日期只承诺版本号。收工只改 docs/O批_进度总账.md 的 O-5 行并 agent_probe -Done。
```
