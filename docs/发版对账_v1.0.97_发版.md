# 发版对账 v1.0.97（2026-09-23）

一句话：**语音「语气表达」四档可调（人设级 + 会话级覆盖）+ B990 两处修复（工具箱发语音报错真因、外语译文夹中文重译）+ 人设「应用到…」补全全部已接入平台** 随一个 clean 公共包发出——公共渠道 + 内测同字节桥接 + 官网五值/更新记录 + 频道 @hykj7。工作台简洁/完整模式重做**不含**在本号（留 1.0.98）。

## 1. 包

| 项 | 值 |
|---|---|
| 文件 | `ChatX-Setup-1.0.97.exe` |
| 大小 | **492,660,997 B**（470 MB；比 1.0.96 多 32,535 B） |
| sha256 | `1b5dcc95a2b71aa9b3022c8b33333ebdcb376591f864c80ba000ef38e216f53f` |
| sha512（latest.yml） | `B/abZuViPh4Iber+dGvL1hT7YyavpOmsD8mj29SKL6U0m7YBwbnAHrFnW0HYtKuLxWXa9W3hOvciExpnMQTqgQ==` |
| blockmap | 511,114 B |
| flavor | clean（`CHATX_ALLOW_STANDARD=1`，`build-info.json flavor=clean`，`smoke_clean.py` 通过） |
| 源 | 分支 `devin/1789968548-wechat-pc-multi-account` @ `8da6a835`（`9446cace` 版本号提交 + `8da6a835` cp-i18n-keys 打包门禁修复；其父 `c9481682` 为语音情感 5 提交合入点），干净 worktree `D:\boundless-r97`（detached） |
| build-info | `commit=8da6a835` `dirty_count=12` —— 与 1.0.96 相同，12 项全部是 `desktop/build/i18n-packs-staged/*.py` 由 `build:backend` 重新暂存出的生成物，无源码脏改 |
| 后端 fingerprint | `e7c517960adc13af…`（files=2237，built_at 2026-09-23T01:56:59Z；首次 `build:backend` 后 shared/copilot 因门禁修复变更，用 `npm run refresh:datas` 同步 datas 后 `check_backend_freshness` 通过） |
| 冒烟 | `build:backend` 安装版后端冒烟全过；predist 门禁 renderer-boot 83 / outbound-wiring 16 / package-layout / uninstall-nsh / shell-i18n / cp-i18n-keys 6 全过；after-pack 路径长度 173/186 通过；随包交付物 22 项齐备 |

打包命令：`npm run build:backend` → `npm run dist:win:clean`。干净 worktree 需和 r96 一样接上 gitignore 的构建依赖：`desktop/node_modules`、`services/{whatsapp-baileys,messenger-web,qq-personal}/node_modules` 走 junction 到 `D:\boundless-r93`，`desktop/build/ffmpeg/` 从 r96 复制（缺任一项 `package-layout.test.js` 会拦）。

## 2. 本号带了什么

| 提交 | 内容 | 主文件 |
|---|---|---|
| `c9481682` 及其前 4 个语音提交 | 语音「语气表达」四档 `restrained/natural/vivid/dramatic`：人设页档位 + 基线情绪控件、预览对比；工作台语音面板会话级覆盖并显示生效档位与来源（session/persona/global），👎 降一档；发送/预览 metadata 带 expressiveness/emotion；档位贯通情绪推导与 TTS；i18n 中/英/繁 补齐，`test_voice_expressiveness.py` 新增 | `ai/voice_emotion.py` `ai/tts_pipeline.py` `ai/persona_voice.py` `ai/voice_tristate.py` `web/routes/voice_routes.py` `web/routes/unified_inbox_send_routes.py` `shared/copilot/components/cp-voice.js` `templates/personas.html` |
| `d4ef0188` | 工具箱「发送语音给对方」失败提示 `[object Object]` → 解包结构化 detail 给出真因 | `copilot-client.js` `cp-voice.js` `unified_inbox.html` |
| `2c1c59d1` | B990：中→外语译文残留汉字专名 → 定向重译一次（`[xlate] cjk_residue`） | `ai/translation_*.py` |
| `5f98dfb3` `9454b7d7` | 人设「应用到…」补全所有登记平台账号（`other_accounts`），按平台分组、平台名 i18n、账号在线状态；路由清单补登记 | `web/routes/persona_routes.py` `web/static/js/persona_apply_modal.js` `web/i18n_packs/persona_studio.py` |
| `8da6a835` | `cp-voice.js` 档位说明由 `"cp.voice.vx_desc_" + lv` 动态拼键改为 `_vxDesc()` 字面键查表（`cp-i18n-keys.test.js` 打包门禁要求键可静态解析） | `shared/copilot/components/cp-voice.js` |

回归：语音分支影响面 93 个测试文件 **1718 绿 / 19 红**；19 红（`test_tts_budget` 7、`test_unified_inbox_stage1` 3、`test_cp_app_i18n_keys` 2、`test_voice_pipeline_integration` 2、`test_voice_source_consistency` 2、`test_i18n_coverage` 1、`test_tts_lang_gate` 1、`test_voice_colloquial_llm` 1）与基线 `9454b7d7` 同名子集（539 绿 / 19 红）完全一致，为既有红，本号未新增、未改测试。

## 3. 发布链

| 步 | 状态 | 证据 |
|---|---|---|
| 公共渠道 | ✅ | `publish_chatx.ps1 -DistDir D:\boundless-r97\...\dist-clean -Channel public -NotesFile tmp\notes_1097.txt -Yes`：exe 远端 sha512 复核一致 → 指针 → pm2 rolling → R2 同步 → 公网 `latest.yml=1.0.97 manifest=1.0.97 exe=HTTP206`；`announcements.json` 带 `release-v1.0.97`；keep=3 剪掉 1.0.94（`tmp/publish_1097.log`） |
| 内测桥接 | ✅ | 同字节 → `tmp/bridge_1097`（`latest.yml` 改名 `latest-internal.yml`）`-Channel internal -Yes -NoAnnouncement`；公网 `internal/latest-internal.yml=1.0.97`，公共指针未动（`tmp/publish_1097_internal.log`） |
| 官网 | ✅ | `64b4c334` 五值 1.0.96→1.0.97 + 更新记录条目（`gen-chatx-changelog.mjs`，中英 8 条）；`gate:content` 6/6 PASS（`tmp/gate_1097.log`）；`deploy.ps1 -FromHead` 部署完成，公网体检 healthy（`tmp/deploy_1097.log`） |
| 频道 | ✅ | @hykj7 **messageId=207**，campaign `release-1097`，按钮「立即更新」→ `/download/chatx`；只发频道（群 @hykjz 自动转发，未双发） |
| 频道文案修正 | ✅ | 207 号消息已就地编辑（`/api/admin/broadcast` 新增 `editMessageId`，按钮按同 campaign 重建，未新发）：「已装 1.0.27+ 会自动收到更新提示」→「**之前版本会自动收到更新提示**」。**发版公告模板规则：更新提示一句固定用「之前版本会自动收到更新提示」，不指具体版本号**（公告源 `tmp/channel_1097.txt` 已同步） |
| 分支 | ✅ | `8da6a835` + `64b4c334` 已推 origin（PR #24 分支） |

公网复核（发布后）：`/downloads/latest.yml` 1.0.97；`/downloads/manifest.json` 1.0.97 / sha256 1b5dcc95…；`/downloads/ChatX-Setup-1.0.97.exe` HTTP 200 Content-Length 492660997；`/downloads/internal/latest-internal.yml` 1.0.97；`/downloads/announcements.json` 含 release-v1.0.97；`/download/chatx` 指向 1.0.97；`/download/chatx/releases` 含 1.0.97。

## 4. 边界 / 未做

- 语气档位现网效果（默认 natural 是否仍偏满、👎 降档是否顺手）需实例上按人设实测后再定默认值。
- 工作台简洁/完整模式重做（原方案文档）留 1.0.98。
- 19 项既有红测试（tts_budget / unified_inbox_stage1 / cp_app_i18n_keys 等）本号未处理，建议单开一轮清理。
- `i18n-packs-staged` 生成物与库内快照不一致（同 1.0.96 §4），不影响产物。
