# v1.0.71 发版范围对账清单（值守循环 v3 §F 硬门禁；建于 2026-09-02 晚）

> 用法：群公告前逐条核「指令项 ↔ 修复/发版提交（能在 diff 里指认）↔ 随包与否」。
> 对不上 → 公告只写实际内容 + 如实列缺项，同时向老板补指令。**核完在「对账」列打 ✓/✗ 并填提交号。**
> 随包判定：`engines/chengjie/src/**`、`desktop/**`、`config/*.yaml`、`i18n_packs/**` 改动随安装包到用户机器；
> `tools/duty*`、`deploy/duty/**`、`docs/**` 是值守侧工具/台账，**不随包**，不得写进面向用户的公告。

## 一、本版应含指令（0902 晚批）

| # | 指令 | 工单 | 落点（可在 diff 指认） | 随包 | 对账 |
|---|---|---|---|---|---|
| D1 | AI 贬损自家阵营推广：①人设 prompt 注入「自家在推活动」白名单（登记表与 P13/P15 同源 = `site_catalog.yaml: camp_promotions`）②出站贬损守卫（自家词/机制泛词 + 负面定性词 → 整句剥离），挂载点与 claim_guard 同（A/B 线共用 `_apply_goal_link_guard`） | #147 | `src/companion/goals/offers.py`（camp_promotions/camp_terms/camp_block）· `claim_guard.py`（find/sanitize_camp_disparagement）· `service.py`（camp_terms 进 `_goal_cta`+`catalog_guard_facts`、`build_camp_block_for_chat`）· `skill_manager.py`（`_camp_block` 注入 + `_guard_camp_disparagement`）· `ai_client.py` 消费 · `stats.py` camp_stripped · `config/site_catalog.yaml` 登记段 · 门禁 `tests/test_camp_guard.py` | ✓ | ✓ `0cdf9ada` |
| D2 | 手机删消息工作台不同步：协议多开 TG worker 补删除事件监听（B87 只接了 A 线）+ 已删消息关联记忆清理（情景记忆按 source_quote 反查删 + A 线上下文历史剔除）+ B 线历史默认剔 peer 软删行 | #146 | `src/integrations/account_orchestrator.py`（`_wire_receipts` 收 UpdateDeleteMessages/Channel）· `protocol_bridge.py`（`report_deleted_messages` 取原文→软删→`register_deleted_memory_purger` 钩子）· `src/inbox/peer_delete_purge.py`（新）· `store.py`（`select_live_by_platform_msg_ids`；`list_recent_messages` 默认剔 `deleted_by='peer'`）· `episodic_memory_store.delete_by_source_quotes` · `context_store.peek` · `unified_inbox_account_routes.py` 注册钩子 · 门禁 `tests/test_peer_delete_memory_purge.py` + `test_peer_delete_sync.py` 口径更新 | ✓ | ✓ `0cdf9ada` |
| E1 | 原生菜单栏/帮助下拉未过 i18n：监听工作台 `/set_lang` 导航 → 同步壳配置 `unified_inbox.lang` + `Menu` 重建 + 壳 renderer 就地换词；shell-i18n 门禁补 SHELL_STR 双语对等/菜单词条/重建机制检查 | #151 | `desktop/shell-lang-sync.js`（新，纯函数）· `desktop/main.js`（`rebuildAppMenu`/`applyShellLangFromNavigation`/`watchWebLangSwitch`，挂 did-attach-webview + openBackendPopup）· `shell-preload.js` `onShellLang` · `renderer/shell-i18n.js` `setLang` · `renderer/renderer.js` 消费 · 门禁 `desktop/test/shell-i18n.test.js` §⑦ | ✓ | ✓ `2b022a22` |
| E2 | 后端重启窗「媒体发送失败」红提示恢复后不消失（钧机 SY8TGE：该时段实际全部送达）：媒体件幂等键随件固定（重试同键，不再双发）+ 无 HTTP 响应的失败标「结果未知」+ 后端恢复/线程刷新后按对话记录对账自动清除幽灵失败 | #148 族（#145件②） | `src/web/templates/unified_inbox.html`（`_stageMedia` cmid、`_doSendMedia` netFail、`_reconcileGhostMediaFailures`、`_loadThreadBody`/`_setConnBanner` 挂点）· `i18n_packs/inbox_workspace.py` 新键 `inbox.media.retry_hint_unknown / ghost_cleared / send_fail_net_restart` · `ui-build.txt` bump | ✓ | ✓ `2b022a22` + `91a6749e` |
| B1/B2 | #149 新克隆声挂载断链+选声必达 & #121 三进宫「疑似无声」误报终审除根 | #149 #121 | `src/ai/voice_profile_guard.py`（新）· `speech_verdict.py`（新）· `persona_voice.py` / `tts_pipeline.py` / `voice_routes.py` / `send_routes` 选声金标接线 · cp-voice 组件 MIME 修正 + 双语 5 键 · 金标夹具 `tests/fixtures/gold_clone_indextts_22k.wav` · 门禁 `test_voice_selection_gold` / `test_clone_profile_mount_guard` / `test_preview_speech_verdict` / `cp-voice-speech-verdict.test.js` | ✓ | ✓ `cfe9ad47` |
| C1-C4 | #123族 序号洞双哨兵（gap-probe 连续性判洞 + bug_intake 跳号哨兵）· rate_capped 值守可见 · guard=force 溯源 · ops_alert 键自检 | #123 #148 | `unified_inbox_account_routes.py` / `store.numeric_platform_msg_id_holes` / `telegram_client._bug_intake_seq_gap_sweep` / `send_routes` force 字段 / `webhook_notifier` / `duty_watchdog` · 门禁 `test_tg_gap_probe` +148 / `test_duty_watchdog` +107 / `test_send_force_guard_origin` | ✓（duty_* 工具不随包） | ✓ `a9cd8243` |
| A1 | #148 文本件重发双发：重发带原件 client_msg_id + 服务端按原 id 幂等 | #148 | `unified_inbox_send_routes.py` resend_of 幂等 · 前端重发按钮带原 id | ✓ | ✓ `3c702b37` |
| A2 | LINE 签名桥重启后 E2EE 钥匙自愈 + 入站媒体 OBS 404「已过期」终局提示 | #148 | `line_media.py` / `account_orchestrator.py` 桥重启 key store 恢复 + `Item_e2ee_key_not_exists` 自动重握手一次 | ✓ | ✓ `2a9bf45a` |
| A3 | #150 语音连发熔断：voice_burst_guard 从只告警升级降级文字（第 N 条开路+冷却自动恢复），A/B 两发送线同接决策入口 | #150 | `src/client/voice_burst_guard.py`（`voice_degrade_reason`）· `sender.py` voice_reply · `autosend_helpers.py` 决策链 · 门禁 `test_voice_burst_circuit_150`（7 例） | ✓ | ✓ `5e15319d` |

**不在本版（如实列缺，公告已写明）**：F1/F2（#152 复读机化漏网直发 + 工作目标不推进）——0902 深夜新增，1.0.71 发版时未派修；**0903 06:xx 值守自修完成**（F1 `30951d10` / F2 `e99a61c7`），待 1.0.72 随包。

## 五、发版结果（0903 05:51 完成）
- 包 `ChatX-Setup-1.0.71.exe` 589,558,402B sha256 `1b004823…3b0a35`；publish_chatx 原子发布 OK（VPS sha512 校验 / latest.yml+manifest=1.0.71 / R2 同步 / announcements release-v1.0.71 / 旧 1.0.68 清理 / 公网 exe HTTP206）；changelog 10 条；gate:content 6/6；deploy 公网体检 healthy。
- 群公告 mid=1098（含逐单说明 + 版本验收追证任务）；#146/#147/#148/#149/#150/#151 标 fixed 并按 §G「先升级」口径回访；update_hint 切 1.0.71。发版提交 `a6bfaf98`。
- 全量 pytest 63 红全为存量（B 批前基线同红）；本批门禁全绿。

## 六、1.0.72 待随包（累计）
| 提交 | 内容 |
|---|---|
| `30951d10` | #152 F1 退化循环截断 + 截空不回落 |
| `e99a61c7` | #152 F2 目标注入判定落日志 + 英雄行原因胶囊 |

## 二、已提交、必须随本版到用户机器（老板 0902 点名）

| 提交 | 内容 | 为什么要随包 | 对账 |
|---|---|---|---|
| `6fa431a6` | feat(diag): 诊断包数据目录年龄标注 + 卸载「彻底删除」日志丢失警示（5GZHWT 冷启动包复盘） | 诊断包标注跑在**用户机器**的后端里（不随包＝下一份冷启动包依旧无标注、值守继续误读）；卸载警示在安装器/壳层，同样只随包生效 | ✓ 在 HEAD 祖先链（c76aa4aa 之后），随本次全量打包 |
| `9b18ce7b` | feat(duty): duty_evidence probe（报障人版本/活跃/错误遥测自动提取） | 值守侧工具，**不随包**——列在此只为公告口径排除，勿写进用户公告 | 不随包 |

## 三、并行线（非本批指令，发版前由各自负责人登记）

- C1-①（#123 族）镜像序号洞探测（`store.numeric_platform_msg_id_holes`）——另一条线在改，是否进本版由该线登记。

## 四、发版动作清单（照 v1.0.70 c76aa4aa 的形态）

1. `desktop/package.json` version → 1.0.71 / displayVersion 1.071；`website` 五值/changelog 同步。
2. `npm run predist:win`（含 shell-i18n 门禁 §⑦、renderer-boot-invariants、package-layout、smoke）。
3. 公告按 §I 三段式：症状语言（「AI 不再贬损自家活动」「手机删了工作台同步删且不再记住」「切英文菜单栏同步」「重启窗假『发送失败』自动清除」），不报工单号；D1 需提醒运营**登记活动**（`config/site_catalog.yaml: camp_promotions`）才生效——没登记＝守卫不知道谁是自家。
4. 更新 `docs/值守症状族对账单.md`（族4 第 6 层媒体半边标 v1.0.71）；D1/D2/E1 各工单 fix_note 走 `duty_reply.py`，回访前按 §G 核 `reporter_version ≥ 1.0.71`。
