# 指令 Q-10：工作台小件（草稿消费 · 主题持久化 · Telegram 登录崩溃 · 帮助语料迁移）

> 预算 **$120**。工单 **#267 #268 #254**（CV4E22 / 8FSXX2 / RU89S6 / N9JQ3M / NVFCZB / 4KEJ63 / 22KVXF④⑤ / 9NWYRW「closed database」；Q-0 拆单后以新单号为准）。目标版本 1.0.79 能带就带，否则 1.0.80。第二波（与 Q-8 并行，不依赖它）。
> 底座 `docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-10 行）。落点表 `docs/发版对账_v1.0.79_Q10.md`。

## 开工前必读

1. `docs/聊天核心能力_三视角方案_2026-09-09.md` §1 Q13–Q15、§3 Q-10。
2. 证据：`tmp_diag\CV4E22\`（采用 L1 草稿「Talk soon, take care!」手动发出后同一草稿仍「已就绪」，日志无二次起草——旧稿未 consumed）；`N9JQ3M`（主题「定时」下点浅色 / 深色无反应；主题在 URL 参数 `theme=dark`，刷新回退，4KEJ63 同族）；`9NWYRW`（`telegram_protocol_login._advance→_migrate→pyrogram sqlite_storage sqlite3.ProgrammingError: Cannot operate on a closed database` 两次栈，13:32 会话）；`22KVXF`（帮助语料自动播种 upsert 295 条每版递增进用户 KB；要求迁悬浮球独立存储 + 「一键清除预置条目」按来源）。
3. 代码事实：人工发送路径 `src/web/routes/unified_inbox_send_routes.py`（**有他人在途 hunk → stage_hunks**）；草稿状态在 `drafts.py`（Q-2 的——你只调用它的「置 consumed」接口；没有就在 `autodraft_helpers.py` 加一个小函数）；工作台主题 JS（`workspace_base.html` / `unified_inbox.html` 内联或 `static/workspace/*.js`——他人在途 hunk，stage_hunks）；`src/integrations/telegram_protocol_login.py` `_advance` / `_migrate`；`src/utils/kb_store.py` 帮助语料播种（P-4 B 做了陪伴域首装为空——**帮助语料是另一条播种**）；悬浮球（小智）独立存储位置 `src/assistant/*`（小智线的文件——只加读取口，不改其逻辑；有在途则写总账交小智线）。
4. `scripts\agent_probe.ps1` 看 ACTIVE。

## 红线

- 只动归属矩阵 Q-10 行；`drafts.py` 不动（Q-2）；小智线文件不改逻辑。
- `git add <显式路径>`；每 commit 带对应工单号。

## A 采用草稿即消费

- 采用（Tab）并发送成功（含修改后发送）→ 草稿 `status=consumed` 并从面板移除；仅新入站产生新稿；「忽略」语义不变。日志 `[draft] consumed draft_id=… by=agent_send`。tests。

## B 主题切换退出定时 + 持久化

- 点浅色 / 深色 → 自动退出「定时」并立即生效（定时按钮显示「已手动接管」，可再点回定时）；主题改为持久化设置（后端偏好接口或 localStorage + 服务端同步），不再依赖 URL 参数，刷新 / 切页 / 顶栏胶囊不重置（4KEJ63 一起关）；前端日志 `theme change source=manual|schedule|system value=…`。

## C Telegram 协议登录 `_migrate` closed DB

- 复现 13:32 会话栈：`_advance` 在 storage 关闭后调用 `_migrate`；修：迁移前检查连接状态 / 重新打开 / 迁移放到 `_advance` 之前；加 test；日志一行。

## D 帮助语料迁悬浮球 + 一键清除预置

- 帮助语料（295 条）不再播进用户 KB：改写进小智独立存储（只加一个读取 / 写入口，逻辑交小智线）；启动不再 upsert 进 `kb_entries`；存量：KB 页「一键清除预置条目」按来源过滤（`vendor` / `system` / `help`），先 dry-run 列清单再删（N-5 误删教训：**不在自检里自动跑**）；日志。

## 收工

- 落点表 `docs/发版对账_v1.0.79_Q10.md`；`docs/Q批_进度总账.md` 只改 Q-10 行；`agent_probe.ps1 -Done`；回访文案交 R79。

## 开新对话粘贴

```
读 docs/指令_Q-10_工作台小件_2026-09-09.md，按它做 Q-10（#267 #268 #254：采用草稿并发送成功即置 consumed 并从面板移除 / 主题点浅色深色自动退出定时并持久化不再依赖 URL 参数 / telegram_protocol_login._migrate 对已关闭 DB 操作的崩溃 / 帮助语料 295 条不再播进用户 KB 改小智独立存储 + KB 页「一键清除预置条目」按来源 dry-run 后删）。预算 $120。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵，再 scripts\agent_probe.ps1 -Intent 登记；unified_inbox_send_routes.py 与工作台模板有他人在途 hunk 用 tools/stage_hunks.py --mine；drafts.py 不动（Q-2），小智线文件只加读取口不改逻辑；清除只 dry-run 不自动跑。每个 commit 带工单号并立刻补落点表 docs/发版对账_v1.0.79_Q10.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-10 行并 agent_probe -Done。
```
