# 发版对账 v1.0.74 · J-4 工作台 UI 小件（2026-09-05）

> 指令：`docs/指令_J-4_工作台UI小件_2026-09-05.md`。预算 $150。七件全部完成 + 替 J-2 / J-3 落地四项 + #188。
> 全部是前端 / 路由 / 静态资产小活，**一件一个 commit**（`git add <明确路径>`；`unified_inbox.html` 同区域夹带的
> 他线未提交 hunk 均用 `tools/stage_hunks.py` 按块剔除，见 §「树上仍有他线未提交」）。
> 模板 / i18n / CSS / copilot 组件 = 热更新即上生产（已双戳 `ui-build.txt` + `?v=`）；`.py` 三处（#179 `persona_routes.py`、
> G `admin.py`/`goal_routes.py`、J-3 交办 `unified_inbox_account_routes.py`）**尚未装进 zhiliao**（末次重启 09-04 19:38），
> 下一重启窗（12:30）装载后再看 §验收「装载后」。

## 落点表

| 项 | 修什么 | 落点 | commit | 状态 |
|---|---|---|---|---|
| A #170 幽灵未读角标 | skuio：账号角标 1 / 3 点开 15 个归档会话仍不消，点角标只切账号 | `unified_inbox.html`：① `_renderCompactBar` 补「归档中还有 N 条未读」pill（原 `#buried-hint` 长在 `.list-header-body`，列表一滚整块折起 → 入口只在展开态可见；紧凑条同判据：私聊范围 / 非归档视图 / 无搜索 / 非历史），点击跳归档视图并展开；② `_renderBuriedHint` 加「取消归档这 N 条」→ `_unarchiveBuried`：清单以 `/api/admin/buried-conversations` 为权威（DB 级，与 HealthWatchdog 被埋告警同判据，覆盖主列表 top-N 窗口外的被埋行）按当前平台/账号裁剪 + 窗口内 `allChats` 已知被埋行并入兜底；写入走 `_archiveBatchPatch(cids,false)`（与单条归档同一有序链），成功 `_undoToast` 可撤，busy 互斥防连点；③ `verify_account_rail_ui.py` 场景 13：非 attn 角标点击出 `/account-unread` 浮层、行数==count、角标数==total、含「全部标已读」、Esc/外点关闭（**同 WHERE 恒等**）。i18n `inbox.buried.unarch*` zh+en；CSS `.bh-unarch`；双戳 ui-build `20260905-0700` / css `?v=20260905a` | `09bd0294` | 在树（热更新已生效） |
| A 核对结论（不改代码） | 指令 §2 修法 2 / 4 | **归档视图打开会话同样走 mark-read**：`_clearUnread` 无 archived 早退，后端 `mark_conversation_read` 不看 `archived`；**新入站自动浮回默认视图已在** `store._unarchive_on_inbound`。⇒ 88MP86 的 7 条 / 64h 被埋＝**人工归档后再无入站**的状态，不是代码缺陷；角标点开无导向＝#159 浮层只绑一形态 → A ① ③ 收口。**#170 无后端项** | — | 结论 |
| B #173 批量挂链哑按钮 | 钧图 1185：`workflows.html` 批量挂链点了没反应 | 根因：Electron 渲染进程 `window.prompt()` 直接抛 "is and will not be supported"（`typeof prompt` 仍是 function，调用即炸；alert/confirm 正常）。新增 `shared/copilot/ui-prompt.js`（`window.uiPrompt(title,def,opts)` → Promise；Enter 确认 / Esc、背板取消 / 单弹层 / zh-en 缺省 / 只用主题 token），三宿主接入（`_win_unique.html` base+workspace 两壳、copilot `app.html`）；替换裸 prompt 三处：`workflows.html WF.bulkStart`、`cp-chain-exec.js` 成交金额、`cp-rel-stage.js` 降阶原因（弹层缺席如实降级，绝不回落原生 prompt）；**批量挂链按钮 tooltip**（`wf_bulk_btn_tip` zh/en，＝J-2 交办项）；静态门禁 `tests/test_no_bare_prompt.py`（禁新增裸 `prompt(`，存量 ratchet，钉导出+双树镜像+宿主引入）。双戳 cp-rel-stage/cp-chain-exec `?v=20260905a`、ui-build `20260905-0721`。跟进 `7b56f894`：gate_sweep 点名 ui-prompt 引用未定义 token `--cp-bg-card` → 改 `--cp-surface`，`?v=20260905b` | `3c101f0e` + `7b56f894` | 在树 |
| C #164 媒体红字→黄字 | 钧：发图红字「发送失败」但对方收到、手机端一条（03:12 老板代转） | `_doSendMedia`：`res==null`（TypeError/abort/超时）**一律**黄字 `inbox.media.send_fail_net_restart`「结果未知」，去掉「后端稳则红字」分叉（`_mediaBackendUnstable` 判稳误导重发＝客户收两条）；catch 落 `console.warn('[send-media] no-response url/ms/reason/size/cmid')`（壳 console-message → renderer 日志，值守可拿包）；`sendVoiceReply` 同口径 `[send-voice] no-response`；`cp-voice.js sendVoice`：reqFail / `{status:0,code:'network'}` → 黄字 `cp.voice.result_unknown` + 派发 `cp-voice-result-unknown` → 宿主 `loadThread` 让坐席亲眼确认 + 埋点 `cpv_result_unknown`；后端明确 `ok:false` 仍红字。i18n cp-i18n zh/en + zh_hant ext。双戳 cp-i18n / cp-i18n-ext / cp-voice `?v=20260905b`（unified_inbox + app.html 双宿主 + personas.html），ui-build `20260905-0734` | `345241a6` | 在树 |
| D #179 「default —」幽灵行 | 钧图 1186：人设「应用到 → 账号默认」TG 多一行 `default —` | `persona_routes.py` `/api/personas/status` TG 合并段两道模块级纯函数过滤：`registry_row_is_placeholder(row)`（account_id 为 default/空恒占位；无 label 无 self_name 且从未上线的 pending 空壳占位；真号哪怕离线/无 label 只要曾上线或有昵称一律保留）+ `config_default_account_is_live(tg_cfg,cfg)`（config default 槽只在协议客户端真会拿它登录时才列——与 `bootstrap.services` 起客户端闸门同源：非 desktop_mode + 真凭证；桌面包/占位凭证不列；117 旧形态保留）。#78 二轮判据原样保留。门禁 `tests/test_persona_account_enum_179.py`（10 例）。**纯 .py，无前端改动不 bump** | `17230974` | 在树，**待装载** |
| E #178 「双向称呼」说明条 | skuio 图 1192 + CJRYZ9：档案 tab 一段只指路的说明 | `personas.html` 删 `ps-row` 块（#155 迁移遗留）；`i18n_packs/persona_studio.py` 两键随删；`tests/test_contact_names.py` #155 门禁反转（两键不得再被引用）；存量值 `call_peer`/`peer_calls_you` 随保存原样回传（JS 不动）。ui-build `20260905-0753` | `ed1b746e` | 在树（热更新已生效） |
| F #174 WhatsApp 电话掩码 | skuio 图 1189：顶栏全号 / 右栏 `171****61` 两口径，坐席要全号无处取；**老板决策 D2＝点击揭示** | `loadInfoSidebar` 电话行：默认 `_maskPhone`，值本体 role=button/tabindex 可点可回车，`data-full/masked/shown` 三态；`_togglePhoneReveal`（只切 textContent；换会话整卡重建自然回掩码，揭示态不跨会话残留）挂 window；旁挂 ⧉ 复制钮走既有 `_copyIdent` 复制全号；埋点 `info_phone_reveal/hide`。CSS `.iv-phone/.iv-phone-shown/.iv-copy-btn`；i18n `inbox.info.phone_reveal_t/hide_t/copy_t`。双戳 css `?v=20260905b` + ui-build `20260905-0758`。刻意不动顶栏会话名（那是会话标题，不在 D2） | `38bfda40` | 在树（热更新已生效） |
| G 桌面包隐藏告警 nag | 坐席机常驻「告警通道未接通 / 达成推送未接通」——只有内部运维处置得了；**老板决策 D3** | 形态判定单源 `ui_visibility.is_client_flavor`（`AITR_DESKTOP_MODE` / `app.desktop_mode` / `flavor=client`）：`admin.py` 模板上下文注入 `ui_client_flavor`（异常回落 False=不隐藏）；`_alertlink_connect.html` client 形态 `bannerMode=false`（胶囊/每日卡片/5min 轮询全不启动，弹窗本体保留给 ops 页 CTA；admin 角色闸不变）；`/api/goals/notify-status` 新增 `nag_suppressed`（client 形态 或 agent/viewer 角色——放接口而非模板属性：cp-goal 也跑在桌面壳静态 `copilot/app.html` 无 Jinja）；`cp-goal.js _notifyRowInner` `nag_suppressed` 时 scan_off / push_off 两条 warn 返回空串，push_covered 绿字照常，旧后端缺字段=旧行为。双树镜像；cp-goal `?v=20260905c` 三处；ui-build `20260905-0820`。门禁 `tests/test_nag_desktop_suppress_j4g.py` | `9f045de9` | 在树；`admin.py`/`goal_routes.py` **待装载**（模板/组件侧已热更） |
| **替 J-2 落地** 链卡三态 | #168：`enrich_execution` 已给 `card_state`/`card_label`，卡上只有一个「已暂停/失败」徽标看不出所以然 | `cp-chain-exec.js`：`yielded`（让路暂停：客户几点回话、静默多久后续跑）/ `failed`（失败原因）补一行 `cardExplain`；`running` 与既有 meta+timing 同义不重复；旧后端缺字段=空串=旧行为。`?v=20260905b` 双树同步（unified_inbox + app.html） | `3c1213e6` | 在树 |
| **替 J-2 落地** 批量挂链 tooltip | 图 1185 | 已并入 B（`wf_bulk_btn_tip`） | `3c101f0e` | 在树 |
| **替 J-2 落地** cp-goal `?v=` | A2 `e4a62ee5` 改了 cp-goal.js | **D1b 线已自己 bump**（`abe96ba3` → `20260905a`）；G 又 bump 到 `20260905c`，覆盖 | `abe96ba3`（他线）/ `9f045de9` | 已覆盖 |
| **替 J-3 落地** 媒体按钮灰态 tooltip | #180：`send-caps` 多回 `caps_reason/worker_state/backoff_sec`，前端只有通用灰字 | `_capsDenyTitle` 三分因：`worker_not_running`（带 `{state}` + 「约 N 秒后重试」）/ `no_worker` / `no_send_media`（voice 变体 `no_send_voice`）；旧后端缺字段走通用文案。i18n `inbox.caps.no_worker / no_send_media / no_send_voice / worker_not_running / backoff_in` zh+en | `c9759aea` | 在树 |
| **替 J-3 落地** fetch-media 重试路由 | #172：重试路由把 fake 空壳 `{id,contentType,contentMetadata:{}}` 传 `download_line_media` → Letter Sealing 媒体永远拉不到，404 时 `age=-1` 分不清过期/未到 | `unified_inbox_account_routes.py`：新 `_line_find_recent_message(client,chat_key,pmid)` 在 worker 最近消息（100 条）找 `id==pmid` 真行（含 `contentMetadata`/chunks/`createdTime`），找到原样传；找不到回落 fake 并补 `createdTime=ts*1000`；`download_line_media(..., out=dl_out)` → 响应 `{ok:false, reason:'download_failed', miss_reason, expired, retryable, real_message}`（旧 `reason` 保留兜底）。前端重试按钮 title 按 `miss_reason` 分因（`expired`/`not_found`/`pending`/`encoding`/`e2ee_no_key`/`e2ee_decrypt_failed`，其余 `miss_other:{reason}`），`expired` 直接改按钮文案「已过期，请客户重发」。`tests/test_media_fetch_backfill.py` 对齐新契约（mock 收 `out=`、断 `createdTime` 为数字串）。ui-build `20260905-0835` | `500d9b4d` | 在树；路由 `.py` **待装载** |
| #188 手动发送气泡短暂双显 | 钧：手动发一条，工作台短暂两条、手机端一条 | 显示层竞态：发送 POST 在途期间恰有一轮静默轮询 → 服务端已镜像出站行，但旧判据要 `text===` 且 `|Δts|<10s` 才认回执（服务端翻译/分条/口语层/引用前缀改了文本，或坐席机时钟偏差）→ 真气泡 + 「发送中」乐观气泡同屏。修：① 文本比对归一化空白 + 窗放宽 60s；② 有在途 POST（`_retryBody`）的乐观稿，只要本轮**新冒出**（上一快照 `_prevIds` 没有）的出站行落在 60s 窗内即视为回执 → 标 `_shadowed`（`renderMessages` 跳过，对象仍留，失败路径翻 `_failed` 后照样可见可重发）；埋点 `send_opt_shadowed`。ui-build `20260905-0840` | `3fee4790` | 在树（热更新已生效） |

## 树上仍有他线未提交（J-4 刻意不收）

`git status` 当前 `unified_inbox.html` / `unified-inbox.css` / `inbox_workspace.py` 三文件的剩余 dirty hunk 全是 **I-2 线 09-04 的
#156 黄点绑人设（`_openAcctPersonaBind`）/ #159 未读角标浮层（`_acUnreadBadge` + `_openAcctUnreadPop` + `.ac-unread-pop` + `inbox.acct.unread_*`）
/ #155 称呼输入（`.info-addr-in`）/ 09-04 悬浮撕窗清单下线 CSS**——1.0.73 对账表标「本线」但至今未 commit。
⚠ J-4 A 的场景 13 断言（角标点击出浮层）**依赖这批未提交的 `_openAcctUnreadPop`**；若那条线的工作树被回退，1.0.73 已发的
#159 与 J-4 A 的浮层验收一起失效。**值守请催 I-2 线显式列文件提交**（不要由别的线代提——那是他的落点表）。

## 验收

- 门禁（本树 06:5x–08:4x 各 commit 前分别跑）：
  - A：`tools/verify_account_rail_ui.py` 13/13（新场景 13 恒等断言）；`verify_inbox_density.py` 17/17
  - B：`tests/test_no_bare_prompt.py` + `test_copilot_shared_sync` + `test_copilot_theme_tokens` + `test_copilot_js_parses` 全绿
  - C：`test_copilot_shared_sync / test_copilot_js_parses / test_cp_voice_ui_revamp / test_goal_ui_revamp / test_inbox_inline_handlers_exported / test_assistant_i18n_keys / test_ui_build_freshness` 全绿；`tools/verify_cp_voice_ui.py` 76/76
  - D：`tests/test_persona_account_enum_179.py` 10 passed
  - E：`tests/test_contact_names.py` 全绿（门禁已反转）
  - F：哑按钮 / 重复 id / 孤儿引用 / i18n 键 / free-capture 门禁 343 passed；`verify_inbox_identity.py` 33/33；真浏览器：掩码 → 点开全号 → 再点回掩码；⧉ 得全号
  - G：`tests/test_nag_desktop_suppress_j4g.py` 全绿
  - J-3 交办：`tests/test_media_fetch_backfill.py` 全绿；i18n packs 双语/无冲突 + `test_template_window_t_keys_resolve` 全绿
  - `gate_sweep.ps1`：J-4 引入的红只有一条（ui-prompt token）已在 `7b56f894` 修掉
- **装载后**（12:30 重启窗）：
  - #179：桌面包 / 117 打 `/api/personas/status`，TG `accounts` 不再出现 `default —`；117 旧形态若仍走 config default 登录则保留一行（判据同源）
  - G：坐席机（`AITR_DESKTOP_MODE=1`）`/api/goals/notify-status.nag_suppressed=true`，工作台无告警胶囊、右栏目标卡无「未接通」黄字；117 管理员照旧可见
  - J-3 fetch-media：LINE 会话点「拉取失败，点击重试」→ 响应带 `miss_reason`，悬停能读到分因；Letter Sealing 视频（#172 真机）走真消息路径
- **1.0.74 装机后真机**（skuio / 钧）：
  - #170：账号角标数字可点 → 浮层列表条数 == 角标数 → 逐条点开或「全部标已读」→ 角标归零；归档视图头部「归档中还有 N 条未读」常驻 + 「取消归档这 N 条」
  - #173：批量挂链弹出页内输入层（不再静默）
  - #164：断响应只见黄字「结果未知」+ 消息流自刷；红字只在后端明确失败
  - #174：右栏电话默认掩码，点开全号，⧉ 复制
  - #188：手动发送不再短暂双显（若仍现，看 `send_opt_shadowed` 埋点是否为 0——为 0 即另有一因）

## 刻意不做 / 交出去

- #170「取消归档」端点：已有 `_archiveBatchPatch(cids,false)` 批量链，**无需新端点**；「归档会话新入站自动浮回」后端已存在，不属缺陷
- `cp-goal.js ?v=`：D1b 线已 bump，J-4 G 再 bump 覆盖；D1b 收工时若再改 cp-goal 需自己再 bump
- `config.example.yaml` 两处文档键（J-1 A / J-2 B）仍等 D1b 收工后由值守补（该文件他在改）
- ops 卡「已发假声明」行（J-1 A 提到「看板属 J-4/值守」）：**未做**——J-4 指令未列，交值守 backlog
- I-2 线三文件未提交 hunk：见上节，交值守催提交
