# 发版对账 v1.0.74 — J-2（目标 / 工作链 / 自动化档位：#166 #168 #167）

> 指令：`docs/指令_J-2_目标与工作链引擎_2026-09-05.md`。总账：`docs/J批_进度总账.md` J-2 行。
> **本条不发版、不重启实例。** `.py` 改动攒批等下个重启窗装载；桌面包改动随 1.0.74。
> 每完成一项立刻追加一行；未完成项也留一行写「进行中：已改 X，还差 Y，下一步 Z」。
> 接手：先读本表逐行核对，再跑 `git -C D:\boundless log --oneline -15 -- engines/chengjie` 确认 commit 真在。

## 落点表

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注/剩余 |
|---|---|---|---|---|---|
| A1 注入口可见性 + 键一致性 | #166 | `d0aa6560` | `src/companion/goals/service.py`（`_note_meta`：有会话 id 的 `no_goal` 升 INFO 带 conv/plat/chat_key/acct/chain，按会话 10 分钟节流；外层 except DEBUG→WARNING 带异常类名+键，同会话 10 分钟只带一次堆栈；新增 `resolve_inject_lookup_keys` 在入口归一查找键：conv↔三元组互补、`''/'default'` 账号以 conv 内账号为准、显式真实账号优先——**账号锁不放宽**）；`store.find_active_goal` except DEBUG→WARNING 带异常类名+键；`tests/test_goal_inject_keys_166.py`（24 例）；`tests/test_goal_inject_visibility_152.py`（#152 ratchet 改口径） | 新文件 24 passed；`test_goal_inject_visibility_152` + `test_goal_wiring` + `test_goal_account_scoping` + `test_goal_store` 共 113 passed | **静态核对结论**：拟稿链（`autodraft_helpers.enrich_auto_draft` → `persona_reply` → `generate_inbox_draft` L3314 → `_inject_goal_context` → `build_block_for_chat`）与右栏卡（`/api/goals/for-conversation` → `_split_conversation_id`）对 BABY BEAR 会话传入的四键**逐字一致**（conv=`whatsapp:17345893506:13308422244`、acct=`17345893506`），store 是进程单例同一库；88MP86 里 INFO 级 0 DEBUG 级，`no_goal`/读库异常/注入链异常三条路径全 DEBUG ⇒ 日志**无法**判定走了哪条。本项即把三条路径全部升到 INFO/WARNING；修后同会话下一条入站必出 `[goal-inject] injected` / `NOT injected reason=…` / `ERROR <类名>` 三者之一（88MP86 复盘口径）。`skill_manager._inject_goal_context`（J-1 归属）零改动。service.py 里另有他线未提交的 `catalog_persona_id`（#145③）三个 hunk，已用 `tools/stage_hunks.py` 按块排除，未装进本 commit |
| C 首登不直接全自动（#63/#156/#167 同结论） | #167 | `45261c78` | `account_mode_onboarding`：`FIRST_LOGIN_GRACE_SEC=7d`；`is_new_account` 把「接入时刻未知」和「登录早于 baseline 宽限内」改成待确认（修首登 upsert 早于首次入站冻结 baseline → 被当成存量 → bootstrap 写成 auto_ai）；`persona_is_user_explicit` / `cap_unconfirmed_default_persona`（未确认 + 默认/未选人设 → review）。`automation_mode.resolve` 叠最后一道封顶；`maybe_bootstrap`：**onboarding 开且未确认不落盘**（bootstrap 只对已确认账号生效）。存量未确认 + 用户显式人设仍回落全局 auto_ai（不打扰已在跑的号）。`tests/test_onboarding_bootstrap_167.py` 矩阵 8 格 + 竞态/未知/存量两档 | `test_onboarding_bootstrap_167` + `test_account_mode_onboarding` + `test_automation_mode` + `test_automation_mode_downgrade` + `test_persona_unselected` **50 passed** | **#63 / #156 / #167 同一结论**：接管方式和以谁的身份接管都必须是人的显式决定。#63＝新号未确认 review；#156＝未选人设封顶 review；#167＝补上「首登竞态 + 未知 created_at + 未确认默认人设」三条漏网，bootstrap 不能再把未确认号写成全自动。值守回访 #63/#156 时带上本单 |
| B SOP 解释层 + 重试原因 + max_per_tick | #168 | `983e05ba` | `workflow_runner`：`format_step_fail_reason`；步骤重试/失败/单条异常升 WARNING 带原因；`last_error` 写入已有 `last_result_json` + `context_json`（**未改 store.py**）；`_execute_step` 异常带类名；`auto_start_chains(..., max_per_tick=0)` 0=不限（旧测兼容），逐条 INFO `reason=silence_days=…|churn_risk_high|stage_enter=…`；静默窗/互斥顺延也 WARNING。`workflow_autorun.resolve_auto_start_cfg`：`max_per_day` 默认 30、`max_per_tick` 默认 **5**，tick 透传。`workflow_monitor`：`explain_execution_card` 三态（`card_state`/`card_label`）——运行中 · 下一步 X 后（步名）/ 已让路暂停（客户 HH:MM 回话，静默 N 分钟后续跑；无倒计时→待坐席恢复后续跑）/ 失败（原因）。`workflow_auto_step` SOP 拟稿钉子「以当前人设第一人称、朋友口吻推进，不出现助理/团队/开户/引导页/几分钟搞定」；拟稿失败 WARNING `no_inbound`/`empty_draft`。`tests/test_workflow_explain_168.py` 新。`config.example.yaml` 的 `auto_start.max_per_tick: 5` **未装进本 commit**（同文件混有他线 `outbound.unlimited_mode` hunk，按 hunk 切失败，代码默认已是 5） | `test_workflow_explain_168` + `test_workflow_autorun` + `test_workflow_auto_step` + `test_workflow_step_log` + `test_workflow_deal_engine` + `test_relationship_workflow` + `test_workflow_reply_yield` + `test_workflow_exec_ops` **114 passed** | **交 J-4**：链卡消费 `card_state`/`card_label`（`enrich_execution` → `/api/workspace/chain-executions` 与 conv 级同口）；若消费点在 `unified_inbox.html` 内联或 `cp-chain-exec.js` → J-4。图 1185「批量挂链」tooltip → J-4（#173） |
| A2 引擎真相 + sprint 出厂开 + UI 不兜售 | #166 | `e4a62ee5` | `service.sprint_engine_status`（单一出口：goals/sprint 开关 + **派发终点 care enabled/dry_run** + `sprint.platforms` 白名单；自然档 bridge + proactive_topic enabled/dry_run；`sprint_blockers`/`natural_auto_blockers` 逐闸点名）；`goal_routes`：新 `GET /api/goals/engine-status`（goals 关也 200）、`/templates` caps 加 `sprint_effective/sprint_blockers/sprint_platforms/natural_auto_*`（旧三键保留）、`for-conversation` 带 `engine` + `beats{used,cap,progress_kind,time_pct}`、`sprint_live.ticker_on` 改为整条链有效（care dry_run → False，`blockers` 点名）；`cp-goal.js` 双树：auto 卡引擎不能出手时灰掉+原因、推荐徽标让给 suggest、缺省不落灰卡；目标卡 auto 标签打叉+卡身原因行；进度口径「时间 89% · 动作 N/M 拍」；sprintLine 点名阻塞点；i18n 17 键 zh/en（`i18n_packs/goals.py`）；`config/profiles/cloud_light.yaml` + `config/config.desktop.internal.yaml` 加 `companion.goals.sprint.enabled: true`（D1；internal 种子经 `_merge_missing` 补齐存量内测机）；`background_tasks.py` 启动日志：sprint 关 → WARNING「未启用」，开但被拦 → WARNING「派发被拦（原因）」，全通才 ✅ | `test_goal_engine_status_166` 16 passed；`tests/test_goal_*.py` 全部 + 路由清单 + copilot 双树/i18n + zh_hant 覆盖 + deploy_profiles **948 passed / 0 failed**（含修掉 I-3 记录的预存红 `test_proactive_goal_opener_guarded`：断言字面跟 08-27 的 proactive_topic 源码；以及 `test_goal_sprint_p2::test_nudge_schedules_immediate_phase` 的 NOW 时间炸弹） | **⚠ 老板决策 D1 只翻 `sprint.enabled` 在两种出厂形态上都不足以让「自动推进」真出手**（代码事实，engine-status 会如实报）：① 冲刺拍的派发终点是 care 派发器——桌面 internal 种子 `proactive_care.dry_run: true`（skuio 机同）→ 主动拍只拟稿不真发（`care_dispatcher.run_once` L597/L714）；cloud_light/example 更是 `proactive_care.enabled: false` → 派发器空转；② `sprint.platforms` 出厂 `[telegram]`，skuio 的 BABY BEAR 是 WhatsApp → 白名单拦；③ 3 天自然档目标根本不走 sprint（走 `goals.bridge`，出厂关）。**需老板追加决策 D1b**：冲刺行是否豁免 care 的 enabled/dry_run 闸（改 `care_dispatcher` + `background_tasks._goal_row_policy`，属 care 线文件，本条未动）+ `sprint.platforms` 是否加 whatsapp/line + 自然档 auto 是否随 bridge 出厂开。未拍板前 UI/日志已如实标注，不再兜售。**交 J-4**：`cp-goal.js` 已改 → `unified_inbox.html` 与 `shared/copilot/app.html` 两处 `?v=` bump + `ui-build.txt`。zh_hant 覆盖：17 个新键未 regen（`python -m scripts.i18n_hant generate` 会整文件重生成并卷入他线未提交键，刻意不跑；覆盖率门禁 ≥99% 仍绿，新键繁中回落简体） |

## 交 J-4

- `cp-goal.js` 已改（A2 `e4a62ee5`）→ `unified_inbox.html` + `shared/copilot/app.html` 两处 `?v=` bump + `ui-build.txt`
- 链卡消费 `card_state` / `card_label`（B `983e05ba`，数据已在 `enrich_execution`）；消费点若在 `unified_inbox.html` 内联或 `cp-chain-exec.js` → J-4
- 图 1185「批量挂链」tooltip → J-4（#173）

## 顺手（不修）

88MP86 BABY BEAR `guard=fresh post_humanize 延迟窗内客户插话` 09-04 20:39–22:25 **30+ 次**（高频语音）。`inbound_debounce` 默认窗 8s / 硬上限 25s，碎片自适应只认 ≤2 字文本；语音条是完整片段、不进碎片模式，连发语音各自触发拟稿再被 fresh-guard 放弃＝白烧 LLM。建议：语音入站用更长合并窗（15–20s）或按 `content_type=voice` 单独 `window_sec`，本条未动。

## 老板决策未拍板

- **D1b**（A2）：只翻 `sprint.enabled` 不够让自动推进真出手——care `enabled`/`dry_run` + `sprint.platforms` 出厂 `[telegram]` + 自然档走 `goals.bridge`（出厂关）
