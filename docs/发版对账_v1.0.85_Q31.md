# 发版对账 v1.0.85 — Q-31 筛选面板系统标签筹码：处理完自动消 / 可关闭 / 单人端默认折叠（#317 · BS7E5Q）

> 指令：`docs/指令_Q-31_头部瘦身与状态带唯一出口与列表降噪_2026-09-12.md`（20:5x 缩为 #317 单项；本轮按老板粘贴块只做 **A / B / C** 三段，**D 段（PUUWJB 顶栏「发前确认」计数）未在本轮范围**，留待下一条指令）。前置（已读、在其上加、不重做）：Q-17 冷却摘标链（`risk:medium` 处理完会消，不动）、P-2 `dormant_review.py`（沉寂清单 / `dormant:ignored` 只加不摘）、O-1 A `stop_contact.py`（冻结 / 告别「最多一条」守卫）、Q-26 状态带。总账：`docs/Q批_进度总账.md` Q-31 行。预算 $60。
> 红线：只改显示分组与生命周期，**不改** `risk_grader` / Q-17 摘标冷却 / 停联告别守卫的判定；只用现有令牌，不新增颜色；`unified_inbox.html` 共享文件走 `stage_hunks.py`（本线只暂存自己的 hunk）。
> 执行：2026-09-12 23:1x 开工（`agent_probe -Intent "q-31-panel-sys-tags-317 (…)"` 已登记）。**每个 commit 落地后立刻补一行。**

## §〇 BS7E5Q 为什么「处理完点确定后筹码仍在」（代码只读核对，HEAD `0b606199`）

| # | 现场 | 代码事实 |
|---|---|---|
| 1 | 筛选面板「dormant:ignored 6 / risk:medium 3 / 停联 1」与用户标签同列 | `_renderPanelTags()`（`unified_inbox.html` L5652）取 `_tagStatsCache` 在用标签 Top8 带计数，只剔「需人工」；系统写入的 `dormant:*` / `risk:*` / 「客户要求停联」/「人工接管中」与用户手打标签不分家。点击＝`setTagFilter`（筛选），没有「关闭」 |
| 2 | 处理完筹码不消 | chip 是**标签统计**：标不摘 chip 不消。`dormant:ignored` 全仓只有 `dormant_review.apply_action("ignore")` 一处**加**（L620），没有任何摘除点，会话后来又有新入站也不摘；`note_real_inbound` 已存在但**零调用点**（只删清单 pending 行，不动标签） |
| 3 | 停联「确认」后标仍在 | 会话头「我知道了（摘标）」＝`PUT /tags` → `clear_needs_human(actor=agent_ack)` 只摘「需人工」；「客户要求停联」标保留（停联是事实，`frozen_reason` 靶它判冻结）→ 它继续占筛选筹码位 |
| 4 | 单人端也看见多坐席才需要的系统噪音 | 面板没有坐席模式差异；`MULTI_SEAT`（`ws_multi_seat` → `resolve_seat_mode`）只管认领 UI |

## 一、改动落点（每 commit 一行，落地即写；均带 #317）

| 段 | commit | 落点 |
|---|---|---|
| **A 系统标签分组 + × / Esc 清筛选** | `ba18d8ce` | `src/web/templates/unified_inbox.html`（只暂存本线 2 hunk：L397 `unified-inbox.css?v=20260912q31` + L5652–5725）：新 `_isSysTag(t)`＝`_tagKind` 命中 `tko` / `attn` 精确态 **或** `^(dormant|risk):` **或** `_STOP_CONTACT_TAG`（`\u5ba2\u6237\u8981\u6c42\u505c\u8054`＝`stop_contact.STOP_CONTACT_TAG` 数据值）——**不动 `_TK_RE`**（列表 chip 配色零变化）；`_renderPanelTags()` 分两列：用户标签照旧 **Top8**，系统族归第二行 `<button class="fp-sys-hd" data-sys-toggle aria-expanded>` 「系统标签 ▸/▾ N」+ `<div class="fp-sys-body" hidden?>`；折叠态 `_fpSysOpen()`＝localStorage `ws_fp_sys_open`（坐席手动开合记住）→ 没记过按 **`!MULTI_SEAT` 收起 / 多坐席展开**；正按某个系统标签筛时强制展开（激活 chip 不许被藏）；**× 只长在激活 chip 尾部**（`<span class="fp-x" data-ptag-x>`，title `inbox.filter.chip_clear`）＝`setTagFilter('')`——未激活 chip 没有可清的筛选，×＝死按钮故不渲染；容器级事件委托新增 `[data-ptag-x]` / `[data-sys-toggle]` 两支（无内联 onclick，哑按钮门禁零新增暴露）；`document` keydown：**筛选面板开着时 Esc**＝有标签筛选则清（`iflt_ptag_esc`），否则关面板。埋点 `iflt_ptag_x` / `iflt_systags_open|close`。`src/web/static/workspace/unified-inbox.css`（+13）：`.fp-sys-hd` / `.fp-sys-arr` / `.fp-sys-body[hidden]` / `.fp-chip .fp-x`（只用 `--border` / `--text-tiny` / `--text-sub` / `currentColor`，**零新颜色**；`.t-cnt` 选择器补 `.fp-sys-hd .t-cnt`）；`ui-build.txt` → `20260912-2326`（单行戳文件，覆盖他线未提交的 `-2304`，同语义）。`src/web/i18n_packs/inbox_workspace.py` zh+en：`inbox.filter.sec_sys_tags` / `sec_sys_tags_t` / `chip_clear`，**B 段确认框 6 键同批预落**（`inbox.handoff.bar_ack_stop(_t)` / `stop_confirm_title|msg|ok|done|fail`——模板热更新直上生产，键必须先于模板落地）。门禁：`test_sealed_pages_no_cjk_in_own_scripts[unified_inbox]` / `test_template_window_t_keys_resolve[unified_inbox]` / `test_workspace_i18n_keys_translated_in_both_langs` / `test_template_unique_ids` / `test_css_comment_trap` / `test_ui_build_freshness` / `test_i18n_zh_hant` 绿；node 抽函数真跑：单坐席 `aria-expanded=false`+body hidden / 多坐席展开 / `tagFilter=risk:medium` 强制展开且只有该 chip 带 × / 用户标签 Top8 不受影响。**既有红（非本线）**：`test_layer2_inline_handlers_globally_reachable` 点名 `loadConnectModes`（接入方式失败重试按钮，他线工作树未提交 hunk）；`test_legacy_blue_ratchet` css 3>0 / html 24>15 与 `test_css_token_refs` 多页缺定义——HEAD 同红，与本批无关。 |
| **B 生命周期** | `e759c8c4` | ① **`dormant:ignored` 新入站自动摘（只写一处）**：`src/inbox/dormant_review.note_real_inbound(store, cid, inbound_ts=)` 从「零调用点的清单撤下」扩成唯一写点——清单 pending 行撤下 + 摘 `IGNORED_TAG`；**只摘忽略决定之后到达的入站**（`inbound_ts > dormant_review.decided_ts`，与 `_unarchive_on_inbound` 的 `archived_at` 判据同一纪律，历史重放 / thread 回灌不摘；判不出 ts 按「现在」），日志 `[dormant] action=unignored conv= inbound_ts= decided_ts=`；沉睡复盘再判沉睡 → `upsert_pending` 复活 → 坐席再忽略会重新打，语义不变。唯一挂点 `src/integrations/protocol_bridge.ingest_incoming`（全平台入站汇流处）：与 `note_backfill` 互为正反向——`direction=="in" and not _quiet and _n>0` 才调（回填 / 自聊走原路不摘）。`apply_action("ignore")` 加标处不动。② **停联确认 = 归档 + 标签移入会话档案**：`src/inbox/store.py`（只暂存本线 2 hunk）迁移 `conversation_meta.stop_contact_at REAL DEFAULT 0` + `set_stop_contact_at(cid, ts)` / `get_stop_contact_at(cid)`（写口只有 stop_contact 两处）；`src/inbox/stop_contact.py` 新 `stop_contact_at(store, cid)`（旧 store / MagicMock → 0.0）+ **`frozen_reason` 改为「标签在场 或 `stop_contact_at > 0`」→ `stop_contact`**（守卫不弱化：`autosend_policy` / `drafts` 告别「最多一条」/ `split_targets` frozen 栏 / goals / adult_grader 全部经它）；新 **`confirm_stop_contact(store, cid, actor=, now=)`**：`clear_needs_human(actor)`（元数据 / risk_hold 同清，与「我知道了」同口）→ 先写 meta 再摘「客户要求停联」（两步之间任何读者看到的仍是冻结；已有值不覆盖＝首次确认时刻是历史事实）→ `set_conv_archived(source="stop_contact_confirm")`；旧 store 无列 → **标签保留不摘**（否则＝解冻）、只归档摘需人工、`ok=False error=no_stop_contact_meta`；档位 / 账号级名单 / 告别守卫全不动；日志 `[stop-contact] conv= action=confirmed … still_frozen=stop_contact`。`unfreeze_conversation` 加 `meta_cleared`（人工解冻是唯一清零口）。客户日后再来消息 → `_unarchive_on_inbound` 自动回列表，冻结不变、AI 仍沉默。③ **确认框写清后果**：`src/web/routes/unified_inbox_workspace_tags_routes.py` 新 `POST /api/workspace/conv/{cid}/stop-contact/confirm`（actor `agent_ack:<user>`，广播 conv_tagged / conv_archived，回 `{ok, tags, archived, stop_contact_at, frozen}`）+ `tests/test_admin_route_inventory.py` 基线加一行（只暂存本线 1 hunk）；`unified_inbox.html`（只暂存本线 3 hunk）：`_convIsStopContact(c)`（标签在场或 `handoff_meta.reason==stop_contact`）；`_paintAcctBanner` 需人工条的「我知道了」在停联会话改读 `inbox.handoff.bar_ack_stop(_t)`「已处理 · 归档并移除」；**只剩停联标（需人工早摘）的冻结会话此前点进去零出口**——新增同款横幅 `inbox.handoff.stop_bar` + 同一枚按钮；`_handoffBarAck` 分流 → `_handoffBarAckStop`：`_appConfirm` 标题「确认已处理「客户要求停联」」正文「**归档并从筛选移除 · 不再自动回复**。停联事实会从标签移入会话档案（历史可查）…客户日后再来消息会自动回到列表」→ POST → 本地两标清 + `archived=true` + `applyFilters/loadChats`；埋点 `handoff_bar_ack_stop`。`ui-build.txt` → `20260913-0002`。回归：`test_stop_contact_hard_stop + test_dormant_review_p2` 80 / `test_needs_human_autoclear_207 + test_risk_hold_q3` 36 / `test_admin_route_inventory` 4 / `test_protocol_bridge + test_backfill_no_automation_p2 + test_unarchive_on_inbound + test_drafts_risk_policy + test_risk_grader_gate + test_ingest_identity + test_tg_history_sync + test_outbound_sse_event + test_goal_default_stage_q8 + test_messenger_stranger_policy` 253 全绿；模板 `test_workspace_template_js_syntax` / CJK / window.T / unique_ids 绿（`test_template_orphan_refs` 红为 `reply_settings.html: rps-po-empty`，Q-26 线工作树未提交件）。**`.py` 须 zhiliao 重启才生效，本线未重启**（23:1x 探针显示 15.7min 前刚重启，攒下一窗）。 |
| **C 门禁** | `b89a2f28` | 新 **`tests/test_panel_tags_q31.py`**（9 例，指令要求四例全在）：① `test_single_seat_folds_system_tags_user_tags_top8_untouched`——node 抽 `_TK_RE / _STOP_CONTACT_TAG / _tagKind / _sysTagLabel / _isSysTag / _fpSysOpen / _fpSysToggle / _renderPanelTags` 真跑：`_isSysTag` 真值表（dormant:ignored / risk:medium / risk:high / 停联 / 接管 / 需人工 为真；VIP / `dormant` / `risky` / 成交 为假）、单坐席 `aria-expanded=false` + `fp-sys-body hidden`、用户列恰 8 枚（VIP+t1..t7，t8 截掉）且一枚系统标不漏进去、系统列四枚 + 头上 N=4、「需人工」照旧剔除、多坐席展开、正按 `risk:medium` 筛时单坐席也强制展开且只有该 chip 带 ×；② `test_dormant_ignored_cleared_on_new_real_inbound`——真库：5 天前入站 → 清单 → ignore 打标 → **历史重放（ts<decided）不摘 / 回填不摘** → 客户真开口摘标 + `[dormant] action=unignored` 日志 → 幂等；源码钉：全仓 `note_real_inbound(` 调用方只有 `protocol_bridge.py`，`dormant_review.py` 加标 / 摘标各恰一处，`protocol_bridge.py` 不含 `IGNORED_TAG`（只写一处）；③ `test_stop_contact_confirm_moves_tag_to_meta_guard_still_blocks`——XAM4KV 口径回放：「please stop」→ 告别稿 + 冻结 → `confirm_stop_contact` → 两标不在 `conv_tags`、`stop_contact_at` 落值、`archived=1`、`handoff_meta` 清 → **`frozen_reason` 仍 `stop_contact`、档位仍 manual、第二条「so stop writing to me」不起草、pending 仍只有那一条告别** → 再确认不覆盖首次时刻 → 客户再来消息自动解档但仍冻结、`split_targets` 仍归 frozen 栏 → `unfreeze` 才 `meta_cleared` 并还原 auto_ai；④ `test_chip_x_and_esc_clear_filter_not_tags`——node 抽容器级点击委托 + document keydown 两支处理器当纯函数跑：× → `setTagFilter('')` 且零 `_removeConvTag / _saveConvTags` 调用；折叠头只记 localStorage 不动筛选；普通 chip 原语义；Esc 三态（面板关着不管 / 开着在筛→清 / 开着没筛→关面板）+ Enter 不管；源码钉面板段落不含 `_removeConvTag / _saveConvTags / tag-library / DELETE`。另：`_TK_RE` 不含 dormant / risk: / 停联（配色零变化）+ 11 个新键 zh/en 齐 + 确认框正文含「归档并从筛选移除」「不再自动回复」+ 面板段落零内联 onclick；旧 store 无列 → 标签保留 `ok=False`；`frozen_reason / stop_contact_at` 容忍 MagicMock / None；`POST …/stop-contact/confirm` 在标签路由域注册且 TestClient 真打 → `ok / archived / frozen` 三真、两标清、仍冻结。**Temp 冒烟脚本（`%TEMP%\q31_smoke.py`）已删，仓内无临时文件。** |

## 二、未做 / 边界（如实）

- **D 段（PUUWJB 顶栏「发前确认」计数）本轮未做**——老板粘贴块只列 A / B / C，预算 $60；D 段（`risk-summary` `actionable` 分桶 / `auto_expire_hours` 出厂 48h / 审批台「清空超龄稿」/ Messenger 边车 `backfill:true`）原样留在指令表里等下一条指令。
- `_sysTagLabel` 未扩：英文界面下「客户要求停联」chip 仍显示数据值中文（与列表 / 会话头 chip 现状一致，改它会同时改三处显示，不在本单最小面）。
- × 只渲染在**激活** chip 上：未激活 chip 没有可清的筛选，渲染 × 就是死按钮（dead-click 门禁口径）；折叠态记 localStorage `ws_fp_sys_open`——坐席手动开合后不再按坐席模式缺省（这是「记住我的选择」，与 P1 排序偏好同款）。
- `note_real_inbound` 只挂 `ingest_incoming`：A 线 telegram_client 进程内 sink 也走它（`register_inbound_sink(lambda m: ingest_incoming(store, **m))`），四平台 + 桌面 / leadbus 路由全覆盖；`demo_seeder` 直调 `store.ingest_message` 不经此路（演示数据，不打 dormant 标）。
- 停联「确认」后 `freeze_conversation` 若再被触发（客户又说一次「别再发了」→ 新「需人工」+ 会重新打「客户要求停联」标）→ 坐席再确认一次即可；这是「新一次停联事件」的如实呈现，不是回归。`login_scan_backfill` 两路都先问 `frozen_reason`（读 meta）→ 已确认的会话不会被复冻回标。
- `.py`（store / stop_contact / dormant_review / protocol_bridge / tags 路由）须 zhiliao 重启装载；模板 / CSS / i18n 已热更新直上生产——中间态自洽：新按钮打旧后端会得 404 → 前端 toast「处理失败，请稍后重试」，不会误清标签。
