# 指令 Q-2：现实承诺守卫五类（见面 · 地址电话 · 照片视频 · 礼物 · 金钱）

> 预算 **$200**。工单 **#263 #264**（PHBRBW / CBD4J5 / XNZR8R / 7KXNHB①② / XBGPBN②；Q-0 拆单后以新单号为准）。目标版本 1.0.79（第一波）。决策 **D-Q3 见面政策默认永不**。
> 底座 `docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-2 行、接口约定②④）。落点表 `docs/发版对账_v1.0.79_Q2.md`。

## 开工前必读

1. `docs/聊天核心能力_三视角方案_2026-09-09.md` §1 Q2 / Q4（被质问分支）、§2.1 第 4 条、§3 Q-2。
2. 证据 `tmp_diag\PHBRBW\ GTCQS9\ XBGPBN\ 7KXNHB\`：23:47:35 起草「Saturday noon sounds lovely, I'll make sure to have some fresh tea ready」`risk=low shadow=-`；23:49 客户「Just need your address」才命中 `privacy`；审核稿「I'll text you my address in a bit」；被质问后「Ah, you're right, I completely forgot to send those, my bad. I'll make sure to grab them for you later today, promise」。
3. 代码事实（已核）：`src/inbox/drafts.py` L31 `_SENSITIVE_PATTERNS`（只有 privacy / payment 两类，high / medium 只决定 `would_hold_level`，发送由 `autosend_policy.decide` 单点决定）；P-3 `docs/发版对账_v1.0.79_P3.md`（`outbound_promise_guard` 已成动作型：`media_claim` / lie_caught 动作——**你把它并进五类一套逻辑，不重做照片那支**）；O-1 C `persona_guard` 客服腔守卫「改写一次 → 仍命中转人审」结构；`persona_manager.py` L62–104 人设字段（`speaking.forbidden_phrases` / `boundaries.topics_to_avoid` / `boundaries.escalation_phrases`）；底稿 `domains/conversion/prompts/system_companion.txt`（O-1 C）。
4. 接口：`risk_hold.set(conv, reason, hit)`（Q-3 提供）——未合并前 `hasattr` 兜底；`outbound_humanize` stats 键 `commitment_claim` / `self_blame_repromise`。
5. `scripts\agent_probe.ps1` 看 ACTIVE；`personas.html` 与 Q-6 相册区分段（你只加「见面政策」一格，前缀 `mp-`，stage_hunks）。

## 红线

- 只动归属矩阵 Q-2 行；`autosend_worker.py` / `autosend_policy.py` 不动（是 Q-3 的）。
- 政策默认「永不」：模型**只能从委婉延后话术里选**，禁止接受；被追问仍拒；不解释之前为何没做。
- `git add <显式路径>`；每 commit 带 `#263`。

## A 五类意图 + 风控词表

- 新 `src/inbox/commitment_guard.py`：`detect_commitment(inbound_text, lang) -> kind|None`，五类：`meet`（见面 / 上门 / come over / meet up / see you / 约 / 来找你）、`contact`（地址 / 电话 / your address / phone number / 微信 / line id）、`media`（照片 / 视频 / 语音通话 / video call——照片支线委托 P-3）、`gift`（寄 / 送 / 礼物 / 收货地址）、`money`（转账 / 借钱 / 红包 / 打钱 / send money / cash app）；三语词表 + 句式（疑问 / 请求 / 确认「you really want me to…」）。`drafts.py` `_SENSITIVE_PATTERNS` 加 `commitment` 类 → `risk=high` + `shadow=commitment:<kind>`；命中即调 `risk_hold.set(conv, "commitment:"+kind, hit)`。
- 验收：PHBRBW 23:47 那句 → `meet`；tests 五类 × 三语各 2 例 + 误伤例（「我今天见了老板」不算）。

## B 人设见面政策（D-Q3）

- `persona_manager` 新字段 `boundaries.meeting_policy: never|after_months:N|handoff`（默认 `never`），`boundaries.commitment_style`（委婉 / 直接）；`personas.html` 一格三选 + 拒绝话术预览（`mp-` 前缀）；导出 / 导入 / 备份随人设。

## C 命中即自动发委婉延后 + 审核稿候选

- 命中 `commitment` → **不等人工**：按政策从话术库选一句（多语言、人设口吻 few-shot：「I'm not ready to meet just yet, let's keep getting to know each other」/「Not yet, I don't want to rush this」/「我还没准备好见面，等我们更熟一点再说」；contact：「I don't share my number on here」；money：「I don't do money stuff on here」）走 pacing 发出，同时通知人工（needs_human 由 Q-3 的 risk_hold 联动）；`handoff` 政策则不发只转人工。
- 审核稿：在政策指令下生成 2–3 条拒绝候选（禁止接受 / 承诺给地址），人工一键选。
- 第二次坚持（同客户同类 24h 内再提）→ 需人工 + 暂停该会话全自动；已答应过的给「收回」话术（「刚才说得太快，其实我还没准备好见面，别生气，我们慢慢来」），禁工作借口。
- 日志 `[commitment] conv=… kind=… policy=… decision=refuse_sent|handoff|second_insist`。

## D 出站拦截（`outbound_humanize` 挂两类）

- `commitment_claim`：邀约轮出现 sounds lovely / see you Saturday / I'll have … ready / my address is / I'll text you my address / I'll be waiting / 到时见 / 我地址是 → 改写为拒绝话术，落 stats；
- `self_blame_repromise`：you're right / my bad / completely forgot / I promise / later today / 我忘了 / 下次一定 → 改写为直接随意拒绝（not sending pics right now / I don't do that on here / nah not tonight / 今晚不行）；被追问分支与 P-3 lie_caught 动作对齐（照片：有 mid 重发，无则实话）。
- 与 P-3 `media_claim` 并成一张表 `CLAIM_KINDS`，质检页「AI 指纹」卡（P-1 D）自动多两格。

## E 底稿三句

- `system_companion.txt`（O-1 C）加：不能做的事第一次就拒、被追问仍拒、不解释之前为何没做；现实动作（见面 / 地址 / 照片 / 礼物 / 钱）一律按人设政策；never 政策下不给任何时间承诺。销售域底稿不动。

## F 门禁

- `scripts/commitment_gate.py`：20 条邀约 / 要地址 / 要钱用例（三语）→ 全部委婉拒绝、0 接受、0 地址；10 条要照片（相册有 / 无匹配）→ 只出现发图或拒绝（复用 P-3 用例）；不过不发版。tests 覆盖 A–D。

## 收工

- 落点表 `docs/发版对账_v1.0.79_Q2.md`（§〇 「为什么会答应上门」三段）；`docs/Q批_进度总账.md` 只改 Q-2 行；`agent_probe.ps1 -Done`；回访文案交 Q-0 / R79。

## 开新对话粘贴

```
读 docs/指令_Q-2_现实承诺守卫五类_2026-09-09.md，按它做 Q-2（#263 #264：新 commitment_guard 五类意图 meet/contact/media/gift/money 三语词表 + drafts.py 风控词表加 commitment 类 → risk=high + risk_hold / 人设 boundaries.meeting_policy 默认 never + personas.html 一格三选 / 命中即自动发委婉延后话术并通知人工、审核稿 2–3 条拒绝候选、第二次坚持转人工 + 收回话术 / outbound_humanize 挂 commitment_claim 与 self_blame_repromise 两类拦截并与 P-3 media_claim 并成一表 / 陪伴底稿加三句 / commitment_gate 20+10 用例门禁）。预算 $200。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与接口约定②④、docs/发版对账_v1.0.79_P3.md（照片支线已成动作型，并入不重做）、D-Q3，再 scripts\agent_probe.ps1 -Intent 登记；不动 autosend_worker.py / autosend_policy.py；personas.html 只加 mp- 前缀一格并用 stage_hunks。每个 commit 带 #263 并立刻补落点表 docs/发版对账_v1.0.79_Q2.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-2 行并 agent_probe -Done。
```
