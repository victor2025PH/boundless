# 指令 Q-15：成人内容分级与拦后软回应（#271）

> 预算 **$120**。工单 **#271**（报告 Z25RQS / 9JP5SZ，09-10 06:15，skuio 1.0.78）。底座 `docs/修复指令_Q批_并行_2026-09-09.md`。落点表 `docs/发版对账_v1.0.79_Q15.md`（赶不上 1.0.79 则改 v1.0.80）。
> **前置：Q-2 与 Q-3 两行收工**（风控词表归 Q-2、needs_human 闸归 Q-3，本线在它们之上加分级与软回应，不重做）。

## 事实（证据 `tmp_diag\Z25RQS`、`tmp_diag\9JP5SZ`）

- 06:15:29 WhatsApp vanessa(447742959746)×Chris：客户「…a weekend of non stop best sex of your life lol…」→ `auto_generate_draft L1 shadow=adult hits=sex` → `[needs_human] 打标 reason=high_risk auto_clearable=False` → `[stop-contact] action=review reason=risk_high`。客户 **19 分钟零回应**，到 06:37 坐席手发才摘标。
- 界面只显示「客户消息命中高风险拦截」，**不写类别与命中词**。
- 06:11 同会话 AI「A weekend of pampering and adventures with you sounds dangerously tempting, I'll admit」——再次滑向接受周末见面，**归 Q-2**（PHBRBW 族），本线不动。
- 关键词 `sex` 一词即 high：不分「提及 / 调情 / 露骨 / 施压」，对陪聊业务过粗。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 分级** | 新 `src/inbox/adult_grader.py`：`grade(text, lang) -> {level: mention|flirt|explicit|pressure, hits}`。`mention/flirt`（单个成人词、玩笑语气、无施压）→ `risk=medium shadow=adult_flirt`，**不打 needs_human**；`explicit`（露骨描写）→ high 但走 B 的人设策略；`pressure`（露骨 + 要求 / 施压 / 索要）→ high + needs_human（现状）。三语词表（en/zh/ja）+ 施压词（now / send / show / want you to / 必须 / 快）。`drafts.py` 风控词表 adult 命中处**一行接入**（Q-2 落地后的版本上加，stage_hunks） | 新文件 + `drafts.py` 一行 |
| **B 人设策略** | `persona_manager` `boundaries.adult_policy`：`human`（拦截转人工）/ `soft_reply`（自动软回应 + 标记，**陪聊域默认**）/ `mark_only`；`personas.html` 与 Q-2 的 `meeting_policy` 同一格区（`mp-` 前缀，stage_hunks） | `persona_manager.py`、`personas.html` |
| **C 拦后不沉默** | `soft_reply` 时：命中 explicit/pressure → 立即以人设口吻发一条**软回应**（既不接、也不硬拒，多语言 few-shot，禁客服腔；不含任何罐头句），同时 needs_human 打标（Q-3 的闸照常拦后续自动稿）；`human` 时：3 分钟坐席未接手 → 同一软回应补发一次，落 `[adult] soft_reply conv=… level=… policy=…` | 新 `adult_grader.py` 话术库 + `protocol_autoreply.py` 打标处只加调用（Q-3 文件，stage_hunks） |
| **D 可见** | 工作台 needs_human 卡片 / 稿头：「命中：成人内容（sex）· 露骨」+「一键人设口吻软回应」按钮（走既有发送路由）；`reply_diagnosis` 的 high_risk finding 带 category/hits/level | `unified_inbox.html`（`ad-` 前缀，stage_hunks）、`reply_diagnosis.py` |
| **E 门禁** | `tests/test_adult_grader_gate.py`：10 条——5 条调情 / 提及不转人工且不拦、5 条露骨施压转人工且软回应发出；纳入 R 批门禁清单 | tests |

## 红线

- 不动 `autosend_worker.py` / `autosend_policy.py`（Q-3）、不改 `drafts.py` 词表本体（Q-2），只在它们之上加分级钩子。
- 软回应不是「稍等我看看哈~」类缓冲句，是人设口吻的一句话；不引入任何全局兜底。
- 默认只对**陪聊域**人设生效；销售 / 客服域保持 `human`。

## 开新对话粘贴

```
读 docs/指令_Q-15_成人内容分级与拦后软回应_2026-09-10.md，按它做 Q-15（#271：新 adult_grader 把 shadow=adult 分 mention/flirt/explicit/pressure 四级，只有露骨+施压才 needs_human / 人设 boundaries.adult_policy human|soft_reply|mark_only，陪聊域默认 soft_reply / 命中即发人设口吻软回应不沉默、human 策略 3 分钟未接手补发一次并落 [adult] 日志 / 工作台卡片写明「命中：成人内容(sex)·露骨」+ 一键软回应，reply_diagnosis 带类别 / 10 条门禁）。预算 $120。前置：docs/Q批_进度总账.md 的 Q-2 Q-3 两行须已收工，先读它们的落点表与 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵，再 scripts\agent_probe.ps1 -Intent 登记；不动 autosend_worker.py / autosend_policy.py，drafts.py 与 protocol_autoreply.py 只加一行钩子并用 tools/stage_hunks.py --mine；不引入任何罐头句。每个 commit 带 #271 并立刻补落点表 docs/发版对账_v1.0.79_Q15.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-15 行并 agent_probe -Done。
```
