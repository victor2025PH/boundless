# 发版对账 v1.0.75 — L-7（值守登记链 + 收口：报告自动立单 · 每日对账 · bot 误判 · K-5 ④⑤ · 1.0.75 全批收口）

> 指令：`docs/指令_L-7_值守登记链与收口_2026-09-06.md`。底座 D-L8 / D-L9：`docs/修复指令_L批_并行_2026-09-06.md`。总账：`docs/L批_进度总账.md` L-7 行。预算上限 $120。
> 接手方：先 `git -C D:\boundless log --oneline -20` 核对本表 commit 真在；再看「断点 / 交接」节从未勾的一步继续。任何写台账先 dry-run；对外只走 `tools/duty_reply.py`。
> 执行：2026-09-06 11:10 起（接手时 L-1…L-5 已收工、L-6 #213 已提交；L-6 B/C/D 在 11:13–11:35 陆续入库）。

## 0. 一句话

A（报告到达自动挂单/立单 + 回执带单号）、B（报告↔工单每日对账 + 08:00 计划任务）、C（smalltalk 请求词白名单 + 序号哨兵豁免自发消息）三件已提交并对生产生效（A/B 是值守工具，tick 每 2 分钟从工作树跑；C 随 11:45 zhiliao 重启装载）。E 收口按 K-5 模板走（见 §5）；D 的 69 单 dry-run 已过目、待两包发布后 `--apply` + 两份回访（文案见 §4）。

## 1. A —— 报告到达即登记（根治漏立单）

| 项 | 落点 | commit | 测试 | 状态 |
|---|---|---|---|---|
| 自动挂单/立单 | **新** `tools/duty_auto_ticket.py`（仓库根 tools/，与 duty_channel_reminder 同目录）：`parse_report_head`（报告头三行 → kind/fp/app/time/note；无 field-agent 头且文件名 selfcheck/probe → `selfcheck` 不算报告）→ `decide` 四级：① note 含 `#N` → 挂 #N（`dup_of` 链跟到主单，不存在的号不算命中）；② note 以 【复现/【复验/复验/补证/关联 开头或含已知 6 位报告码 → `tickets_for_code` 三处反查（取证台账 `received.diag` / 工单标题正文含码 / 该码目录 `ticket.txt`），多张单时**未关闭优先、id 大优先**；③ 同报障人（fp DC8F→skuio / B990→钧）30 分钟内群消息立的开放单，note 前 60 字与标题 CJK bigram 重叠 ≥0.45 → 挂那张；④ 都不命中 → `record_bug_ticket(text=标题)`（标题＝去掉开头【标签】的首句 ≤80 字；首句 <12 字并上第二句）→ 正文＝note 全文 + `[报告 <码> 时间 app fp]` 戳、`reporter_version=app`、事件 `auto_split`；`record_bug_ticket` 自己按标题相似归并时视作挂单。`*:verify` 不立单（带 #N 只补一条复验 note）；`【汇总` 总表不立单、记 `auto_skip` 事件推值守。→ `apply` 写：工单 note（`append_ticket_note`，带戳）/ `bug_events`（`auto_attach` / `auto_split` / `auto_skip`）/ 取证台账 `duty_evidence.jsonl received diag=<码> by=duty_auto_ticket` / `tmp_diag/<码>/ticket.txt`（追加一行 JSON，末行为准）。`auto_ticket(code)` 幂等：已有 ticket.txt 直接返回既有归属。任何异常只记日志返回 None。 | `085cd14b`（⚠ 由兄弟线 11:24 整树分主题提交带走，内容与本线工作树逐字一致；本线测试 + tick 注释 `5f6f3916`）；selfcheck 分类 + `--attach` 在 `d1fcae8c` | `tests/test_duty_watch_autoticket.py` **9 passed** | **生效**（tick 从工作树跑，无需重启） |
| 回执带单号 | `tools/duty_channel_reminder.py`：`check_new_reports` 对 report 类先 `_auto_ticket(code)`，回执改「{name} 报告已收到：<码>（主题）→ 已立 #N / 已挂 #N / 总表，值守人工逐项对 …」（`receipt_item`）；verify 无 #N 仍不回执；工具失败/异常 → 回落无单号回执，**绝不丢回执**。DEFAULT_REMIND 里「值守立单/挂单后再回单号」改「自动回一条『收到 <码>，已立/已挂 #N』」。`tools/duty_watch_tick.ps1` 头注释补第三步（无新发送面）。 | 同上 | 演练：内存里摘掉 BJZFSX/C6EFRS/E42974/KJ6BSF/TUC9EH/MG65CT 的 receipted 跑 `check_new_reports(dry_run=True)` → 一条回执「BJZFSX（…）→ 已挂 #207、C6EFRS（…）→ 总表，值守人工逐项对、E42974（…）→ 已挂 #202、KJ6BSF（…）」；MG65CT diag 不回执、TUC9EH verify 无 #N 不回执 | 生效 |
| 历史归属回填 | `--backfill-index`：85 份历史报告按既有映射补 `ticket.txt`（不写库不发消息）；standalone 水位初始化 113 码不回放；`tmp/duty_reports_index_0906.py` 加「归属」列（读 ticket.txt） | — | 85 份写入 | 完成 |
| 指令 A-2「群截图按主题词匹配挂单」 | **未做**：改法在 `bug_intake.py` 收集窗（`note_screenshot` / `_in_collect_window`），超出底座给 L-7 的「只动两处」；D-L9 后群不再是提交渠道，价值下降。#194 body 里 mid 1206–1223 截图堆进最近一单的现象仍在，等下一批 | — | — | 交下批 |

**真机证据（12:37–12:49 skuio 三份真报告，全部 2 分钟内回单号）**：

| 报告 | 到达 | 判定 | 回执 |
|---|---|---|---|
| FX5YRE「断线弹窗 5BKTRB：12:34 右下角仍…」 | 12:37:50 | ② 关联 5BKTRB → **挂 #196**（12:37:53） | **mid 1248** 12:37:55「收到 FX5YRE（…）→ 已挂 #196」 |
| M2SHYA「主动关怀未发送：Kxhm(telegram)‘kim? are you at cebu now?’到点显示『下轮巡检就处理』…」 | 12:47:01 | ④ 无命中 → **新立 #214**（P2，reporter_version 1.0.74.0，事件 auto_split） | **mid 1249** 12:47:04「→ 已立 #214」 |
| 9YYD44「【BUG·主动关怀不发送】根因 proactive_care dry_run=True … 关联 ZQ4ASK / 9K6G7W / M2SHYA」 | 12:49:01 | ② 首码 ZQ4ASK → 挂 #182（已修单）——**判错**，应挂 2 分钟前 M2SHYA 刚立的 #214 | 12:49:05 回执被 send 的 120 秒近重复守卫 **409** 拒掉，且 receipted 已标 → **零回音** |

据此三改（`392d60de`）：多码 → 汇总候选单按「开放 → 已修 → 已关，同级 id 大」取；receipted 只在 send 成功后标，失败下一轮重发；标题不切英文引句里的 `?`（#214 标题原被截成「…‘kim」，已按新规则重算为 76 字）。修完：9YYD44 `--attach 214` 落库（ticket.txt 末行 #214）、从 receipted 摘掉 → 13:37 tick 自动重发 **mid 1254**「收到 9YYD44 → 已挂 #214」。日志 `.ops\duty_auto_ticket.log` / `.ops\duty_channel_reminder.log`。

**#214 交办**：skuio 新报「主动关怀到点不发、『立即发』无执行」——根因线索 `proactive_care 派发循环 dry_run=True`（启动行 `enabled=True, dry_run=True`；care LLM 影子扫描 shadow=True；页面顶部灰字「已审 0/8 条，审够就放心开真发」是唯一提示）。他的六条要求（卡片标「试运行中」+ 开真发按钮 / 立即发在 dry_run 下弹确认真发不静默 / 8 条评审门槛可跳 / 用户显式关怀优先级不低于 AI 自主开场 / 落日志 / 字段改「要发的话」）属 **J-8 B（关怀）/ D1b（care dry_run 灰度门）** 线，不在 L 批任何指令内 → **交下批（老板定）**；值守口径先不承诺修法，只答「已立 #214，根因是关怀派发仍在试运行档」。

## 2. B —— 每日对账

| 项 | 落点 | commit | 状态 |
|---|---|---|---|
| `duty_reconcile --reports [--since YYYYMMDD] [--alert]` | `engines/chengjie/tools/duty_reconcile.py`：`reconcile_reports(diag, db, ledger)`——每份 report 类报告必须挂单（ticket.txt / 取证台账 / 正文含码 三处任一）；verify 只记复验不需挂单；ticket.txt=summary 不需挂单；输出「报告 N 份（report×a verify×b）· 已挂单 M · **未挂单 K 份**」逐份列时间/码/机器/note + 处置命令 `duty_auto_ticket.py --code <码> --force --dry-run`（或 `--attach N`）；`--alert` 且 K>0 → `tools/duty_alert.deliver`（@ai_zkw，bot 失败回落支持号私聊）；退出码 3=有未挂单。只读。 | `d1fcae8c` | 完成 |
| 计划任务 | **新** `deploy/duty/run_duty_reports_reconcile.ps1`（日志 `deploy/duty/logs/duty_reports_reconcile_<日>.log` 留 14 天）；`schtasks` **DutyReportsReconcile** 每日 08:00，`run_hidden.vbs` 隐藏运行器（XML 注册——`/TR` 261 字符上限会截掉 `.ps1`，`Last Result -196608` 就是这个坑）；12:02 手动跑一次 rc=0 | 同上 | 完成 |
| 首跑（历史全扫） | 101 份 → 剔除 5 份 09-02 首装自检（K5G7N4 NS5KJD VCMEJS WACJFY ZRYNKN）→ **96 份（report 94 / verify 2）· 未挂单 12 份**，全部是 skuio 09-04/05 报告（群消息立的单没登记码）：E2F5JB→#153 · 5NAR3T→#166 · KJ6BSF/T8VMJ5→#168 · 9SZ8DY→#167（提案本体等老板）· Z3RZH2/ABFET2→#173 · 8Z6ESR→#174 · CZEQAN/PJ2JFF→#176 · E42974→#202（自动，经 29BAG6）· C6EFRS 汇总不立单。dry-run 过目 → `--attach` 落库（只补 note / 取证台账 / 事件，不改状态不发消息）→ **96/96 已挂单**（12:00） | — | 完成 |
| tests | `tests/test_duty_reconcile.py` +3 例（三处反查面 + 未挂单清单 + since 过滤 + 全挂后 0 份 / 缺库缺台账容忍 / 计划任务包装钉）→ 与 A 合跑 16 passed | 同上 | 完成 |

## 3. C —— bot 两处误判（`src/ops/bug_intake.py` 只动两处 + webhook formatter 一分支）

| 项 | 落点 | commit | 测试 | 状态 |
|---|---|---|---|---|
| C-1 smalltalk 白名单 | `INFO_REQUEST_WORDS`（列出 / 清单 / 进度 / 修复了什么 / 哪些修了 / 修复结果·情况·进度·状态 / 对账 / 总表 / 汇总 / changelog / release note / 更新日志 / 发版说明 / 什么时候修·发 / 修好了吗 / 修复了吗 / 修了吗 / 有没有修）+ `is_info_request()`：词表命中，或 ≥6 字且以 吗/么/嘛 收尾的无问号问句（ev#810 钧「那个表情在客户手机上会动吗」形态）。`trigger_verdict` 静默前一处：命中 → `_bump("request_for_info")` + 事件 `request_for_info` + `_publish_alert("request_for_info", P2)`（每用户 10 分钟去抖，事件不去抖）→ `return False`（AI 不接：不许编台账，值守人工答）。「列表」刻意不进表（bug 词「会话列表不刷新」先命中；「这个列表好看」是闲聊）。bug/usage/feedback 分类优先级不变。`src/inbox/webhook_notifier.py` `bug_intake_alert` 加 `request_for_info` 分支「📋 报障群有人要清单/进度/答复（AI 未接，值守人工答）」——否则 else 分支渲染成假「新工单」 | `b7e9ef5d` | `tests/test_bug_intake_request_words.py`（词表正反 18 例 + 事件/告警/去抖/分类优先级/formatter） | **已装载**（11:45 重启；`/api/admin/bug-intake` stats 已见 `request_for_info` 键） |
| C-2 序号哨兵豁免自发消息 | 新 `_own_outbound_mids(config, account_id, chat_id, ids)`：只读 inbox.db（`inbox.db_path` 或 `config_dir()/inbox.db`），查 `conversation_id = telegram:<acct>:<chat>` 且 `direction='out'` 的 `platform_msg_id`——本账号经 `/api/unified-inbox/send` 发出的消息 TelegramClient 收不到 update（0906 03:43 mid 1224 → 1225 到达时成洞 → P1 `seq_gap` 误报 + 补拉自己）。`due_seq_gaps` 收割后先剔自发号：记 `seq_gap_own` 事件 + INFO，不告警不补拉；混合洞只对真缺号告警；剔完为空整组静默。缺库 / 账号不明 / 异常 → 退回旧行为（宁可多告警）。**未**在 send 路径加登记钩（那是 L-1 的 `unified_inbox_send_routes.py` / `telegram_client.py`），harvest 时查库即可、跨重启也成立 | 同上 | `tests/test_seq_gap_own_message.py`（豁免 / 混合洞 / in 不算 / 他号不算 / 缺库回落 / 默认路径 / 接线钉）；两文件 **29 passed**；邻接 `test_bug_intake` 等 **178 passed**、webhook 7 passed | **已装载**（stats 已见 `seq_gap_own`） |

## 4. D —— K-5 ④⑤ 收口（D-L8：#170 #173 剔除条件已解除，随 1.0.75 一并标）

- ④ 计划两份：`engines/chengjie/tmp/k5_mark_fixed_1074.json`（49 单，K-5 备好、原样）+ **新** `tmp/l7_mark_fixed_1075_new.json`（20 单 #194–#213，fix_note 全 ≤300 字、人话、无 commit 号；来自六线验收口径）。**dry-run 已跑**：`tmp/l7_mark_fixed_49_dryrun.txt` 49 项全 `new → fixed` 0 跳过 / `tmp/l7_mark_fixed_20_dryrun.txt` 20 项全 `new → fixed`；`--count-fixed-unnotified` 现 61 → 应 130。执行命令（两包发布 + 群播报之后）：
  ```
  cd D:\boundless\engines\chengjie
  python tools\duty_ledger_fix.py --data-root D:\chengjie-instances\zhiliao\data --mark-fixed-file tmp\k5_mark_fixed_1074.json --apply
  python tools\duty_ledger_fix.py --data-root D:\chengjie-instances\zhiliao\data --mark-fixed-file tmp\l7_mark_fixed_1075_new.json --apply
  python tools\duty_ledger_fix.py --data-root D:\chengjie-instances\zhiliao\data --count-fixed-unnotified     # 应 130
  ```
- ⑤ 回访两份（每人 ≤2 条，先贴这里；`duty_reply --group neice --receipt --text-file …` 原文发）：
  - **skuio**：第一条 = C6EFRS 46 项逐条对账 **1.0.75 版**（`tmp/mapping_c6efrs_1075.py` 重跑：口径「1.0.74 已修·1.0.75 请验」23 行 /「1.0.75 已修·请验」22 行 / 上游说明 1 行；头部带内测包手装链接），文件 `D:\chengjie-instances\.ops\duty_0906\r_mapping_C6EFRS_1075_p1.txt`（3430 字）；第二条 = 合计 + 优先验 5 项（#210 / #205 / #207 / 后台分层 / 出厂默认）+ 需动手项（#210 开关位置 / #170 角标浮层路径 / #192 残放大图）+ D-L9 规则，`…_p2.txt`（577 字）。
  - **钧**：一条 `…\r_jun_1075.txt`（1142 字）：先装内测包 → 本版新修 2（#195 再收一个动图贴纸 / #213）→ 1.0.74 已修 16 逐单一句（#67 **必含相册重传**；#145② 不说已修）→ 优先验 4 项 → 需动手 #67 → D-L9 规则。短、结构化、无作息寒暄。
- **已执行（13:29–13:40）**：④ 两份计划 `--apply` → 69 单 fixed（`ledger_fix` 事件 ×69，不触发回访），`--count-fixed-unnotified` 61 → 126（K-5 预估 130：#61 #63 #64 #67 早有 notify_ts）。⑤ 三条经 `duty_reply --group neice --text-file`（先 `--dry-run` 过红线闸）发出：**skuio mid 1251 + 1252、钧 mid 1253**（文案即上面三个文件，逐字）。记账 `duty_notify_summary --reporter … --msg-id … --ids <文案真提到的单> --apply`：skuio 50（分两次——`list_pending_notify(limit=100)` 一页装不下，第二次补 #189–#212）+ 钧 15 = **65 单**，`note=summary:20260906 mid=1251/1253`。**刻意不做**：fixed 未回访剩 **61**＝8 月老积压（skuio 47 / 钧 8+6），两份文案没提到它们，标「已告知」是假账；一句一单草稿已备 `.ops\duty_0906\r_skuio_backlog_1075.txt`（2308 字）/ `r_jun_backlog_1075.txt`（773 字），**交下一班**明天发（每人每日 ≤2 条），发后 `duty_notify_summary --reporter … --apply` 即清零，`duty_notify_pending --alert` 在此之前仍会按 12h 去抖提醒一次。
- 症状族对账单：族 4 +16（#210）+17（#207）+18（#196）层、族 5 +4（#208）层、**新立族 7 后台可见性**（8 层）；用户画像加 09-06 L-7 一行（见 §6）。

## 5. E —— 1.0.75 全批收口（照 K-5 ①②③ 模板）

| 步 | 状态 | 事实 |
|---|---|---|
| L-6 收工核对 | ✅ | 四件全在 HEAD：A `3cca7d84`（10:49）/ B `7c3e6e45`（11:13）/ D `e56b72a0`（11:22）/ C `2d804c5f`（11:35）；落点表由 L-6 自己补，不阻塞 |
| `restart_preflight` | ✅ 11:38 **GO** | 五项全绿：loadable 静默 / 33 脏 py 语法 0 错 / app 装配 4 passed / Advise 正常 / 冷却 162 min；advisory 提示 12:30±45 批处理窗 → 等到 11:45 窗口开再动手 |
| zhiliao 装载 | ✅ 11:45:26 | `restart_instance.ps1 -Instance zhiliao -Reason "L-7 E: load L batch"`：soft-stop 拒绝后 hard-stop（headless 常态）→ `/login` **14s** 就绪（SLA 180s）；ops alert 已发；HEAD 当时 `b7e9ef5d` |
| 装载后自检 | ✅ | `tmp/l7_postload_check.py`：八个新端点全非 404（L-2 `preset-catalog` / `schema-keys` / `profiles/export` 200；L-3 `channel-alert/mute` 200；L-4 `developer-mode` 403（未解锁，预期）/ `drafts/translate` 200 / `license` 带 `effective_plan,plan_source,gate_enabled`；bug-intake stats 含 `request_for_info` + `seq_gap_own`）；app.log 197 行 **0 ERROR / 0 Traceback**，WARNING 8 条全是既有巡检类；启动行：`✅ 冲刺推进器已常备` / `危机升级桥已装载` / `HealthWatchdog surface_in_ui=True` / `学习队列存量清标（L-4 #201）: noise_rejected 2, dup_recalced 7, dup_cleared 7`；服务器实例不打「随包种子缺席」（预期）；`[needs_human] 启动扫描` 未见＝zhiliao 无带标会话（L-3 口径） |
| 全量回归 | ✅ 11:46–12:31 | **75 failed / 25339 passed / 36 skipped，44 min**（`tmp/l7_pytest_full.txt`）；对 K-5：59 已知红仍红、10 K-5 红转绿、16 新 → 串行 **13 稳定 + 3 抖动**；13 条逐条归属见 `发版对账_v1.0.75.md` 表（3 条 L-7 顺手消化：#207 两按钮补挂 window `22ffee95` / 功能矩阵重导出 / ui-build bump；10 条交回 L-1 3 / L-2 2 / L-4 4 / L-3 1，全是门禁·棘轮·台账层，**0 功能性回归**） |
| gate_sweep | ✅ 12:4x | 门禁 40 文件 **7 failed / 797 passed**（3:05）：1 预存 J-8 lint + 6 条上表 L 批门禁债；重跑仍红＝真红 |
| 版本 bump + 繁体 + changelog | ✅ | `desktop/package.json` 1.0.74→**1.0.75 / 1.075**；`python -m scripts.i18n_hant generate` → 16382 键（+5）13 passed；`gen-chatx-changelog add --version 1.0.75`（中英各 14 条，源 `tmp/_notes_1075{,_en}.txt`）；ui-build 20260906-1246；`chatxContent.ts` 五值；**release commit `df4797e2`** |
| 打包 smart / clean | ✅ 12:47–13:07 | smart 590,508,994 B + `latest-internal.yml`（后端全量重建 11.5 min，两冒烟过）；clean 519,318,557 B（495 MB）sha256 `99e3ca41…9ff5f` + `latest.yml`（首启零数据冒烟过）；日志 `tmp/l7_dist_1075_{smart,clean}.txt` |
| publish 两渠道 / 官网 / 坐席 / fleet | ✅ 13:15–13:28 | 公共：VPS sha512 核过 / R2 / `latest.yml=1.0.75` / announcements `release-v1.0.75` / 清理 1.0.71；内测：`latest-internal.yml=1.0.75`，公共指针未动；`gate:content` PASS → 官网 deploy 13:22:56 OK；kouxing / lianbei push 全 OK（`channel: internal`，1.0.75.0，seatverify FRESH，smoke OK）；fleet：**kouxing / lianbei 1.075 smart/internal**，yunsheng 1.074 smart/internal（等 4h 自更＝L-5 §3 那一腿）。日志 `tmp/l7_publish_1075_{public,internal}.txt` / `l7_website_deploy.txt` / `l7_push_{kouxing,lianbei}.txt` / `l7_fleet_1075.txt` |
| 播报 | ✅ 13:29 | `duty_update_hint --set`（热重载）；报障群 bot **message_id=1250**（`tmp/_broadcast_1075.txt`）；频道 @hykj7 **messageId=152**（`tmp/_channel_post_1075.py`） |
| 两机装载核版本 | ⏳ | 无 SSH，回访 mid 1251/1253 + 群播 1250 都带手装链接 `https://bd2026.cc/dl/downloads/internal/ChatX-Setup-1.0.75.exe`；他们装完核 L-5 §5.2 三样（boot 段无「随包种子缺席」/ `✅ 冲刺推进器已常备` / `crisis-events audit_on=true`）+ `duty_evidence probe` 版本 + skuio 机「拆条已按 1.0.75 默认收紧」行 |

## 6. 断点 / 交接

（每完成一步在这里追加一行；接手方从最后一行继续）

- 11:10 接手；A 11:28 完成并生效；C 11:36 提交；11:45 zhiliao 装载；B 12:02 完成（96/96 已挂单，计划任务已注册）；D 两份 dry-run + 三份回访文案备好；E 等全量回归。
- 12:31 全量回归出；12:4x gate_sweep；12:46 ui-build；12:47–13:07 两包；13:15–13:28 publish 两渠道 + 官网 + 坐席两台；13:29 播报；13:29–13:40 D ④⑤ 落地（69 fixed / 3 条回访 / 65 记账）；13:35 A 真机三改 `392d60de`；13:37 9YYD44 回执补发 mid 1254。**L-7 收工 13:5x**。
- **交下一班值守**：① yunsheng 4h 内应自更 1.0.75（`chatx_fleet_status` 核 1.075）；② 两台内测机装 1.0.75 后核三样 + skuio 拆条行，并把 `duty_evidence probe` 版本记进 #210 / #166 / #185；③ 明天发两份老积压补充汇总（`.ops\duty_0906\r_{skuio,jun}_backlog_1075.txt`，发后 `duty_notify_summary --reporter … --msg-id … --apply`）→ fixed 未回访归 0；④ #214（主动关怀 dry_run）等老板定线；⑤ 08:00 `DutyReportsReconcile` 首次自动跑，红了看 `deploy\duty\logs\duty_reports_reconcile_*.log`。
- **交回各线（门禁债，不拦发版）**：L-1：`test_seed_switch_upgrade_coverage` ×2（`explicit_newline_only` 登升级覆盖表）/ `test_packaged_capability_ledger`（bubbles B 类登 `ui_unavailable`）；L-2：`test_avatar_voice` 两例改 D-L4 新契约；L-4：`test_template_inline_color_ratchet`（`--th-bd-amber3` 定义）/ `test_template_orphan_refs`（`rps-po-empty` 两行残留引用）；L-3：`unified_inbox.html` 内联硬色 12 > 11。
- **待老板（回访前应贴、本次随收工报告贴）**：L-4 四问（D 三档预设入口 / `plan_override` 徽标 / partner 是否藏 bug_tickets / 歌房资产不齐运行时视为关）；K-D1 孤儿线 `unlimited_mode` 已被 11:22 整树提交带进 HEAD `2b05ef61` 并随 1.0.75 打包（运行态与 09-04 起一致）；#214 归哪条线；L-5 §5.4「版本旁标 clean/smart」diag 元数据三处未做（预算内未排）。
- **本条发现但不该我动的业务缺陷**：#207 两按钮漏挂 window（已代修 `22ffee95`，属 L-3）；#214 关怀 dry_run 静默（新单，交下批）；A-2 群截图按主题词挂单（收集窗改法在 `bug_intake.py` 两处之外，交下批）。
- 预算：约 $95–110（读七份落点表 + A/B/C 三件 + 45 min 全量回归 + 两包 16 min + 两渠道发布 + 三条回访 + 真机三改）。
