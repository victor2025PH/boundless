# 发版对账 v1.0.79 — Q-1 目标引擎「已知即不问」+ 识破族（K24YJ2 / FCQFKF / 57S9XD / AEMAMJ / T9KN8X → #264 #269 #270）

> 指令：`docs/指令_Q-1_目标引擎已知即不问_2026-09-09.md`；底座 `docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-1 行、接口约定①③）。建在 O-3 C（`183e43c1`，`docs/发版对账_v1.0.78_O3.md` C 行）之上——线索词 / 出站校验 / `_probe_pending` / `[goal-inject]` 命名空间**不重做**，只改决议与上限。
> 证据：`tmp_diag\K24YJ2\ FCQFKF\ AEMAMJ\`（23:41 `target=occupation cue=job mode=cue`；23:46 AI 自己说「it's a job… convention center」仍 `missed→retry`；02:05–03:17 注入 13 次 asked 3 次；「20th time」「I feel like you are a bot now」「I promise I'm not a robot」「Nori? No, I'm Mizuki」）；#270 T9KN8X（十小时 `result=asked` 8 次：职业 ×6 年龄 ×2 → 「talking to a wall」→ Goodbye）。
> 预算 $180；执行方 Q-1 对话，09-10 10:36 开工（`agent_probe -Intent` 已登记）。

## §〇 为什么问到第 20 次（四段，按代码事实）

| # | 病因 | 代码位置（O-3 C 落地后） | 本条怎么改 |
|---|---|---|---|
| 1 | **只判「问否」不判「知否」** | `service.decide_probe_target` L1249：`all_unfilled = missing_slots(prof_fields)`——候选只看画像字段空不空；客户 23:46 说了「union carpenter at the convention center」、AI 自己也复述过，字段没人写就永远是「未填」 | A：槽位三态 `unknown / mentioned / confirmed`（`profile_slots.slot_state`）——字段之外**扫最近 30 轮客户 + 我方文本**：客户「说过」（`MENTION_LEXICON` / 确定性抽取候选）、我方非问句「复述过」、我方「问过且对方接了话」→ mentioned；候选只从 unknown 取 |
| 2 | **抽取关着** | `capture_status` 自己写了 `profile_llm_off`（clean 包基线未开）——引擎明知 LLM 抽取关着，仍把「字段空」当「客户没说」 | 同 A：mentioned 判定不依赖抽取；卡片槽位行显「已提及（未确认）」+「确认」（E）；D-O3-1 `profile_llm` 进基线仍交 Q-4 E / Q-5 |
| 3 | **重试无上限 + 问过的加回末尾** | `ordered = missing_slots(exclude=asked_slot_keys)` 下一行 `unfilled = ordered + [k for k in all_unfilled if k not in ordered]`——排除等于没排除；`retry_slot`=上轮 missed 即重试，无上限；`floor` 每天兜一个又把同槽兜回来 | A 撤「加回末尾」（`asked_slot_keys` 只排序）；B 每槽每日 `missed ≤2`（`PROBE_RETRY_PER_DAY`，`list_events(kinds=probe_missed, since=今日)` 按 `slot@` 计数）到上限**当日休眠**（不 retry / 不 cue / 不 floor）；`verify_pending_probe` 判 missed 前先查 **covered**（回复含该槽问法 / 线索 / 提及词 → 不计 missed 不重试） |
| 4 | **无出站重复提问守卫、无识破处置、账号名≠人设名没进提示词** | 回复主链没有「24h 内我方问过同一件事」的拦截；「you are a bot」只是普通入站；O-1 E 只做了红条 | C `repeat_question_guard`；D `exposure_guard`（停目标 24h + needs_human + 一句不辩解挽回 + 自证句黑名单）；F `prompt_addenda.identity_addendum`（Q-6 接线） |

## 一、改动落点（每 commit 一行，落地即写；均带 #264 / #269）

| 项 | commit | 落点 |
|---|---|---|
| **A + B** 槽位三态 + 30 轮扫描 + 只选 unknown / retry ≤2 + covered | `b9e1c353` | `profile_slots.py`：`SLOT_STATE_*` / `HISTORY_SCAN_TURNS=30` / **`MENTION_LEXICON`**（13 槽双语「陈述自己该项事实」词表，与 `CUE_LEXICON`「话头」分开：说「下雨」是问坐标的话头，说「I live in LA」才是提及；`re:` 小写正则 / `RE:` 保留大小写正则——「I'm in Chicago」靠大写专名、「i like that / I'm in bed」刻意不中）/ `_mention_hit` / **`cell_view(cell) -> (value, source, status)`**（三形状：Q-5 新形 `{value, source, status}`、旧形 `{v, src, ts}`——`agent` / `auto` → confirmed、`llm` / `llm_pending` → mentioned、裸字符串 → confirmed；接口约定①）/ **`slot_state(slot, prof_field, history) -> (state, reason)`**（reason `field:agent` / `said:<词>` / `captured` / `echoed:<词>` / `answered`）/ `slot_states` / `deepen_line`（「【已知不再问】…绝不要再问「X」…最多顺着 TA 说过的追一句细节…今天只这一次」）/ `known_slots_line`（负向清单一行）/ **`reply_covers_slot`**（B）；`pick_probe_target(mentioned=, deepen_ok=)`：unknown 里 retry > cue > floor，否则 mentioned + 有线索 + 今日未深化 → `deepen`（**不按下限兜**——「每天问一遍已知的事」正是病）。`service.py`：`recent_history(inbox, conv, 30)`；`decide_probe_target(..., history=) -> (slot, cue, mode, cues, states)`——候选只从 unknown 取、`asked_slot_keys` 只排序**不加回**、每槽今日 missed ≥2 剔除（`_event_detail_slots_today`）、retry 槽到上限即清、`deepen_ok` 按 `probe_deepen` 事件每槽每日 ≤1；`discovery_probe_asks(inbox_store=)` 计划句同样过滤 mentioned（否则「今天至少问一个：做什么工作」钉在卡上就是打脸）；`build_block_for_chat`：注入前 `slot_states` → 「已知」槽剔出软缺口（`_gap_key` 命中即 `profile_gap=""`）、`known_slots_line` 拼进 `probe_line`（有硬行 / 深化行前缀，无目标时 + 软缺口合成一行）、deepen 不挂 `_probe_pending`（不校验不重试）只记 `probe_deepen`；`verify_pending_probe` 三态 → 四态 **`asked / covered / missed / pending`**（`probe_covered` 事件，covered 不给 retry_slot）；`PROBE_RETRY_PER_DAY=2` / `PROBE_EVENT_COVERED` / `PROBE_EVENT_DEEPEN`；日志 **`[goal-inject] target=<槽> cue=<词> mode=cue|retry|floor state=unknown decision=probe result=pending`** / **`target=<槽> cue=<词> state=mentioned decision=deepen reason=answered|said:…`** / **`target=- state=occupation:mentioned,age:unknown decision=skip reason=all_known|no_cue_or_asked_today`** / `result=covered`；`build_beats_trace` 新 status `covered` / `deepen` + `summary.probe_covered / probe_deepen`。`stats.py`：`probe_covered / probe_deepen` + `/metrics goals_probe_total{result=covered|deepen}`。**tests** 新 `test_goal_known_not_ask_264.py` 9 例（三形状 / 三态四来源 + 30 轮边界 + 误中保护 / 决议只选 unknown + 不加回 + deepen 只跟线索 + confirmed 连 deepen 都不出 + retry 只认 unknown / **回归「同会话职业已答后不得再问」K24YJ2 回放**：23:41 硬问职业 → 23:46 客户答（字段仍空）→ 之后 12 轮含 work / job / boss / shift 线索一律不出「必问职业」、occupation 零 `probe_missed`、深化 ≤1 / 计划句过滤 / covered / **T9KN8X 形态**：坐标线索每轮都有但只硬问 2 次即休眠、次日醒）；`test_goal_probe_active_leg_236` HM7XBA 回放随新口径（坐标硬问恰 2 次；第 10 轮改次日 10:00；答后「坐标」进已知清单） |
| **C** 出站重复提问守卫 | `20f93db5` | 新 **`src/inbox/repeat_question_guard.py`**：`check_repeat_questions(text, *, conversation_id, lang, own_questions=None, store=None, now=None) -> (text, report)`——`own_questions_for(store, conv)` 从 InboxStore `list_recent_messages` 取 **24h 内 `direction=out`** 的问句（客户问的不算；`store` 缺省走 `protocol_bridge.get_inbox_store()`）；`normalize_question` = 小写 / 去停用词 / 英文词干（与 claim_guard 同源）/ 中日 2 字片段 + **同义小表 `_SYN`**（job / occupation / career / 工作 / 职业 → work；city / based / 哪里 / 坐标 → city / where；old / born / 几岁 → age…）+ **槽位标签 `slot:<key>`**（复用 `profile_slots.SLOT_ASK_KEYWORDS`，不另造词表）；`similarity`：同槽标签 → **1.0**（「what do you do for work」vs「what kind of work do you do」vs「做哪一行」是同一件事），否则 token Jaccard；≥ `SIM_THRESHOLD=0.8` → 该句删、其余保留（`action=strip`），整稿只剩它 → `filler_line`（按语言模板池、conv 取模稳定；`action=rewrite`）；判不准不拦、异常原文放行；日志 **`[repeat-q] conv=… matched_ts=HH:MM sim=1.00 action=strip\|rewrite q=… matched=…`**。挂点 **`outbound_humanize.apply_draft_humanize`**：`claim_guard.check_claims` **之前**（+10 行小 hunk，Q-2 归属见「三」），`meta.repeat_q` / `meta.repeat_q_stripped`。**tests** 新 `test_repeat_question_guard.py` 8 例（同槽同义中英 / `is_question` 漏问号疑问词起头 / 24h 窗 + 只算我方 + 非问句不算 / strip 保留前后句 + 日志 / 只剩重复句 → rewrite 无问号 / 新问题与无历史 clean / 从 store 取 / 挂点先于 `check_claims`）。回归 `humanize\|claim_guard\|draft` 邻域 649 例仅 1 失败 `test_autodraft_bootstrap_persists_auto_ai`（HEAD 上即失败：`set_automation_mode(..., source='bootstrap')` 与断言不符，非本线） |
| **D** 识破守卫 | `e0d59628` | 新 **`src/inbox/exposure_guard.py`**：`detect_exposure(text) -> hit`（识破词表 `EXPOSURE_PATTERNS`：you're a bot / are you a bot / is this a bot / u sound like a bot / chatgpt / you already asked / asked me that again / asking me again / keep asking the same / I (already) told you that|already|before / you told me that already / **Nth time** / you don't remember；中文 机器人吧 / 是不是机器人 / 是 AI 吧 / 不是真人 / 你又问 / 又问一遍 / 问过了 / 问了几遍 / 第 N 次问 / 我(都\|已经)说过了 / 跟你说过——**只收明确指认**：「you told me about your dog」「my robot vacuum」「第一次来这个城市」刻意不中）；**目标侧** `pause_goal(store, goal, hit=)` → `params._paused_until = now+24h` / `_paused_reason=exposure` + 事件 `exposure_pause`，`goal_paused_until(goal)`；**会话侧** `handle_inbound(text, conversation_id, inbox_store, inbound_ts)`（幂等：KV `exposure_hit:<conv>` 记 `{key=ts\|hit, ts, hit, consumed}`，同一条入站只处理一次）→ `protocol_autoreply.tag_needs_human(reason="exposure", source="exposure_guard")`（Q-3 已把它接成 risk_hold 闸）+ 会话标签 **「疑似识破」**（`EXPOSURE_TAG`）；**出站** `guard_outbound(text, conversation_id, lang, inbox_store)`：① 最近入站命中 → 先 `handle_inbound`；② 标记未消费且 ≤6h → **整稿换一句轻松不辩解的挽回**（`recovery_line` 模板池 en/zh/ja，conv 取模稳定：「lol sorry, long day. you did tell me」「哈哈抱歉，今天脑子有点糊，你确实说过」），消费标记（只换一次）；③ 任何时候删**自证句**（`SELF_PROOF_PATTERNS`：I promise I'm not a robot / I'm not a bot / I'm a real person / I'm human(句尾) / 我不是机器人 / 我是真人 / 真人在 / 我发誓…真人；「I'm real tired」不中），删空 → 挽回句。日志 **`[exposure] conv=… hit=… action=pause_goals\|needs_human\|recover\|strip_selfproof`**（WARNING）。**挂点**：`goals/service.build_block_for_chat` 找到目标后、达成信号之前——命中 → `pause_goal` + `handle_inbound`，本轮不注入 `reason=exposure_paused`（meta 带 hit）；停牌期内每轮 `reason=goals_paused`（meta 带 paused_until），24h 后自醒；`outbound_humanize.apply_draft_humanize`：repeat_question_guard **之后**、claim_guard **之前**（`meta.exposure`）。**不动** persona_reply / autosend_worker：出站侧自己从 InboxStore 取最近一条入站判定，自动链 / 手动稿 / 关怀链同一入口。**tests** 新 `test_exposure_guard.py` 7 例（识破词 10 中 7 不中 / 自证句只删该句 + 「real tired」不误伤 / handle_inbound 幂等 + 打「需人工」「疑似识破」+ KV / 出站：第 1 稿 recover（无问号无 robot）→ 第 2 稿只删自证句 → 第 3 稿 clean / 只剩自证句 → 挽回句 / **目标停 24h**：命中后 `_paused_until≈now+24h` + 事件 + 1h / 20h 后都 `goals_paused` + 24h 后重新注入 / 挂点顺序 repeat→exposure→claim） |
| **E** 总开关 + 「已提及（未确认）」/ 确认 | `5057865b` | `service.py`：**`DISCOVERY_PAUSE_KEY="goals:discovery_paused"`** 落 **InboxStore app_settings**（goals `store.py` 不动；无 InboxStore → 视为未暂停，不因缺存储把目标全停）；`discovery_paused(inbox_store)` / `set_discovery_paused(paused, inbox_store=, by=)`（日志 `[goal-inject] discovery_paused=1 by=…`）；`build_block_for_chat`：模板带 `profile_slots` 且开关开 → 不注入 **`reason=discovery_paused`**（卡片 / 计划照常）；**`confirm_profile_slot(store, plat, chat, slot, value="")`**：mentioned → confirmed——旧形 cell 写 `src=agent`（接口约定① `cell_view` 读作 confirmed），store 同值覆盖会 `continue` → **先清再写两步**；无现值且未给值 → `no_value`。`goal_routes.py`：**`GET/POST /api/goals/discovery-pause`**（`{paused}`；viewer 403；无 store 503）、**`POST /api/goals/profile/confirm`**（`{conversation_id\|platform+chat_key, slot, value?}` → `{ok, slot, value, state}`；未知槽 / 无值 400）；`for-conversation` 有目标 / 无目标两条路都带 `discovery_paused`；`_attach_slots_progress` 逐槽新增 **`state=unknown\|mentioned\|confirmed`**（`slot_states(sel, fields, recent_history(inbox, conv))`，与注入口径同源——卡上说「已提及」引擎就一定不问）。**cp-goal.js 双树同文**（`shared/copilot/components/` + `desktop/renderer/shared/copilot/components/`）：顶部 `_renderDiscoveryBar(d)`（active / empty 两态都出；`role=switch aria-checked`；旧后端缺 `discovery_paused` 键＝不渲染）→ `_dpauseToggle` POST + toast + beacon `goal_dpause_on/off`；槽位 chip `state=mentioned` → **「职业：已提及（未确认）」**（琥珀虚边 ◐）+ 行内 **「确认」**（`data-act=slot_confirm data-slot data-value`；无现值先 `prompt` 问值）→ `_slotConfirm` POST → toast「已确认」+ `cp-goal-changed` + refresh；`onAction` 两分支；CSS `.gl-slotchk.mention / .gl-slot-confirm / .gl-dpause`。**`?v=20260910a`** 三处（`shared/copilot/app.html:238` / `desktop/renderer/shared/copilot/app.html:238` / `src/web/templates/unified_inbox.html:105`——后者工作树被他人整文件 CRLF 翻动，**只把 HEAD+本行替换写进 index**，工作树同步字节替换，他人改动不带）。**i18n** zh / en：`inbox.goal.slots.mentioned{label}` / `mentioned_t` / `confirm` / `confirm_t` / `confirm_prompt` / `confirmed_toast`、`inbox.goal.dpause.label / on / pause / resume / hint / toast_on / toast_off`、`inbox.goal.skip.exposure_paused / goals_paused / discovery_paused`（hero「为什么没注入」直读）；zh_hant `scripts.i18n_hant generate` 再生，`stage_hunks --mine` 只暂存本线 3 hunk。路由清单 `_ADDITIONS_2026_09_10_Q1_DISCOVERY_PAUSE` 3 条（`stage_hunks` 只暂存本 hunk；该文件工作树另有 wecom / DY / Q14 他人 hunk 未入）。**tests** 新 `test_goal_discovery_pause_confirm_264.py` 5 例（开关开 → `discovery_paused` 不注入、关即恢复、无 store 视为未暂停 / 端点 GET-POST + for-conversation 两路带键 + viewer 403 / **slots_progress state**：客户说过「union carpenter」→ occupation=mentioned、age=unknown → 确认无值 400 → 带值 200 → cell `("union carpenter","agent","confirmed")` → 卡上 confirmed → 之后 boss / shift 线索也**不出「做什么生意/工作」且不出 deepen** / llm 摘录同值确认翻 confirmed（两步写）/ 双树同文 + `?v=` 三处一致 + 16 键三语） |
| **F** 身份补丁函数 | `ebc37c4e` | 新 **`src/inbox/prompt_addenda.py`**：**`identity_addendum(persona, account, *, lang="zh") -> str`**——账号显示名 ≠ 人设名 → 「对方可能用「X」称呼你，那是你在这个平台的昵称，不得否认、不必解释，照常以自己的身份回应。」（en / ja 各一版）；一致（含「Mizuki 🌸」⊃ Mizuki）/ 任一未知 / 判不出 / 异常 → `""`。纯函数零 I/O；`persona_display_name` / `account_display_name` 兼容 dict / 对象 / 裸字符串（`meta.self_name`（account_registry 富集）/ `self_name` / `display_name` / `name` / `nickname` / `profile.name`）；一致性复用 O-1 E `utils.account_name_check.names_consistent`（单一口径，配置页红条与提示词同判）。**未接线**（本线不动 persona_reply）。**tests** 新 `test_prompt_addenda_identity.py` 5 例 |

## 二、验收

| 测试 | 例数 | 钉什么 |
|---|---|---|
| `tests/test_goal_known_not_ask_264.py` | 9（A+B） | 三形状 / 三态 / 只选 unknown / 不加回 / deepen / **回归「同会话职业已答后不得再问」（K24YJ2 回放）** / covered / T9KN8X 两次即休眠 |
| `tests/test_repeat_question_guard.py` | 8（C） | 同槽同义 = 1.0 / 24h 窗只算我方 / strip 保留其余句 / 只剩重复句 → rewrite / 挂点先于 claim_guard |
| `tests/test_exposure_guard.py` | 7（D） | 识破词中 / 不中 / 自证句只删句 / 幂等 + 需人工 + 疑似识破 / 一次 recover / **目标停 24h 且期内不注入、24h 后醒** |
| `tests/test_goal_discovery_pause_confirm_264.py` | 5（E） | 总开关三态切换 / 端点 + viewer 403 / **slots_progress state + 确认 → confirmed → 不再问** / llm 同值确认 / 双树 + `?v=` + 三语 |
| `tests/test_prompt_addenda_identity.py` | 5（F） | 不一致出句 / 一致 / 未知 / 形状 + 三语 / 不 import persona_reply |
| 随改 | — | `test_goal_probe_active_leg_236::test_hm7xba_replay…`：`loc_hard == 2`（O-3 C 时无上限）、missed 含其它槽、日志断言加 `state=unknown decision=probe`、第 10 轮次日；`test_admin_route_inventory` 登记 3 条 |

回归（A+B 后，09-10 11:0x）：`pytest tests -k "goal or profile_slot or care_goal_link or liveness"` **1175 passed**。
回归（F 后，09-10 11:4x）：`pytest tests -k "goal or profile_slot or care_goal_link or liveness or repeat_question or exposure or outbound_humanize or claim_guard or draft_humanize or prompt_addenda or route_inventory or i18n_zh_hant or account_name"` 见下「收工回归」。

**活体口径（R79 装载后）**：同会话客户答过职业 → 之后 12 轮日志只见 `[goal-inject] target=occupation … state=mentioned decision=deepen`（≤1/日）或 `decision=skip reason=all_known`，**不得再见 `target=occupation … decision=probe`**；客户说「are you a bot」→ 同秒 `[exposure] … action=pause_goals` + `action=needs_human`，下一条出站 `action=recover` 且正文无问号无 robot；目标卡该会话 hero 行「客户疑似识破…」；`/api/goals/discovery-pause` POST `{paused:true}` 后所有摸底目标 `NOT injected reason=discovery_paused`。

## 三、越界点名

| 文件 | 归属 | 本线动了什么 | 为什么 |
|---|---|---|---|
| `src/inbox/outbound_humanize.py` | **Q-2**（未开工） | `apply_draft_humanize` 里 `check_claims` 之前 +20 行两小段（repeat_question_guard / exposure_guard 各一 try 块；`meta.repeat_q / repeat_q_stripped / exposure`），不改任何既有行 | 指令 C 明写「挂在起草层净化（P-1 A）之前」，该入口就在这个文件；Q-2 D「出站 claim 拦截」也会在同一处加钩，顺序建议 **repeat → exposure → claim(Q-2 扩) → humanize** |
| `src/companion/goals/store.py` | 默认不碰 | **零改动** | 总开关落 InboxStore app_settings KV；确认写 confirmed 用「先清再写」两步绕开同值跳过；三态由 `cell_view` 兼容读 |
| `src/web/templates/unified_inbox.html` | 共享 | 只改 `?v=` 一行；工作树被他人整文件 CRLF 翻动 → **index 只写 HEAD + 本行**（`git hash-object` + `update-index --cacheinfo`），工作树同步字节替换 | 与 O-3 C 台账同一坑，不把他人 28k 行翻动带进本线 commit |
| `tests/test_admin_route_inventory.py` | 共享 | `stage_hunks --mine Q1_DISCOVERY_PAUSE` 只暂存本线 hunk | 工作树另有 wecom / DY / **Q-14 块被删**（Q-3 台账已提醒）三处他人 hunk，未入 |
| `src/web/i18n_packs/zh_hant_auto.py` | 共享（再生产物） | `scripts.i18n_hant generate` 后只暂存本线 3 hunk | 他人 zh 新键未再生的 170 行不带 |
| `persona_reply.py` / `autosend_worker.py` | Q-6 / Q-3 | **零改动** | 识破守卫出站侧自己从 InboxStore 取最近入站；F 只出函数 |

## 四、交 Q-6 / Q-8 / R79

- **Q-6 F（接线）**：`from src.inbox.prompt_addenda import identity_addendum`；组 system prompt 处 `add = identity_addendum(persona, account, lang=persona_lang)`，非空即追加到人设段末尾（一句，不加标题）。`account` 传 account_registry 行（`meta.self_name` 已由 `account_self_profile` 富集）或直接传显示名字符串；`persona` 传人设 dict / `profile` / 名字字符串均可。一致或未知返回 `""` → 什么都不加，零回归面。
- **Q-8 C（注入分级 must 的 hook 位置）**：`src/companion/goals/service.py::build_block_for_chat` **probe 段**——`decide_probe_target(store, goal, prof_fields=, sel_slots=, inbound_text=, retry_slot=, now=, history=) -> (slot, cue, mode, cues, states)` 是**唯一决议点**：`mode ∈ cue|retry|floor` + `states[slot]` 三态在这里已知；硬行文案由 `HARD_ASK_DISCIPLINE` + `_probe_pending` 挂账。分级 must / should / may 建议就在 `decide_probe_target` 返回后、`probe_line` 拼装前插一层（读 `mode` / 当日 `probe_asked` 计数 / 阶段计划），把 `【本轮必问】` 降为 `【顺带问】` 等；`verify_pending_probe` 四态 `asked / covered / missed / pending` 不用动。总开关 `discovery_paused()` 在同函数更早处早退（`has_slots` 判定后），账号级默认目标（Q-8 A）建目标时须尊重它。识破停牌 `params._paused_until`（`exposure_guard.goal_paused_until`）在找到目标后立即早退——Q-8 的阶段推进计划也须在它之后。
- **Q-5（画像 schema）**：`profile_slots.cell_view(cell) -> (value, source, status)` 已兼容 Q-5 新形 `{value, source, status}`（`user / confirmed → confirmed`；`nickname / ai_inferred → mentioned`）与旧形 `{v, src, ts}`；Q-5 换 schema 后 A/E 的三态与「确认」不需改（确认端点若 Q-5 改成写 `status=confirmed`，替换 `service.confirm_profile_slot` 的两步写即可）。
- **Q-3**：`exposure_guard.handle_inbound` 走 `tag_needs_human(reason="exposure", source="exposure_guard")` → Q-3 的 `risk_hold.set(needs_human, hit=exposure)` 自动登记，L2 直发被闸；无需 Q-3 再接。
- **R79**：`.py` 改动须实例重启生效（模板 / i18n / cp-goal.js 热更，`?v=20260910a`）；门禁新增五项里本线两项 = `test_goal_known_not_ask_264.py::test_regression_occupation_answered_never_asked_again_k24yj2` + `test_exposure_guard.py::test_goal_paused_24h_on_exposure_and_not_injected_until_wakes`。**回访文案（交 Q-0 / R79，每条带工单号只给版本号）**：
  - **#270（Mike / T9KN8X）**：「1.0.79 起，客户说过的事引擎按『已知』处理：字段没填也会扫最近 30 轮对话，说过职业就不再问，最多顺着 TA 说过的追一句；同一项一天最多问两次没回应就当天不再问；24 小时内问过的同类问题出站前会被拦掉。目标页顶部新增『暂停全部摸底目标』总开关，卡片上『已提及（未确认）』一键确认。」
  - **#264（K24YJ2 / FCQFKF「you are a bot / 20th time」）**：「1.0.79 起，客户一旦说『你是机器人吧 / 你又问』，该会话所有目标自动停 24 小时并转需人工，AI 这一轮只回一句轻松的『抱歉，你确实说过』，且任何时候不再说『我不是机器人 / 我是真人』这类自证句。」
  - **#269（Nori / Mizuki 账号名≠人设名）**：「1.0.79 起，账号昵称与人设名不一致时，AI 会知道对方可能用昵称称呼自己，不再否认（配置页红条提醒仍在）。」（依赖 Q-6 F 接线，若 Q-6 未收工则此条不发）

## 五、收工回归

09-10 11:3x–11:4x：`pytest tests -k "goal or profile_slot or care_goal_link or liveness or repeat_question or exposure or outbound_humanize or claim_guard or draft_humanize or prompt_addenda or route_inventory or i18n_zh_hant or account_name"` → **1377 passed, 4 failed**，4 例全是本线该随改的钉桩（`cp-goal.js?v=20260908d` 三处 host 断言 → `20260910a`；`test_goal_routes::test_no_goal_returns_none` 精确字典补 `discovery_paused: False`），随改后四文件 **197 passed**（收工 commit 带）。中途一次 `test_goal_inject_visibility_152::test_note_meta_logs_not_injected` 失败为编辑 `service.py` 时 `inspect.getsource` 读到半截文件，单跑 / 再跑均过。

**HEAD 上即失败、非本线**（两处，值守 / 归属线请核）：`test_autodraft_bootstrap::test_autodraft_bootstrap_persists_auto_ai`（`set_automation_mode(..., source='bootstrap')` 与断言不符）；`test_admin_route_inventory::test_all_baseline_routes_still_registered` 在**纯 index / HEAD** 上缺 20 个端点（wecom SSO / wechat_kf / wechat_pc / desktop heartbeat 路由文件仍是未跟踪文件，基线却已登记）——工作树上因文件在场而通过。

## 六、花费

≈ $95 / $180（A+B ≈ 40，C ≈ 12，D ≈ 15，E ≈ 20，F ≈ 3，台账 ≈ 5）。
