# 指令 P-5：值守链三修 + 归位 + 回访口径

> 预算 **$80**。工单 **#106（误挂）#259 #254**。目标版本 1.0.79（C 段是引擎代码，随包；A/B 是值守工具，提交即生效）。
> 底座 `docs/修复指令_P批_并行_2026-09-08.md`。落点表 `docs/发版对账_v1.0.79_P5.md`。

## 开工前必读

1. `docs/全自动出站链_三视角方案_2026-09-08.md` §0 N11、§1.1 第 8 条、§2 P1-10。
2. 事实（已核）：
   - `tools/duty_auto_ticket.py` L76 `_TICKET_RE = re.compile(r"#\s*(\d{1,4})\b")` → MTRCH2 正文「出站收口翻译器#106(8DVDVC)」被当挂单意图，挂到 08-31 已 fixed 的 #106 并 `auto_severity P1->P0`（13:37 事件）。
   - `.ops\duty_channel_reminder.log` 13:47–14:09：32PTMK 回执 12 次 `HTTP 502 {"detail": "消息未送达："}`（原因为空），14:11 才成功 mid=1345；`inbox.db` 群会话里留下 **12 条无 `platform_msg_id` 的 out 行**（13:47:04 … 14:09:02）——群里实际只到 1 条，但收件箱 / 工作台会看到 12 条「已发」。同时段 R78 正在装载重启 zhiliao，疑为重启期适配器未就绪。
   - 2WD7FY（无备注）13:33 也回执了「（无备注）」；ZFFBG6 kind=verify 无 #N 正确未回执。
   - N-5 A 段 11:26–11:31 已把 #236–#251 收口；#252 #254 confirmed、#256 #257 new、#258 new（1.0.77 启动日志，P2）、#259 new P0。
3. 工具：`engines/chengjie/tools/duty_reply.py --group neice --ticket N --receipt --text-file F [--status confirmed] [--dry-run]`；`duty_evidence.py --data-root D:\chengjie-instances\zhiliao\data …`；立单模板 `tmp\duty_split_0906c.py`（dry-run → `--apply`）；只读总览 `tmp\new_since_0908.py "YYYY-MM-DD HH:MM" YYYYMMDD-HHMMSS`。
4. `scripts\agent_probe.ps1` 看 ACTIVE：`unified_inbox_send_routes.py` 有他人在途 hunk（抖音线 10:55 改过、未提交）——C 段只用 `tools/stage_hunks.py --mine` 暂存自己的 hunk；若该线仍活跃，C 段写进落点表交下一班，不硬改。

## 红线

- 台账写入先 dry-run；对外只经 `duty_reply.py`，每条带工单号，只给版本号不给日期。
- 不动 `duty_auto_ticket.py` 的 N-5 D 三档规则本体（只收紧 `#N` 上下文与 fixed/closed 处理）。
- `git add <显式路径>`；每 commit 带工单号。

## A 挂单规则收紧 + 不升 fixed/closed 单

- 修法：`_TICKET_RE` 改为只在「行首 / 前后空白或标点 / 前缀词（工单|挂|关联|见|同）」上下文匹配：`(?<![\w\u4e00-\u9fff)])#\s*(\d{1,4})\b`，紧贴中文或字母词尾（翻译器#106、器#106）不算；目标单 `status ∈ {fixed, closed}` 时：**不挂、不升级**，改为「按同报障人 90 分钟规则 / 同主题」找开放单，找不到就新立并在 note 里写「正文提及 #106（已修）」；`auto_severity` 只对 open 单生效。tests：MTRCH2 原文 → 不挂 #106；「关联 #254」→ 挂 #254。
- 顺手把 13:37 的误操作复原：#106 severity P0 → 原 P1，note 一行说明；MTRCH2 见 D 段。

## B 回执重试上限 + 幂等 + 空备注不回执

- 修法：`duty_channel_reminder.py` 回执发送失败 → 最多重试 3 次（跨 tick 计数进 state），之后记 `receipt_gave_up` 并写 `.ops` 告警一行（值守下次人工补）；每次重试前查 `inbox.db` 该群会话最近 10 分钟是否已有**同文本且带 mid** 的 out 行 → 有则视为已发、标 receipted；`note` 为空或只有「（无备注）」的 report 包不回执（只登记）。
- 验收：模拟 502 三次 → 第四次不再发；模拟「已有带 mid 行」→ 直接标 receipted；空备注包 → 无回执。

## C send 失败不留幻影 out 行 + 502 带原因（引擎侧，随 1.0.79）

- 修法：`src/web/routes/unified_inbox_send_routes.py` 失败分支（L814 / L865 / L877 / L1311 附近 `HTTPException(502, …)`）：适配器返回失败 → 不写 out 行，或写 `status=failed` 且工作台气泡标红「未送达 · 原因」；`detail` 必带原因（适配器异常文本 / 「适配器未就绪（后端启动中）」/ 超时），补进 M-3 D 四分类；日志 `[send] fail conv=… reason=…`。**该文件有他人在途 hunk，用 stage_hunks；若冲突不可解，写落点表交下一班。**
- 存量：把 13:47–14:09 那 12 条无 mid 的 out 行标 `failed`（dry-run 列出行 id 再改），别让工作台继续显示「已发 12 条」。
- 验收：关掉适配器发一条 → 502 带原因 + 无幻影行 / 有 failed 行标红。

## D 归位 + 回访

- MTRCH2 从 #106 摘出：新立 **#N「1.0.77 出厂态四件：Web 监听 0.0.0.0 / clean 包缺 handoff yaml / fatex.db 进包 / 首启体验档文案 None」**（reporter skuio，P1，version 1.0.77.0，去向 P-4）；`duty_evidence received --diag MTRCH2`；#106 note 一行「MTRCH2 为 1.0.77 验收对照，误挂已摘出 → #N」。
- #259 note 汇总六份报告去向：Y3C6PS / U4ZAD7 → P-1；ZH3ZQ5② / H3BAJD → P-2；6SRA2B → P-3；34585H → P-6；ZH3ZQ5③ → P-1 B。#252 挂的 BBRGMG / A7PB2F 同样注记（→ P-3 / P-2 F）。
- 回访两条（先 dry-run，过闸，每条带工单号）：① #259：「1.0.78 已发出去的消息零破折号（O-1 B 在发送门）；草稿预览里的净化、登录 / 切档不群发、停联随备份、发图不说谎四件随 1.0.79；你现在别按账号开全自动」；② #254 / 新单：出厂态四件与首装 KB 随 1.0.79；后台监听在 1.0.79 前的临时办法：不在公共 Wi-Fi 上开后台，或在设置里……（**不教改配置文件**，guard 会拦——只写「1.0.79 提供开关」）。
- 收各线回访文案进 `docs/发版对账_v1.0.79_P5.md` §回访清单，交 R79。

## 收工

- 落点表 `docs/发版对账_v1.0.79_P5.md`；`docs/P批_进度总账.md` 只改 P-5 行；`agent_probe.ps1 -Done`。

## 开新对话粘贴

```
读 docs/指令_P-5_值守链三修与归位_2026-09-08.md，按它做 P-5（#106 #259 #254：duty_auto_ticket `#N` 只在挂单上下文匹配、fixed/closed 单不挂不升、复原 #106 严重度 / duty_channel_reminder 回执重试上限 3 + 查收件箱幂等 + 空备注不回执 / unified_inbox_send_routes 失败不留幻影 out 行且 502 带原因 + 存量 12 行标 failed / MTRCH2 从 #106 摘出新立出厂态四件单去向 P-4 / #259 #252 六份报告去向注记 / 两条回访）。预算 $80。先读 docs/修复指令_P批_并行_2026-09-08.md 的归属矩阵，再 scripts\agent_probe.ps1 -Intent 登记；台账写入先 dry-run；对外只经 engines/chengjie/tools/duty_reply.py，每条带工单号、只给版本号；unified_inbox_send_routes.py 有他人在途 hunk，只用 tools/stage_hunks.py --mine 暂存自己的，冲突不可解就写落点表交下一班。每个 commit 带工单号并立刻补落点表 docs/发版对账_v1.0.79_P5.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/P批_进度总账.md 的 P-5 行并 agent_probe -Done。
```
