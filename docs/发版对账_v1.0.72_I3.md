# 发版对账 v1.0.72 — I-3（工作目标不推进：#153 / #65 / #40 / #38 / #152②）

> 指令：`docs/指令_I-3_目标推进_2026-09-04.md`。总账：`docs/I批_进度总账.md` I-3 行。
> **本条不发版、不重启实例。** `.py` 改动攒批等下个重启窗装载。
> 每完成一项立刻追加一行；未完成项也留一行写「进行中：已改 X，还差 Y，下一步 Z」。
> 下一个账号接手：先读本表逐行核对，再跑 `git -C D:\boundless log --oneline -15 -- engines/chengjie` 确认 commit 真在。

## 落点表

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注/剩余 |
|---|---|---|---|---|---|
| C1 节奏档接进主规划链 | #65 #153 #152② | `e95994c4` | 主链 `pace` **HEAD 已接线**（`service.refresh_goal` 用 `slot_key`/`planner_thresholds`/`sprint_push`；`sprint_ticker` 只扫 sprint；`planner.py` 刻意不 import `pace`——`slot_key` natural 回落 `day_key`，循环导入）。本项补的是**入口失真 + 进度口径 + inbox 无时刻锚点**：① `pace.default_pace_for_deadline`：没给 pace 按跨度落档（≤2.5h session / <20h today，对齐 clamp 区间；白名单外仍 natural）；② `goal_routes` 创建口消费它，子日跨度原样入库（不再先当 natural 夹成 1 天）；③ `ledger.settle_goal` custom 分支：sprint 时按 `extras.sprint_beats/sprint_cap` 爬进度（封顶 0.9，不自动完成）；natural 不带这两个键 → 一字不变；④ `refresh_goal(inbound_turn=)`：inbox 读不到入站时刻时把本条开口锚在 now，避免 session 整段 `s:0`；`build_block_for_chat` 在 `inbound_text` 非空时传入 | `test_goal_pace` + `test_goal_planner_ledger` + `test_goal_service` + `test_goal_routes` + `test_goal_sprint_ticker` **209 passed**。验收：60 分钟 custom 3 个入站回合内 ≥1 次注入且不 expired；3 小时 today 16 分钟时钟进度 <15%，1 拍后 ≥20% 且第 2 拍继续爬；`test_time_only_progress_never_auto_done` 仍 `progress==0.95`；`beat_rejected` ≥2 → push=none 仍在 | ⚠ 指令字面「<1 天全 session / 1–2 天 today」**未照做**：session clamp 15–120 分，硬套会把 3–8 小时目标夹成 2 小时。阈值跟 clamp 走。`planner.py` 只改 docstring。坐席表单不暴露 pace（写 `params.pace`）。**交 I-2**：无需 `unified_inbox.html` / `store.py` 改动。装载等重启窗 |
| C2 摸底类目标软提醒改硬约束 | #38 #40 | `b24266ea` | `profile_slots.resolve_inject_gap`：每轮只带登记序/勾选序**一个**未填槽；有入站则按 `_gap_asked` 轮换已问仍空的槽（同条入站指纹不轮，防 A/B 双链连耗两槽）。`context_block` / `merged_beat_intent` 文案钉死「像朋友闲聊，不要像查户口，一轮只问一个」。`service.build_block_for_chat` 注入成功后把 asked 写回 `goal.params`。无勾选时取全 TRACKS 第一空槽（不再默认一次列两个 bant）。未碰 `unified_inbox.html` / `store.py` | `test_goal_wiring` + `test_goal_acquire_funnel` + `test_goal_context_block` **90 passed**。验收：5 轮 5 个不同未填槽；同条入站不轮；已采年龄不再问 | **交 I-2**：无需前端/store。装载等重启窗 |
| C3 画像归纳兜底（过 grounding） | #38 #40 | `f4d3485b` | `profile_llm.ground_extracted` 改走只读复用 `memory_grounding.fact_grounded_in_user_msg`（删自制 bigram 阈值，两套会分叉）。过接地的 LLM 值入库 `src=llm_pending`，`facts_line` / `slot_src` 标「待确认」不丢弃。事故金标（大阪/不用上班/深夜在线）一条不入档。未改 `memory_grounding.py`、未碰 `unified_inbox.html` / inbox `store.py` | `test_goal_wiring` 新增 3 例 + `test_ground_extracted_*` / `test_run_llm_capture_*` / `TestProfileFactsAndStrategy` / `test_memory_grounding` **61 passed** | **交 I-2**：画像卡若要坐席看见「待确认」chip，读 `fields.*.src==llm_pending`（`slot_src` 已认）。装载等重启窗 |
| C3 复核补丁：超短值字面锚定 | #38 | （本提交） | 复核实测漏洞：`fact_grounded_in_user_msg` 对取不出内容 token 的值（单 CJK 字 / 单位数，如「3万」「男」）**保守放行**——那是长句记忆的「无从判断」分支，画像值却常这么短：原话「公司有5个人」，编造「预算 3万」「性别 男」全过。`profile_llm._short_value_anchored`：规范化后 ≤3 字的值额外要求是原话字面子串；长值仍完全交护栏判。护栏本身照样必过、未改 `memory_grounding.py` | `test_goal_wiring::test_profile_llm_short_values_need_literal_anchor`（钉护栏放行 + 补丁拦截 + 真短值照常入档）；I-3 八个相关测试文件 **435 passed / 1 failed**——唯一红 `test_goal_growth_loop::test_proactive_goal_opener_guarded` 为**预存红**（07-29 断言源码字面 `if _photo_plan and …`，`proactive_topic.py` 自 08-27 起已是 `and _photo_plan and …`），非 I-3 范围 | 预存红交 proactive 线：改断言字面或按 `tests/test_fixture_time_bombs.py` 那套登记留债 |
