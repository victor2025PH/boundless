# 发版对账 v1.0.85 · Q-36 客户「提议发图」不被当索图拒 + 入站图描述不夸大（#313 2JK95C · #318 VV7BRY「Marina」部分）；群拟稿只复核不立项

> 指令：`docs/指令_Q-36_入站图描述不夸大与客户提议发图意图与群聊拟稿复验_2026-09-12.md`；归属矇阵见 `docs/修复指令_Q批_并行_2026-09-09.md`。**在 Q-25（`fact_gate` 共享门）/ Q-20（`recent_outbound_media` 认领）/ Q-23（`guard_context` 场景维度）之上加，不重做**；AI **自己**提议发图的 offer-accept 桥（`outbound_promise_guard.detect_media_offer / offer_accepted`）已在库，本线一行不碰。
> 执行：2026-09-12 23:1x 开工（`agent_probe -Intent "Q-36 offer_media intent + inbound caption observation fact + outbound quantifier soft-rewrite (…)"` 已登记）。**每个 commit 落地后立刻补一行。** 预算 $60。
> 红线：`request_media` 风控语义（Q-17 分级 / Q-20 D 认领）不弱化——只新增更精确的兄弟意图，带「你的照片 / your pic / 发我」领属的混合句仍按索要；B 段是软改写 + 事实锚，不是硬拦；群聊拟稿路径（`autodraft_helpers.guard_auto_draft_action` / `peer_bot_guard`）本条一行不改。

## §〇 2JK95C 根因（HEAD `037ef2c5` 复核）

日志窗 12:34–12:44（`tmp_diag/2JK95C/…report.md`）：客户 12:39「我还没吃呢，刚做了几个菜」→ AI「做了几个什么菜」→ 客户 12:41「**一会我拍照片给你看呢**」→ AI 12:42「**照片就先不发啦，先陪我聊会儿**，我这边正瘫着不想动😄 你一个人做**一桌菜**…」。两处对应两段：

1. 「拍照片给你看」在三处判定里全是**索图**：`companion_selfie.detect_selfie_request`（`_REQUEST_PATTERNS[0]` 「拍…照片」命中，`_OWN_PHOTO_GUARD` 只收固定子串「我拍个 / 我去拍」，「一会我拍照片」漏）、`outbound_promise_guard.wants_media`（`_OWN_ACTION_RE` 锚句首「我…拍」，时间副词「一会」在前锚不到 → `_WANTS_IMG`「拍照片」命中 → claim 门 `media_context` 误开）、B 线 `commitment_guard.detect_commitment`（`_MEDIA_STRONG`「sent a photo」把「I sent a photo, did you see it?」当邀约 → `handle_inbound` 出「不发照片」罐头）。能力关的人设随即按 `photo_capability._NO_PHOTO_CONSTRAINT` 的示例句「对方要照片时…『照片就先不发啦，先陪你聊会儿』」逐字回了。
2. 「几个菜 → 一桌菜」：识图 / 客户原话进 prompt 后无「只说看到的、不加量词」约束，出站也无量词 vs 原词的核对；FactGate 只有 `fact / transient` 两类，画面所见没有落点。

## 一、判据

| 项 | 规则 |
|---|---|
| `offer_media`（A） | 客户句里**领属是自己**（我的 / 我拍 / 我发 / my / mine / 私の / 内 / mía / minha…）且在提议发 / 已发 / 问要不要看 → `commitment_guard.detect_offer_media` 返回 `offer_media`；句中同时出现我方领属索要（你的照片 / your pic / 发我 / send me / 你也发张你的）→ `""`（索要优先）；否定（不发 / won't send / 送らない）→ `""`。六语词干：zh 10 / en 8 / ja 8 / ko 7 / es 7 / pt 7 |
| 处置（A） | 接受 + 期待：`inbound_enrich.build_offer_media_hint` 注入「对方在提议发 TA 自己的照片…接受并表示期待（发来看看），绝不用拒发话回应」（A/B 两线同经 `apply_inbound_enrichments`）；`photo_capability._NO_PHOTO_CONSTRAINT` / `NO_PHOTO_PERSONA_LINE` 同源加「方向区分」句；`detect_selfie_request` / `wants_media` / `detect_request` / `detect_commitment` 四处对 offer 返回空 → 不走 Stage A 发图、不开 claim 门、不进 `request_media`、不出 Q-2 拒发模板 |
| 处置句放行（A） | `commitment_guard.is_media_accept_prompt`（发来看看 / send it over / 見せて / 보여줘 / mándamela / manda aí…六语）→ `repeat_question_guard` 不当重复问句（24h 内说过也保留）、`claim_guard` 误伤保护 ③ 不当无锚引用改写 |
| 风控不弱化（A） | `detect_request` 其余四类零改动；`_REQ_MEDIA` **加严**两条：「有你的照片吗 / got any pics of you」（问有没有**我方**照片＝索要，此前 `detect_request` 漏、只有 `detect_selfie_request` 认） |

## 二、落点（每 commit 一行，落地即写）

| 项 | commit | 落点 |
|---|---|---|
| **A `offer_media` 意图 + 处置接受不走拒发 / media_promise / claim_guard**（#313） | `46de759b`（⚠ **非本线提交**：本线 00:04:3x 显式 `git add` 九文件后、`git commit -F` 之前的十几秒里，Q-31 线 `git commit` 「ledger row B」把共享 index 里本线已暂存的 9 文件一并带走——AGENTS.md 协议第 7 条形态；内容逐字是本线的（+297/-5 与暂存 stat 一致，见 `git show --stat 46de759b`），历史不改写，#313 标记由本行与后续 commit 补；此后本线 stage + commit 同一条命令原子完成） | `src/inbox/commitment_guard.py`（意图表所在文件，+184）：**新** `OFFER_MEDIA_KIND="offer_media"` / `OFFER_KINDS`、`detect_offer_media(text, lang) -> "offer_media"\|""`——六语词干表 `_OFFER_MEDIA_ZH`（10：我…拍/发(张)(我的)…给你看 / 发张我的 / 给你看(看)我的 / 要不要看我做的 / 我发了张照片看到了吗 / 看到我发的图 / 这是我拍的 / 我的照片发给你 / 我拍了几张发给你 / 粤语 我影张相畀你睇）`_EN`（8：I'll·let me·I can·gonna send/show/take you a pic / wanna see my… / I (just) sent a photo / did you see the pic I sent / here's a pic of me / can I send you a picture / I'll take a pic and send / this is me in the photo）`_JA`（8）`_KO`（7）`_ES`（7）`_PT`（7）；`_OFFER_EXCLUDE`（我方领属：你的照片 / 你也发张你的 / your pic / send me / show me yours / あなたの写真 / 네 사진 / tu foto / mándame / sua foto / me manda → 返回空，索要优先）+ `_OFFER_NEG`（不发 / won't send / 送らない / 안 보내 / no te mando / não mando → 空）；`is_media_accept_prompt(sentence)`（处置句六语：发来看看 / 拍好了发我 / 等你的照片 / 俾我睇 / send it over / show me / can't wait to see / 見せて / 楽しみ / 보여줘 / 기대 / mándamela / a ver / manda aí / quero ver…，≤80 字）。`detect_request` 的 `media` 与 `detect_commitment` 的 `media` 各加 `and not detect_offer_media(t)`（其余四类零改动）；`_REQ_MEDIA` **加严** 2 条：「有(没有)你的照片吗 / got any pics (of you)」——此前 `detect_request("有你的照片吗")` 返回 None，只有 `detect_selfie_request` 认。`src/inbox/risk_grader.py`：`CATEGORIES` 加 `offer_media`（level low / floor low / overridable False / action `accept_expect`），`grade` 入站分支 `detect_offer_media` 命中 `_add("offer_media", "offer:media", "low")`，hits 标签前缀表加 `offer:`（日志形态 `[risk] low conv= category=offer_media hits=offer:media`）。`src/web/i18n_packs/risk_grader.py`（Q-26 线在编辑，`tools/stage_hunks.py --mine offer_media --mine accept_expect` 只带本线 3 个 hunk，自证①②均 OK）：ZH / EN / ZH_HANT 各 +`inbox.risk.cat_offer_media` + `rps_rk_act_accept_expect`（`test_rk_i18n_pack_covers_every_category_level_action` 要求类别 / 动作键三语齐）。`src/companion/photo_capability.py`：**新** `OFFER_MEDIA_RULE` / `offer_media_rule()`「注意区分方向：对方说的是 TA 自己要拍 / 要发 / 已发照片给你看…接受并表示期待（好呀，发来看看），绝不用『照片就先不发啦 / 我这边发不了图 / 先陪我聊会儿』回应，也不要反过来承诺发你自己的图」并入 `_NO_PHOTO_CONSTRAINT` 尾；`NO_PHOTO_PERSONA_LINE` 同源加一句。`src/ai/companion_selfie.py` `detect_selfie_request`：`_OWN_PHOTO_GUARD` 子串护栏之后加 `detect_offer_media(text) → False`（A 线 Stage A / B 线 `plan_autosend_image` 不再把客户 offer 当索图发自拍）。`src/ai/outbound_promise_guard.py` `wants_media`：`_OWN_ACTION_RE` 之后加 offer → `""`（claim 门 `media_context` 不因客户 offer 误开——「一会我拍照片给你看」时间副词在前，旧句首锚 `^我…拍` 锚不到）。`src/inbox/repeat_question_guard.py`：**新** `_is_media_accept` → `check_repeat_questions` 对处置句 `keep`（24h 内说过「发来看看」也不删）。`src/inbox/claim_guard.py`：误伤保护 ③ `_is_media_accept(s)` → 原句保留不改写。`src/inbox/inbound_enrich.py`：**新** `build_offer_media_hint(text)`（offer → 「【对方在提议发 TA 自己的照片 / 视频给你看】」+ `offer_media_rule()`），`apply_inbound_enrichments` 汇入 `_topic_switch_hint`（A/B 两线同经）。**探针**（`%TEMP%\q36_probe.py`，不入库）：38 条六语 offer 全判 `offer_media` 且 `detect_request` / `detect_commitment` 均 None；16 条反例（有你的照片吗 → request media ✓ / send me a pic / can I see your face / show me yours / 看看你的照片 / 你也发张你的 / 我不发我的照片 / 写真送って / 사진 보내줘 / mándame / me manda / bank card / they asked for my phone number / I won't send you my photo）offer 全空；处置句 9 例判定正确。门禁：`test_risk_grader_gate` + `test_commitment_guard` + `test_outbound_promise_guard` + `test_repeat_question_guard` + `test_claim_guard` + `test_photo_capability` + `test_selfie_wiring` + `test_inbound_enrich` **356 passed**。装载：`.py` **需重启**（不因本线重启）；i18n 包热生效。**交接**：`reply_settings.html` 风控分级卡的 `RPS_RK_I18N.cats` 是模板内硬编码键表（Q-26 C–E 线工作树在编辑，本线不碰）→ 新类别行暂显 id `offer_media`，Q-26 线加一行 `offer_media: {{ (i18n or {}).get('inbox.risk.cat_offer_media', "提议发自己的照片 / 视频")\|tojson }}` 即可。 |
| **B 入站图 caption 落 FactGate observation（TTL 24h）+ 出站夸大量词软改回原词 + 注入措辞「TA 发过一张…的照片」**（#313 · #318 Marina） | `8e6fa26b` + `1399da0b`（一个逻辑改动分两提交：`8e6fa26b` 只带出了 `outbound_text_guard.py` / `skill_manager.py` 两个 `stage_hunks --apply` 文件——提交脚本 `.ps1` 里 CJK 文档路径在 PS5.1 GBK 解码下失配，同一条 `git add` 整条落空，`$ErrorActionPreference` 不拦 native 命令；`1399da0b` 立刻补齐其余六件 + 落点表。两提交间 HEAD 的 `skill_manager` / `outbound_text_guard` 对 `image_observation` 均为 try 内惰性导入，运行时无害） | `src/companion/fact_gate.py`（**只加 kind**）：`KIND_OBSERVATION="observation"` / `OBSERVATION_TTL_SEC=24h`，`check` 的 transient 分支扩成 `kind in (KIND_TRANSIENT, KIND_OBSERVATION)`——evidence（caption）必填且逐字在同一条入站 + 值锚在 caption；不做类型尺子 / 语种 / 主体守卫。**新** `src/inbox/image_observation.py`（+346）：`split_caption`（`配文\n[图片内容] 描述` → 配文 / 描述 / kind；`[视频内容]` 同表，贴纸不算观察）、`caption_from_inbound(text, media_desc)`、`gate_caption`（FactGate kind=observation）、`note_inbound(text, cid, media_desc, inbox_store, ts)` → KV `image_observation:<cid>` `{items:[{caption, kind, ts, ttl_sec}]}`（**含原 caption**，最多 3 条，与 Q-29 `peer_time` hint KV 同模式：`get_app_setting / set_app_setting`，store 缺席 → `protocol_bridge.get_inbox_store()` 回落 → 仍缺席静默；日志 `[observation] note conv= kind= ttl=24h caption=` / `[observation] drop … reason=`）、`read_observations`（过期即不存在）、`captions_from_texts`（近史用户文本里的 `[图片内容] …`，KV 缺席时的回落）、`observation_captions(cid, user_texts)`；`soften_quantifiers(text, captions) -> (text, hits)`——出站夸大量词表 `_INFLATE_ZH`（一桌(子) / 一大桌 / 一整桌 / 满桌(子) / 满满(一) / 好多 / 好几 / 一堆 / 一大堆 / 一屋子 / 一大片 / 这么多 / 那么多 / 超多 / 一大锅 / 一大盘 / 一大碗 + 可选形容词 + 名词 1–2 字）/ `_INFLATE_EN`（a whole table of / a table full of / a huge spread of / tons of / loads of / so many / a bunch of / lots of / plenty of / piles of / dozens of / countless + 名词），只在 caption 里有**该名词的小数量原词**（`[一两二三四五几数1-5] + 量词 + 名词` / `a few·a couple of·two…five + noun`）时改回原词并**保留形容词**（满桌子好菜 → 几个好菜），否则一字不动（软改写非硬拦）；`observation_line`「TA发过一张照片（识图所见：…）」/ 视频「TA发过一段视频（缩略图所见：…）」、`format_inbound_media_line`、`observation_note`；常量 `CAPTION_RULE`（媒体块系统句）/ `OBSERVATION_RULE`（注入规则句）。`src/inbox/inbound_enrich.py` `apply_inbound_enrichments`：`_media_kind ∈ {image, video}` 且有 `_media_desc` → `note_inbound`（A/B 两线同经；cid 取 `conversation_id` / `chat_id`，与出站守卫同口径）。`src/ai/ai_client.py` 媒体块（`elif _mdesc:` 分支，仅图 / 视频；贴纸块不动）：识别内容后追加 `CAPTION_RULE`「只描述 / 引用画面里看到的具体物件与数量，不加『一桌 / 满桌 / 好多 / 一堆 / 满满』类量词或程度词；画面里的地物 / 地名 / 房子只是画面所见，不当作对方的住处、所在地或专名；不确定就不说」。`src/ai/outbound_text_guard.py`（label-leak 线在编辑，`stage_hunks --mine caption_quantifier --mine _resolve_cfg_base --mine soften_quantifiers …` 5 hunk / 3 hunk 他人，自证①②均 OK）：`apply_outbound_text_guard(..., conversation_id="")` 新步 `caption_quantifier`（`goal_meta` 后、`lang_mix` 前；`observation_captions(conversation_id, user_texts)` KV 优先回落近史）→ `meta["caption_quantifier_hits"]=[{from, to, caption}]` + `_STATS["caption_quantifier"]`（`setdefault` 零流量出 0）；`resolve_cfg` 拆 `_resolve_cfg_base`（原键面逐字不变，`test_resolve_cfg_defaults` 钉住）+ 只在显式配置 `companion.outbound_text_guard.caption_quantifier` 时进键，apply 端 `get(…, True)` 默认开。`src/skills/skill_manager.py`（HOT-ZONE，label-leak 线在编辑，`stage_hunks --mine "Q-36" …` 3 hunk / 1 他人，自证 OK）：① `_apply_outbound_text_guard` 传 `conversation_id=_q36_cid` + **可观测行** `[caption-guard] conv=<cid> soft_rewrite=一桌菜->几个菜 caption=<40字>（Q-36 量词改回识图原词）`；② `_episodic_memory_extract_async` Q-25 门循环里**只加一处**：本条入站带 caption（`mu != mu_facts`）且 LLM 事实的 evidence 落在 caption 而非客户原话（`fact` 门 `unanchored / no_evidence` 且 evidence ⊂ caption）→ 改按 `kind=observation` 过门 → 不写 LLM 推断本身、写固定措辞 `observation_line(caption)`（category `observation`，`source=ai_inferred`，`source_quote=caption`），日志 `[memory] observation conv= fact= dropped_inference=`。**如实记**：抽取 LLM 的输入自 P1（08-19）起已 `strip_media_desc`，caption 事实在正常路径进不了 `_episodic_memory_extract_async`（VV7BRY 的「Marina」更可能来自 `format_recent_context` / 历史窗口里的原文 `[图片内容] …` 行被当客户话语），本处是上游一旦不剥时的安全网。`src/utils/proactive_variety.py` `format_recent_context`：对方（`direction≠out`）行经 `format_inbound_media_line` → 「TA（3小时前）：TA发过一张照片（识图所见：一栋水边的房子，旁边有码头和几艘船）」（`proactive_topic._gen_text` 主动开场唯一上下文口；截断给包装留 18 字）。`src/inbox/handoff_memory.py` `build_handoff_note`：对方有识图行（**新** `_has_peer_media_desc`）→ 「对方说过/发过」段尾附 `（OBSERVATION_RULE）`；`_fmt_row` 措辞「发了图片（画面：…）」不变（`test_handoff_memory` 钉住）。**探针**（`%TEMP%\q36_probe_b.py` / `q36_smoke_b.py`，不入库）：caption「餐桌上有几个菜：一盘青菜、一碗汤和一盘炒蛋」× 出站「你一个人做一桌菜，是自己吃还是有人一起？」→「…做几个菜…」hits `一桌菜->几个菜`；「满桌子好菜啊」→「几个好菜啊」；「好多年没见了」/「我做了一堆事情」/「lots of love」零改动；en「a whole table of dishes」→「three dishes」；`resolve_cfg(None)` 无新键、显式 `caption_quantifier: false` 关闭有效；KV 25h 后读空 → 回落 user_texts；`format_recent_context` / `build_handoff_note` 输出如上。门禁：`test_fact_gate_q25` + `test_handoff_memory` + `test_inbound_enrich` + `test_label_leak_guard` + `test_manual_outbound_extract_o2` + `test_memory_scope_o2` + `test_outbound_text_guard` + `test_peer_time_q29` + `test_proactive_variety` + `test_proactive_no_reply_backoff` **336 passed**（首轮 2 红：`test_handoff_memory` 钉「画面：」措辞 → 回退标签只加规则句；`test_resolve_cfg_defaults` 钉键面 → 改成显式配置才进键）。装载：`.py` **需重启**（不因本线重启）。 |
| **C 门禁 `tests/test_media_intent_q36.py` + 2JK95C / VV7BRY 回放 + 群拟稿复核（只写结论不改代码）**（#313） | `8652be05` | **新** `tests/test_media_intent_q36.py`（54 例 / 14 函数）：① 31 条六语 offer 参数化——`detect_offer_media == offer_media` 且 `detect_request None` / `detect_commitment ≠ media` / `detect_selfie_request False` / `wants_media ""`；`grade` → `offer_media / low / accept_expect / offer:media`；`build_offer_media_hint` 含「发来看看」「接受」且明令禁拒发话；`handle_inbound(photos_ok=False)` → `clean`（不出 Q-2 拒发罐头）；处置句「好呀，发来看看！等着看你做的菜～」经 `sanitize_no_photo_reply` / `detect_media_promise` / `detect_media_claim(media_context=True)` / `apply_claim_rewrites` 全部原样；`no_photo_constraint` 含 `OFFER_MEDIA_RULE`；六语词干表各 ≥6。② 8 条索要参数化（有你的照片吗 / 发张照片给我看看 / send me a pic / can I see your face / show me yours / 看看你的照片 / got any pics of you）→ `detect_offer_media ""` + `detect_request media` + `grade request_media / medium`；混合句「你也发张你的」/ 否定句 / 写真送って / 사진 보내줘 / mándame 不是 offer；money / contact / meet / gift 四类零改动；高级表逐字；`offer_media` low 不可覆写不在 `OVERRIDABLE`；AI 自提议桥 `offer_accepted("好呀", …) == image` 仍在。处置句：`check_repeat_questions` 对「好呀，发来看看？」（24h 内说过同句）`clean`、真重复「你在哪个城市呀？」仍 strip / rewrite；`claim_guard` 对「像你说的那几个菜，发来看看」零历史不改写、真无锚英文引用仍 rewrite；`is_media_accept_prompt` 六语 6 正 1 反。③ `soften_quantifiers`：事故出站「…你一个人做一桌菜…」× caption「餐桌上有几个菜…」→「做几个菜」hit `一桌菜->几个菜`；「满桌子好菜啊」→「几个好菜啊」；「好多年没见了」/「一堆事情」/「lots of love」/ 无 caption 一字不动；en「a whole table of dishes」→「three dishes」；`apply_outbound_text_guard` 从 `user_texts` 里的 `[图片内容] …` 取锚改回 + `caption_quantifier_hits` + `_STATS` +1；显式 `caption_quantifier=False` 关闭；无 caption 不动；`resolve_cfg(None)` 键面不变；`ai_client` 源码含 `CAPTION_RULE`。FactGate：`KIND_OBSERVATION` / 24h；caption 锚在入站 → 过；不在 / 出站行 / 无 evidence → `unanchored / no_evidence`；`fact` 类判据不变；`note_inbound` 写 KV **含原 caption**、23h 可读、25h 读空 → 回落 `user_texts`；贴纸 / 纯文本不落观察；`apply_inbound_enrichments`（monkeypatch `get_inbox_store`）真落 KV。④ **2JK95C 回放**：「一会我拍照片给你看呢」→ offer_media / 不索图 / 不开 claim 门 / `grade` low / `_topic_switch_hint` 含「提议发 TA 自己的照片」+「发来看看」；事故出站经守卫「一桌菜」→「几个菜」。**VV7BRY 回放**：`format_recent_context` 对方图行 → 「TA发过一张照片（识图所见：…）」且无 `[图片内容]` 原文；`build_handoff_note` 附 `OBSERVATION_RULE`（含「住处」）；`observation_line` 不出「Marina」；`skill_manager` 源码钉 `_episodic_memory_extract_async` 内 `KIND_OBSERVATION` / `observation_line` / `[memory] observation` 且 Q-25 `slot_or_kind="fact"` 门在前，`[caption-guard]` + `conversation_id=_q36_cid` 接线。静态钉：`commitment_guard` 两处 `not detect_offer_media(t)`、`detect_selfie_request` / `wants_media` / `grade` 各含 offer 判定；**群拟稿路径零改动**——`autodraft_helpers` / `peer_bot_guard` 源码不含 `offer_media` / `image_observation`。**顺带（#318 goal 注入）**：`src/companion/goals/profile_slots.py` `_hist_items`（Q-1 归属，**只加** 6 行）——客户行含 `[` 时先 `strip_media_desc`：复核 `goal-inject` 时发现 `slot_state` 扫客户文本命中提及词表就把槽标 `mentioned:said`，一张「海边的别墅 / 码头」照片的识图描述能让 `residence / location` 变 mentioned 并驱动 goal 追问——与 `profile_fill` / 记忆抽取 P1 同口径「只吃客户自己的话」。门禁加两例（照片行 → `unknown`；客户自己说「我住在海边的别墅」→ `mentioned`）；`test_goal_known_not_ask_264` + `test_goal_probe_active_leg_236` + `test_profile_fill_anchor_q19` + `test_goal_growth_loop` + `test_goal_acquire_funnel` + `test_goal_wiring` + `test_fact_gate_q25` **425 passed**。 |

## 二·一 群拟稿复核结论（不立项、不改代码）

skuio `7331682688` 对报障群 `-1004345824259` 11:41–12:03 连续 L1 稿 + 成人分级（`[policy] … risk_hold=adult forced=L1 was=L1 mode=review shadow=risk_hold:adult`，`[draft] … origin=auto lang=zh … preview='收到，#296 #290 #286…'`）——按 `tmp_diag/7JN493/20260912-114609_report_report.md`（skuio 11:46:09）与 HEAD 复核：

1. **版本**：该报告头 `app: 1.0.82.0`——事发时 skuio 跑的是 **1.0.82**，Q-23（`guard_context` 场景维度 + `never_auto_reply` 硬名单 + 软回应单一闸门）在 1.0.83 定格、1.0.84 09:1x 才发公共渠道，skuio 21:23 的 VV7BRY 报告头才是 `1.0.84.0`。所以 11:41–12:03 的「群里照拟稿 + 成人分级」是 **1.0.82 的旧行为**，不是 1.0.84 的缺陷复现；「群聊 / 报障群自动**发送**」在 1.0.82 也被 `mode=review forced=L1` 挡住（只拟稿不发，与指令表一致）。
2. **1.0.84 下的名单口径**：`peer_bot_guard.never_auto_reply_ids` 的 `groups` = **本实例** `bug_intake.groups ∪ notify_webhooks telegram target ∪ 显式 inbox.peer_bot_guard.never_auto_reply.groups`——报障群 / 运维群 / 值守账号 6834964252 / 8506426282 只在 **117 zhiliao 实例**的配置里；skuio 实例（客户机）没有 `bug_intake`，其 `never_auto_reply` 天然不含我们的群与账号。旁证：skuio VV7BRY 报告 19:52:56 `[care] 捕获 1 条关怀约定 contact=telegram:7331682688:8506426282`——它把我们的值守账号 8506426282 当普通客户跑关怀捕获。**结论：skuio 侧 `never_auto_reply` 群名单不含该群（且按设计也不该含——那是我们实例的名单，不是产品级常量）。**
3. **「群聊默认不拟稿」开关已在库**：`inbox.auto_draft.skip_group_chats`（`autodraft_helpers` L1616，默认 **false** = 旧行为照拟；`skip_group_chats_trust_weak_evidence` 默认 false 防 messenger 弱证据误判私聊）→ `automation_mode.group_draft_skip`。1.0.84 的 Q-23 `guard_context` 只让群消息**不评估风控 / 不打标 / 不出站**，拟稿本身仍由这个开关决定。
4. **待老板拍板的产品口径**（本线不做）：a) 出厂默认改 `skip_group_chats: true`（走 `release_defaults` 五步；代价 = 群里真有 1:1 客服需求的客户看不到草稿）；b) 保持默认 false，skuio 侧配 `inbox.auto_draft.skip_group_chats: true` 或显式 `inbox.peer_bot_guard.never_auto_reply.groups: ["-1004345824259"]`（只影响该群）；c) 都不动，接受「群里只拟稿不发」的现状。本线**一行未改** `autodraft_helpers.py` / `peer_bot_guard.py` / `automation_mode.py`（门禁 `test_static_pins_and_group_draft_path_untouched` 钉住）。

## 三、验收对照

| 验收项 | 状态 |
|---|---|
| 意图表加 `offer_media`，六语词干各 ≥6；处置 = 接受 + 期待，不走 claim_guard / media_promise / 拒发模板 | ✅ A（`commitment_guard` zh10 / en8 / ja8 / ko7 / es7 / pt7；`build_offer_media_hint` + `OFFER_MEDIA_RULE`；`handle_inbound → clean`、处置句四守卫原样） |
| `request_media` 排除领属是客户自己；风控语义不弱化 | ✅ A（`_OFFER_EXCLUDE` 我方领属优先；其余四类零改动；`_REQ_MEDIA` 只加严） |
| `repeat_question_guard` 不把「发来看看」当重复 | ✅ A（`_is_media_accept` keep；`claim_guard` 误伤保护 ③ 同源） |
| caption 注入加「只描述看到的具体物件与数量，不加一桌 / 满桌 / 好多类量词」系统句 | ✅ B（`ai_client` 媒体块 `CAPTION_RULE`） |
| caption 经 FactGate `kind=observation` TTL 24h 落事实（含原 caption） | ✅ B（`fact_gate.KIND_OBSERVATION` + `image_observation.note_inbound` KV） |
| 出站冲突量词由 `outbound_text_guard` 软改回原词并落可观测行 | ✅ B（`caption_quantifier` 步 + `[caption-guard] conv= soft_rewrite=一桌菜->几个菜 caption=` + `_STATS.caption_quantifier`） |
| #318：图描述来源的记忆事实标 `observation`；proactive / goal / 接力摘要只写「TA 发过一张…的照片」，不把画面地物当住处 / 专名 | ✅ B（`_episodic_memory_extract_async` 一处 kind 标注 + 固定措辞；`format_recent_context` 对方图行；`handoff_memory` 规则句；goal 注入：`goal-inject` 文本本身只由槽位状态 / 意图组成，不含识图原文，但槽位线索扫描曾读到识图描述 → `profile_slots._hist_items` 加 `strip_media_desc`（C 段顺带，Q-1 归属只加）） |
| `tests/test_media_intent_q36.py` + 2JK95C / VV7BRY 回放 | ✅ C（52 例） |
| 群拟稿只写复核结论不改代码 | ✅ §二·一（1.0.82 旧行为 + 本实例名单口径 + `skip_group_chats` 开关已在库 + 三选一待拍板） |
| 每 commit 带 #313 / 显式 `git add` / 提交前 index 为空 | ⚠ A 段被 Q-31 线 `46de759b` 顺手带走（见 A 行，#313 由 B/C 提交与本表补）；B/C 提交均显式路径 + 原子 stage+commit，提交前后 index 为空 |

## 四、上线备注

- `.py`（`commitment_guard` / `risk_grader` / `photo_capability` / `companion_selfie` / `outbound_promise_guard` / `repeat_question_guard` / `claim_guard` / `inbound_enrich` / `fact_gate` / `image_observation` / `ai_client` / `outbound_text_guard` / `skill_manager` / `proactive_variety` / `handoff_memory`）需实例重启才生效（`deploy\instances\restart_instance.ps1`，本线未重启）；`i18n_packs/risk_grader.py` 热生效。
- 生效后盯：`[risk] low … category=offer_media hits=offer:media`（客户 offer 被认出）、`[observation] note conv= … caption=`（识图落 KV）、`[caption-guard] conv= soft_rewrite=`（量词被改回，多了说明 caption 系统句没压住模型）；`guard_stats().caption_quantifier` 进 `/api/workspace/metrics.outbound_text_guard`。
- 未做 / 交接：`reply_settings.html` 风控卡新类别行标签（Q-26 线，见 A 行）；skuio 群拟稿口径三选一（§二·一 第 4 条，老板拍板）；`goal_routes` / `persona_reply.py` / `autodraft_helpers.py` / `peer_bot_guard.py` 零改动。
