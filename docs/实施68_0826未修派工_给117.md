# 实施68：0826 未修派工（给 117 执行线立即施工）

> 值守线 2026-08-26 整理。 testers 现网包 **1.0.55**（0826 16:23 钧机已自动升 **1.056**，skuio 待确认）。  
> 已在 1.0.52~1.0.55 落码的 P0 / P1-1~P1-11 / P2-c,f,i,j,k,l **不要重做**，只修下面清单。  
> 动手前：`scripts\agent_probe.ps1 -Intent '实施68未修批'`。细节与验收仍以实施49/实施64 原文为准。

---

### 0829 06:56 LINE 只发不收·根因改判（skuio 回归报障）

**现象**：skuio 06:56 报"line 无法接收消息只能发送消息"。远程拉包（DC8F，码 523XH6）核实。

**日志事实**（logs/app/backend.log）：
- line-worker receiver 持续正常轮询：每轮 ~10.3s、clean=True，08-28 至今 6800+ 轮回，中途 4 次 HTTPSConnect 断开（21:44-22:51）均自动恢复；
- 但每一轮 **入站累计 0**——receive 长轮询（line-chrome-gw /api/operation/receive）永远返回空；
- 06:44 用户重启客户端：通讯录同步 3 条、会话占位同步 3 条成功，06:52 登录 provider 注册正常，发送可用 → **当前 token 有效**。

**改判**：此前"token 过期（code 119）需重扫"的结论已过时（用户可能已重扫，或当时探针命中的是另一账号态）。现症状=**receive 增量拉取位点（op sync revision 游标）卡死**：同步类接口全通、唯独增量收信永远空转，消息实际停在 LINE 服务器（手机端可见）。纯协议桥代码问题，与用户操作无关。

**派工（并入 P1-15，P0 级）**：
1. 检查 okline receive 游标的持久化与推进逻辑：是否存在 revision 不更新/回绕/初始化为最新位点导致永远拉空的 bug；
2. 增加自愈：连续 N 轮（如 60 轮≈10 分钟）clean 但入站 0、且同期发送/同步有流量时，强制重置 sync 游标并告警；
3. 增加监控指标：receiver 空转轮数 → 日志 WARNING + 群告警，不要再靠用户手机对账才发现。

**已群内答复 skuio**（06:59）：不用重扫，根因是我方位点卡死，今天修；期间手机端盯 LINE。


### B137 防封额度设置界面缺失（0829 07:08 skuio 实录·值守答复口径事故）

**现象**：skuio 触发"账号防封额度已用完（300 条/天）"后，值守答复"设置 → 协议自动回复调高每日上限"，skuio 找遍界面回"都没有设置的地方"。

**代码核实（0829 值守，别再凭印象答）**：
1. 拦人的是 `companion_send_gate`（账号级反封号闸门，随包默认 `target_cap: 300`、`warmup_start_cap: 100`、`warmup_ramp_days: 3`、`reserve_for_manual` 缺省 0）——**无管理 API、无设置 UI，只能改 YAML**；界面上唯一入口是被拦横幅的"白名单此客户"按钮（写 exempt_peers overlay，skuio 已加 9 个）。
2. 另一层 `protocol_autoreply.rate`（时/日限速）**后端 API 齐全**（GET/POST /api/accounts/auto-reply/config、按账号 POST /api/accounts/{platform}/{account_id}/auto-reply/override、audit、health），**但前端零调用**——全库 grep 无任何页面/JS 引用这四个端点。skuio 机器上该层已配 hourly:0/daily:0（不限），实际拦人的只有第 1 层。

**派工（P1，并入防封/额度族）**：
1. 设置页新增"账号防封额度"卡：
   - 全局默认：target_cap / reserve_for_manual / 预热参数（warmup_start_cap、ramp_days），当前值+说明；
   - 按账号覆盖：复用已有 override API 的形态，把 companion_send_gate 也补一套账号级覆盖与管理 API；
   - 当天用量条：used/cap/auto_cap（数据源 send_gate_snapshot 已有，别另算）；
   - exempt_peers 白名单管理列表（现在只能从横幅逐个加，无法查看/删除）。
2. 把 /api/accounts/auto-reply/config 等 4 个已有端点接上 UI（同一张卡里"协议限速"分节），别让 API 继续裸奔。
3. 值守流程教训（写给自己也写给下任）：答复用户"哪里能设置"之前，必须先 grep 前端确认入口真的存在。

**临时口径（已群内更正）**：横幅"白名单此客户"按钮 / 手改 config.local.yaml 的 companion_send_gate.target_cap（重启生效）/ 等滚动窗自然释放。


### 0829 07:20 skuio 三项置顶要求（客户明确排最前列）

skuio 原话："重点三个问题：1.后台人设绑定克隆语音一直无法生效；2.line登录账号之后可以正常发送信息，但是无法接收信息，3.账号每日发送信息上限要关闭。重点先修复这三个问题，排上最前列的日程"

**逐项状态与派工要求**：
1. **克隆语音（B124）**：修复代码已完成（18 测试通过），**催发版**——这是 skuio 置顶项，别再压在仓库里等大版本，尽快出下一个客户端版本让他验证。
2. **LINE 只发不收（并入 P1-15，今晨已改判 P0）**：receive 位点卡死，今天施工（详见本文件 0829 06:56 段）。
3. **防封额度可关闭（并入 B137）**：客户要求整个关闭。已给临时口径（config.local.yaml 的 companion_send_gate 段加 enabled: false 重启生效）。B137 设置卡需求补充：加"完全关闭"开关，附风险确认弹窗（关闭后无发送量护栏，封号风险自担）。
   ⚠️ 顺带修一个配置陷阱：companion_send_gate.evaluate 里 `target_cap=int(gc.get("target_cap", 15) or 15)`——**配 0 会被 `or 15` 吃掉回落成 15**，用户想用 0 表示"不限"会适得其反。改为显式语义：0/负值=不限或拒绝保存并提示，别静默变 15。

**答复记录**：07:21 已群内逐条答复（含 enabled: false 关法与 target_cap: 0 陷阱警告、风险声明）。


### 0829 08:10 补充：B137 升级为随下版必发（已对客户承诺）+ 值守红线记录

1. **B137 范围升级、优先级升 P0-随版**：skuio 客户端 1.059（当前最新发布版），在等语音克隆修复版本。值守 08:12 已对客户承诺：**『关闭/调大防封额度』做进下个版本的设置页（带风险提示的开关），与 B124 语音克隆修复同版本发布**。117 注意：这条已是对外承诺，B137 的最小可发范围=防封额度开关（关闭+调整 target_cap）+风险确认弹窗，完整设置卡（用量条、白名单管理、协议限速接线）可以随后补。
2. **值守红线（老板 2026-08-29 指令，duty_reply.py 已强制拦截）**：对外答复不得包含修改配置文件的方法，只能指引界面操作（设置页/按钮），界面没有的登记产品缺口。今晨 07:13、07:21 两条 YAML 教程发生在红线生效前，08:12 已对客户公开更正口径（不推荐自己动手改文件）。
3. 过渡期客户口径：重点客户用被拦横幅『白名单此客户』按钮放行，其余等滚动窗释放。


## 0. 不要动手（待拍板 / 挂起）

| 编号 | 原因 |
|---|---|
| B26 | 浮层撤除，待老板 |
| B32 | 计划类免逐条确认，待老板 |
| B34 | 额度自定义，待老板（与 B101 同族） |
| B48 | 通知归拢，待产品定稿 |
| B93 / P2-t | 默认人设编辑面，两案待产品 |
| B101 | 让路线硬限→提醒+用户放开，待老板（值守立场已记：服务方建议、决定权归用户） |
| B105 | 小柔语气，挂起等 P2-z |
| B111 ⑤ | 新客户默认轻采集，默认行为变更，待产品；①~④可做 |
| P2-ac / B107 | 引用回复，产品建议，可排 P2 末 |

---

## 1. 本批立即修（P1 / 候升 P0）— 按此顺序

### P1-13 / B88 群线程视图冻结（候升 P0）
钧机 1.0.55。重启后线程停在重启前最后一条，列表时间戳仍动、引擎仍收仍回，UI 不补拉。筛片：清空有效、单片 × 无效。  
**修**：打开会话对比 dialog 顶 id vs 镜像 max id，有缺口自动 sync-history；游标改最新优先；补拉失败顶部明示。筛片 × 事件绑定。  
**验收**：重启期间往群发 3 条，重启后打开全可见。证据 QTSE67 / 实施64:480。

### P1-14 / B95 管理页整组打不开
skuio 1.0.55：数据洞察 / 用量计费 / 安全合规 / 账号资产 →「正在连接…」→ 弹回后台首页。非权限/非档位。  
**修**：0816 管理面壳内加载链；超时必须报错，禁止静默回退。  
**验收**：DC8F 机上述入口全开。实施64:505。

### P1-16 / B54 输入框点不进
skuio+钧，1.0.55。**复现**：主动关怀 → 跳过关怀记录 → **取消** → 输入全死。  
**修**：该弹窗 cancel 释放 focus-trap/遮罩；全局 click 自愈 refocus + 埋点。同款弹窗扫一遍。实施64:556。

### P1-12 / B86+B81 待审/待处理点开为空（加急）
计数真、列表空。B73「去队列放行」路径因此断死。  
**修**：体检/角标直达全局待审（会话名+放行）；计数与列表同源门禁。实施64:181。

### P1-9 残留 / B70+B99 Messenger E2EE PIN 未闭环
1.0.55 半修：浮层避让有了，PIN 后 e2ee 仍未就绪。非 E2EE 能发。后续 FB「暂时阻止」。  
**修**：①PIN 后等 composer 可交互再发送 ②确认 replay 真执行 ③profile 持久化 ④指数退避+上限 ⑤检出 temporarily blocked → 账号冻结接 ban_signal ⑥**（0826 _553 B116 新增）**检出 not contactable / aren't connected 死局文案 → 该消息终局失败留痕停止重试——headed 形态下重试循环=窗口反复弹出直怼用户。diag CUN2TM。实施64:519。

### P1-15 / B98 LINE 入站不同步 —— **0827 事故升级：LINE 现已全停，本项提为最高优先**
0827 凌晨 117 断网事故中 okline(LINE) HMAC 桥崩环刷 stderr 1.46GB/40min 并写崩 app.log，值守已临时 `platform_login.line.protocol_enabled: false`（config TEMP-OFF 20260827 注释块）→ **LINE 客户（B98 报障人在内）收发全停，每停一天丢一天消息**。修复硬要求：①okline 崩溃加熔断退避（禁止无冷却重试环）②stderr/err log 轮转上限 ③修好后删 TEMP-OFF 块回开并回归 B98 入站同步。事故细节见 `D:\chengjie-instances\.ops\NOTE_from_duty_20260827_117_net_incident.md`。
**0827 11:31 观察（值守）**：okline HMAC 桥疑似网络恢复后自愈——两 LINE worker receiver 已从「连败 6 次崩环」转为「轮回 clean=True」稳定长轮询，通讯录同步正常（HMAC 桥在工作），入站累计 0（暂无新消息）。另实测 `platform_login.line.protocol_enabled: false` **并未阻止 line-worker 被拉起**（TEMP-OFF 只停了部分链路）——施工时一并查该开关的消费点。值守未回开：退避/轮转防护落码前回开=裸奔，再抖一次网就重演刷盘。防护落码后按原计划删 TEMP-OFF 块回开并回归 B98。
能发不能收。用户线索未齐也可先查入站订阅/镜像；诊断包加 LINE 日志（并 B76）。实施64:527。

### B87 手机删消息工作台不同步
消费 deleted_messages；UI 同步删；AI 可记但禁主动提已删内容（钧口径）。

### B82 无限额度期顶栏倒计时 —— **0827 值守已定案+放行，勿重复排查**
根因非显示 bug：B40「不限量」服务端实为单机日 500 万上限，DC8F 日烧 738万~865万真烧穿（0827 05:51 用户实证真停）。已设 gw_budget 覆写放行（DC8F 5000万/B990、E4DC 各 2000万，08-29 与 env 还原同批清除）。**剩余代码活**：服务端 quota 响应补 unlimited 语义（ai-gateway.ts quotaSnapshot/setBudgetOverride），客户端 quota_probe（hosted_gateway.py:1129）消费位已就绪。

### P1-17 / B113 全自动开着不回（0826 08:41 新案，待定向但先查闸）
钧 `_543`：会话「十翼」全自动绿、客户已回「Oh really? / Haha…」，AI 未跟。右栏「初识 100%」+「确认进阶」。  
**案情已收窄（0826 撤回图 _544/_545）**：09:17 自愈，非全停、非进阶闸——是 08:13 那条客户消息被跳过 ~1h 未回。修：①调该条为何被跳过（生成失败静默 / 触发判定漏 / 投递丢）；②漏回检测兜底：客户末条消息超时（建议 10min，全自动档）未回 → 自动补答并记原因入日志/横幅。验收：复演跳过场景有补答；日志能答「为什么这条没回」。

### B80 Messenger 工作台镜像重复
用户回报「全一样」→ ingest 去重（E2EE 恢复后重拉）。平台消息 id 优先。

### B85 边车 stale element click
re-resolve + 失手重试一次。若根因仍是 PIN 态，并 P1-9，勿只抹症状。

---

## 2. 本批可修（P2，无拍板依赖）

按用户可感优先：

| 项 | 内容 | 注意 |
|---|---|---|
| P2-a B60 | 会话列表滚不到「已归档」 | unified_inbox 热区，probe 协调 |
| P2-d B47 | 丢弃后编辑器锁死+内容丢 | 两套未保存状态机合一 |
| P2-h | 顶栏控件截断（skuio **0826 17:07 三催**，_574 红框=左上「聊天/客户」组）+ 英文三件/额度徽章 | 本批必修勿再顺延 |
| P2-n B4 | 客户 flavor 漏网页：AI回复策略、人设声音评测 | 藏进高级 |
| P2-o B84 | 学习队列过滤闲聊；禁客服腔回避人设题 | 与档案注入回归 |
| P2-p B89 | 客户列表昵称/备注；消息记录气泡发言人 | |
| P2-q B90 | 客户去重 platform+对端id；过滤群 | |
| P2-r B91 | SOP 种子数据 EN | |
| P2-s B92 | yaml/git 黑话 → 白话存档 | |
| P2-u B94 | 全局规则模板/占位 | 功能保留 |
| P2-v B96 | 复制视角链接 tooltip/toast | 微 |
| P2-w B97 | 原生菜单跟 UI 语言 | 与 B69 看门狗同文件，合并提交 |
| P2-x B100 | 称呼以档案为准（babe≠baba） | 与 B66 同向 |
| P2-y B102 | 「应用到…」合并 config+registry | 绕法已通，弹窗必修 |
| P2-z B103+B112 | **AI 回复**繁体+粤语+地区语感（非 UI i18n） | 默认地区中性；顺序 TW→HK→马新 |
| P2-aa B104 | 无出处禁止「你之前说过」 | 全新联系人实锤 |
| P2-ab B106 | 台词库用途说明，指路知识库 | 微 |
| P2-ad B108 | 口头禅句式防重 | 与 ae 同机制 |
| P2-ae B110 | 关怀：事实锚+钩子+阶段+防重 | |
| P2-af B111 ①~④ | 收集清单模板+进度+热络加速+回写 | ⑤不动 |
| P2-m B76 | 诊断包必须含主 app.log + LINE sidecar | |
| B77 | headed Chrome 去 --no-sandbox 灰条 + 登录引导 | |
| B78 | Web 演示位删除入口；防种子复活 | 是否继续预置交产品，删除能力要修 |
| B79+B83 | 悬浮入口去重；球上留转人工 | 同 UI 批 |
| B115 | 工具箱撤克隆录入/登记块，登记只留人设页语音区（_551，B25-① 口径落定） | 生成语音功能保留 |
| B117 | 贴纸/表情包按消息类型分流：不走照片评论链，改情绪回应（_576 贴纸被当娃照） | 与 P1-1 识图闸同域，勿互踩 |
| B118 **升P1** | 废除内心独白：①生成端硬禁小说体旁白/动作描述 ②出稿守卫命中即重生成（非剥壳）（_578 直发客户，_582 用户定调=暴露 AI 风险）；并调 _578 生成记录查答非所问 | 与 reply_echo/dup 守卫同宿主 |
| B119 | 发图全局闸自查面：人设相册开关旁显示 companion.selfie.enabled 状态+开启入口；英文要图词回归（_580/_581 两闸只开一道 AI 推脱） | 待 skuio 回全局闸状态定向 |
| B120 | 语音链路统一过全局「启用语音回复」闸：B 线主动/autosend 与坐席手动现不吃闸（_588 关闸 _589 仍出语音） | ~~先核对时间线~~ 0827 07:18 关闸态主动关怀语音直发=实锤，直接修 |
| B121 **P1加急** | 出站单语种校验：英文句检出 CJK（或反向）整条重生成（_590「I'm 我 going anywhere.」直发客户） | 与 B118 同为暴露风险，可同宿主实现 |
| B82 **已放行 0827**（值守处置完毕，剩代码尾巴） | 根因=B40「不限量」实为单机日 500 万上限，DC8F 日烧 738万~865万烧穿即停；0827 已设 gw_budget 覆写 DC8F 5000万/B990、E4DC 各 2000万（08-29 与 env 还原同批清除）。**代码尾巴**：服务端 quota 响应无 unlimited 键而客户端 quota_probe 已有消费位——补 setBudgetOverride/响应的不限量语义，横幅误导即消 | hosted_gateway.py:1129 / ai-gateway.ts quotaSnapshot |
| B122 | 草稿/填入对话框提供整段模式（标点连排不切段）；全自动 split_send 拟人节奏保留（0827 03:32 建议） | 只动草稿链，勿动出站分段 |
| B123 | bug_intake 断线补拉：启动/重连按群 last_seen 回填离线期群消息并归档图（0827 凌晨两条漏登实锤） | 值守工具链自察项 |

P2-b/e 兄弟线已装载，**勿动**，转验收。

---

## 3. 已落码勿重做（只验收 / 只残留）

P0-1 升级风暴、P0-2 hosted 激活、P0-3 hub 哑音兜底；  
P1-1 识图闸、P1-2 记忆三件、P1-3 译名保真、P1-4 发送留痕、P1-5 验证窗、P1-6 诊断上传、P1-7 呼格互换、P1-8 出站语言存服务端、P1-10 横幅原因、P1-11 人设 compact 升档；  
P2-c relay 文案、P2-f 关怀预览、P2-i 群聊 care 排除、P2-j 菜单看门狗、P2-k wellbeing 收紧、P2-l 让路线标注。

B40 无限额 08-29 到期、B61 hub 根因＝语音线，值守自持。

---

## 4. 施工回写

每项落码后在实施64「施工状态」追加一行：落码文件 + 门禁 + 是否需装包。值守拿 1.0.55+ 热更/下个包给 skuio/钧验。

---

## 0826 午后追加：B114（克隆音色回落，hub 侧）

skuio 克隆登记成功但音色默认（_549）。网关流水定案：register_spk/clone 均 hub 侧 ~23s 超时失败，客户端无病（P0-2 成立）。**归语音线（B61 提级），117 引擎线不用动**；诊断证据便签在 D:\chengjie-instances\.ops\NOTE_from_duty_20260826_dc8f_clone_fail.md。值守待 hub 恢复后回访验声。

（0826 追加：B71 危机误报样本2（_555 生日派对误报，正面愿望被标紧急）——P2-k 收紧后仍现（或为装载前旧案，先核案例生成时间 vs 0824 收紧装载点），英文词面继续收窄，样本进金标。）

（0826 17:07 补证：skuio 机 delivblock 横幅主因=「出站翻译引擎异常（近30分钟×1）」（_574 顶栏），与 _572「1 条翻译失败被拦」互证——B67 翻译链修复时一并查该失败的引擎侧原因。）

---

## 0827 午后追加：未动清单二次派工（→ 实施74 批）

> 0827 12:10 盘点：§1 P1 批 11 项已全部落码（回写见实施64；引擎侧已随 0827 重启装载，
> 壳/边车侧随 1.0.56 包）。**§2 P2 批与后升级项全部未动**，现抽出二次派工。
> 施工与进度**唯一记录**：《实施74_0827未动清单分阶段开发_唯一进度记录_2026-08.md》，
> 本节只是索引，状态以实施74 为准。

### A. 本批开发（实施74 阶段1-5）

| 阶段 | 项 | 一句话 |
|---|---|---|
| 1 | **B118（升P1）** | 废除内心独白：生成端硬禁小说体旁白 + 出稿守卫括号旁白剥除/重生成 |
| 1 | **B121（P1加急）** | 出站单语种校验：英文句检出 CJK（或反向）整条重生成，绝不出站 |
| 2 | **B120** | 自动语音链路（B线 autosend / 主动触达 / 关怀）统一过全局「启用语音回复」闸；坐席手动链刻意豁免（人的明示决定，与「人工通过≠AI自动发」同原则） |
| 2 | **B119** | 发图全局闸自查面：人设相册面板显示 companion.selfie.enabled 状态+开启入口；英文要图词回归（send a previous selfie） |
| 3 | **B117** | 贴纸/表情包按消息类型分流：不走照片评论链，改情绪回应 |
| 3 | **B122** | 草稿/填入提供整段模式（全自动 split_send 分段节奏不动） |
| 3 | **B123** | bug_intake 断线补拉：启动/重连按群 last_seen 回填离线期群消息（独立模块实现，避让 xiaozhi 报障闭环线） |
| 4 | **实施69 P1-1** | 「需人工」可解释：打标存 {reason, ts, source}，chip 悬停显示 + 一键已处理 |
| 4 | **实施69 P1-2** | 右键菜单「标签」直达：平铺当前标签带 × 直摘 |
| 4 | **实施69 P1-4** | code_stale 告警接线：worker 代码分叉 >30min 即提醒 |
| 4 | **实施69 P1-5** | relogin 前置探活：三态分诊（worker 没起/无此账号/会话过期）确定性文案 |
| 5 | **B115** | 工具箱「生成语音」面板撤克隆录入/登记块，登记只留人设页语音区 |
| 5 | **P2-h** | 顶栏控件截断（skuio 三催）+ 英文三件/额度徽章 |

### B. 本批不做（留后续批次，原因如注）

| 项 | 原因 |
|---|---|
| §0 全部（B26/B32/B34/B48/B93/B101/B105/B111⑤/P2-ac） | 待老板/产品拍板，原判维持 |
| P2-z B103+B112（繁体+粤语 AI 回复） | 大特性（语感层+回归网），单独立项 |
| P2-af B111①~④（收集清单模板） | 中大型特性，与 goals 模板库改造同批更合适 |
| P2-w B97（原生菜单跟语言）、B77（--no-sandbox 灰条）、B78/B79+B83 | 壳/边车侧，随下个客户包批（1.0.57），与热更批分开 |
| P2-a B60 / P2-d B47 / P2-n B4 / P2-o B84 / P2-p B89 / P2-q B90 / P2-r B91 / P2-s B92 / P2-u B94 / P2-v B96 / P2-x B100 / P2-y B102 / P2-aa B104 / P2-ab B106 / P2-ad B108 / P2-ae B110 / P2-m B76 / B71 | 常规 P2 长尾，实施74 批收口后按用户催报频次排下一批 |
| 实施69 P1-3（login_relay 灰度） | 运营决策（挑内测账号+FB 风控面观察），非纯开发 |
| 实施69 P2-1/P2-2/P2-3 | 按方案设定等埋点数据 |



## 0828 凌晨新报（值守登记 05:57，内测群夜间 7 条）

| 编号 | 问题 | 线索 | 优先级 |
|---|---|---|---|
| B124 | 克隆语音「登记成功但仍默认音」**回归**：钧(B990-0C62-FC1C-F668) 02:01 + skuio(DC8F) 04:36 一夜双复现；v1.0.43/45 声称修过的老问题 | 查克隆登记落库与 TTS 选路（登记成功≠生效路径）；已让两人验证重启后是否仍默认音 | **P0** |
| B125 | 混语垃圾回复复现：「You know I'm here, same 我」「im 我」多次出现（skuio 04:39，08-26 已报过一轮） | 非模型正常输出，嫌疑在回复后处理/翻译拼接环节 | P1 |
| B126 | 记忆失效：客户明说过生日，AI 反问「今天是你生日？」（skuio 04:41） | episodic/长期记忆写入或召回链 | P1 |
| B127 | AI 对未知事实乱断言（钧 02:37「你妈手艺吧」），应改提问句 | persona/system prompt 加「未知事实用提问句」硬约束 | P2 |
| B128 | 产品诉求：内置「无界科技·在线顾问」会话要求可删除/隐藏（钧 02:17） | 会话列表驻留逻辑 | P2 |
| — | 单账号发送上限（skuio 05:20）**非 bug**：protocol_autoreply 防封号限速（默认 30/时、200/天滚动窗，0=不限；protocol_autoreply.json 热生效，支持按账号覆盖）；已群内告知调法 | protocol_autoreply_limits.py / protocol_autoreply_settings.py | 已答复 |


### 0828 诊断包定案（skuio DC8F 远程取包 ZQ2YJU，v1.058，值守 06:45）

- **B124 根因（克隆音色回退）**：客户配置 avatar_voice 端点=办公室 LAN（192.168.0.117/140/173:7852），外网客户不可达。hosted_gateway 启动时已声明「克隆语音已接入网关兜底（bd2026.cc/api/ai/hub，LAN 不可达→网关优先）/托管克隆语音已就绪」，但 **TTSPipeline._try_avatar_clone 实际合成仍直连 LAN 7852 → TimeoutError → avatar_clone_unreachable → 回落 edge_tts**。即：托管克隆兜底声称就绪但合成链未接线（cf. test_voice_enroll_hosted_linkage 只保护 enroll 联动，未覆盖合成时选路）。**修复方向：_try_avatar_clone 在 LAN 不可达时改走网关 hub**。另 Whisper STT endpoint 同为 LAN(140:7854)，外网客户转写疑同瘫，一并修。
- **B126 新根因线索（生日没记住）**：同包 backend.log 全晚反复「Embedding 连续失败 N 次，熔断 120s（期间降级关键词召回）」，失败响应体是 **HTML 网页**（bd2026.cc 的 embedding 端点路由 404/返回页面而非 JSON）→ 语义记忆召回全程降级。**修复方向：服务端补 /api/ai/v1/embeddings 代理（或修路由）**；客户端熔断日志应截断 HTML 干扰。
- B124 修复前**无临时缓解**（合成链代码写死 LAN 优先），只能等 117 修复发版。


## 0828 老板定向需求（新版本，值守转达 06:50，P0）

| 编号 | 需求 | 要点 | 优先级 |
|---|---|---|---|
| R-0828-1 | **诊断包全量化（去脱敏）**：下个版本起诊断包不再打码/裁剪，带全部配置、完整日志与相关数据，方便排查问题 | 改 diagnostic_bundle.py 的redact/裁剪逻辑（redact_secrets_text、extra_tail_kb 上限）；远程拉取通道（diag-request）复用同一实现即自动生效 | P0 |
| R-0828-2 | **用户内容服务端全量留存**：所有客户端的用户聊天内容（收发文本、媒体、语音等）自动备份一份到官方服务器或 LAN 存档机，参照微信等 IM 的服务端留存模式，供事后复核与有关部门依法调取 | 需新增：客户端消息上报链路（按机器指纹/账号归档、断网补传）、服务端接收与存储（VPS bd2026.cc 或 LAN 存档机，容量规划：媒体量大建议 LAN 大盘+服务器存索引）、保留策略与调取接口 | P0 |


## 0828 诊断通道三视角方案（值守 07:00 整理，老板确认走派工）

背景：远程诊断拉取通道 0828 实战两单（DC8F 84 秒兑现定案 B124/B126；B990 pending因机器未开）。全链路代码复核：diagnostic_bundle.py / diag_upload.py / hosted_gateway.check_remote_diag / diag-request/route.ts / diag-upload/route.ts / quota 捎带位。

| # | 事项 | 改动点 | 备注 |
|---|---|---|---|
| P0-1 | 修服务端 embeddings 路由（B126 根因，全网语义记忆降级进行中） | yuntech 补 /api/ai/v1/embeddings 代理（现返回 HTML 页）；重启即全网恢复，无需发版 | **最急** |
| P0-2 | B124：克隆合成走网关兜底 + Whisper 转写同修 | tts_pipeline._try_avatar_clone LAN 全不可达时改走 {site}/api/ai/hub（hosted-voice 已声明就绪但合成链未消费）；voice_transcriber 同理（现指 LAN 140:7854） | 发版 |
| P0-3 | 诊断包全量化（R-0828-1） | diagnostic_bundle.py：内测档关 redact、白名单放开（含 DB）、日志全量分卷；diag-upload MAX_BYTES 15MB 同步提升/分卷协议 | 配 P2-9 |
| P1-4 | diag-request?list=1 带设备最近活跃时间 | join ai-gateway 流水（每 mid 最近 quota 请求时刻），登记时可预期设备何时兑现 | B990 pending 实证痛点 |
| P1-5 | 收包自动解包摘要推 TG | diag-upload 收包后跑 ERROR/WARNING 频谱 + 已知故障特征库（embedding 熔断/avatar_clone_unreachable/quota 类），今日两根因均可机扫 | 值守提效 |
| P1-6 | 上传/定位后自动回执用户 | 客户端不打扰通知「已应客服请求上传诊断」+ 修复后版本号回执 | 知情+体感 |
| P1-7 | 重点内测机每日主动巡检 | 服务端定时对白名单 fp 登记取包 + P1-5 摘要，问题先于客诉发现（本次全网记忆降级即属可巡检项） | |
| P2-8 | 包内加专项快照 | diag_upload extra_files 加 avatar_voice_stats / send_gate 快照 / 记忆库统计 | |
| P2-9 | 全量采集告知条款 | 用户协议 + 客户端一次性告知（配 R-0828-2 服务端留存） | 合规 |


### 0828 上午追加（值守 09:45）

| 编号 | 问题 | 线索 | 优先级 |
|---|---|---|---|
| B129 | 文案：后台「回复设定」页 emoji 字样改中文「表情」（skuio 09:42 截图） | 回复设定模板/文案包；按现行规范直接写中文、不做预先国际化 | P2 |
| — | LINE 只出不进用户实证（skuio 06:26）：TEMP-OFF 期间入站全停、出站另链可用，与 P1-15 预期一致；**再催**：okline 熔断+错误轮转修复后删 TEMP-OFF 块，用户在等 | P1-15 | 最高 |

| B130 | 自动回复分段句数不随机：档位「适中 2-4 句」恒出 3 句，机械感强（skuio 09:44）；需求=档位内真随机 + 偶发单句/连发拟人 | 回复分段/节奏链（与 03:32 已登记的「整段模式」同模块，建议同批改） | P1 |


## 0828 施工回执（117 开发线，11:00–12:00）

### 已落码

| 项 | 落码 | 门禁 | 是否需重启/装包 |
|---|---|---|---|
| **B124**（P0 克隆音色回退） | `src/ai/tts_pipeline.py`：新增 `_with_hosted_voice_endpoint()`，`_try_avatar_clone` 解析克隆端点时按 env 契约（`AITR_HOSTED_VOICE_BASE_URL/_FIRST`）**补入网关**；克隆全不可达的日志改为带端点清单 | `test_voice_enroll_hosted_linkage.py` 18 例（新增 7 例覆盖**合成时选路**——原文件只保护 enroll 联动，正是本次缺口） | **需重启**（.py） |
| **B125**（P1 混语垃圾回复） | `src/inbox/autosend_helpers.py`：新增 `_guard_translated_lang_mix()`，挂在出站翻译回调**出口** | `test_autosend_worker_translate.py` +6 例 | **需重启**（.py） |
| **B130**（P1 句数不随机） | 新增 `src/ai/reply_length_variety.py`（纯函数）；`persona_manager` 在档位区间文案后追加**本轮具体句数** | 新增 `test_reply_length_variety.py` 10 例 | **需重启**（.py） |
| **B129**（P2 emoji→表情） | `i18n_packs/reply_settings_page.py` + `templates/reply_settings.html` 兜底文案（ZH 5 处 / 模板 8 处；EN 段与配置键 `emoji_level` 不动） | — | **热更新，已生效** |

影响面回归：701 例全绿（语音管线 / 出稿守卫 / 人设 prompt 链 / i18n / 回复设定）。
三项 `.py` 待下一个重启窗装载。

### 状态纠偏（三条，会改变后续派工）

1. **P0-1 服务端 embeddings 路由：已闭环，勿再派工。** 线上实测
   `POST bd2026.cc/api/ai/v1/embeddings` → `401 application/json`（路由在，只是要设备
   令牌），不再是 HTML 404；客户端 `ensure_hosted_embed` 也已接进 `main.py` 启动序
   （在 `AIClient` 构建之前）。均为**今日 07:34–07:35 另一条线**完成并已部署。
   剩余动作只有验收：等内测机下份诊断包确认 backend.log 无「Embedding 连续失败…熔断」。

2b. **P1-15 LINE 实证复盘（0828 18:0x，探针实测，推翻两条既有推测）**

工具：`engines/chengjie/tools/probe_line_ops.py`（只读，可复跑；日志
`engines/chengjie/logs/probe_line_ops_20260828.log`）。token 已备份至
`D:\chengjie-instances\.ops\line_tokens_bak_20260828_181422`。

**实测数据**

| 账号 | token 年龄 | SSE 开流 | 55s 内事件 | get_profile | getLastOpRevision |
|---|---|---|---|---|---|
| `UBSt…`（A） | 4.6 天 | **200 正常** | ping ×6，**operations=0** | **OK** | 返回 `'52'` |
| `Uk4b…`（B） | 33.8 天 | **200 正常** | ping ×6，**operations=0** | **FAIL** code=119 | FAIL code=119 |

**推翻的两条推测**

- ❌「token 该续期」（`account_orchestrator.py:2059-2061` 与
  `line_recv_supervisor.py:32-35` 两处注释都这么写）——**对账号 A 是错的**：
  它 token 有效、`get_profile` 通，照样零 op。**SSE 开流要 HTTP 200 才继续
  （`okline/operations.py:102`），200 本身就证明鉴权过了**，所以「收不到 op」
  与「token 过期」是两件事，那两条注释会把排障引向错误方向（本线今天差点被带偏）。
- ❌「okline 会自动刷新 token」——okline 的 `_auto_refresh` 只挂在 **HTTP 401**
  （`client.py:99`），而 LINE 是用 **HTTP 200 + 带内错误码 119
  `Access token refresh required`** 表达的 → **钩子永远不触发**，账号 B 因此
  静默烂了 34 天。

**账号 B 的确切死因**：accessToken 过期（code 119）**且 refreshToken 也已过期**
——实弹调 `refresh_access_token()` 返回 `REQUEST_NEED_LOGIN code=10004`。
**无代码解，必须重新扫码登录。**（附带发现：该失败在 transport 层被重试了 13 次，
对永久性失败属无谓重试。）

**账号 A 的状态**：token 与 API 全正常，但网关只推 ping 不推 op，且从未下发
`connInfoRevision`。`getLastOpRevision` 返回 `'52'`，而 okline 枚举里
`EXPIRED_REVISION = 52`（`enums.py:589`）——**疑似 revision 过期需 fullSync**，
但 okline 未暴露 fullSync 调用。同样建议**重新扫码**（重登会重建 revision 与副设备注册）。

**⇒ 结论：两个账号都要重新扫码登录，这是唯一已知出路**（一个是凭据彻底过期、
一个是接收通道不推送）。okline 的 long-poll 备用通道走不通——它要的 `session_id`
只在扫码鉴权流程里存在，token 文件拿不到。

**遗留的系统性缺口（比这次故障本身更值得修）**：`line_recv_supervisor.py:32-35`
明确把「连着但永久聋」交给 `status()` 的 `last_inbound_ts`/`recv_cycles` 读数，
**但从来没人在那些读数上建告警** —— 所以它烂了一整天无人察觉。已有现成机制可复用
（`platform_session_health.record_inbox_health` 就是为「登录态在、消息读不到」的
半死态建的，自带 stall/remind）。**建议下一批把 LINE worker 的这两个读数接进去。**

---

2. **P1-15 LINE：TEMP-OFF 不是当前病因，「删块回滚」删了也不会有任何变化。**
   - 值守把 `line.protocol_enabled: false` 写在 `config.yaml`（基线），
     但 `config.local.yaml`（**overlay，优先级更高**）第 434-435 行一直是
     `line: protocol_enabled: true` —— 基线那块**从未生效**。这就是 0827 值守观察到的
     「配置失效：`protocol_enabled: false` 没能阻止 line-worker 被拉起」的谜底。
   - 实况：两个 LINE 账号 worker 都在正常跑，`receiver 轮回 720 次 … clean=True`，
     08:13 还成功同步了通讯录 2 条（**HMAC 桥是好的**），但 **入站累计 0**。
   - `account_orchestrator.py:2059-2061` 的注释写下过这条判据：「在正常轮回却一条 op
     都不来 —— **那是 token 该续期**，不是重连节奏的事」。当前实况精确符合。
   - ⇒ **建议改派**：okline 熔断退避/日志轮转属加固（仍值得做，防下次断网重演刷盘），
     但**救不了 B98**。要让用户今天收到消息，应先让两个 LINE 账号**重新扫码登录续期
     token**，再看入站累计是否起跳。同时把 overlay 里那行与基线 TEMP-OFF 的冲突清掉
     （二选一，别留两处互相打架）。

3. **B120 语音开关穿透：主链 0827 已修，剩两条边角未穿透。**
   `global_voice_reply_off()` 已接 A 线 sender（`sender.py:1358`）与主动关怀
   （`proactive_topic.py:2060`）；B 线 autosend / 协议 autoreply 走同一配置键等效禁声；
   坐席手动**刻意豁免**（人的明示决定）。**未穿透**：唱歌库存 A 线
   （`skill_manager.py` ~7383 直调 `orch.send_media(media_type="voice")`）、
   RPA 平台 `voice_output`（`voice_autosend.py:357-359` 明文备案）。属可排 P2 尾巴。

### 前置发现（下批开工可直接用，勿重新调查）

- **B127**（AI 乱断言）：`config/global_rules.yaml:100-110` **已有** `no_fact_invention`
  硬约束，连「你妈手艺吧」这个反例都在里面。若生产仍乱断言，**先查实例数据区 overlay**
  （`AITR_DATA_DIR/config/global_rules.yaml`）是否缺条/被禁用 —— 别再写一条新规则。
  B104 的 unfounded-recall 守卫**不能复用**（它管「你之前说过」类引用，语义不同）。
- **B128**（内置顾问会话删不掉）：来源＝`config.desktop.internal.yaml:962` 的 `web_chat`
  种子 + `channel_adapters.py:586` 注入 chip；删不掉是**设计**——web 不在 account_registry
  故 `removable=False`（`unified_inbox_read_routes.py:110`），remove/logout API 直接 404。
  隐藏现有解＝overlay `web_chat.enabled: false`。要做「用户可删」需产品先定：
  是给隐藏开关，还是把 web 纳入注册表。
- **B82 尾巴**：客户端 `hosted_gateway.py:1283-1299` 已就绪（认 `unlimited` 或 `budget<=0`）；
  缺口在服务端 `website/lib/ai-gateway.ts:706-736` 的 `QuotaSnapshot` **不下发 `unlimited`
  字段**。注意 `budget<=0` 两端语义冲突（客户端当不限量，服务端 `setBudgetOverride`
  当「清除覆写并回落默认」），所以**不能靠 budget=0 表达**，须新增显式字段 —— 怎么标记
  「这台机器是不限量档」需产品拍板（阈值哨兵 vs `gw_budget` 加列）。

### 新增文档

《实施79_0828新版本方向_诊断全量化与内容留存_排期_2026-08.md》——
R-0828-1 / R-0828-2 / 诊断通道 P1-4~P2-9 的拆解与排期，含三个必须先拍板的问题
（容量、合规口径与官网 FAQ 的自相矛盾、加密与密钥归属）。
