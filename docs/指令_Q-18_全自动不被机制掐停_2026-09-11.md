# 指令 Q-18：全自动不被机制掐停（成人误触 · 人工发送让位不接回 · 拦截可见可量化）

> 预算 **$160**。工单 **#292 #291 #293 #285 + #278 追加（RKEJYF / N5N6QB）**（skuio 1.0.80 → 1.0.81，09-11 15:45–17:00；95V9DE「!!! 重点修复 · 必须按用户体验重做」）。底座 `docs/修复指令_Q批_并行_2026-09-09.md`。落点表 `docs/发版对账_v1.0.82_Q18.md`。
> 前置：Q-3 / Q-15 / Q-17 已收工，本线在它们之上改。**产品原则（用户原话）：会话设成全自动 = 客户来消息就该发出去；打断自动的不能是机制自己。**

## 事实（证据 `tmp_diag\{RKEJYF,N5N6QB,SHXXJX,DMVE24,86TXNX,95V9DE,357VEU,4V69GD}`；代码只读核对）

1. **成人误触**：Mizuki × Jeeo 15:45「finally got your loving message **cum** reply」（印式英语 = 消息兼回复）→ `adult_grader._EXPLICIT` 含 `cum(?:ming|shot)?` 单词即 explicit → 自动发 soft_reply「You're making me blush…」+ `risk_hold adult 24h` + `needs_human adult:explicit:cum auto_clearable=False` + forced L1 → **全自动停 5.2 分钟**，客户被当色情对象。1.0.81 的 Q-17 把 adult explicit 降为「中」不进需人工，但 **`cum` 单词命中仍会触发软回应**——误触本身没修。
2. **人工发送让位不接回**：华哥 7340576921 顶栏「本会话：全自动」。16:46:58 坐席手发 → `[inflight] cancel by=agent_send` → 16:47:06 `abort=agent_send stage=presend`（60s 窗内，正确）；16:48–16:49 坐席两次 smart_reply 后又手发；16:50:13 客户新消息 → L2 稿 → 16:50:15 **`abort=agent_sent stage=batch`——稿被丢弃，60s 窗过后不再重试**；界面绿灯全自动、草稿躺回复工坊、没有任何「让位中」提示，用户找不到「让 AI 接回」。`autosend_worker._human_priority_gate`：`AGENT_ACTIVITY_WINDOW_SEC=60`，命中即返回原因码 → 调用方按放弃处理（stage=batch 丢稿）。
3. **不可见不可量化**：拦截只在 backend.log；用户自己写脚本数（357VEU / 4V69GD）。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 成人词表消歧** | `_EXPLICIT` 里 `cum` 改为只认 `cumming / cumshot / make (me\|you) cum / wanna cum`；新增印式英语豁免 `\w+ cum \w+`（名词 cum 名词 = and）与 `cum laude`；**单个孤立歧义词永不判 explicit**（≥2 个信号或 1 个强信号 + 施压词才 explicit，否则 ≤ flirt）；补 `test_adult_grader_gate`：「message cum reply」「summa cum laude」= mention/none，「make me cum」= explicit | `src/inbox/adult_grader.py`、tests |
| **B 让位 = 延后不是丢弃** | `_human_priority_gate` 返回 `agent_sent` / `agent_typing` 时：stage=batch / presend 一律 **defer**（稿留队，`deferred_until = last_agent_activity + 60s`），窗过后**自动复检再发**（仍是最新入站且档位仍 auto → 发；期间有更新入站 → 走新稿）；日志 `[autosend] defer=agent_sent conv= until=` 与 `[autosend] resume=agent_window_passed`；`mode_changed / risk_hold / needs_human` 语义不变 | `autosend_worker.py`（Q 批唯一动它的线）、`autosend_policy.py` |
| **C 切全自动 = 明示接回** | 顶栏切到 / 重选「全自动」→ 路由清该会话 `_agent_sent_ts` / `_agent_typing_ts` + 取消 defer 立即放行，日志 `[autosend] resume by=mode_select`；会话头让位期间出 chip「AI 让位中 · 坐席 60s 内发过 · 23s 后接回」（倒计时，点它 = 立即接回）；`reply_diagnosis` 新 finding `agent_yield`（params until / by） | 切档路由（stage_hunks）、`unified_inbox.html`（`ay-` 前缀）、`reply_diagnosis.py`、i18n |
| **D 拦截台账可见** | 回复设置页「今日拦截」卡：按原因码（adult / risk_hold / needs_human / agent_sent / agent_typing / mode_changed / work_schedule）计 24h 次数 + 最近 5 条（会话 / 时间 / 原因 / 命中词），数据来自既有 `[autosend] abort` + `[needs_human] 打标` 落库（app_settings KV 滚动 200 条，不建表）；`why_no_reply` 同步 | `reply_settings.html`（用量线在途，stage_hunks）、`reply_settings_routes.py`、新 `src/inbox/abort_ledger.py` |
| **E 门禁** | `tests/test_agent_yield_defer.py`：手发后 60s 内入站 → defer 不丢；窗过自动发；切全自动立即发；再手发再让位；`test_adult_grader_gate` 扩 6 句；纳入 R 批门禁 | tests |

## 红线

- 不放松真高风险：`risk_hold` / `needs_human` / `mode_changed` 三类闸行为一字不变；本线只改 `agent_sent` / `agent_typing` 的**后果**（延后 + 可见 + 可接回）与 `cum` 一词的判定。
- 软回应只在 explicit / pressure 出，mention / flirt 不发任何东西。
- 不引入罐头句；不动 `drafts.py` / `store.py`。

## 开新对话粘贴

```
读 docs/指令_Q-18_全自动不被机制掐停_2026-09-11.md，按它做 Q-18（#292 #291 #293 #285 #278追加：adult_grader 的 cum 只认 cumming/cumshot/make me cum，印式「X cum Y」与 cum laude 豁免，孤立歧义词永不判 explicit / autosend_worker 的 agent_sent·agent_typing 从丢稿改为 defer 到窗过自动复检再发，日志 defer=/resume= / 切全自动清让位并立即放行 + 会话头「AI 让位中 · N 秒后接回」倒计时 chip 可点接回 + reply_diagnosis agent_yield / 回复设置页「今日拦截」卡按原因码计数 + 最近 5 条，app_settings KV 不建表 / 门禁 手发后入站不丢·窗过自动发·切全自动立即发 + adult 6 句）。预算 $160。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与 Q-3 / Q-15 / Q-17 三张落点表（在它们之上改，不重做），再 scripts\agent_probe.ps1 -Intent 登记；你是本批唯一动 autosend_worker.py / autosend_policy.py 的线；reply_settings.html 与切档路由有他人在途 hunk 用 tools/stage_hunks.py --mine；risk_hold / needs_human / mode_changed 行为一字不变；不引入罐头句。每个 commit 带 #292 并立刻补落点表 docs/发版对账_v1.0.82_Q18.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-18 行并 agent_probe -Done。
```
