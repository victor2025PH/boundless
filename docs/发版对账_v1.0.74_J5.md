# 发版对账 v1.0.74 · J-5 登记链与值守工具（2026-09-05）

> 指令：`docs/指令_J-5_登记链与值守_2026-09-05.md`。预算 $120，零业务代码风险（只改
> `src/ops/bug_intake*.py` / `tools/duty_*.py` / `deploy/field-agent/AGENTS_*.md` / 测试 / 文档）。
> 全部 `.py` 改动**尚未装进 zhiliao**（末次重启 09-04 19:38）——下一重启窗装载后再看 §验收。

## 落点表

| 项 | 修什么 | 落点 | commit | 状态 |
|---|---|---|---|---|
| A backfill 二次观察 | 实时链处理过的消息被回放再观察一次（正文双写 / 假 verified 放大器） | `bug_intake_backfill.py` 共享已观察 mid 集 `bug_intake_seen.json`（`mark_seen`/`is_seen`，与 state 同目录，重启不丢；`note_group_msg_seq` 收 mid 即登记）；`run_backfill_once` 回放前 `is_seen → skip`；`observe_group_message(msg_id=)` + 进程内 `_OBSERVED_MID`/`_OBSERVED_TXT`（同 reporter+同文本+60s 近似兜底）；回放 `reporter_name` = first+last | `da1bbb86` | 在树 |
| B verify 归属 | 裸「好了」翻掉**最新**一张 fixed 单，多单待验时误翻 | `candidate_verify_tickets`（14 天内全部 fixed+notified）+ `extract_ticket_refs` / `resolve_verify_target`：只认文中 `#N`（须在候选集）或 `reply_to_msg_id == notify_msg_id`；其余落 `verify_ambiguous` 事件不翻单；`pending_verify_ticket` 保留为兼容别名 | `7be18026` | 在树 |
| C 限频只管回复不管登记 | 用户超额后真报障被拍扁，只剩 `rate_capped_report` 事件 | `parse_cfg.rate_limit_registration`（默认 False）；`_note_capped_report` 对 `_report_like`（含 verify 意图）消息就地 `observe_group_message(count_reply=False)` 立单/补录/verify；`rate_capped_report` detail 带 `[registered #N …]` 前缀＝「已登记、未回复」 | `9ec70d37` | 在树 |
| D 分类器漏句 + 已知报障人「图+字」 | 0904 五句全判 other→静默；纯文字静默无痕 | `BUG_WORDS` 补词 + `_BUG_PATTERNS`（`显示 N 条` / `<部件>是空的` / 「为什么…」+产品名词）；`is_known_reporter` + `observe(has_photo=)`：已知报障人图+任意文字 other→bug（`photo_report` 事件，截图归单）；`smalltalk_suppressed` 落 `bug_events`（前 200 字） | `1287f489` | 在树 |
| E-1 台账校正 | #138/#140/#142 假 verified | `tools/duty_ledger_fix.py --rollback-verified 138,140,142 [--apply]`：直写 SQLite 短事务（**不走** status 路由，不触发回访），保 `notify_ts`，追加 note，落 `ledger_fix` 事件；默认 dry-run | `6ed68d53` | **已 --apply**（见下） |
| E-2 收集窗 | #165：41 分钟后回着支持号消息追问被立成新单 | `ticket_for_reply` 三级反查（① 单的 `notify_msg_id` ② 进程内 `_OWN_MSG_REFS`＝本方播报文中 #N ③ `self_msg_suppressed` 事件 `mid=… refs=…` 前缀重启兜底；多单只认 reporter 自己的；`dup_of` 跟主单）→ observe 显式锚点压过 30 分钟时间窗（`reply_continue` 事件）；`trigger_verdict` 对此类回复引燃不静默 | `e4cf6896` | 在树（实时链需 reply_to 透传，见「交值守」①） |
| E-3 61 单回访 | I-6 F1② 状态核对 | `duty_ledger_fix.py --count-fixed-unnotified` → **61 单**（`status='fixed' AND notify_ts=0`，与指令预期一致）。**本条不发回访**，只记数 | — | 只读 |
| F field-agent 剧本 | skuio 把占位符原文「E2F5JB + 一句话症状」贴群且晚 21h；selfcheck 重复跑 | `AGENTS_JUN.md` / `AGENTS_SKUIO.md` v1.3：agent 输出成品句 `<码>：<一句症状>` 给用户复制 + 同条回复催发群；「同一天已自检过就不再自检」 | `5f4f68f1` | **待下发**（发群交值守，按 §K 明说覆盖 v1.2） |

## 台账改动（E-1）

命令：`python tools/duty_ledger_fix.py --rollback-verified 138,140,142 --data-root D:\chengjie-instances\zhiliao\data [--apply]`

apply 前 dry-run 计划（06:1x 实跑；下面按 `render_plan` 格式与库内实值复原，字段一致）：
```
[db] D:\chengjie-instances\zhiliao\data\config\bug_intake.db
[DRY-RUN] 假 verified 回滚计划（3 项）：
  #138  verified → fixed  notify_ts 保留（09-02 12:18, msg=1025）  note+=「[值守 0905] 假 verified 回滚：backfill 二次观察误触」  （连不上官网服务这个是什么问题…）
  #140  verified → fixed  notify_ts 保留（09-02 12:18, msg=1024）  note+=「[值守 0905] 假 verified 回滚：backfill 二次观察误触」  （克隆语音还是错误，什么时候能够修复）
  #142  verified → fixed  notify_ts 保留（09-02 09:27, msg=1012）  note+=「[值守 0905] 假 verified 回滚：backfill 二次观察误触」  （排查并修改，不然增加第二道开关影响客户体验，也增加犯错风险）
  将改 3 单——加 --apply 执行
```
`--apply` 后 06:39 复核（只读 `?mode=ro`，原样）：
```
(138, 'fixed', 1788322696.80603, '…\n[值守 0905] 假 verified 回滚：backfill 二次观察误触')
(140, 'fixed', 1788322693.7922726, '…\n[值守 0905] 假 verified 回滚：backfill 二次观察误触')
(142, 'fixed', 1788312473.8571248, '…\n[值守 0905] 假 verified 回滚：backfill 二次观察误触')
ledger_fix events: #750 '#138 verified->fixed | …' / #751 '#140 …' / #752 '#142 …'（ts 1788563975 = 06:39:35）
```
再跑同一条 dry-run（06:4x，原样）：
```
[DRY-RUN] 假 verified 回滚计划（3 项）：
  #138  ⏭ 跳过：status='fixed' 非 verified（连不上官网服务这个是什么问题）
  #140  ⏭ 跳过：status='fixed' 非 verified（克隆语音还是错误，什么时候能够修复）
  #142  ⏭ 跳过：status='fixed' 非 verified（排查并修改，不然增加第二道开关影响客户体验，也增加犯错风险）
  将改 0 单——加 --apply 执行
```
幂等成立。库内 verified 仍剩 4 单（其它人、非本批，不动）。库状态分布：closed 43 / confirmed 25 / fixed 97 / new 24 / verified 4，最大工单 #193。

## 验收（装载后）

- 门禁：`python -m pytest tests/test_bug_intake.py tests/test_bug_intake_backfill.py tests/test_duty_ledger_fix.py -q` → **130 passed**（06:4x 本树）。
  `tests/test_alert_delivery_e2e.py` 现有一条红 `phantom_unread_alert` 缺 e2e payload——**他线**（phantom unread 告警），非 J-5 引入，未动。
- 真机（装载后 24h 看 app.log / `/api/admin/bug-intake/events`）：
  - `[bug_backfill] 补登记 N 条` 后**不再**出现同句第二次 observe（正文不再双写；`observe_dup` 计数 > 0 即去重在工作）。
  - 裸「好了/可以了」不翻单，只落 `verify_ambiguous`；`#N 好了` 翻对应单（`verify_yes` detail `via=hash`）。
  - 同人一小时第 5+ 条报障：`rate_capped_report` detail 带 `[registered #N]`，且工单存在。
  - 钧式短句「为什么我发了一条显示2条」等直接立单；已知报障人图+「这个」→ `photo_report` + 截图归单。
  - `smalltalk_suppressed` 在事件面板可见（此前无痕）。

## 交值守（代码侧做不了 / 不在 J-5 文件边界）

① **实时链透传**（`src/client/trigger.py` L299 `trigger_verdict(...)` + `src/skills/skill_manager.py` L977 `observe_group_message(...)`，均在他线文件）：
  - `trigger_verdict` 现签名多了 `account_id / sender_name / msg_id / reply_to_msg_id`，trigger.py 目前一个都没传 →
    限频就地登记的单 `account_id`/`reporter_name` 为空、mid=0（A 的精确去重退化为 60s 近似兜底）；E-2 的
    「回复支持号消息续单」在实时链**不生效**（`reply_to_msg_id` 恒 0）。加四个 kwargs 即可：
    `account_id=self.user_info.id, sender_name=<first+last>, msg_id=message.id, reply_to_msg_id=getattr(message.reply_to_message,'id',0)`。
  - skill_manager 的 observe 调用已传 `msg_id=context.get("user_msg_id")`，缺 `reply_to_msg_id`——同上补一键。
  - `telegram_client.py` `fetch_history._fetch` 组 row 只取 `first_name`，不出 `reply_to_msg_id`/`last_name` →
    backfill 的 `_row_reply_to` / 全名拼接在拿到这两个键前是空转。加两个字段即可（`reply_to_msg_id: m.reply_to_message_id or 0`、`reporter_last_name`）。
② **AGENTS 重发**：两份 v1.3 发群并明说覆盖 v1.2（钧机 / 花无缺机各一份），按 §K「剧本改版必重发」。
③ 装载：下一重启窗（12:30）`restart_instance.ps1 -Instance zhiliao`（先 `restart_preflight`），本批 `.py` 与 J-1/J-2/J-3 同车。

## 刻意不做

- 不改 `SLAWatcher` / 不发 61 单回访（等老板批文案）。
- `verify` 对「回着回访说好了但另有 #N」以 #N 为准（明说 > 引用），B 门禁钉住。
- 重启后 seen 集为新文件：水位→重启之间实时链处理过的消息可能被回放一次（与旧去重同等边界，靶 `_OBSERVED_TXT` 60s 兜底 + B 的归属收紧后已不会翻错单）。
