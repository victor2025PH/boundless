# 发版对账 v1.0.78 — O-5（值守：3HNCJ7 排期答复 · WYNN22 / HM7XBA 改立新单 · #253→#252 / #255→#254 并单 · 回访口径汇总）

> 指令：`docs/指令_O-5_值守_2026-09-08.md`。底座：`docs/修复指令_O批_并行_2026-09-08.md`。总账：`docs/O批_进度总账.md` O-5 行。预算上限 $60。
> 接手方：本表只记台账与对外答复事实，**没有业务代码改动**；台账写入脚本 `tmp/o5_b_ledger.py`（默认 dry-run，`--apply` 已执行一次、幂等——重跑会因 dup_of≠0 / 状态非 new 而跳过并单，但会**再立两张新单**，勿重跑 `--apply`）。对外一律 `engines/chengjie/tools/duty_reply.py --ticket N`（bot 链自动带 🎫 工单 #N footer）。
> 执行：2026-09-08 08:21 → 08:5x。

## 0. 一句话

3HNCJ7 已答（挂 #254，只给版本号：O-1 A 停联硬停 + O-2 A 记忆基线 = 止血、随下一版 1.0.78 第一批；O-1 B/C/D、O-3 摸底主动腿、O-4 引用一期 = 1.0.78；二期 / 三期只估算排 1.0.79；出站落日志 `[outbound] punct_fix= style_fix=` 在 O-1 B；验证步骤留 1.0.78 回访）。归位四件：**WYNN22 自 #201 改立 #256（P0 → O-2）**；**HM7XBA / 5XRQ7C 自 #255 改立 #257（P1，挂 #236 族 → O-3）**；#253（六份）→ #252、#255（TDNJHQ）→ #254；#252 / #254 `confirmed` 且抬 P0。四条回声全带单号（bot 链 08:34:31–08:35:03）。回访清单见 §5——**1.0.77 07:43 已发、未带 O-1 A / O-2 A，无止血回访可交 N-5 E**；四线回访文案随各线收工填入。

## 1. 开工基线（08:21，全只读）

| 项 | 事实 |
|---|---|
| 意向板 | O-5 08:21 登记；**O-1 / O-2 / O-3 / O-4 四线同刻 ACTIVE（0.0h）**；N-5 3.6h（pre-restart 标记，A 并单等老板点头）；DY / QQ / 微信三线在途。git 索引空闲；HEAD `b256a2e3`（N-5 落点表）；docs 零脏 |
| N-5 A 范围核对 | `tmp/n5_a_merge.py` 只含 #244–#249→#243、#251→#250、#242 关单、MJGHKQ 改挂（尚未 apply）；**不含 #253 / #255**，两条并单归 O-5，不重叠。`tools/duty_auto_ticket.py` 未碰 |
| 1.0.77 | N-5 E 07:30–07:36 三渠道发布、群播报 mid 1335（07:43）。O-1 / O-2 08:2x 才开工 → **止血件未随 1.0.77**，「并入下一版」= 1.0.78 |
| 台账（改前） | #252 new P2 rc1（EC7QZM）/ #253 new P2 rc1（K9F7PU + XAM4KV FW78ZP Q9H2HM RYE8Y8 8FJDUK）/ #254 new P2 rc1（KT992X + 78W8DN 47KNBV 3HNCJ7）/ #255 new **P0** rc1（TDNJHQ + 5XRQ7C HM7XBA）/ #201 fixed（WYNN22 被自动链按「关联 FPFCRE」挂上）/ #236 new rc2 · #216 new（目标引擎族）/ #183 fixed（584AUV 挂上，正确，不动）。取证台账：#252–#255 全部 `received`（自动链登记） |
| 群水位 | 05:19 mid 1334 自动回执 47KNBV / 3HNCJ7 → #254 之后，skuio 无新报告、群无新入站；3HNCJ7 02:30 的排期问题**无人答过** |
| skuio 机 | DC8F 1.0.76.0（报告头）；1.0.77 待装（N-5 回访文案 p1 带手装链接） |

## 2. A —— 3HNCJ7 排期答复（08:34:31 发出，挂 #254 + `--status confirmed`）

- 文案 `D:\chengjie-instances\.ops\duty_0908\o5_r254_3hncj7.txt`（2,076 字；dry-run 过 `support_reply_guard` 与取证门禁后实发；`bug-intake/reply` ok，bot `tgzkw_bot` 08:34:31 落群——支持号 `[群消息] 跳过: 机器人发送者` 即到达凭证，bot 链 mid 不回传）。
- 内容对应他 3HNCJ7 的三问：
  1. **今晚 P0 批次排期**（一件事一个单号，只给版本号）：XAM4KV 停联仍连发 → #252（止血，随 1.0.78 第一批；修法 = D-O1 原文：一条告别 → 冻结 → 切人工 + 标签、risk=high 不直发、词表补四条）；WYNN22 → **#256**（止血，同班车；D-O5）；RYE8Y8 + 78W8DN → #254（D-O2 清单逐条，verbatim / 人工手发除外）= 1.0.78；Q9H2HM → #252 同线（D-O3）= 1.0.78；FW78ZP → #252 同线（D-O4 骨架 + 「拟人程度」三档）= 1.0.78；HM7XBA / 5XRQ7C → **#257**（O-3 五件 + 采集链接 #256）= 1.0.78，前置 M-7 已随 1.0.77。
  2. **47KNBV 引用回复**：#37（1.0.74）已有「连发 ≥2 带引用」基线；一期 MVP 按他的切法五条 = 1.0.78 与 #254 同走；二期 / 三期只评估、各出一份工期估算、排 1.0.79，估算出来在 #254 回他（→ O-4 D 兑现）。
  3. **出站文本落日志**：O-1 B `[outbound] punct_fix=n style_fix=n` 每条一行。
  4. **验证**：留待 1.0.78 回访（200 样例 0 em dash / 0 分号 / 0 总结尾句；停联一条告别后不再发；记忆页「自安装新增」>0；摸底目标第 1 天「主动出手 ≥1」）；1.0.78 前不用复测。
- 红线自查：无日期；版本号只出现 1.0.77（事实）/ 1.0.78 / 1.0.79；无「修改配置文件」类指引；每个去向都带 `#N`。

## 3. B —— 归位（`tmp/o5_b_ledger.py`：dry-run 08:29 → `--apply` 08:30:28）

| 动作 | 结果 | 细节 |
|---|---|---|
| 新单 ① WYNN22 | **#256** new **P0** skuio v1.0.76「1.0.76 干净安装 AI 记忆完全不抽取：intents=[]」 | `record_bug_ticket` 查重无相似开放单 → 新号；body = WYNN22 note 精简 + 五条要求 + 摘出说明（自 #201 摘出 / 去向 O-2 / D-O5 / 关联 #183 #201），916 字；`reporter_version=1.0.76`；事件 `duty_split`；`tmp_diag/WYNN22/ticket.txt` += → #256；`duty_evidence received 256 --diag WYNN22`；**#201 note** 追加「WYNN22 是另一件事、已摘出改立 #256、本单结论不变」 |
| 新单 ② HM7XBA / 5XRQ7C | **#257** new **P1** skuio v1.0.76「目标摸底主动腿：auto=3 sent_24h=0 + 第 1 天不摸 + 采集链死」 | body = HM7XBA 四因 + 5XRQ7C 一段 + 六条要求（④ 采集链写明依赖 #256）+ 摘出说明（自 #255 / 挂 #236 族 / 去向 O-3 / 前置 M-7 随 1.0.77），1,038 字；事件 `duty_split`；`ticket.txt` HM7XBA / 5XRQ7C += → #257；`duty_evidence received 257` ×2；**#236 / #216 note** 各追加「同族新单 #257，去向 O-3；本单 M-7 D 随 1.0.77」。不用 `dup_of`（#236 是 1.0.75 目标不执行、M-7 已修卡片真相；#257 是主动腿与采集链，另一件事） |
| 并单 #253 → #252 | #253 `closed dup_of=252`；#252 `report_count 1→2` | 形态照 N-5 A / `test_e2_ticket_for_reply_follows_dup_of`：`dup_of + closed` + `bug_dup` 事件；#253 body 追加去向（六码全归 #252；8FJDUK ① 归 O-1 C，②③④ N-3 已做）；#252 note 追加六码 + O-1 A~E 去向与版本；`ticket.txt` K9F7PU XAM4KV FW78ZP Q9H2HM RYE8Y8 8FJDUK 各 += → #252 |
| 并单 #255 → #254 | #255 `closed dup_of=254`；#254 `report_count 1→2`，**severity P2 → P0（随并入抬，不降级）** | #255 body 追加「TDNJHQ 归 #254；5XRQ7C / HM7XBA 已改立 #257」；#254 note 追加四码去向（em dash / 客服句式 → O-1 B/C，引用 → O-4）；`ticket.txt` TDNJHQ += → #254 |
| 严重度 #252 | **P2 → P0** | 事件 `ledger_fix`：客户明说 stop 后 stop_contact risk=high 仍直发两条，投诉与封号直接原因（底座 O-1 = P0 / D-O1）。自动链按词表判的 P2 与批次定性不符 |
| 状态 | #252 / #254 `new → confirmed` | 随 §4 回声经 `/reply` 端点 `--status confirmed` 流转（08:34:31 / 08:34:52） |

改后核对（`SELECT` 只读）：#252 confirmed P0 rc2 · #253 closed P2 dup 252 · #254 confirmed P0 rc2 · #255 closed P0 dup 254 · #256 new P0 · #257 new P1；`ticket.txt` 末行：WYNN22→256、HM7XBA→257、5XRQ7C→257、六码→252、TDNJHQ→254。`bug_events` 08:30:28 五条（duty_split ×2 / bug_dup ×2 / ledger_fix ×1）。

## 4. 回声（四条，全 `bug-intake/reply` 路径、bot 链、带 🎫 工单 footer）

| 时刻 | 单 | 文案 | 要点 |
|---|---|---|---|
| 08:34:31 | #254（confirmed） | `o5_r254_3hncj7.txt` | §2 全文；首段即「#254 已确认（P0）：em dash / 客服句式 → O-1 线 B/C，引用回复 → O-4 线；#255 已并入」 |
| 08:34:42 | #256（receipt） | `o5_r256_new.txt` | 「#256 已立（P0）… 从 #201 摘出 → 去向 O-2 线；584AUV 仍挂 #183」+ 1.0.78 验法（记忆页新增数 / 启动 `[episodic]` 白名单行） |
| 08:34:52 | #252（confirmed） | `o5_r252_confirmed.txt` | 「#252 已确认（P0）：客户识破 AI + 停联仍连发 → O-1 线；#253 六份已并入」+ A 止血随 1.0.78 第一批 / B~E = 1.0.78 + 8FJDUK 四处连带去向 |
| 08:35:03 | #257（receipt） | `o5_r257_new.txt` | 「#257 已立（P1）… 从 #255 摘出挂 #236 族 → O-3 线，前置 M-7 随 1.0.77；#255 关闭」+ 1.0.78 验法（第 1 天「主动出手 ≥1」/ `[goal-inject] target=`） |

台账 `duty_replies.jsonl` 四行 ok；每条 `[值守回复]` 已回写对应工单 note（`append_ticket_note` 截 500）。间隔 ≥10s，未触发 `near_duplicate` 409。

## 5. C —— 回访清单（1.0.78 发版时逐条发；文案栏由各线收工时填实，O-5 先按底座决策写草稿）

> 交 1.0.78 发版线（或下一班值守）。**1.0.77 未带 O-1 A / O-2 A → 没有止血回访可交 N-5 E**；N-5 回访 p2（`.ops/duty_0908/r_skuio_1077_p2.txt`）已写「#252–#255 不在这版、排下一批」，与本次答复一致，不用改。
> 口径：skuio 要结论 + 路径（设置→哪→哪）+ 验证步骤；每条带单号；只说版本号不说日期。

| 工单 | 收件人 | 文案（草稿 / 待各线定稿） | 验证步骤（草稿） | 定稿方 |
|---|---|---|---|---|
| #252（含 #253 六份） | skuio | A：客户说 stop 类话最多一条告别 → 会话冻结（不起草 / 不关怀 / 不推目标）→ 切人工 + 列表标签「客户要求停联」；risk=high 稿全自动下转人工审。C：陪伴域「私人聊天」底稿 + 客服腔词表守卫。D：设置 → 回复节奏 →「拟人程度」自然 / 偏快 / 偏慢（默认自然）。E：账号名≠人设名提醒（Vanessa / Mizuki） | 停联场景（please stop / never write me again）：仅一条告别、列表出标签、之后客户再来消息不起草；50 条拒绝·冷淡·告别场景盲评「像真人」；backend.log 每条出站 `[outbound] punct_fix= style_fix=`；起草→发出不再固定 12–13s、边车有 composing | O-1 |
| #254（含 #255 TDNJHQ） | skuio | B：确定性后处理（D-O2 清单），verbatim / 人工手发除外。O-4 一期：Telegram reply_to + WhatsApp quoted；旧消息 >30min 必引 + 10–15% 随机 + 禁引约束；设置 → 回复设置 →「引用回复」开关默认开 | 200 条样例 0 em dash / 0 分号 / 0 总结尾句 / 0 反问尾；`[quote]` 日志出现且引用命中率在档；二期 / 三期估算在 #254 回他（O-4 D） | O-1 B / O-4 |
| #256 | skuio | A（已收工 `ad5c7dbd`，O-2 落点表 §0 原话）：装上带 A 段的包并重启后，`backend.log` 应有 `产品基线已补齐进 overlay: memory.extract.enabled, memory.extract.intents, memory.extract.use_llm`（仅首次）+ `[episodic] 情景记忆已启用 · 可抽取意图 4 个：complaint, direct_chat, greeting, small_talk`；此后客户一条 direct_chat 入站 → `[episodic] schedule run user=…` 后不再出现 `skip: intent=… not extractable`。B 人工手发出站同抽 / C 私事只进客户记忆 / D 记忆页「自安装以来新增 N 条」——待 O-2 B/C/D 收工补 | AI 记忆页新增数开始涨（D 段后顶部直接显示）；启动段两行日志如左；人工手发一条含事实的消息 → 记忆页出现（B 段后） | O-2 |
| #257（#236 族） | skuio | 主动腿 auto=3 sent_24h=0 根因；第 1 天起每天 ≥1 摸底问句 + 活跃自适应；注入变硬带 target 槽；卡片「注入 N · 主动出手 N · 已采集 N/4」；「现在就问一个」按钮 | evelyn×Martin 3 天目标第 1 天卡片「主动出手 ≥1」；客户提天气 / 时间那轮 `[goal-inject] target=`；三计数分开；采集入画像依赖 #256 | O-3 |
| #254 兑现 | skuio | 二期 / 三期工期估算（各一个对话），1.0.79 排期 | 估算文本回 #254 | O-4 D |
| #236 / #216 | skuio | N-5 回访 p2 已含（M-7 卡片说真话 / 每拍可追溯随 1.0.77）；本次只加一句「主动腿另立 #257」 | —— | N-5 E（已在文案） |

## 6. 三日复盘一句话（可选，老板拍后再进群；**未发**）

> 三天（09-05 → 09-08）89 张单 / 87 张有 commit；1.0.77 今早已发、两台内测机待装；今天新开一族（O 批）：客户识破 AI 与停联硬停 / 出站去 AI 标点与客服腔 / 记忆抽取基线 / 摸底主动腿 / 引用回复，目标版本 1.0.78。

发法：`duty_reply.py --group neice --text-file <文件> --dry-run` → 老板点头 → 去掉 `--dry-run`（无工单号，纯播报，不带 `--ticket`）。

## 7. 交回 / 断点

- **O-2**：工单号 **#256**（WYNN22，P0，reporter_version 1.0.76，证据 received）；O-2 落点表 08:4x 尚未建，号已写入底座去向表 + 本表 + 总账 O-5 行；O-2 收工标 fixed 时 fix_note 写「抽取意图白名单进基线 + 启动 WARNING」即可对上 §5 回访。
- **O-3**：工单号 **#257**（HM7XBA / 5XRQ7C，P1，挂 #236 族，不用 dup_of）；采集链一条依赖 #256。
- **O-1**：工单只剩 **#252（P0，含 #253 六份）与 #254（P0，含 #255）**，#253 / #255 已 closed；C 段系统提示词那份 8FJDUK ① 已在 #252 body 注明。
- **O-4**：挂 #254；欠 3HNCJ7 一份「二期 / 三期工期估算」回 #254（O-4 D）。
- 未做 / 不做：`duty_auto_ticket.py` 未碰（N-5 在动）；N-5 A 并单未替做；三日复盘一句话未发（等老板拍）；O-3 落点表未代建（避免与该线同刻 Write 撞车，#257 在底座去向表）；O-2 落点表 08:39 已建 → 头行补 #256。
- 已花 ≈ $28。

## 落点（每个 commit 落地后立刻补一行）

| commit | 段 | 工单 | 文件 | 说明 |
|---|---|---|---|---|
| `f6a0d444` | A/B/C | #252 #254 #256 #257 | `docs/发版对账_v1.0.78_O5.md`（新）/ `docs/修复指令_O批_并行_2026-09-08.md`（去向表）/ `docs/发版对账_v1.0.78_O2.md`（头行 #256）/ `docs/O批_进度总账.md`（O-5 行） | 只有文档；台账写入在 `bug_intake.db`（08:30:28 五事件）与 `tmp_diag/*/ticket.txt`、`duty_evidence.jsonl`（不入库）；本行在 commit 后追加，随下一次 docs 提交入库 |
