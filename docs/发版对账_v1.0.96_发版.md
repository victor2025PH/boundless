# 发版对账 v1.0.96（2026-09-22）

一句话：**Q-41「不误发、不编造」出站真实性专项（P0–P3）+ 微信 PC 版多账号双开** 随一个 clean 公共包发出——公共渠道 + 内测同字节桥接 + 官网五值/更新记录 + 频道 @hykj7。

## 1. 包

| 项 | 值 |
|---|---|
| 文件 | `ChatX-Setup-1.0.96.exe` |
| 大小 | **492,628,462 B**（470 MB；比 1.0.95 多 108,379 B） |
| sha256 | `1c54e3a3007fccb8b1123f5923f93f2b13f05a7ff9b259745bf37f0035f8772d` |
| sha512（latest.yml） | `RTvuQlbZgYrltuy2KP2XxcJbMOhe4SjfSEsPgcYYi8s9qpnMnN2dGixMtuuIwA+niwPk7rEVVqLup8p0/zN2mQ==` |
| blockmap | 510,938 B |
| flavor | clean（`CHATX_ALLOW_STANDARD=1`，`resources/seed-data` 不存在，after-pack 反向断言通过） |
| 源 | 分支 `devin/1789968548-wechat-pc-multi-account` @ `7117c589`（版本号提交；其父 `9c463c33` 为 Q-41 P0–P3 + 微信 PC 双开 + 实施 102 全部内容），干净 worktree `D:\boundless-r96`（detached） |
| build-info | `commit=7117c589` `dirty_count=12` —— 12 项全部是 `desktop/build/i18n-packs-staged/*.py`，为 `build:backend` 从源码 i18n 重新暂存出来的**生成物**（库内那份是 d2d91aec 入库的旧快照），无源码脏改 |
| 后端 fingerprint | `d9498f918cc33b83…`（files=2237，built_at 2026-09-22T10:50:56Z） |
| 冒烟 | `build:backend` 内置安装版后端冒烟 **全过**；after-pack 路径长度门禁 最长 173/186 通过；随包交付物 22 项齐备 |

打包命令：`npm run build:backend` → `npm run dist:win:clean`（后端 ~5 min，electron/NSIS ~10 min）。

## 2. 本号带了什么（Bug 群 → 代码）

| 工单 | 改动 | 主文件 |
|---|---|---|
| #332 / #339 误发图 | 删 `selfie_fallback`；通用池只在显式意图（要图/承诺/接受 offer/指令）时挑图；点名场景无货**诚实落空**（按 trigger 区分话术） | `inbox/image_autosend.py` |
| #341 / #342 编造 | 出站**事实门**（无据个人情况/共同经历/第三方消息/空头承诺 → 删后重写，救不回打「需人工」`fact_gate_blocked`） | `inbox/autosend_worker.py` |
| 记忆来源 | care/主动/复聊记忆注入 `stated_only`（`memory.proactive_stated_only`），AI 推断印象不当事实说 | `inbox/skill_manager.py` `inbox/persona_reply.py` |
| 复问 | 天气问句族进 `repeat_question_guard`（24h） | `inbox/repeat_question_guard.py` |
| #340 固定开场 | 复读感叹开场自动摘掉 + 累计阈值告警 `opener_repeat_top` | `inbox/autosend_worker.py` |
| #343 翻译 | conf 低不直发（重译/HOLD `low_confidence`），中档按目标语分桶 `by_target`（`_`/`-` 归一，粤语 `yue`），`held_lang_unknown` 桶补回 | `inbox/outbound_translate.py` `inbox/outbound_lang_stats.py` |
| directive 落空 | 不对客户改口 → 坐席侧持久 `album_miss_marker`（发送成功自动清） | `inbox/album_miss_marker.py` |
| 生成图归类 | 入册写 `kind:selfie`；存量库幂等回填（首启日志 `[persona_media] 存量生成图补 kind:selfie N 条`）；`scene_kind_of` 删读时兜底 | `companion/persona_media*.py` |
| 微信 PC 双开 | 每号独立驱动、绑窗口/微信号、串号切窗、两号发送不串行；账号卡片 24h 报告 | `platform/wechat_pc/*` |

实测流程见 `实测流程_P0-P3_2026-09-22.md`（升级后**须重启一次工作台**）。

## 3. 发布链

| 步 | 状态 | 证据 |
|---|---|---|
| 公共渠道 | ✅ | `publish_chatx.ps1 -Channel public -Yes`：exe 远端 sha512 复核一致 → 指针 → pm2 rolling → R2 同步 → 公网 `latest.yml=1.0.96 manifest=1.0.96 exe=HTTP206`；`announcements.json` 带 `release-v1.0.96`；按 keep=3 剪掉 1.0.93 |
| 内测桥接 | ✅ | 同字节 → `tmp/bridge_1096`（`latest.yml` 改名 `latest-internal.yml`）`-Channel internal -Yes -NoAnnouncement`；公网 `internal/latest-internal.yml=1.0.96`，公共指针未动 |
| 官网 | ✅ | `1e84cefb` 五值 1.0.95→1.0.96 + 更新记录条目（`gen-chatx-changelog.mjs`，中英 9 条）；`gate:content` 6/6 PASS；`deploy.ps1 -FromHead` 部署完成，公网体检 healthy |
| 频道 | ✅ | @hykj7 **messageId=205**，campaign `release-1096`，按钮「立即更新」→ `/download/chatx`；只发频道（群 @hykjz 已链接自动转发，未双发） |
| 分支 | ✅ | `7117c589` 已推 origin |

公网复核（发布后）：`/downloads/latest.yml` 1.0.96；`/downloads/manifest.json` 1.0.96 / sha256 1c54e3a3…；`/downloads/ChatX-Setup-1.0.96.exe` HTTP 200 Content-Length 492628462；`/download/chatx` 指向 1.0.96；`/download/chatx/releases` 含 1.0.96。

## 4. 边界 / 未做

- 灰度实测（P0–P3 各组用例、48h 观察）需现网实例，本号发包不替代；出问题按实测流程附的配置开关回退（不必回滚安装包）。
- `i18n-packs-staged` 生成物与库内快照不一致（见 §1 dirty_count），不影响产物；后续可把重新暂存的 12 个文件入库，或把该目录改为不入库。
- 未打内测 smart 包（内测渠道用同字节 clean 包桥接，与 1.0.93–1.0.95 做法一致）。

## 5. 下一版（1.0.97）待发内容

| 提交 | 问题 | 修法 | 位置 |
|---|---|---|---|
| `d4ef0188` | 工具箱「发送语音给对方」失败提示显示 `[object Object]`，真实原因（501 平台不支持/重连中、503 渠道断开、409 发送受限）被吞 | 客户端统一解包结构化 `detail`（message/code/reason 提到顶层），发语音/发送/重发 4 处提示走同一取值 | `web/static/workspace/copilot-client.js` `cp-voice.js` `unified_inbox.html` |
| 本次 | 机器码 B990-0C62-FC1C-F668：中→英手动翻译后英文里夹杂「智聊」「西贡十五日」等中文专名（提示词"专名原样保留"被模型套到汉字上） | 翻译系统提示与 `FIDELITY_PROMPT_RULE` 增加"目标语为不同文字系统时专名须译出/音译，不得留汉字"；`TranslationService.translate()` 在非 CJK 目标语译文残留汉字时，用同一引擎带残留词提示定向重译一次（`[xlate] cjk_residue` 打点），重译失败退回首译 | `ai/translation_engines.py` `ai/translation_fidelity.py` `ai/translation_service.py` |
| 本次 | 人设「应用到…」弹窗账号列表只出 TG/Messenger/WhatsApp/LINE，微信 PC 等已接入平台无法指定人设 | `/api/personas/status` 新增 `other_accounts`（运行时登记表中除上述四平台外的全部账号，带 `platform` 字段，按登记表实际平台枚举）；弹窗按 platform 分组渲染并统一走 `registry-account/{platform}` assign 端点；assign 白名单补 `wechat` `wechat_kf` `douyin` `tiktok` `web`；JS 缓存版本 p3→p4 | `web/routes/persona_routes.py` `web/static/js/persona_apply_modal.js` `web/templates/personas.html` |
| 优化 | 人设「应用到…」弹窗：平台标签是英文（WeChat/Douyin）；registry 账号看不出在线与否，微信 PC 多窗口无法区分；路由清单测试因微信 PC 多账号路由未登记长期红 | 平台标签接 i18n（微信 / 微信客服 / 抖音 / QQ机器人 / 网页客服…，中英同步，未知平台兜底大写）；`/api/personas/status` registry 行新增 `status`，弹窗账号行显示在线 / 离线 / 待登录 / 异常 / 已封禁 / 已过期，非在线行标灰（微信 PC 多窗口各行独立）；路由清单补登记 `wechat_pc/accounts|windows`（4 条）与 `ops/inbound-ledger`，`test_admin_route_inventory` 转绿；JS 缓存版本 p4→p5 | `web/routes/persona_routes.py` `web/static/js/persona_apply_modal.js` `web/i18n_packs/persona_studio.py` `web/templates/personas.html` `tests/test_admin_route_inventory.py` |

测试：`tests/test_translation_cjk_residue.py`（新增 5 条）+ 翻译相关 42 个测试文件 649 绿 / 4 红，4 红（`test_document_file_translate.py` ×3、`test_document_translate.py` ×1，engine passthrough 期望 deepl 得 identity）在基线 `d4ef0188` 干净 worktree 上同样失败，为既有问题，未改测试。

未做（留待观察）：手动发送路径（`unified_inbox_send_routes.py`）暂未加最终「残留即阻断/确认」门——现有 `lang_mismatch` 409 只在整句含 CJK 时触发，少量专名残留仍可能放行；若重译后线上仍见残留，再接 `cjk_substantial` 门让坐席确认。
