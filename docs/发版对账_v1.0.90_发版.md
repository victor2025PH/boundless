# 发版对账 v1.0.90 — 发版线 R90（电脑微信一键副驾 / 发语音 / 接管迟到闸 / 人设不设限 → 一个 clean 包 → **公共渠道 + 内测桥接 + 官网 + 播报**）

> 规矩：`.cursor/rules/chatx-desktop-install.mdc` 「单一版本」——一个 semver = 一个 clean 包。1.0.89 已于 09-18 上架，不能同号重切。
> 范围：公共渠道 1.0.89 → 1.0.90。未点名坐席机，不 `push_chatx`。140 不是坐席。

## 0. 一句话

**1.0.90 单包** `ChatX-Setup-1.0.90.exe` **578,037,543 B** sha256 `b210ee867b26ce34977dfa0a2c4e8956bbc34a074fe36be770738704088123b8`（551 MB，clean，`MZ` 头核过，build-info flavor=clean commit=`9e8d1ade`，干净 worktree `D:\boundless-r90`）。内容 = 1.0.89 全部 + 电脑微信三步启停（冻结态 `backend.exe --wechat-pc-driver`）+ 全自动档人设语音 + 接管迟到闸 + 人设成人政策「不设限」（未成年仍转人工）。公共渠道 + 内测同字节桥 + 官网 + 频道 @hykj7 **messageId=196**（campaign `release-1090`）。

## 1. 未提交功能盘点（进包）

| 项 | 处置 |
|---|---|
| 微信 PC 副驾 supervisor / 引导三步 / 托盘 | 进包 |
| 冻结态驱动分流 + `COLLECT_ALL` uiautomation + sounddevice/soundfile | 进包（否则安装包点「启动副驾」坏） |
| 微信出站语音（队列/守卫/VB-CABLE） | 进包 |
| 入站语音请打字 | 进包 |
| Telegram 接管迟到闸 / never_auto_reply 豁免 | 进包 |
| 人设 `adult_policy: open` + 未成年硬停 | 进包（默认行为不变；「不设限」是人设档 / 机器配置） |
| 打包态封 DevTools | 进包 |
| 小智语料 + 发版说明 JSON | 进包 |
| `desktop/config.json` 本机令牌 / 坐席监控日志 / 117 运维脚本 | **不进** |

## 2. 全链

| 步 | 状态 | 事实 |
|---|---|---|
| 干净 worktree 打包 | ✅ | `npm run build:backend` + `dist:win:clean` @ `D:\boundless-r90`（ffmpeg 走与主树 junction） |
| import_sweep | ✅ | modules=1306 bad=0 |
| 小智发版金标 | ✅ | `wechat_pc_start` / `wechat_pc_voice` / `takeover_late_gate` / `adult_policy_open` 四题 GUIDED |
| gate_sweep | ⚠ | 干净树上 20 红（棘轮/对照类为主，串行复跑仍红）。`import_sweep` 绿。未对 1.0.89 逐条对照「本版引入」。 |
| publish 公共 | ✅ | `latest.yml=1.0.90` exe HTTP 200；manifest sha256 一致 |
| publish 内测桥接 | ✅ | 同字节；公共指针未动 |
| 官网 | ✅ | `deploy.ps1 -FromHead -SkipAssets` → 部署完成；五值 `118b42e9` |
| 频道 | ✅ | @hykj7 messageId=196。未发报障群（频道已链群） |
| 坐席三台 | ⏭ | 走 `latest.yml` 自动更新，未 push |

## 3. 下载

- https://bd2026.cc/download/chatx
- https://bd2026.cc/downloads/ChatX-Setup-1.0.90.exe
