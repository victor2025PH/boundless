# 指令 H（1.0.72 修复批）并行拆分版 —— 对话 A / 对话 B（值守线 0904 00:5x 交老板）

> 用法：开两个新对话，各粘一份。两份文件集互不重叠；共同纪律见每份开头。完整背景版见 `docs/修复指令_H批_1.0.72_2026-09-03.md`（v2），本文件是执行版。
> 卡住判据：开工 10 分钟内 `engines/chengjie` 下无任何文件变动＝卡了，停掉重开，只粘剩余项。

---

## 对话 A（P1 批：收件箱前端 / 翻译路由 / 草稿放行 / 审批页）

**读完立即从 A1 开工，不要先输出整体计划或总结。** 工作区 `D:\boundless`，引擎根 `engines/chengjie`。另有一个对话 B 在同一工作区并行改语音/未读/人设文件，所以：
- 只改本份列出的文件；**`store.py` 只有你能改**；不要碰 `lang_voice_route.py / speech_verdict.py / avatar_voice.py / voice_routes.py / tts_pipeline.py / unified_inbox_read_routes.py / personas.html / persona_voice.py / deploy/field-agent/*`。
- 提交只用 `git add <明确文件列表>`，**禁止 `git add -A` / `git add .`**；提交前 `git status` 核对没有别人的文件被装载；遇到 `.git/index.lock` 等 5 秒重试。
- 只跑与本项相关的测试文件，不跑全量 pytest（全量由值守在两边都完成后跑）。
- `git status` 里 `deploy/desktop/SEAT_LOG_MONITOR_LOG.md`、`monitor_seat_logs.state.json`、`deploy/instances/net_probe_117.ps1`、`@AutomationLog.txt`、`pc_runner_audit.jsonl`、`*.reqseq` 是值守侧改动，不要装载。F1/F2 已在 HEAD（`30951d10`/`e99a61c7`）不要重做。
- 每项一个 commit，信息带工单号；自动执行所有命令不用问，删数据、改生产配置除外。代码分析钉 v1.0.71 同源（`a6bfaf98` 之后的 HEAD）。**不发版。**
- 结束时告诉我：做了什么、测试结果、还能再优化什么、下一阶段建议。

### A1（P1）#154 会话「发→X」铆定被程序清空
证据 `D:\boundless\tmp_diag\YQJMZ7\backend.log`：13:11:09 sendpoint「出站语言已按铆定修正 pin=en」→ 14:50:35/14:50:45/14:58:15/14:58:24 A 线直发日语，14:50 后 sendpoint 零日志＝`outbound_lang_pin()` 为空；会话 `telegram:7331682688:8852939166`；报障人确认未动选择器。
根因三处：① `src/web/templates/unified_inbox.html` L18805 `_loadXlateForConv` 只从 localStorage 灌 `_xlateOut`（`convEntry.out||bd.out||''`），从不读服务端 `GET /api/unified-inbox/conv-xlate-out`；② L19026 `_persistXlatePrefs`→L19041 `_syncConvXlateOutSrv` 在任何翻译偏好变动（收→/预览/回译/语气）时把当前可能为空的 `_xlateOut` POST 上去；③ `src/web/routes/unified_inbox_translate_routes.py` L614-646 `lang=""` → `src/inbox/store.py` L4624 `set_outbound_lang` L4632 直接 DELETE，无审计。
修：a) 服务端删除必须显式 `clear:true`，空串 no-op 并 200 返回当前值；`set_outbound_lang` 加审计表 `conversation_outbound_lang_log(conversation_id, prev, new, source, ts)`，source ∈ `ui_select|ui_prefsync|api|import|clear`，清除打 WARNING；b) 前端 `_syncConvXlateOutSrv` 只在 `_onXlateChange` 且 `#xlate-out` 真被改时发送（source=ui_select）；`_loadXlateForConv` 先 GET 服务端值 hydrate（服务端非空优先并回写 localStorage）；`bd.out` 不再隐式钉别的会话；c) `src/ai/sendpoint_guard.py` L235 无 pin 时按会话历史我方出站主语种保守回落，不因对方一条外语整段切语言；d) 坐席侧出站译文（✎ 行）target_lang 取会话「收→」设置。
验收：POST 空不删行、`clear:true` 删行有审计；会话 A 设「发→英」→切 B 改「收→」→回 A 服务端仍 en 且选择器显示 en；清 localStorage 重开仍 en；四条日语原文 pin=en 全改写英文金标；`test_sendpoint*`/`test_tg_gap_probe*` 不红。

### A2（P1）#158 草稿审批工作台进得去出不来
证据包 `D:\boundless\tmp_diag\U7WQES`。页面 `/workspace/drafts`（`src/web/routes/drafts_routes.py` L3558）模板 `draft_review.html` extends `workspace_base.html`，「聊天」= `workspace_base.html` L845 `#nav-inbox href="/workspace"`。嫌疑：① 页在 `desktop/main.js` L1684 `openBackendPopup` 弹窗里，点 `/workspace` 被 L1723 `makeBackendPopupHandler` deny / `focusMainInbox` 吞掉；② 页级脚本错误/全局 click 拦截（#129 同页曾报脚本出错）。打包态从 L4 草稿「去审批」进入先定层。
修：审批页顶部固定「← 返回聊天」（弹窗态=关弹窗并 `focusMainInbox`；主窗态=`location='/workspace'`）；弹窗内点 shell 导航→关弹窗并聚焦主窗对应标签，不能静默 deny；导航不依赖页脚本。
验收：两条进入路径→点「聊天」与「返回」都回 `/workspace`；renderer.log 无 Uncaught；`desktop/test` 加导航用例。

### A3（P1）#160 全自动放行策略重定义 + 分类器修复 + 挂起可见

> ⛔ **本节（A3）的策略部分已于 2026-09-04 作废，不要照它施工。**
> 老板已改判为 **v2：风险扣稿一条不留、全部放行、触发只写台账**（四条硬底线全部取消，
> 含 `stop_contact` / `self_harm`），坐席侧零改动，一个月后读台账再定新规则。
> **权威指令：`docs/指令_I-1_放行策略_2026-09-04.md`。**
> 下面 A / C 两段的「硬底线」「挂起可见 / 卡片显示挂起原因 / 聊天页横幅 / holding_reply 开启」
> 全部不做；**只有 B 段（分类器整词化）仍然有效且更重要**（台账可信度与告警信噪比取决于它）。

老板决策（**已作废的 v1**）：全自动不再因客户入站命中风险词扣稿；L3 全放；L4 只留硬底线。
证据：包 `GRECHC`/`JTU34M` + 文档 `D:\chengjie-instances\zhiliao\data\protocol_media\telegram\6834964252_1148.md`：客户原文 `"Yes you did because I was happy chatting with you i didn't want to stop"` → `src/ai/chat_assistant_service.py` L246-249 `_detect_intent` `any(k in t for k in ("stop",...))` 子串命中 `stop` → 停止联系 → `_detect_risk` L278 `reasons=[stop_contact]` → high → L4，干净的 AI 稿 `"Aww, that's really sweet…"` 被扣到 21:04。现状：`src/inbox/drafts.py` L1014-1020 risk=max(入站 quick_analyze, 入站关键词)；L1192 `risk_to_autopilot` high→L4/medium→L3 无条件；L1211 `is_autosend_allowed` 只放 L2；`src/inbox/autosend_worker.py` L1544 只捞 L2；`src/inbox/holding_reply.py`（为 2744 分钟挂起事故建的缓冲话术）`inbox.l2_autosend.holding.enabled` 默认关、levels 只 `["L3"]`。
A. 放行矩阵——新模块 `src/inbox/autosend_policy.py`，纯函数 `decide(peer_risk, peer_reasons, reply_risk, reply_reasons, account_policy) -> (level, hold_reason)`；`drafts.py` 的 `auto_generate_draft`/`enrich_draft`/`risk_to_autopilot`/`is_autosend_allowed` 与 worker 捞稿全部只认它。出站稿 `reply_risk=low` 且入站任意（含 high 提及类）→ **L2 自动发**，入站风险只作上下文信号（写 draft `peer_risk/peer_reasons`；给人设产线注入守卫提示「对方提到 xx，不得报价/不得给账号或付款信息/不得越界」）；`reply_risk=medium`（AI 稿含报价/承诺/法律表述）→ L3 审批保留；其余 medium 入站全放。**硬底线（任何模式挂起，`inbox.l2_autosend.hard_floor` 可配，默认四条）**：① `stop_contact` → 不发任何内容（含缓冲话术）+ 会话标 `do_not_contact` + 待处理带原因；② `self_harm` → 挂起 + 立即通知 + 既有 crisis 话术；③ `reply_risk=high`（AI 稿自身付款/账号/密码/裸露/自残）→ L4；④ `credential_or_payment_request`（客户**索要**：`(send|give|share|tell) me (your )?(bank|card|account|password|otp|address)|把.*(卡号|密码|验证码|地址).*(发|给)`，区别于「提及」）→ 挂起。账号注册表 meta `autosend_policy:{high_inbound, medium_inbound, hard_floor}` 默认 auto/auto/四条；设置页账号行加「高风险入站也自动回」开关（默认开）。
B. 分类器：`_detect_intent` L246-249 停止联系须整词+对象或礼貌式 `\bstop\b\s+(texting|messaging|contacting|talking to me|writing)|please stop\b|don't contact|unsubscribe|leave me alone`，排除否定式 `(didn't|don't|can't|couldn't|never|won't)\s+(want to\s+)?stop`，中文正则保留；`_detect_risk` L266-284 与 `drafts.py` L29-42 `_SENSITIVE_PATTERNS` 子串→整词/词组（英文 `\b`，中文精确），`quick_analyze` 返回命中词 `risk_hits`。金标 `tests/test_drafts_risk_policy.py`：放行 `i didn't want to stop`/`can't stop thinking about you`/`non-stop`/`you look sexy tonight`/`bank holiday`/`email address`/`hotpot`/`你骗我啦`；挂起 `please stop messaging me`/`别再联系我`/`I want to kill myself`/`send me your bank card number`/AI 稿含 `transfer to this account`。
C. 挂起可见+兜底：`auto_generate_draft` L1070 / `enrich_draft` L1144 日志带 `level reasons hits hold_reason`；`draft_review.html` L217/L327 卡片显示「挂起原因：xx（命中词：yy）」（i18n `src/web/i18n_packs/draft_review_page.py` 三语 `draft.hold_reason`/`draft.risk_hit`）；全自动挂起→复用 #144 `protocol_autoreply.tag_needs_human(reason=hold_reason)` 进待处理 + `unified_inbox.html` 聊天页横幅「AI 草稿因〈原因〉挂起→去放行/改写」（i18n `inbox_workspace.py`）；`holding_reply` 默认改开、levels `["L3","L4"]`，stop_contact/self_harm 跳过泛化话术（self_harm 走 crisis）；新增 `hold_notify_min`（默认 15）挂起超时→通知中心 + 可选 `duty_alert`，不因超时自动发。
验收：`i didn't want to stop`→L2 自动发且卡片显示 peer_reasons 已放行、日志有 hits；`please stop messaging me`→零发送+do_not_contact+待处理；`I want to kill myself`→挂起+通知+crisis；AI 稿含 `send the deposit to account 1234`→L4+横幅+主管放行可用；客户 `send me your bank card number`→挂起；账号开关切 hold 后首例变挂起；`test_drafts*`/`test_autosend*`/`test_holding*` 不红。

### A4（P2）#162 观测补丁：入站日志不带账号
证据群 mid=1147：钧机 16:05-16:28「收到消息 [私聊/Xh2222222222]」全记同一用户名下，同句 14s 两次，无法区分两号各收与串号；16:09:01 `[automation] A线让位 mode=manual chat=6420823328` 而用户称现为全自动。
修：`src/client/telegram_client.py`「收到消息 [私聊/…]」日志行补 `account=<id> conv=<cid>`（B 线镜像同口径）；`automation_mode_log` 写入补 `source`（ui/api/bootstrap/inherit；落点 `src/inbox/automation_mode.py` 与 store 的写入口）。
验收：两号各收同文可按 account 分开；改档日志带 source。

### A5（收尾）
新建 `docs/发版对账_v1.0.72.md`（照 `docs/发版对账_v1.0.71.md` 模板）：写入 A1-A4 落点表 + 已在 HEAD 的 F1/F2（`30951d10`/`e99a61c7`），并留一节 `## 对话 B 落点（B 填）` 空位；随包判定同 v1.0.71 模板。`docs/值守症状族对账单.md`：族2 加第 5 层（#154 铆定被清）、族4 加第 9 层（#160 入站风险词扣稿静默）。公告口径备注一句：「AI 不再因为客户提到敏感词就停手，只在四种情况等人：对方要求停止联系、自残倾向、AI 自己要说付款/账号类内容、对方索要卡号/密码」。

---

## 对话 B（P2 批：语音链 / 未读聚合 / 人设归属）

**读完立即从 B1 开工，不要先输出整体计划或总结。** 工作区 `D:\boundless`，引擎根 `engines/chengjie`。另有一个对话 A 在同一工作区并行改收件箱前端/草稿/翻译文件，所以：
- 只改本份列出的文件；**不要碰** `store.py`、`unified_inbox.html`、`drafts.py`、`autosend_worker.py`、`chat_assistant_service.py`、`draft_review.html`、`workspace_base.html`、`desktop/main.js`、`telegram_client.py`、`unified_inbox_translate_routes.py`、`sendpoint_guard.py`。需要 store 新查询时**新建模块**而不是改 `store.py`。
- 提交只用 `git add <明确文件列表>`，**禁止 `git add -A` / `git add .`**；提交前 `git status` 核对没有别人的文件被装载；遇到 `.git/index.lock` 等 5 秒重试。
- 只跑与本项相关的测试文件，不跑全量 pytest。
- `git status` 里 `deploy/desktop/SEAT_LOG_MONITOR_LOG.md`、`monitor_seat_logs.state.json`、`deploy/instances/net_probe_117.ps1`、`@AutomationLog.txt`、`pc_runner_audit.jsonl`、`*.reqseq` 是值守侧改动，不要装载。
- 每项一个 commit，信息带工单号；自动执行所有命令不用问，删数据、改生产配置除外。代码分析钉 v1.0.71 同源。**不发版。**
- 结束时告诉我：做了什么、测试结果、还能再优化什么、下一阶段建议，并把落点表按 `docs/发版对账_v1.0.71.md` 的表格格式填进 `docs/发版对账_v1.0.72.md` 的 `## 对话 B 落点` 一节（若该文件尚不存在，先建 `docs/发版对账_v1.0.72_B.md`，写明待合并）。

### B1（P2）#161 克隆声多语种：日语乱音放行且误标「疑似无声」/ 闸门与引擎真值不一致 / STT 令牌缺失 / zl_collect 上传失败
证据群 mid=1145（钧机 1.0.71 报告 22:46-23:04）：日语 22:46/22:53/23:01 三次 CER>0.35→重合成仍「ゾオパパパ」→仅凭有能量放行并标「疑似无声」；22:54 转录「타 타 타」判幻觉丢弃后 AvatarHub STT 二级回落因令牌缺失失败；vi/th/id/ru `clone_lang_unsupported`→Edge；yue→edge zh-HK；zh/es/tl 克隆成功且校验通过；zl_collect `CODE=UPLOAD_FAILED`（VPS nginx 无请求记录＝客户端网络侧）。
修：a) `src/ai/lang_voice_route.py` L110-121 `_CLONE_BACKEND_LANGS["avatar_clone"]=(zh,en,ja,ko)` 与 `CLONE_ENGINE_LANGS`（index_tts zh/en，fish zh/en/ja/es）不一致→闸门以实际引擎真值为准（hub 钉住引擎查表；ja/ko 无可念引擎即拦→Edge），es/tl 实测可用登记进表；b) `src/ai/speech_verdict.py` L47-80：转写存在但 CER 高+有能量→新判 `garbled`（疑似念错），不放行、触发重合成/改派 Edge；`src/web/routes/voice_routes.py` 文案分「疑似无声」「疑似念错」（i18n）；c) `src/ai/avatar_voice.py` L1396-1397 `stt.token_file` 默认 `D:/faceX/mfys/secrets/service_token.txt` 是开发机路径→改为网关令牌/配置下发，缺失时启动自检告警一次，不再每次静默失败；d) 语种切标准声时语音卡明示「本条使用标准声（xx 语暂不支持克隆）」——若落点在 `unified_inbox.html`，**不要改它**，把需要的位置和文案写进结束报告交对话 A；e) `deploy/field-agent/zl_collect_jun.ps1` / `zl_collect_skuio.ps1`：上传失败重试 3 次（退避 5/15/30s）+ `-Task reupload`；失败提示改「上传失败（网络），报告已保存在 <路径>，可把 .md 直接发群」；改版后需重新以 .txt 发群明说覆盖（值守负责发，你只改文件）。
验收：日语文本走克隆链→被拦或改派，绝不产出乱音；文案分无声/念错；用户机 token 缺失只告警一次；zl_collect 断网重试通过（PS 5.1 下验证）；`test_voice_selection_gold`/`test_preview_speech_verdict` 不红。

### B2（P2）#159 账号栏幽灵未读（steven 号 tg 7092595256 三处显示 7，清单为空）
证据包 `D:\boundless\tmp_diag\G4BYWH`。族3 第 3 层：1.0.70 修的是 SLA 徽标口径与归档剔除，本层是账号级聚合与有效未读不一致。
修：`src/web/routes/unified_inbox_read_routes.py` L375-394 `_unread_aggregate_maps` 目前调 `store.sum_effective_unread_by_account`（`store.py` L2537，**不要改 store.py**）→ 新建 `src/inbox/unread_aggregate.py`，按清单同一口径（effective_unread，剔归档 / `conversation_tombstones` / 已删 / 协议号手机端残值，见 read_routes L1042 注释）用 store 的连接重新聚合 by_account/by_platform，`_unread_aggregate_maps` 改调新模块；账号栏数字可点击直达那 N 条或一键清零。
验收：构造 7 条残值会话（归档/墓碑/残值各若干）→账号栏、侧栏、会话入口三处与清单一致；`tests/test_unread_aggregate.py` 覆盖。

### B3（P2）#155 + #156 人设归属层级
#155：`src/web/templates/personas.html` L1102-1103 两格（`pe-pf-name-callpeer`/`pe-pf-name-peercalls`，i18n `src/web/i18n_packs/persona_studio.py` L788-792）——#24 二轮结论是「客户关系属性」不是人设属性：改为按联系人存（用 store 已有 `get_conv_meta`/`update_conv_meta` 存进 conv_meta，**不改 store.py**），入口放收件箱右栏客户关系面板——该面板若在 `unified_inbox.html`，**不要改它**：先做后端 API（`/api/unified-inbox/conv-meta` 读写两字段）+ prompt 注入改读联系人级 + 人设页删两格 + 存量人设值迁移为该人设已绑定会话的联系人默认值，前端入口的位置和字段写进结束报告交对话 A。
#156：`src/ai/persona_voice.py` L628-644「上线补默认人设」→新账号不自动绑定，账号栏显示「未选人设」并引导选择（账号抽屉/设置页，非 unified_inbox.html），未选期间 AI 不代答（与 #63 已有「按账号确认接管方式」联动：确认接管时一并选人设）。
验收：新登录 TG 账号→无 persona_id 写入，日志「等待用户选择人设」，UI 有入口；人设页无两格；联系人级爱称经 API 设置后注入生效（金标：AI 用 babe 称呼客户，绝不反向）。

### B4（收尾）
`docs/值守症状族对账单.md`：族1 加第 5 层（#161 日语克隆乱音放行）、族3 第 3 层填 #159；落点表交 `docs/发版对账_v1.0.72.md` 的 `## 对话 B 落点`（或 `_B.md`）。
