# 发版对账 v1.0.84 — Q-29 客户当地时间感知：已知城市即知时区，AI 不再问「你那边白天还是晚上」（#305 · 2RKH3H）

> 指令：`docs/指令_Q-29_客户当地时间感知与时间问句去重_2026-09-12.md`。前置（已读、在其上加、不重做）：Q-1 `docs/发版对账_v1.0.79_Q1.md`（槽位三态 `cell_view` / `slot_state` + C `repeat_question_guard`）、Q-8 F `docs/发版对账_v1.0.79_Q8.md`（人设当地时间注入 `excuse_budget.build_time_schedule_addendum` = `persona_reply._prompt_addenda` ④ try-block）、Q-25 `docs/发版对账_v1.0.83_Q25.md`（`fact_gate.check` 共享门）。总账：`docs/Q批_进度总账.md` Q-29 行。预算 $80。
> 红线：不动 `decide_probe_target`（Q-1）/ `persona_reply.py`（经 `_prompt_addenda` 既有 ④ 接线）；`location` 槽语义不变；**城市表命中不到就 None，不猜**（多城市撞不同时区也 None）。
> 执行：2026-09-12 08:5x 开工（`agent_probe -Intent "q-29-peer-local-time-305 (…)"` 已登记）。**每个 commit 落地后立刻补一行。**

## §〇 2RKH3H 为什么问到「白天还是晚上」（`tmp_diag\2RKH3H\20260912-052748_report_report.md`，代码只读核对）

| # | 现场 | 代码事实 |
|---|---|---|
| 1 | 03:50 `[goal-inject] state=…location:confirmed…`——客户城市**已确认** | `src/inbox/time_context.build_now_anchor_hint` / `excuse_budget.local_time_label` 只算**人设**当地钟（`persona_location.resolve_place_with_fallback(persona)`），没有任何「客户时区」概念；`location` 值没人换算成时区 |
| 2 | 03:57 客户「我这边四点了，再一会天就亮了」→ `[extract] facts=0 slots=0 llm=0` | 时间叙述不是任何画像槽（`profile_slots.CUE_LEXICON.location` 只把 `现在是晚上 / morning here` 当**问坐标的话头**，`capture_from_text` 不产出）；抽取为零 = 引擎对「对方现在几点」零事实 |
| 3 | 04:38 AI「你那边应该已经是晚上了吧」→ 04:41 客户「你又记忆不行了我说我这边四点了…」→ 05:25 AI「你那边现在是白天还是晚上？」 | `repeat_question_guard` 只比对**我方 24h 内问过的问句**（同槽标签 / Jaccard）——「白天还是晚上」不属任何 `SLOT_ASK_KEYWORDS` 槽，且我方此前没问过同句，客户**答过**不构成拦截依据 |

## 一、改动落点（每 commit 一行，落地即写；均带 #305）

| 段 | commit | 落点 |
|---|---|---|
| **A 城市 → 时区 + 时间叙述检出 + 纯格式化 + FactGate kind** | `（待填）` | 新 **`src/companion/peer_time.py`**：`CITY_TZ` **290 城** × 中 / 繁 / 英 / 日别名（英文词边界 casefold、≤3 字母缩写 `LA / SF / NYC / HK / KL / SG` 区分大小写整词、汉字子串 + `_CJK_TRAP_LOOKBEHIND`「北京都 / 完成都」不撞 京都 / 成都；刻意不收歧义名 Birmingham / Portland / San Jose / Naples / Valencia / Santiago / Nice…）+ `COUNTRY_TZ` **52 个单时区国家**兜底（美 / 加 / 澳 / 巴西 / 墨 / 俄 / 印尼 刻意不收——「在美国」推不出时区）；`city_to_tz(text, free_text=)`：命中恰一个时区才返回，**撞多个或没命中 → None**（`free_text=True` 扫整句时跳过 Austin / Charlotte / Orlando… 人名形别名）；`resolve_peer_place / resolve_peer_tz(profile, history=)`：`location` / `residence` 经 Q-1 `cell_view` 三形状取值，confirmed 优先 mentioned，字段无值再扫 history 里 `MENTION_LEXICON` 命中 / `capture_from_text` 抽到的那条客户自述；`peer_local_time(tz, now) -> {hh_mm, hour, period 早上/白天/傍晚/夜里, weekday}`（IANA / `UTC±N` / tzinfo 都认，无效 None）；`offset_from_statement(hour, minute, said_ts) -> UTC±N`（客户说「四点」反推整点偏移）；**`detect_time_statement(text)`**：只认对方说**自己这边**（`我这边 / 这里 / here / on my side` 锚或 `现在 / 已经 / …了`）——钟点「我这边四点了 / 现在凌晨两点 / it's 4am here / it's almost 5 here」→ `{kind=clock, hour, minute, period}`（上下午靠 凌晨 / 晚上 / 下午 修饰词、am/pm、句内 天亮 / 睡 线索定，定不了 `hour=None` 宁可不推）；时段「我这边现在是晚上 / 刚过中午 / 刚天亮 / morning here / it's pretty late here」→ `{kind=period, period}`；**不认**：「你那边应该是晚上了吧」（对方说我方）、「晚上八点见 / 八点下班」（约时间）、「这里有 3 点建议 / 一点都不冷」、问句、「白天也上班，晚上…」（作息不是此刻）。`src/companion/fact_gate.py`（Q-25，**只加 kind**）：`KIND_TRANSIENT="transient"` + `TRANSIENT_TTL_SEC=12h`——判据 = 原句锚定（evidence 必填且逐字在同一条入站）+ 值锚定在 evidence；不做类型 / 语种 / 主体。`src/inbox/time_context.py`（只加）：**`build_peer_time_hint(info, lang)`** 纯格式化——已知 → 「【对方当地时间】对方那边现在 周六 04:57，夜里（依据…）。**不要问对方几点、白天还是晚上、是不是该睡了**——你已经知道；按这个时段自然问候…」（en 同版）；未知 → 「对方时区未知…可以顺口问一次…问过之后 24 小时内不要再问」+ 已问过 → 「你 HH:MM 已经问过一次，本轮不要再问」。回归：`test_fact_gate_q25 + test_profile_fill_anchor_q19` 81 / `test_time_context + test_time_state_consistency` 49 全绿。 |
