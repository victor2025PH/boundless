# 指令 Q-19：画像 AI 推断只写可锚定事实（槽类型校验 · 原话锚定 · 只吃客户入站 · 待确认不计已采集）

> 预算 **$120**。工单 **#294 + #272 追加（7FN84P / 4R2K76「!!! 加急 · 请今日热修」）**（skuio 1.0.81，09-11 16:42–17:03）。底座 `docs/修复指令_Q批_并行_2026-09-09.md`。落点表 `docs/发版对账_v1.0.82_Q19.md`。
> 前置：Q-5（`profile_fill.py` / `skill_manager` 抽取合一）已收工，本线在其上改；`goals/service.py` 不动。

## 事实（证据 `tmp_diag\{4JW2M4,7FN84P,4R2K76}`；代码只读核对）

- 华哥 telegram:7331682688:7340576921 客户全程粤语聊岚山散步 / 周末 / 新配搭，**从未说过年龄与职业**。16:42:41 `[profile] slot=occupation value='織り機の前に座って、新しい帯の色合わせ' source=ai_inferred`；16:48 `residence='我住在去嵐山散步'`。右侧「摸底 1/10」把幻觉计成已采集；年龄行画出竖排长句乱码（控件按自由文本渲染）。
- 值是**日语**——客户说的是粤语，日语只可能来自我方出站稿（人设日语咖啡馆稿）或翻译串台 → 抽取吃到了非客户文本。
- `profile_fill.apply()` 只做 `unknown_slot / empty / 长度截断 / 来源强弱` 判定：**无槽类型校验、无原话锚定、不区分文本来源**；`ai_inferred` 走 `mentioned` 状态却被进度条计入。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 槽类型校验** | `profile_slots` 每槽加 `validator`：`age` 只收 16–99 整数或年龄档（20s / 30s / 「三十多」）；`occupation` ≤ 24 字单短语、禁含句末标点 / 动词短句；`location / residence` ≤ 24 字地名（禁「我住在…」整句）；`name` ≤ 12 字；`marital_status / family_status` 枚举；不过校验 → `skipped[k]="invalid:<why>"` + `[profile] drop slot= reason=invalid` | `profile_slots.py`、`profile_fill.py` |
| **B 原话锚定** | 候选必须带 `evidence`（客户原句片段），且 `evidence` 能在**最近 30 条客户入站**里逐字（或去空白 / 大小写归一后）找到，值的核心 token 在 evidence 内 → 否则丢弃 `reason=unanchored`；`skill_manager` 抽取提示词明确「只从客户消息抽、附原句」 | `profile_fill.py`、`skill_manager.py`（只动抽取调用一处） |
| **C 只吃客户入站** | 抽取输入只喂 `direction=in` 文本；我方出站 / 翻译稿 / 关怀稿一律不进抽取（`source=manual_out` 已 skip，补 `auto_out` / `care_out` / 译文）；语言不一致守卫：候选值语种 ≠ 客户近 30 条主语种 → `reason=lang_mismatch` 丢弃 | `skill_manager.py`、`profile_fill.py` |
| **D 待确认不计已采集 + 控件** | 进度「N/10」只数 `confirmed`（`mentioned` 另显「待确认 M」）；`age` 槽渲染为数字 / 年龄档控件，任何槽显示 ≤ 24 字并省略号，长文本不撑爆；`ai_inferred` 卡片必带「✓ 确认 / ✕ 丢弃」 | `cp-goal.js`（双树）、`goal_routes.py` 只加只读字段 |
| **E 门禁 + 清洗** | `tests/test_profile_fill_anchor_q19.py`：日语值 × 粤语客户 = 丢；「我住在去嵐山散步」= invalid；「32」= age 通过；无 evidence = 丢；一次性清洗脚本 `tools/profile_purge_unanchored.py --dry-run`（列出所有 `ai_inferred` 且不过 A/B 的槽，`--apply` 才删）交回访附使用法 | tests、tools |

## 红线

- `confirmed`（坐席确认 / 用户填写）值一字不动；只处理 `ai_inferred` / `nickname` 来源。
- 不动 `goals/service.py` 决策与 `decide_probe_target`（Q-1）。
- 判不准就不写：宁可 0/10 也不写脏。

## 开新对话粘贴

```
读 docs/指令_Q-19_画像推断只写可锚定事实_2026-09-11.md，按它做 Q-19（#294 #272追加：profile_slots 每槽 validator（age 16–99 或年龄档、occupation/location ≤24 字短语禁整句、枚举槽）不过即 drop 落 [profile] drop reason=invalid / 候选必须带客户原句 evidence 且能在最近 30 条入站逐字找到否则 unanchored 丢弃 / 抽取只喂 direction=in 文本，出站·翻译·关怀稿全不进，值语种≠客户主语种丢弃 / 进度 N/10 只数 confirmed、mentioned 另显待确认、age 控件数字化、长文本 ≤24 字省略 / 门禁 + tools/profile_purge_unanchored.py --dry-run 清洗脚本）。预算 $120。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与 docs/发版对账_v1.0.79_Q5.md（在 Q-5 之上改，不重做），再 scripts\agent_probe.ps1 -Intent 登记；goals/service.py 不动；skill_manager.py 只动抽取调用一处；confirmed 值一字不动。每个 commit 带 #294 并立刻补落点表 docs/发版对账_v1.0.82_Q19.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-19 行并 agent_probe -Done。
```
