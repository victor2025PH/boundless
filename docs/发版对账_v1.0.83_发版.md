# 发版对账 v1.0.83 — 发版线 R83（单一版本：Q-23 + Q-24 + Q-25 + 界面切语言根修 → 一个 clean 包 → **只装坐席三台 + 117 壳；不发公共渠道、不播报**）

> 规矩：`.cursor/rules/chatx-desktop-install.mdc` 「单一版本（D-Q11）」——一个 semver = 一个 clean 包。前例 `docs/发版对账_v1.0.82_发版.md`（本表按其结构逐节对齐）。
> 范围（老板 09-12 04:4x 选项 `r83_now`）：**「现在从 510a9570 切 1.0.83 完整 R 线（内容 = Q-23/24/25 + 本修复；干净 worktree 打 clean 包 + import/gate/regression 三扫 + 装 117 与 173/198/104；不发公共渠道 / 不播报）」**。release commit **`29b3a71a`**（05:02）。
> ⚠️ **切包之后 05:06–06:02 主干又落了 12 个 commit**（#304 `73c8ab6b` / Q-27 A–D / Q-26 A–B / Q-22 A/C/D + 前端），这些线的落点表自称「v1.0.83」，且总账 05:0x 行写 #304「要随 R83 出包」——**它们都不在本包里**（§5 断点 ①）。要么 1.0.83 就此定格、它们随 1.0.84；要么从 HEAD 重切 1.0.83（约 1 小时，只需重装四台）。老板定。
> 接手方：先 `git -C D:\boundless log --oneline -16` 核对本表 commit 真在。意向板 slug `r83-release-1-0-83-29b3a71a-q-23-24-25-i18n-lang`。
> 执行：2026-09-12 04:4x → 06:3x，见 §5。

## 0. 一句话

**1.0.83 单包** `ChatX-Setup-1.0.83.exe` **523,353,499 B** sha256 `5134f5ec00e9756bb59573dea0ce62cef0b9a39f4ef86ad33aa72ffaa8248aa4`（clean 形态，`MZ` 头核过，build-info flavor=clean commit=29b3a71a，干净 worktree `D:\boundless-r80@29b3a71a`）。内容 = 1.0.82 全部 + **Q-23**（自动出站单一闸门 GuardContext + 场景维度：报障群 / 内部群不再吐成人软回应）+ **Q-24**（Messenger 边车发送链止血：composer 重绑 / 七码结构化失败 / 待重试铃铛 / PIN 可见）+ **Q-25**（事实闸门）+ **Q-20**（称呼层级）+ **界面切语言根修**（`510a9570`）。坐席三台 **yunsheng / kouxing / lianbei 全 UP · 1.083 · clean/public**（05:47–05:56 smoke 六端点全 OK + vision active），**三台上切语言链路活体核过**（§4）。**117 壳已装 1.0.83，但 117 的后端半边未装载**（zhiliao 重启 NO-GO，§4 / §6）。**未发公共渠道、未播报、未动台账**（老板要求）。

## 1. 本版收掉的坑（本线 = 切语言；Q-23/24/25 见各自落点表）

| 事故 | 根因 | 本版 |
|---|---|---|
| 本机切语言无效：一直繁体；切英 / 越 / 泰 / 印尼只见中文；越南语只有「文件 / 编辑 / 视图 / 窗口 / 帮助」五个菜单变 | ① 壳 `renderer.js` 把配置语言钉进每次导航 `?lang=`，`inject_i18n` 优先级 query > cookie，`/set_lang` 写了 cookie 立刻被 query 钉回；② `/set_lang` 303 回 Referer 原样带 `?lang=`；③ 壳只同步配置 / 菜单，页面不跟；④ 空 vi overlay `{}` 为真值 → `SS()` 落到简中 | `login_redirect.set_lang_redirect_target` 回跳剥 `lang`；壳 `watchWebLangSwitch` 监听 `/set_lang` 同步配置 + homeUrl + 菜单；`SS()` 三级回落（ext → base → key）；vi/th/id 壳词典 HY-MT 生成 94–98%；`ui_lang_src` 冲突时页面提示「本页被 URL 钉住」 |
| 切语言入口三套不一致（登录 / 初始化只有 zh⇄en 二元，工作台是六语） | 各模板各写 | `base.html` 用户菜单 / `login.html` / `setup.html` 统一挂 `wsToggleLang` 六语 + 跟随系统；标签显示 `UI_LANG_NATIVE` 原生名 `(Tiếng Việt)`，不再 `(EN)/(ZH)`；门禁 `test_lang_switch_entry_unified_on_auth_and_admin_pages` |
| 小智答不出「怎么切语言」 | 无词条 | `help_terms.ui_lang_switch` + `howto_pack ui-lang-switch` |

## 2. 开工基线（04:4x）

| 项 | 事实 |
|---|---|
| 主干 | 切语言批 **`510a9570`**（04:56，34 文件，只收本线；`admin.py` 用过滤补丁只暂存 i18n 段，别线 `conv_model_route` 注册段留在工作树）；其前 `2a3f3880`（Q-27 / Q-26 分派）、Q-24 收工 `2c1fd52b`、Q-20 `1ba8133f` |
| 意向板 | 本线两条 i18n intent 在板；HY-MT 生成器（pid 10320）跑完才提交；热区 10 分钟静默核过 |
| 工作树 | 别线脏文件（`ops/bug_intake.py` / `bug_tickets_page.py` / `tests/test_bug_*` `test_duty_*` / `tools/duty_*` / `deploy/desktop/SEAT_LOG_MONITOR_LOG.md` 等）**未动、未进任何 commit** |
| 版本链 | `desktop/package.json` 1.0.82→**1.0.83** / 1.082→**1.083**（字节替换，CRLF 保留）；`python -m scripts.i18n_hant generate` → **17519 键**；`gen-chatx-changelog add --version 1.0.83` **6 条双语**（切语言修复 / 六语菜单 / vi-th-id 壳词典 / Q-23 单一闸门 / Q-24 Messenger / Q-25 事实闸门）；`bump_ui_build.py --severity notice` → **20260912-0501**；小智 `help_terms` +1 / `howto_pack` +1 → 主干 **`29b3a71a`**（05:02） |
| 官网五值 | **未改**（不发公共渠道；`chatxContent.ts` 仍 1.0.82，`gate:content` 不跑） |

## 3. 回归 / 门禁（干净 worktree `D:\boundless-r80@29b3a71a`，四项并跑 05:0x 起）

| 项 | 结果 |
|---|---|
| worktree 复用 | `D:\boundless-r80` 原 `5ccc0013`（R82）：`git checkout -- .` + `git clean -f` 只清 R82 构建改写的 `i18n-packs-staged`，再 `checkout --detach 29b3a71a` |
| `import_sweep` | modules=**1252** bad=0 / from-import names bad=0 **ALL GREEN** |
| predist（clean） | `build:backend` 全量 + `dist:win:clean` 05:0x–05:3x：后端冒烟全过、干净包首启冒烟全过、after-pack 随包交付物 **22 项**；`latest.yml` version 1.0.83 size 523353499 |
| `gate_sweep -NoSyntaxScan` | **25 failed / 1706 passed**（1:06:01），transient 复跑仍红。逐条对 R82 的 18 条：14 项 R79 旧债同名 + `sealed_templates_no_legacy_date_formatters`（Q-14 B）+ `ui_build_stamp_not_stale`（worktree checkout mtime 伪红，`workspace_base.html` 05:02:48.42 vs 戳 05:02:48.38，主树绿）+ `sql_phantom_columns`（TK-3，`tiktok_huoke_bridge.py` 8 处 `kind / health / last_seen_ts / last_out_ts` 幽灵列，本次并行 worker 崩把同文件 4 条都记红，串行只 1 红）+ `no_dynamic_dot_access_concat`（Q-15）= 18 同名。**多出的 7 条**：`sql_phantom_columns` ×3 worker 崩连坐（串行绿）、`test_fixture_time_bombs` / `test_settings_en_js_keys_are_english` / `test_agent_perf_localized_dict_injected[zh-zh-CN]` 三条 `[gwN] node down` 连坐（串行 **绿**）、**`test_cp_i18n_overlay_coverage::test_mt_debt_registry_not_stale` 是本版真新红**——vi/th/id overlay 被本线做到 94% / 98% / 96%，债表 `_MT_OVERLAY_DEBT` 仍登记「vi 62/974 · th 0 键 · id 0 键」→ 债表过期。主干 **`4d805655`**（06:19）清空债表扶正（测试不进包，**1.0.83 不重打**）。**0 条功能性新红** |
| 全量回归（干净 worktree） | `scripts\regression.ps1` **133 failed / 27549 passed / 40 skipped，2:28:34**（生产在线 → BelowNormal + 并行度 4，与构建 / gate_sweep 并跑拖慢）。串行复跑 133 → **102 仍红 / 31 绿**（31 条并行 flaky）。**同 133 条在 1.0.82 release commit `5ccc0013` 的临时 worktree `D:\_r83cmp` 上复跑：98 红 / 34 绿 / 1 skip**。差集「1.0.83 红 / 1.0.82 绿」= **4 条**：① `test_ui_build_stamp_not_stale` = worktree checkout mtime 伪红（主树绿）；② `test_cp_i18n_overlay_coverage::test_mt_debt_registry_not_stale` = 本线把 vi/th/id 做起来后债表过期，`4d805655` 已清；③ `test_i18n_extra_langs::test_extra_lang_overrides_subset_of_zh` = `29b3a71a` 的 `zh_hant_auto.py` 从脏主树生成时吃进了模型路由线（`b0db0f1e`，切包后才提交）尚未提交的 `mb_feat_unrestricted_model` 键 → 繁体多一个 zh 还没有的键；主干 `b0db0f1e` 落后已绿，包内只是多一条无宿主的繁体词条，零影响；④ `test_ops_alert_noise_20260910::test_a_line_own_bot_blocks_but_does_not_alert` = **Q-23 有意变更**：硬名单（同事 / 本租户账号 / 自家 bot）先于 Tier0 且不看 enabled，自家 bot 私聊在这一层即拦为 `colleague:<id>`、不告警、不再顺带降 manual（比原 `tg_is_bot` 路径更早更硬）；测试钉旧原因码，主干 **`3677ace0`** 跟新。**四条全非功能性回归**；安装包不含 tests，**1.0.83 包不重打**（字节 = `29b3a71a` 构建）。剩 **98 条两版同红** = HEAD 既有债（§6，家族与 R82 同）。`D:\_r83cmp` 已 `worktree remove` |
| 本线门禁 | `test_i18n_autodetect`（含新加 `test_ui_lang_native_covers_all_ui_langs` / `test_lang_switch_entry_unified_on_auth_and_admin_pages`）、`test_profile_lang`、`desktop/test/shell-i18n.test.js` 主干绿；主树 `test_zh_hant_overlay_covers_every_key` 红是 **Q-22 未提交的 `cp.voice.*` 12 键**（干净 worktree 绿），归 Q-22 收口时 `i18n_desktop_ext generate --langs zh_hant` |
| 未做 | zhiliao 活体核对（§4 117 行）；官网 / 公共渠道 / 内测桥接（老板要求不发） |

## 4. 全链

| 步 | 状态 | 事实 |
|---|---|---|
| 坐席三台 | ✅ 05:47–05:56 | `push_chatx.ps1 -TargetSsh {yunsheng,kouxing,lianbei} -Setup <r80>\dist-clean\ChatX-Setup-1.0.83.exe -Install -Relaunch -Smoke`：三台 exit 0、`[push] OK`、smoke 六端点 OK + vision active + pacing ok；`chatx_fleet_status` **三台 UP · 1.083 · clean/public**（06:1x）。日志 `%TEMP%\r83\push_{yunsheng,kouxing,lianbei}.txt` / `fleet.txt` |
| 坐席三台切语言活体 | ✅ 06:2x | 三台本机 18799 探针（`%TEMP%\r83\probe_lang_seat.ps1`）：`GET /set_lang?lang=zh` + `Referer=/login?lang=zh_hant&theme=dark` → **303 `Location: /login?theme=dark`**（`lang` 已剥）；`/login?lang=vi` → `<html lang="vi-VN">` + `window.wsToggleLang = function` + `wsToggleLang(event)` 入口、无 `(EN)/(ZH)` 旧入口。三台全一致 |
| 117 本机 | ⚠️ 壳 ✅ / 后端 ✗ | `install_chatx_node.ps1` exit 5（旧进程已死、18799 被「外部进程」占——实为引擎树 zhiliao pid 22828 + portproxy，合法）→ 手跑 `ChatX-Setup-1.0.83.exe /S` → 注册表 `智聊 ChatX 1.0.83` + `Uninstall 智聊.exe` → `relaunch_chatx_node.ps1` 拉起（智聊.exe 1.0.83.0）。**117 的壳是纯客户端**（`%APPDATA%\telegram-ai-desktop\config.json` `backend.spawn.enabled=false`，18799 = `python D:\workspace\boundless\engines\chengjie\main.py` zhiliao）→ `admin.py / login_redirect.py / web_i18n.py` 那半修复要 zhiliao 重启才装载。`restart_preflight.ps1` **NO-GO**（Q-21/22/26/27 + yue-route 线 10 分钟内在存 `ai_client.py` / `translation_*.py` / `persona_reply.py` / `reply_settings_routes.py`）→ **按双实例纪律未重启**。所以 117 上现在：壳侧修复（`SS()` 回落 / vi 词典 / homeUrl 同步）已活，页面侧仍会被旧后端 303 钉回 |
| publish 公共 / 内测桥接 / 官网 | ⛔ 不做 | 老板 `r83_now`：不发公共渠道、不播报。渠道 `latest.yml` 仍 1.0.82 |
| 台账 / 回访 / 播报 | ⛔ 不做 | 同上。#298（Q-24）/ #303（Q-23）/ 切语言（无单）未标 fixed、未回访 |
| release 真值 commit | ✅ | `desktop/build/i18n-packs-staged` 自 worktree 构建拷回（**5 改 + 1 新** `messenger_sidecar_q24.py`）+ `platform-staged/licensing/ledger` 2 改 → **`5eca2306`**（06:21）；债表扶正 `4d805655`；本表 + 总账 R83 行（见 git log） |

## 5. 断点 / 交接

- 04:4x 老板选 `r83_now` → 04:56 `510a9570` 切语言批（等 HY-MT 生成器跑完 + 10 分钟静默）→ 05:02 `29b3a71a` 版本链 → 05:0x worktree 回滚 + detach → import_sweep / build:backend + dist:win:clean / gate_sweep / regression 四项并跑 → 05:3x 包出（`MZ` / sha256 / latest.yml / build-info 核）→ 05:4x 117 手装 + 拉起、preflight NO-GO 不重启 → 05:47–05:56 坐席三台 → 06:1x fleet 三台 1.083 → 06:1x gate_sweep 25 红定性 → 06:19 `4d805655` 债表 → 06:21 `5eca2306` 真值拷回 → 06:2x 三台切语言活体探针 → 意向刷新 R83 → 本表 → 06:3x 老板定「等 1.0.84」→ 07:2x regression 收口 133 红 → 07:3x 串行复跑 ×2（`D:\_r83cmp@5ccc0013` 对照）→ 4 差异逐条定性 → `3677ace0` 一钉扶正、`_r83cmp` remove → 意向 -Done。
- **断点 ①（要老板定）**：包 = `29b3a71a`（05:02）。之后主干 05:06–06:02 又落 **12 commit**：`73c8ab6b` #304 钧机粤语 / 克隆假阳性（总账写「要随 R83 出包」）、`0f0dca59 dc1f56be 636f19d9 823893da` Q-27 A–D、`6d9356ff 4412dffb` Q-26 A–B、`b9b37262 20b088d3` Q-22 A/C/D + 前端、`231c46c1` 文档。四条线落点表都叫「v1.0.83」，**但 1.0.83 包里没有**。两条路：(a) 1.0.83 定格为「Q-23/24/25/20 + 切语言」，其余随 1.0.84（各落点表 / 总账改口）；(b) 从 HEAD 重切 1.0.83（version chain 不变、重跑 hant / changelog 追加 / ui-build、worktree detach HEAD 重打、四台重装；约 1 小时，因未发公共渠道无外部暴露）。**本线未擅自重切。** → **老板 06:3x 定：先不动，等 Q-21 / Q-22 全部收工后一起切 1.0.84**。即 1.0.83 = `29b3a71a` 定格；#304 / Q-27 / Q-26 / Q-22 随 1.0.84（各线落点表「v1.0.83」字样由各线自己改口，本线不代改）。
- **断点 ②**：117 后端半边等 zhiliao 重启窗口（`restart_preflight` GO 后 `deploy\instances\restart_instance.ps1 -Instance zhiliao`，攒批：#304 / Q-22 / Q-27 / Q-26 都在等同一个窗口）。
- worktree `D:\boundless-r80@29b3a71a` 保留（`dist-clean\ChatX-Setup-1.0.83.exe`；`1.0.82.exe` 旧包可删）。临时文书全在 `%TEMP%\r83\`（`filter_admin_patch.py` / `bump_version.py` / notes 双语 / 各步日志 / `probe_lang_seat.ps1`），未入库。

## 6. 交回各线

- **切语言修复装载面**：坐席三台 = 包内新后端，**已全量生效**（§4 活体）；117 = 壳生效、页面等 zhiliao 重启。值守回访口径：切语言 → 页面立刻换 + 桌面菜单跟 + 刷新不回退；vi/th/id 为 β（HY-MT 机翻，`ui_lang_coverage` 显示覆盖率）。
- **Q-22**：主树 `test_zh_hant_overlay_covers_every_key` 红 = 你们未提交的 `cp.voice.*` 12 键缺繁体 → 收口时 `python -m scripts.i18n_desktop_ext generate --langs zh_hant` + bump `cp-i18n-ext ?v=`。
- **MT 债表**：`_MT_OVERLAY_DEBT` 已清空（`4d805655`），以后哪个语种掉回 <90% 由 `unlisted_gaps` 抓；再跑生成器请带 `--langs`，别再让「0 键报成功」静默降级。
- **TK-3**：`sql_phantom_columns` 8 处幽灵列仍在 `tiktok_huoke_bridge.py`（R80 起同红）。
- **#304 / Q-27 / Q-26 / Q-22 线**：你们的 `.py` 在 HEAD 但**不在 1.0.83 包、也不在 zhiliao 内存**——等断点 ① 老板定 + 断点 ② 重启窗口；落点表里「随 R83 出包」的说法在老板定 (a) 前请先别对外。
- **未进本版**：Q-21（#289 #290 #286）未收工；#302 / #304 未标 fixed。
