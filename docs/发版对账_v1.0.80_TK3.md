# 发版对账 v1.0.80 — TK3（TikTok 个人号私信：huoke 真机脑手分离 · 网页托管边车）

> 指令：`docs/指令_TK-3_TikTok个人号私信_真机脑手分离与网页边车_2026-09-10.md`。底座：`发版对账_v1.0.80_TK.md`（TK-1 C/D/E 三段，其中 E 段评论桥 `3a6b485f` 是本条 ①-B 的起点）。预算约 $270。
> 全部代码**默认关**：`tiktok.huoke_bridge.enabled` / `platform_login.tiktok.web_enabled` 缺省 false；两路皆非官方 → 准入区 + `notice_unofficial`；`platform_registry` 官方私信 `implemented=False` 不变。
> 执行：2026-09-10 21:54 起。抖音代码一行不动；公共层只追加 `tiktok:*` 条目。**提交只用裸 `git commit`**（TK-1 E 段 pathspec 事故沉淀）。

## 0. 开工基线（21:54，只读）

| 项 | 事实 |
|---|---|
| 意向板 | `tiktok TK-3 personal DM …` 21:54 登记；同板 ACTIVE 文件全是运维降噪线（`webhook_notifier.py` / `send_ops_group_report` / seat monitor），与本线无交集；索引空闲；HEAD `2ed11f1c` |
| 共享文件实况 | `account_orchestrator.py` / `channel_policy.py` / `send_gate_status.py` / `onboarding_guide_routes.py` / `errors.py` / `engines/huoke/**` 工作树干净 |
| 关键事实 | `window_guard.send_block_reason` / `channel_policy.policy_for` 都支持 `mode`（`platform:mode` 键，`whatsapp:official` 先例），但编排器 `send` L857-871 **不传 mode** → `whatsapp:official` 24h 键此前从未在编排器生效（既有隐性缺口，E1 传 mode 会让它开始生效——本线先只对 `tiktok` 生效，WA 副作用留 shadow 与 WA 线确认）；huoke `tiktok.py`：`send_dm` L2709 / `check_inbox` L2850 / `_handle_inbox_message` L3410（本地大脑 + 协调器 CRM 192.168.0.118:8000）/ `_get_auto_reply_engine` L4016；task_type `tiktok_send_dm`/`tiktok_check_inbox` 已有（`schemas.py` L34-35）；`config/apps/tiktok.yaml` `send_dm: {hourly: 5, daily: 15}` |

## 1. 落点

| 段 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|
| 立项 | `3ffb71c2` | `docs/指令_TK-3_…_2026-09-10.md`（决策 D-P1..P7、七段、红线、开新对话粘贴）；本落点表 §0 | — |
| **①-B 真机 chengjie 侧** | `3ffb71c2` | `src/integrations/tiktok_huoke_bridge.py` 586→~850 行：**私信通道** `kind=dm`——`POST /api/tiktok/huoke/dm`（会话键 `tiktok:user:<username>` 与官方私信 worker 同键；`direction=out` 是 huoke 首触回显、不起草不计配额；关系态 `is_mutual/is_follower/thread_type` 进 `chat_ctx`；纯媒体占位 `[image]`）；`POST/GET /api/tiktok/huoke/devices` 设备↔账号绑定（IANA 时区校验、账号级 `dm_daily_cap` 与全局取小、认领不再依赖先进线）；私信闸门 `_enqueue_dm`：D-P3 `policy_message_request_pending`（会话无对方入站一条不发，自动/人工同判）→ `text_block_reason(mode=personal_rpa)` → `policy_text_too_long`（1000）→ `policy_peer_silent`（72h 未回：自动停、人工只补 1 条）→ `policy_quiet_hours`（`work_hours_gate` 只拦自动链）→ `policy_daily_cap`（**账号时区切日** `_local_day_start`，修 TK-1 UTC 切日缺陷）→ `device_offline`（>2h 无心跳 503）；origin 取 `send_context.is_manual_send()`；认领节奏：同账号同时只 1 条私信在途 + `min_gap_sec`；**状态镜像**：入队即写 out `hb:<id>`（status 空＝无勾＝待真机）→ 回执成功 `report_message_status(...,"sent")` 升勾，失败 `record_failed_outbound(reason="huoke:<err>")`（前端「发送失败 + 一键重发」）；**心跳健康** `heartbeat_state`（15min reconnecting / 2h expired）变化才写 `platform_session_health`，`sweep_health()` 供巡检；状态库幂等 ALTER 增列（老库零停机）。评论通道行为不变（4 例回归绿，`tiktok:user:*` 由 not_bridged 改判消息请求）。`config.example.yaml` `huoke_bridge` +5 键 +5 行注释（CRLF blob，只含本线行） | `tests/test_tiktok_huoke_bridge.py` 4→**9**（私信进线 / 六闸门含时区切日与 UTC 反例 / 认领节奏·镜像升 sent·失败留痕 / 设备绑定·三态心跳·sweep / 路由含 dm·devices·窗口字段归一 / e2e 评论与 e2e 私信含 503 透出）全绿 26s |
| **B5/E1 公共层** | `073c14eb` | `src/inbox/channel_policy.py`：追加 `tiktok:personal_rpa` / `tiktok:web` 两条 mode 键（≤1000 字、首条禁链、无按钮、**无 48h·10 条窗**——那是官方 API 配额，套到个人号会误拦；真机可发图、边车阶段 1 不发媒体；note 带「非官方 notice_unofficial」）；`_apply_overrides` 先找 `channel_policy.<platform>:<mode>` 再回落 `<platform>`（给个人号配窗口不殃及官方号，反之亦然；无 mode 键时旧行为）。`src/integrations/account_orchestrator.py`：新 `_managed_mode()`；`send` 的 `policy_for / text_block_reason / send_block_reason` 与 `send_media` 的 `media_block_reason / send_block_reason` 都传 `mode=`（修「策略表 mode 键在编排器从未生效」的隐性缺口）。**WA 副作用核实**：`whatsapp:official` 只声明窗长无 `per_window_cap` → `has_window=False` → `window_rule=None`，传 mode 后 WA 零行为变化（测试钉住），无需 shadow 期 | `test_channel_policy` +2（`test_tiktok_personal_modes_have_no_official_window`、`test_orchestrator_passes_managed_mode_to_channel_policy`）；`test_channel_policy + test_window_guard + test_tiktok_official + test_tiktok_shop_cs + test_douyin_official + test_douyin_mock_worker` 72 绿；`test_*orchestrator* + test_whatsapp_official* + test_send_guard*` 51 绿 |
| **B6 人话** | `c23af79a` | `send_gate_status.blocked_reason_key` 在泛 `policy_` 之前分出 7 族：`policy_message_request` / `policy_peer_silent` / `policy_daily_cap` / `policy_quiet_hours` / `policy_one_reply` / `policy_comment_len` / `device_offline`。新词包 `src/web/i18n_packs/tiktok_huoke.py` 六语（zh/en/zh_hant/vi/th/id）覆盖 `err.inbox.send_blocked_*` + `inbox.failr.*`（不碰正在改的 `zh_hant_auto`）。发送路由：桥 `ChannelSendError.reason_code` 命中上述族 → `_send_blocked_exc`（409 形；真机离线保留 503）。工作台 `_failReasonText` 同族气泡人话；503+`send_blocked` 走同一横幅 | `test_blocked_reason_key_families` +7 断言；`test_tiktok_huoke_gate_i18n_six_langs`；桥 9 例 + `test_template_inline_js_syntax` 绿。`test_send_gate_feedback` 另 2 例（额度数字）红——`companion_send_gate` 本线未改，属他线预存在债 |
| **①-A huoke 侧** | `08f62a80` | 新模块 `engines/huoke/src/app_automation/tiktok_chengjie_bridge.py`：`reply_cfg`（缺省 local）、互斥警告、`messages_from_conversation`（含 out 回显）、`post_dm` / `bind_device` / `drain_handback`（认领→`send_dm`→ack，失败分级 `send_failed` / `exception:*`）、`dispatch_inbox_reply`。`tiktok.py` `check_inbox`：chengjie 时整段对话转发、不进 `_handle_inbox_message`；local 条件多一个 `not _cj`。`executor` `tiktok_check_inbox` 成功后 bind+drain（local 零调用）。`config/apps/tiktok.yaml` `reply_engine: local` + `chengjie` 块 | `tests/test_tiktok_chengjie_bridge.py` 5 例 + `test_executor_tiktok_ai` 2 例全绿 |
| **E2 口径 / 页签数据** | `cb413377` | `tiktok_connect_panel` 增加 `tabs` / `personal_rpa`（桥开时列出设备心跳）/ `web`（阶段 1 未开工占位）；`platform_registry` note 补 personal_rpa/web 非官方（`implemented=False` 不变，JSON 已 export）；`howto_pack` 支持哪些平台：不再写「TikTok 暂不支持」一刀切，改为三行——个人号无官方私信接口 / 真机 opt-in 只接管对方开口之后 / Shop 官方默认关 | `tests/test_tiktok_personal_onboarding.py` 3 例 + `test_platform_registry` JSON 同步绿 |

## 2. 第二阶段（2026-09-11 02:06 起，按优先级：真机主路补强 → 可见性 → 边车骨架）

| 段 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|
| **P1 独立认领轮询器** | （本 commit） | 修「认领只挂 `tiktok_check_inbox` 尾部——长时间不巡检就不发已入队回复」。chengjie：`GET /api/tiktok/huoke/handback/pending`（`TikTokHuokeStateStore.pending`：**只看不认领**，先回收过期认领，报 queued/claimed/queued_dm/oldest_wait_sec；带 device_id 顺手记心跳）。huoke：`tiktok_chengjie_bridge.py` +`pending_handback` / `plan_handback_tasks`（纯决策：local·无 endpoint·poll_sec=0 全跳；设备已有 pending/running 的 handback 或 check_inbox → 跳且不打 HTTP；智聊 queued==0 → 不唤醒真机不占设备锁）/ `run_handback_poll_once`（三处宿主依赖可注入）/ `start_handback_poller` 守护线程（每轮重读 yaml，local 只睡不动：零 HTTP 零任务）；新任务型 `tiktok_chengjie_handback`（`schemas.TaskType` + executor 分支 bind+drain + 超时 300s + 中文名「智聊回传发出」）；`api.py` lifespan 起线程；`tiktok.yaml` `handback_poll_sec: 120`（0 关，下限 30）。不入 `_IDEMPOTENT_TASK_TYPES`（孤儿不自动重排，防双发）。`api.py/schemas.py/core.js` 工作树原为 CRLF（索引 LF、stat 缓存遮蔽）已归一 LF，diff 只含本线行 | chengjie `test_tiktok_huoke_bridge` 路由例 +pending 断言（只看不改状态 / 认领后 queued 0 claimed 1 / 缺参 400）9 绿；huoke `test_tiktok_chengjie_bridge` 5→**8**（轮询决策 / 注入宿主建任务 / executor 分支 local skip + chengjie drain 调用序 devices→handback→ack）+ `test_executor_tiktok_ai` 2 绿 |
| **P2 真机联调探针 + 跨引擎契约** | （本 commit） | huoke 新 `src/app_automation/tiktok_chengjie_probe.py` + 薄壳 `scripts/tiktok_chengjie_probe.py`：`check`（只读：reply_engine / endpoint / token 掩码 / IANA 时区 / 设备映射 / 轮询开关）→ `GET /status` 四分法（连不上 · 401/403 token · 404 桥未挂 `huoke_bridge.enabled` · OK 带 policy/queue/health）→ `--bind` 设备绑定 → `--roundtrip` 探针私信进线（peer `chengjie_probe`，msg_id `probe:*`，工作台会出现探针会话）→ `pending` 只看；每步 FAIL 附可执行 fix；退出码 0/1/2/3。chengjie 新 `tests/test_tiktok_huoke_contract_e2e.py`：把 huoke 模块按别名包装进来（两仓顶层都叫 `src`），TestClient 充当 HTTP，走「探针全绿 → 适配器人工发送入队+无勾镜像 → 轮询决策唤醒 → 认领 → send_dm → 回执 → 镜像升 sent → 空队列不再唤醒」整条线；huoke 树缺则 skip 不假绿。顺手修 `_echo_sent` 无镜像回落补回显时带 `sent`（老队列项不再像待真机）。**联调 SOP**：huoke 机 `tiktok.yaml` 填 `reply_engine: chengjie` + `chengjie.endpoint/token/timezone/device_account_map`；智聊 `tiktok.huoke_bridge.enabled: true` 重启；`python scripts/tiktok_chengjie_probe.py --bind --roundtrip` 全绿；自有号让对方先发一条 → 跑一次 `tiktok_check_inbox`（auto_reply 忽略）→ 工作台看到会话并回复 → ≤120s 轮询器建 `tiktok_chengjie_handback` → 真机发出 → 气泡打勾 | huoke `tests/test_tiktok_chengjie_probe.py` 4 例（配置退出 1 + fix 文案不泄 token / 连通四分法 / 绿路调用序 status→devices→dm→pending / 绑定失败与无设备提示）；chengjie 契约 1 例 + 桥 9 例 = **10 绿** 15s；真配置跑 CLI → `exit 1` 三条 FAIL 指向 yaml（预期，默认 local） |
| **未做（下阶段）** | — | ②-B 人工可发（须 72h 自有账号验证）；Shop 会话 48h/10 放宽归 TK-2 | — |
