# 指令 Q-33：视觉令牌收口——字号 / 间距 / 圆角 / 颜色 / 图标 / 深色对比 / 层级 / 可达性

> 预算 **$180**。五视角方案 P2-11～18（`docs/分析_聊天工作台_五视角美化优化方案_2026-09-12.md` §五）。落点表 `docs/发版对账_v1.0.85_Q33.md`。
> 与 Q-31 / Q-32 **同文件同 hunk 顺手做**优先；本条负责它们没碰到的剩余面与门禁封顶。

## 事实（代码只读核对；数字见五视角文档 §三）

- 令牌三层已在：品牌 `--bl-*`（`brand.css`）、全站 `--tk-*`（`workspace_base.html` L232–296）、页面 `--accent / --bg-* / --text-* / --xl-* / --bdg-* / --rail-*`（`unified_inbox.html` L113–235）、副驾 `--cp-*`（`shared/copilot/tokens.css`）、内联 `--th-*`（`theme-tokens.css`）。
- 但页面里：模板 **728 处** hex、`unified-inbox.css` **1,055 处** hex、**305 处** <12 px 字号、圆角 4/6/7/8/10/12/14 七档、间距 3/5/7 px 散落、`z-index` 十几个裸数字、头部 chip 用 `color-mix` 自配底色、i18n **110 条** 串带 emoji、暗色靠 L6–26 `!important` 块硬盖。
- 长期红门禁：`inline_color_ratchet ×3` / `legacy_blue ×2` / `workspace_emoji_ratchet` / `copilot_tiny_font` / `input_theme_contrast`。

## 要做的六段

| 段 | 内容 | 位置 |
|---|---|---|
| **A 字号阶梯** | 只允许 12 / 13 / 14 / 16 / 20：chip 与副行 `var(--cp-fs-tiny)`（12）、正文 `var(--cp-fs)`（13）、名字 / 标题 14、弹层标题 16、空态 20；305 处 <12 px 全改；行高 1.45（正文）/ 1.3（chip）；`copilot_tiny_font` 门禁范围扩到工作台并封顶 0 | `unified-inbox.css`、模板内联 |
| **B 间距 + 圆角** | 间距只用 4 / 8 / 12 / 16 / 24（`--cp-gap-*` / `--tk-gap`）：chip `4px 8px`（高 24）、按钮 `6px 12px`（高 28）、卡内 12、区块 16；删 3 / 5 / 7 px。圆角三档：chip / 按钮 `--tk-radius-sm`、卡 / 输入 `--tk-radius`、弹层 / 抽屉 `--tk-radius-lg`；气泡 14 / 首尾 4 保持 | CSS |
| **C 颜色收口** | 模板 728 处 hex → `--th-*` 或删内联改类；CSS 1,055 处：结构面 `--bg-*`、文字 `--text-*`、状态 `--tk-*-ink` + `--bdg-*-bg`；头部 / 列表 chip 一律 `--bdg-*`（删 `color-mix` 自配）；暗色只靠令牌翻转，删 L6–26 `!important` 块；`inline_color_ratchet ×3` / `legacy_blue ×2` 封顶 0 | 模板 + CSS |
| **D 图标统一** | emoji 全换 `uiIcon`（档位四档 hand / edit / shuffle / rocket；AI 状态 `activity`；待审 `inbox`；锁 `lock`；翻译 `languages`），16 px stroke 2 `currentColor`；i18n 110 条带 emoji 串去 emoji（zh/en + 繁体 regen）；`workspace_emoji_ratchet` 封顶 0 | `uiIcon` 词表、`inbox_workspace.py` |
| **E 深色对比 + 层级 + 动效** | `[data-cp-theme=dark]` 下状态带 / 草稿条 / 列表副行 / 翻译族 WCAG AA 抽查（正文 ≥4.5、UI ≥3），不达标只改令牌值；`z-index` 收成 `--z-base 1 / --z-sticky 10 / --z-pop 100 / --z-drawer 200 / --z-modal 1000`；阴影只用 `--tk-shadow` / `--tk-shadow-md`；chip / 状态带出现 `.14s` 淡入（`--cp-dur`），`prefers-reduced-motion` 豁免 | `theme-tokens.css`、CSS |
| **F 可达性 + 门禁** | 所有图标按钮 `aria-label`（与 title 同源）；焦点环 `--tk-ring`；分段控件 `role=radiogroup`。`tests/test_tokens_q33.py`：工作台 CSS / 模板 <12 px = 0、裸 hex ≤ 白名单（渐变 / 品牌）、`z-index` 裸数字 = 0、emoji 串 = 0、对比度抽查表 | tests、既有门禁封顶 |

## 红线

- **零新色、零新字号、零新变量**——只映射到现有令牌；缺令牌先在五视角文档 §二 登记再议，不在页面里发明。
- 不改布局与交互（那是 Q-31 / Q-32）；本条 diff 应当是「值替换」。
- 品牌蓝 `--bl-growth #1e8cf2` / `--tk-brand` 一处定义，不复制字面量。
- `unified_inbox.html` 共享文件：`stage_hunks.py --mine`；与 Q-31 / Q-32 同时在途时按文件归属矩阵先协调再动同一区块。

## 开新对话粘贴

```
读 docs/指令_Q-33_视觉令牌收口_字号间距圆角颜色图标深色层级_2026-09-12.md，按它做 Q-33（五视角 P2-11～18：A 字号只留 12/13/14/16/20，305 处 <12px 改 var(--cp-fs-tiny)，copilot_tiny_font 扩到工作台封顶 0 / B 间距 4/8/12/16/24 用 --cp-gap-*/--tk-gap，圆角三档 --tk-radius-sm/--tk-radius/--tk-radius-lg / C 模板 728 处 + CSS 1,055 处 hex 映射到 --th-*/--bg-*/--text-*/--tk-*-ink/--bdg-*，删 color-mix 自配与暗色 !important 块，inline_color_ratchet ×3 / legacy_blue ×2 封顶 0 / D emoji 全换 uiIcon，i18n 110 条去 emoji + 繁体 regen，workspace_emoji_ratchet 封顶 0 / E 深色 WCAG AA 抽查只改令牌值，z-index 收五档变量，阴影两档，.14s 淡入 + reduced-motion 豁免 / F aria-label + --tk-ring + radiogroup + 门禁 tests/test_tokens_q33.py）。预算 $180。先读 docs/分析_聊天工作台_五视角美化优化方案_2026-09-12.md §二 令牌表 §三 数字 §五 P2，再 scripts\agent_probe.ps1 -Intent 登记；零新色零新字号零新变量，diff 只做值替换不改布局交互；unified_inbox.html 用 python tools/stage_hunks.py --file <路径> --mine <regex> --apply 只暂存自己的 hunk，与 Q-31/Q-32 同时在途时先看 ACTIVE 意向再动同一区块。每个 commit 带 Q-33 并立刻补落点表 docs/发版对账_v1.0.85_Q33.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-33 行并 agent_probe -Done。
```
