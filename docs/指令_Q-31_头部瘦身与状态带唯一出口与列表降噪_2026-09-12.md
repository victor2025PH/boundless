# 指令 Q-31：头部瘦身 + 状态带唯一状态出口 + 列表行降噪（含 #317 筛选标签筹码）

> 预算 **$160**。工单 **#317（BS7E5Q，skuio，1.0.84，09-12 20:19）** + 五视角方案 P1-5/6/7（`docs/分析_聊天工作台_五视角美化优化方案_2026-09-12.md` §五）。落点表 `docs/发版对账_v1.0.85_Q31.md`。
> 前置：Q-26 B（`#cs-band` 状态带四色）在 1.0.84；Q-30 A（单人端无认领）建议先落或同批。

## 事实（代码只读核对）

- 头部 `unified_inbox.html` L773–830：**14 个控件**同一行（头像 / 名 / `mode-select` / `mode-eff-chip` / `aif-chip` / `claim-hdr-btn` / `cx-pilot-pill` / AI 状态 / 原生页 / 能力对照 / 转企微 / 工作目标 / 接管 / 助手），405 px 视口折三行；`hdr-btn` 高度 26–30 px 不一，圆角 4/6/8 混用。
- 「不发」状态出口 ≥6 个：`#cs-band`、`identity-bar`（未绑人设）、`acct-out-banner`、`lang-warn-bar`、`aif-chip`、PIN 黄条、让位倒计时 chip（L1037–1043 三条横幅 + 头部 chip）；`cs-band` 在 auto 时隐藏，用户感受「有时有有时没有」。
- 列表行 `_handlerBadgeHtml` L6469–6519 + L6840 `tagChips`：一行最多出 未读 / SLA / 风险点 / 我·AI·审·手 / 标签 ×2 / +N / 灰勾——8 枚。
- **#317**：筛选面板 `#fp-tags`（L5625–5654）把会话里出现过的**每个系统标签**按计数出 chip：`dormant:ignored 6 / risk:medium 3 / 客户要求停联 1`；点击只是筛选跳转；「处理完点确定」后 chip 仍在，因为 chip 是**标签统计**——标签没摘就不会消。`dormant_review.py` L620 只加 `dormant:ignored` 不减；`stop_contact.py` L399 确认后只 `clear_needs_human`，停联标本身保留；`risk:medium` 摘标在 Q-17 冷却链。

## 要做的五段

| 段 | 内容 | 令牌 / 文件 |
|---|---|---|
| **A 头部三件** | 左：头像 40 px + 名（`--text-main` 14/600）+ 副行（`--text-sub` 12：平台 · 账号 · 人设）；中：**档位分段控件**（4 段，文字 + `uiIcon` hand/edit/shuffle/rocket，无 emoji，选中 `--tk-brand` 底白字，未选 `--bg-soft` + `--text-sub`，高 28，圆角 `--tk-radius-sm`，`role=radiogroup`）；右：**助手**按钮 + **⋯ 更多**（AI 状态 / 原生页 / 能力对照 / 转企微 / 工作目标 / 接管）。删 `mode-eff-chip` / `aif-chip` / `claim-hdr-btn`（Q-30） / `cx-pilot-pill`，其信息并入状态带。`hdr-btn` 统一 28 高、`--tk-radius-sm`、`--btn-border` | L773–830、`.hdr-btn` / `.mode-select` CSS |
| **B 状态带唯一出口** | `cs-band` **常驻**（auto 时也显示一行绿「AI 会自动回 · 标准档」）；四色只用 `--tk-emerald / --tk-amber / --tk-danger / --text-sub` + 对应 `--bdg-*-bg`，文字 13 px `--text-main`，动作按钮 `--tk-brand` 文字链；`identity-bar` / `acct-out-banner` / `lang-warn-bar` / `aif-chip` / PIN 黄条 / 让位倒计时**全部并入**为 `conv_state` 的 state（`no_persona` / `acct_out` / `lang_mismatch` / `pinned` / `deferred`）；带内倒计时每秒本地刷新，过渡 `--cp-dur`。文案全部走 Q-30 D 的 `inbox.reason.*` | `cs-band` CSS、`conv_state.py` 加三源、L1037–1043 撤除 |
| **C 列表行降噪** | 行 = 头像（平台徽标 `--rail-ring` 抠边）+ 名 + 时间 + 摘要（12 px `--text-sub` 单行省略）+ 右侧**最多两枚**：未读数（`--tk-brand` 底白字）或 SLA 时钟（`--bdg-warn`）+ 风险红点（`--tk-danger`）；「我 / AI / 审 / 手」仅 `MULTI_SEAT` 显示；标签 chip 从行里移到 hover / 详情；`conv-done-tick` 12 px `--text-tiny`；行高 56 | `_handlerBadgeHtml`、L6840、列表 CSS |
| **D #317 筛选标签筹码** | ① `#fp-tags` 分两组：用户标签直出；**系统标签**（`dormant:*` / `risk:*` / 停联 / 需人工）折叠成「系统标签 ▸ N」，单人端默认收起；② 每个 chip 尾部 × = 清该筛选（不是删标签），当前筛选中的 chip 高亮并可 Esc 清；③ 生命周期：`dormant:ignored` 在该会话**有新入站**时自动摘（`dormant_review` 加 `settle` 反向）；停联「确定」= 归档 + 摘 `客户要求停联` 进 `conv_meta.stop_contact_at`（历史可查，标不占筛选）；`risk:medium` 沿 Q-17 冷却摘标，摘完统计即消；④ 「处理完」按钮语义写清：确认框文案说明会发生什么（归档 / 摘标 / 切档） | L5625–5654、`dormant_review.py` L620、`stop_contact.py` L385–399、`inbox_workspace.py` |
| **E 门禁 + 截图** | `tests/test_header_q31.py`：头部一级控件 ≤ 5；`cs-band` 在 auto / manual / held / deferred / no_persona 五态各有文案且无第二条横幅可见；single 端列表行无「我 / AI」徽标；`#fp-tags` 系统标签在 single 端默认折叠；dormant 会话来新消息后标消。Playwright 405 / 1024 / 1280 三视口 × 浅深主题截图各一张进 `tests/visual/q31/` | tests |

## 红线

- 只用现有令牌（`--tk-*` / `--bdg-*` / `--text-*` / `--bg-*` / `--cp-*`），**零新色、零新字号**；emoji 一律换 `uiIcon`。
- 不动 `conv_state` 已有六态语义（Q-26 A），只加源。
- 多坐席（显式 multi）保留「我 / 同事」与「我的」筛选。
- `unified_inbox.html` 共享文件：`stage_hunks.py --mine` 只暂存自己的 hunk。

## 开新对话粘贴

```
读 docs/指令_Q-31_头部瘦身与状态带唯一出口与列表降噪_2026-09-12.md，按它做 Q-31（#317 + 五视角 P1-5/6/7：A 头部收成「身份 + 档位分段控件（4 段 uiIcon 无 emoji，选中 --tk-brand）+ 助手 + ⋯ 更多」，删 mode-eff-chip / aif-chip / claim-hdr-btn / cx-pilot-pill，hdr-btn 统一 28 高 --tk-radius-sm / B cs-band 常驻含 auto 绿行，四色只用 --tk-emerald/--tk-amber/--tk-danger/--text-sub + --bdg-*-bg，identity-bar / acct-out-banner / lang-warn-bar / aif-chip / PIN 黄条 / 让位倒计时全并入 conv_state 的 state 由状态带唯一出口，文案走 inbox.reason.* / C 列表行右侧最多两枚（未读或 SLA + 风险点），我/AI/审/手仅 MULTI_SEAT 显示，标签 chip 移出行 / D #fp-tags 系统标签折叠「系统标签 ▸ N」单人端默认收起，每 chip × 清筛选，dormant:ignored 新入站自动摘，停联「确定」= 归档 + 摘标进 conv_meta，处理确认框写清后果 / E 门禁 + Playwright 三视口两主题截图进 tests/visual/q31/）。预算 $160。先读 docs/分析_聊天工作台_五视角美化优化方案_2026-09-12.md §二（令牌表）§五 P1 与 Q-26 落点表 docs/发版对账_v1.0.84_Q26.md，再 scripts\agent_probe.ps1 -Intent 登记；只用现有令牌零新色零新字号；unified_inbox.html 用 python tools/stage_hunks.py --file <路径> --mine <regex> --apply 只暂存自己的 hunk；显式 multi 的我/同事/我的筛选保留。每个 commit 带 #317 并立刻补落点表 docs/发版对账_v1.0.85_Q31.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-31 行并 agent_probe -Done。
```
