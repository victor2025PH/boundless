# 指令 Q-31：筛选面板系统标签筹码——处理完自动消 / 可关闭 / 单人端默认折叠（#317）+ 顶栏「发前确认」计数只计可行动稿（PUUWJB）

> 预算 **$60 → $100**（首版把五视角 P1-5/6/7 美化项也塞在本条，20:5x 核对后拆走：状态带常驻 / 头部瘦身 / 列表降噪属**美化线**，并入 Q-32 待老板拍板；本条只剩 #317 这一个 bug；**21:0x 加 D 段**：同一报障人同一账号的 PUUWJB「顶栏发前确认 47 / 待处理 6 计数无限堆叠」，自动立单按 90 分钟同实体规则误挂到 #317，主题不同但同属「单坐席系统噪音入口的生命周期」，并入本条不另立）。工单 **#317（BS7E5Q，skuio，1.0.84，09-12 20:19）+ PUUWJB（#317 误挂，09-12 21:01）**。落点表 `docs/发版对账_v1.0.85_Q31.md`。

## 事实（代码只读核对，HEAD `0b606199`）

- 筛选面板 `#fp-tags` 由 `_renderPanelTags()`（`unified_inbox.html` L5652–5660）渲染：取 `_tagStatsCache` **在用标签 Top8 带计数**，只剔除「需人工」；`dormant:ignored` / `risk:medium` / `客户要求停联` 这些**系统写入**的标签与用户手打标签同列——用户看到的「dormant:ignored 6 / risk:medium 3 / 停联 1」就是这里。点击 = `setTagFilter`（筛选），没有「关闭」。
- 「处理完点确定后筹码仍在」：chip 是标签统计，**标不摘 chip 不消**。三条标签的生命周期各缺一段：
  - `dormant:ignored`：`src/inbox/dormant_review.py` L620–621 只**加**，没有任何地方摘（全仓 `IGNORED_TAG` 仅此一处写）；会话后来又有新入站也不摘。
  - `客户要求停联`：`src/inbox/stop_contact.py` L385–399 确认后只 `clear_needs_human`，停联标本身保留（这是**对的**——停联是事实），但它不该再占筛选筹码位。
  - `risk:medium`：Q-17 冷却摘标链已在，处理完会消——本条不动。
- 状态带 / 原因码等其余项已在 Q-26 A/B（1.0.84），与本单无关。
- **PUUWJB（21:01）顶栏「发前确认 47 / 待处理 6」**：药丸 `#ws-l4-badge` 在 `workspace_base.html` `refreshL4()`（L3443–3470）= `GET /api/drafts/risk-summary` 的 `by_level` **L1+L3+L4 全部 pending 草稿之和，不看稿龄**；点击直达草稿审批台（那里已有「批量拒绝」）。既有机制三件都在 HEAD 但**没接成闭环**：① 陈旧护栏 `inbox.auto_draft.stale_approve_hours`（24h）只禁「原样发」（`approve_blocked=age|replied`），**稿仍 pending 仍计数**；② 超龄自动作废 `inbox.sla_watcher.auto_expire_hours` **出厂 0 = 关**（`sla_watcher.py` L35）；③ 入站年龄闸 `max_inbound_age_hours` 72h + `ingest backfill=true` 不起草（`unified_inbox_account_routes.py` L1637）都在——但 **Messenger 边车的 `backfill pushed`（Q-24 C PIN 过后 `scheduleBackfillAfterPin` 重建队列）推送体不带 `backfill` 标**（`services/messenger-web/server.js` 无该字段），72h 内的历史消息按新入站起 L1 稿——「登录后历史会话各自出一条待确认草稿」的候选根因，需用 skuio 的 `reply_drafts.created_ts` 核（47 条是否集中在 PIN 通过 / 重登后几分钟）。用户疑的 D-M1「全局默认半自动」只是让这些稿落成 L1 而不是 L2，不是堆叠本身的原因。

## 要做的四段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 系统标签分组** | `_renderPanelTags`：把 `_tagKind(t)` 命中系统族（`dormant:*` / `risk:*` / 停联 / 接管）的标签归到第二行「系统标签 ▸ N」折叠块，**单坐席（`!MULTI_SEAT`）默认收起**，多坐席默认展开；用户标签照旧 Top8。每个 chip 尾部 × = 清该筛选（`setTagFilter('')`），Esc 同效；不是删标签 | `unified_inbox.html` L5652–5672、`_tagKind`、CSS `.fp-chip` |
| **B 生命周期** | ① `dormant:ignored`：该会话**有新入站**时自动摘（`protocol_autoreply` 入站钩子或 `dormant_review.settle` 反向，二选一，只写一处）；沉睡复盘再判沉睡会重新打，语义不变；② 停联：`stop_contact` 确认 = 归档 + 把「客户要求停联」从 `conv_tags` 移到 `conv_meta.stop_contact_at`（历史可查、`stop_contact` 判定改读 meta，**不弱化**「最多一条告别」守卫）；③ 处理确认框文案写清后果（「归档并从筛选移除 · 不再自动回复」） | `dormant_review.py`、`stop_contact.py` L385–399、`inbox_workspace.py` |
| **C 门禁** | `tests/test_panel_tags_q31.py`：单坐席渲染系统标签折叠且用户标签不受影响；dormant 会话来新消息后 `dormant:ignored` 不在 `conv_tags`；停联确认后标不在 `conv_tags` 且 `stop_contact` 守卫仍拦第二条；× 清筛选不改标签 | tests |
| **D 顶栏「发前确认」只计可行动稿（PUUWJB）** | ① `risk-summary` 加 `actionable` 分桶：只计 `approve_blocked==""`（稿龄 ≤ `stale_approve_hours` 且会话此后无出站，复用 `DraftService._approve_block_reason` 单一入口，**不另算一套**），药丸数字与 title 改读它，超龄 / 已回过的稿只在审批台可见；② `auto_expire_hours` 出厂 **48h** + `auto_expire_levels: [L1]`（只作废未发稿、审计留痕、不改任何发送行为——走 Q-4 F `release_defaults_changed.json` 五步 + release_notice 一行）；③ 审批台顶部 + 药丸悬停出「清空 N 条超龄稿」= 既有批量拒绝，`reason=bulk_stale`；④ Messenger 边车 backfill 推送体带 `backfill: true, backfill_source: "msg_backfill"`（与 TG history sync 同口径），ingest 走既有「backfill 不起草 → `dormant_review backfill_unreplied`」；⑤ 单坐席不删入口（零可行动数时 `_setPill` 既有隐藏语义即可） | `workspace_base.html` L3443–3470、`drafts_routes.py` risk-summary、`sla_watcher.py` + `config.example.yaml` + `release_defaults_changed.json`、`draft_review.html`、`services/messenger-web/server.js` 推送体 |

## 红线

- 只改显示分组与生命周期，**不改** `risk_grader` / Q-17 摘标冷却 / 停联告别守卫的判定。
- 只用现有令牌（`.fp-chip` 已有样式），不新增颜色。
- `unified_inbox.html` 共享文件：`stage_hunks.py --mine`。
- D 段：`_approve_block_reason` 判定一字不改（药丸只是换成读它）；`auto_expire` 只作废 **未发** 稿，不碰 approved / 在途；边车推送体只**加**字段不改既有字段；Node 边车与 `.py` 都须重启才生效。

## 开新对话粘贴

```
读 docs/指令_Q-31_头部瘦身与状态带唯一出口与列表降噪_2026-09-12.md，按它做 Q-31（#317 + PUUWJB：A _renderPanelTags 把 dormant:* / risk:* / 停联 / 接管 系统族标签归到「系统标签 ▸ N」折叠块，单坐席默认收起多坐席默认展开，用户标签照旧 Top8，每 chip 尾部 × 与 Esc 清筛选不删标签 / B dormant:ignored 在该会话有新入站时自动摘（只写一处），停联确认 = 归档 + 把「客户要求停联」从 conv_tags 移到 conv_meta.stop_contact_at 且 stop_contact 守卫改读 meta 不弱化，确认框写清后果 / C 门禁 tests/test_panel_tags_q31.py 四例 / D 顶栏「发前确认」药丸只计可行动稿：risk-summary 加 actionable 分桶复用 _approve_block_reason 单一入口、auto_expire_hours 出厂 48h 只作废 L1 未发稿并走 release_defaults_changed 五步、审批台「清空 N 条超龄稿」= 批量拒绝 reason=bulk_stale、Messenger 边车 backfill 推送体带 backfill:true 走既有不起草链，先用 skuio 报告核 47 条稿的 created_ts 是否集中在 PIN 通过后）。预算 $100。先读 dormant_review.py 与 stop_contact.py 的现有语义（Q-17 冷却摘标不动）与 workspace_base.html refreshL4 / sla_watcher.auto_expire，再 scripts\agent_probe.ps1 -Intent 登记；unified_inbox.html 用 python tools/stage_hunks.py --file <路径> --mine <regex> --apply 只暂存自己的 hunk；不新增颜色。每个 commit 带 #317 并立刻补落点表 docs/发版对账_v1.0.85_Q31.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-31 行并 agent_probe -Done。
```

## 开新对话粘贴（09-13 00:4x 老板拍板 B：只做 D 段——A–C 已由前一条线收工在 HEAD，不要重做）

```
读 docs/指令_Q-31_头部瘦身与状态带唯一出口与列表降噪_2026-09-12.md，只做 Q-31 的 D 段（PUUWJB，挂在 #317 下：skuio 顶栏「发前确认 47 / 待处理 6」计数无限堆叠、要一条条点开确认，用户要求去掉入口或只计新来的 + 过期自动清 + 一键清空）。A–C 已收工在 HEAD（ba18d8ce / e759c8c4 / b89a2f28，落点表 docs/发版对账_v1.0.85_Q31.md），先读落点表与文档「事实」里 PUUWJB 那条再动。D 要做：① GET /api/drafts/risk-summary（drafts_routes.py L182）加 actionable 分桶——只计 approve_block_reason(d)=="" 的 pending（稿龄 ≤ stale_approve_hours 且会话此后无出站，复用 DraftService.approve_block_reason 单一入口不另算），workspace_base.html refreshL4（L3443–3470）药丸数字与 title 改读 actionable，超龄 / 已回过的只在审批台可见；② inbox.sla_watcher.auto_expire_hours 出厂 0→48 + auto_expire_levels [L1]（只作废未发稿、审计留痕、不改任何发送行为），按 Q-4 F 五步维护 config/release_defaults_changed.json + i18n_packs/release_notice.py 三语 + 跑 tests/test_release_notice_q4.py；③ 审批台 draft_review.html 顶部 + 药丸悬停出「清空 N 条超龄稿」= 既有批量拒绝 reason=bulk_stale；④ services/messenger-web/server.js 的 backfill 推送体加 backfill:true, backfill_source:"msg_backfill"（只加字段），走 unified_inbox_account_routes.py L1637 既有「backfill 不起草→dormant_review backfill_unreplied」；⑤ 单坐席不删入口，零可行动数时沿用 _setPill 隐藏。门禁 tests/test_pill_actionable_q31.py：超龄稿不计 / 已回过不计 / 新鲜稿计 / auto_expire 默认值与 levels / 边车推送体含 backfill 字段（node --test 一例）/ 批量清空只动超龄。预算 $40。先用 skuio 报告 tmp_diag\PUUWJB 核 47 条稿是否集中在 PIN 通过后（有 messages 数据才下结论，没有就写「候选根因」）。先 scripts\agent_probe.ps1 -Intent 登记；approve_block_reason 判定一字不改；auto_expire 不碰 approved / 在途稿；Node 边车与 .py 都须重启才生效，不要自己重启（发版线统一装载）。每个 commit 带 #317 并立刻补落点表 docs/发版对账_v1.0.85_Q31.md D 行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md Q-31 行的 D 部分并 agent_probe -Done。
```
