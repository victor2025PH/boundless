# P1 工程化改进 实施记录

**日期**：2026-07-11
**执行人**：AI 助手
**配套文档**：`工作区分析与优化报告.md`、`P0实施记录.md`
**硬约束**：mur/Anaconda 环境未经确认不得改动 → 本阶段只做「纯文件、零环境改动、零运行中断」的部分；凡需安装/改环境的一律出命令清单待确认。

---

## 一、本阶段范围重划（对原 P1 方案的优化）

原 P1 计划含「pip-compile 依赖锁定 / 安装 gitleaks / 安装 pre-commit」——这些都会改动 Python 环境，违反硬约束。故把 P1 拆两批：

- **P1-a（已执行，零环境改动）**：配置模板补齐、端口单一权威来源、密钥扫描配置、pre-commit 配置文件。
- **P1-b（待你确认，涉及环境变更）**：安装并启用 pre-commit/gitleaks、依赖锁定。

执行前已核实 **mobile-auto0423 未在运行**（无进程，8000/18080 未监听），改其配置文件无运行中断风险。

---

## 二、P1-a 已执行动作（已生效，均已语法校验通过）

| # | 动作 | 文件 | 价值 |
|---|---|---|---|
| 1 | 补齐缺失的启动模板 | `mobile-auto0423/config/launch.env.example` | 工具（start.ps1 / repo_health.ps1）已引用但文件缺失，现补回 |
| 2 | 端口单一权威来源 | `mobile-auto0423/.env`（注释掉死配置 `OPENCLAW_PORT=8000`） | 消除 .env(8000)/launch.env(18080)/cluster.yaml 端口冲突歧义 |
| 3 | 5 个敏感配置脱敏模板 | `config/cluster.yaml.example` 等 5 个 `.example` | 支撑 P0 的 git 取消跟踪（他人可据模板部署） |
| 4 | mobile 密钥扫描 | `mobile-auto0423/.pre-commit-config.yaml`（新建，含 gitleaks + 大文件拦截） | 从源头防 secret/大文件误入库 |
| 5 | telegram 密钥扫描 | `telegram-mtproto-ai/.pre-commit-config.yaml`（追加 gitleaks + check-added-large-files） | 同上 |

### 端口单一权威来源的定性依据（深挖后结论）

- `server.py` 端口 = `src/openclaw_env.py::openclaw_port()` = 环境变量 `OPENCLAW_PORT` 或默认 **18080**。
- `start.ps1` **只把 `config/launch.env` 载入进程环境**；全项目 **无任何 `load_dotenv`**，故 `.env` 不被 Python 加载。
- 结论：`.env` 的 `OPENCLAW_PORT=8000` 对服务器**完全无效**，是历史遗留噪音。已注释并留指引，权威端口唯一来源 = `config/launch.env`(18080)。

### 已验证

- 7 个新文件经 `git check-ignore` 确认**均可入库**（未被 .gitignore 误吞）。
- 2 个 pre-commit 配置、6 个 json/yaml example 全部通过 Python 解析校验。

---

## 三、P1-b 待确认执行（涉及环境变更，命令已备好）

### B1. 启用 pre-commit + gitleaks（一次性，会下载 hook 环境）

```powershell
# 在各仓库根目录执行；pip install 会改动当前 Python 环境，故需你确认
pip install pre-commit
cd D:\workspace\mobile-auto0423   ; pre-commit install ; pre-commit install --hook-type pre-push ; pre-commit autoupdate ; pre-commit run --all-files
cd D:\workspace\telegram-mtproto-ai ; pre-commit install ; pre-commit install --hook-type pre-push ; pre-commit autoupdate ; pre-commit run --all-files
```

> `pre-commit autoupdate` 会把配置里 `gitleaks` 的 `rev: v8.21.2` 自动更新到最新稳定版。
> 首次 `run --all-files` 若报历史遗留 secret，可用 `.gitleaksignore` 登记或轮换后忽略。

### B2. 依赖锁定（需先定「锁给哪个解释器」——见下方关键决策）

**关键发现（必须先决策）**：当前机器上 **声明的 mur/Anaconda 环境未激活**（conda 不在 PATH、`CONDA_DEFAULT_ENV` 为空），默认 `python` 及正在运行的 telegram 主程序用的都是**系统 Python 3.13.9**，index-tts 用的是 uv 管理的 3.10。→ 锁定依赖前必须确认**部署目标解释器到底是哪一个**，否则锁出来与实际运行不符。

本机已装 `uv 0.9.30`，推荐用它的「只解析不安装」模式（不改动任何环境）：

```powershell
# 方式一：uv 解析生成 lock（env-safe，不安装、不动 mur）——推荐
cd D:\workspace\telegram-mtproto-ai
uv pip compile requirements.txt -o requirements.lock --python-version 3.13
cd D:\workspace\mobile-auto0423
uv pip compile requirements.txt -o requirements.lock --python-version 3.13

# 方式二：如果想「锁定当前确实跑得起来的版本」，用实际运行解释器 freeze（只读，不改环境）
& "C:\Users\Administrator\AppData\Local\Programs\Python\Python313\python.exe" -m pip freeze > requirements.installed.txt
```

> 方式一得到「最新可解析」的可复现集合；方式二得到「此刻真实跑通」的版本快照。二者用途不同，建议先方式二留存现状快照，再方式一做干净锁定。

### B3. requirements 里内联 pytest/开发依赖（不改环境，仅整理）

可选：把两项目 `requirements.txt` 里被注释的 pytest 等移到 `requirements-dev.txt`，让运行时依赖更纯净。此为纯文件整理，可随时执行。

---

## 四、下一阶段（P1 剩余 + P2 前置）

| 优先级 | 事项 | 是否改环境 | 说明 |
|---|---|---|---|
| 高 | 确认部署目标解释器 → 执行 B2 依赖锁定 | 否(uv 解析) | 先解决 mur vs Python313 的口径 |
| 高 | 执行 B1 启用 pre-commit（含把 P0 的 git 取消跟踪一起做） | 是(pip install) | 需维护窗口/确认 |
| 中 | P0 尾巴：轮换集群 secret + 云端 Key + 弱口令 | 是(重启服务) | 见 P0实施记录 第四节 |
| 中 | 拆巨石入口 `main.py` / `facebook.py`（单独排期，配套测试） | 否 | 大重构，分模块小步提交 |
| 中 | 根目录 40+ 调试脚本归档到 scripts/scratch/ | 否 | 纯文件整理，降噪 |
| 低 | 端口一致性 doctor 脚本（启动前自检 .env/launch.env/cluster.yaml） | 否 | 防未来漂移 |

---

## 五、本阶段改动清单（便于回滚/审查）

```text
mobile-auto0423/
  .env                                   (改：注释 OPENCLAW_PORT，gitignore 忽略，仅本地)
  .gitignore                             (P0 已改：+5 敏感文件规则)
  .pre-commit-config.yaml                (新增)
  config/launch.env.example              (补回：工具引用的模板)
  config/cluster.yaml.example            (新增)
  config/device_registry.json.example    (新增)
  config/device_aliases.json.example     (新增)
  config/cluster_state.json.example      (新增)
  config/notify_config.json.example      (新增)
telegram-mtproto-ai/
  .pre-commit-config.yaml                (改：追加 gitleaks + check-added-large-files)
```

未触碰任何业务源码；未安装/卸载任何包；未重启任何服务。

---

## 六、P1-a 追加：根目录调试文件归档（2026-07-11 执行）

### 安全铁律（本次优化的核心）

telegram 主程序**正在运行**，故定规则：**只移动 git 已视为忽略（gitignored）的一次性文件**——这类文件必然不被应用导入，移动绝不影响运行；凡是已入库的真实工具一律不动。移动前先"只列不动"预演确认。

### 执行结果

| 仓库 | 调试脚本→`scripts/scratch/` | dump 产物→隔离区 | 被占用跳过 | 主程序 |
|---|---|---|---|---|
| telegram-mtproto-ai | 33 个（`investigate_*` / `tmp_*` / `_*` / `dbg_*` / `probe_*` / `check_deferred*` 等） | 128 个（png/xml/log/json/txt/html） | 0 | 全程存活未中断 |
| mobile-auto0423 | 0（其 .bat 均为真实运维工具，保留） | 20 个 | 0 | 未运行 |

- 安全门正确**保留** telegram 6 个已入库脚本：`check_log_config.py` / `check_status.py` / `check_syntax.py` / `diagnose_ai_connection.py` / `diagnose_emotion_issue.py` / `diagnose_group_reply.py`。
- 根目录调试残留：telegram 161 → 3（残留即上述被保留的真实工具），mobile → 0。
- dump 产物移至 `_cleanup_quarantine_20260711/root_dumps/<repo>/`，可恢复。

### 关键优化：防止"归档反而制造新的未跟踪垃圾"

root 的忽略规则（如 `/investigate_*.py`）是**根锚定**的，脚本一旦移进子目录 `scripts/scratch/` 就不再被那条规则忽略，会变成"未跟踪但可提交"的新垃圾。为此在 `scripts/scratch/.gitignore` 写 `*` + `!.gitignore`，让整个 scratch 目录不入库。已验证：`scripts/scratch/investigate_mr.py` 处于 ignored 状态，git 仅会跟踪 `scratch/.gitignore` 本身。

### 本次新增改动

```text
telegram-mtproto-ai/
  scripts/scratch/.gitignore   (新增：整目录不入库)
  scripts/scratch/*.py         (33 个调试脚本移入，均被忽略)
  (根目录 128 个 dump 移出到隔离区)
mobile-auto0423/
  (根目录 20 个 dump 移出到隔离区)
```

---

## 七、P1-b 执行：依赖锁定（env-safe，已完成）

目标解释器已定为**系统 Python 3.13.9**。用 `uv 0.9.30` 的 `pip compile`（**只解析、不安装、不碰任何环境**）生成锁定文件：

```powershell
uv pip compile requirements.txt -o requirements.lock --python-version 3.13 --no-header
```

| 仓库 | 结果 | 耗时 | 说明 |
|---|---|---|---|
| mobile-auto0423 | `requirements.lock`（99 行，全量固定版本） | 0.9s | 14 依赖，干净 |
| telegram-mtproto-ai | `requirements.lock`（224 行） | 64s | 含 `openai-whisper==20250625` → `torch==2.13.0` 等传递依赖 |

两个 lock 均确认 `git check-ignore` 为**可入库**。全程未安装/升级任何包，mur 与运行中的服务零影响。

**深挖发现（供后续决策）**：telegram `requirements.txt` 声明 `openai-whisper`（拽入 torch），但实际运行配置 `config.local.yaml` 用的是 `faster_whisper`（在 requirements 里反被注释）——**声明依赖与实际运行不一致**。锁定按声明如实固定；建议后续核对：若生产不用 openai-whisper，应从 requirements 移除以避免无谓的 torch 体积。

> 如需「锁定当前实际跑通的版本」而非「最新可解析版本」，另可对目标解释器执行只读快照：
> `& "…\Python313\python.exe" -m pip freeze > requirements.installed.txt`

## 八、P1-a 追加：根目录 PLAN 文档归档（安全子集）

原打算把 14 个 PLAN/STATUS 类文档整体归档，但**引用分析**(git grep)发现它们高度耦合：

- **仅 3 个孤儿**（无任何引用）→ 已移至 `docs/archive/`：`DEVELOPMENT_PLAN.md`、`HANDOVER_MESSENGER_RPA.md`、`PROJECT_SUMMARY.md`。
- **11 个被引用**，且 `CURSOR_HANDOFF.md`/`DEPLOYMENT_STATUS.md`/`TODO_NEXT.md` 等被 **`AGENTS.md`/`CLAUDE.md`**（agent 指令文件）直接引用，其余又被 `DEPLOYMENT_STATUS.md`/`TODO_NEXT.md` 引用，形成引用网。**移动会断链**，故暂不动。

**优化点**：避免了"为清爽而移动却把 AGENTS/CLAUDE 的文档链改断"。这 11 个应作为一次专门的"文档重组 + 同步更新引用"的可评审改动来处理。

根目录 `.md` 由 18 → 15。telegram 主程序（PID 24476）全程存活。

### 本次新增改动

```text
mobile-auto0423/requirements.lock            (新增)
telegram-mtproto-ai/requirements.lock        (新增)
telegram-mtproto-ai/docs/archive/            (新增，含3个孤儿文档)
```

---

## 九、P1-a 追加：配置安全/一致性自检 doctor（新增）

### 设计决策（避免重复造轮子）

- 核实 `scripts/ops/repo_health.ps1` 已覆盖分支/脏文件/磁盘/vendor/pre-commit 状态，但**不检查**端口一致性、密钥泄露、敏感文件是否真未被 git 跟踪、`.example` 是否齐全 → 新 doctor 只补这些**安全/一致性缺口**，不重复已有检查。
- 用 **Python（stdlib，跨平台，可 CI/测试化）**，非再写一个 PowerShell。
- **不把泄露密钥明文写进脚本**：改存其 SHA-256，靠哈希比对判断"是否仍未轮换"，既能检测又不二次泄露。

### 新增文件

`mobile-auto0423/scripts/ops/config_doctor.py`（只读；`python scripts/ops/config_doctor.py [--json]`；退出码 0/1/2）。检查项：

1. 5 个敏感文件是否「已忽略且未被 git 跟踪」
2. 5 个 `.example` 模板是否齐全
3. 端口一致性（launch.env ↔ cluster.yaml；.env 不应再设活跃端口）
4. `shared_secret` 是否为占位/空/已知泄露值（哈希比对）
5. `requirements.lock` 是否存在且不落后于 requirements.txt

### 首次运行结果（真实反映 P0 待办）

```
verdict [2] ACTION REQUIRED  (FAIL=6 WARN=0)
  FAIL x5  5 个敏感文件仍被 git 跟踪  → 待执行 P0 的 git rm --cached
  FAIL x1  shared_secret 仍是已泄露旧值 → 待轮换
  OK       5 个 .example 模板齐全（P1 已建）
  OK       端口一致（launch.env↔cluster.yaml=18080，P1 已修）
  OK       requirements.lock 存在（P1 已生成）
```

doctor 精确暴露"P0 安全尾巴还没做完"，做完后会自动转绿——正是回归守卫的价值。

### 关于 requirements-dev 拆分：经核实后放弃（避免冗余）

原计划把两项目的 dev/test 依赖拆到 `requirements-dev.txt`。核实发现 **telegram 已有 `requirements-ci.txt`**（24 行，含 pytest/xdist/asyncio 等）承担该职责，再建就是冗余。故**放弃此项**，改为：需要时在 CI 用现有 `requirements-ci.txt` 即可。这是"深挖后砍掉低价值/冗余任务"的优化。

### 本次新增改动

```text
mobile-auto0423/scripts/ops/config_doctor.py   (新增，只读自检)
```

---

## 十、P1-b 追加：whisper 依赖核查与修正（本轮）

原计划"移除 telegram 多余的 openai-whisper 省 torch"。**深挖后反转**：

| 发现 | 证据 |
|---|---|
| openai-whisper **不是冗余** | `src/voice_transcriber.py:179 import whisper` —— 是 `whisper_local` 可选 ASR 后端（UI 可选），删了=砍功能 |
| faster-whisper 才是默认后端**却未声明** | 代码/配置默认 `provider=faster_whisper`（`audio_pipeline.py`/`voice_transcriber.py`），但 requirements.txt 里被注释成"可选" |
| 运行环境实况 | Python313 实装 `faster-whisper 1.2.1` + `openai-whisper 20250625` + `torch 2.6.0+cu124`(GPU) |
| lock 的 torch 与实际不符 | lock 解析出 `torch 2.13.0`(CPU/PyPI)，运行的是 `2.6.0+cu124`(pytorch 专用索引) —— GPU 部署坑 |

**已做的安全修正**：在 `telegram-mtproto-ai/requirements.txt` 补上默认后端声明 `faster-whisper>=1.0.0`（并注明 openai-whisper 是可选重依赖），重生成 lock（264 行，含 `faster-whisper==1.2.1`/`ctranslate2==4.8.1`）。**未删 openai-whisper**（保功能），是否为减体积而移除留作产品决策。

**torch GPU 坑**与 **openai-whisper 体积决策**已写入 `维护窗口执行手册.md` 第 4 节。

### 新增交付

```text
telegram-mtproto-ai/requirements.txt   (修正：声明默认后端 faster-whisper)
telegram-mtproto-ai/requirements.lock  (重生成，264 行)
docs-business/维护窗口执行手册.md       (新增：B 类收尾一站式手册，以 doctor 为闸门)
```

---

## 十一、C 类启动：main.py 拆巨石 Stage 1（已安全落地）

C 类（拆巨石）本属"需单独立项"的大改，但存在一个**服务运行中即可安全做且可验证**的最小切口——CLI 抽取，故先落地它作为 Stage 1 试点。

### 动作

- 新建 `src/bootstrap/`（`__init__.py` + `cli.py`），把 `run_config_check`/`run_init` 从 main.py **原样抽出**（行为不变），main.py 改为 `from src.bootstrap.cli import ...`。
- 依据：这两个函数仅被 `__main__` 调用、别处零引用（git grep 确认）。

### 验证（行为不变）

| 检查 | 结果 |
|---|---|
| `py_compile main.py + cli.py` | 退出 0 |
| 隔离 import cli.py（不加载 main.py） | OK，零副作用 |
| `python main.py --check`（对照基线） | **逐字一致**：exit 0，0 错误/1 警告 |
| main.py 行数 | 4098 → 4022（-76，符合预期） |
| 线上 telegram 主程序 | 本轮期间发生过一次重启（PID 24476→25088，23:12:39），**时间戳早于我的 main.py 编辑（23:13:14）→ 非本次编辑所致**（原因未定：用户/看门狗/崩溃自拉起）。新进程加载的是内存旧代码；我改的 main.py 经 --check 验证，下次重启才生效且安全。当前服务健康（web API 正常）。 |

> 过程中 PowerShell `Measure-Object -Line` 一度误报 3777 行，用 Python 精确计数复核为 4022（=4098-76），并 grep 确认 `class/initialize/start/stop/__main__` 结构完好、`run_config_check/run_init` 已从 main.py 移除——排除误删。

### 后续（Stage 2-4）

完整分阶段方案见 `docs-business/REFACTOR_BLUEPRINT_main.md`。关键结论：**Stage 2（抽 ~1230 行内联 FastAPI 工厂）及之后必须在 git worktree 隔离环境跑全量 pytest 验证 + 维护窗口重启**，不能在运行中的主工作树直接做（会与线上抢 SQLite/端口）。

### 新增交付

```text
telegram-mtproto-ai/src/bootstrap/__init__.py   (新增)
telegram-mtproto-ai/src/bootstrap/cli.py        (新增，从 main.py 抽出)
telegram-mtproto-ai/main.py                      (改：CLI 改为 import，-76 行)
docs-business/REFACTOR_BLUEPRINT_main.md         (新增：分阶段拆巨石蓝图)
```
