# 发版对账 v1.0.78 — O-3 目标摸底主动腿（HM7XBA / 5XRQ7C → #257，挂 #236 族）

> 指令：`docs/指令_O-3_目标摸底主动腿_2026-09-08.md`；底座 `docs/修复指令_O批_并行_2026-09-08.md`；前置 M-7 `docs/发版对账_v1.0.77_M7.md`（每拍可追溯 / 日志命名空间 / 到期结算 / 卡片六态——**不重做**）。
> 证据：`tmp_diag\HM7XBA\20260908-021043_report_report.md`、`tmp_diag\5XRQ7C\`。O-5 已把两份从 #255 改立 **#257**（P1，挂 #236 族）；commit 带 `#236 #257`。
> 预算 $150；执行方 O-3 对话，09-08 08:19 开工（`agent_probe -Intent` 已登记）。

## §〇 `auto=3 sent_24h=0` 到期未发根因（五选一）

**答：「没到期」——而且是设计上的没到期；再往后一天则是嫌疑 ①（客户活跃反而永不出手）。**

| 时刻（09-07/08） | 事实（报告 + 代码） | 判定 |
|---|---|---|
| 14:45 | evelyn×Martin 建 3 天「客户摸底」（`profile_discovery`）。`pace.py` L18：摸底模板只走**自然档** | 主动腿＝`sprint_ticker.due_daily`（自然档每日一拍） |
| 14:45–23:59 | `due_daily` 规则 1：**建目标当天（day 0）不排**（「当天再追一句是连珠炮」，D1b P0-3 设计） | 没到期 |
| 18:42 / 22:43 | `health_watchdog goal_sprint_liveness stalled: auto=3 sent_24h=0 oldest_h=4.0→8.0` | **误报**：全局 `stall_verdict` 用「建目标起的年龄 ≥4h」，不知道自然档第一个可出手时刻在次日 10:00（19h 后） |
| 00:00–10:00 | `due_daily` 规则 2：只在 `natural_window` 10–20 点 | 没到期（卡片「下一次≈10:00」这一句是真的） |
| 00:11–02:01 | 客户 9 条入站 → 每稿 `build_block_for_chat` → `refresh_goal` 排 2026-09-08 当日拍 → `mark_action(consumed)`；注入是软建议，9 条回复全跟客户话题、零问句（C 段问题） | 当日拍 `consumed` |
| 10:00–20:00（推演） | `due_daily` 规则 3：当日拍 `consumed` → 不拍。即便客户 6h 没开口、沉默闸放行，主动腿也因「回复链已经带过方向」整天不出手 | **嫌疑 ①**：`consumed` 被当成「今日已互动」，实际一句摸底都没问 |
| 02:10 报告 | 「已推进 2 拍」＝1.0.76 数 `consumed` 行（M-7 A/D 已改数 `beat_sent`）；摸底 0/4 是真的 | 卡片虚报由 M-7 覆盖；主动腿与采集链是本条 |

不是：到期被安静时段顺延（还没到期）/ 被出站闸 HOLD（没排过行）/ 起草为空（没起过）/ 派发器没跑（`冲刺推进器已常备 … natural_daily=True`，且派发器是 care 派发器，同机 care 行正常）。

**对症一处**（A `c3c64176`）：`due_daily(discovery=True)` 对摸底目标只认 `sent` 或 `detail=asked:<slot>`（C 段出站校验证实**真问出了问句**）才让位；`consumed` 但没问出来 → 到点照样排主动拍（沉默闸仍守，不打断正聊着的对话）。配套：全局 stall 判据改按「首拍资格时刻」起算（自然档 +24h、冲刺即刻），自然档建目标当天不再误喊 stalled；逐目标 stalled 在 `liveness` 落 **WARNING**。

**采集链纠偏（给 D 段与回访）**：报告称「采集走情景记忆抽取链，WYNN22 intents=[] 全跳过」——按代码不成立。摸底槽位采集 = `profile_slots.capture_from_text`（确定性正则，宁漏不错，坐标只认菲/东南亚白名单地名）+ `profile_llm.schedule_llm_capture`（LLM 摘录轨，`companion.goals.profile_llm.enabled`，**clean 包基线未开**，仅内测种子开）。`memory.extract.intents` 是情景记忆（O-2），与画像槽位不同表不同链。HM7XBA 0/4 的真因：客户只说了「昨天下雨 / 早上」——**没说城市、职业、年龄、爱好**，两条采集轨都无从填；AI 从没问。所以修法重心在「问」（B/C/E），D 段黄条按真实依赖写两条（画像 AI 抽取未开 / 记忆抽取白名单空），不把 0/4 归咎到记忆抽取。

## 一、改动落点（每项一个 commit，均带 #236 #257）

| 项 | commit | 落点 |
|---|---|---|
| **A 根因 + 对症** | `c3c64176` | `sprint_ticker.py`：`due_daily(discovery=)`——摸底目标 `consumed` 且 detail 非 `asked:*` **不让位**（普通模板逐字不变）；`probe_asked_in_action` / `is_discovery_goal` / `PROBE_ASKED_DETAIL_PREFIX`；`run_once` 摸底目标主动拍 `natural_source_text(probe_hint=discovery_gap_for_goal)` 钉「自然问一句：人在哪个城市（像朋友闲聊，一轮只问一个）」+ `[goal-probe] daily goal= conv= day= slot_hint= consumed= asked=` INFO；`probe_norm` / `parse_goal_care_kind` 新分型 `("probe", gid, ts)`＝`goal:{gid}:q{ts}`（E 段坐席手动摸底行，派发器按普通目标行 prompt 拟稿）；运行时闸 `RUNTIME_GATE_ORDER` 加 **`frozen`**（`conversation_frozen(meta)`：`stop_contact / contact_frozen / frozen / *_until` 任一为真 → 拦，记 `beat_blocked(frozen@槽)`；O-1 A 落地后只需对齐键名一处）；`BLOCKED_REASONS` 加 `frozen`。`liveness.py`：快照新增 `oldest_eligible_sec`（`first_eligible_delay_sec`：自然档 24h / 冲刺 0）；`stall_verdict` 优先读它（旧快照回落 `oldest_age_sec`）；逐目标 stalled → `logger.warning("[goal-liveness] stalled goal= conv= title= age_h= sent_24h=0 …")`。`tests/test_goal_probe_active_leg_236.py` 12 例 + `test_goal_liveness.py` 两处口径随改（30h/32h 才响；`hits2` 按 loop 过滤） |
| **B 第 1 天就摸 + 活跃自适应** | `5d1cc20b` | `planner.py`：`plan_beat(probe_asks=, active=)`——摸底模板（`gap_in_intent`）今日意图钉 `；今天至少自然问一个：<第一个未填槽问法>`（**第 1 天也钉**；退避陪伴日 / 情绪 hold / 沉默熔断优先，不钉——安全语义不被 KPI 覆盖），返回加 `probes / probe_slots / advanced`（非摸底模板形状 `{intent, push_level}` 逐字不变）；`customer_active(inbound_30m)`（`ACTIVE_INBOUND_MIN=5` / 30 min）→ 里程碑 0「先暖场」提前用里程碑 1「顺着话头带出想了解的那件事」意图池（力度仍按真实里程碑）；`with_probe`。`templates.py`：`profile_discovery` 里程碑 0 两条文案改「先把互动热起来…聊到相关处顺口问一个你想了解的，问完回到闲聊」/「…再顺着话头轻轻问一个」（旧「不急着问」与同块「本轮顺势了解：坐标」自相矛盾——HM7XBA 右栏「第 1/3 天·今日：先把互动热起来；顺着 TA 的话回聊」就是这句）；`PROBE_CLAUSE_SEP(_EN)` / `strip_probe_clause`；`intent_en_for` 拆子句：基础意图池精确匹配 + 问法按槽位表 `ask_zh→ask_en`（英文卡片不回落中文）。`signals.py`：`inbound_count_since` + `extras["inbound_30m"]`。`service.py`：`discovery_probe_asks(store, template, goal)`（未填勾选槽问法按勾选序、已问仍空排后）→ `refresh_goal` 传 planner；**`[goal-plan] goal= conv= day=1 probes=1 slots=['人在哪个城市'] active= inbound_30m= advanced= push=`** INFO；`merged_beat_intent` 先 `strip_probe_clause` 再按**本轮**缺口合流——卡片 `today.intent` 留计划子句给人看，注入块一轮只带一个问法（逐轮轮换 / C 段线索词换槽不与计划子句打架）。tests +5 |
| **C 注入变硬 + 线索追问** | `183e43c1` | `profile_slots.py`：`CUE_LEXICON`（双语闭集：天气 / 下雨 / 时差 / 早上好 / morning here / time zone → `location`；上班 / 加班 / work / boss → `occupation`；爸妈 / 孩子 / mom / kids → `family_status`；单身 / 男朋友 / boyfriend → `marital_status`；岁 / 生日 / birthday → `age`；周末 / 电影 / hobby / gym → `interests`；住 / 搬家 / apartment → `residence`）+ `detect_cues(text, slots=)`（拉丁词按词边界 + 常见词尾：rained 命中 rain、train 不命中）；`SLOT_ASK_KEYWORDS` + `has_question` + **`reply_asks_slot`**（有问句 **且** 带该槽问法关键词才算 asked——宁判 missed 多问一次）；`pick_probe_target(cues, unfilled, asked_today, retry_slot)`：`retry`（上轮没问出来同槽重试）> `cue`（话头对上未填槽）> `floor`（今天还没真问过 → 轮换第一个未填槽，兜「每天 ≥1」）> 已问过且无线索 → 不追问；`probe_hard_line(slot, cue)`＝「【本轮必问】客户刚提到「昨天下雨」，顺着这个话头接一句，然后**必须**问到：人在哪个城市——本条回复里要有这一个问题（像朋友闲聊，不要像查户口，一轮只问一个，但这一个必须问出来）」。`context_block.py`：`build_goal_block(probe_line=)`——与纪律行同级，超长**不丢**（上限按其长度放宽），有它时软【画像缺口】行不出。`service.py`：**`verify_pending_probe`**（goals 包唯一能看到出站正文的钩子＝下一次注入时刻）：读 `params._probe_pending{slot,cue,ts,mode,chain}` → 会话里该时刻之后的出站消息（多气泡合并）→ `asked` / `missed` / `pending`（还没出站，保留）/ `expired`（>24h 静默丢）；asked / missed 落事件 `probe_asked` / `probe_missed`（detail `slot@cue`、`text_head` 回复头）+ **`[goal-inject] target=location cue=昨天下雨 result=asked|missed conv= goal= reply=`**；asked 时当日拍 `detail=asked:<slot>`（A 段 `due_daily` 让位钥匙）；`decide_probe_target` → 硬行接管本轮（意图 `strip_probe_clause`、`profile_gap=""`），挂起 `_probe_pending` + 目标槽进 `_gap_asked` 轮换表 + **`[goal-inject] target= cue= mode=cue|retry|floor result=pending`**；`_note_meta` 多 `target_slot / probe_mode`（API `goal_applied` 可见）；`params._inject_count` 每目标每稿 +1（D 段「注入 N 次」，与 `beat_injected` 每槽一次不同口径）；params 一次落盘（缺口轮换 + 挂起 / 清除 + 计数）；`build_beats_trace` 新 kind `probe`（status asked / missed，phase=槽，reason=线索词）+ `summary.probe_asked / probe_missed`（含 today）。`stats.py`：`record_probe(asked|missed|manual)` + snapshot `probe{}` + `/metrics goals_probe_total{result=}`。tests +6 |

## 二、验收

| 测试 | 例数 | 钉什么 |
|---|---|---|
| `tests/test_goal_probe_active_leg_236.py` | 12（A） | HM7XBA 时间线回放（18:42 / 22:43 / 02:10 均 not_due，9/8 10:05 首次到期）；全局判据 8h 不响、30h 响、旧快照回落；`first_eligible_delay_sec` 冲刺 0 / 自然 24h；stalled WARNING 一行带 conv；摸底目标 consumed≠asked 三态 + 非摸底旧口径；ticker 端到端「00:11 consumed → 11:00 排入主动拍，source_text 含『人在哪个城市』」/ `asked:` 后不排；沉默闸仍守（记 `silence@d…`）；`natural_source_text` 四形态；probe 分型 + 旧三型不变；冻结闸四判 + ticker `frozen@d…` |
| 同文件 B 段 | 5 | 第 1 天计划含问句 + 旧暖场文案退场 + 英文反查两段 + 非摸底 / 全填零变化；活跃 → 里程碑 1 池、力度不变、阈值 5；退避 / 情绪优先于问句 + `with_probe` / `merged_beat_intent` 剥子句·轮换·全填；`refresh_goal` 端到端 `[goal-plan] day=1 probes=1 slots=['人在哪个城市']` + 9 条/30min → `active=True inbound_30m=9`；`discovery_probe_asks` 填一格少一格；注入块单问法 + 卡片 today.intent 留计划子句 |
| 同文件 C 段 | 6 | 线索词映射（rain / 下雨 / morning here + work / mom；train、message 不误中；按槽过滤）；`reply_asks_slot` 四象限（问句 × 关键词）；决议优先级 retry > cue > floor > 不追问 + 硬行文案；**HM7XBA 00:11–00:43 九条回放**：≥2 条注入块带坐标硬追问、块内同问法只出一次、软行让位；模型九次都不问 → 九次 `probe_missed` + `mode=retry` 重试 + `_inject_count=9` + beats trace `probe_missed`；第 10 轮问出「which city are you in?」→ `probe_asked` + 当日拍 `asked:location` + 日志 `result=asked` + 第 11 轮不再硬追（不连环）；`verify_pending_probe` 四态（无 inbox 保留 / 挂起前出站不算 / 两条气泡合并判 asked / >24h expired 不记事件 / none）；硬行超长不丢 + 软行让位 + 无硬行时软行原样 |
| 随改 | — | `test_goal_liveness.py`（30h/32h；hits2 过滤 loop）；B 段 `test_inject_block_day1_carries_probe_once` 口径改「第一稿走 floor 硬行」 |

回归（C 后，09-08 09:5x）：`probe_active_leg + wiring + context_block + inject_no_silence_236 + inject_keys_166 + inject_visibility_152 + service + stats + beats_trace_236 + acquire_funnel + bridge + chain_e2e + routes + discoverability` **364 passed**（中途 `test_goal_wiring::test_discovery_five_turns_rotate_unfilled_slots` 红一次：硬行措辞没带 I-3 的 `HARD_ASK_DISCIPLINE` 原句——改硬行用同一常量，不改测试）。

回归（B 后，09-08 09:2x）：`test_goal_probe_active_leg_236 + planner_ledger + templates + service + inject_no_silence_236 + inject_keys_166 + inject_visibility_152 + acquire_funnel + wiring + bridge + chain_e2e + routes + agenda` **357 passed**（中途 `test_goal_wiring` 三例红＝计划子句与逐轮缺口双问法打架，已用「注入前剥计划子句」修掉，不是改测试）。

回归（09-08 08:5x）：`test_goal_probe_active_leg_236 + test_goal_liveness + test_goal_autoadvance_d1b + test_goal_beats_trace_236 + test_goal_sprint_ticker + test_goal_preflight_p04 + test_goal_expiry_settlement_236` 104 passed / 1 failed——红的是 `test_goal_expiry_settlement_236::test_card_frontend_states_and_watchdog_names_goal` 断言三宿主 `cp-goal.js?v=20260907e`，**N-5 发版 `aaaf983c` 已把戳改成 `20260908b` 未随改测试**（先于本线开工），D 段 bump 戳时一并修。

## 三、越界点名

无。`health_watchdog.py` 的 INFO 行未动（WARNING 落在本线 `liveness.py`）；`background_tasks.py` `_care_sent_hook` 未动（probe 行走 else 分支照记 `beat_sent`，E 段在 `build_beats_trace` 按 care 行 `topic_norm` 回标 `probe`）；`skill_manager.py`（O-2 ACTIVE）/ `persona_reply.py` / `drafts.py` / inbox `store.py` 未碰。

## 四、交 O-5 / 下一收口

（随 B–E 补）
