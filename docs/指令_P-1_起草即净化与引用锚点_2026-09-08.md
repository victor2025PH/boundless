# 指令 P-1：起草即净化 + 引用锚点守卫 + 「AI 指纹」卡

> 预算 **$150**。工单 **#259 #254**（报告 U4ZAD7 / Y3C6PS / ZH3ZQ5③ / 34585H E·F）。目标版本 1.0.79。
> 底座 `docs/修复指令_P批_并行_2026-09-08.md`（归属矩阵 P-1 行）。落点表 `docs/发版对账_v1.0.79_P1.md`（自建，每 commit 一行）。

## 开工前必读

1. `docs/全自动出站链_三视角方案_2026-09-08.md` §0 N1 / N4、§1.1 第 1 / 5 条。
2. `docs/发版对账_v1.0.78_O1.md` **B 行**（`583af62b`：`src/inbox/outbound_humanize.py` `humanize(text, lang, mode)`，挂在 `src/inbox/outbound_translate.py` L736 `pass_gate_only` 之后；`scripts/outbound_style_gate.py` 200 样例门禁）与 **C 行**（`ea20cefd`：`persona_guard` 客服腔句级守卫「改写一次 → 仍命中转人审」——`claim_guard` 照这个结构做）。
3. 证据：`tmp_diag\ZH3ZQ5\*_report.md`（14:37:43 `auto_generate_draft L1 reason=first_contact` 草稿含 — 且「that hiking trail you mentioned a while back」，机器当天 clean 重装、记忆 0 条）；`U4ZAD7`（约 80% 回复带 —；用户要求单独最先交付）。
4. 锚点：`src/inbox/drafts.py` L1238 附近 `auto_generate_draft` 落 draft 行处（净化要在**写入 draft 表之前**）；`src/inbox/persona_reply.py`（LLM 生成侧系统提示）；关怀润色 `src/contacts/care_dispatcher.py`、目标冲刺 `src/companion/goals/sprint_ticker.py`、问候 `GreetingSkill`（`skill_manager.py` L678 附近）各自的文本出口——**找到它们共同经过的最后一处**（大概率就是 `auto_generate_draft` / `deliver_now` 前的一处），净化只挂那一处 + 发送门兜底。
5. `scripts\agent_probe.ps1` 看 ACTIVE：R78 在装载回归（不碰 `package.json` / 发版脚本）；P-3 也会动 `persona_reply.py`（它只动结构化 `send_photo` 段）——先登记意向，谁先提交谁先，后者 rebase 自己的 hunk。

## 红线

- 只动归属矩阵 P-1 行；`drafts.py` 只加「净化调用」一处（P-2 会在同文件加「年龄闸」一处，互不覆盖，提交前 `git diff` 确认只含自己的 hunk）。
- verbatim 原文直发 / 人工手发 / 翻译 identity **不净化**（O-1 B 已有旗标，沿用）。
- `git add <显式路径>`；每 commit 带 `#259`；不 `-A`、不 `reset --hard`。

## A 净化前移到起草层（草稿 = 将发文本）

- 现象：1.0.77 用户看到草稿里仍有 —（O-1 B 未装载是一半原因；另一半：即使装了，B 只在发送门处理，工作台草稿、L1 审核稿、关怀预览仍显示原文）。
- 修法：`auto_generate_draft` 写入 draft 行之前调用 `humanize(text, lang, mode)`，把 `punct_fix / style_fix` 计数存到 draft meta；发送门保留兜底并记 `punct_fix=0`（正常）/ `>0`（说明有绕过起草层的路径，告警一行）；关怀润色 / 目标冲刺 / 问候若不经 `auto_generate_draft`，各自出口同样先净化再落库。
- 验收：工作台任意新草稿肉眼无 — / ; / 弯引号；日志每条草稿 `[draft] punct_fix=n style_fix=n`；发送门 `punct_fix=0` 占比 100%（绕过路径为零）；tests：起草层净化 + 发送门兜底 + verbatim 不净化。

## B 引用锚点守卫 `claim_guard`

- 现象：「that hiking trail you mentioned a while back」——清库重装、记忆 0、人设 0，无任何依据；与 RRYKF6「被工作埋了」同类编造。
- 修法：新 `src/inbox/claim_guard.py` 纯函数 `check_claims(text, *, history_texts, memory_facts, lang) -> (text, report)`：引用类短语（you mentioned / you said / you told me / last time (you) / a while back (you) / as you said / remember when / 你之前说 / 上次你说 / 你提过 / 你说过 / 记得你 / 前回…日文 前に言ってた 等三语表）→ 抽取被引用的实体 / 关键词（名词短语）→ 在最近 200 条会话文本 + 该客户记忆里找锚点（子串 / 词干 / 同义小表）→ 找不到：整句改写为中性（"how have you been?" / "what have you been up to?" / 「最近怎么样？」类，按语言取模板）；找到：放行。日志 `[claim-guard] conv=… phrase=… anchored=1|0 action=pass|rewrite`。挂在 A 段净化之前（先去谎再去标点）；覆盖自动回复、opener、关怀 AI 润色、目标冲刺；verbatim 除外。
- 验收：回放 ZH3ZQ5 → 改写为中性；给一条历史含 "hiking" 的会话 → 放行；tests 三语各 3 例 + 误伤例（"you mentioned" 出现在客户原话引用里不改）。

## C 生成侧同步

- `persona_reply.py` 系统提示（陪伴底稿 O-1 C 之上）加硬禁：不用破折号 / 分号 / 项目符号；不引用未在上下文出现的事实；无历史时不假装熟悉；5–10 条 few-shot（有 `spoken_style` 包的用包，否则用底稿自带）。MaxTokens 默认 60–80（O-1 B 若已做则核对）。
- 验收：200 样例门禁跑生成侧（不只后处理）破折号 <5%（后处理兜到 0）。

## D 质检页「AI 指纹」卡

- 位置：既有质检 / 出站质量页（找 `ops_quality` 或 `outbound` 统计页；没有就挂在 ops_overview 一张卡）。四个数：破折号率（起草层 `punct_fix>0` 占比）/ 客服腔命中率（O-1 C 日志）/ 引用无锚点次数（B 段）/ 承诺无动作次数（P-3 落日志后自动有值，先留位）。近 24h，红黄绿阈值 0% / <2% / ≥2%。i18n 三语；`?v=` 版本号。
- 验收：真浏览器看到四格；数值与日志 grep 一致。

## 收工

- 落点表 `docs/发版对账_v1.0.79_P1.md`（A–D 每 commit 一行 + 装载要求：`.py` 需重启、模板热更）。
- `docs/P批_进度总账.md` **只改 P-1 行**（收工后 P-6 才能开，写清 `outbound_humanize.py` 接口是否有变化）。
- `agent_probe.ps1 -Done`；回访文案交 P-5（给 skuio：草稿层净化在哪看、`[draft] punct_fix` 日志、claim-guard 例子）。

## 开新对话粘贴

```
读 docs/指令_P-1_起草即净化与引用锚点_2026-09-08.md，按它做 P-1（#259 #254：去 AI 标点从发送门前移到起草层使草稿=将发文本 / 新 claim_guard 引用锚点守卫「you mentioned·你之前说」无锚点改中性 / 生成侧禁令+few-shot / 质检页「AI 指纹」卡）。预算 $150。先读 docs/修复指令_P批_并行_2026-09-08.md 的归属矩阵与 docs/发版对账_v1.0.78_O1.md B/C 行（在 O-1 B/C 之上做，不重做），再 scripts\agent_probe.ps1 -Intent 登记；drafts.py 只加净化调用一处（P-2 会加年龄闸一处），persona_reply.py 与 P-3 分段；verbatim/人工手发不净化。每个 commit 带 #259 并立刻补落点表 docs/发版对账_v1.0.79_P1.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/P批_进度总账.md 的 P-1 行并 agent_probe -Done，回访文案交 P-5。
```
