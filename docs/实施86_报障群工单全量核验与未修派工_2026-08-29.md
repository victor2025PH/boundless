# 实施86：报障群工单全量核验与未修派工（2026-08-29 晚值守盘点）

> 作者：duty-watch 值守线（只核验不施工）。本文＝**开发线的开工输入**。
> 工单库 `D:\chengjie-instances\zhiliao\data\config\bug_intake.db`，处置台
> `/admin/bug-tickets`；截图证据在
> `D:\chengjie-instances\zhiliao\data\protocol_media\telegram\6834964252_<msgid>.jpg`。
> 核验方法：46 张开放工单逐单过目 + 三个修复提交（386d069f / 2cc6388c /
> b2e24d09）与 v1.0.59/60 发版清单交叉对照 + 关键点直接读码验证。
> 现网 testers 版本 **1.0.60**（09:50 发布已广播）；两位主力测试人：
> skuio（8942577244）、钧 JUN/十翼（8852939166，机器码 B990-0C62-FC1C-F668）。

## 0. 一句话

46 张开放单里：**8 件已随 1.0.59/60 出货等验证（勿重复开发）、2 个域正被活跃
lane 施工（勿撞车）、其余按 8 个域打包待开发**——最重的三块是「克隆语音在
客户机仍回落」「Messenger 发送失败家族」「客户端↔官网连通性」。

## 1. 已修复已上线（1.0.59/60），等报障人验证——**勿重复开发**

| 工单 | 事项 | 载体 | 状态 |
|---|---|---|---|
| #20 | 会话列表滚不到「已归档」入口 | `.conv-items` min-height 热修 | fixed，19:45/20:08 已回访（带按钮） |
| #44（#46 并单） | LINE 只发不收 | line_pull_sync 位点根治，随 1.0.59 起 | fixed，20:12 按钮回访，等 skuio 点 ✅ |
| #45（#30 并单） | 「无界科技·在线顾问」入口隐藏 | 386d069f 客户形态默认隐藏 | fixed，20:12 按钮回访，等钧点 ✅ |
| #35 | 发送额度硬限制 → 用户自主 | 1.0.60 设置→回复设定→「账号发送额度」（调/关/白名单/实时用量） | fixed，回访并入 20:11 群答复 |
| （值守承诺） | 防复读拦截后空窗 | b2e24d09 拦截后自动换说法重试 | 已上线，钧的 taihua009 案例即此 |
| （值守承诺） | 客户消息不进系统通知/通知中心 | 2cc6388c，开关默认关、系统事件保留（`test_notify_center_denoise` 钉住） | 已上线 |
| （值守承诺） | AI 让位横幅：原因+接回 | 2cc6388c 补原因；「让 AI 接回」按钮既有（`_renderTakeoverBanner` rearm 段，unified_inbox.html:808） | 已上线；客户端待验证 |
| （值守承诺） | 查看≠接管 | 行为本就如此；9af0e6ce 加触发面门禁 `test_takeover_trigger_surface` 钉死 | 已钉住 |

⚠ **克隆语音（#41/#42/#26）虽随 1.0.59 官宣修复，但 skuio 10:21-10:27 复测仍
回落通用音**——不能算已修，见 §3 域A。

## 2. 施工中（活跃 lane 已认领）——接单前先过意向板，勿撞车

| 域 | lane（意向板可见） | 覆盖的工单/承诺 | 未被认领的残余 |
|---|---|---|---|
| 工作目标节奏 | goal sprint pace P1（intents/outcome/report + cp-goal，`src/companion/goals/pace.py` 在途未提交） | 「推进速度档」（natural/today/session 三档+封顶）、「阶段完成即进下一阶」、「本阶段指令」（cp-goal 侧） | 新客快速摸底期、AI 画像抽取兜底、摸底类目标硬约束（见 §3 域E）；#54 调整期限死按钮需与该 lane 对齐（cp-goal 正在改） |
| 繁体 UI 语言包 | zh_hant lane（`i18n_packs/zh_hant_auto.py` + `cp-i18n-ext.zh_hant.js` 等今天活跃编辑中） | 界面繁体 | **AI 回复侧繁体自适应完全没做**（读码确认 reply 链零 zh-hant 处理），见 §3 域G |
| 克隆语音复测 | 值守在跟（skuio 三步排查：确认 1.0.60 / 浏览器开官网 / 重测语音预览） | #41/#42/#26 的「修没修好」判定 | 若坐实 1.0.60 仍回落 → §3 域A 升 P1 立即开工 |

## 3. 未修派工（本批开发对象，按域打包）

### 域A 语音克隆与语音发送链 —— P1（老板日程第一位，已两次官宣修复被复测打脸）

1. **克隆音在客户机回落通用音（#41 skuio / #42+#26 钧机 B990）**
   - 证据：10:24 截图（…674.jpg）工具箱明示「人设 steven·后端 edge_tts·音色
     zh-CN-XiaoxiaoNeural·由 avatar_clone 回落／⚠克隆音色暂不可用」；10:27
     （…675.jpg）克隆对方音色绑定成功仍系统音；钧机 TimeoutError（#26）+
     试听无声。
   - 关键疑点（值守已给出统一假设）：同时段 10:11 该机报「连不上官网服务」
     （#51，…669.jpg 红字实锤），新版克隆链走官网云端通道（bd2026.cc hub
     TTS 中继），**客户机→官网连通性中断则克隆必回落**。117 侧 19:56 黑盒探
     `POST /api/ai/hub/v1/tts/clone` 返回 401 invalid_token＝路由在线，非全网故障。
   - 排查落点：桌面包内 `voice_profile` 的 backend 键实际值（截图显示仍是
     `avatar_clone`——若包内 profile 没切到云端通道键，回落是必然）；
     `src/ai/tts_pipeline.py`（B124 sidecar 回落已进 1.0.59）与
     `src/ai/hosted_gateway.py` 的 TTS 走向；`config.desktop.internal.yaml`
     的 TTS 段；hub 侧 `website/app/api/ai/hub/v1/tts/[op]/route.ts`
     （机器 token 鉴权/额度）。
   - 验收：skuio 与钧两台机，1.0.60 上工具箱语音预览显示「后端=克隆链」
     不再出现「由 avatar_clone 回落」；试听=克隆声；#41/#42/#26 按钮回访 ✅。
2. **客户端↔官网连通性诊断与降级（#17 P0 + #51）**
   - #17 是 P0 且 8-19 就报了「还是连不上官网」+输入框无光标；#51 是 10:11
     报障上报失败同根因。客户端多条链依赖 bd2026.cc（报障上报/克隆 TTS/额度
     网关/更新检查），**没有连通性自诊断与失败暂存**。
   - 开发项：①「求助与支持」上报失败本地暂存+网络恢复自动补传；②工作台加
     官网连通性探针（失败给用户一句人话+重试按钮，而不是各功能各自静默回落）；
     ③克隆 TTS 通道不可达时预览区已有黄字警示（好），但应同步引导「检查网络/
     稍后重试」而非只说「暂不可用」。
   - 验收：断网模拟下报障不丢（恢复后补传成功）；克隆回落时用户能看懂原因。
3. **语音发送失败旧案复验批（#3/#4/#5/#9 steven 音频红框报错族 + #8 语音翻译失败）**
   - 全是 08-16~08-18 旧案，期间语音链大改（B114 hub 超时→104 专用节点、
     B124 sidecar、1.0.60 tts 口语化 batch1）。**先复验再决定**：让报障人在
     1.0.60 重测一轮，仍复现的才开工；值守可代跑回访。
   - #8 附带产品诉求（已认可待做）：电脑版语音输入支持「麦克风录音」与
     「发送本地语音文件」两种方式（当前仅「打字→克隆合成」）。
4. **工具箱克隆登记入口收敛（#31/#25/#41 内三次重复诉求 + B115 旧案）**
   - 诉求一致：工具箱的克隆录入撤掉，只留人设工作室一个入口。产品已倾向
     同意（B115 登记过）。纯前端收敛 + 指路文案。

### 域B Messenger 链 —— P1（发送失败是持续性主诉，四单同族）

1. **发送失败家族（#21 网络失败 / #23 send failed 500 / #34 PIN 重登后仍失败 /
   #49 Too Many Requests）**
   - 三种报错三个层次：500=我方 messenger-web worker 异常（`services/
     messenger-web/server.js` composer 回读链）；Too Many Requests=平台限频
     （需自动降速+退避，当前失败后不重试不降速）；#34 还叠加「重登+PIN 后仍
     失败」+ 一次「You're temporarily blocked」平台风控页。
   - 开发项：①失败分类透出（平台限频/风控/我方异常三类文案分开，别都叫
     「发送失败」）；②Too Many Requests 自动退避降速（对齐 WA 的节奏闸思路）；
     ③失败消息可一键重发（#21 附带「无法发送的消息也没有记录」——失败消息
     要留痕可重试，不能消失）。
   - 验收：三类失败各自有明确文案；限频后自动降速不再连环撞墙；失败条留在
     会话里带重发按钮。
2. **登录二次验证窗口不可用（#22 无法放大 / #27 被成功提示遮蔽）**
   - 桌面壳 webview 弹窗尺寸/层级问题：验证码页无法放大导致手机收不到 6 位码
     流程走不完；「登录成功」提示层遮住验证码输入框。落点 desktop 壳
     （`desktop/main.js` 弹窗窗体）+ messenger 登录流。
   - 验收：验证窗可缩放/全屏；任何提示层不遮输入框。
3. **消息重复显示（#31，手机端正常、工作台重复）**
   - 镜像入库去重缺口（同一出站被回读+镜像双记嫌疑）。落点 messenger-web
     回读确认链 + inbox 镜像去重。与 #14（全自动回复 3 条各重复 1 次，
     telegram 侧）可能同根——**先查是显示层重复还是真双发**（防双发主线有
     send_dedup，但 RPA 回读镜像不走它）。
   - 验收：手机端与工作台消息条数一致。

### 域C 客户端基础设施 —— P1

1. **TG 更新后掉线要重登，WA 不用（#19）**
   - 疑点：更新过程动了 telegram 会话文件的存放/权限（WA 凭据存放路径不同
     所以幸免）。落点：desktop 打包/更新器（`deploy/desktop/
     push_chatx_hotpatch.ps1` + electron-updater 安装流）与包内后端 telegram
     session 数据根的更新期保留策略。
   - 验收：连续两次版本更新，TG 账号保持在线。
2. **自动更新后顶部功能栏消失（#25 内）**
   - 更新完成后 UI 缺块，重开窗口恢复嫌疑。落点 desktop 壳更新后的窗口重载链。
3. **报障上报失败暂存补传（并入域A-2，#51）**

### 域D 记忆与人设一致性 —— P1/P2（「AI 像不像人」的主诉集中地）

1. **生日没记住反问客户（#43，P1 建议）**
   - 实录：客户说「因为今天是我生日」，AI 回「哦等等，今天是你生日？」。
     生日是最高价值的 user_stated 事实，抽取/召回两头都要查：抽取器是否落了
     bullet、prompt 召回窗是否带上。落点 `episodic memory` 抽取（
     `extract_memory_bullets`/启发式）+ 生日类事实的召回权重；另有现成
     milestone_ritual（纪念日）体系可挂。
   - 验收：evals 加生日场景金标（当轮说生日→下一轮不反问且能主动祝福）。
2. **称呼混乱 babe/baba（#24 + #34 内 + #41 内 ma'am 案）**
   - 双向称呼错位：我们叫她 babe、她叫我们 baba，AI 聊着聊着互换；且记忆库
     还错记了「用户自称 baba」（错误记忆放大错误称呼）。落点：称呼作为结构化
     档案字段（谁叫谁什么）而非散落记忆；persona prompt 注入「你对 TA 的称呼/
     TA 对你的称呼」两条钉子；memory_grounding 对称呼类事实加方向性校验。
   - 验收：金标对话 20 轮称呼零互换；错误称呼记忆可在记忆库改正（见 3）。
3. **记忆库管理四问（#41 内，产品缺口）**
   - 用户问：确认属实=入记忆？删除=不入？错误记忆能不能编辑？未确认的是否
     已生效？——当前记忆库 UI 只有「确认属实/删除」两键，语义不透明。
   - 开发项：记忆条目加「编辑后确认」；页面顶部一句话说明三态语义（未确认=
     已参与召回但低权重/还是不参与？按实际实现写，别猜）。
   - 验收：#41 报的「用户自称 baba」错误记忆可改正为正确方向。
4. **背景资料不生效（#29，1.054 旧案）**
   - 「父母职业等背景完全错误、AI 不按人设框架聊」——1.054 时代报的，其后
     人设链改动很多，**先在 1.0.60 复验**；仍复现才开工（查会话实际生效人设
     vs 编辑的人设是否同一个，历史上有「改了非生效人设」的同类误报，见 #3 内
     值守答复）。
5. **出站单语种校验（#43 内「I'm 我 going anywhere」= B121，0827 已标 P1 加急）**
   - 中英混杂出站是硬穿帮。B121 在实施68 派过 P1 加急，**核验时未见对应提交**，
     视为未修。落点：出站前语种一致性守卫（复用 translation_confidence 的
     错语种检测口径，拦下→重写一次→仍混则发原语种版本）。
   - 验收：eval 金标（中英混杂样本 100% 拦截重写，纯英/纯中零误伤）。

### 域E 工作目标/工作链 —— P2（goals pace lane 在途，以下是其未认领部分）

1. **新客快速摸底期（钧 04:27-04:29 拍板的默认行为）**
   - 新添加客户自动进入摸底期：AI 每轮自然带出一个轮廓问题（称呼/坐标/生意/
     兴趣），轮廓齐或满 3 天回落常规。落点 `src/contacts/`（画像采集）×
     `skill_manager` 提问注入；与 pace lane 的「本阶段指令」共用推进语义但
     属画像域，**开工前与 goal sprint lane 对齐分工**。
2. **AI 画像抽取兜底（钧 04:27 承诺件 2）**
   - 现状严格规则「客户明确说才入档」；承诺：定期让 LLM 归纳聊天内容进画像，
     低把握标待确认。落点 contacts 画像 + 既有 llm_extract 影子机制可复用
     （care 线 llm_extract 的 shadow→enable 路子照搬）。
3. **工作链任务失败（#48）+ 页面脚本出错（#13）+ 英文版残留中文（#33/#11 内）**
   - #13 页面顶部「页面脚本出错」是硬报错（workflows 页），先修它（大概率
     顺带解释 #48 的任务失败）；#33 的定位混淆（工作链 vs 工作目标像同一功能）
     → 已在 20:11 群答复里给了口径（SOP=提醒坐席、目标=驱动 AI），产品侧
     建议在工作链页首加同款一句话说明；英文包补词条。
4. **#54 调整期限死按钮**：cp-goal 在途改动区，**转 goal sprint lane** 或其收口后复验。

### 域F 通知与弹层 UX —— P2（新单 #52/#53 + 旧账）

1. **#52 克隆绑定弹层点外部即整层关闭**（试听生成中误触即丢）——改为仅
   ×/完成/Esc 关闭，对齐 cp-goal 背板防误触语义（backdrop 判脏）。证据 …676.jpg。
2. **#53 右下角运维提示常驻**（「告警通道未接通」「图片或语音没看懂」）——
   加关闭+点击跳转+自动消失；**并请产品裁决**：「告警通道未接通」是运维语，
   客户形态是否该显示（同 #7 的「接通告警通道」弹窗测试失败 HTTP 200——
   客户端根本不该让终端用户配告警通道，建议客户形态整块隐藏，与
   CLIENT_HIDDEN 体系对齐）。证据 …677.jpg。
3. **#39 输入框经常性不能输入**（主动关怀页实锤+「软件经常性」泛化描述 +
   #17 内输入框无光标）——弹层关闭后焦点未归还嫌疑（「点击取消后卡住」）。
   落点 care_schedule.html 弹层焦点管理；全站 modal 关闭→焦点恢复审计。
4. **#16 主动关怀预览失败 AbortError**——前端 abort（超时）；impl84 care 大改
   后**先复验**；仍复现则拉长预览 abort 或走 SSE 分段。
5. **#11 绑定人设小提示显示不完整**（多次报）——popover 裁剪。
6. **#10 新人首引第四步全自动引导做不到**——新人无会话时第四步「设一个会话
   为 auto_ai」必然失败；按钧建议改为图文介绍或跳过该步。
7. **#12 真发开关联动提醒**——用户开「全自动」档时若 `l2_autosend.deliver`
   实际关闭，应当场提示「还差真发开关」并给跳转，而不是让草稿静默排队
   （skuio 原话「可以打开全自动但是却不工作」）。落点 reply_settings/首引导
   的档位应用回执。

### 域G 多语言 —— P2

1. **AI 回复繁体自适应（#36 + #40 尾巴 + 钧 10:20 追问）**——reply 链零
   zh-hant 处理（读码确认）。方案建议：会话级语言标签扩 zh-Hant（客户消息
   繁体占比/地区信号→出站转换层 OpenCC 或提示词硬约束），界面语言与回复
   语言解耦（他明确要求界面保持简体）。粤语语气包（#36 追加）作二期。
   过渡方案已在群里给（人设指令「对这位客户一律用繁体」）。
2. **语言跟随失效族（#24 内「收中发英不生效」/ #31 内「图片表情后语言漂移
   回中文」/ #25 内同款）**——图片/表情等非文本入站会打断会话语言记忆，
   回落中文。落点：会话语言状态不应被非文本消息重置（language anchor 已有
   Phase8 语言钉子，扩到媒体轮）。与 B121（域D-5）同一个验收网。

### 域H 平台能力/产品决策 —— P3

1. **#37 AI 引用回复**——A/B 线出站带 reply_to（TG 原生支持；其他平台按能力
   矩阵）。建议只在「对方连发多条后针对其中一条作答」时引用，全量引用很机械。
2. **#32 手机端删除消息同步**——产品已倾向灰色标记保留上下文，钧倾向彻底
   删除+AI 不提被删内容；**要产品定稿**再开工（消息删除事件消费当前缺失）。
3. **#25 内危机信号误报**（「明年想自己办生日派对」触发危机）——detect_crisis
   词表对「庆祝自己」类误伤，报给安全评测网（crisis eval 有金标体系，补反例）。
4. **#25 AI 生成图片入口置灰无解释**——按钮可见但「功能未启用」，要么隐藏
   要么给启用路径说明。

## 4. 已关闭/垃圾单（本晚值守处置）

#2（上线冒烟）/ #15（B44 AI 回声）/ #50（bot 回访自咬，实施82 守卫缺陷）
已关；#46 并 #44、#30 并 #45。

## 5. 值守工具链三缺陷（.ops 便签已发，可并入本批施工）

详见 `D:\chengjie-instances\.ops\NOTE_from_duty_20260829_duty_tools_three_defects.md`：
①bot 自咬守卫冷启动 getMe 负缓存窗失效（垃圾单 #50 的成因，每次冷启动后
10 分钟内标 fixed 都会复制）；②观察管线 per-reporter 限频把同一测试人的多个
**不同** bug 吞成 rate_capped_report 事件（今天吞 4 条，处置台不可见，值守
手工补登 #51-54）；③duty_watchdog「任意官方出站=已应答」被 bot 回访清零，
掩盖了今天 9 小时的欠账（10:11-10:36 八连报无人应答，19:45 bot 一条回访把
时钟清了）。

## 6.5 【0830 凌晨追加批】钧实测夜产出——根因全部日志级锁死，修法已写进工单

> 值守 0830 00:45-02:40 与钧 JUN 实时对测的产出。**每单的 body 末尾都有带日志
> 引文的根因 note 和明确修向，接单先读工单本体**。证据源：VPS 网关账
> （gw_usage_archive/ai-gateway-20260830-0210.jsonl）、104 服务日志
> （ssh shengyin + C:\idx_serve.log）、钧机诊断包（FTDKJU.zip，值守本地
> tmp_diag_FTDKJU.zip 有副本）。⚠ 本批与 impl86 修复线的域B（Messenger，
> 已认领在途）不冲突；动语音前看意向板（tts 相关多线活跃过）。

| 单 | 一句话根因（已实锤） | 修法（已写单） | 优先级 |
|---|---|---|---|
| **#58** | 副语言标记 `[breath]` 随文本进 104 IndexTTS-2 被当英文念出（8 发日志引文在单）——标记体系只在 CosyVoice3 验证过消费 | clone 后端为 minicpm_clone/IndexTTS-2 时禁用注入或合成前剥 `[breath]/[sigh]/[laughter]/<strong>` 全家族（`voice_emotion.inject_paralinguistic` 消费侧） | **P1**（每条带气口的克隆语音都在穿帮） |
| **#59** | 试听/预览路径不分段单发 76-80 字，104 RTF≈1.7-1.8 需 27-32s，客户端 ~15-25s 超时即回落默认音——**服务端 42 发全 200 零失败**，克隆成品被丢弃三输；发送链分段是好的 | 预览路径接 `split_text_for_clone` 分段（或按字数动态超时 0.45s/字+10s）；104 合成速率复核（队列/隧道开销）顺带 | **P1** |
| **#57** | 智聊「手机扫码操控」LAN 配对入口未就绪（弹窗自诊红字实锤，`192.168.0.120:18799/kz?pair=` 手机打不开） | 查包内后端 /kz 配对服务 LAN 绑定 / Windows 防火墙入站 / 多网卡选址三嫌疑面（工单有截图证据 …_694.jpg） | **P1**（功能整体不可用） |
| **#12+#63** | 档位预设把「新会话默认档」和「真发总闸」捆绑翻转；全局停发时会话「全自动」绿标照亮零提示；A线只认会话档、B线还受总闸管——生效面不一致（钧机审计行 01:54/01:57 + 启动行 deliver True→False 实锤，两位报障人复现） | ①预设拆两个独立控件+应用时明示影响面（波及 N 个会话）②会话徽标叠加全局态（「全自动·全局已暂停真发」黄标）③按账号默认档（#63 本体，accounts 维度 default_mode，bootstrap 按账号取） | **P1**（「开着全自动却不工作」家族收口） |
| **#61** | 人设「应用到」只枚举第一个登录 TG 账号，多账号不可选 | 绑定账号枚举遍历多账号注册表（截图在单） | P2 偏高（多账号是内测主力用法） |
| **#62** | AI 自述行为（泡茶）不入下一轮上下文 → 31 分钟后当客户面自我否认（原图引文在单） | 「你刚说过」自述事实块进生成上下文（对齐场景状态单一事实源哲学），或出站前与上轮自述矛盾校验 | P2 |
| （无单，网关实锤） | `register_spk` 对新上游 404×2——IndexTTS-2 无该端点（CosyVoice 契约残留），客户端登记链仍在调 | 确认 404 是否对用户冒「登记失败」；hub relay 兼容响应或客户端去掉该调用 | P2（合成不受影响，防误导） |
| 值守工具（实施87 P0-3 已改写） | 自咬垃圾单真根因=backfill 补拉扫进无守卫的 `observe_group_message`，dup 候选排除 fixed 源单 → **每张 fixed 回访必然复制垃圾单**（#50/55/56/60 四例与补拉日志逐秒吻合） | observe 入口加 is_official_bot 守卫 + backfill 过滤双保险（归 duty-tools 线，勿抢） | P1（工具线在途） |

## 6.6 【0830 午批】上午验证潮产出——按修复框架打包，工单 body 全带日志/截图级证据

> 值守 0830 07:19-12:05 与两位测试人实时对测的第二波产出（v1.0.62 发布后的
> 验证潮）。**接单先读工单 body 尾部的值守 note**（根因/证据/修向都在）。
> 前一批 §6.5 的 #58/#59/#57/#12/#63/#61/#62(v1) 已随 1.0.62 出货勿重复。

### 批 0（最高性价比，先做）：#68 ASR 隧道一行改指向

全网语音转录瘫痪 7+ 小时仍无人接：`deploy/instances/vision_tunnel.ps1`
`18415:192.168.0.176:8765`→`192.168.0.198:8765` + 重启 VisionTunnel 任务，
照抄 `.ops/NOTE_P1_20260830_asr_relay_dead_fix_oneliner.md`（含验证步骤），
5 分钟活。修完 #68 标 fixed。

### 批 A（P1）：出站文本卫生三件套——同一守卫框架一次做完

| 单 | 内容 | 备注 |
|---|---|---|
| #64 | 中英混杂出站（I'm 我）——出站语种一致性校验：检出混杂→拦截→重写一遍再发 | B121 家族，0827 加急至今，报障人**三度复报**（复报最多的未修项） |
| #71 | 舞台指示泄漏（(tone shifts to playful) 直发客户）——生成端硬禁+出站剥离守卫，中英括号旁白全家族（（语气…）/(smiles)/(laughs)/(pauses)） | B118 家族英文变体，P1 |
| #74 | 客户发图后回复漂中文——识图中文描述污染上下文；媒体轮补会话语言锚（描述仅供理解勿跟随），或识图描述按会话语言产出 | 与既有 Phase8 文本轮语言锚互补，机制同 bug_intake.LANG_ANCHOR 注释记载的反向案例 |

### 批 B（P1）：人设档案硬约束——「档案字段不敌上下文」家族，同一注入/校验框架

| 单 | 内容 | 证据亮点 |
|---|---|---|
| #24 | 称呼双向硬钉子（你称呼对方 X/对方称呼你 Y，绝不互换）+ 出站互换校验重写；**优先级必须压过 AI 推断记忆**（记忆库有条「用户不喜欢被叫babe」的推断在反向教唆） | 档案明写 babe 仍被上下文 baba 带跑（skuio 档案回证） |
| #29 | 人设背景字段（职业/家庭/居住地）进生成上下文的消费链核查 | 两机两人设互证（钧机背景全错 + skuio 机位置被无视） |
| #82 | 位置三层模型：①档案位置=基线（只许取档案）②会话中人为引入的临时行程（出差X地N天）=覆盖层，期间位置/天气/场景按它叙事 ③时限到自动回归；天气接真值或禁提具体现象（雨/雪） | AI 自称「马尼拉雨晨」而档案=薄荷岛（虚构位置实锤）；两例 10 分钟 |
| #62(扩) | 承诺类自述必须有原句锚（对不上不许说「之前答应过」）+ 历史上下文逐条方向标注（你说的/TA说的） | 第三例=客户说「你可以打字嘛」被 AI 扭成「我答应过打字陪你」（方向混淆实锤） |
| 记忆管理五件套（域D-3 升级，挂 #41 note） | 确认/删除语义显式化+条目可编辑+**来源溯源（抽取自哪句原话+时间）**+档案>推断记忆的优先级 | 「这条推断记忆从哪来的」无人能答=溯源缺失 |

### 批 C（P1）：相册链三层（#67）+ 打包质量（#70）

#67：①上传落盘进了**程序安装目录**（打包态相对路径错位，更新即清空——
被服务静态资产不变量的桌面态漏网）→ 改数据根绝对路径+存量迁移；②上传文件
本体损坏（两机互证，上传后无 magic bytes 校验——违反媒体产物验证纪律）；
③发送 decode 失败被误报 album_empty（归因修正）。#70 顺手：1.062 种子
config.yaml 重复键（telegram 键，每启动报 ERROR）+ 诊断包补收 renderer 日志。

### 批 D（P1）：发送真相层（telegram_client 同域两单）

#72 手动发送假阴性：慢发送（限流 30-65s/条）撞客户端超时误报「发送失败」
诱导重发双条——文本手动链补「超时→对账」三态机制（语音链 voice_send_tracker
同款下放）。#73 dead-peer 误标 blocked 不解除：实际可达客户被 AI 永久跳过
（05:47 真送达实锤）——标记来源核查+成功送达即清标/TTL 重试+会话可见标记；
顺带查同 mid 每 10 分钟重复镜像的去重疑似失效。

### 批 E（P2）：工作台 UX 收口

#53 提示四件套（三票两人）：×关闭/点击跳转/30-60s 自动淡出/同类聚合计数；
#48 工作链 step0 集体失败根因（条件自动启动一轮 10-20 条打满预算）+ 失败提醒
并入聚合；#75 左侧滚动条拉不动（1.0.62 回归嫌疑，#20 家族）；#78 人设「应用到」
残余（LINE/WA 账号枚举+default 占位隐藏+条目显示账号/人设两行）；#77 白名单
豁免可观测（拦截日志补目标 peer+豁免链热更消费验证+命中时横幅说明）。

### 批 F（P2/feature）：体验增强

#79 WA 扫码窗内置「配对时手机改用流量」指引+失败原因透出（根因已定案：同网
配对风控，用户按此方案一次成功）；#84 语音面板内置翻译（母语→选目标语言→
自动译→合成+双文对照，复用 translation.engines 栈）；#51 应用内报障失败本地
暂存+补传；#17 客户端↔官网连通自诊。

## 6.7 【0830 晚批】1.0.63 出货后首轮实测产出——三张新单，证据全为原图级

> 值守 0830 18:19-18:55 产出。批 A-D 十单已随 v1.0.63 出货（回访按钮在群），
> 本批是**新版首轮实测暴露的边界缺口**，接单前先读工单 body 尾部值守 note。

| 单 | 严重度 | 一句话根因（值守原图级判读） | 修向 |
|---|---|---|---|
| #88 | P1 | **dead-peer 黄条自动解除钩子未覆盖全部送达路径**：Yhang 会话 18:20 单勾/18:38 双勾真实送达，黄条『曾被对方拉黑』仍挂着（Kate 同理由旧标记同病）；802 原图另见同文案双气泡+发送失败标=#72 假阴性花纹在 1.0.63 前存量 | 解除钩子补齐 手动/人审通过/自动投递 三路径 + 存量旧标记跑一次核销脚本；18:18 手动失败真实错误码待 DC8F 诊断包（VPS SSH 恢复后值守补） |
| #91 | P1 | AI 凭空称『你上次提过惠普OMEN』被戳穿后回『**可能把别人的事记到你头上了**』——①疑似跨会话记忆串扰（OMEN 或来自钧机其他联系人）②「他人串扰自曝」话术=人设穿帮红线 ③『被你抓包了』认错口癖单模板复读；808 案例页系统自聚「承诺未兑现」×10 佐证 | ①用 1.0.63 记忆溯源查 OMEN 条目归属（若显他人会话原话=跨会话串扰重病，查记忆键隔离）②出站红线新增「他人/多客户自曝」词类（persona_guard 家族）③认错话术进变体池 |
| #92 | P2 | 晚餐自述矛盾（问客户『泡面配青菜鸡蛋没』又自称『随便炒个饭』，语音链）——#62 家族复测变体；疑点=语音口语化改写发生在自述锚注入**之后**会绕过约束 | 升 1.0.63 复测；复发则查 meal_state/自述锚在语音链的注入顺序 |

另：skuio 确认两守卫（防互刷/日发额度）默认关合预期（值守已答用法建议，无单）。

### 勿抢（在途归属）

#38/#65 目标节奏档=goal sprint pace 线在途（摸底硬约束/AI 画像归纳若其不认领
可并批 B 施工，先对意向板）；实施87 值守工具链（含 backfill 自咬守卫，垃圾单
已累计 **12 例**、且已证 bot 全部出站长文均会被咬=值守答复越勤垃圾越多，
`.ops/NOTE_to_dutytools_20260829_P0-3_rootcause_corrected.md` 有 0830 追加段）
=duty-tools 线；域B Messenger 发送失败家族（#49/#21/#23/#34）
=bugfix-dev 一组昨晚认领，开工前对意向板确认其进度，撂了就接。

## 6. 开工纪律（照抄共享树协议，别省）

- 动手前 `scripts\agent_probe.ps1 -Intent "..."` 登记；goals/cp-goal、
  unified_inbox.html、zh_hant packs 都是**活跃热区**，先看意向板再动。
- 模板/i18n 热更新直上生产：每步保存必须自洽；前端批次 bump
  `ui-build.txt` + CSS `?v=` 双戳。
- 修完的单在处置台标 fixed（写一句话 fix_note）→ 自动带按钮回访，**不要**
  手写回访文案；需要发版的修复攒 1.0.61 一批走，别一单一发。
- 涉及桌面包的修复（域A/B/C 大部分）要 build:backend + 发版才到客户机，
  服务端热修不解决客户侧问题——排期时把发版窗算进去。
