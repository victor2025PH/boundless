# 发版对账 v1.0.78 — 发版线 R78（O 批收口：装载 → 回归与门禁 → 三包 → 三渠道 + 官网 → 坐席三台 → 台账 → 回访）

> 指令：老板 09-08 11:2x 原话——读 `docs/O批_进度总账.md`（五行须全部收工）与 `docs/发版对账_v1.0.77_N5.md` §6 E 段照它的全链步骤发 1.0.78。总账：`docs/O批_进度总账.md`「全批收口」节。
> 接手方：先 `git -C D:\boundless log --oneline -20` 核对本表 commit 真在；再看「断点 / 交接」节从未勾的一步继续。任何写台账先 dry-run；对外只走 `tools/duty_reply.py`；**自检只许 GET**（N-5 purge 事故）；不改业务代码。
> 执行：2026-09-08 11:24 起。意向板 `R78 release 1.0.78 closeout`（11:28 登记）。

## 0. 一句话

**11:24–12:1x：前置条件不满足，全链停在第一步。** O-1 行仍「进行中」：D 段（拟人程度三档 `deliver_delay.profile` + `humanize.PACING_PROFILES` + `reply_settings.html` 三档 UI）代码**未提交、半成品在工作树**（`humanize.py` +162 / `autosend_worker.py` / `reply_pacing_settings.py` / `reply_settings.html` / `reply_settings_page.py`，最后一次保存 10:30–11:20，之后无动静），E 段（账号名提醒 / 盲评门禁）未开；意向板 O-1 08:18 登记未 `-Done`。`reply_settings.html` 门禁 `test_all_inline_scripts_parse` 红（`Unexpected token '{'`）且 EN 渲染残留中文。**12:3x 更正**：JS 红是门禁盲区不是生产事故——O-1 D 在 `<script>` 块内写了 Jinja 注释 `{# … #}`，门禁只把 `{% %}` / `{{ }}` 占位、不剥 `{# #}`，node 看到裸 `{`；Jinja 真渲染时注释被剥掉，页面正常（`template_compile_check` 全程绿）。修法一行：把那段 `{# #}` 改成 JS `//` 注释。三包从共享树打包＝会把这份半成品带进包，值守 11:45 也在总账留话「O-1 行未变收工前不要 bump / dist」。**本线不载、不打、不发；等待期把不依赖 O-1 的发版文书全部备好（§3），O-1 一收工即从 §4 第一步续跑。**

## 1. 开工基线（11:24–11:35，全只读）

| 项 | 事实 |
|---|---|
| HEAD | `d303ad05`（DY/TK 落点表）→ 12:0x `fc2c8f1d`（值守补总账验收核对）。工作树 51 改动 + 27 未跟踪（微信 KF / PC 线 + DY / TK 线 + 小智线 + O-1 D 在途），git 索引空闲 |
| 五行收工核对 | O-2 全部收工（`ad5c7dbd` / `45b56b64` / `6b42dc10` / `63daf5e3`）；O-3 收工（`c3c64176` / `5d1cc20b` / `183e43c1` / `26d8a0e0` / `1d41c7da` / `36e2e754`）；O-4 一期收工（`1c4ced51` / `63c66713`）；O-5 收工（`f6a0d444` / `ec8b987b`）；**O-1 进行中**：A `ddc5d282` / B `583af62b` / C `ea20cefd` 已提交，**D / E 未收工** |
| `agent_probe` | O-1 意向 3.1h ACTIVE 未 `-Done`；`reply_settings_page.py`（HOT-ZONE）/ `unified_inbox.html`（HOT-ZONE）/ `workspace_base.html` / `assistant-ball.js` 3.9 min 前保存；微信 KF 线 `wechat_kf_webhook.py` 11 min；QQ route-1 意向 0.6h。N-1…N-5 意向仍在板（pre-restart 标记，N-5 台账段 11:3x 仍在跑：`n5_a_merge_dryrun2.txt` 11:26） |
| O-1 D 在途件（`git diff --stat`） | `humanize.py` +162（`PACING_PROFILES` 读 / 想 / 打字分量模型）/ `autosend_worker.py` 67 行 / `reply_pacing_settings.py` 21 行（`PACING_PROFILE_CHOICES` + `FIELDS["inbox.l2_autosend.deliver_delay.profile"]` + 三预设带 profile）/ `reply_settings.html` 34 行 / `reply_settings_page.py` 34 行。`holding_reply.py` 515 行（06:28）/ `persona_reply.py`（06:39）/ `reply_split.py`（02:18）是更早别线的在途，非 O-1 |
| zhiliao | 05:58 装载 1.0.77 批（N-5 E），运行中 pid 24488，端口 18799；`restart_cooldown` 326 min 前；批处理窗 12:30 ±45 内 |

## 2. 预跑门禁（等待期，全只读；给 O-1 收工后的正式跑对号）

| 门禁 | 结果 | 归属 |
|---|---|---|
| **O-1 B `scripts/outbound_style_gate.py`** | **samples=200 violations=0**（em_dash 0 / semicolon 0 / summary_tail 0 / tag_question 0；fixes punct=534 style=246 trimmed=48）exit 0 | 发版门禁第一条 **过** |
| `gate_sweep`（11:4x，`tmp/r78_gate_sweep_pre.txt`） | **12 failed / 793 passed**（复跑仍 12 红＝真红）。**7 老债**（32.9–47h：`deploy_profiles` / `seed_switch`×2 / `inline_color_ratchet`×3 / `orphan_refs`，与 N-5 §6 一致）。**5 新（0.0h）**：① `test_admin_route_inventory::test_no_unexpected_extra_routes`——微信 KF 线 3 端点 `/api/unified-inbox/kf/{close,session-state,transfer}` 未登清单；② `test_i18n_coverage::test_sealed_pages_en_no_cjk_leak[reply_settings.html]`——O-1 D「拟人程度档 / 自然 / 偏快 / 偏慢 / 读 / 想 / 打字」EN 未 i18n；③ `test_template_inline_js_syntax::test_all_inline_scripts_parse`——**`reply_settings.html` 第 0 个 `<script>` `Unexpected token '{'`，盘上即生产**（O-1 D 半成品）；④⑤ `test_inbox_inline_handlers_exported` / `test_rpa_inline_handlers_exposed`——`unified_inbox.html` `kfTransferToHuman` 定义了没挂 window（微信 KF 线在途） | ①④⑤ 微信 KF 线；②③ **O-1 D**；均在对方编辑窗内，不替修。① 若到正式 preflight 仍红则按 N-5 / M-6 先例代登记清单（门禁台账非业务代码） |
| `restart_preflight` 预跑（12:0x，`tmp/r78_preflight_0.txt`） | **NO-GO**，唯一红＝装配门禁上面的 ①；脏 py 54 语法 0 错；active-files 安静；cooldown 361 min；批处理窗内 | 等 O-1 |
| 装载前自检基线（`tmp/r78_postload_check.py --since 05:57`，只 GET） | 旧代码基线：`/api/reply-settings` 无 `quote_reply.enabled` / `deliver_delay.profile`；`summary.growth=null`；`[episodic] 情景记忆已启用` / `[stop-contact]` / `[outbound]` / `[quote]` / `[goal-plan]` 全 ×0；app.log 05:57 起 686 行 3 Traceback（pyrogram 网络类）/ 94 WARNING（asr/vision true_probe 超时、pyrogram 重连为主） | 装载后同脚本对比即活体核对 |

## 3. 发版文书（等待期已备好，O-1 D / E 定稿后只需补两行）

| 件 | 路径 | 状态 |
|---|---|---|
| changelog 10 条双语 | `tmp/_notes_1078.txt` / `tmp/_notes_1078_en.txt`（O-1 A/B/C 三条 + O-2 四条 + O-3 两条 + O-4 一条；**O-1 D / E 各一条待 O-1 落点表定稿后追加**） | 备好 |
| 标 fixed 计划 | `tmp/r78_mark_fixed_1078.json`：#252 / #254 / #256 / #257 四单 fix_note 各 ≤300 字（#252 已含 D / E 一句，落地后按实改） | 备好，未 dry-run（等 O-1） |
| 回访四条 | `D:\chengjie-instances\.ops\duty_0908\r78_{252,254,256,257}.txt`（各带单号；结论 + 路径 + 验证步骤；只说版本号；#254 含 O-4 二期 / 三期估算兑现；#257 用 O-3 §四原话；#256 用 O-2 §5.1 原话）| 备好，未 `--dry-run` 过闸（等 O-1 定稿 #252 D / E 句） |
| 群播报 / 频道稿 | `tmp/_broadcast_1078.txt` / `tmp/_channel_post_1078.txt`（campaign `release-1078`，走 `tmp/n5_channel_post.py` 同法改 campaign） | 备好 |
| 装载后自检 | `tmp/r78_postload_check.py`（**只 GET**：care/health · goals/custom-slots · drafts/autosend-status（需主管权限，403 属预期）· episodic-memory/summary.growth · reply-settings 含 quote / profile 键 · goals 列表 sprint_live.counts；app.log ERROR / WARNING 折叠 + 关键行 `[episodic]` / `[stop-contact]` / `[outbound]` / `[quote]` / `[persona-guard]` / `[goal-plan]` / `[goal-inject]` / `[goal-probe]` / `[goal-liveness]` / `system prompt variant`） | 备好，旧代码基线已跑一遍 |

## 4. E 段全链（照 N-5 §6，每步落地即填；**O-1 行改「收工」前一步不动**）

| 步 | 状态 | 事实 |
|---|---|---|
| 五行收工核对 | ⏸ | O-1 D / E 在途（§0） |
| `restart_preflight` | ⏸ | 预跑 NO-GO 见 §2；正式跑前若 KF 三端点仍未登 → 代登记 `_ADDITIONS_2026_09_08_WECHAT_KF`（归属注明） |
| zhiliao 装载 | ⏸ | `restart_instance.ps1 -Instance zhiliao -Reason "R78: load O batch (O-1 A-E stop_contact/humanize/companion prompt/pacing profile · O-2 memory baseline · O-3 goal probe · O-4 quote) for 1.0.78; piggyback: wechat_kf/pc + DY/TK + QQ in-flight .py"` |
| 装载后活体核对 | ⏸ | `tmp/r78_postload_check.py --since <装载时刻>`：O-1 A（worker 快照 `total_skipped_stop_contact` 键在场；`[stop-contact]` 行按真实入站；停联三例回放以 `tests/test_stop_contact_hard_stop.py::test_replay_xam4kv_one_farewell_then_silence` 在装载后的代码上跑，**不对生产会话投测试消息**）/ O-2 A（`[episodic] 情景记忆已启用 · 可抽取意图 N 个` 启动行；zhiliao 是服务器实例不走 `_ensure_baseline`，不应出现「产品基线已补齐」）/ O-3 D（`/api/goals` 任一目标 `sprint_live.counts` 三计数 + `capture`；zhiliao 现仅 1 条 cancelled m7smoke 目标 → 以 `goals/custom-slots` 200 + 路由清单 `probe/preview` 在场代之）/ O-4（`[quote]` 每条自动出站一行；`/api/reply-settings` 含 `quote_reply.enabled`）/ O-1 D（`/api/reply-settings` 含 `deliver_delay.profile`） |
| 全量回归 | ⏸ | `scripts\regression.ps1 *> tmp\r78_pytest_full.txt`（生产在线 BelowNormal + 4 worker，N-5 用时 1:03） |
| `gate_sweep` | ⏸ | 对号 §2 表；O-1 D 两条（②③）必须转绿才 bump |
| **发版门禁** | ✅ 200 样例 / ⏸ 盲评 | 200 样例 0 违规（§2）；50 条拒绝·冷淡·告别盲评：O-1 C 代理盲评 50/50（`test_companion_prompt_service_tone.py` ④），**真人盲评交 skuio 装 1.0.78 后取 50 条真实出站打分 ≥45**（写进 #252 回访） |
| bump / 繁体 / changelog / 缓存戳 / ui-build | ⏸ | `desktop/package.json` 1.0.77→1.0.78 / 1.077→1.078；`python -m scripts.i18n_hant generate`；`node website/scripts/gen-chatx-changelog.mjs add --version 1.0.78 --date 2026-09-08 --tags fix,improve --title-zh … --title-en … --zh tmp/_notes_1078.txt --en tmp/_notes_1078_en.txt`；`tmp/m6_bump_cp_v.py 20260908e` + `tmp/m6_fix_eol.py`（CRLF 坑）+ `sync_copilot_mirror`；`bump_ui_build.py --severity notice` 最后 |
| release commit | ⏸ | `stage_hunks` 隔开微信 PC / DY 在途 hunk |
| 三包 | ⏸ | `npm run dist:win:smart` / `dist:win:clean` / `dist:win:lite` → `tmp/r78_dist_1078_*.txt` |
| publish 三渠道 | ⏸ | `publish_chatx.ps1 -DryRun` → `-Yes`（public ← dist-clean；`-DistDir dist -Channel internal`；`-DistDir dist-lite -Channel lite`） |
| 官网 | ⏸ | `npm run gate:content` 6/6 → `scripts/deploy.ps1` |
| 坐席三台 | ⏸ | `push_chatx.ps1 -TargetSsh {kouxing,lianbei,yunsheng} -Install -Relaunch -Smoke` → `chatx_fleet_status` |
| 播报 | ⏸ | `duty_update_hint --set` / `duty_reply --group neice --text-file tmp/_broadcast_1078.txt --no-mention` / `n5_channel_post.py`（campaign release-1078）`--apply` |
| 台账 / 回访 | ⏸ | `duty_ledger_fix --mark-fixed-file tmp/r78_mark_fixed_1078.json`（dry-run → `--apply`）→ 四条 `duty_reply --group neice --text-file …\r78_<N>.txt --ticket N`（先 `--dry-run`）→ `duty_notify_summary --reporter 8942577244 --msg-id <mid> --ids 252,254,256,257 --apply` |
| 总账 | ⏸ | 「全批收口」后两项勾选 |

## 5. 断点 / 交接

- 11:24 读总账 + N-5 §6；11:2x `agent_probe`：O-1 在途、D 段半成品在工作树 → **停**；11:28 登记 R78 意向；11:3x–12:15 备发版文书（§3）+ 预跑三门禁（§2）+ 轮询 O-1（`git log` / 文件 mtime / 意向板，每 4–8 min 一次）。**12:15 最后一次轮询：O-1 五个在途文件 mtime 仍是 10:30–11:20、落点表 10:18、意向未 `-Done`、无新 commit——O-1 线已停摆约 1 小时**；N-5 意向 12:0x 已 `-Done`。本线到此收笔向老板报告，R78 意向留在板上（续跑时 `-Intent` 同名刷新）。
- **O-1 收工信号**（三件齐才动）：① `git log` 出现 O-1 D / E commit 且 `git status` 里 `humanize.py` / `reply_pacing_settings.py` / `reply_settings.html` / `reply_settings_page.py` / `autosend_worker.py` 不再是 O-1 的脏文件；② 意向板 O-1 `-Done`（或总账 O-1 行「收工」）；③ `gate_sweep` 的 ②③ 两条（reply_settings.html JS 语法 / EN 泄漏）转绿。
- **老板 12:2x 拍板**：O-1 D / E 另开对话续做（指令 `docs/指令_O-1_DE续做_2026-09-08.md`，R78 代写），收工后 R78 打包发布；不等 D / E 先发的方案作废。
- 续跑：从 §4 第一步起，每步落地即填本表；发版事实收口时同步 `docs/O批_进度总账.md`「全批收口」两项与 O-5 落点表 §5「文案栏」指向 `r78_*.txt`。
