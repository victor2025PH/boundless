# Q 批进度总账（跨对话 / 跨账号交接的唯一入口）

> 任何新对话、新账号、崩掉后重开，**第一件事就是读这里**。不依赖聊天记录。
> **谁改这里：** 每条指令的执行方在**收工时（或额度将尽时）**更新自己那一行，**只改自己那一行**。**每个 commit 落地后立刻补落点表一行。**
> 建立于 2026-09-09 07:5x。公共底座 `docs/修复指令_Q批_并行_2026-09-09.md`（去向 / 归属 / 接口约定 / 决策 D-Q1–D-Q8）；方案 `docs/聊天核心能力_三视角方案_2026-09-09.md`。

## 一句话现状

1.0.78 首夜（09-08 23:16 → 09-09 03:39）skuio 50 份报告：同一客户被问职业到第 20 次、客户说「you are a bot」；AI 答应周六上门喝茶；needs_human 挂着仍重新起草 L2 直发、切手动后 35s 节奏窗内仍发出；假发图四轮；夸赞中退场；班表 / 额度出厂开且按上海时区；关怀开关被升级翻回 False；画像抽取 0。**P 批在 HEAD 未装机；1.0.79 = P 批 + Q-1～Q-6；今天先跑 Q-0 止血。**

## 进度表

| 指令 | 主题 | 工单 | 预算 | 状态 | 已完成 | 剩余 | 落点表 | 已花 |
|---|---|---|---|---|---|---|---|---|
| **Q-0** | 值守止血与归位（第 0 波 + 09-11 第二轮） | #263 #264 #267 #269 #270 · 第二轮 #268 #271 #272 #273 #274 #275 #276 #277 → 新 #278 #279 | $80 + $60 | 第一轮未开工（A 之一已由值守代做：#270 回访 14:12）；**第二轮收工**（09-11 13:0x–13:3x，零代码，`f9a6f9f5`） | 第一轮：A 回访三条（#270 那条已发，#267 / #263 两条未发）/ B 我方实例班表关 + 注入停 / C 三张单按主题拆（#270 并入 #264 族）/ D 聚类规则双条件 + 上限 / E 两份汇总总表对账。**第二轮**：A 回访三条 13:11 经 `duty_reply.py`（#275 并 #273 #274 #277①③ 根因 + 1.0.80 internal + 验法；#276 立 Q-16 + 过渡「切窗口先看工具条发→X」+ #277② 拆 #278；#272 赠量核销 + 续额方式 + Bad MAC 立 #279）/ B 台账：#273 #274 #275 #277 #271 #268 → fixed(1.0.80) 带 fix_note，#270 P2→P0，**#278**（P0，Q-17，8 证据码）/ **#279**（P1，wa-sidecar Bad MAC 227500391174370 入站疑丢，JS63M2）/ #272 额度：官网台账查实 skuio 赠量从未核销 → VPS 本地 console 核销 100k（key 不离机）→ `fulfill_trial` 守护 13:07 签发 `topup`，客户端 120s 轮询自动入账 / C 去向表：Q-17 行与「已修」行纠错（GXRD67 E2NMBE 归已修；7M4QQZ NAVHF6 BQ5HBK CC97JP 4C85ZK 归 #278；CF5SCB 去重；JECKCD BNNJWQ 3A7ZE5 未归位待派）。断点：admin 列表 `has_topup_voucher` 与 console `topup_ready` 不一致（网站线）；skuio 机跑 100k 档而 1M 试用授权疑未激活；#279 处置未派 | `发版对账_v1.0.79_Q0.md` | ≈$25（第二轮） |
| **Q-1** | 目标引擎「已知即不问」+ 识破族（**#270 客户流失，第一优先**） | #264 #269 #270 | $180 | **代码完成 / 收工**（09-10 10:36–11:5x；`.py` 须实例重启才生效，cp-goal.js `?v=20260910a` / i18n 热更） | A+B `b9e1c353` / C `20f93db5` / D `e0d59628` / E `5057865b` / F `ebc37c4e` / 对账 + 随改钉桩（收工 commit）。**A** `profile_slots.slot_state(slot, cell, history30) -> (unknown\|mentioned\|confirmed, reason)`（字段三形状 `cell_view` + 最近 30 轮「说过 / 复述过 / 问过且接了话」）；`decide_probe_target` 只从 unknown 取、「问过的加回末尾」已撤、mentioned 只许 `deepen` ≤1/槽/日；日志 `[goal-inject] target= state= decision=probe\|deepen\|skip reason=`。**B** `PROBE_RETRY_PER_DAY=2` 每槽每日 missed ≥2 当日休眠；`verify_pending_probe` 四态 asked/covered/missed/pending。**C** `src/inbox/repeat_question_guard.py`（24h 内我方问句 · 词干 + 同义 + 槽位标签 ≥0.8 → 删句 / 改非问句，`[repeat-q]`）。**D** `src/inbox/exposure_guard.py`（识破词 → `goal.params._paused_until=+24h` + `tag_needs_human(reason=exposure)`（→ Q-3 risk_hold）+ 标签「疑似识破」+ 下一稿整稿换一句挽回 + 任何时候删自证句，`[exposure]`）。**E** `GET/POST /api/goals/discovery-pause`（KV `goals:discovery_paused`，开 → 摸底目标 `reason=discovery_paused` 不注入）+ `POST /api/goals/profile/confirm`（mentioned → confirmed）+ `slots_progress[].state` + cp-goal.js 双树顶部开关条 / 「已提及（未确认）」+「确认」+ 三语。**F** `src/inbox/prompt_addenda.identity_addendum(persona, account) -> str`（未接线）。tests 5 新文件 34 例 + 回归「同会话职业已答后不得再问」K24YJ2 回放；收工回归 1377+197 passed | **交 Q-6 F**：`identity_addendum` 非空即追加 system prompt。**交 Q-8 C（注入分级 hook）**：`goals/service.py::build_block_for_chat` probe 段——`decide_probe_target(...) -> (slot, cue, mode, cues, states)` 是唯一决议点，分级 must/should/may 在它返回后、`probe_line` 拼装前插一层（读 `mode` cue\|retry\|floor + `states[slot]` + 当日 `probe_asked` 计数）；`discovery_paused()` 与 `_paused_until` 两处早退在它之前，Q-8 A 账号级默认目标须尊重。**交 Q-2**：`outbound_humanize.apply_draft_humanize` 顺序 repeat → exposure → claim（Q-2 D 在 claim 处扩）。**回访文案**（#270 / #264 / #269 三条）在落点表「四」交 Q-0 / R79。**HEAD 上即失败非本线**：`test_autodraft_bootstrap_persists_auto_ai`；`test_admin_route_inventory` 纯 HEAD 缺 20 个 wecom/wechat_kf/wechat_pc/heartbeat 端点（路由文件仍未跟踪） | `发版对账_v1.0.79_Q1.md` | ≈$95 |
| **Q-2** | 现实承诺守卫五类 | #263 #264 | $200 | **代码完成 / 收工**（09-10 12:11–12:5x；`.py` 须实例重启才生效，ops 模板 / i18n 热更） | A `54096c0e` / B `48969425` / C `f401a8f6` / D `8f72c134` / E `8d73b5f2` / F `720b1ac7` / 落点表 hash `a5bc8751`。**A** `commitment_guard.detect_commitment` 五类 `meet/contact/media/gift/money` 三语词表；`drafts.py` commitment 类 → `risk=high` + `commitment:<kind>`。**B** `boundaries.meeting_policy` 默认 `never`（D-Q3）+ Studio `mp-` 一格三选。**C** 首次 `refuse_sent` 罐头句走 pacing（不挂 `risk_hold`，否则 Q-3 worker 闸取消本条）；`handoff` / 二次坚持才 `risk_hold.set(commitment:<kind>)` + 暂停全自动；审核稿 `commitment_alt:`；`drafts.py` 四处 `policy_decide(..., conversation_id, store)`。**D** `outbound_humanize` 挂 `CLAIM_KINDS`（`commitment_claim` / `self_blame_repromise` / P-3 `media_claim` 并表不重做）+ 质检卡两格。**E** 陪伴底稿三句（销售域不动）。**F** `commitment_gate.py` 20 邀约全拒 + 10 要照片只发图或拒绝 | **回访文案**交 Q-0 / R79（落点表「回访」节）。**交 Q-8 D**：`CLAIM_KINDS` 已就位，`exit_claim` 可接。Q-3 总账「待 Q-2」的 drafts 四处会话键已在 C 落地。装载须重启服务端 | `发版对账_v1.0.79_Q2.md` | ≈$160 |
| **Q-3** | 风险挂会话 + 人工优先 | #264 | $160 | **代码完成 / 收工**（09-10 10:57–11:2x；R79 `4ba5e0bc` 已代落 Q-4 交接的 worker 两处 + tests 3，随 1.0.79 发出；`.py` 须实例重启才生效，模板 / i18n 热更） | A/B/C/E + D(worker 侧) `c0154cdd` / D(路由 + 前端) `de60a4fd` / 对账 `3ea62b13` `eed161fa`。**risk_hold API**（`src/inbox/risk_hold.py`，app_settings KV 键 `risk_hold:<cid>`，不动 store.py）：`set(store, conv, reason, hit="", ttl_h=24, by)` / `active(store, conv) -> reason\|None` / `clear(store, conv, by, only_reasons=None)`（`by=system:*` 只清泛因 needs_human）/ `all_active`。`decide(..., conversation_id=, store=)` 持有 → 强制 L1 + 继承 shadow；needs_human 标 = 闸（捞稿 + 真发前二次复检 abort=mode_changed\|agent_typing\|agent_sent\|risk_hold\|needs_human）；切手动 / 坐席打字（新 `POST /api/unified-inbox/agent-typing`）/ 坐席发送 → `worker.cancel_inflight` 取消在途 approved 稿 + toast「已取消 N 条待发 AI 消息」三语。tests `test_risk_hold_q3.py` 23 例（含门禁「切手动 1 秒取消」/ XBGPBN 回放） | **待 Q-2**：`drafts.py` 四处 `policy_decide(...)` 补 `conversation_id=conv_id, store=self._store`（补前由 `autodraft_helpers` 封顶 review 兜住，但影子台账无行）；承诺守卫命中 → `risk_hold.set(store, conv, "commitment", hit)`。O-1 A `freeze_conversation` 登记已代做。回访文案交 Q-0 / R79。**提醒**：共享工作树 `tests/test_admin_route_inventory.py` 已丢 Q-14 已提交的 `_ADDITIONS_2026_09_09_Q14_AI_FAIL` 块（HEAD 有、工作树无），下次谁整文件 `git add` 会把它删掉——Q-14 / 值守请核对 | `发版对账_v1.0.79_Q3.md` | ≈$60 |
| **Q-4** | 撤回错误默认 + 基线红线 | #267 #265 | $160 | **代码完成 / 收工**（09-10 12:10–；`.py` / 种子须实例重启才生效——回滚与回读跑在 `ConfigManager.load()`；模板 / 词包 / 静态热更） | 核心 A/B/E `c860436e` / 页面 A/B/G `938fd1fb` / 运行时 A `7d6be0ad` / 关怀 D `a870e8ff` / 弹窗 F `3fab55c1`。**红线三条**钉进 `feature_registry` docstring + `_validate` fail-fast + `test_baseline_redlines_q4.py` 14 例：① 只补缺席键、绝不覆盖显式值（`load()` 回读 `[upgrade] user_flags_preserved=N changed=[]`）② 会改变发送行为的开关不得 `baseline=True`（`SEND_BEHAVIOR_KEY_PREFIXES`，A 类命中进程起不来）③ 时区键不得默认服务器本机。**撤回**：`inbox.work_schedule.enabled` A→B 出厂关（start/end 出表成建议值）、`companion_send_gate.enabled` B 出厂关；存量只回滚「1.0.78 原样形态且无标记」→ `enabled:false + baseline_rollback:"1.0.79"` + WARNING `[baseline] rollback <key> reason=D-Qn`；用户改过一字不动；启动横幅 `[baseline] work_schedule.enabled= tz= \| send_gate…`。**A** 开班表时区必填（后端 `tz_required` + 前端拦 + 换算行「上海 01:00 ＝ 纽约 13:00(-1)」）；off_hours 拦截 INFO `[work_schedule] hold=off_hours conv= until= tz=`（拟稿侧）+ 会话标签「作息外 · 到点重新拟稿」（读侧计算不落库）；`catch_up_regenerate_hours` 默认 0＝过夜积压全作废（`catch_up_regen_due` 已备）。**B** `gate_decision` `origin=manual` 恒放行；额度卡去人工列 + 进度条 ≥80% 橙 / ≥100% 红 +「300 · 推荐」+ 依据句。**C** 关怀开关翻回 False **config 层复现不出**（三嫌疑逐一排除，见落点表 §四），已加升级回读日志兜底。**D** verbatim 人工时刻默认照发（`quiet_policy=keep`；deferred 队列 `extra.ignore_quiet` 不二次顺延）；排期落安静时段弹「仍按 01:20 发 / 顺延到 08:00」；顺延多条 5 min 槽位错峰 ≤40；时区客户→人设→账号→服务器；关怀页 `#cs-quiet` 卡（区间 / 开关 `POST /api/care/quiet` / 时区依据）；「出手」→「发送」。**E** `companion.goals.profile_llm.enabled` A/True 新增、`memory.extract.use_llm` 已 A/True 复核。**F** `config/release_defaults_changed.json` + `/api/release-notice*` + `release-notice.js`（master/operator 升级首开弹一次，每行「保持 / 改回」，班表改回必填时区，选完写显式值；zh/en/zh_hant）。**G** 「未设置账号的兜底档位」+「N 个账号已设全自动 / M 个跟随本页」+ 一行三层图 + bootstrap 折进高级。tests 新 6 文件 76 例 + 改 8 文件；`unified_inbox_read_routes.py` / `test_admin_route_inventory.py` / `admin.py` 三个共享文件均 `git apply --cached` 只入我方 hunk | **交 Q-3（`autosend_worker.py`，R79 前必落，否则 regen 0 档退化为「一律不重拟」）**：① 班表扣留分支 `total_skipped_off_hours += 1` 前加 `log_off_hours_hold(_conv, _ws_cfg_hold, _ws_plat, _ws_acct)`；② 复班补觉分支把 `_regen_h > 0 … > _regen_h*3600` 换成 `catch_up_regen_due(_oh, _draft_ts, shift_started_ts=schedule_state(_ws_cfg, plat, acct)["shift_started_ts"])`——精确代码位置与验收在落点表 §四。**交 Q-0 回访**（#267 / #265 文案见落点表 §四 C 与 §〇）：#265 请用户补 9/7 15:00–16:00 backend.log（`运营开关已更新` 行）与 `config.local.yaml` `companion:` 段。**交 Q-0 / R79 顺手**：`config.example.yaml` L3595 `catch_up_regenerate_hours` 注释改「0（默认）=过夜积压全部作废重写；N>0=只重拟超龄」；`unified_inbox.html` 「作息外」标签悬停句（词条 `inbox.conv.off_hours_hold_t` 已就位）；zh_hant 走 regen（reply_settings / care_page / work_schedule_q4 新键；release_notice pack 已自带 ZH_HANT）。**R79 粘贴块前置（每版必做）**：`release_defaults_changed.json` 维护五步——① `releases` 下加本版号；② 每条默认变更写 `key / kind / old / new / i18n`（marker 行 `show_if=marker` 且与 `feature_registry.ROLLED_BACK_BASELINES` 同源，其余 `missing` / `always`）；③ `needs_timezone` 只给班表类键；④ `i18n_packs/release_notice.py` 加 `<i18n>_title/_desc` 三语；⑤ 跑 `tests/test_release_notice_q4.py`（随包已由 `build_backend.py` DATAS 登记，`package-layout.test.js` 钉住）。**HEAD 上即失败非本线**：`test_care_prompt_context.py` 两例（persona extras 未实现）、personas.html i18n 两例、`test_audit_action_labels`、`test_nurture_panel_224` cp-i18n 同步 | `发版对账_v1.0.79_Q4.md` | ≈$150 |
| **Q-5** | 抽取两链合一 + 昵称解析 | #263 | $150 | **代码完成 / 收工**（09-10 14:0x–；`.py` 须实例重启才生效，cp-goal.js / i18n 热更） | A/B/E `9753dd15` / C/D `2b7372f7` / 对账 见落点表。**Q-4 E 两键已核在基线**（`c860436e`：`companion.goals.profile_llm.enabled` A/True、`memory.extract.use_llm` A/True），本线只读 | A 一次 LLM 调用同产事实 + 槽位两路分发（`[extract] conv= facts= slots= llm=`）/ B `nickname_parse` 「Michael/46/New york/union carpenter/5.28」→ 46 / New York / union carpenter 三项待确认 / C 「AI 推断 · 待确认」「来自昵称 · 待确认」徽标 + ✓✎✕，冲突走通知中心 / D `[profile]` 每格一行 + 50 连零告警（记忆页 `#cs-stall` + 目标页 `#gr-extract-stall`）/ E 格 schema v2 双形读写 + 导出带版本号 | `发版对账_v1.0.79_Q5.md` | **交接**：`unified_inbox.html` 宿主 `cp-goal.js?v=20260910b` 只改了工作区未入库（文件被别线整文件改写），R79 前须入库；`test_manual_outbound_extract_o2` 一例红是成本门禁线未提交 hunk 所致；zh_hant 走 R79 regen |
| **Q-6** | 假发图闭环 + 相册建议词 | #263 #266 | $160 | **代码完成 / 收工**（09-10 12:11–；`.py` 须实例重启，相册 UI 热更） | C/D `42b9640c` / B `be6378c7` / E `f8c14a6c` / E UI `ca1deed8` / A/F `79112004` / 对账 `92bcc5b8`。**A/F** `_prompt_addenda` 相册场景清单 + 已发媒体（空则「从未发过」）+ Q-1 `identity_addendum` + 无匹配禁稍后发；blocked 期间 `_SkipGoalInject` 掏空 `_inject_goal_context`。**B** 每次评估 `[album_match]`；建议词 0.6；索图随机关；风景→outdoor 不抓自拍。**C/D** `media_claim_block` 10 轮 / spiral 2 / 复诉词 + still loading 改写；第 2 次 `tag_needs_human(reason=media_lie_caught_repeat)`。**E** `apply=auto` + `kind:` + 采纳/批量路由；UI `al-` 三段计数 / 一键采纳 / 场景下拉 / 多选批量，「缺触发词」→「待确认」。tests 5 新文件 + `media_gate` 10+10 | **交 Q-2 F**：`src/inbox/media_gate.py` 10 条要照片只发图或拒绝，R79 可并入 `commitment_gate`。**交 Q-1**：blocked 期间 goal-inject 已在 `persona_reply` 调用侧过滤。回访文案（#263 / #266 / #269 身份接线）在落点表「四」交 Q-0 / R79。`config.example.yaml` 未整文件提交（默认在 `resolve_album_ai_cfg`）。**未动** `autosend_worker.py` / `drafts.py` | `发版对账_v1.0.79_Q6.md` | ≈$155 |
| **R79** | 发版 1.0.79（Q-1～Q-6 收工后开） | #259 #252 #254 #260 #262 #263 #264 #266 #267 #269 #270 | $150 | **已发（09-10 17:57–18:09 三渠道 + 官网 + 坐席三台 1.079）** | 基点 `4ba5e0bc`（Q-4→Q-3 worker 两处代补 + Q-5 cp-goal 戳入库）→ 版本链 `23fd13dd`（1.0.79 / 1.079 / 繁体 17393 键 / changelog 15 条 / ui-build 20260910-1537）→ 干净 worktree `D:\boundless-r79` release commit **`2adef601`**（= `0b0b78c3` + 三处代补：personas.html `_selOpt` 函数头 `54768ff3`｜`persona_reply.platform_scope` 容错导入 `cfc4409d`｜cp-goal.js 原生 prompt→uiPrompt）→ smart 593,254,839 B / clean 522,064,046 B sha256 `2c294c6d…6d2a2`（498 MB）/ lite 525,392,577 B → publish 三渠道 → 官网 gate PASS + deploy OK → 坐席三台 smoke 全 OK → 真值 `4dc1a806` → #259 #260 #270 confirmed→fixed、#262 #263 #264 #266 #267 #269 new→fixed（#252 #254 R78 已 fixed）→ 12 条回访（#265 只发进展）→ 记账 9 积压 0 → 群播报 mid 1389 / 频道 msg 160。zhiliao 15:10 装载 16s 0 ERROR；回放 226/227（唯一红＝微信线工作树覆写 unified_inbox.html 丢 Q-14 chip）；style_gate 200/0；全量回归（worktree）149 红 / 26647 绿，62 新红全归因 0 功能回归；gate_sweep 25 红（14 旧债 + 微信线 6 + Q-2 B 4 + Q-14 1） | **Q-8 / Q-10 / Q-15 收工于基点之后（16:09–17:30），不在 1.0.79，随 1.0.80**（发版对账 §4.2）。**三处 HEAD 带病、工作树掩盖**的坑全靠干净 worktree 打包 + worktree 回归抓出（§4.1）——建议把 `tmp/r79_import_names_sweep.py` 静态导入名扫描进 gate_sweep。主干 cp-goal.js 两处 `prompt()` 未动（Q-8 编辑窗）交 Q-8；zhiliao 17:31 被 Q-8 线重启：已载 `4ba5e0bc` 与代补 ①②，但同时载入了不在 1.0.79 的 Q-8/10/15 主干 .py 与微信脏树（zhiliao ≠ 安装包行为）；Q-4 两件顺手活未做；D:\boundless-r78 待回收 | `发版对账_v1.0.79_发版.md` | ≈$140 |
| **Q-8** | 自主推进与退场闸（第二波，Q-1 / Q-2 / Q-4 收工后） | #264 #263 | $200 | **代码完成 / 收工**（09-10 15:1x–17:3x；`.py` 须实例重启才生效——goals service / goal_routes / exit_gate / excuse_budget / care_dispatcher / feature_registry；cp-goal.js `?v=20260910d` / 模板 / i18n 热更） | A+B+C（+E 触发）`122c176b` / E registry `f6d5cf63` / D `bd3685eb` / F `ddc0dcc0` / G `15772a21` / H `4f1f05c1` / 落点表（收工 commit）。**A** `goals/defaults.py` 账号级 / 人设级默认目标（InboxStore KV，账号级优先）→ `build_block_for_chat` 首条真实入站自动挂（护栏：群 / 自聊 / 停联 / 需人工 / 摸底暂停 / 每日 60），`[goal-default] account= attached=1|0 reason=`；存量 `preview_existing`（默认不勾停联 / 自聊 / 需人工 / 群 / 已有目标）+ `attach_existing`，`[goal-default] … attached=n skipped=m mode=batch`；`GET/POST /api/goals/defaults` + `/defaults/attach`；卡片「新会话会自动挂 / 批量挂到存量 / 设为本账号默认」。**B** `planner.intimacy_stage` 四带（intimacy_engine 既有分）+ `plan_stage_beat` 当日确定性轮换 `relationship_stage` 意图池，**不建目标行**；无目标陪伴域会话注入「【关系阶段 · 今日主线】」，销售域关；卡顶「阶段 · 今日主线」行；`[goal-inject] level= reason=stage_plan`。**C** `signals.no_new_info(_streak)`：连续 ≥2 轮纯夸赞 / 应答或今日 missed ≥2 → `probe_mode=must` + `MUST_DISCIPLINE_ZH`（以问句收尾、不许只回应夸赞、不许自己退场），候选仍只来自 Q-1 unknown + 每槽每日上限；`[goal-inject] level=soft|must reason=no_new_info|missed_x2`。**D** `inbox/exit_gate.guard_exit`（`apply_draft_humanize` 里 claim 守卫之后）：第一人称退场词表 zh/en；合法退场三种（班表到点 `work_hours_gate` / 客户先告别 / `inbox.exit_gate.allow_silence`）→ 放行 + 钩子句 + `care_schedule` 排 3h 后真实后续；否则砍退场分句 + 补问句转回对方；`[exit] conv= allowed=0|1 reason=`；AI 指纹卡「自行退场」格。**E** `feature_registry` `proactive_topic.enabled` C→B + `DOMAIN_DEFAULTS`（陪伴域缺席 → 开，销售仍关，不写 baseline）；`proactive_topic.resolve_proactive_topic_enabled` 运行时同口径；`build_block_for_chat` 客户连续 2 轮无新信息且本轮无硬注入 → 投人设话题库新话题（受停联 / risk_hold / 节奏 / 预算），`[proactive_topic] conv= topic= reason=no_new_info_x2`。**F** `inbox/excuse_budget.build_time_schedule_addendum`：人设当地时间 + 今日四时段动线（`companion_selfie` 同源）→「理由只能取自这条日程」；`persona_reply._prompt_addenda` **只加 ④ 一个 try-block**；「工作」类借口出站计数（发送门 `count=True` / 起草层只改写 / 人工 `note_outbound_excuse`）同客户每日 ≤1 超则砍句，`[excuse] conv= kind=work today= cap= action=`；`system_companion.txt` 夸赞「接住 + 反投问题」一句。**G** `probe/preview` 候选带可编辑 `text` + `route`（客户 10 分钟内活跃 → must / 沉寂 → direct）+ `budget` 只提醒；`probe/send {slot, text, force}` **不建 care 行不过 contact_budget**：must → `params._probe_must`（TTL 30 分）下一条回复「【本轮必问·硬性】」一次性消费；direct → `CareDispatcher.send_text_now`（与 N-1 A 立即发共用 `_send`+`_deliver_now`，节奏打字段停顿 ≤12s）同步回执「已发出 hh:mm」+ `record_outreach` + `beat_sent`；冻结 / risk_hold → 409；失败 `reason_key` 人话；按钮超预算灰显「今天已主动联系 TA N 次 · 仍要发」仍可点。**H** `tests/test_hm7xba_replay_q8.py` 整段回放 HM7XBA 时间线：watchdog 两时刻 not_due + `stall_verdict` None（O-3 A 口径）→ 九条入站 missed → 10:05 ticker 排 → `CareDispatcher.run_once` **真发** → `beat_sent` → 不再 stalled；观察项：真发后当日拍行仍 consumed，同日不重发靠 care 去重（交后续） | `发版对账_v1.0.79_Q8.md`（§〇 三段根因 / §H 回放结论） | 红线：`autosend_worker.py` / `drafts.py` 零改动；`persona_reply.py` 恰好一个 try-block。交 R79：回访文案、「5 轮纯夸赞不退场且带出摸底问句」门禁、§H 观察项 |
| **Q-10** | 工作台小件（第二波） | #267 #268 #254 | $120 | **代码完成 / 收工**（09-10 15:0x–17:0x；`.py` 须实例重启才生效——A 服务端 consume / C / D 路由；模板 / appearance.js / i18n 热更） | C `ca4cd456` / A `2dc55ced` / D `4474605c` / B `4bf1092e` / 对账回填（收工 commit）。**A** `autodraft_helpers.consume_drafts_on_agent_send`（不动 `drafts.py`）：坐席 send 成功 → body.draft_id + 会话在途 pending/enriching 稿原子置 `consumed`（`[draft] consumed draft_id= by=agent_send`），响应 `consumed_drafts`；前端两条发送路径带 `draft_id`、已 consumed 不再补 cancel、面板先收；「忽略」不动。**B** 快捷面板夜间 chip 改走 `setNightMode`；`releasePin` 落 `localStorage(cp_theme_user_set)` → `?theme=` 钉只在用户从未选过时当出厂默认（刷新 / 切页 / 顶栏 / 重启壳不再变回深色，4KEJ63）；定时中手动选档 → `cp_theme_sched_handoff`（chip「（已手动接管）」+ 用户菜单「定时已手动接管 · 恢复定时」）；console `theme change source=manual\|schedule\|system\|restore\|sync value= mode=`；`appearance.js?v=20260910b` + ui-build `20260910-1651`。**C** `_storage_closed` 探针（`SELECT 1`）：`_migrate` 入口拒尸体一行 INFO、`_advance` closed-db 不再自动重试、`poll` 短路、终态 `session_closed` + 前端文案。**D** 事实核对：帮助语料自 1.0.51 只在 `assistant_help.db`，从未进 `kb_entries`（日志措辞误导）→ 播种日志点明去向 + `help_kb.help_corpus_status()` 只读口（小智逻辑零改动）；`kb_store.preset_entries / purge_preset_entries`（vendor/system/help，user/import/learner fail-closed）+ `POST /api/kb/entries/purge-preset`（默认 dry_run，`dry_run:false` 须带 ids）+ KB 页「🧹 清除预置条目」弹窗（厂商横幅一键清空也改走它），启动 / 自检零自动调用。tests 4 新文件 26 例（B 用 node+vm 真跑 base 头部 + 引擎），既有 `test_theme_pin_window_local` / KB 三闸 / inventory 基线全绿 | **回访文案**（#267 CV4E22/RU89S6、#268 N9JQ3M/4KEJ63、13:32 栈、#254 22KVXF）在落点表 §2 交 R79。**顺带记录（非本线）**：① 工作树 `unified_inbox.html` 为 LF 且缺 Q-14 B `#aif-chip` 代码（实施97 线基线陈旧），谁整文件 `git add` 会回退 Q-14 B——本线用 HEAD 字节 + 我方 hunk 手工 index blob 提交；② `test_admin_route_inventory` 1 红 = Q-8 `/api/goals/defaults*` 未登记；③ `test_css_token_refs` / `test_legacy_blue_ratchet` / `test_stale_update_prompt` 7 红 HEAD 同红（app.html / tailwind 台账）。**语义注记**：B 让用户显式选过的主题在同 profile 各窗口持久生效（桌面壳 `?theme=dark` 不再复活）——这是 4KEJ63 的诉求，与 2026-08-15 「钉不进共享键」不变量不冲突 | `发版对账_v1.0.79_Q10.md` | ≈$85 |
| **Q-12** | 情感陪伴引擎 v1（1.0.80 专项，R79 后开） | #264 | $250 | 未开工 | | A 接线阶段 + 情绪进提示词 / B 战术包 v1 / C few-shot 真实高分对话 / D 主动仪式 / E 推拉留白 / F 评分 + 盲评 | `发版对账_v1.0.80_Q12.md` | |
| **Q-13** | 口语简写层（原 P-6，1.0.80） | #259 | $150 | 未开工 | | 见 `docs/指令_P-6_口语简写层_2026-09-08.md` | `发版对账_v1.0.80_Q13.md` | |
| **Q-14**（第一波追加，09-09 09:0x） | AI 超时可见性 + 官网网关 + katie 三件 + 告警链接 | #262（Q-0 若另立新单则改） | $160 | **代码完成**（09-10 13:5x 收工；D 已在 VPS 生效；E 与 A/B 的 `.py` 须 zhiliao 重启才生效） | A `2dcdaeab` / B `38707c79` / C `c32de531` / D `3aed9ef9`（katie 80→301 自有块 + 502/503/504 友好页 Retry-After 20 自刷 + 判别日志，VPS `nginx -t` 过、301 与友好页实测）/ E `e83a7d53`（`links=login‖magic‖off`，`/ops/glance` 十分钟一次性只读页，`notify_webhooks.json` tg-ops/tg-ywqz 已置 `magic`） | C 网关代码已随 09-09 15:03 网站构建上线，但 pm2 `yuntech` 仍 fork 模式——`ecosystem.config.js` 未被 `pm2 start` 接管，下次 `pm2 reload` 仍是 restart；切 cluster 一次性动作待拍板；VPS 根盘 96% | `发版对账_v1.0.79_Q14.md` | |
| **Q-15**（09-10 追加，Q-2 + Q-3 收工后） | 成人内容分级 + 拦后软回应（`sex` 一词即 high、拦后 19 分钟沉默、提示无类别） | #271 | $120 | **代码完成**（09-10 15:07–16:5x；`.py` 须实例重启才生效，`personas.html` / `unified_inbox.html` / i18n 热更） | A `7c829d25` / B `7490e57f` / C `54add934` / D `22ea85b3` / E `5915dc20` / 对账 `2ed44274`。**adult_grader API**（`src/inbox/adult_grader.py`）：`grade(text, lang) -> {level: mention|flirt|explicit|pressure, hits, pressure_hits}`；`adult_policy_of(persona, cfg) -> (human|soft_reply|mark_only, source)`（人设 `boundaries.adult_policy` 显式优先，缺省 companion→soft_reply / 其余→human）；只有 explicit（政策非 mark_only）/ pressure（任何政策）→ high + `risk_hold.set(adult)` + `tag_needs_human(reason=adult:<level>:<hit>, source=adult_grader)`；mention / flirt → medium + `adult_flirt`（不拦不转）。软回应经 `DraftService._inbox_deliver_cb`（人工通过链，**不进 L2**，不受 Q-3 人工优先闸）：soft_reply 政策命中即发；human 政策 3 分钟无人接手（标未摘 / 无出站 / 未补过 / 未冻结）补发一次，日志 `[adult] soft_reply conv= level= policy= mode=`。钩子各一行：`drafts.py`（quick_analyze 之后、policy_decide 之前）/ `protocol_autoreply.tag_needs_human`。工作台：卡片 / 原因条「命中：成人内容（sex）· 露骨[+施压]，命中词」+ 「一键人设口吻软回应」（`GET /api/unified-inbox/adult/soft-reply?cid=` 取词 → 既有 `sendMsg`）；体检 `reply_diagnosis` 新 `high_risk` block finding（category/level/hit/policy，privacy / commitment / 泛因持有也可见）。人设工坊「成人话题处理」四格（缺省 / 软回应 / 转人工 / 只标记）。tests `test_adult_grader_gate.py` 21 例 | **R79 门禁清单加 `tests/test_adult_grader_gate.py`**（Q 批新增第六项）。非本单红项：`test_admin_route_inventory` 3 条 `/api/goals/defaults*`（Q-8 未入册）、`test_inbox_inline_handlers_exported` `kfTransferToHuman`（微信线未挂 window）、`cp-i18n.js` 镜像（copilot 线）。**工作树 `unified_inbox.html` 落后 HEAD**（缺 Q-14 `ai_last_fail` / Q-8 `cp-goal.js?v=…c`），谁整文件 `git add` 会退回已提交内容——值守核对。回访文案在落点表 §2 | `发版对账_v1.0.79_Q15.md` | ≈$95 |
| **Q-16**（09-11 追加） | 发送语言按会话保存 + 手发前语种确认（日语误发美国客户） | #276 | $120 | **收工**（09-11 13:0x–13:5x，`c72a0aa2` + 落点表 commit） | A `__default__` 不存 out / 切会话初值 `convEntry.out‖''` / 存量 `__default__.out` 迁移丢弃 / 空档显示「自动 · 跟对方：X」；B `[lang_pref] conv scope=conv send by`（no-op 不落）+ `[manual_xlate] conv from to decided_by path confirmed`（send + preview 两路）；C 目标语≠客户语言 `window.confirm` 阻断（''/auto/未知不弹；纯函数真值表）+ `xl_confirmed=1`；D 「本会话」小字 + 「收→ 全局默认 / 发→ 仅本会话」tooltip + zh/en/zh-Hant 6 键；E 52 条 mismatch 统计（全 `provider=none`，27 条是 60s 负缓存回放；#275 裁剪不波及翻译 `_bare_system`；卡在 `[xlate]` 行无 `tgt=`，①②候选根因已随 `4fdd238f`/1.0.80 修，待 skuio 升级复看）；F 新 `test_lang_pref_routes_q16.py` 12 例（含 node 真跑会话切换）+ js_syntax 门禁 | E 补日志字段 `tgt=/src=` 与前端失败负缓存两条建议待立单（`translation_service.py` 他人在途，本线不碰）；zhiliao 路由日志随下次重启装载 | `发版对账_v1.0.81_Q16.md` | ≈$40 |
| **Q-17**（09-11 追加，Q-2/Q-3/Q-15 之上） | 风控三级分级 + commitment 只评出站 + 摘标冷却 + 风控页公开 | #277② | $160 | **收工**（09-11，`6ec6e0d6` 后端 A/B/C/E + `6adeebf8` D/UI + 落点表 `774f83cd` `b6be0ea6`） | A 新 `risk_grader.py`：`grade(text, direction, persona)` 三级 + `CATEGORIES` 15 类（高 self_harm / minor 新 / threat 新 / money_request / scam 新 / payment_keyword；中 adult / stop_contact / request_* / complaint；低 privacy / narrative）+ `regrade_inbound` 钩子（Q-15 同式，高一字不变、只降「仅可降因子撑起的 high」、中打 `risk:medium`、低只 `[risk] low` 日志）+ `classify_reason` / `risk_overrides_of`（可覆写类别有地板）；B `keyword_risk_hits(text, direction)` 旧签名逐字旧口径，`in` 不跑 `detect_commitment*` 只 `detect_request → request:<kind>`，`out` 仍评承诺；`your address / phone number / my address is` 拆到 `_SENSITIVE_PATTERNS[3]` low；C `risk_hold.touch / cooldown / cooldown_blocks`（`risk_cooldown:<cid>` KV，30 分钟同类不重打、更高级可打）+ `tag_needs_human` 保持期幂等（不重写 meta ＝ 不重弹横幅）+ `clear_needs_human` 人工摘标写冷却 + PUT `/tags` 摘标改经 `clear_needs_human`（**修 Q-3 漏口**：此前摘标不解 risk_hold）+ 打标 / 摘标日志 `level= category= hits= cooldown_skipped=`；D `GET/POST /api/reply-settings/risk-grader[/overrides]` + 回复设置页「🧯 风控分级」卡（只读表 + 人设 `boundaries.risk_overrides` 下拉，地板以下不给选）+ 收件箱 `risk:medium`「风险·中」标签 / 「风险保持 · <类别>」会话头 chip / 横幅「命中：<类别> · <级别>，命中词」+ `reply_diagnosis` high_risk 带类别 + 新 i18n pack `risk_grader.py` 三语 62 键；E `test_risk_grader_gate.py` 37 例（15 句叙述含 Cameron 原句零需人工 / 5 句真高风险 / 冷却四态 / 幂等 / 旧签名 Q-2 钉桩 / 覆写地板 / 路由往返 / i18n 覆盖），邻接 12 文件 369 + 模板 i18n 路由门禁 538 绿 | **R 批门禁清单加 `test_risk_grader_gate.py`**（与 `test_commitment_gate` / `test_adult_grader_gate` 同组）；`.py` 改动须实例重启；`quick_analyze._RISK_TERMS["money"]` / 支付词表刻意不降（账号安全地板，`chat_assistant_service.py` 不在归属）；`risk:medium` 标只加不自摘；i18n 走新 pack 而非 `zh_hant_auto.py`（生成物他人在途，已记落点表）；`test_reply_settings::test_platforms_match_worker_registry` 红为他人在途 tiktok/douyin worker，非本单 | `发版对账_v1.0.81_Q17.md` | ≈$55 |
| **R81** | 发版 1.0.81 —— **单一版本**（老板 09-11 14:3x「不要内测版，只要一个版本，把所有的功能都进行提交」）：1.0.80 全部 + Q-16 + Q-17 → 一个 clean 包 | #276 #278 | — | **已发（09-11 16:0x 公共渠道 + 内测渠道桥接同字节 + 官网 + 坐席三台 1.081 clean/public）** | 版本链 `06951ac4`（1.0.81 / 1.081 / 繁体 17513 / changelog 7 条 / ui-build 20260911-1436 / 小智词条 ×2 + how-to ×2）→ 官网五值 `76efe139` → 干净 worktree `D:\boundless-r80@06951ac4` `dist:win:clean` **522,854,426 B** sha256 `18d58ac9…` → 回归 148 红 / 27207 绿，串行 103 红，**与 1.0.80 release commit 对照零新增** → 坐席三台 14:59–15:07 → publish 公共 + internal 桥接（同 sha512）→ `gate:content` 6/6 + `deploy -FromHead` → #276 #278 fixed + 回访两条 + 群播报 mid 1431 + 频道 162 | 未进包：Q-12 / Q-13 未开工；HEAD 既有 102 条红交各线（见落点表 §6） | `发版对账_v1.0.81_发版.md` | |
| **Q-18**（09-11 17:2x 追加，P0） | 全自动不被机制掐停：adult `cum` 误触 / 人工发送让位 60s 后丢稿不接回 / 拦截不可见 | #292 #291 #293 #285 #278追加 | $160 | **收工**（09-11，`6882f874` A + `d9d7f6c6` B/C + `40a4d8cd` D + `c2f341db` E/gate_sweep + 落点表 `416beb34` `bb3302fe`） | A `adult_grader.py`：`cum` 只认 `cumming / cumshot / make (me|you) cum / wanna cum`，`neutralize_cum()` 抹掉印式「\w+ cum \w+」与 `cum laude`；`_AMBIGUOUS_EXPLICIT`（cock/pussy/anal/hard on/boner/nude）孤立命中 + 无施压词 → 最高 flirt（`ambiguous=True`），≥2 信号或 1 强 + 施压才 explicit；门禁 +8（6 句：「message cum reply」「summa cum laude」= none/mention、「make me cum」= explicit 等）。B `autosend_policy.py` `YIELD_DEFER_REASONS / AGENT_YIELD_WINDOW_SEC=60 / AGENT_YIELD_MAX_DEFERRALS=10 / agent_yield_state()`；`autosend_worker.py` 人工优先闸 `agent_sent / agent_typing` **从 abort 改 defer**：batch 段稿留 pending + `_yield_defer` 登记 + `call_later` 窗过唤醒（`[autosend] defer=agent_sent stage=batch draft= conv= until=`，每窗只记一次）→ 窗过 `[autosend] resume=agent_window_passed`；presend 段已批稿带 `_yield_defer` 回 `_retry_queue`，`next_ts=until+0.5`；让位期间坐席真发一句 → deferred 稿撤（`abort=… stage=deferred`，防同问双答）；连续 10 次让位仍被顶 → `yield_exhausted` WARNING 按旧口径 abort。`mode_changed / risk_hold / needs_human` 一字未动（`test_risk_hold_q3` 钉桩）。C `resume_agent_yield(cid, by)` 清 `_agent_sent_ts/_agent_typing_ts` + 撤 defer + `next_ts=0` 立即放行（`[autosend] resume by=mode_select|chip`）；POST automation 选 `auto_ai` 顺带接回（响应 `agent_yield_resumed`）；新 `POST /api/unified-inbox/agent-yield/resume`；GET automation 带 `agent_yield` → 会话头 `ay-` 蓝 chip「AI 让位中 · 坐席 60s 内发过 · N 秒后接回」本地倒数、点即接回（chip 主体随兄弟线 `7c4aae3b` 整文件入 HEAD，本单补接线钩子）；`reply_diagnosis` finding `agent_yield`（by/until/remaining）+ 体检面板「立即让 AI 接回」按钮；新 i18n pack `agent_yield.py`。D 新 `abort_ledger.py`：app_settings 单键 `abort_ledger:v1` JSON 滚动 200 条（**不建表**），只挂既有 `[autosend] abort=` 三处 + 班表扣留（按 until 去重）+ `[needs_human] 打标` 落地行（adult vs needs_human）；defer 不进账；`GET /api/reply-settings/abort-ledger` + 回复设置页「今日拦截」卡（24h 按 7 原因码计数 + 最近 5 条，旧后端 404 隐藏）+ 新 i18n pack `abort_ledger.py` 16 键 + `why_no_reply` verdict `abort_ledger`。E `tests/test_agent_yield_defer.py` 23 例（手发后 60s 内入站不丢 / 窗过自动发 / 切全自动立即发 / 再手发再让位 / presend 回队 / 让位中真发撤稿 / 上限 / 其他码不变 / 路由 / 诊断 / i18n / 模板接线 / 台账 7 例）；`gate_sweep.ps1` 固定清单 +`test_adult_grader_gate / test_risk_grader_gate / test_agent_yield_defer`（三文件 89 绿）。回归：autosend 域 63 文件 1403 绿，D 邻接 129 + 523 绿 | **`.py` 改动须实例重启，本单未重启**（adult_grader / autosend_* / abort_ledger / protocol_autoreply / reply_diagnosis / 两条路由）；`note_agent_send / note_agent_typing` 仍按原样撤在途稿——让位只针对「窗内新入站」，这是指令原意；台账从重启起记不回填；`test_silent_exception_ratchet`（inbox 322 / _root 11）、`test_platforms_match_worker_registry`、`test_sealed_templates_no_legacy_date_formatters`、`test_frontend_dr_wiring_static` 四红为他人在途 / 旧债，非本单；Q-17 曾要求门禁清单加 `test_risk_grader_gate` 未落脚本，本单顺手补 | `发版对账_v1.0.82_Q18.md` | ≈$120 |
| **Q-19**（09-11 17:2x 追加，P0 加急） | 画像 AI 推断只写可锚定事实：槽类型校验 / 原话锚定 / 只吃客户入站 / 待确认不计已采集 | #294 #272追加 | $120 | **收工**（09-11 17:4x–18:2x，`d334508c` 后端 A/B/C/E + `6563c3c9` D/UI + 落点表 commit） | 根因答了（7FN84P：`ground_slots` 只验出处不验形态 + `ASSISTANT:` 出站进 prompt + 无语种守卫；「我住在去嵐山散步」是 `_RESIDENCE_PATTERNS` 裸「住」正则）。A `profile_slots.slot_validate(key, value)`（age 16–99 / 年龄段归一；occupation/location/residence ≤24 字短语禁句中标点 / 第一人称 / 动词起头 / 语气词收尾；marital/family 闭集含英文别名归一；name ≤12）→ 不过 `skipped=invalid:<why>` + `[profile] drop reason=`；B `anchor_reason`：evidence 必填 + 最近 30 条 direction=in 逐字（归一）子串 + 值在 evidence 内，枚举槽验触发词，不过 `unanchored`；C `run_extraction` 不再把 reply 喂 prompt、语料 = `recent_inbound_texts(inbox_store)` + 本条，`lang_mismatch_reason` 逐条投票主语种（值 neutral / 汉字×日文客户 / 中英夹杂 ≥25% 放行）；旧 LLM 轨 `profile_llm` 同尺子、`user_clock_resolver` 居住地启发式过 `place_value_ok`（三条实锤整句 → ""）；D `goal_routes` 只加只读 `confirmed / slots_counts / kind / options`，`cp-goal.js` 双树 N/10 只数 confirmed + 「待确认 M」+ age `inputmode=numeric` 前置校验 + `_clipVal 24` 省略 + ✓/✕ 保留，i18n 4 键 ZH/EN，戳 `20260911f` 三处；E `test_profile_fill_anchor_q19.py` 41 例（含事故回放：夹引用日文 + 整句居住地 + 数字年龄 → 只落 `age=32 / location=嵐山`）+ `tools/profile_purge_unanchored.py --dry-run/--apply/--json`（zhiliao 活体 dry-run 35 命中 = 34 unanchored + 1 too_long，全 `src=llm`，**未 apply**）；宽门禁 94 文件 2028 绿 / 6 红均基线既有 | `.py` 随下次重启装载；清洗 `--apply` 待值守核清单；`skill_manager.py` / `goals/service.py` 零改动；4 条他线戳 pin（`20260910[abcd]` 闭集）基线已红建议改「≥」 | `发版对账_v1.0.82_Q19.md` | ≈$45 |
| **Q-20**（09-11 17:2x 追加 + 09-12 扩 D） | 称呼层级：空值不沿用人设爱称、客户侧「对方怎么叫我」优先于人设名、否认名字出站拦、刚发图认领 | #178追加（VDUJX6 / PQE9ZF / Q9GDEH / 2PKKM6） | $100 | **收工**（09-12 03:0x–04:0x，`22c61e0d` A + `1f586aba` B + `8857b2e2` C + `691f9b02` D + `455f8173` E + 落点表 `7194b814` `f933d8ed` `b33f057e` `63cbf884` `1d91e1c1`） | 根因两句（§〇）：#155 的「逐字段回落人设级」让一百个客户共用 `babe`；联系人级「对方怎么叫我 = Alicia」只进了「绝不拿它称呼对方」没进「那就是你的名字」。A `contact_names.resolve_address_context(store, cid, persona)` 成层级单一事实源：有 cid **绝不读** `persona.names.*`——`call_peer` 空 → 会话显示名（`usable_display_name`：非裸号码 / ≠chat_key / 含字母 / ≤40）或不称呼，`peer_calls_you` 空 → 空；无 cid 才人设级；`overlay_sendpoint_names` 改替换语义与 prompt 同源 + 带 `conversation_id`；`_build_address_pin` display 措辞「不要用任何爱称（人设档里的爱称也不用）」；占位 `inbox.info.call_peer_ph` zh/en 改「未填则用对方显示名 / 暂不称呼」+ `zh_hant_auto` 再生成只 stage 两键。B `prompt_addenda.identity_addendum(persona, account, *, lang, conversation_id="", peer_calls_you="")` + `client_side_name_sentence` 三语「对方一直叫你「X」…绝不纠正、绝不说『我是 Y 不是 X』；自我介绍用「X」」，旧 2 参调用逐字不变；同句经 `persona_manager.client_side_name_pin` 进 full / compact 硬约束。C `persona_guard.find_name_denial / strip_name_denial(text, client_name, self_names)`（en `not X / I'm not X / it's Y, not X / who's X`；zh `不是 X / 我不叫 X / 不认识 X / X 是谁`；ja `X じゃない`；元语句 / 所有格放行）→ `sendpoint_vocative_pass` ④ 剥句、剥空换「It's me, X」+ `[persona-guard] name_denial conv= name= action=` WARNING；能力自曝黑名单补 没这个功能 / 不支持发图 / 我这边不能发照片 / 只能发文字 / I can't send photos / no such feature（临时不便「现在不能发，在开会」不命中）。D `image_autosend.recent_outbound_media(conv, within=30min)` 三源合并（receipt 台账 / inbox 出站媒体行＝**人工手发** / 相册 sent_history）+ `is_media_identity_question` + `recent_media_claim_note` 三语；`run_autosend_image` 「是你吗」追问且刚发过 → 不发第二张；`sent_media_addendum / album_miss_addendum` 加 `conv_key/inbound`，台账空但刚发过 → 「刚发过一张…认领」替代「从未发过 / 无可用照片」；`last_media_receipt(cid)` 绑 ContextVar 桥（20s）让 addenda 在 persona_reply 未传 conv 时取会话。E `tests/test_address_names_q20.py` 5 例（四门禁 + 对照；VDUJX6 回放走真实 `persona_reply._prompt_addenda`）绿；`test_contact_names` 21 重写为 Q-20 口径；guard 域 `-k` 1967 绿 | **`persona_reply.py` 零改动**（Q-6 拆桥一行接线见落点表 §四.1）；`skill_manager` 生成侧 name_denial + #24 互换仍读人设级（§四.2，Q-25 在编辑）；「改写不成转人审」未做（drafts.py 热区），出站改写确定性兜底；`.py` 随下次重启装载，本单未重启；既有红非本线：`test_image_autosend` 7 / `test_info_question_hits[抹茶…]` / `test_packaged_capability_ledger` 三项 B 类能力未登记 | `发版对账_v1.0.82_Q20.md` | ≈$60 |
| **Q-21**（09-11 17:2x 追加；09-12 04:5x 修订：#289 已由 `7c4aae3b` 修划掉，加 lang_catalog + conv_lang_plan + #302 / Y82GWM） | 语言能力矩阵单点（34 码含 yue / zh-tw，五处只读它）/ 起草语言计划 `[lang-plan]` 可追 + `lang_unknown` 不再静默 / 粤语生成链 / 译文字体不遮原文 | #290 #286 #302 Y82GWM | $100 | **收工**（09-12 05:0x–07:1x；`.py` 须实例重启才生效——lang_catalog / 路由 / 起草链 / 引擎路由；模板 / CSS / i18n / copilot 组件热更） | A `f3db1410` / B+C `ea882136` / D `8edc9ae3` / 落点表 + 总账（收工 commit）。**A** 新 `src/i18n/lang_catalog.py` 34 码（= HYMT 模型卡，含 `yue` `zh-tw`）× 中 / 英 / 繁名 + 能力位 `draft/translate/tts_clone` + `GET /api/lang-catalog`；回复工坊 `cp-draft` / 翻译工具 `cp-xlate-tools` / `copilot-client` 双适配器 / 人设 `#p-language` 只读它（短表仅兜底）、顶栏 `_XL_CATALOG` 门禁同序、`HYMT_TARGET_LANGS = CODES`（手写 34 码删）。**B** `outbound_translate` 只加 `build_conv_lang_plan`（六级**原序** → 客户明确请求 → 账号先验 → unknown；一行 `[lang-plan] conv= reply= xlate= tts= by= via=`；注册表 + 写穿 KV `conv_lang_plan:<cid>` 供 Q-26 状态带）；`autodraft_helpers` 起草前建计划、`generate_persona_reply(reply_lang=计划)` 只读它、`l1_reason.lang_unknown` 同源（英文「Hi」判 en 不再 lang_unknown）；对方零证据 × 人设有语言 → 按人设语言起草不 HOLD + 会话头 `#lp-bar`（Q-26 `cs-band` 在场让位）+ 稿头 `#lp-cdraft`「对方语言未知 · 按人设语言（X）回」zh/en/zh-Hant + `GET /api/unified-inbox/lang-plan` + `/api/drafts` 行内 `lang_plan`。**C** `persona_reply.yue_style_directive`（口语粤语指令 + 3 few-shot，三处注入）、`ai_client._LANG_NAMES` +yue/zh-tw、`EngineRouter` 对 `supports_target=False` 引擎直接让路（yue 只落 HYMT / LLM / Google / MS / Baidu）、`[xlate]` 加 `tgt=`。**D** `.msg-translation` 字号恒 0.9× 原文 + 行高同原文 + 普通块流钉死 + 收到气泡译文色跟 `--ifg`；深 / 浅主题弹层 + 34 语第二屏 + 译文行截图 `docs/shots/q21/`（Playwright 实测 11.7px=13×0.9、overlap=false）。门禁 `test_lang_catalog_gate` 15 + `test_lang_plan_q21` 13 全绿；邻居 204 绿；i18n 2 红均他线 / 基线 | 红线：`resolve_outbound_lang` 六级顺序零改动（源码钉）；`unified_inbox.html` 只动 `?v=` / `lp-` hunk（`stage_hunks`）；`reply_settings.html` / `drafts.py` / TTS 零改动。**边界**：人设也没定语言 × 零证据 → 计划 `unknown` 不落 zh、出站闸仍 D-M3 HOLD（没语言可回落时 HOLD 是对的）。**交值守**：`gate_sweep.ps1 $gates` 补两套新门禁（文件里有 Q-27 未提交 hunk 紧贴列表尾，本线未追加）；zhiliao 重启后 `/api/lang-catalog` 才 200（此前两组件按设计回落短表）；#290 #286 #302 回访随 1.0.84。前段 05:0x–05:5x 由上一段会话完成未提交，本段 06:0x 接手核对补齐 | `发版对账_v1.0.83_Q21.md` | ≈$85 |
| **Q-22**（09-11 17:2x 追加，语音线；09-12 修订加 #304 / #239） | 克隆语音语种不支持不静默换系统音（默认不发，系统音需二次确认；自动链改发文字）+ 语种一致性 + 登记结果面板 | #287 #288 #239追加 #304 | $130 | **收工**（09-12 05:1x–06:4x，`b9b37262` A/C/D 后端 + `20b088d3` A 前端红条 + `1867bac4` B/E/F） | 根因两句（落点表 §0 证据 V9YAAX / YNH6ZW / GWJ2RZ / 4EXEEY）：D-L4 的 `clone_engine_offline` 守卫把「语种闸 / Token 耗尽」两类**豁免**了→克隆不可用照旧静默回落 edge；`_route_cantonese` 粤文一律切 zh-HK 系统声连克隆人设也切。A `tts_pipeline.synthesize(confirm_system_voice=False, tts_lang="")`：克隆主链 ×（语种闸 / 念错 / 钱包耗尽 / 引擎不可达）且未确认 → `ok=False error=clone_unavailable:<why>` + `extra.clone_unavailable / degrade_to_text / system_voice=<同语种同性别预置声>`，`[tts] fallback blocked reason=clone_unavailable:…` 一行，edge 零调用；`confirm_system_voice=True` 才走既有 edge 兜底并标 `system_voice_confirmed`；`_route_cantonese` 克隆人设不切系统声（交语种闸）、`clone_lang_gate` 粤文按 yue 查表；voice-send / tts-test 阻断响应 `{ok:false, blocked, reason, clone_unavailable:{why,lang,system_voice,system_voice_available}, message}` + **路由级拒收** `provider∉CLONE ∧ fallback_from∈CLONE ∧ 无 confirm`（试听复用 / #93 补标旁路同拒）；右栏 `cp-voice.js`（双树）红条 `pv-note err` + 「改发文字」（`cp-voice-send-as-text` → 宿主填 `#reply-ta` 不自动发 / 桌面壳走 cp-fill 桥）+ 「用系统音发（对方会听到非人设声）」（原生 confirm 二次确认 → `confirm_system_voice=1` 直发，仅 `system_voice_available` 才出键）；`copilot-client.voiceTts` / `desktop/main.js` IPC 白名单透传。B `/api/voice/profiles` 克隆行 +`voice_langs`（`clone_supported_langs` = effective-config 同口径）→ 人设语音卡 `#pe-vc-langs`「克隆声支持语种：zh / en」；右栏状态行「会念 zh / en」+ 目标语超出克隆能力 → **生成按钮置灰带原因**（`_syncGenGate`，预置声不置灰）。C A 线 `sender.py` / B 线 `voice_autosend.py` 传 `tts_lang=plan_tts_lang(...)`，阻断 → `[tts] skip_voice reason=clone_unavailable:… conv= lang=` 改发文字（`record=False`）；minicpm 探到冷启动 → 自动链等 `avatar_voice.cold_start_retry_sec`（缺省 20s，受预算裁剪）重试主链**一次**。D `_lang_consistency()` 文本 / 音色 locale / `conv_lang_plan.tts_lang` 三方核对（`plan_tts_lang` hasattr 兜底，Q-21 未合并返回空）→ 合成生效行 `text_lang= tts_lang= source=`，不一致 `error=lang_mismatch` + `[tts] skip_voice reason=lang_mismatch`。E `/api/voice/enroll` 三出口 additive `supported_langs + preview{kind:tts_test, tts_test, reference} + health{probe_spawned,last}`（不在登记请求里同步合成）→ `personas.html` `#pe-vc-result` 可停留面板（成功=录音名 + ▶ 试听 + 支持语种 + 体检回填 + 质检提示；失败=原因 + 服务端原文；× 才关）；`cp-voice-enroll-failed` 新事件；errors_stock 6 键 / persona_studio 10 键 / cp-i18n 13 键 ZH+EN。F `tests/test_tts_clone_unavailable_q22.py` 7 例绿；语音 10 套件 197 绿；既有 D-L4 / 粤语路由 / minicpm 6 例改 Q-22 契约 | **不新增任何默认回落音**（红线守住：system_voice 只是「可二次确认的候选」，缺省不合成）；`.py` 随下次重启装载，本单未重启（zhiliao 攒 #304 / Q-27 / Q-26 同批）；`copilot-client.js` 缓存戳沿用 Q-21 在途 `20260912q21` 由其 commit 携带（新字段为空即省略，旧戳不致错）；未做：登记面板「试听」仍复用 `peVcPreview()` 单句探针（未做多情绪四段）；Q-21 `conv_lang_plan.tts_lang` 落地后 D 段自动升级为计划语种核对；既有红非本线：`test_i18n_coverage` 2（conv_route `inbox.mp.*` / Q-14 `toTimeString`）、`test_legacy_blue_ratchet` 2（ops_* / reply_settings）、`test_template_orphan_refs`（reply_settings `rps-po-empty`） | `发版对账_v1.0.83_Q22.md` | ≈$85 |
| **R82** | 发版 1.0.82 —— **单一版本**（D-Q11）：1.0.81 全部 + Q-18 + Q-19 + 用户管理 P0 / xlate-pop / cp-goal #263 #264 / 老板 16:3x 四批 → 一个 clean 包 | #292 #291 #293 #285 #284 #294 | — | **已发（09-11 20:1x 公共渠道 + 内测渠道桥接同字节 + 官网 + 坐席三台 1.082 clean/public）** | 版本链 `5ccc0013`（1.0.82 / 1.082 / 繁体 17510 / changelog 10 条 / ui-build 20260911-1838 / 小智词条 ×4 + how-to ×3）→ 官网五值 `e008298c` → 干净 worktree `D:\boundless-r80@5ccc0013` `dist:win:clean` **523,068,004 B** sha256 `5acac8ef…` → import_sweep 1248/0 绿、gate_sweep 18 红与 R81 同名零新增 → 回归 127 红 / 27437 绿，串行 104 红，**与 1.0.81 release commit `06951ac4` 对照差 6：1 mtime 伪红 + 5 测试钉子过期（`da66a0f9` 扶正，包不重打），0 功能性回归** → 坐席三台 19:02–19:09 → publish 公共 + internal 桥接（同 sha512）→ `gate:content` 6/6 + `deploy -FromHead` → 6 单 fixed + 回访 6 条 + 群播报 mid 1453 + 频道 163 | 未进包：Q-20 / Q-21 / Q-22 未开工（#178 #289 #290 #286 #287 #288 随下一版，群播报已明示）；zhiliao 20:28 被 watchdog_start 重启已载入 Q-18 / Q-19 `.py`（探针 abort-ledger 401 vs 假路由 404）；`profile_purge_unanchored --apply` 待值守核；HEAD 既有 98 条两版同红交各线（落点表 §6） | `发版对账_v1.0.82_发版.md` | |
| **Q-23**（09-12 02:2x 追加，**事故 P0**） | 自动出站单一闸门 + 守卫场景维度：群 / 频道 / 报障群、人审手动档、同事消息永不自动发软回应；软回应改经 autosend 门、语言跟对方、人设口吻短生成 | **#303**（第 0 步立，P0，报障群 mid 1445 1454 1459 1461，account 7331682688） | $140 | **收工**（09-12 02:5x–04:1x，`fbb75d15` A0 止血入库 + `cc972944` A/B/C + `af2733f2` D + `7f7f680f` E/gate_sweep + 落点表随每 commit） | 0 立 #303 + 21:01 止血原样入库 + 门禁「TG 负 peer 无 chat_type 也 skip_public_chat」。A 新 `guard_context.py` `GuardContext{is_group, mode, sender_kind, lang}`（群 = `is_group_conversation` ∪ `is_bug_group` ∪ never_auto_reply ops_group；发送方 out/==account→self、同事名单→agent、bot→peer_bot；档位缺省 `resolve_automation_mode`，解析不了 → review fail-closed），`drafts.auto_generate_draft` 构造一次传 `keyword_risk_hits(ctx=)` / `risk_grader.regrade_inbound(ctx=)` / `adult_grader.regrade_inbound(ctx=)`：群 / 非客户 → 不评估不出站不打标（`info.skipped`，日志 `[guard-ctx] skip= stage=`）；`ingest.conv_dict` 透传 `sender_id`。B 软回应唯一出口 `AutosendWorker.deliver_soft_reply`（stage=soft_reply）：`autosend_policy.decide(kind="soft_reply", ctx=)`（场景 L0 `scene:*` / 冻结 / 非 auto_ai → `mode:<m>` review_required / 他因持有 L1）→ `_human_priority_gate`（只豁免自家 adult 持有；agent_sent/typing 对软回应是放弃非 defer）→ 生成 → 再过闸 → `_deliver_now`（从 `deliver_human_approved` 抽出的共用投递核心，人工通过行为逐字不变）→ `record_sent` 账本；manual/review 档 → reasons `adult_soft_alt:<mode>` 审核候选不出站；`_inbox_deliver_cb` 只剩坐席人工通过一个调用方，`set_inbox_deliver_callback(soft_reply_cb=)` + `web_app` 两处 worker `app=` 接线；`dispatch_soft_reply` 排 `svc._soft_reply_cb` 行（`no_soft_reply_cb` 绝不回落人工通过链）。C `persona_reply.generate_soft_deflect`（`resolve_effective_persona` + `generate_reply_with_intent(intent=adult_soft_deflect)`，语言跟对方 `resolve_outbound_lang`，一句预算后检，**无兜底句**）；worker `_soft_reply_generate` lang_unknown / no_app / 生成失败 → `gen=skip` 不发。D `peer_bot_guard.never_auto_reply_ids/reason`（同事 = `bug_intake.support_accounts` ∪ own_bot ∪ 通知 bot ∪ 本租户账号 ∪ 显式；群 = `bug_intake.groups` ∪ webhook target ∪ 显式）钩在 A 线 / B 线守卫 `enabled` 之前 + `protocol_autoreply` 群 opt-in 之前，日志 `[peer-guard] skip reason=colleague:|ops_group: cid= line=a|b|protocol`。E `test_guard_context_gate.py` 11 例 4 条门禁（报障群回放零出站零打标含真 worker `scene:*`；review 候选 + 真 worker `mode_changed` + gen 失败零发送 + gen 成功文本 ∉ 固定句库；同事/自己不评估而真客户判定与无 ctx 完全相等；静态 `_inbox_deliver_cb` 引用文件 == `[drafts.py]`）入 `gate_sweep.ps1`；4 套 gate 合跑 108 绿，autosend 人工通过 7 套 126 绿，peer_bot 74 绿，邻接 380 绿 | **`.py` 改动须 zhiliao 重启才生效，本线未重启**（`deploy\instances\restart_instance.ps1`，交接值守）；`bug_intake.py` 工作树 `build_fix_notify_text` diff 与 `test_bug_intake.py` 不属本线未带；`test_autodraft_bootstrap::test_autodraft_bootstrap_persists_auto_ai` 红为 09-07 `3fe64e53` 既有（`source="bootstrap"` 断言过期）非本线；软回应旧固定句库 `_SOFT_LINES` 只剩坐席挑句端点用，自动链零引用；#303 回访随 1.0.83 | `发版对账_v1.0.83_Q23.md` | ≈$60 |
| **Q-24**（09-12 02:2x 追加，P0） | Messenger 网页代发链止血：composer 重绑 / 失败原因码进界面 / e2ee PIN 可见 + 手机出站回抄 / 反应入站 / 登录窗必隐藏 / 人工图不当客户图 | #298 #297 #299 #300 | $200 | **收工**（09-12 11:0x–12:xx，`8a1c1555` 边车 A–E + `6ce57f75` Python B/C/F + `8fc8d4c6` 工作台 B/C/D + 落点表 commit） | 根因三句（§〇）：DXAPAX/FJM9ER = `box.click()` 的 ElementHandle 被 Messenger 重绘换掉 → detached → 500 无码 + 立刻 re-navigate 再撞 → 4 连败进 40s backoff 静默；TKV3HB = `maybeAutoHideAfterLogin` PIN 分支一次性 return 永不再武装 + Playwright 默认 `--no-sandbox` 黄条；P7P8FY = 反应 aria 正则早已匹不到当前胶囊。A **新** `send_chain.js` 纯函数层：`inputWithRebind`（detached → 同线程 `resolveComposerStable` 重查（waitForSelector + MutationObserver 300ms 静默）再输 1 次 → 仍 detached 才 re-navigate 1 次 → 抛 `composer_detached`）、per-jid 连败 ≥2 进待重试队列（60s×3、只重放同文案、重试前回读防双发）+ `postStatus send_stuck / send_recovered / send_stuck_final`；B 七码 `composer_detached\|thread_not_found\|e2ee_pin_pending\|call_overlay\|send_backoff\|login_expired\|upload_failed` → `/send` `/send-media` 全部失败分支 `sendFailBody`（404/429/503/502/409/500 按码）→ Python `http_error_fields` 读 `code/retries/detail`（老边车 `sidecar_fail_code` 归一）→ `MessengerWebWorker.send/send_media` 带 `sidecar_*`（媒体不再裸抛）→ 路由 502 `{code:"sidecar_send_fail", sidecar_code, retries, retry_after_sec, message}` → 工作台红字三段式「原因 · 已自动重试 N 次 · 你可以手机发 / X 秒后再试」+「重试」（同文案）+ 留痕 `fail_reason=七码` + `reply_diagnosis` finding `sidecar_send_fail`；**新** `sidecar_send_alert.py` session-status `send_stuck*` → 通知中心 `sys_status` 同会话合并（不进 PlatformSessionHealth）；C PIN 浮层在场 → `_pinState=missing` + `postInboxHealth` → `hint_code=e2ee_pin_required` 不等半死即透 `inbox_hint` → 会话头黄条「Messenger 需在手机确认 PIN 后才能同步」+ 托管 PIN 按钮；PIN 过（自愈 / 手机 / 心跳复查）→ `scheduleBackfillAfterPin` 重建 backfill 队列；手机发出 `mirrorManualOutbound` 回抄 `direction=out origin=external sender_id=external` → 只落库（起草只对 in、让位只在工作台手发路由）+ 工作台「手机发出」角标；D `REACTION_ARIA_RE + parseReactionPill` 双解析入 `readThreadTail`，反应经既有 `kind=reaction` ingest 落库 + 角标词条，`reaction_followup` 默认关不起草；E `maybeAutoHideAfterLogin` 自重武装 `hideTick`（PIN 在场每 15s 复查、`HIDE_PIN_GRACE_MS` 2min 后强制隐藏、`window visible reason=` 日志），`bringToFront` 核实只在登录三阶段，`detectCallOverlay` 自动关来电 / 通话 dialog，`launchOptions.chromiumSandbox=!MSG_NO_SANDBOX` 治黄条；F `normalize_history` 出站媒体占位 `[我方发出的图片]`、`update_message_text` 按 media_ref 回写只落入站行；G `send_chain.test.js` 16 例（含 **DXAPAX 四连败回放**：4 次全结构化码、第 2 次起排队 + 铃铛、重试成功出队）→ `node --test` 152 绿；**新** `test_messenger_sidecar_q24.py` 23 例 + 邻接 36 套 731 绿；i18n 走新包 `messenger_sidecar_q24.py` 三语 | **Node 边车 + zhiliao 实例均须重启才生效，本线未重启**（交值守）；`autosend_worker.py` / `autosend_policy.py` / WA / TG 边车 / 翻译区零改动；失败重试永不换文案；Y82GWM 草稿语言证据已记落点表归 Q-21（`tmp_diag/Y82GWM` 空包）；`test_i18n_coverage::test_sealed_templates_no_legacy_date_formatters` 为 HEAD 既有基线红（`toTimeString` 他线模板债）；#298 #297 #299 #300 回访随 1.0.83 | `发版对账_v1.0.83_Q24.md` | ≈$70 |
| **Q-25**（09-12 02:2x 追加，P0） | 事实门二期：FactGate 共享（画像 / 记忆 / 目标回填）、「AI 问出来的答案」不回填、记忆主体守卫、#295 确认按钮必有反应 | #295 #300 #272追加 Y39D8U | $120 | **收工**（09-12，`08554f31` A + `618b25e5` B + `30ce48d5` C + `d6b69a1d` D + `48bf5ae0` E + 落点表 `6d934d45`） | 根因答了（Y39D8U：客户答「been a couple months」→ 目标引擎**旧 LLM 回填轨** `profile_llm.run_llm_capture` 只用 `fact_grounded_in_user_msg` 任一 token 重合，`work` 命中 AI 自己问的「inspection work」→ `occupation=AI work`；`slot_state` 对「AI 问过 + 客户答了 ≥2 字」直接给 `mentioned:answered` 无值，卡片把它画成「已提及（未确认）」+ 确认钮，钮对空值静默 `return`）。A 新 `companion/fact_gate.py` `check(value, slot_or_kind, evidence, inbound_texts, customer_lang) -> (ok, reason)`：类型尺子复用 Q-19 `slot_validate`、来源守卫（只认 `direction=in`）、evidence 归一后须**逐字在同一条**客户入站（禁跨句拼接）、值的每个拉丁 token 须**整词**在 evidence（`work` ≠ `inspection work` / `network`）+ CJK 段字符 / 二元组锚定、语种守卫、`kind=fact` 加主体守卫（`一起 / 我们 / together…` 与原句「我 / 出差」冲突即 `subject`）+ 不变量 token（人名 / ≥2 位数字）须在原句；任何异常 → `(False, "gate_error")` 不放行；`profile_fill.apply` ai_inferred 分支改调它（`anchor_reason` 成薄封装，Q-19 41 例逐字绿）。B `profile_slots.answer_only_kind(text)`（duration / yes_no / number / filler）；`goals/service.py` **只动** `schedule_llm_capture` 一处：客户只答时长 / 是 / 数字 → `[profile] drop reason=answer_only:<kind>` 不起 LLM；`profile_llm.run_llm_capture` 抽出值改过 FactGate（`evidence=客户原句`）；`decide_probe_target` / `verify_pending_probe` 零改动。C `skill_manager._episodic_memory_extract_async` LLM 事实 `add_fact` 前过 `check(kind="fact")`，无原句 / 主体冲突 / 未锚定 → `[memory] drop conv= fact= evidence= reason=` + 计入 `grounding_dropped`（50 连 0 告警保留）。D `cp-goal.js` 双树：无值 mentioned → 「{label}：AI 有线索，待补值」禁用确认钮只留 ✎；有值确认**必发** `POST /api/goals/profile/confirm`（网络层 800ms 重试一次）成功 toast + 行内 `✓` 再对账、失败行内红字 + toast；取数 3 次退避 + 5s 自动重刷；i18n 5 键 ZH/EN；戳 `20260912a` 三处 + 三处 pin。E `test_fact_gate_q25.py` 40 例（couple months → AI work 丢 / 部分匹配不许 / 我出差去越南 ≠ 一起去 / 回放 Y39D8U 两条轨 occupation 不写 / #295 空值不可确认 · 有值必发请求 / 清洗 `--memory`）+ `profile_purge_unanchored.py --memory --dry-run/--apply/--user-id`（zhiliao 活体 dry-run：画像 35 与 Q-19 同、记忆 **122**＝无引文 121 + 未锚定 1，多为演练号旧事实，**未 apply**）；回归 Q-19 41 + 邻接 223 + copilot / i18n 门禁绿 | `.py` 六件须实例重启（未重启，下一窗口）；`confirmed` 一字未动、`persona_reply.py` 零改动；`--apply` 待回访核清单；`test_i18n_coverage::test_sealed_templates_no_legacy_date_formatters` 为 HEAD 既有基线红 | `发版对账_v1.0.83_Q25.md` | ≈$60 |
| **Q-26**（09-12 04:5x 追加，界面；21:0x 值守补 C-2） | 会话状态带（六种「不发」一条状态一句话一动作）+「自动化与风控」设置中心收口（修 #301 死链）+ **C-2** 风控分级卡「人设覆写」下拉定性（#301 追加 FJ8YU7） | #301 #302 #293后续 + FJ8YU7 | $160 | **A/B 收工已在 1.0.84**（`6d9356ff` A conv_state + `4412dffb` B cs-band，落点表 §1）；**C–E 半成品在工作树未提交、待接手**（`reply_settings_routes.py` +42 / `reply_settings.html` +358 / `i18n_packs/automation_risk_q26.py` 新 / `persona_manager.py` +20 / `personas.html` +11 / `unified_inbox_send_routes.py` +2，最后动 09-12 06:10；意向板条目 05:01 已过一次重启） | A conv_state 只读聚合 + 端点 / B 一条状态带四色 | C 设置中心 + 人设覆写互链 + C-2 下拉定性二选一 / D 死链门禁（加 `rps_rk_ov_adult` / `rps_rk_persona_none` 键面扫描）/ E 门禁 + 截图；粘贴块已改为「接手 C–E」版 | `发版对账_v1.0.84_Q26.md` | |
| **Q-27**（09-12 04:5x 追加，P0） | 风控最小硬拦集（未成年 / 自伤 / 威胁 / 要钱要凭据 / 诈骗）+ risk_hold 生命周期（TTL 2h、摘标 / 人工发 / 切全自动即释放、低级不续命）+ 打标行必带类别 + phrase_whitelist | #301追加 AFD2CD #296 #303复验 | $140 | **收工**（09-12 05:0x–07:5x，`0f0dca59` A + `dc1f56be` B + `636f19d9` C + `823893da` D + `41ce1c43` E/落点表；`.py` 须 zhiliao 重启才生效，**本线未重启**，攒 R83 断点② 同一批） | 根因四条（落点表 §〇）：adult explicit 与 pressure 同路 → 一次命中 = 24h 持有 + 打标；保持行照抄本条 benign 入站的 `level=low category=- hits=-`，低级入站反把 hold「保持」下来（`touch n=26`）；释放只有摘标 / 手发两口，切全自动不碰 hold、到期日志不叫 clear；`TRUE_HIGH_REASONS` 含 `money`/`adult` + `_SENSITIVE_PATTERNS[0]` 锁死 `_downgradable` → keyword-only 可单独 high。**A** `TRUE_HIGH_REASONS={self_harm, stop_contact, credential_or_payment_request}` + `adult:pressure` 为 adult 撑 high 唯一因子；`money` 单词需 `detect_request` / `_CRED_REQUEST` / 承诺兜底第二信号才 `money_request`，否则 `money_mention` 低仅日志；`payment_keyword` 降中级 `mark_review`；adult explicit×(human|soft_reply) → medium 不 `risk_hold.set` 不 `tag_needs_human`，soft_reply×auto_ai 走 Q-23 闸门 `send_soft_reply`（短生成、失败不发、无罐头句）且 drafts 不另拟稿（`skip reason=adult_soft_reply`），human → `persona_catch`；pressure 一字不动。**B** `risk_hold.DEFAULT_TTL_H=2.0`；到期 / 人工 / 切档统一 `[risk_hold] clear conv= reason= by=ttl|agent_ack|agent_send|mode_select|low_inbound`；新 `release_on_auto` 挂在 `POST /api/unified-inbox/automation mode=auto_ai`（紧跟 Q-18 让位接回），兜「有持有无标」的 1.0.82 遗留；`autosend_policy.py` **最终一行未改**（`_resolve_risk_hold` 只认 `active()`，clear 后 shadow 自然消失）。**C** `risk_grader.release_hold_on_low`：本条低级 ∧ 旧 hold（≥60s、非 stop_contact）→ 清持有 + 标 + meta（系统动作不冷却），位于 `policy_decide` 之前所以本条稿即 L2 不继承旧 shadow；`tag_needs_human` 保持路径同规则 `保持→clear`，保持行必带 `level= category= hits= hold_level= hold_category= hold_hits=`（无类别回落 handoff_meta，chip / 横幅同源）。**D** `neutralize_cum` 扩为 `phrase_whitelist`（cum 消歧 + `phone number`→phone / `your address`→your place 叙述 L0 + 配置 `risk_grader.phrase_whitelist` 只收 ≥2 词短语、只碰 mention 层与关键词层、explicit/pressure 词表与句式层不吃配置、无黑名单入口）。**E** 三条回放全绿：① Jeeo「message cum reply」L0 全自动正常回；② 华哥旧 24h adult 持有 → 下一条 benign `cleared_by=low_inbound` 稿 L2 无 `forced=L1`，随后威胁仍 L1；③ bank card number 仍 L1 + `[needs_human] category=money_request level=high hits` + 2h 持有到期 `by=ttl`。四门禁（risk_grader / adult_grader / guard_context / risk_hold_q3）120 passed；gate_sweep 1738 passed / 27 红逐条核对均他线域（模板 / i18n / ratchet / tiktok SQL / mode-writer 台账 / silent_exception 全树越顶），`test_risk_hold_q3` 纳入 sweep | A 硬拦集收紧 / B hold 生命周期 / C 打标行带类别 / D 白名单 / E 回放三条 + 门禁 | `发版对账_v1.0.83_Q27.md` | **事故登记**：`dc1f56be` 误含 Q-26 线当时已暂存的 4 文件（conv_state.py / unified_inbox_read_routes.py / test_admin_route_inventory.py / test_conv_state_q26.py），add 与 commit 链在一条命令、index 非空没先看；内容正确归属错位，未改写历史，此后每次 add 前单独一条命令核 index 为空。重启交接：下一个有重启需求的线顺带 `restart_instance.ps1 -Instance zhiliao`，重启后现网核 `[risk_hold] clear … by=` / `[needs_human] 保持 … level= category=` 出现、低级入站 `touch n=` 不再增长 |
| **Q-28**（09-12 04:5x 追加，P1） | WhatsApp 边车 Bad MAC 会话失步自愈（retry receipt → assertSessions → 标状态）+ 解密失败占位入站可见 + creds 回滚告警 | #279 | $80 | 未开工 | | A 自愈 / B 可见 / C creds 保护 / D 门禁 | `发版对账_v1.0.83_Q28.md` | |
| **Q-29**（09-12 08:4x 追加） | 客户当地时间感知：已确认城市 → 时区注入「不要问几点 / 白天还是晚上」；客户时间叙述作 12h 瞬态事实；时间问句族进去重 | #305 2RKH3H | $80 | **收工**（09-12 08:5x–09:4x，`4614c5d5` A + `feeaf38d` B + `120cac42` C + `610eaf7b` D + 落点表 `7754d15b`；`.py` 须 zhiliao 重启才生效，**本线未重启**） | 根因（落点表 §〇）：引擎只有**人设**当地钟（`time_context` / `excuse_budget.local_time_label`），客户 `location:confirmed` 没人换算成时区；「我这边四点了」不是任何画像槽 → `[extract] facts=0`；`repeat_question_guard` 只比我方 24h 问过的句子，客户**答过**不构成拦截依据。**A** 新 `companion/peer_time.py`：290 城 × 中繁英日别名 + 52 单时区国家 → IANA，`city_to_tz` **命中恰一时区才返回、撞多 / 不中 → None 不猜**（美 / 加 / 澳 等多时区国名、Birmingham / Portland 等同名城刻意不收）；`resolve_peer_tz(profile)` 走 Q-1 `cell_view`（confirmed > mentioned，location > residence）；`peer_local_time` → `{hh_mm, period 早上/白天/傍晚/夜里, weekday}`；`detect_time_statement` 只认对方说自己这边（钟点 / 时段，约时间 / 计数 / 问句 / 我方说对方不认；复述「我说我这边四点了」标 `quoted`）；`fact_gate` **只加** `KIND_TRANSIENT`（原句锚定）+ `TRANSIENT_TTL_SEC=12h`；`time_context.build_peer_time_hint` 纯格式化。**B** 经 Q-8 F `excuse_budget.build_time_schedule_addendum` 尾部追加（= `persona_reply._prompt_addenda` ④ 同一 try-block，**persona_reply 零改动**、Q-8 pin 不变）：KV `peer_time_hint:<conv>` 12h（读 KV → 扫最近入站 → 过 FactGate 写回，复述不当新读钟）；hint 优先于城市，有钟点反推 `UTC±N`，与城市差 ≤1h 取城市精确钟，否则 **以客户原话为准** `conflict=1`；已知 → 「【对方当地时间】对方那边现在 周六 04:57，夜里——不要问对方几点、白天还是晚上」，未知 → 「顺口问一次，24h 内不再问」+ 问过则「已经问过一次」；开关 `companion.peer_time.enabled` 缺席陪伴开 / 销售关。**C** `repeat_question_guard`：`is_time_question` 时间问句族（几点 / 白天还是晚上 / 早上还是晚上 / 天亮了吗 / 该睡了吗 / what time is it there / morning or night / time zone；约时间 / 作息 / 偏好 / 问我方不算）→ `slot:peer_time` 同族标签 1.0；对方当地时间**已知即砍**（`peer_time_known` 懒求值只读，`matched=peer_time:hint|city|hint+city`）；`outbound_humanize` 挂点一行传 `cfg_root`。**D** `tests/test_peer_time_q29.py` 86 例：東京→Asia/Tokyo + 注入「不要问几点」/「我这边晚上」→ 12h 内砍「白天还是晚上」、12h 后放行 / 未知顺口问一次、问后 24h 内砍、24h 后放行 / **2RKH3H 回放**（03:57 四点 → 05:25「白天还是晚上」砍成「哈哈好的，那你先去忙吧。」，反推 UTC+8 05:25 早上）+ persona_reply / decide_probe_target 零改动钉 + location 槽语义不变钉。邻域回归 263 + Q-1 37 + goal 64 全绿。**未接入站钩子**（hint 采集走生成前无状态兜底，行为等价，见落点表 §二）。 | A 城市→时区 / B time_context 注入 / C transient 事实 + 问句族 / D 门禁 | `发版对账_v1.0.84_Q29.md` | `.py` 改动待 zhiliao 重启生效 |
| **Q-30**（09-12 20:4x 追加，**P0**；20:5x 按 HEAD 核对缩量；22:3x 加 E） | 单人端不再误判多坐席（`resolve_seat_mode` 无显式配置时缺省 single——B13 的单坐席守卫全在库，只是被这个缺省绕过）+ 查 `abort_ledger` 定 #311 真闸 + 补 `media_lie_caught_repeat` / `colleague` 两个原因码 + #308 复核 + **E** #319 切档瞬间同句双发（`resume_agent_yield` 放行 deferred 载荷时同会话只出一条；让位回队载荷不算重试项，照过 `_deliver_once.claim` 与近重复守卫） | #309 #310 #311 #307 #319 + #308 复核 | $140 | **A–D 收工**（09-12 23:0x–00:2x，`e13bd46f` A + `6ae59bdb` B/C + `69dd5af1` D + 落点表 / ui-build commit；`.py` 须 zhiliao 重启才生效——`ui_visibility` / `admin` / `store` 迁移与 `cancel_pending_l1_drafts` / `autosend_worker` / `conv_state` / `peer_guard_marker` / `autodraft_helpers` / `reply_pacing_settings` / 两路由；`reply_settings.html` / `unified_inbox.html` / 词包热更）；**E 未做**（本次开工语只含 #309 #310 #311 #307 + #308、$100，E 是 22:3x 追加） | A `resolve_seat_mode` 缺省 single（显式 `ui_visibility.seat_mode` 不改；presence 近 30min ≥2 人才 multi；取不到数据 single；「多坐席协作」开关进 自动化与风控 `rps-ms-`；`DELETE FROM conversation_claims` 一次性迁移；17 例 + 旧钉子改语义）/ B **#311 真闸＝拟人首回延迟（不是 claim / agent_typing）**：W2CSGA 40min 日志零 abort 零 defer，唯一闸 `first_reply hold=115s`，三稿都进了投递链——`first_reply_hold` 进状态带（琥珀倒计时「沉寂 9.9 小时后的首条回复刻意慢一点」），`/api/workspace/typing` 按条件分支不改；真 worker 回放进门禁 / C `media_lie_caught_repeat` 人话 + `peer_guard_marker`（跳过写标 / 放行清标）→ 状态带 `held` `colleague:<id>` `ops_group:<id>` 人话 + 门禁 `test_reason_codes_q30`（码集 ∪ 键集）/ D #308 复核＝**切档前的陈旧 L1 稿**（worker 只捞 L2，旧稿挂着写「全局默认半自动」）+ 预算软停误标 → 切全自动作废旧 L1（`cancelled_l1` toast）+ `peer_budget` 原因码；档位写入路径无 bug。同轮他线红（非本线）：`test_platforms_match_worker_registry`（douyin/tiktok）、`toTimeString` 门禁 + `loadConnectModes` 未挂 window（connect-modes 在途 hunk）、`rps-po-empty` 孤儿引用（d6942318 起） | E #319 切档瞬间同句双发（`resume_agent_yield` 放行互斥 + `_deliver_once.claim` 键改 conv+归一化文本 + 让位回队载荷不算重试 + `tests/test_yield_resume_dup_q30.py` + UG6KJG 回放）——待另开工 | `发版对账_v1.0.85_Q30.md` | ≈$100（A–D，预算内） |
| **Q-37**（09-12 22:3x 追加） | 主动触达调度钟三源同源（`user_clock_resolver` 接 Q-29 `resolve_peer_tz` 作 replace 级核实钟 → 人设钟 → 服务器钟，proactive / care 安静时段同读 `resolve_schedule_clock`）+ 人设当地 23–8 且客户 30min 无入站不主动 + 文本时刻与调度钟 / 人设钟同一只尺 | #318 VV7BRY（15:22 上海 = 03:22 纽约主动发「It's 3am here」；「Marina」部分归 Q-36 B） | $60 | 未开工 | 已在库只接线：Q-29 `peer_time`、`_persona_clock_note`、`world_clock_guard`、`schedule_clock` replace-only | A 三源接线 / B 人设夜间不主动 / C 文本尺 / D 门禁 + 回放 | `发版对账_v1.0.85_Q37.md` | |
| **Q-31**（09-12 20:4x 追加；20:5x 缩为 #317 单项；21:0x 加 D） | 筛选面板系统标签筹码：`dormant:*` / `risk:*` / 停联折叠「系统标签 ▸ N」单坐席默认收起 + × 清筛选 + `dormant:ignored` 新入站自动摘 + 停联确认摘标进 `conv_meta` + **D** 顶栏「发前确认」药丸只计可行动稿（`risk-summary` `actionable` 复用 `approve_block_reason`）+ `auto_expire_hours` 出厂 48h（L1）+ 审批台「清空超龄稿」+ Messenger 边车 backfill 推送带 `backfill:true` | #317 BS7E5Q + PUUWJB（误挂 #317） | $100 | **A/B/C 收工**（09-12 23:1x–09-13 00:1x，`ba18d8ce` A + `e759c8c4` B + `b89a2f28` C + 落点表 `3321dab6` / `46de759b` / `8707d2f8`；`.py` 须 zhiliao 重启才生效，**本线未重启**，攒下一窗）；**D 段未开工**（本轮粘贴块只列 A/B/C、$60，D 留待下一条指令） | 根因三条（落点表 §〇）：`_renderPanelTags` 只剔「需人工」，`dormant:*` / `risk:*` / 停联与用户标签同列且点击只有筛选没有关闭；chip 是标签统计——`dormant:ignored` 全仓只加不摘（`note_real_inbound` 零调用点）、停联标是冻结判据不能摘；单人端与多坐席同一面板。**A** `_isSysTag`（`_tagKind` tko/attn + `^(dormant|risk):` + 停联数据值，不动 `_TK_RE`）→ 第二行「系统标签 ▸ N」折叠块（`!MULTI_SEAT` 收起 / 多坐席展开 / localStorage 记开合 / 正按系统标筛时强制展开），用户标签照旧 Top8；× 只在激活 chip 上＝`setTagFilter('')`，面板开着 Esc 同效，零新颜色零内联 onclick。**B** `dormant_review.note_real_inbound` 唯一写点（`protocol_bridge.ingest_incoming` 真入站挂点，与 `note_backfill` 互为正反向；只摘 `inbound_ts > decided_ts`，历史重放 / 回填不摘）；`conversation_meta.stop_contact_at` 新列 + `stop_contact.confirm_stop_contact`＝摘需人工 + 停联标移入 meta + 归档，`frozen_reason` 读「标签 或 meta」→ 告别「最多一条」守卫不弱化，`unfreeze` 才清零；`POST /conv/{cid}/stop-contact/confirm` + 会话头「已处理 · 归档并移除」确认框正文「归档并从筛选移除 · 不再自动回复」（只剩停联标的旧冻结会话也补了同一出口）。**C** `test_panel_tags_q31.py` 9 例（node 抽模板函数真跑 ×2 + 真库 ×5 + 路由 TestClient + 红线钉）。既有红非本线：`loadConnectModes` 未挂 window（接入方式线未提交 hunk）、legacy_blue / css_token_refs / `rps-po-empty` 旧债。 | A 分组 / B 生命周期 / C 门禁 / D 发前确认计数 | `发版对账_v1.0.85_Q31.md` | |
| **Q-32**（09-12 20:4x 追加，**美化线 · 待拍板**） | Composer 两层 + 翻译抽屉 + 空态 + （原 Q-31）头部瘦身 / `cs-band` 常驻 / 列表降噪——非 bug；Q-21 翻译与 Q-26 状态带已在 1.0.84 | 五视角 P1 | $240 | 待老板拍板 | | | `发版对账_v1.0.85_Q32.md` | |
| **Q-33**（09-12 20:4x 追加，**美化线 · 待拍板**） | 视觉令牌收口：字号 / 间距 / 圆角 / hex → 令牌 / emoji → uiIcon / 深色 AA / z-index——非 bug | 五视角 P2 | $180 | 待老板拍板 | | | `发版对账_v1.0.85_Q33.md` | |
| **Q-34**（09-12 20:4x 追加，**工程底座 · 待拍板**，独立周） | `unified_inbox.html` 拆五区 + 视觉门禁 + 长期红门禁清零——非 bug | 五视角 P3 | $200 | 待老板拍板（Q-30～Q-33 全收工后才开） | | | `发版对账_v1.0.86_Q34.md` | |
| **Q-35**（09-12 20:4x 追加；20:5x 缩量） | 相册批量上传逐文件可见失败 + 重试 + 修 logger 格式串（#316）；#306 只补坐席侧「相册没有匹配 · AI 已改口 · 去补标签」一行（AI 侧 `note_album_miss` / `album_miss_addendum` 已在库） | #316 4HK54G / #306 BPMEWX | $60 | **收工**（09-12 23:0x–09-13 00:3x；模板 / 词条已热更，`.py` 须 zhiliao 重启——**本线未重启**，攒 04:00 窗；坐席包随 R85） | B `8751d114` + A `c3931380` + C `1ae6c3dc`（`conv_state.py` 三 hunk 随 Q-30 `6ae59bdb` 先落）。**B** `persona_media_routes` 七个拒绝分支 → `MediaReject` handler 回 `{ok:false, reason, detail, limit_mb/ext/max_sec}`（`detail` 键保留）+ 每条拒绝落一行 `[pmedia] 上传拒绝 … reason=`（此前 raise 在成功日志之前＝服务端零痕迹，4HK54G「两张成功后无上传行」正是此形状，最像 iPhone `.HEIC` 走 `ext_not_allowed`）；全仓 AST 扫占位符≠实参 **3 处全在 `skill_manager.py`**（乱码吃掉格式串 / 第 4 参 / 把 `switched` 卷进字符串）→ 修 + 新门禁 `tests/test_logger_format_args.py`；复现 6×2MB 逐 POST 全过零格式化错误。**A** `pmaUpload` 逐文件 `{name,size,ok,existed,status,reason,detail}`，409 / deduped＝已存在不算失败，网络 `network:` / 超时 `timeout`；新 `#pma-up-result` 挂 `_pmaRender` 尾（修「结尾状态句被 pmaLoad 重绘抹掉」）：成功 / 已存在 / 失败数 + 展开明细（原因人话 `pma_fr_*`）+「重试失败项」只重发失败 File；词条 zh/en + 繁体 regen（只切本线 hunk）。**C** 新 `album_miss_marker`（KV，24h，命中即清）← `pick_registered_media` candidates=0 旁路写（Q-6 打分 / addendum 不动）→ `conv_state.compute` 只读进 `notes`（不进 sources 不改 state）→ `unified_inbox.html` `#cs-note` 复用 cs-band 琥珀零新 CSS「相册没有匹配『…』的图 · AI 已改口 · hh:mm」+「去相册补标签」→ `/personas?pid=&pma=1&filter=notrg`（personas 消费 → 相册页签缺触发词筛选）。门禁 3 新文件 33 例 + 既有 6 文件全绿；他线红 / 旧债只报告（落点表 §2） | A 逐文件 / B 服务端 + logger / C 坐席侧一行 | `发版对账_v1.0.85_Q35.md` | 4HK54G 真因本轮不可回溯（当时两端都不记），下次报障看「展开失败明细」+ 服务端拒绝行即可定案 |
| **Q-36**（09-12 20:4x 追加；20:5x 缩量） | `offer_media` 意图（客户提议发自己的图不被当索图拒；AI 自提议的 offer-accept 桥已在库）+ 入站图 caption 不夸大（FactGate observation + 出站量词软改）；群拟稿只复核不立项 | #313 2JK95C | $60 | 未开工 | | A offer_media / B caption / C 门禁 + 复核 | `发版对账_v1.0.85_Q36.md` | |
| **R84** | 发版 1.0.84 —— **单一版本**（D-Q11）：1.0.83 定格内容（此前未发公共）+ Q-27 + Q-21 + Q-22 + #304 + Q-26 A/B + conv-model-route 已提交部分 → 一个 clean 包；**发公共渠道 + 内测桥接 + 官网 + 播报** | #303 #298 #297 #299 #300 #295 #178 #272追加 #301 #296 #290 #286 #287 #288 #239 #304 #302（#289 引 `7c4aae3b`；#305 不标） | — | **已发（09-12 09:1x 公共渠道 + 内测渠道桥接同字节 + 官网 + 坐席三台 1.084 clean/public）** | 版本链 `06fee5b8`（1.0.84 / 1.084 / 繁体 17593 / changelog 9 条 / ui-build 20260912-1515 / 小智词条 ×8 + how-to ×8）→ 官网五值 `53b4010a` → 干净 worktree `D:\boundless-r80@06fee5b8` `dist:win:clean` **523,501,804 B** sha256 `cffef4fb…` → import_sweep 1259/0 绿、gate_sweep 19 红（18 同名旧债 + 1 条 Q-22 `cp.voice.*` 12 键缺繁体，overlay 已补不重打）→ 发版门槛 worktree 88 pytest + 16 node 全绿（报障群回放零出站 / DXAPAX / couple months / Q-27 三回放 / ja×STEVEN）→ 坐席三台 09:11–09:19 → publish 公共 + internal 桥接（同 sha256）→ `gate:content` 6/6 + `deploy -FromHead` → 16 单 fixed + 回访 19 条（#305 只说明 Q-29）+ 群播报 mid 1488 + 频道 165 | 未进包：Q-26 C–E / Q-28 / Q-29（主干已收工 `6239faaf`，须 zhiliao 重启）；117 后端半边仍等重启窗口；全量回归 139 红 / 27656 绿（1:16:40）→ 串行 109 红 vs `29b3a71a` 同集 98 红，差 11 条全非功能（mtime 伪红 / Q-22 旧钉预期静默换系统音 / Q-21 zh-tw 一等码 / 额度测英文稿撞 lang_mismatch / lang-plan 多读 store / mock 未收 tts_lang）→ 钉子另 commit，包不重打 | `发版对账_v1.0.84_发版.md` | |
| **R83** | 发版 1.0.83 —— **单一版本**（D-Q11）：1.0.82 全部 + Q-20 + Q-23 + Q-24 + Q-25 + 界面切语言根修 `510a9570` → 一个 clean 包；**老板要求不发公共渠道 / 不播报**，只装坐席三台 + 117 | #298 #303（未标 fixed）| — | **已装（09-12 05:5x 坐席三台 1.083 clean/public + 117 壳；公共渠道仍 1.0.82）** | 版本链 `29b3a71a`（1.0.83 / 1.083 / 繁体 17519 / changelog 6 条 / ui-build 20260912-0501 / 小智词条 +1 how-to +1）→ 干净 worktree `D:\boundless-r80@29b3a71a` → import_sweep 绿 / gate_sweep 25 红（18 同 R82 + 6 worker 崩连坐串行绿 + 1 真新红 MT 债表过期 → `4d805655` 扶正）/ regression 133 红 → 串行 102 红 vs 1.0.82 同集 98 红，差 4 条全非功能（mtime 伪红 / MT 债表 `4d805655` / 繁体多一孤键 / Q-23 有意变更钉子 `3677ace0`）→ 包 523,353,499 B sha256 `5134f5ec…8aa4` → 三台 push OK + 切语言活体探针三台一致 → 真值拷回 `5eca2306` | **断点 ①**：切包（05:02）后主干 05:06–06:02 又落 12 commit（#304 `73c8ab6b` / Q-27 A–D / Q-26 A–B / Q-22 A/C/D），落点表都自称 v1.0.83 **但不在包里**——老板 06:3x 定 **先不动，等 Q-21 / Q-22 收工后一起切 1.0.84**（1.0.83 = `29b3a71a` 定格）；**断点 ②**：117 壳=纯客户端，后端 = zhiliao，`restart_preflight` NO-GO 未重启，`admin.py/login_redirect.py` 半边等重启窗口（与 #304 / Q-22 / Q-27 / Q-26 攒同一批） | `发版对账_v1.0.83_发版.md` | |

## 老板拍板记录

- 09-09 07:4x：D-Q1～D-Q8 全部按建议生效（撤回 D-O4 班表默认开；额度默认关且永不限人工；见面政策默认永不；今天暂停摸底自动注入；proactive_topic 陪伴域 B 类默认开；1.0.79 = P 批 + Q-1～Q-6；P-6 顺延 1.0.80；情感引擎立 1.0.80 专项）。
- 09-09 09:0x：D-Q9（告警 magic link）/ D-Q10（ai.timeout 60）按建议生效 → Q-14。
- **09-09 14:1x：#270 客户流失（Mike 十小时被问 8 次后「Goodbye」）。截至 14:10 Q 批 14 行全部未开工（意向板只有成本对账线）。值守已代做 Q-0 A 之一：#270 回访 14:12 发出（暂停全部摸底目标 / Mike 切手动 / 手补画像 / 1.0.79 修法 / 验证口径）。D-Q6 修正：R79 触发条件 = Q-1 + Q-3 收工即发。开工顺序：Q-1 → Q-3 → Q-2 → Q-4 → Q-6 → Q-5 → Q-14 → Q-0（B–E）。**
- 09-09 19:0x（值守日清，`docs/值守_今日bug与未修复清单_2026-09-09.md`）：截至 19:00 只有 **Q-14 开了工**（A/B/C 三 commit `2dcdaeab` / `38707c79` / `c32de531` 落地，D katie / E magic link 在途；C 官网代码已提交未部署）；Q-1/Q-3 仍未开工，Q-0 B（我方实例班表关 + 摸底注入停，D-Q4「今天」）仍未做。库里 18 张开放单（P0 六张：#263 #264 #266 #267 #269 + #259 待发版），#270 库内仍 P2 与回访口径不一致待 Q-0 C 归位。14:17 后 bug 群零新报告；16:08–18:31 办公室出网抖动六次 + VPS 探针五次超时（非产品 bug，Q-14 D 判别日志的验证材料）。
- 09-10 10:3x（值守）：夜间零 commit，Q-1/Q-3 仍未开工。06:15 新单 **#271**（skuio vanessa×Chris：客户一句含 `sex` 的调情 → `shadow=adult` 直接 high + needs_human，客户 19 分钟无回应，界面不写类别）→ 追加 **Q-15**（`docs/指令_Q-15_成人内容分级与拦后软回应_2026-09-10.md`，Q-2 + Q-3 收工后开）；同报告 06:11 AI 再次滑向接受周末见面归 Q-2 不另立。**skuio 托管 AI 字符额度剩 13k、约 1 天用尽**（首启体验档 100k）——Q-0 A 追加：值守经官网 console 体验档补量 + 回访说明续额方式。开工顺序不变：Q-1 → Q-3 → R79 → Q-2 → Q-4 → Q-6 → Q-5 → Q-14 D/E → Q-15 → Q-0（B–E）。
- 09-11 12:5x（值守）：夜里 skuio 1.0.79 五张新单 #273–#277（30 份报告）。**#275 / #273 / #274 / #277①③ 已由 `4fdd238f`（06:13，裁剪器 CJK 估算根因 + 预算 6000→12000 + 人设段永不砍 + 能力自曝拦截 + 主链 deepseek-flash）修并随 1.0.80 上架公共 + internal 两渠道（09:23）——台账仍全 `new`、skuio 未收到回访**。未修两张 P0 追加指令：**Q-16** #276 发送语言按会话（根因 `_persistXlatePrefs` 把「发→X」写进 `__default__` 跨会话继承）、**Q-17** #277② 风控三级分级 + commitment 只评出站 + 摘标 30 分钟冷却 + 风控页公开（根因 `keyword_risk_hits` 对客户入站也跑 `detect_commitment`）。另：#272 额度用尽是否已补量无记录、wa `Bad MAC` 无人接。开工顺序：Q-16 → Q-17 → Q-0 第二轮（回访 #273–#277 + 台账标 fixed + #277② 拆单 + Bad MAC 立单 + 额度）→ R81。
- **09-11 14:3x：老板「我们不要内测版，只要一个版本，把所有的功能都进行提交」→ D-Q11：从 1.0.81 起 R 批只出 clean 包，坐席机与内测机装同一个包；内测渠道指针只做同字节桥接，fleet / skuio 全部显示 clean/public 后停更 `/downloads/internal/`；`dist:win`（smart/internal）不再是发版产物。** R81 16:0x 已按此发出（见 R81 行）。1.0.80 两包同号的坑（坐席机上 1.0.80 不含 P0 修复）由 R81 重推坐席收掉。
- 09-11 17:2x（值守）：skuio 15:09 升 1.0.80、16:33 升 1.0.81，两小时 29 份报告 → **#280–#294 十五张新单**（#280–#283 为启动对照单无动作）。两条「!!! 重点修复」：**#292 族**（全自动被机制掐停：`cum` 印式英语误判成人 → 软回应 + 需人工 + 全自动停 5 分钟；坐席手发后 60s 让位窗过稿被丢、绿灯全自动却不发、无「让 AI 接回」）→ **Q-18**；**#294 / #272 追加**（画像 AI 推断把我方日语稿幻觉成客户职业 / 「我住在去嵐山散步」写进居住地，年龄槽画长句，1/10 把幻觉计已采集）→ **Q-19**（用户要求今日热修）。另立 **Q-20**（#178 追加：对方怎么叫我=Alicia 被 AI 当面否认「It's Mizuki, not Alicia」；空值沿用人设爱称不合理）、**Q-21**（#289 翻译「我的消息」下拉看不见——先核是否 Q-16 D 回归；#290 缺粤语；#286 字体）、**Q-22**（#287 #288 克隆声不支持日语静默换微软男声发出）。开工顺序：Q-18 → Q-19 → Q-20 → Q-21 → Q-22 → R82（单一版本）。
- 09-12 02:2x（值守，`docs/分析_1.0.81首日问题归类与工作台三视角方案_2026-09-12.md`）：R82 20:3x 已发 1.0.82（Q-18 / Q-19 + 用户管理 P0 + xlate-pop + 四批提交）；其后新单 #295–#301 + 追加。**事故**：报障群 mid 1445 / 1454 / 1459 / 1461 是 skuio 实例账号 7331682688 对我方值守消息自动发的成人软回应（其群会话 mode=review），Q-15 软回应链绕过档位与场景；工作树里 21:01 有一份未提交的止血（`blocks_adult_outbound` 群 / 报障群 skip + 中文词表消歧）**不在 1.0.82**。追加 **Q-23**（事故：单一闸门 + 场景维度）/ **Q-24**（#298 P0 Messenger 代发链）/ **Q-25**（事实门二期，Y39D8U 在 1.0.82 复现 + #295 + 记忆主体）/ **Q-20 扩 D**（刚发图认领 + 自曝黑名单）。开工顺序：**Q-23 → Q-24 ∥ Q-25 → Q-20 → R83（1.0.83 单一版本，门槛 = Q-23 + Q-24 + Q-25 收工）→ Q-21 → Q-22 → Q-26（状态带 + 设置收口，指令待写）→ R84**。
- 09-12 05:0x（#304 语音）：老板直问钧机「普通话跳粤语 / 不能克隆」→ 读 GWJ2RZ 逐行核因并修，`73c8ab6b` 一个 commit（粤语特征字拆独占/共用、`_cer_cjk` 繁简归一、入站转写统一简体、桌面包 collect-all opencc；语音·语种 15 套件 460 绿，2 红 HEAD 既有），落点 `发版对账_v1.0.83_304语音.md`。**要随 R83 出包**（`.py` + `build_backend.py` 变更 → 后端全量重打）。Q-22 D 段 #304 假设已划掉，改为第二道保险。
- 09-12 04:5x（值守）：Q-23 / Q-24 / Q-25 / Q-20 四线 03:02–03:57 全部收工在 HEAD，**未发版**（渠道仍 1.0.82）。新单 #302（jun / B990：`lang_unknown` → L1 人审不可见，用户误以为人设未选）、#303（Q-23 立）、#304（jun：十翼会话语音突然说广东话）；#301 追加 AFD2CD 是产品原则级要求（五类硬拦其余不停自动、摘标 / 切全自动后旧 hold 不得再 forced=L1）；skuio 03:23 十二份「1.0.82 复验未修」核日志多为补传旧证据（升级后无 agent_sent abort / 无 6000 预算，画像已在 drop unanchored），真开放项归 Q-21 / Q-22。追加 **Q-27**（风控最小硬拦集 + hold 生命周期，P0）/ **Q-26**（状态带 + 设置收口）/ **Q-28**（WA Bad MAC）；**Q-21 修订**（#289 已由 `7c4aae3b` 随 1.0.82 修，划掉；加 #302 `lang_unknown` 处置 + `conv_lang_plan`）；**Q-22 修订**（加 #304 语音语种一致性 + #239 登记结果面板）。开工顺序：**R83（现在，带 Q-20/23/24/25）→ Q-27 ∥ Q-21 ∥ Q-22 ∥ Q-28 → Q-26 → R84 → Q-0 第三轮（对照单归档 + 老单 released + 回访）**。
- 09-12 08:4x（值守）：R83 05:02 定格 `29b3a71a`（Q-20/23/24/25 + 切语言根修），**只装坐席三台 + 117 壳，未发公共渠道**（老板 04:4x `r83_now` + 06:55 断点①定「1.0.83 定格，其余随 1.0.84」）→ **skuio 仍在 1.0.82，所有 P0 修复对客户尚未生效**。之后 Q-27 / Q-21 / Q-22 / #304（`73c8ab6b`）07:05 前全部收工在 HEAD；Q-26 A/B 已提交（`6d9356ff` `4412dffb`），C/D/E 在工作树未提交（reply_settings / personas / 路由 / i18n，最后动 06:10）；Q-28 未开工。新单 **#305**（jun：客户说过当地时间 AI 又问白天还是晚上）→ 追加 **Q-29**。库里 30 张开放单没有一张被标 fixed（R83 未发版不标）。**下一步：R84 现在开**（基点 HEAD，Q-26 未提交部分自然不进包，1.0.84 = Q-27 + Q-21 + Q-22 + #304 + Q-26 A/B + conv-model-route + 1.0.83 内容；发公共 + 桥接 + 坐席 + 台账 #303 #298 #297 #299 #300 #295 #178 #272追加 #301 #296 #290 #286 #287 #288 #239 #304 #302 标 fixed + 回访）→ Q-26 C–E ∥ Q-28 ∥ Q-29 → R85。
- 09-12 09:3x（R84）：1.0.84 单包按 D-Q11 发出（见 R84 行 / `发版对账_v1.0.84_发版.md`）。公共 + 内测桥接同字节 + 官网 + 坐席三台 1.084 clean/public + 16 单 fixed + 回访 19 条（#305 已立 Q-29 不标修复）+ 群 mid 1488 / 频道 165。下一步：Q-26 C–E ∥ Q-28 ∥ Q-29 装载（zhiliao 重启窗口）→ R85。
- 09-12 20:4x（值守）：12:39 后无新 R；渠道仍 **1.0.84**；HEAD 新增接力记忆二～四期（`c8b043a3` `a52b7e3d` `817c0148`，人工发图进 AI 记忆——#312 / #314 的「AI 不接人工发的图」在此已修但**未发版**）+ 上下文深度四档 `0b606199`。新单 **#312 #313 #314 #315 #316 #317**：#312 / #314 真因是 jun 自测账号 `8852939166` 在「永不自动回复」名单（`peer_bot_guard skip reason=colleague`），不是识图；#315 是 1.0.84 启动对照日志单（信息单，不立项）。追加 **Q-30～Q-36** 七条指令（Q-30 P0：认领体系退出单人端 + 先查 abort_ledger 定真闸 + 档位一真相 + 人话表）。Q-26 C–E 仍在工作树未提交（`reply_settings_routes.py` +42 / `reply_settings.html` +358 / `automation_risk_q26.py` 新，最后动 06:10）；Q-28 未开工。**开工顺序建议**：Q-30 → Q-35 / Q-36 / Q-28（并行）→ Q-26 C–E 接手 → R85（须 zhiliao 重启装载 Q-29 / 接力记忆 `.py`）→ Q-31 / Q-32 / Q-33（同文件顺手）→ R86 → Q-34 独立周。
- 09-12 20:5x（值守·更正）：老板指出「很多已修复」，按 HEAD 逐条核对后**首版分析有错**：① 「selectChat 无条件 renewClaim」不成立——`renewClaim` 首行 `if(!MULTI_SEAT) return`（B13），单坐席隐认领 UI 五处守卫全在库，真正的洞只有 `resolve_seat_mode` 无显式配置时 **≥2 账号即 multi** 把守卫全绕过；② 原因码人话与回落（`_handoffTip` / `_L1R_KEYS`）、状态带六态（Q-26 A/B）、翻译目录（Q-21）、相册无命中 AI 改口（`note_album_miss`）、AI 自提议发图桥（`detect_media_offer`）、人工发图进记忆（接力记忆，HEAD 待发）都已在库；③ #312 / #314 不是 bug（jun 自测账号在「永不自动回复」名单）；#315 信息单；群拟稿是 skuio 侧配置 / 产品口径。**结果**：Q-30 $200→$100、Q-31 缩为 #317 单项 $60、Q-35 $60、Q-36 $60；Q-32 / Q-33 / Q-34 标「美化线 · 待拍板」不算修复指令。真 bug 只剩四条：Q-30 / Q-31(#317) / Q-35(#316) / Q-36(#313) + 老线 Q-28(#279) / Q-26 C–E。
- 09-12 21:2x（值守）：老板拍板回访 jun → #312 / #314 已回访（报障群两条，`duty_reply` 21:19 / 21:20），台账 new → **confirmed**（不是 bug：对端 `8852939166` 是自家账号按设计不自动回复；请换账号复测；顺带告知下一版状态带会明说 + 人工发图进 AI 记忆已在库待发）。`duty_reply` 红线提醒：正文不能出现配置键名（`peer_bot_guard` 被拦），改写成人话即可。#317 又来一份 PUUWJB 已自动挂到 #317。
- 09-12 22:3x（值守，老板「给我指令，开新对话优化修复」）：新单 **#318 #319**（21:25 两包，skuio 1.0.84）。**#318 VV7BRY**「15:22 主动发 It's 3am here / Marina 无出处」→ HEAD 核：15:22 上海 = 03:22 纽约，「3am」是 `_persona_clock_note` 人设钟批注如实写的，错在**发送时机**——`companion_proactive` 安静时段闸只认服务器钟或显式核实过的客户钟（08-19 事故后 replace-only），`user_clock_resolver` 没读 Q-29 `resolve_peer_tz`（New York confirmed）→ 回落服务器钟放行；人设钟只进 prompt 不进闸 → **新出 Q-37**（$60：三源接线 / 人设夜间不主动 / 文本尺 / 门禁）；「Marina」= 水边房子照片的画面推断进了记忆再被开场引用 → **Q-36 B** 扩到记忆抽取与主动措辞。**#319 UG6KJG**「01:48 `resume by=mode_select` 后同句连发两条」→ HEAD 核：Q-18 B deferred 载荷在 `_retry_queue`、Q-18 C `resume_agent_yield` 置 `next_ts=0` 放行，同会话若还有一条新稿则两条各投；B41 `_deliver_once.claim` 与 dup_guard / fresh_guard 都对重试项（`_attempt>0`）豁免、且两稿 `draft_id` 不同 → 同文两条都出门 → **Q-30 E**（$100→$140）。两份报告日志窗（18:23–21:23）都没盖到事发时刻，两条线开工先用 skuio 数据坐实（`outreach_log batch_id` / `messages` 两条出站的 `draft_id`）。zhiliao **21:47 又重启一次**（composer 模型 / 模式双面板 `82a00fa0` + piggyback ASR P0 等未提交 `.py`）。**开工顺序（本轮给老板的粘贴块）**：Q-30（P0，含 E）→ Q-35 / Q-36 / Q-31 / Q-28 / Q-37 并行 → Q-26 C–E 接手 → R85；Q-32 / Q-33 / Q-34 美化线待拍板不发粘贴块。
- 09-12 21:1x（值守·接手）：值守链活（`DutyWatchTick` 每 2 min，20:55 / 20:57 exit 0）；20:23 后两份新报告、**无新 NEW TICKET**（max 仍 #317）。**FJ8YU7**（20:18，自动挂 #301）：风控分级卡「人设覆写」下拉选了人设也无行可调 → HEAD 核对 `rpsRkRender` / `risk_grader.CATEGORIES`：只 7 类可覆写（request_* ×4 / complaint / privacy / narrative），8/15 行锁死，adult 行文案仍是 #301 那条死链 → **归 Q-26 C-2**（保留加说明 + 锁死行灰显 + 深链，或移除下拉迁人设页，二选一）+ D 门禁加键面扫描；#301 台账 `fixed` 只对 Q-27 部分成立，死链 + 下拉两项随 Q-26 C–E 出包后再回访。**PUUWJB**（21:01，自动按「90 分钟同报障人同实体」误挂 #317）：顶栏「发前确认 47 / 待处理 6」计数无限堆叠 → HEAD 核对：药丸 = `risk-summary` L1+L3+L4 全部 pending 不看稿龄；`stale_approve_hours` 24h 只禁原样发不减计数；`auto_expire_hours` 出厂 0 关；Messenger 边车 backfill 推送**不带** `backfill` 标（72h 内历史消息按新入站起 L1 稿 = 「登录后每会话一条」候选根因）→ **归 Q-31 D**（$60→$100：actionable 分桶复用 `approve_block_reason` / auto_expire 出厂 48h 走 release_defaults 五步 / 审批台清空超龄稿 / 边车推送带 backfill）。**更正交接摘要**：zhiliao **19:47:40 已重启**（reason「四档上下文 + piggyback 接力记忆二～四期 / conv-model-route / yue-route / Q-26 / Q-21」，`boot_20260912_194720` 0 ERROR，`/api/lang-catalog` `/api/unified-inbox/conv-state` 探针 401=已装载）→ Q-29 / 接力记忆 `.py` **已装载**，R85 前置只剩现网核 Q-29 `peer_time` 行（等有已确认城市的陪伴会话触发）；同一次重启也把工作树未提交的 Q-26 C–E / `conv_route` / `vendor_params` / `bug_intake` 半成品装进了 zhiliao（zhiliao ≠ 安装包，发版仍以干净 worktree 为准）。意向板 21:0x 新 ACTIVE：ASR P0（`voice_transcriber` / `autodraft_helpers` 语音块 / account_routes ingest / `asr_server.py`）、conv-model-route picker 续（`developer.html` / `developer_page.py` / `unified_inbox_setup_routes.py` / 两测试）——值守不碰。**下一步不变**：Q-30（P0）→ Q-35 / Q-36 / Q-31（含 D）/ Q-28 并行 → Q-26 C–E 接手 → R85（基点 HEAD；回归红对照 1.0.84 release commit `06fee5b8`；回访一并带 #312 #314「换账号测」+ #301 FJ8YU7 + PUUWJB 说明）。
- 09-11 20:3x（R82）：1.0.82 单包按 D-Q11 发出（见 R82 行 / `发版对账_v1.0.82_发版.md`）。开工顺序表里 Q-18 → Q-19 → R82 已走完；Q-20 / Q-21 / Q-22 未开工，随下一版。

## R79 开新对话粘贴（Q-1～Q-6 六行全部「收工」后）

```
读 docs/Q批_进度总账.md（Q-1～Q-6 六行须全部收工）与 docs/P批_进度总账.md（P-1～P-5 收工），照 docs/发版对账_v1.0.78_发版.md 的全链发 1.0.79：agent_probe 确认无 P/Q 批线在途 → restart_preflight → zhiliao 装载 + 活体核对（Q-1 同会话已答不再问回放 / Q-2 邀约回放 / Q-3 切手动取消在途 / P-2 重装登录零自动草稿 / P-3+Q-6 要照片三轮 / Q-4 netstat 与班表默认关）→ 全量回归 + 门禁（含 outbound_style_gate、P-1 引用锚点门禁、P-4 打包清单门禁、Q 批新增五项）→ bump 1.0.79 → 干净 worktree 三包 → publish 三渠道 + 官网 → 坐席三台 → release commit 钉真值 → 台账：#259 #252 #254 #260 #263 #264 #267 #269 标 fixed，回访文案取各线落点表「回访」节经 duty_reply 发出（每条带工单号，只给版本号），群播报一条「行为止血版」。写 docs/发版对账_v1.0.79_发版.md 并改 Q 批总账 R79 行。
```

## 全批收口

- [ ] Q-0 今天完成（回访已发 / 实例配置已改 / 拆单完成）
- [x] Q-1～Q-6 收工 → R79 发 1.0.79（09-10 17:57–18:09 三渠道 + 官网 + 坐席三台；`docs/发版对账_v1.0.79_发版.md`）
- [x] Q-8 / Q-10（+ Q-15）收工于 R79 基点 `4ba5e0bc`（15:14）之后 → **随 1.0.80**（R79 §4.2：改基点须重装载 + 重跑回归 + 重打三包，未并）
- [ ] Q-12 / Q-13 → 1.0.80

## 独立线（不在 Q 批编号内；09-10 18:35 老板：微信 / QQ / TikTok 三条未完成线各给续做指令，能入库的入库并打包测试）

| 线 | 指令 | 现状（09-10 19:xx） | 去向 |
|---|---|---|---|
| **WX** 微信（实施97） | `docs/指令_微信线_续做_2026-09-10.md` | 代码全部入库 **`f00c4d00`**（96 文件 +12842/−70，共享文件只带本线 hunk、index-only）+ **`183e2322`**（两处门禁红 + ui-build 戳）；全部开关默认关；入库树导入扫雷 1220/0、本线 136 绿、gate_sweep 本线旧红 9 项全消；打包测试 D:\boundless-wx@183e2322：uild:backend 后端冒烟全过 → dist:win 预发门禁 76/16/81/303 + after-pack 22 项 + 内测包冒烟全过，smart 593,800,745 B（R79 +546 KB），PYZ 含 wechat_pc/wechat_kf/wecom_sso/relay_client/kf_window_guard，connect_guide.html 在包；**未发布**（版本号仍 1.0.79，只作测试）。**09-10 20:2x–21:4x 续做 A/B/C（`docs/发版对账_v1.0.80_WX.md`）**：A `b156daa7` 11 个本线测试 + `scripts/import_sweep.py` 进 gate_sweep 固定清单，并修了 gate_sweep.ps1 无 BOM 被 PS5.1 按 GBK 吞行的老 bug（107 条清单此前只跑 50 条，96→~40）；A2 `69efc665` 清单跑全后暴露的本线 4 处模板旧红（kfTransferToHuman 未挂 window 等）归零，第二次 sweep 107 文件 1603 绿 / 18 红全非本线、import sweep 1226/0；B `4210e7cd` prod_edge_watchdog 探 relay healthz（独立 2-strike 告警）+ relay 24h 设备清理 + `devices_stale` + relay/tests 7 绿 + `deploy_relay -Status`，已上 VPS（active）；C 联调残留 dry-run 清单 6 条（portproxy/防火墙/`D:\wxpc_dev`/计划任务=无/隧道=无/重复行 rowid 13+16 主键）**只列未执行** | **1.0.80 进包**；C 段等主人拍板后执行；D/E/F 等企微凭证 |
| **QQ** route-1（Milky 边车） | `docs/指令_QQ线_续做_2026-09-10.md` → 落点 `docs/发版对账_v1.0.80_QQ.md` | 09-10 21:4x：**A 诚实标位 `54af3f00`**（注册表 `driver_state` 三态、qq=mock → JSON / 工作台 QQ 组头·账号卡·连接面板琥珀标「边车已装 · 驱动未接入（演示态）」/ 事实卡「预览中（尚不能真收发）」；`test_platform_registry` +1）；**B 真驱动移植 `dbcc314f`**（`ntq/qqnt-driver.js` + 版本表 `qqnt-versions.js` + 进程内 agent `qqnt-agent.cjs` + 管道 RPC + 段转换，全在 GPL 目录；`driver-contract.test.js` mock/qqnt 同契约表 + `ipc.test.js` 23/23；NOTICE 逐文件来源；表外版本 / 用户自装 QQ / 无 QQ 三种情形均回退 mock 并标 `driver_reason`）；共享文件全部 hunk 级暂存、他人 hunk 未卷入。**C 未开始**（缺测试号 + 验证机 + 72 h 窗口，真机行为零验证，版本表 `verified` 皆空）；D 按指令不加 REQUIRED；E 等 C | A 随 1.0.80 进包（包内如实演示态）；B 代码随包但不 REQUIRED；C 需老板给测试号与验证机 → 72 h 通过后 D（REQUIRED）→ E（`/help/onboarding/qq` 建页） |
| **TK** TikTok（TK-1 + TK-3） | `docs/指令_TikTok线_续做_2026-09-10.md` → TK-3 `docs/指令_TK-3_TikTok个人号私信_真机脑手分离与网页边车_2026-09-10.md` | TK-1 C/D/E 已入库（`16fe54f6`/`36f1e53b`/`3a6b485f`）。**09-10 21:54–夜 TK-3 个人号线**：①-B `3ffb71c2` 桥私信通道 / B5/E1 `073c14eb` 个人号策略键+编排器传 mode / B6 `c23af79a` 闸门六语人话 / ①-A `08f62a80` huoke `reply_engine=chengjie`（local 零 diff）/ E2 `cb413377` 接入页页签+小智三行口径。**09-11 凌晨第二阶段**：P1 `a78eb6a2` 独立认领轮询器 / P2 `c3db4f54` 真机联调探针+跨引擎契约 / P3 `d038263b` 接入页真机页签 UI（一键开桥+绑定） / E3 `d08bdbf9` 来源徽标+对方未回 / E5 `27a8442c` 运维卡（真机离线进链路卡+巡检+status.ops） / ②-A `f2b18bcb` 网页边车骨架 assistOnly。全部默认关，`implemented=False`，不宣称官方个人号私信。未做 ②-B。落点 `docs/发版对账_v1.0.80_TK3.md` | **1.0.80 可进包**（`tiktok.enabled` / `shop.enabled` / `huoke_bridge.enabled` / `platform_login.tiktok.web_enabled` / huoke `reply_engine=local` 皆默认关）；真机联调 SOP：接入页「个人号·真机」页签开桥+绑定 → huoke `tiktok.yaml reply_engine: chengjie` + endpoint/token/timezone → `python scripts/tiktok_chengjie_probe.py --bind --roundtrip` 全绿 → 对方先发一条 → 工作台回复 → ≤2 分钟真机发出；②-A 边车选择器等可用区真号核对 |
