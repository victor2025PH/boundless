# 发版对账 v1.0.76 · M-4 工作台第三轮（2026-09-06 → 07）

> 指令：`docs/指令_M-4_工作台第三轮_2026-09-06.md`。预算 $150，实花 ≈ $95。四件（A #222 / B #221 / D #206 补证 / C #230）按 A → B → D → C 全部完成，**一件一个 commit**（B 另有一个修正 commit，见 §「提交归属异常」）。
> 模板 / CSS / i18n / panel-manifest = 热更新即上生产（已双戳：`unified-inbox.css?v=20260906f`、`ui-build 20260907-0105` notice）。
> `.py` 四处（`src/inbox/unread_aggregate.py` / `src/inbox/health_watchdog.py` / `src/web/routes/unified_inbox_read_routes.py` / `src/web/routes/unified_inbox_account_routes.py`）**须重启装载**，装载前走 `scripts\restart_preflight.ps1`。装载后首个看门狗 tick 会跑一次 `[buried] startup_sweep`（见 A③）。
> 主题窗口级钉子不变量（`workspace_base.html` L22–34 / `unified_inbox.html` L7–19）**未推翻**：#221 的修法是「站内导航原样携带当前 `?theme=` 与 `lang=`」，不是持久化（`tests/test_ws_nav_theme_safe_221.py` 钉住 wsNav 块不写 localStorage）。

## 落点表

| 项 | 单 | 修什么 | 落点 | commit | 状态 |
|---|---|---|---|---|---|
| A | #222 幽灵未读第三轮（口径统一 + 群/频道单列 + 存量清理） | ① **口径二选一 → 选「角标 = 当前列表 scope 内能点开的未读」+ 悬浮明细**：`unread_aggregate` 私聊 / 群 / 频道三桶互补（legacy 负号键 / LINE `:group:` 归群桶，与 `store_row_to_chat → infer_chat_type` 对同一行判定一致），`unread_maps` / `unread_conversations` 加 `scope`，新增 `unread_breakdown`（private / group / channel / archived 四桶，一次 GROUP BY，全走 `_LISTABLE` 闸）；`/chats` 全局响应带 `unread_breakdown_by_account` + `ai_skip_groups`；`/account-unread?scope=` 与角标同一份 WHERE。前端：`_scopedAcctUnread` 群视图读群桶、私聊视图读 private（＝原 `unread_by_account`，主徽标口径不变）；角标悬浮「角标 = 私聊里能点开的未读数 / 另有：群 N / 频道 N / 归档 N / 点开看是哪几条」；浮层标题带视图名、下方一行「另有 …」。② **群/频道**：永不进主徽标（不变），群/频道视图列表上方说明条「AI 不处理群 / 频道消息（只收不回），这里的未读不计入账号角标」——只在服务端 `inbox.auto_draft.skip_group_chats=true` 时出，不猜。③ **存量一次性清理**：看门狗 `_startup_hygiene` 新增 `sweep_buried_unread(min_idle_sec=72h, manual_only=True)`——「人工归档 + 无入站 > 72h」的被埋未读推已读水位（与打开会话同一机制，永不回弹；数据不删），日志 `[buried] startup_sweep cleared N`；<72h 与自动归档的留给 L-3 B 横幅；配置 `health_watchdog.buried_conv_remind.sweep_hours`（默认 72，0=关）。横幅加第二动作「全部标已读」→ `POST /api/unified-inbox/buried-mark-read`（按当前平台/账号视角，同口径，日志 `[buried] banner_mark_read cleared N`）。④ **被埋计数口径统一**：看门狗 `_check_buried_conversations` 与 `/api/admin/buried-conversations` 优先走 `unread_aggregate.buried_conversations`（私聊 + 有效未读 + 可见消息 + archived=1，与横幅/主徽标同一 WHERE），旧口径 raw `unread>=1` 把 skip_groups 堆积的群也报成「客户在等」；旧 store / 测试桩回落 `list_buried_archived`。⑤ 其余建议核实：归档点开触发已读 **已在**（J-4 A 结论）；角标点击定位 **已在**（#159 浮层逐条直达）；孤儿清理 **已在**（L-3 B `purge_orphan_unread`）；新入站自动取消归档 **已在**（skuio 已复验）。 | `src/inbox/unread_aggregate.py`（+202）· `src/inbox/health_watchdog.py`（`_startup_hygiene` + `_buried_sweep_hours` + `_check_buried_conversations` 取数口）· `src/web/routes/unified_inbox_read_routes.py`（`_ai_skip_groups` / 四桶 / scope / `buried-mark-read`）· `src/web/routes/unified_inbox_account_routes.py`（`/api/admin/buried-conversations` 取数口，+9 行）· `unified_inbox.html`（角标口径：状态变量 + `_accountsForPlatform` 种子 + `_acUnreadBadge` 悬浮 + `_openAcctUnreadPop` scope；被埋存量清理入口：`_renderBuriedHint` 第二按钮 + `_markBuriedRead` + `#scope-note`）· `unified-inbox.css` · i18n `inbox.buried.read_*` / `inbox.acct.bd_*` / `inbox.scope.ai_skip_note*` zh+en · `tests/test_admin_route_inventory.py` 登记 | `b9ea59f3` | .py 待装载；模板/CSS 已热更 |
| B | #221 顶栏胶囊 / 主题 / 溢出 / 徽标（D-M8） | ① **wsNav 助手**（`workspace_base.html` 头部，全工作台页继承）：`wsNavUrl(url)` 只补当前 URL 里有、目标里没有的 `theme` / `lang`（外链 / `javascript:` / `#` 锚不动）；`wsNav(url)` = `location.href = wsNavUrl(url)`；**window 捕获相位**同源 `<a href>` 改写（先于 `_win_unique` 的 document 捕获去重拦截器，故 `__wsGoHome` 读到的已是带 theme 的地址）；`__wsGoHome` 包一层补默认 url（`_win_unique.html` 未动）。模板内 **30 处**裸 `location.href='/…'` 全部改走 `wsNav`（`__wsGoHome` 守卫原样保留；`test_workspace_home_nav_dedup` 的裸跳正则同时抓 `wsNav('/workspace')`）。② **「自动 (N)」胶囊页内筛选**：`onclick → window.wsAutoPillClick`——收件箱页存在 `window.wsFilterAutoConversations()`（**接口名，行为定义归 M-2 E**，见「交出去」）即调用、不整页重载；否则 `__wsGoHome` 去重 + `wsNav('/workspace')`；`href` 保留给 CmdK 采集/无 JS 兜底；悬浮改「…点击筛出全自动会话，不刷新页面」（计数口径仍是「本页打开以来 AI 自动处理 N 条」，若 M-2 E 要改成「N 个会话处于全自动」在 `_l2AutoBump` 处改）。③ **徽标改回「旗舰版」**：正文 `mb_plan_<plan>`，`plan_override` 的「（厂商自营）」进 `title` / `data-plan-full`（`feature_gate.badge_label_key` 与授权卡口径不变）；加皇冠图标态。同时闭 **L-4 待老板②**：有 override 的内测机徽标不受影响（仍显「旗舰版」）。④ **收缩规则**：行动药丸组 `.ws-pills` 容器 ≤1280 先让位（`min-width:0; overflow:hidden`）→ ≤1180 药丸只留图标（既有）→ ≤1100 徽标文字→图标 → ≤1024 用户名收起；`.ws-user-btn{min-width:34px}` + `.ws-user-wrap{flex-shrink:0}`——铃铛 / 徽标 / 头像不再被推出右缘。**主题开关**：网页顶栏本无独立主题开关元素，三档分段控件（跟随系统 / 亮色 / 暗色）早已在头像下拉（`#ws-theme-seg`，与 全部外观设置 / 告警通知 / 语言 / 求助·发诊断 同列）——7E3XKR 所述「被挤出的主题开关」实为右侧元素串整体溢出，收缩规则落地即闭；壳标题栏 ⋯ 菜单（关于 / 上传诊断 / 检查更新）属 `desktop/`，未加主题项（不在本条范围）。 | `workspace_base.html`（头部 wsNav 块 +72 行 / 药丸组容器 / 徽标 / 收缩 CSS / `wsAutoPillClick` / 30 处 wsNav 替换）· i18n `base.pill.l2auto_t` / `l2auto_dyn`（workspace_shell）· `tests/test_workspace_home_nav_dedup.py`（正则） | `898715ac` + 修正 `4358ca4b` | 模板已热更（无 .py） |
| D | #206 补证 右侧「会话工具」整块删除（D-M7） | ① 工具箱「会话工具」组的 `#conv-ops`「会话」卡（标签行 / 搁置 / 归档 / 删除 / 认领 / 全自动状态卡）**整块删除**——标签 / 归档 / 删除与会话行右键 · ⋯ 菜单（`_convCtxOpenForKey`：置顶 / 标已读 / 认领 / 搁置 / 打标签 + × 直摘 / 归档 / 回收站 / 清空聊天记录 / 删除会话）完全重复；两个「删除」语义不明（面板裸「删除」没了，行菜单两项各有明确名字：**清空聊天记录** / **删除会话**）；「+加标签」下拉被卡体裁切（标签编辑早已是浮层，遮挡随之消失）；折叠区空壳（用户确认）。② **「全自动运行中」状态卡迁 AI 状态弹层**：`#auto-safety-bar`（今日自动发 / 拦截 · 记录 / 参数 / 暂停）挪到 `#ai-diag-overlay` 顶部，id 不变（`_syncAutoUi` / `_loadAutoStats` / `pauseAutoMode` 零改动），`openAiDiag` 打开即按档位刷显隐与统计。③ 认领按钮 `#claim-hdr-btn` 回 `.chat-header-acts`（`ws_multi_seat` 门不变）。④ 工具箱只留 翻译 / 语音克隆发送 / 人设绑定 / AI 生成图片（M-5 C 处置）/ 养号（M-5 B）；组标题「会话工具」→「选中会话后可用」，空态说明书 s2 指路行菜单。⑤ `panel-manifest.js`（双树同步）convops `surfaces ["web","app"] → ["app"]`（壳右栏无行菜单，app 侧 `cp-conv-ops` 不动）。 | `unified_inbox.html`（会话工具面板删除：`#conv-ops` 段 −32 行 / 头部 +认领 / 弹层 +状态卡 / `openAiDiag` +1）· `shared/copilot/panel-manifest.js` + 镜像 `desktop/renderer/shared/copilot/panel-manifest.js` · i18n `inbox.cpg.tools.session` / `s2` zh+en | `198d1e17` | 模板/清单已热更（无 .py） |
| C | #230 多选专用工具条 | ① 根因：`toggleBatchMode` 把 26px `icon-only` 多选按钮的 `textContent` 换成「✕ 退出多选」→ 折成三行。改为图标不动、只切 `active` / `title`。② 进入多选后标题行整体切换为工具条 **「已选 N 项 ｜ 归档 · 标签 · 删除 ｜ ✕」**（`.list-title.batch-on`：标题 / 会话·联系人分段 / 值守胶囊 / 多选图标隐去），按钮 `nowrap + flex:none`，300px 最窄侧栏（≈240px 内容）不折行；0 选中时动作按钮 disabled；底部 `#batch-action-bar` 保留次级动作（取消归档 / 搁置 / 分配）。③ 新增 `batchDeletePrompt`：与右键菜单「删除会话」同端点 `/api/unified-inbox/conversations/delete`、同门 `can_delete_conv`（服务端 msg-ops meta 不允许则按钮不出现）、一次确认逐条删除、成功即从列表消失、部分失败如实回执。 | `unified_inbox.html`（多选工具条：标题行 markup + `toggleBatchMode` / `_updateBabCount` / `batchDeletePrompt`）· `unified-inbox.css`（`.list-batch-bar`）· i18n `inbox.batch.exit_t/archive_t/tag_t/del_t/del_confirm/deleted_n/deleted_part` zh+en | `dfa46687` | 模板/CSS 已热更（无 .py） |

## 提交归属异常（共享工作树，一次误卷入，已修正）

| commit | 应属 | 实际 | 卷入内容 | 处置 |
|---|---|---|---|---|
| `898715ac` fix(workspace): #221（M-4 B） | **M-4** | 额外含 **M-3 C 尚未提交的两处 hunk**（`unified_inbox.html` L13709 `_mediaQueues` 按会话隔离 / L13739 `_stageMedia` 归属钉死，#228） | 我对共享热区文件用了 `git add <file>` 而非 `stage_hunks`（B 提交时该文件本只该有我 2 行） | **`4358ca4b` revert(workspace)**：只在 index 里把该文件恢复为「`7899cc9a` 版本 + M-4 的 2 行」，工作树未动 → M-3 的改动重新显示为未提交，随后由 M-3 自己按 #228 提交（`b80a3907` 前后已入树）。不改写历史。M-4 之后对 `unified_inbox.html` / `inbox_workspace.py` 的 D / C 提交一律 `stage_hunks --mine/--theirs --apply`（D：12 hunk 挑 5，排除 M-2 B `_acPickModeCtl` / M-3 D；C：4 hunk 全为本线） |
| `b9ea59f3` / `898715ac` | — | `ui-build.txt` 两次改写时**误丢第二行** `severity=notice` | 我用 Write 覆盖了整文件 | `198d1e17` 起恢复两行形态；缺省即 notice，期间无行为差异 |

## 验收口径（交 M-6 回访）

- **#222**：「角标数 = 列表里能点开的未读数；群/频道单列；旧的被埋未读已清」——skuio 机装载后：steven 号角标悬浮显示「角标 = 私聊里能点开的未读数 / 另有：群 N / 频道 N / 归档 N」，点角标浮层条数 == 角标数、每条可点开；切到「群组」视图角标改读群未读且列表上方有灰色说明条「AI 不处理群 / 频道消息…」；装载日志有 `[buried] startup_sweep cleared N`（人工归档且 >72h 无入站的那批，100h 的 9 个应归零）；<72h 的仍在琥珀横幅，横幅现有两键「取消归档这 N 条」「全部标已读」；看门狗「被埋会话：」WARNING 的 N 与横幅 N 一致（不再把群算进去）。
- **#221**：「顶栏『自动(N)』点一下筛出那 N 个会话，不再整页刷新、不再变浅色；主题开关在头像菜单」——桌面壳内点「自动 (N)」：不刷新、主题不变、renderer.log 不再出现 `loadChats error … Failed to fetch`；若 M-2 E 尚未挂 `wsFilterAutoConversations`，退回为带 `?theme=dark&lang=zh` 的收件箱导航（仍不变浅色）。顶栏任何链接（草稿 / 待跟进 / 会员 / 头像菜单）跳转后地址栏带 `theme=`；徽标只显「旗舰版」，悬浮见「旗舰版（厂商自营）」；1024 / 1280 / 1440 三档：药丸组先缩 → 徽标变皇冠图标 → 用户名收起，铃铛 / 徽标 / 头像始终可见（三档截图本机无法出，装机后由 skuio 取）。
- **#230**：「多选有专用工具条」——点 ☑ 进入多选：标题行变成「已选 0 项 ｜ 归档 · 标签 · (删除) ｜ ✕」，勾选后计数跟动、按钮亮起；✕ 退出；缩到最窄侧栏不折行；☑ 图标不再变成三行文字。
- **#206**：「右侧会话工具已撤，标签/归档/删除在会话行和 ⋯ 菜单」——右侧工具箱没有「会话」卡；会话行右键 / ⋯ 有 搁置 / 打标签（含 × 直摘）/ 归档 / 清空聊天记录 / 删除会话；「AI 状态」弹层顶部在全自动档下显示「全自动运行中 · 今日自动发 N · 拦截 N · 记录 / 参数 / 暂停」；多坐席部署会话头部有认领按钮。

## 验证（本机）

- 新增 4 个测试文件 **38 例全绿**：`test_unread_badge_parity_222.py`（17：三桶互补 / 四桶恒等 / legacy 群键不进私聊 / 占位·墓碑不进四桶 / 被埋清单同口径 / 72h 清扫只清人工归档且幂等 / 横幅标已读按视角 / 看门狗接线三例 / 桩回落 / 路由与前端静态接线 / i18n）/ `test_ws_nav_theme_safe_221.py`（9：node 跑模板 `wsNavUrl` 真实源码——携带 / 不覆盖 / 外链不动 / 无钉零改写 / `__wsGoHome` 默认 url；裸 `location.href=` ratchet 7 文件；胶囊 / 徽标 Jinja 渲染 / 收缩规则 DOM 契约）/ `test_conv_tools_panel_removed_206.py`（6）/ `test_batch_toolbar_230.py`（6）。
- 既有相关测试全绿：`test_unread_aggregate` / `test_buried_alert_surface_170`（11）/ `test_buried_unread_badge` / `test_buried_conv_watchdog` / `test_admin_route_inventory`（已登记 `buried-mark-read`）/ `test_workspace_home_nav_dedup` / `test_settings_tier_197` / `test_copilot_panel_manifest` / `test_copilot_shared_sync` / `test_convops_snooze_ux` / `test_ui_visibility` / `test_template_inline_js_syntax` / `test_i18n_zh_hant` / `test_workspace_i18n_render` / `test_i18n_coverage`（sealed 页 EN 零中文泄漏、untagged 不回归）。
- **树上既有红（非本条引入）**：`test_template_inline_color_ratchet::test_inline_color_not_above_ceiling`（`unified_inbox.html` 12 > 11，HEAD 亦 12）与 `test_th_token*`（`_client_tier_upgrading.html` 未定义 token，L-4 在途）；`test_unified_inbox_stage1` 三例（翻译端点 `_no_translatable_body` = M-1 B 在途；`lang==='auto'` 两例 HEAD 亦缺；orchestrator 路由 34→37 = M-2 在途）；`test_i18n_coverage` 四例 `inbox.acct.bulk_*` / `inbox.gate.semi_pending` = M-2 B 在途（其 `dc8c5e0a` 已提交 i18n 半边，应已转绿）。交对应线。

## 交出去

- **交 M-2 E**：胶囊行为接口名 **`window.wsFilterAutoConversations()`**（在 `unified_inbox.html` 内定义：筛出全自动会话并高亮）；B② 的 `wsAutoPillClick` 已按「有则调、无则带 theme 跳收件箱」接好，M-2 挂上函数即生效，零改 `workspace_base.html`。计数口径若要改为「N 个会话处于全自动」在 `workspace_base.html` `_l2AutoBump` 处改并同步 `base.pill.l2auto_t/dyn` 文案。
- **交 M-5 C**：工具箱 `image` 卡按 M-5 C 处置（开发中态），M-4 D 未动它。
- **交 M-3**：`4358ca4b` 归属说明（见上）；`unified_inbox.html` 剩余 4 处裸 `location.href=`（L5621 / L15328 / L22677 / L27219）在 `test_ws_nav_theme_safe_221` 里 ratchet 为天花板 4（不在 M-4 四个改动位内），`<a>` 导航已由 window 捕获相位改写覆盖；谁下次动到那几行顺手改 `wsNav`。
- **交 M-6**：四单验收口径见上；#222 装载首 tick 的 `[buried] startup_sweep cleared N` 日志行请入台账；1024 / 1280 / 1440 三档顶栏截图装机后取。
- **未做 / 刻意不做**：主题**不**持久化（不变量）；壳标题栏 ⋯ 菜单未加主题项（`desktop/`，非本条）；`_win_unique.html` 未改（经 `__wsGoHome` 包装层补 theme）；`deleteConv` / `toggleArchive` / `snoozeConv` 函数保留（E / S 快捷键与 window 导出仍用）；`skip_group_chats` 本身不改（说明条只是把口径说清楚）；`unified_inbox.html` 之外的工作台子页裸 `location.href=` 只 ratchet 不改（`contact360` 2 / `workspace_dashboard` 1 / `queue_monitor` 1 / `escalation_log` 1 / `_win_unique` 1）。
