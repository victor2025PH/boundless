# 发版对账 v1.0.89 — 发版线 R89（群进工作台 / 间隔记忆 / 西语标点 / 媒体账本 / 173 毛刺 → 一个 clean 包 → **公共渠道 + 内测桥接 + 官网 + 播报**）

> 规矩：`.cursor/rules/chatx-desktop-install.mdc` 「单一版本」——一个 semver = 一个 clean 包。1.0.88 已于 09-18 10:08 上架，不能同号重切。
> 范围：公共渠道 1.0.88 → 1.0.89。未点名坐席机，不 `push_chatx`。140 不是坐席。

## 0. 一句话

**1.0.89 单包** `ChatX-Setup-1.0.89.exe` **576,867,579 B** sha256 `88ac8dbf0ff04e44352994c513352201292d503044a2e1b39735546564d5e020`（550 MB，clean，`MZ` 头核过，build-info flavor=clean commit=`ba2bcf5e`，干净 worktree `D:\boundless-r89`）。内容 = 1.0.88 全部 + 群未触发也进工作台 + 间隔/媒体账本/173 连接毛刺 + 西语 ¿/¡ + 「说真的」收敛。公共渠道 + 内测同字节桥 + 官网 + 频道 @hykj7 **messageId=188**（campaign `release-1089`）。

## 1. 未提交功能盘点（进包）

| 项 | 处置 |
|---|---|
| `translation_service` 西语 ¿/¡ | 进包 |
| `voice_colloquial_llm` 去掉「说真的」模板 | 进包 |
| `telegram_client` 白名单拦截 INFO 节流 | 进包 |
| 副驾 stale `severity=silent` | 进包 |
| 模型与密钥页 i18n 重排 | 进包 |
| `machines.json` `chatx_seat` + 坐席监控读台账 | 进包（运维，不影响客户功能） |
| 视频歌曲产物 / 本地 db / 坐席监控日志 | **不进** |

已在 1.0.88 之后、本版一并带走：`af8b93d6` 群镜像、`bede6d1a` 记忆/时间/媒体账本/173 毛刺。

## 2. 全链

| 步 | 状态 | 事实 |
|---|---|---|
| 干净 worktree 打包 | ✅ | `npm run build:backend` + `dist:win:clean` @ `D:\boundless-r89` |
| publish 公共 | ✅ | `latest.yml=1.0.89` exe HTTP 200 |
| publish 内测桥接 | ✅ | 同字节；公共指针未动 |
| 官网 | ✅ | `deploy.ps1 -FromHead -SkipAssets` → 部署完成；五值 `a6db3be3` |
| 频道 | ✅ | @hykj7 messageId=188。未发报障群（频道已链群） |
| 坐席三台 | ⏭ | 走 `latest.yml` 自动更新，未 push |

## 3. 下载

- https://bd2026.cc/download/chatx
- https://bd2026.cc/downloads/ChatX-Setup-1.0.89.exe
