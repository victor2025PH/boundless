# 指令 Q-23（事故）：自动出站单一闸门 + 守卫场景维度——群聊 / 人审档 / 同事消息永不自动发

> 预算 **$140**。事故：报障群 mid **1445 / 1454 / 1459 / 1461**（09-11 17:24 / 20:32 / 20:34 ×2）——skuio 实例账号 7331682688「小界客服支持」对我方值守消息自动回「你让我脸红了…」「你这样说我有点不知道怎么接了…」，其群会话 `mode=review`。**未立单：本线第 0 步立单（P0）。** 底座 `docs/修复指令_Q批_并行_2026-09-09.md`；分析 `docs/分析_1.0.81首日问题归类与工作台三视角方案_2026-09-12.md` §二 R1、§四 P0-1。落点表 `docs/发版对账_v1.0.83_Q23.md`。
> 前置：Q-15 / Q-17 / Q-18 已收工，本线在其上改。**Q-18 线已 -Done，本线是本批唯一动 `autosend_worker.py` / `autosend_policy.py` 的线。**

## 事实（代码只读核对）

- 软回应链：`adult_grader.dispatch_soft_reply()`（L479–）把稿排进 `svc._inbox_deliver_cb`（人工通过链）——**Q-15 刻意「不进 L2、不受 Q-3 闸」**，所以它不看会话档位（review / manual）、不看是不是群、不看发送方是谁；固定中文句（`soft_reply_candidates(lang)` 罐头）。
- 守卫是纯文本函数：`adult_grader.grade(text, lang)`、`risk_grader.grade(text, direction, persona)`、`drafts.keyword_risk_hits(text, direction)` 都没有会话上下文。
- **工作树里已有一份未提交的止血**（`adult_grader.py` +63/−3、`ops/bug_intake.py`，mtime 09-11 21:01，作者线已停 5 小时未入库）：`blocks_adult_outbound(conv, cfg)`（Telegram 负 peer / `is_group_conversation` / `is_bug_group` → skip，日志 `status=skip_public_chat`）+ 中文词表消歧（「给我看你的」必须接身体部位，防报障群「给我看你的日志」误伤）。**它不在 1.0.82 里。**
- 同类绕闸风险：Q-2 C 委婉延后罐头句、stop_contact farewell、Q-15 human 政策 3 分钟补发——都走同一条人工通过链。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **0 立单 + 入库止血** | `record_bug_ticket`（P0，reporter 值守，title「群聊 / 人审档软回应外泄：报障群 4 条」，证据 mid 1445 1454 1459 1461 + 发送方 7331682688）；`agent_probe` 核 `adult_grader.py` / `bug_intake.py` 无 ACTIVE 后，把 21:01 那份未提交改动**原样入库为本线 A0 commit**（先跑 `test_adult_grader_gate`），补一条门禁「Telegram 负 peer 会话 → `skip_public_chat`」 | `adult_grader.py`、`ops/bug_intake.py`、tests |
| **A 守卫场景上下文** | 新 `src/inbox/guard_context.py`：`GuardContext{is_group, mode, sender_kind: customer\|agent\|self\|peer_bot, lang, chat_id}` 由 `drafts.py` 风控入口 / `protocol_autoreply` 钩子构造一次；三处守卫加可选参数 `ctx`（缺省 None = 旧行为）：`is_group` → 不评估、不出站、不打标、不持有；`sender_kind ≠ customer` → 不评估；`mode ∈ manual/review` → 任何自动出站改为「候选进审核稿」 | 新文件、`adult_grader.py`、`risk_grader.py`、`drafts.py`（一行）、`protocol_autoreply.py`（钩子一行） |
| **B 软回应改道** | `dispatch_soft_reply` / Q-2 委婉延后 / stop_contact farewell / human 补发 **全部改经同一发送门**：`autosend_policy.decide(..., kind="soft_reply")` → `autosend_worker` 新 stage=`soft_reply`（跳过节奏排队但**必过** `_human_priority_gate` 与场景闸）；`_inbox_deliver_cb` 只留坐席「人工通过」一个调用方，其它调用点删光并加门禁「非坐席动作不得调 `_inbox_deliver_cb`」 | `adult_grader.py`、`drafts.py` L1215（Q-2 C）、`stop_contact.py`、`autosend_policy.py`、`autosend_worker.py` |
| **C 软回应语言与口吻** | 软回应文本：语言 = `resolve_outbound_lang` 结果（跟对方），文本由人设口吻**短生成**（`persona_reply` 单句预算，system 只带人设 + 「一句话轻轻带开话题」）；生成失败 → **不发**（不用固定句）；日志 `[adult] soft_reply … gen=persona\|skip` | `adult_grader.py`、`persona_reply.py` 只加一个短生成入口 |
| **D 对方是机器人 / 同事** | `peer_bot_guard` 扩：同事账号（同租户其它账号 / 值守 bot 8506426282 / 6834964252）、报障群与运维群 chat_id 列入 `never_auto_reply`（`notify_webhooks.json` / bug_intake 配置同源），命中 → 不起草不出站，日志 `[peer-guard] skip reason=` | `peer_bot_guard.py`、配置 |
| **E 门禁** | `tests/test_guard_context_gate.py`：① 报障群回放（我方文本含「成人 / cum / 露骨 / 给我看你的日志」→ 零出站零打标）；② review 档软回应进审核稿不发；③ agent / self 文本不评估；④ 非坐席调 `_inbox_deliver_cb` 静态扫描 0 处；纳入 R 批门禁 | tests |

## 红线

- 真高风险（pressure / 索钱 / 威胁）的**私聊 + 全自动**行为不变——本线只加「场景不对就不动」的否定门。
- 不引入任何新罐头句；现有固定句只允许在「生成失败 → 不发」时被删除，不得替代。
- `autosend_worker.py` 改动只加 stage=`soft_reply` 与调用门，不动 Q-18 的 defer / resume 逻辑。

## 开新对话粘贴

```
读 docs/指令_Q-23_自动出站单一闸门与场景维度_2026-09-12.md，按它做 Q-23（事故：报障群被软回应刷屏 mid 1445/1454/1459/1461——第 0 步 record_bug_ticket 立 P0 单并把工作树里 21:01 未提交的 adult_grader.py blocks_adult_outbound + bug_intake.is_bug_group 止血原样入库加门禁 / 新 guard_context.GuardContext{is_group,mode,sender_kind,lang} 给 adult_grader、risk_grader、keyword_risk_hits 三处守卫：群不评估不出站不打标，非客户发送方不评估，manual/review 档任何自动出站改进审核稿 / 软回应·Q-2 委婉延后·stop_contact farewell·human 补发 全部改经 autosend_policy + autosend_worker 新 stage=soft_reply 必过 _human_priority_gate 与场景闸，_inbox_deliver_cb 只留坐席人工通过一个调用方并加静态门禁 / 软回应语言跟对方、文本人设口吻短生成，失败即不发不用固定句 / peer_bot_guard 加同事账号与报障群运维群 never_auto_reply / 门禁四例）。预算 $140。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与 Q-15 / Q-17 / Q-18 三张落点表（在其上改），再 scripts\agent_probe.ps1 -Intent 登记；你是本批唯一动 autosend_worker.py / autosend_policy.py 的线，不动 Q-18 的 defer/resume；私聊全自动的真高风险行为一字不变；不引入罐头句。每个 commit 带新单号并立刻补落点表 docs/发版对账_v1.0.83_Q23.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-23 行并 agent_probe -Done。
```
