# 发版对账 v1.0.74 · J-8 陪伴安全 + 关怀语义（2026-09-05）

> 指令：`docs/指令_J-8_陪伴安全与关怀语义_2026-09-05.md`。预算 $190。
> 全部 `.py` 改动**尚未装进 zhiliao**（下一重启窗装载后再看 §验收）；模板 / i18n pack 热更新已生效。
> 安全链（`skill_manager` 危机识别 / 红线 / R8 判定）**零行改动**——三件 crisis 评测门禁全绿。

## 落点表

| 项 | 修什么 | 落点 | commit | 状态 |
|---|---|---|---|---|
| A #185-1 三档空态 | 「功能未开」与「无事件」同一空态，关着也显示「好消息」 | `crisis_audit.html`：`state ∈ audit_off / audit_on_empty / has_events`；`audit_off` = 红条 `#ca-banner`（按 `audit_on === false` 判，有历史也亮）+ 「危机留痕未开启」空态 + 一键开启按钮，**绝不**出「好消息」；`audit_on_empty` = 「留痕已开启：近 30 天无危机事件」；后端 `crisis_audit_routes.py` `audit_page_state()` / `wellbeing_switches()` 纯函数，`GET /api/crisis-events` 多回 `state / window_days / audit_on / escalation_on / wellbeing_enabled` | `256e3380` | 在树 |
| A #185-2 一键开启 | 开关只能改 YAML | `POST /api/crisis-events/enable`（`_api_write` 角色门）→ `config_manager.set_overlay_flag` 写 `companion.wellbeing.crisis_audit` + `crisis_escalation`（白名单 `_ENABLE_FLAGS`，ruamel 保注释）+ 审计行 `crisis_audit.enable`；`err.ca.config_unavailable / enable_failed` | `256e3380` | 在树 |
| A #185-3 出厂开（D7） | 留痕 / 升级出厂关 → severe 命中不记录、不叫人 | `config/profiles/cloud_light.yaml` + `config/config.desktop.internal.yaml` 加 `companion.wellbeing.{crisis_audit: true, crisis_escalation: true, escalate_after: 1}`（只加 8 行，D1b 同文件 goals 段未动） | `256e3380` | 在树 |
| A #185-4 R8 → 工作台 | 升级只走 webhook，桌面包没配 ⇒ 没人被叫到 | 新 `src/companion/wellbeing_escalation_bridge.py`：`CrisisEventStore.record()` 新增监听器（`add_crisis_event_listener`，推路径零延迟）+ watchdog `_check_crisis_escalation_bridge` 补扫水位以上事件（扫路径，首扫只立水位不追溯）；severe+escalated → `tag_needs_human`（HANDOFF_TAG 红徽标同源）+ `set_conversation_pinned` + `record_escalation(source=crisis)` 案例；同事件两路去重（`ESCALATION_DEDUP_SEC`）；反查不到会话 → `unresolved` 如实；异常全吞不反噬主回复。**skill_manager 零改动** | `256e3380` | 在树（待重启装载） |
| A #185-5 人话化 | R4/R6/R8、配置键、「用户 ID 前缀」黑话 | 页名「危机审计」→「客户安全预警」（nav_schema / web_i18n / help_terms / howto_pack / draft_review / health_routes 全改）；状态 = 关注 / 已兜底 / 已升级 + 「状态说明」图例；等级 = 严重 / 偏高；类别 = 自伤风险 / 绝望情绪 / 语音痛苦信号；筛选 = 「按客户搜索」；配置键不再出现在页面 | `256e3380` | 热更已生效 |
| A #185-6 角色门 | 高敏内容 viewer 也能看 | `web_user_store.py` `crisis_audit` 页移除 `ROLE_VIEWER`（master/admin/supervisor） | `256e3380` | 在树 |
| A 门禁 | | `tests/test_wellbeing_escalation_bridge.py`（17 例：纯函数 / 幂等 / 推扫去重 / 懒 store / **watchdog 真调用** / profile 出厂值 / 摘要 ≤120）、`test_web_crisis_audit_api.py`（+空态 / 开关 / enable 保注释 / 权限 / 审计）、`test_crisis_audit_empty_state.py`（三档）、`test_admin_route_inventory` 加 enable 路由；三件 crisis 评测全绿 | `256e3380` | 绿 |
| B #182 | 「补一条关怀」把操作员指令当客户事件 | 未开工 | | 待 |

## 已知边界（A）

- 桥只在 `crisis_audit` **且** `crisis_escalation` 都开时工作：`record()` 落库是推路径的载体，留痕关着 ⇒ 事件不落库 ⇒ 桥收不到。一键开启按钮**同时**开两键，页面「人工升级未开启」黄条只在留痕开、升级关时出现。
- 首扫只立水位：装载前已落库的陈年升级事件**不追溯**点亮（避免重启后一次钉顶几十条旧会话）。
- 反查会话按 `user_id → chat_key` 取**最近活跃**那条；同一客户多账号会话只点亮一条。

## 88MP86 回访文案口径（交值守）

agent 报告里「危机识别未开启」是**误判**：识别与安全回复红线随 `wellbeing.enabled` 出厂开、一直在跑；关着的是**留痕**（R9）与**人工升级**（R8）。回访时说：
> 「AI 一直在识别并兜底，只是之前没有记录、也没叫人。装 1.0.74 后：页面改名『客户安全预警』，留痕与人工升级默认开；如仍看到红条『危机留痕未开启』点一键开启即可。」

装 1.0.74 后 skuio 机页面应显示 `audit_on_empty`（「留痕已开启：近 30 天无危机事件」）而非黑话标题。

## 验收（重启装载后）

1. `GET /api/crisis-events` 回 `audit_on: true, escalation_on: true, state: audit_on_empty|has_events`。
2. 日志出现 `危机升级桥已装载`（首 tick 一次）。
3. 侧栏「客户安全预警」；viewer 账号不可见该页。

## 与他线并行的红（非本线，未动）

- `tests/test_workspace_sidebar.py` ×5：`/workspace/channels/*` 渠道中心融合线在途。
- `tests/test_admin_route_inventory.py::test_no_unexpected_extra_routes`：`/api/kb/entries/purge-source` `/api/kb/health`（J-9）。
- `tests/test_alert_delivery_e2e.py` + `test_watchdog_check_audit.py` 无真调用棘轮：`phantom_unread`（他线 health_watchdog 在途 hunk）。
