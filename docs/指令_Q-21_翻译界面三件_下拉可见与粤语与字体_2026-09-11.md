# 指令 Q-21（09-12 04:5x 修订）：语言能力矩阵单点——粤语四处同源 · 起草语言计划可追 · 译文字体不遮挡

> 预算 **$100**。工单 **#290（ZDPNMT / R33MZW / N8G3JZ 1.0.82 复验仍报）/ #286 / #302（GWJ2RZ 同机 jun：`lang_unknown` → L1 人审不可见）/ Y82GWM（全自动给英文客户起草中文）**。底座 `docs/修复指令_Q批_并行_2026-09-09.md`。落点表 `docs/发版对账_v1.0.83_Q21.md`。
> **#289「我的消息」下拉看不见已由 `7c4aae3b`（xlate-pop 自适应，1.0.82）修复**——skuio 1.0.82 复验未再报；本线只做 A 段的深色对比复验截图，不重做几何。
> 前置：Q-16 已收工（「发→X」按会话）。

## 事实

- **#290**：回复工坊「跟随人设 / 账户」语言下拉只有 中英泰越印日韩俄西葡（10 种），无粤语；右栏工具箱 `cp-xlate-tools.js` `LANGS` 15 种也无 `yue`；对话翻译目录 `HYMT_TARGET_LANGS`（08-29 扩容）**已含 `yue` / `zh-tw`**——三处列表不同源。华哥实聊是粤语书面。
- **#302（jun，B990，1.0.82）**：Gabb 会话 `auto_generate_draft L1 reason=lang_unknown`（`mode=review`），用户以为「人设未选定」——真因是对方语言判不出按 D-M3 HOLD 人审，界面没有任何原因。
- **Y82GWM**：Messenger 客户只发英文「Hi」，全自动起草中文（顶栏译文=中文）——`reply_lang` 决策来源（人设 / 全局默认 / 会话 pin / 首触默认 / 消息证据）没有单点日志，查不出 `lang=zh` 从哪来。
- **#286**：选择翻译后译文字体变大遮挡原文（读 `tmp_diag` 中 #286 首份报告截图描述）。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 语言目录单点** | 新 `src/i18n/lang_catalog.py`（或复用 `HYMT_TARGET_LANGS` 所在模块）作为**唯一目录**（34 码，含 `yue` / `zh-tw`，带 zh/en/zh-Hant 名称与「可起草 / 可翻译 / 可 TTS 克隆」能力位）；回复工坊「跟随人设 / 账户」列表、对话翻译「对方 / 我的消息」、顶栏译文、人设语言、`cp-xlate-tools.js` `LANGS` **五处只读它**（前端经一个 `/api/lang-catalog` 端点或模板注入）；门禁：五处 ⊆ 目录且含 `yue` | 新模块、`unified_inbox.html`、`reply_settings.html`、`personas.html`、`cp-xlate-tools.js`（双树）、路由 |
| **B 起草语言计划** | 起草前算一次 `conv_lang_plan{reply_lang, xlate_target, tts_lang, decided_by, confidence}`（复用 `resolve_outbound_lang` 六级 + 人设语言），一行 `[lang-plan] conv= reply= xlate= tts= by=` 日志；`reply_lang` 只读它（Y82GWM 可追）；**`lang_unknown` 处置改为**：全自动 / 半自动会话用人设默认语言起草并在会话头标「对方语言未知 · 按人设语言（English）回」+ 稿头同标，**不再静默 L1**；人审档保持人审但显示原因（D-M3 修正，默认按此建议，老板可否） | `outbound_translate.py`（只加计划函数，不改六级顺序）、`drafts.py` 调用一处、`autodraft_helpers.py`、会话头（`lp-` 前缀）、i18n |
| **C 粤语生成链** | `yue` 选中 → 出站按粤语口语生成（system 提示词加粤语口语指令 + few-shot 3 句），翻译走支持 yue 的引擎（核 `translation_engines` 哪个真支持，日志标 `provider`），不当普通话；TTS 侧仅传 `tts_lang=yue` 给 Q-22（本线不动 TTS） | `persona_reply.py` 语言指令一处、`translation_service` 路由 |
| **D 字体** | 译文行字号 / 行高与原文一致或 0.9 倍，不覆盖原文；按 #286 截图复现后修；深色主题下拉对比度复验截图（浅 / 深各一） | `unified-inbox.css`、`unified_inbox.html` |
| **E 门禁** | 目录同源测试（五处 ⊆ 目录，含 yue）；`test_lang_plan_q21`：英文「Hi」× 中文人设 × 全自动 → `reply_lang=en by=message`；`lang_unknown` × 全自动 → 人设语言 + 标注不 L1；模板 JS 语法 + i18n | tests |

## 红线

- 不改 `resolve_outbound_lang` 六级顺序（D-M3），只在其上加计划与 `lang_unknown` 处置。
- `unified_inbox.html` 只动 `xl-` / `lp-` 前缀 hunk 并 `stage_hunks --mine`；不新增内联色。
- 不动 TTS（Q-22）。

## 开新对话粘贴

```
读 docs/指令_Q-21_翻译界面三件_下拉可见与粤语与字体_2026-09-11.md（09-12 修订版），按它做 Q-21（#290 #286 #302 Y82GWM：新 lang_catalog 唯一目录 34 码含 yue/zh-tw 带能力位，回复工坊·对话翻译·顶栏译文·人设语言·cp-xlate-tools 五处只读它 + 门禁五处⊆目录含 yue / 起草前 conv_lang_plan{reply_lang,xlate_target,tts_lang,decided_by} 一行 [lang-plan] 日志，reply_lang 只读它；lang_unknown 在全自动/半自动改为按人设语言起草 + 会话头「对方语言未知 · 按人设语言回」标注不再静默 L1，人审档显示原因 / yue 选中出站按粤语口语生成 + 翻译走真支持 yue 的引擎并标 provider / 译文字号不遮原文 + 深浅主题下拉对比复验截图 / 门禁）。#289 已由 7c4aae3b 修，不重做几何。预算 $100。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与 docs/发版对账_v1.0.81_Q16.md，再 scripts\agent_probe.ps1 -Intent 登记；不改 resolve_outbound_lang 六级顺序；unified_inbox.html / reply_settings.html 只动 xl-/lp- 前缀并 tools/stage_hunks.py --mine；不动 TTS。每个 commit 带 #290 并立刻补落点表 docs/发版对账_v1.0.83_Q21.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-21 行并 agent_probe -Done。
```
