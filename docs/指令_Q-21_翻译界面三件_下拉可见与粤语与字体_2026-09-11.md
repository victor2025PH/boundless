# 指令 Q-21：翻译界面三件——「我的消息」下拉看不见 · 语言列表缺粤语 · 译文字体变大遮挡

> 预算 **$80**。工单 **#289（BFBF6E，界面 P1）/ #290（R33MZW）/ #286**（skuio 1.0.81，09-11 16:4x）。底座 `docs/修复指令_Q批_并行_2026-09-09.md`。落点表 `docs/发版对账_v1.0.82_Q21.md`。
> 前置：Q-16 已收工（「发→X」空档新标签「自动 · 跟对方：X」+ `xl-scope-tag`）。#289 复现发生在 Q-16 落地后，**先核是不是 Q-16 D 段引入的回归**（1.0.80 上同一操作是否正常），再修。

## 事实

- **#289**：Telegram 华哥会话，深色主题，对话翻译弹层 → 点「我的消息」（现显示「自动跟对方：中文」）→ 原弹层消失，语言选择器漂到会话左侧大面积黑底，只在底部隐约见 English / 日本語，选项与背景对比度≈0。判定：下拉 portal 定位错（未贴触发器）+ 深色主题选项文字色失效 + 可能被 overflow 裁切 + 打开下拉时父弹层被关。群内附图 1 / 图 2。
- **#290**：回复工坊「跟随人设 / 账户」语言下拉只有 中英泰越印日韩俄西葡，**无粤语**；而对话翻译目录（`HYMT_TARGET_LANGS`，含 `yue` / `zh-tw`，08-29 扩容）已有 → 两处列表不同源。华哥实聊是粤语书面。
- **#286**：选择翻译后字体变大、遮挡原文（读报告 `tmp_diag` 中 #286 首份报告的截图描述）。

## 要做的四段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 下拉可见** | 语言选择器 portal 锚定在触发按钮正下方（视口边界翻转），`z-index` 高于弹层且打开时**不关闭**父弹层；深色主题：选项 / 搜索框走 `--th-*` 令牌（门禁 `inline_color_ratchet` 不许新增内联色）；列表容器 `overflow` 允许可见；含搜索框、语言名一眼可见；浅 / 深两主题各截一张图进落点表 | `unified_inbox.html`（`xl-` 前缀，stage_hunks）、工作台 CSS |
| **B 粤语同源** | 回复工坊「跟随人设 / 账户」语言列表改读同一份目录（`HYMT_TARGET_LANGS` / 34 码），加 `yue`（粤语）与 `zh-tw`；对话翻译「对方消息 / 我的消息」、顶栏译文语言、人设语言三处核对同源；选 `yue` 出站按粤语口语生成 / 翻译（`outbound_translate.normalize_target` 已认 yue，核对生成侧 `reply_lang` 传递），不当普通话 | `reply_settings.html` 或回复工坊模板（stage_hunks）、对应路由、`persona_manager` 语言枚举 |
| **C 字体** | 译文行字号 / 行高与原文一致或 0.9 倍，不覆盖原文；按 #286 截图复现后修 | `unified_inbox.html` CSS |
| **D 门禁** | 模板 JS 语法门禁 + `test_i18n_coverage`（新词条）+ 语言目录同源测试（三处列表 ⊆ 目录且含 yue） | tests |

## 开新对话粘贴

```
读 docs/指令_Q-21_翻译界面三件_下拉可见与粤语与字体_2026-09-11.md，按它做 Q-21（#289 #290 #286：对话翻译「我的消息」下拉 portal 锚定触发器正下方 + 深色主题 --th- 令牌高对比 + 打开时不关父弹层 + overflow 不裁，浅深两主题截图 / 回复工坊「跟随人设/账户」语言列表与对话翻译、顶栏译文、人设语言四处同读 HYMT_TARGET_LANGS 目录并加 yue、zh-tw，选粤语出站按粤语生成 / 译文字号不遮原文 / 门禁：JS 语法 + i18n + 语言目录同源）。预算 $80。先核 #289 在 1.0.80 是否同样复现（判定是否 Q-16 D 回归），再读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与 docs/发版对账_v1.0.81_Q16.md，scripts\agent_probe.ps1 -Intent 登记；unified_inbox.html / reply_settings.html 他人在途 hunk 用 tools/stage_hunks.py --mine，只动 xl- 前缀；不新增内联色。每个 commit 带 #289 并立刻补落点表 docs/发版对账_v1.0.82_Q21.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-21 行并 agent_probe -Done。
```
