# 指令 Q-26：会话状态带（六种「不发」一条状态一句话）+「自动化与风控」设置中心收口（修 #301 死链）

> 预算 **$160**。工单 **#301（XX23SR：「在人设工坊调成人内容政策」死链）/ #302（jun：`lang_unknown` 人审不可见）/ #293 #285 后续（拦截可见）**。分析 `docs/分析_1.0.81首日问题归类与工作台三视角方案_2026-09-12.md` §三、§四 P1-7 / P1-8。落点表 `docs/发版对账_v1.0.84_Q26.md`。
> 前置：Q-18（让位 chip `ay-` + 今日拦截卡）/ Q-17（风控分级卡）/ Q-14 B（AI 未生成灰标 `aif-`）/ Q-24（sidecar 失败红字 `ms-`）/ Q-21 B（`lp-` 语言标注）已收工或在途——本线**把它们合成一条**，不新造状态源。

## 事实

- 会话头现在最多同时出 5 个 chip：档位徽标、`ay-` 让位倒计时、`rk-` 风险保持、`aif-` AI 未生成、`ms-` Messenger 失败 / PIN 黄条，各自读各自的 KV（`risk_hold:<cid>` / `ai_last_fail:<cid>` / `_agent_sent_ts` / handoff_meta / sidecar 状态）；用户问的只是一句话：「这个会话现在会不会发、为什么」。
- #302：`auto_generate_draft L1 reason=lang_unknown` 界面零提示（Q-21 B 会加 `lp-` 标注，本线把它纳入同一条带）。
- #301：回复设置页成人内容行文案「在人设工坊调成人内容政策（Q.15）」——人设工坊无该入口（Q-15 B 的 `adult_policy` 格在 `personas.html` 见面政策同区，用户找不到 / 或未渲染），死链。风控相关设置现在散在：回复设置页（风控分级卡 / 今日拦截卡 / 用量 / 档位默认）、人设页（见面政策 / 成人政策 / 相册）、目标页（暂停摸底总开关）。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 服务端会话状态聚合** | 新 `src/inbox/conv_state.py`：`compute(store, cid) -> {will_send: bool, state: auto\|deferred\|held\|human\|manual\|off_hours\|sidecar_down\|lang_unknown\|ai_fail, reason_code, reason_text_key, until_ts, action: resume\|ack\|retry\|confirm_pin\|none, params}`，只读既有六个来源（`risk_hold` / handoff_meta / `_agent_sent_ts`（经 worker 只读口）/ `ai_last_fail` / work_schedule / sidecar 状态 / `lang_plan`）；`GET /api/unified-inbox/conv-state?cid=` + `/thread` 响应顺带；优先级：manual > held(needs_human) > sidecar_down > off_hours > deferred > lang_unknown > ai_fail > auto | 新文件、`unified_inbox_read_routes.py`（stage_hunks） |
| **B 一条状态带** | 会话头一条状态带替代五个 chip：固定四色语义——**绿**「AI 会自动回」/ **琥珀**「AI 23 秒后接回 · 坐席刚发过」「对方语言未知 · 按人设语言回」/ **红**「AI 不发 · 需人工（要钱 / 要凭据 · 命中『…』）· [我知道了]」「AI 不发 · Messenger 输入框丢失 · 已重试 2 次 · [重试]」/ **灰**「人工模式」；一句话 + 一个动作按钮；旧 chip（`ay-` / `rk-` / `aif-` / `ms-` / `lp-`）改为状态带的子渲染，不再各画各的；`reply_diagnosis` 面板保留为「详情」 | `unified_inbox.html`（`cs-` 前缀）、`unified-inbox.css`、i18n zh/en/zh-Hant |
| **C 「自动化与风控」中心** | 回复设置页新增一级分组「自动化与风控」，把 档位默认 / 让位规则（60s）/ 风控分级卡 / 成人政策默认 / 见面政策默认 / 今日拦截卡 / 暂停摸底总开关 收进同一区；人设页只保留「本人设覆写」（成人 / 见面 / 风控级别）并**互相链接可达**（修 #301：文案指向真实入口，点击直达并高亮该格）；导航 schema 同步；小智词条 `help_terms` + `howto_pack`「AI 为什么没回 / 去哪调」 | `reply_settings.html` + `reply_settings_page.py`（stage_hunks）、`personas.html`（`mp-` / adult 格）、导航 schema、`help_terms.py` / `howto_pack.py` |
| **D 死链门禁** | 新 `tests/test_settings_deeplinks.py`：所有设置文案里的「去 X 调」链接必须解析到存在的锚点 / 路由（静态扫描 `data-goto` / href）；#301 文案改「自动回复设置 → 自动化与风控 → 成人内容」 | tests |
| **E 门禁** | `test_conv_state_q26`：六种来源各造一例 → 状态 / 文案 / 动作正确，优先级正确；模板 JS 语法 + i18n 覆盖；三档视口截图（405 / 1024 / 1280）浅深主题各一进落点表 | tests |

## 红线

- 不新造状态源：`conv_state` 只读，不写任何 KV；动作按钮走既有端点（摘标 / 重试起草 / 接回 / 边车重试）。
- `unified_inbox.html` 只动 `cs-` 前缀 + 把旧 chip 挂到状态带（`stage_hunks --mine`），不新增内联色，全部走 `--th-*` 令牌。
- 不动任何守卫 / 发送逻辑（Q-23 / Q-27 范围）。

## 开新对话粘贴

```
读 docs/指令_Q-26_会话状态带与自动化风控设置收口_2026-09-12.md，按它做 Q-26（#301 #302 #293后续：新 conv_state.compute 只读聚合 risk_hold / handoff / agent_sent / ai_last_fail / work_schedule / sidecar / lang_plan 六源 → {will_send,state,reason_code,until,action} + GET conv-state 端点与 /thread 顺带，优先级 manual>held>sidecar_down>off_hours>deferred>lang_unknown>ai_fail>auto / 会话头一条状态带四色语义一句话一动作，ay-/rk-/aif-/ms-/lp- 旧 chip 改为其子渲染 / 回复设置页新增「自动化与风控」分组收进档位默认·让位规则·风控分级·成人政策·见面政策·今日拦截·暂停摸底，人设页只留本人设覆写并互链直达高亮，修 #301 死链文案，小智词条「AI 为什么没回」/ 新 test_settings_deeplinks 静态扫描所有「去 X 调」链接必可达 / 门禁六源各一例 + 截图三档两主题）。预算 $160。先读 docs/分析_1.0.81首日问题归类与工作台三视角方案_2026-09-12.md §三§四 与 Q-14B / Q-17 / Q-18 / Q-24 落点表（状态源已存在，只读不写），再 scripts\agent_probe.ps1 -Intent 登记；unified_inbox.html / reply_settings.html / personas.html 只动 cs- 前缀与旧 chip 挂接并 tools/stage_hunks.py --mine；不新增内联色；不动守卫与发送逻辑。每个 commit 带 #301 并立刻补落点表 docs/发版对账_v1.0.84_Q26.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-26 行并 agent_probe -Done。
```
