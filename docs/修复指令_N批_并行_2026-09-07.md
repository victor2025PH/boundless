# 修复指令 N 批（1.0.76 首日复验：新暴露 6 件 + M-7 收口）—— 并行公共底座

> **本文件只是「索引 + 去向表 + 文件归属 + 老板决策」的公共底座，不要拿本文件开对话。**
> 每条指令各自成文，**一条指令 = 一个新对话**。开对话粘贴话术在各指令文件末尾，也汇总在 `docs/N批_进度总账.md`。
>
> 依据：1.0.76 于 09-07 03:36 发到三渠道，skuio 机 04:01 自更、13:14 **卸载重装**后复验一整天（25 份报告 13:21–17:05 → 新单 #237–#249，登记链全部自动挂单/立单并回执）；钧 18:31 群提交 → 自动提醒 → 他用**旧版 zl_collect v1.2.2** 上传失败、21:53/22:07 把报告贴回群 → #250/#251（重复）。M-7（#236 目标引擎）今晨 06:25–07:00 提交 A/B/C 三件后**对话中断**：D 未做、落点表一~四节空、总账行仍「未开工」。
> **结论：13 张新单实为 6 件事。两件 P1 是 1.0.76 自己带出来的——「人设备份恢复」首次真用就 `_esc is not defined`（他卸载重装后人设池为 0、恢复失败、只能手工重建），「主动关怀·立即发」点了 85 分钟没发出（M-1 A 只做了 verbatim 默认与真值统一，没碰派发/立即发链）。其余四件：相册 AI 识别未触发、克隆音色登记反馈缺失、域包错配（陪伴人设套了销售域的模板/标签/KB 分类）、KB 预置 GXP 支付话术。钧的语音「疑似无声」是 #215③ 第二次。**
> 目标版本 **1.0.77**。**N-1 / N-2 先开（对客 + 数据面），N-3 / N-4 并行，接手 M-7 可立即开，N-5 收口。**

| 指令 | 预算 | 类型 | 内容 | 文件 |
|---|---|---|---|---|
| **N-1** | $120 | 主动关怀「立即发」真投递（M-1 A 的收口缺口） | #243（并 #244–#249）：立即发＝同步直投 / bring_forward 置 due_at=now 立即 tick / 错峰不作用于人工指定时间 / deferred 队列启动即初始化 + 出队落日志 + 查 16:47 未出队 / 卡片状态机 + 防重复 / 派发循环 enabled=False 与运行时切真发的关系 | `docs/指令_N-1_主动关怀立即发_2026-09-07.md` |
| **N-2** | $150 | 人设工作室 1.0.76 回归三件 | #237 备份恢复 `_esc is not defined`（P1）/ #239 克隆音色登记反馈缺失 / #238 相册上传 AI 识别 0/144 未触发 | `docs/指令_N-2_人设工作室回归三件_2026-09-07.md` |
| **N-3** | $150 | 域包错配与预置清理 | #241 陪伴人设跑在 conversion 域包上（BANT 摸底标签 / 客户画像 schema / 「标成交」产品金额 / KB 分类 / 「营销目标」字样）/ #240 KB 首装预置 GXP「系统话术·直接」 | `docs/指令_N-3_域包错配与预置清理_2026-09-07.md` |
| **N-4** | $120 | 语音质量闸（钧 #250 = #215③ 第二次） | 「已核有声」vs「疑似无声」同屏矛盾 / 试听过期逻辑 / 阈值与 Mizuki 配置核 / TTS 日志统一前缀便于取证 | `docs/指令_N-4_语音质量闸_2026-09-07.md` |
| **接手 M-7** | 余额 | 目标引擎 D 项 + 落点表 + 总账 | A/B/C 已提交（`d0933356` `9ff2e90a` `fe6a7861`），D「卡片说真话 + 单目标 stalled」未做；`docs/发版对账_v1.0.77_M7.md` 一~四节空 | `docs/指令_M-7_目标引擎可追溯与真出手_2026-09-07.md` + 本文末尾接手话术 |
| **N-5** | $100 | 值守收口 | 并单（#244–#249→#243，#251→#250）/ #242 关闭 / 钧 zl_collect v1.3.1 重发（K-5 ⑤ 遗留）/ 钧机两包 TAZ9QS（#215）+ 新请求（#250）定性 / 自动立单去重加强 / 1.0.77 装载·两包·三渠道·回访 | `docs/指令_N-5_值守收口_2026-09-07.md` |

**投放顺序**：N-1 与 N-2 先开（文件不重叠）→ N-3 / N-4 / 接手 M-7 并行 → N-5 等全部收工。
**跨对话 / 跨账号交接唯一入口**：`docs/N批_进度总账.md`。
**背景**：`docs/M批_进度总账.md`、`docs/发版对账_v1.0.76.md`、`docs/发版对账_v1.0.76_M1.md`（关怀 A 项做到哪）、`docs/发版对账_v1.0.75_L2.md` E（备份与迁移）/ A（语音三态）、`docs/发版对账_v1.0.74_J7.md` B（相册 AI 补标链）、`docs/发版对账_v1.0.74_J9.md`（KB 隔离）。证据全在 `D:\boundless\tmp_diag\<码>\`（`ticket.txt` 记归属）。

## 13 张新单 + 钧 2 张的去向

| 去向 | 单 / 报告 | 说明 |
|---|---|---|
| N-1 | #243 #244 #245 #246 #247 #248 #249（B5TAA3 G7KEUT GGUNG4 NRTDJH WZPMXF 8VGQDM RGPTQF TXP5G3 5CM8QB FHSZTA） | 七张单一件事；FHSZTA 已把三层根因写全（提前不改 due_at / 到期 tick 不发只入 deferred / deferred 未出队），照它做 |
| N-2 | #237（RN2SKR MJGHKQ）/ #239（8CAJWA R8ECFS）/ #238（QENDQ2 F35X38） | #237 是 1.0.75 L-2 E 新功能首次真用即败；MJGHKQ 被自动链误挂到 #232，N-5 改挂 |
| N-3 | #241（78PDGB 3NVM7Q 4CNK6G TN736F VAQGZY）/ #240（ZNW5CN 698J28） | 3NVM7Q 已定性为域包错配（`ConversionDomainHook` + 域人设「线上陪伴」同秒注册） |
| N-4 | 钧 #250（+#251 重复；群图 1315 / 报告文本 mid 1319）+ #215③ | 证据待 `dr-mtrcb0iy-svzop9`（09-07 22:3x 已发）；旧包 TAZ9QS（17:41，#215）N-5 先读 |
| 接手 M-7 | #236 + 09-07 04:17/04:19 两份（GTNG25 X6JMGZ：1.0.76 启动 04:01/04:12:13/04:12:57 三次，需查） | D 项 + 三次启动原因写进落点表 |
| N-5 | #242（版本确认，关）/ VC9ERR（无备注，不立）/ 上述并单 | |
| 只记不修 | 698J28 里「帮助语料 291 条」是悬浮球帮助语料不是客户 KB（第三次澄清，回访写清）；X6JMGZ 提到会话头部「本会话：全自动」已出现 = M-2 E 验收通过 | |

## 文件归属矿阵（共享工作树；开工先 `scripts\agent_probe.ps1` 看 ACTIVE 再动）

| 文件 / 目录 | 归属 |
|---|---|
| `src/contacts/care_dispatcher.py`、`care_schedule.py`、`src/integrations/companion_proactive.py`（派发循环 enabled / dry_run / interval 与运行时切换）、`src/web/routes/care_routes.py`（立即发 / bring_forward）、多平台 deferred 队列（grep `multiplatform_deferred`）、care 页模板 / i18n、对应 tests | **N-1** |
| `personas.html`（备份恢复块 / 语音 tab 登记反馈 / 相册上传引导三处）、`static/js/persona_studio_core.js`（`_esc` 作用域）、`src/web/routes/persona_routes.py`（import/export）、`persona_media_routes.py`（上传 `autotag` 默认 / retag-all / 进度）、相册 VLM 打标任务、`i18n_packs/persona_studio.py`、对应 tests | **N-2** |
| 域包注册（grep `Domain hook registered` / `ConversionDomainHook` / `Domain persona set`）、`src/companion/goals/profile_slots.py`、目标模板种子、客户画像 schema（右栏「客户关系」字段集）、`goal_routes.py` 路由名、`src/utils/kb_store.py`（GXP 系统话术种子 + 分类）、`i18n_packs/goals.py`、对应 tests | **N-3** |
| `src/ai/tts_pipeline.py`（synth_verify / 疑似无声阈值 / `[tts]` 日志前缀）、`src/ai/avatar_voice.py`（只动判定）、`unified_inbox.html` **只动**业务助手语音面板（「已核有声」/「疑似无声」/「试听已过期」三处口径）、`personas.html` 语音 tab 体检回填（与 N-2 协调：N-2 做登记反馈，N-4 做体检数据）、对应 tests | **N-4** |
| `src/companion/goals/*`、`cp-goal.js` 双树、`goal_routes.py`（**只动** M-7 D 在途 hunk 所在处，先 `git diff` 认领） | **接手 M-7** |
| `tools/duty_*`、`deploy/field-agent/AGENTS_JUN*`、`deploy/**`、发版脚本、台账（只经端点 / `duty_*`）、`docs/**` | **N-5** |
| `skill_manager.py`、`drafts.py`、`store.py`、`autosend_worker.py`（M 批刚收口，本批不碰）、孤儿线、assistant 线、DY / QQ 线文件 | **谁都不碰** |

## 老板决策（默认按此执行；不同意的划掉，对应对话按划掉后执行）

| 决策 | 默认（= 我的建议） | 为什么 | 影响 |
|---|---|---|---|
| **D-N1** 业务域单一来源 + 陪伴域摸底标签 | **采纳 3NVM7Q**：首装选业务域一次，Domain hook / KB 分类 / 系统提示词 / 目标模板 / 摸底标签 / 客户画像全部跟随；陪伴域摸底标签 = 称呼 / 坐标 / 职业 / 年龄 / 兴趣 + 家庭状况 / 婚恋状态 / 收入水平 / 当前居住地 / 个人资产，删 6 个 BANT；收入 / 资产等敏感项默认不勾、AI 只可多轮自然带出禁止连问；「营销目标」字样统一「工作目标」；「标成交」的产品 / 金额字段陪伴域收起 | 日志实锤：`Domain hook registered: ConversionDomainHook` 与 `Domain persona set: 线上陪伴` 同秒——陪伴人设跑在销售域包上，#217「转化成交」模板、#240 KB 分类含命理、BANT 标签是同一个根 | N-3 |
| **D-N2** KB 首装预置 | **陪伴域不导入「系统话术·直接」GXP 支付条目**（`kb_store` 种子按域过滤）；存量标「系统预置」一键清除；「帮助语料 291 条」是悬浮球语料不进客户 KB，回访第三次写清 | J-9 只挡了 `knowledge_base.db` 随包拷贝，没挡 `kb_store` 自己的话术种子 | N-3 |
| **D-N3** 相册上传默认 AI 识别 | **默认 `autotag=True`** + 上传进度 + vision 不可达明确报错；保留手动「AI 补标」并在上传完成后弹引导；首次上传引导锚脸基准照 | 144 张零触发词 = AI 发图只能随机（K5XHJ2 325 张同病）；识图走官网网关（L-6 A 已修 num_ctx），成本可接受 | N-2 |
| **D-N4** 「立即发」语义 | **采纳 FHSZTA ①②③⑥**：立即发 = 同步直投绕过派发循环与错峰，返回结果并显示「已发出 hh:mm」或失败原因；bring_forward 至少置 due_at=now 并立即触发一次 tick；错峰偏移不作用于人工指定时间；卡片状态机 已排→已提前→排队中→已发出/失败 + 按钮防重复 | 用户点了三次「立即发」，85 分钟零动作、零反馈 | N-1 |
| **D-N5** 钧机取证脚本 | **由值守把 `zl_collect_jun.ps1` v1.3.1 文件直接发进群并明说覆盖**（I-6 F2 / K-5 ⑤ 遗留至今） | 他 v1.2.2 上传失败只能贴文本，且关键词抓不到 TTS 行 | N-5 |

## 共同纪律 / 省钱纪律 / 断点续传

与 J/K/L/M 批完全相同，见 `docs/修复指令_J批_并行_2026-09-05.md` 对应三节。三条硬红线再钉一次：

- 提交只用 `git add <明确文件>`；提交前 `git diff --cached --name-only` 为空再 add；⛔ `git add -A` / `-a` / `reset --hard` / `checkout .` / `restore .` / 裸 stash。**M-7 的教训：A/B/C 提交了但落点表一~四节空、总账行没改——每个 commit 落地后立刻补落点表一行，别攒到收工。**
- 开工先 `powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "…"`；收工 `-Done`。模板 / i18n / 静态 JS 热更新即上生产，中间态必须自洽。
- 每项一个 commit 带工单号；落点表写 `docs/发版对账_v1.0.77_N<N>.md`；总账只改自己那一行；对外答复一律走 `tools/duty_reply.py`。

## 接手 M-7 的话术（整块复制）

```
接手 M-7（#236 目标引擎可追溯与真出手）。先读 D:\boundless\docs\M批_进度总账.md M-7 行与 D:\boundless\docs\发版对账_v1.0.77_M7.md（§2 取证四问已答完；一~四节空）。核 A d0933356 / B 9ff2e90a / C fe6a7861 三个 commit 真在（B 查出 goals 包 logger 命名空间导致生产 INFO 全丢——落点表要写清）；git status 看 cp-goal.js（双树）与 goal_routes.py 在途 hunk 是否是 D 项「卡片说真话 + 单目标 stalled」——是就续做完并提交，不是就按 D:\boundless\docs\指令_M-7_目标引擎可追溯与真出手_2026-09-07.md §6 做 D。顺带把 skuio 04:17/04:19 两份（GTNG25 X6JMGZ）里「1.0.76 启动 04:01:37 / 04:12:13 / 04:12:57 三次」的原因查清（renderer.log / run_sentinel.json：壳拉起还是后端自杀）写进落点表。然后补齐落点表一~四节、总账 M-7 行收工、-Done。不改冲刺安全闸本身；每项一个 commit 带 #236；预算余额上限 $80。
```
