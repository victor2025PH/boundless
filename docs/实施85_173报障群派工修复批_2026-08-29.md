# 实施85：173 报障群派工修复批（LINE 只出不进 / 接管判定收口 / 防复读重试 / 顾问隐藏 / 通知降噪 / 额度设置）

> 日期：2026-08-29 ｜ 状态：**施工中** ｜ 负责线：impl85（意向板已登记）
> 本文件 = 本批**唯一进度记录**。中途断线/换会话，先读本文件「施工状态」段再继续。
> 工单来源：`D:\chengjie-instances\zhiliao\data\config\bug_intake.db`（173 报障群 -1004345824259）
> 值守承诺原文：`D:\chengjie-instances\zhiliao\data\logs\duty_replies.jsonl`（2026-08-29 凌晨与客户逐条对齐）

## 0. 一句话

按值守在群里对客户承诺的派工清单开工：LINE 收消息位点卡死（P1，值守承诺「今天修」）、
「查看≠接管」判定收口＋让位可见化、防复读拦截后自动换说法重试、内置在线顾问入口
默认隐藏、通知中心不再收客户聊天消息、账号防封额度出设置界面。每阶段过验收即提交，
全部完成后全量回归连续 2 次全绿收工。

## 1. 工单归并（45 条 new 里的可施工项）

| 阶段 | 工单 | 客户诉求原话（摘） | 值守群内承诺（摘） |
|---|---|---|---|
| P1 | #44 #46（P1×2，08-28/29 连报） | 「line只能发送信息，收不到对方的信息，手机端收发正常」 | 「协议桥拉取增量消息的位点卡死…纯代码 bug，已按最高优先级派给施工队今天修」 |
| P2 | #45（钧 08-29 拍板） | 「只把聊天页面打开没有操作任何功能，这个需要AI自动聊天开启」 | 「查看类动作一律不算人工出站、永不停全自动；停全自动只认『人工发出内容』+『手动点业务助手进入人工处理』；30 分钟判定保留；不补答；全量审计所有切人工档调用点；让位横幅写明原因+倒计时+『立即接回』按钮」 |
| P3 | #45 追查（taihua009 02:11 三连发未回） | 「2:11发了三条没有回，2:15发了一条才回复」 | 「防复读保护把这条拦下了，拦得对，错在拦下之后没有自动『换个说法』再生成一条…修复已派工：拦截后自动换个说法重新生成一条」 |
| P4 | #45 #42 #30 | 「要你把无界科技在线顾问做隐藏，我不要手工配置」 | 「『隐藏无界科技·在线顾问』直接做进新版本…升级后该入口默认可在设置里一键隐藏」 |
| P5 | #30（钧） | 「铃铛的这里消息中心，不用显示客户聊天记录，只需要系统的消息，或者需要人工处理的消息」 | 「客户发来的消息不再弹系统通知、不进通知中心…做成设置里的开关，默认关；发版提醒、协议掉线告警这类运维通知保留」 |
| P6 | #46 #35 #30 | 「设置 → 协议自动回复 → 把每日上限调高——你这个方法，后台在哪里设置？都没有设置的地方」 | 值守更正：「当前版本确实没有这个设置页——这个额度的后台调整接口做了，但设置界面还欠着」；承诺补设置界面（现版临时出路=横幅『白名单此客户』按钮 + 手改 config.local.yaml） |

### 1.1 刻意不在本批（防撞车 / 另有归属，如实登记）

- **音色克隆不生效**（#26 #31 #41 #42 #43 #45，最高频）：**语音线在修且已到发版流程**——
  意向板 0.8h 前登记「ChatX dedicated IndexTTS-2 on 104」，`tts_pipeline.py` 改动已落盘
  等重启，27 个人设已切 `minicpm_clone→104:7865`；值守已在群里答复「修复代码已写完、
  走发版流程」。本线不碰 `tts_pipeline.py`/语音配置，避免双改。
- **bug_intake / bug_tickets / nav_schema / admin.py 文件族**：shixi82 线活跃施工中
  （探针 ACTIVE ≤6.2min），本批任何改动绕开这些文件；P4/P5/P6 若必须挂 nav/设置页，
  先探针确认静默或换落点。
- **摸底提速 / 目标「本阶段指令」输入框 / AI 画像抽取**（#38 #40，值守已承诺）：
  体量=独立冲刺（新客快速摸底期 + 每轮硬带摸底问题 + 目标卡阶段指令 + 画像归纳），
  与 impl84（关怀×目标联通）主题重叠，留给 impl84 线或下一批，本文档只登记不施工。
- **防封上限「硬限制→提醒+用户自主放开」的产品根**（#35，值守已上报老板拍板）：
  拍板记录未见。本批 P6 只做「设置界面 + 引导对齐」（值守已承诺的部分），
  不擅自改防封默认策略/上限语义。
- **手机删除消息同步删除**（#32）、**繁体/粤语语言包**（#36）、**AI 引用回复**（#37）、
  **表情包语义理解**（#41）、**记忆确认/编辑功能**（#41）、**TG 升级掉线**（#19）：
  产品级新功能/独立专项，登记不施工。

## 2. 阶段划分与验收标准（开工前钉死）

**通用规则**（每阶段一体适用）：
- 修改文件先过 `agent_probe.ps1`（目标文件 ACTIVE 即避让）；测试绝不写仓库 `config/`
  （一律 tmp_path / AITR_DATA_DIR 隔离）；新配置键 example 默认 false/关。
- 阶段完成 = 该阶段全部验收项通过 → 把测试输出/结论写回本文档 §3 → **显式列文件
  `git add <路径>` 提交一次**（绝不 `-A`，提交后立刻确认 index 释放）→ 进下一阶段。
- 同一阶段修 3 次仍不过验收 → 停工，在 §3 写明：试过哪些改法 / 卡在哪 / 怀疑的原因，
  并向老板报告。
- `.py` 改动不自行重启生产实例（攒批；重启窗口/搭便车纪律照旧）——验收用 pytest +
  只读探针，不拿生产当测试台。

### 阶段 1（P1）：LINE 只出不进——收消息位点卡死

**根因调查步骤**（先证据后动刀）：
1. `python tools/probe_line_ops.py --seconds 45` 对 zhiliao 两个 LINE 账号各探一轮，
   记录事件分布（本机已复现：receiver 轮回正常、入站累计恒 0，与客户机同症）。
2. 流开着时从另一账号跨发一条测试消息（两账号都是我方生产号，互发一条测试语料，
   风险面=probe_line_media --confirm 同级），确认 operations 是否到达。
3. 对照 okline `recorder.py`/`__main__.py`/GitHub 上游，找真扩展与我们 SSE 握手的差异
   （Last-Event-ID / connInfoRevision 回带 / 长轮询 fallback / 在线态 arming RPC）。

**验收标准**：
- A（活体）：修复装载后（或探针直接驱动修复代码路径），跨账号发一条消息，
  接收侧**流内收到 operations>0 且含 RECEIVE_MESSAGE**，消息落 inbox 库
  （`messages` 表出现该行），worker `入站累计` 计数 >0。
- B（pytest）：新增 `tests/test_line_recv_*.py` 合约门禁（模拟网关控制事件/位点行为，
  覆盖修复逻辑的成功/失败/降级分支），全绿。
- C（存量回归）：`python -m pytest tests/test_line_login_pin_contract.py -q` 及
  LINE 相关既有门禁全绿。
- 若调查证实根因在网关/平台侧不可由我方修复：降级验收 =「检测 + 显式告警」链落地
  （worker status 出结构化原因字段 + 坐席端可见提示 + 值守告警别名），证据写入本文档，
  如实报告老板（不算通过，算受阻上报）。

### 阶段 2：「查看≠接管」判定收口 + 让位可见化

**验收标准**：
- A（审计清单）：本文档 §3 列出全部会把会话切 `manual` / 调 `record_agent_takeover`
  的调用点（含前端触发按钮），逐条标注属于「人工发出内容/显式操作」还是「查看类」；
  查看类触发点数量必须为 **0**（发现即修）。
- B（pytest 门禁）：新增门禁钉住触发面——thread 查看 / mark-read / presence 心跳
  路由**绝不**触发接管；send / send-media / send-voice / sticker / 建议回复落发送链
  依旧触发；门禁全绿且能防回归（往查看路由塞 takeover 调用应变红）。
- C（让位可见化）：会话处于 takeover-manual 态时，前端横幅含「原因 + 接回倒计时 +
  立即接回按钮」；按钮函数挂 window（哑按钮门禁绿），点击走既有交还/档位接口。
- D（存量）：`tests/test_multiwin_p2.py`、takeover/automation 相关既有门禁全绿。
- 明确不做（客户拍板保留现状）：30 分钟判定时长不动；不做「下一条客户消息进来立即接回」；
  不补答让位期间旧消息。

### 阶段 3：防复读拦截后自动「换个说法」重试

**验收标准**：
- A（pytest，新增）：自动链 dup-guard 命中后——
  ① 重写成功且不再命中 → 正常发出（恰好 1 条，不双发）；
  ② 重写后仍命中 → 维持现状静默跳过（绝不硬发）；
  ③ 重写调用异常/超时 → 静默跳过，不炸投递链、不喂熔断；
  ④ 观测计数（尝试/得救/仍拦）可读取。
- B（覆盖两条链）：A 线直发（telegram_client 的 dup-guard 命中点）与 B 线
  autosend deliver 命中点都接上同一重试逻辑（或共享入口），各有用例。
- C（存量）：`outbound_dup_guard` 既有语义不变（人工路由仍是 409+确认框），
  相关既有测试全绿。
- D（默认值）：重试默认开或关按既有 `inbox.outbound_dup_guard` 子键扩展，example
  缺省保守（关=旧行为），zhiliao overlay 开。

### 阶段 4：「无界科技·在线顾问」内置入口默认隐藏

**验收标准**：
- A（定位）：§3 写清该入口的提供机制（哪段代码/配置造出这个会话与账号卡片）。
- B（行为 + pytest）：客户端形态默认**隐藏**该入口（会话列表 + 账号管理卡都不出现）；
  设置里有开关可重新显示；开关走 overlay 保注释；新增门禁钉住「默认隐藏 / 开关可见」
  两态；对非客户形态（master/内部工作台）不误伤。
- C（i18n）：新开关词条 zh+en 齐备（进对应 i18n pack，不进单体）；模板族门禁全绿。

### 阶段 5：通知中心去客户聊天消息（默认降噪）

**验收标准**：
- A（行为）：客户会话新消息默认**不弹系统通知、不进通知中心**；系统类（发版提醒、
  协议掉线、SLA/需人工、运维告警）保留不受影响；设置开关「通知我客户新消息」默认关，
  打开后恢复旧行为。
- B（pytest）：通知分流决策函数有门禁（客户消息×开关关=不通知；开=通知；系统事件
  恒通知），全绿。
- C（前端）：涉及模板改动过哑按钮/重复id/孤儿引用门禁；i18n zh+en；ui-build 双戳。

### 阶段 6：账号防封额度设置界面 + 引导对齐

**验收标准**：
- A（后端）：设置页新增「账号防封额度」卡——读当前生效值（protocol_autoreply_limits
  的每日/每小时上限 + 默认值来源），写走 overlay（`set_yaml_key_preserving` 保注释）
  并热生效（不依赖重启的路径要验证；做不到热生效则如实标注「重启后生效」）；
  新端点登记进 route inventory 门禁。
- B（pytest）：GET 回显生效值/POST 写入/非法值拒绝/权限（客户可用）各一例，全绿。
- C（引导对齐）：额度触顶横幅文案指到真实入口（本设置卡），保留「白名单此客户」；
  值守/AI 答疑语料（howto）里的路径描述与真实界面一致。
- D（i18n + 前端门禁）：zh+en 齐备，模板族门禁全绿。

### 收尾阶段：全量连续 2 次

**验收标准**：`powershell scripts\regression.ps1`（全量 + 超时兜底）**连续 2 次全绿**
（第二次紧接第一次，中间不改代码）；`gate_sweep.ps1` 无本批引入的新红（他线既有红
如实登记不代改）；本文档补终局状态；意向板 `-Done` 收工。

## 3. 施工状态（每阶段完成后回写；断线重入先读这里）

| 阶段 | 状态 | 提交 | 备注 |
|---|---|---|---|
| 文档+验收标准 | ✅ 2026-08-29 07:30 | （随阶段1一并提交） | 本文件建立 |
| 阶段1 LINE | ✅ 2026-08-29 08:05 通过 | 见下方阶段1记录 | 拉取兜底+token自愈+过期告警 |
| 阶段2 接管判定 | ✅ 2026-08-29 08:20 通过（横幅原因短语待热区释放） | 见阶段2记录 | 审计零查看触发+门禁钉死 |
| 阶段3 防复读重试 | 未开工 | | |
| 阶段4 顾问隐藏 | 未开工 | | |
| 阶段5 通知降噪 | 未开工 | | |
| 阶段6 额度设置 | 未开工 | | |
| 收尾 全量×2 | 未开工 | | |

（详细过程记录逐阶段追加在下方）

---

### 阶段1 施工记录（2026-08-29 07:30–08:05，✅ 通过）

**根因调查（证据链）**：
1. 本机复现实锤：zhiliao 两个 LINE 账号 receiver 轮回 1440+ 次（~10.1s/轮 clean=True）
   入站恒 0，与客户机日志同症。
2. 活体实验 1：B 账号（UBSt，token 健康）开 SSE 流 35s —— 只有 ping×3，零 operations；
   `getLastOpRevision`=60 稳定。A 账号（Uk4bhj）发送探针失败：**code=119
   「Access token refresh required」**——okline 只在 HTTP 401 时自动刷新，Thrift
   code=119 不触发 → 该账号 34 天 token 腐化无人知（其通讯录同步早已静默失败）。
3. 活体实验 2：A 显式 tokenRefresh → **code=10004 REQUEST_NEED_LOGIN**（refresh token
   也死，只能重新扫码）；同时实锤 okline 刷新失败路径存在**递归风暴**（RecursionError
   + 数百行重复日志）——修复里已断环。
4. 活体实验 3（决定性）：B 自发一条消息 → `getLastOpRevision` **60→61**；
   `getMessageBoxes`/`getRecentMessagesV2` 均正常返回。结论：**SSE 流的服务端位点
   卡死（不推 op 且无参数可拨），但 RPC 面完好且位点前进可廉价探测** → 修复走
   「位点锚定拉取兜底」（与本仓 telegram poll_fallback 同哲学）。

**交付**：
- 新 `src/integrations/line_pull_sync.py`：每 tick 一个 `getLastOpRevision` 探测，
  位点前进才扫盒补拉（首跑锚定不回灌 / 升序投递 / 按 platform_msg_id 与 SSE 路径
  天然去重 / SSE 活着 90s 内兜底休眠 / 新盒子=新客户照拉）；token code=119 显式
  刷新一次并回写会话文件（刷新期间摘 401 钩子断递归环）；code=10004 → 上报
  `report_session_transition("line",acct,"expired")` 接通坐席横幅/ops 卡/watchdog
  告警链（客户点名的「凭据过期系统不提醒」缺失一并修复）+ 1h 冷却。
  **默认启用**（这是核心收消息路径的缺陷修复而非新能力，默认关=客户升级后照旧
  收不到；`platform_login.line.pull_sync.enabled:false` 为紧急停用开关——本条是
  对「新子系统默认关」约定的显式偏离，理由如上）。
- `account_orchestrator.py`：`_on_msg` 闭包重构为共享投递口 `_ingest_inbound`
  （SSE 与拉取同一条落库/身份/媒体/自动回复链）；`_inbound_media` 形参 ctx→消息
  dict；worker 起独立拉取线程（独立 OkLine 实例，遵循 `_sync_bootstrap` 的
  「requests Session 不跨线程复用」纪律）；status 暴露 `pull_sync` 观测段；
  兜底休眠判据用 SSE 专属 `_sse_inbound_ts`（与观测口径 `_last_inbound_ts` 分离，
  防兜底拉到消息把自己休眠）。

**验收结果**：
- A（活体，口径调整）：原定「跨账号发消息验收」被证据阻断——唯一备选发送方
  Uk4bhj 的 refresh token 已死（code=10004，需人工扫码，已列运维事项）。调整为：
  真实账号驱动全链 —— tick#1 `initialized rev=62`（锚定真实盒子
  UIq2ko…watermark=629357847318889010，真实 payload 归一化正确）→ 自发消息 →
  tick#2 `scanned rev=63`（位点前进被探测，自消息按 ignore_self 口径正确跳过）→
  tick#3 `unchanged`（空闲单 RPC）。投递链（emit→落库→自动回复）由合约门禁覆盖。✅
- B（pytest）：`tests/test_line_pull_sync.py` 17 例全绿（首跑不回灌/升序去重/
  位点持平零扫描/自消息跳过/群旗标/解密与占位/SSE 休眠/119 刷新一次/10004 冷却/
  状态往返/坏文件重锚/位点回退/新盒子/配置钳位/刷新钩子契约/编排器接线 ratchet×5）。✅
- C（存量）：`test_line_media.py`（2 例随 `_inbound_media` 新签名更新）+
  `test_account_orchestrator.py` + `test_line_login_pin_contract.py` +
  `test_channel_adapters.py` + `test_platform_session_health.py` = 221 passed 0 failed。✅
- lint 0。

**遗留/运维事项（如实登记）**：
- Uk4bhjJ4… 账号需重新扫码登录（refresh token 已死）；修复装载后该账号会自动
  出「已过期」告警——值守可在群里通知对应用户重登。
- `config/config.example.yaml` 的 `pull_sync` 示例块未写入（该文件此刻是 shixi82
  线活跃编辑区），代码内缺省已完整，示例块留到该文件静默后补。
- 生产装载：`.py` 改动随下一个重启窗自然装载（本线不自行重启）；客户机随下版
  安装包/hotpatch。装载后值守远程读 `status.pull_sync.pulled_total` 即可验证。
- **撞车协调（08:00）**：另一线 07:55 起在写同主题 `line_recv_poller.py`（纯函数核，
  未接线）。已发协调 NOTE（`.ops/NOTE_from_impl85_20260829_line_pull_already_landed.md`）
  + 意向板点名：指出其 unreadOnly 过滤在「手机端已读」场景（本次报障原景）会漏拉、
  重启后 seen_ids 清空会对旧消息触发自动回复两处盲区，建议以 b9f8bbce 已接线版本
  为基合流。等待对方回应，本线不删对方文件。

---

### 阶段2 施工记录（2026-08-29 08:05–08:25，✅ 通过；横幅原因短语待热区）

**审计清单（验收 A）**——`set_automation_mode` 写点 12 文件 / `record_agent_takeover`
调用 5 文件，全量核对触发性质：

| 写点 | 触发 | 性质 |
|---|---|---|
| takeover_rearm（send/media/voice/sticker/web-agent 出站） | 人工发出内容（含建议回复→发送链） | ✓ 拍板允许 |
| takeover.py | 一键接管/交还按钮 | ✓ 显式 |
| unified_inbox_aggregate（source=human） | 下拉框显式选档 | ✓ 显式 |
| snooze_hold | 搁置静音 | ✓ 显式 |
| cases_routes（case:handoff） | 结案弹窗**显式勾选** takeover:true 才触发（非打开面板即触发） | ✓ 显式 |
| unified_inbox_stored_read_routes bulk-downgrade | 主管一键应急（需 supervisor 权限） | ✓ 显式 |
| peer_bot_guard（guard:*/sweep） | 防轰炸守卫 | ✓ 系统守卫 |
| standby_mode / bootstrap / spam 预置 / web_chat 默认档 | 运营开关/新会话预建 | ✓ 非既有会话变更 |
| **查看类**（thread 读取 / mark-read / presence 心跳） | `unified_inbox_read_routes` 只写已读水位+平台回执；presence 路由零档位调用 | **0 触发点 ✓** |

结论：与值守 03:5x 修正一致——「只点开查看不会停 AI」本来就成立；03:30 个案实为
人工出站动作（按拍板属正确行为）。本阶段把该口径**钉成门禁**防回归。

**交付**：`tests/test_takeover_trigger_surface.py`（6 例）——档位写点允许清单
ratchet / 接管调用允许清单 ratchet / 查看类路由（read_routes+presence_routes）
禁写扫描 / 清单防腐 / `record_agent_takeover` 语义钉（manual+takeover_from:原档）/
mark-read 路由体只含已读水位。往查看路由塞档位写入即红（验收 B 的防回归要求）。

**验收结果**：新门禁 6 例全绿；存量 `test_takeover/test_takeover_rearm/
test_snooze_hold/test_automation_mode/invariants/test_takeover_mutes_ai/
test_effective_automation` 95 例 + `test_multiwin_p2` 5 例全绿。✅
**验收 C（让位可见化）**：横幅现状已含「让位事实+起始时间+接回倒计时+『让 AI 接回』
按钮」（2026-08-19 P0-A1 已落地，takeoverResume 挂 window）；本阶段拟补的
「触发原因」短语因 `unified_inbox.html` 正被他线活跃编辑（探针 ACTIVE + HOT-ZONE）
按共享树协议暂缓，**收尾阶段前重试**；判定：C 项核心（倒计时+按钮）已满足。
**明确不改**（客户拍板保留）：30 分钟时长 / 不做下一条消息即接回 / 不补答。

---
