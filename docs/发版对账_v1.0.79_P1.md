# 发版对账 v1.0.79 · P-1 起草即净化 + 引用锚点守卫 + 「AI 指纹」卡（#259 #254）

> 指令：`docs/指令_P-1_起草即净化与引用锚点_2026-09-08.md`；底座 `docs/修复指令_P批_并行_2026-09-08.md`（归属矩阵 P-1 行）。建在 O-1 B（`583af62b` `outbound_humanize.humanize` 挂发送门）/ C（`ea20cefd` `persona_guard.rewrite_service_tone` 「改写一次 → 仍命中转人审」）之上，不重做。证据 `tmp_diag\ZH3ZQ5\20260908-144048_report_report.md`（14:37:43 opener「that hiking trail you mentioned a while back —」，clean 重装记忆 0）、`tmp_diag\U4ZAD7\20260908-145021_report_report.md`（≈80% 回复带 —）。
> 执行：2026-09-08 15:46 开工（`agent_probe -Intent` 已登记）。**每个 commit 落地后立刻补一行。**

## 一、根因（一句）

| 报告 | 症状 | 根因 |
|---|---|---|
| U4ZAD7 / Y3C6PS | 1.0.77 草稿 / 出站仍带 — | O-1 B 未装载是一半；另一半：B 只挂**发送门**，工作台草稿 / L1 审核稿 / 关怀预览显示的仍是 LLM 原文 |
| ZH3ZQ5③ | 开场白「你之前提过的徒步路线」——记忆 0、人设 0 | 出站链没有「引用必须有锚点」的守卫；O-1 E 只管迟回理由 |
| 34585H E/F | 生成侧无硬禁 / few-shot、无质检读数 | 提示词只有底稿禁令；无起草层日志与四格读数 |

## 二、落点（每 commit 一行，落地即写）

| 项 | commit | 落点 |
|---|---|---|
| **A + B** 起草即净化 + `claim_guard` + 四格计数底座 | `2d9fb3f0` | **新** `src/inbox/claim_guard.py`：纯函数 `check_claims(text, *, history_texts, memory_facts, lang, seed) -> (text, report)`——三语引用短语表（EN `you mentioned / you said / you told me / as·like you said / last time you·we / you … a while back·ago / remember when·how·that / do you remember / you always say / the other day you`；ZH `你之前·以前·上次·上回·前几天…说·提·讲·聊(过·到·起·的) / 你说过·提过·提到·说起·讲到·聊到 / 你不是说 / 记得你·咱们·那次·上次 / 像你说的·正如你说·如你所说·你常说·你老说·你一直说`；JA `前に·以前·前回·この前·あの時·先日 言ってた·話してた·教えてくれた / 覚えてる / 言ってた通り / あなたが·君が言った`）→ 按句抽实体（EN 去停用词后**词干**前缀匹配 `\bhik`→hiking；中日文去停用字后 **2 字片段**）→ 在 `history_texts`（最近 200 条会话，双向）+ `memory_facts` 找锚点 → 找不到：**整句删**，其余句子未在提问则补一句中性问（`_NEUTRAL` 9 语模板，`seed`=conv 取模稳定：en `how have you been? / what have you been up to? / how's your week going?`，zh `最近怎么样？/ 这几天都在忙什么？/ 最近过得好吗？`，ja `最近どう？/ 元気にしてた？/ 最近何してた？`）；找到：放行 `anchor=history|memory`。**误伤保护**：引用短语在引号（"…" “…” 「…」）里不动；整句与历史某条逐字相同不动；抽不出实体的裸引用（"like you said"）只在**零历史**时改。`log_report` → `[claim-guard] conv=… stage=… phrase=… keywords=… anchored=1|0 anchor=… action=pass|rewrite`（clean 不记）。`history_texts_for(store, cid, 200)`（`list_recent_messages`，软删 / 撤回行 store 已剔）；`memory_facts_for(chat_key, account_id, store=)`：`set_memory_facts_provider` 注入优先，否则**只读**反查 `bot.db` `episodic_memory`（`memory.db_path` 或 `inbox.db` 同目录；键 `chat_key` / `acct:chat_key` / `*:chat_key`，`status` 非 active 不算）——与 O-1 B `deferred_verbatim_pending` 同款口子。**`src/inbox/outbound_humanize.py`**（P-6 接手前接口变化在此）：新 `apply_draft_humanize(text, *, conversation_id, lang, origin, stage, draft_id, history_texts, memory_facts, cfg_root, mode) -> (text, meta)`——先 `check_claims`（去谎）再 `humanize`（去标点 / 句式，与发送门同一函数同一配置 `inbox.l2_autosend.humanize`），`origin ∈ BYPASS_ORIGINS`（verbatim / manual / human / identity）或配置关 → 原文 `skipped=True`；**每稿一行** `[draft] conv=… stage=enrich|care draft=… origin= lang= len= claim=clean|pass|rewrite punct_fix=n style_fix=n trimmed=n dash=n fp= preview=`；`meta` = `punct_fix / style_fix / trimmed / dash / claim_action / claim_phrases / claim_anchor / skipped`。发送门 `apply_outbound_humanize` 保留兜底，新增：`punct_fix>0` → **`[outbound-leak] conv= stage= origin= punct_fix= dash=` WARNING 一行**（说明有绕过起草层的路径——协议直发 / 未挂点的生成口）+ 计入 `gate_leak`；`_service_tone_pass` 复检传 `record_stats=False`。**`src/inbox/drafts.py` 只加一处**（`enrich_draft`，O-1 E 迟回守卫之后、`effective_risk` 之前；P-2 年龄闸在 `auto_generate_draft`，不相邻）：`apply_draft_humanize(reply, conversation_id, draft_id, stage="enrich", lang=reply_lang or draft_lang, history_texts=history_texts_for(store, cid, 200), memory_facts=memory_facts_for(chat_key, account_id, store))`，净化后为空回退原稿——这里是 B 线全部 AI 稿（自动回复 / opener / L1 审核稿）**写入 draft 表前**的唯一落点（`auto_generate_draft` 落的是规则模板占位，AI 正文经 `finalize_draft_enrichment` 才进表）。**`src/contacts/care_dispatcher.py`**（关怀 + 目标冲刺共用出口）：AI 分支 `_avoid_disliked` 之后、`check_care_reply` 事实校验 / 强制预览 / 入队之前同一净化（锚点 = `context_block` 会话要点 + `source_text` 客户原话 + `topic` + `memory_block` + `goal_block`；`stage=care`，`draft_id=care:<id>`）→ 预览卡 `sent_text` 与 deferred 行都是净化后文本；verbatim 分支不经过。**新** `src/inbox/ai_fingerprint_stats.py`：`record_draft(punct_fix) / record_service_tone(action) / record_claim(action) / record_gate(punct_fix) / record("promise_checked"|"promise_no_action")`（P-3 落承诺日志时调后两键，卡片自动有值）；进程内事件环 + `logs/ai_fingerprint/fp_YYYYMMDD.jsonl`（相对 CWD＝实例数据根，`AITR_AI_FINGERPRINT_DIR` 覆写）首次读数回填近两天（record 也先回填，不重复计）；`snapshot(hours=24)` 四格各 `hits / total / rate_pct / level`（0 绿 / <2% 黄 / ≥2% 红 / 无样本 na）+ `gate_leak` + `level` 总灯。**`src/utils/persona_guard.py`**（O-1 C 文件，O-1 已收工无人在途）：`rewrite_service_tone(text, *, formal_you=True, record_stats=True)` 外壳计 `svc_checked / svc_hit`，实现搬到 `_rewrite_service_tone_impl` 逐字不变；`enrich_draft` / 协议链默认计、发送门复检不计。**tests** `tests/test_claim_guard.py` 27 例 + `tests/test_draft_humanize.py` 8 例（见 §三）；`tests/test_care_dispatcher.py` 两例把 `context_provider` 从 `"ctx"` 改成含被引用事实（「想换工作」「复查」）——AI 稿引用「你提过换工作」现在必须有锚点，测试口径随之。装载：全 `.py` **需重启**；`humanize` 配置键热读。 |

## 三、验收

| 项 | 门禁 | 结果 |
|---|---|---|
| B 回放 ZH3ZQ5 | `tests/test_claim_guard.py::test_replay_zh3zq5_no_history_no_memory_rewrites_to_neutral` | 零历史零记忆 → 「that hiking trail you mentioned a while back —」整句换中性问句、无 — 无 mentioned 无 hiking、同 seed 稳定；`keywords=hik|trail` |
| B 有锚点放行 | `test_history_with_hiking_anchors_and_passes` | 历史「we did a hiking trip」→ pass anchor=history；记忆「likes hiking on weekends」→ pass anchor=memory |
| B 三语 | `test_trilingual_unanchored_rewritten` ×9 / `test_trilingual_anchored_pass` ×9 | en / zh / ja 各 3 例：无锚点改写（引用短语消失、末尾是问句）；给一条相关历史 → 逐字放行 |
| B 误伤 | `test_quoted_customer_words_not_touched` / `test_verbatim_restatement_of_history_not_touched` / `test_everyday_text_zero_hits` / `test_bare_reference_like_you_said_passes_only_with_history` | 「You literally wrote "you mentioned it twice"」不改；「你说「你之前说过的」这句我笑了」不改；复述历史原句不改；「I went hiking a while back」「Are you busy tonight?」「你说得对。」零命中；「Like you said」有历史放行、零历史改 |
| B 记忆反查 | `test_memory_facts_bot_db_readonly_fallback` | 临时 `bot.db`：`whatsapp:acct1:447…` / 裸 `447…` 两键命中、`ignored` 行不算、他人不算；无 bot.db → 空；provider 优先 |
| A 起草层 | `tests/test_draft_humanize.py::test_apply_draft_humanize_claim_then_punct` / `..._trims_and_counts_style` | 先 claim 再 punct；有锚点只去 —（`, did you`）`punct_fix=1 dash=1`；`[draft]` 两行 `claim=rewrite` / `claim=pass punct_fix=1`；`[claim-guard] … anchored=0 anchor=- action=rewrite`；分号 / isn't it / 感悟句 → 无分号无反问、末句保留提问 |
| A verbatim / 人工不净化 | `test_bypass_origins_not_humanized` | verbatim / manual / human / identity 四种 origin 原样 + `humanize=skip` 四行；None 透传；`humanize: false` 原样 |
| A enrich_draft 真库 | `test_enrich_draft_stores_sanitized_text` | ① 首次接触 + ZH3ZQ5 → 落库文本中性问句、`[claim-guard] conv=whatsapp:acct1:u1 … action=rewrite` + `[draft] … claim=rewrite`；② 历史含「went hiking on that trail」→ 放行只去 —；③ 「Just made coffee; … Life is really about … What's your plan today?」→ 无分号、感悟句删、末句保留；**发送门再过一遍 = 幂等 `punct_fix=0`、无 `[outbound-leak]`** |
| A 发送门兜底 | `test_send_gate_leak_warns_once_per_text` | 带 — 的文本到发送门 → 仍净化 + `[outbound-leak] … punct_fix=1` WARNING 一行 + `gate_leak hits=1/total=1` |
| D 底座 四格 | `test_fingerprint_snapshot_levels_and_persistence` / `test_service_tone_guard_counts_at_draft_not_gate` | 100 稿 0 — → 0% 绿；1/101 → 黄；6/106 → 红；客服腔 1/2 红；引用 1/2；承诺 `ready=False` → P-3 记一条即 `ready=True`；30h 前不计；进程清零后从 JSONL 回填数值一致；`record_stats=False` 那次不计 |
| 接线静态钉 | `test_wiring_static` | drafts.py 恰一处 `apply_draft_humanize(`，位于迟回守卫之后 / `effective_risk` 之前；care_dispatcher 恰一处，位于 verbatim 分支之后 / `_avoid_disliked` 之后 / `check_care_reply` 之前；发送门复检 `record_stats=False` |
| 相邻回归 | `test_outbound_humanize`(34) `test_late_reply_excuse_guard` `test_companion_prompt_service_tone` `test_persona_guard` `test_drafts_risk_policy` `test_stop_contact_hard_stop` `test_protocol_autoreply` → **273 绿**；更大一圈 43 文件（care / enrich / autodraft / draft_risk / persona_guard / humaniz / claim / stop_contact / late_reply / draft_quality / sprint）**1055 绿 + 4 红均既有非本线**：`test_autodraft_bootstrap_persists_auto_ai`（O-1 落点表已登记：bootstrap 写档位带 source）、`test_care_prompt_context` ×2（`_compose_extra_blocks` 丢 `memory_block`——本线未动 care_prompt，用 `build_care_prompt` 直接复现与 dispatcher 无关）、`test_protocol_contacts_enriched::test_pipeline_guard_shares_the_one_service_list`（pyrogram 导入无事件循环，环境） |

## 四、越界 / 未做 / 待老板 / 交接

- **drafts.py 「一处」的实际位置是 `enrich_draft` 而非指令写的 `auto_generate_draft`**：后者落库的是规则模板占位（`_suggestions` 首条）与告别模板，AI 正文只在 `enrich_draft` 经 `finalize_draft_enrichment` 进表——挂在 `auto_generate_draft` 净化不到 AI 稿。P-2 的年龄闸在 `auto_generate_draft` 入口段，两处不相邻，`git diff` 已核只含本线 hunk。
- **draft 行无 meta 列**：指令要「punct_fix / style_fix 存到 draft meta」——`reply_drafts` 没有 meta / extra 列，`finalize_draft_enrichment` 也无 meta 参数；写进 `quality_breakdown` 需在 `enrich_draft` 再动第二处（`update_draft_quality` 那行），为守「只加一处」未做。计数以 `[draft]` 日志 + `ai_fingerprint_stats` JSONL 为口径（卡片读的就是它）。
- **未罩的生成口（发送门兜底 + `[outbound-leak]` 会指出来）**：① 工作台「重新生成」/ 人设试聊路由 `drafts_routes.py` L796 / L921 直接 `upsert_draft`（不经 `enrich_draft`；归属不在 P-1）；② 协议链 `protocol_autoreply.run_autoreply` 直发（不落 draft，发送门净化即最终文本，草稿预览不存在——无「预览≠发出」问题，但 claim_guard 未挂）；③ A 线 Telegram `skill_manager.py` GreetingSkill（HOT-ZONE，O-1 E 同口径不碰）。装载后 grep `[outbound-leak]` 若持续出现 `stage=protocol` 就是 ②，补挂一行即可。
- **care 的客服腔计数**：care 稿不经 `enrich_draft`，客服腔只在发送门复检（`record_stats=False`）→ 卡片「客服腔命中率」不含关怀稿；关怀稿本身仍被发送门剥。
- **`persona_guard.py` 是 O-1 C 的文件**（归属矩阵 P-1 行未点名）：只加 `record_stats` 外壳，实现逐字搬到 `_rewrite_service_tone_impl`；O-1 已收工、意向板与 git status 均无人在途，登记备查。
- **`test_care_dispatcher.py` 两例改口**：AI 稿「记得你提过想换工作」在 `context_provider` 只给 "ctx" 时按新守卫属无锚点引用 → 测试把 ctx 改成含被引用事实。这是守卫的正确行为（care 的 LLM 也会编「你提过 X」）。
