# 指令 I-6：61 单回访积压冲刷 + zl_collect 下发 + 台账校正 + 监控补窟窿

> **预算硬上限 $190。** 正常花费应在 $30–50 —— **本条零业务代码风险，是最便宜的一条，
> 可以和任何其他对话并行跑。**
>
> ⛔ **任何写数据库的动作先 dry-run 打印将要改什么，等我点头再执行。**
> 台账是运维判断「这个号这周被风控几次」的依据，掺假比没有更糟。
>
> **F1 是本批对整体信任度影响最大的一项**（比任何代码修复都大），先做它。

---

## 0. 开工前必读（两份，不可跳过）

1. `docs/I批_进度总账.md` —— **先看 I-6 那一行**，已完成项一律不重做。
2. `docs/修复指令_I批_并行_2026-09-04.md` 的「共同纪律」+「省钱纪律」+「断点续传协议」。

**读完立即从 F1 开工，不要先输出整体计划。**

## 1. 红线与边界

- 提交只用 `git add <明确文件路径>`。**禁止 `git add -A` / `git add .` / `git commit -a`**。
- ⛔ **绝对禁止** `git reset --hard` / `git checkout .` / `git restore .` /
  不带路径的 `git stash push`。只回滚自己的：`git checkout -- <明确路径>`。
- ⛔ `src/ops/bug_intake.py`（1268 行）**先 Grep 后读窗口**，禁止整读。

**文件边界：**
- ✅ 你可以改：`deploy/duty/**`、`deploy/field-agent/**`、`src/ops/bug_intake.py`、
  `tools/duty_*`、`docs/**`、台账数据（**dry-run 后经我确认**）
- ⛔ **不改任何业务代码**。你在本条里发现的业务缺陷，写进结束报告分派给 I-1..I-5，
  不要自己动手。

**开工先登记意向板：**
```
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "I-6 duty ops: 61 notify backlog / zl_collect rollout / ticket audit"
```

---

## 2. F1（最高优先）—— 冲刷 61 单回访积压

### 事实

台账 `D:\chengjie-instances\zhiliao\data\config\bug_intake.db`：
`status='fixed' AND notify_ts=0` 共 **61 单，全部已有 fix_note** —— 修复文案早就写好了，
**一条都没发出去**。

按人分：**skuio（花无缺）47 单、钧 14 单**。

**这就是 #64 被复报 3 次、#145 里那句「这个问题为什么一直没有解决」的直接原因**——
问题修了，但从来没有人告诉他们。用户看到的世界是「报了没人管」。

### 接口齐备（不用自己造）

- `src/ops/bug_intake.py` L616 `build_fix_notify_text`
- L651 `mark_notified`
- L697 `list_pending_notify`

### 做三件事

**① 查清冲刷动作为什么从未执行。** 三个可能：没有 worker / worker 没起 /
被 gate 在「发版后才回访」这个条件上。**如实说结论** —— 如果是流程缺失就补计划任务
（`deploy/duty/**` 下加，照现有 duty 任务的写法）。

这一问比冲刷本身更重要：不查清楚，下一批 61 单还会这样躺着。

**② 按人合并成一条汇总回访。**
47 条逐条推是**轰炸**（对方会直接静音这个 bot，那就彻底失去反馈渠道了）。
合并成一条：

> 「你之前报的这些问题已经修好了，随 1.0.72 上线。麻烦重点验这几项：<列 3–5 个最关键的>」

⚠ **两份文案（skuio 一份、钧一份）先 dry-run 打印给我看过再发。**

文案里必须包含的三样：
- 已修清单（工单号 + 一句话，别贴技术细节）
- **需要用户额外操作才生效的项要单独标出来**（见 §4 的 #67 相册重传）
- 请他们优先验的 3–5 项（列 47 项等于没列）

**③ 回访后按 `mark_notified` 记账**，让 `verified` 自动闭环链（`bug_intake.py` L33）
能正常工作。不记账的话下一轮又会把这 61 单当成待回访。

---

## 3. F2 —— zl_collect v1.3.1 下发两台坐席机

### 根因已查明并修好，但没下发

`d46784cc`：`x-diag-meta` 头里含中文备注 → PowerShell 5.1 的 `Invoke-RestMethod`
发送前就抛 `invalid Control characters`，**服务器侧零请求**（所以服务端日志里什么都没有）。
0903 两台机 6 次 `UPLOAD_FAILED` 全部是它。

**但两台机还在跑 v1.2.2。** 修好的代码在仓库里，坐席机上不是。

### 做

按 `docs/` 里 SOP 的 §K「剧本改版必重发纪律」下发：
- `deploy/field-agent/zl_collect_jun.ps1`
- `deploy/field-agent/zl_collect_skuio.ps1`

**明说覆盖旧文件**（不说的话对方会以为是新增，两个版本并存更糟），
让两人各跑一次 `-Task selfcheck` 确认 `CODE=` 出短码。

### 验收

1. 两机各一次 selfcheck **上传成功**
2. `-Task reupload` 可单独重传（这是他们下次遇到问题时的自救手段）

---

## 4. F3 —— 台账销单与状态校正

**① #36 状态是错的**（现在是 `new`）。
AI 出向繁体 / 粤语在 8/29–8/30 已交付。改为 `fixed` + fix_note 写清：

> 「会话里设『发→繁体中文 / 粤语』即生效；**界面本身仍是简体**（那是另一件事）。」

这句括号很重要 —— 不写清楚，用户改了设置看界面还是简体，会以为又骗他。

并进 F1 的回访。**剩余的「地区语气自适应」半边另立新单指向 I-4**，
不要混在 #36 里（混在一起会导致 #36 永远关不掉）。

**② 复核四单的 fix_note 与代码实况是否一致：**

| 单 | 内容 | 特别注意 |
|---|---|---|
| #61 | 人设应用到多账号 | |
| #63 | 按账号管接管方式 | |
| #67 | 相册三层齐修 | **需用户一次性重传相册才生效** |
| #64 | 出站混语守卫 | 被复报 3 次，回访文案要格外具体 |

这四单都是「**代码称已修、用户从未验收**」。

⚠ **#67 的回访文案必须写明「请重传一次相册」这个操作步骤** ——
不写的话用户重传前会一直以为没修，然后第 4 次复报。

**③ #145 是复合单（5 件）**，按件拆分，或者在 body 里标清哪几件已修、
哪几件转 I-5。现在这样一张单里 5 件混着，永远无法判定能不能关。

---

## 5. F4 —— 监控自身的两处窟窿

**① VPS 探针反复失败**（见 `deploy/duty/logs/duty_watchdog_*.log` 与
`deploy/instances/net_probe_117.ps1`）。查清是**探针脚本问题**还是**链路问题**。
别让红噪音常态化 —— 常态化的红灯等于没有红灯，下次真出事没人看。

**② 群里 mid=1156 的未应答提问**已经告警过但仍未应答，`DutyWatchTick` 每 2 分钟
在重复播报同一条。**要么答复，要么销掉** —— 别让告警变成背景音。
顺便看看这个「重复播报同一条」本身要不要加去抖（告警发过 N 次未处理就升级 / 静默，
而不是无限重播）。

**③ 两份未提交的改动要定归属：**
```
git -C D:\boundless diff --stat -- engines/chengjie/src/inbox/health_watchdog.py engines/chengjie/src/inbox/webhook_notifier.py
```
`health_watchdog.py`（+95 行）与 `webhook_notifier.py`（+34 行）是谁的、要不要提交？
**未提交的工作在这棵树上是随时被误伤的裸奔状态**（2026-08-29 那次裸 stash 卷走了
169 个未提交文件）。查 `git log` 看最近谁在动这两个文件，或者在意向板上找主人。
**不是你的就不要提交**，写进结束报告点名。

---

## 6. 交付 + 换账号怎么接

- 落点表 `docs/发版对账_v1.0.72_I6.md`
- 更新 `docs/值守症状族对账单.md` 与 `docs/报障对账_钧_花无缺_20260904.md` 的对账结果
- **更新 `docs/I批_进度总账.md` 的 I-6 行**
- 收工清意向板：`scripts\agent_probe.ps1 -Intent "I-6 duty ops: 61 notify backlog / zl_collect rollout / ticket audit" -Done`
- 结束报告：
  1. **61 单为什么从没被冲刷过（结论）** —— 这是本条最有价值的产出
  2. 两份回访文案实际发出的版本
  3. 两机 selfcheck 结果
  4. 四单复核结论（一致 / 不一致哪里）
  5. `health_watchdog.py` / `webhook_notifier.py` 的归属结论
  6. 本条发现但不该你动的业务缺陷 → 分派给哪条指令

**下一个账号的新对话粘这段：**

```
接手 I-6（61 单回访冲刷 + zl_collect 下发 + 台账校正 + 监控补窟窿）。按顺序做三件事：
1. 读 D:\boundless\docs\I批_进度总账.md 找 I-6 行，看已完成到哪
2. 读 D:\boundless\docs\发版对账_v1.0.72_I6.md —— 这是断点，逐行核对
3. 查台账实况：D:\chengjie-instances\zhiliao\data\config\bug_intake.db 里
   status='fixed' AND notify_ts=0 还剩几单（这是 F1 进度的唯一事实源）
然后读 D:\boundless\docs\指令_I-6_值守流程与积压_2026-09-04.md，从第一个未完成项继续。
⛔ 不改任何业务代码；任何写数据库的动作先 dry-run 给我看，等我点头再执行。
预算上限 $190（含前一轮已花的，按总账里记的余额算）。
```
