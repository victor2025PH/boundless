# 指令 Q-5：抽取两链合一 + 昵称解析（画像不再永远为空）

> 预算 **$150**。工单 **#263**（GAWRU5 / 9APUMX / T5PGMG；Q-0 拆单后以本体单号为准）。目标版本 1.0.79（第一波）。
> 底座 `docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-5 行、接口约定①）。落点表 `docs/发版对账_v1.0.79_Q5.md`。

## 开工前必读

1. `docs/聊天核心能力_三视角方案_2026-09-09.md` §1 Q7、§2.1 第 7 条、§3 Q-5。
2. 证据 `tmp_diag\GAWRU5\ 9APUMX\ T5PGMG\`：目标卡黄条「采集不可用：画像 AI 抽取未开（companion.goals.profile_llm）」；23:16–23:48 全局零 `[profile]` 写入；`[episodic] extract done heuristic_count=0 llm_count=0`（含 163 字消息）直到 00:10 首现 `llm_count=1`；联系人昵称「Michael/46/New york/union carpenter/5.28」含年龄 / 城市 / 职业未解析。
3. 代码事实：O-2 A `ad5c7dbd`（`memory.extract.{enabled,intents,use_llm}` 进基线——**核实 use_llm 的基线值**）；O-2 B `45b56b64`（`manual_out` 同抽）；O-2 C `6b42dc10`（`memory_scope` 私事只进客户记忆）；`goals/service.py` `capture_status`（`profile_llm_off` 注释写明「客户自由表达的回答不会自动记入画像，只剩固定句式正则」）；`skill_manager.py` L5584 `[episodic]` 抽取调度；O-3 B/C 的 `profile_slots.py`（槽位表 / CUE_LEXICON）。
4. 接口：画像字段 schema `{value, source: user|nickname|ai_inferred|confirmed, status: unknown|mentioned|confirmed}`——你写、Q-1 读；容忍旧格式。基线两键由 **Q-4 E** 代加（你只在代码里读键、并在总账确认 Q-4 已加）。
5. `scripts\agent_probe.ps1` 看 ACTIVE：`skill_manager.py` 只动抽取调用合并一处 + 结果分发；`goals/service.py` 不动（Q-1 的），槽位写入走你新建的 `profile_fill.py`。

## 红线

- 只动归属矩阵 Q-5 行；`feature_registry.py` 不动（Q-4）；`goals/service.py` 不动（Q-1）。
- AI 推断值一律 `status=mentioned`、`source=ai_inferred`，**不自动升 confirmed**；只在与已确认值冲突时打断人工（W7ZTSB）。
- `git add <显式路径>`；每 commit 带 `#263`。

## A 两链合一：一次调用同时产出记忆事实与画像候选

- `skill_manager` 抽取调度：episodic 抽取 prompt 加「画像槽位候选」输出段（按当前业务域槽位表：陪伴 = 称呼 / 坐标 / 职业 / 年龄 / 兴趣 / 家庭 / 婚恋 / 收入 / 居住 / 资产），一次 LLM 调用 → 两路分发：记忆事实（既有路径）+ `profile_fill.apply(conv, candidates, source="ai_inferred")`；`profile_llm` 与 `memory.extract.use_llm` 任一开即走 LLM（两键语义合并，页面只留一个「AI 抽取」开关，另一键跟随）。
- 日志 `[extract] conv=… facts=n slots=n llm=1|0`；`heuristic_count / llm_count` 保留。

## B 昵称 / 简介解析预填

- 新 `src/contacts/nickname_parse.py`：分隔格式（`/`、`|`、`,`、空格 + 数字）解析 名 / 年龄（2 位数字 18–80）/ 城市（地名词典 + 大写词）/ 职业（职业词典）/ 日期片段忽略；结果 `profile_fill.apply(source="nickname", status="mentioned")` 标「来自昵称 · 待确认」；联系人同步 / 会话创建时触发一次，昵称变更再触发。
- 验收：「Michael/46/New york/union carpenter/5.28」→ 年龄 46 / 城市 New York / 职业 union carpenter，三项待确认。

## C 待确认一键确认

- 画像卡（cp-goal / 客户画像区）AI 推断值显示「AI 推断 · 待确认」徽标 + ✓ 确认 / ✎ 改 / ✕ 否；确认写 `status=confirmed source=confirmed`；与已确认值冲突 → 通知中心一条「画像冲突：职业 已确认 X，AI 推断 Y」（不打断对话）。i18n 三语。

## D `[profile]` 日志 + 停滞告警

- 每次槽位写入 `[profile] conv=… slot=… value=… source=… status=…`；连续 50 条入站 `facts=0 slots=0` → 记忆页 / 目标页告警行「抽取可能未生效：检查 AI 抽取开关与模型」（O-2 D 的告警行同处扩）。

## E 字段 schema 迁移

- 存量画像裸字符串 → 视为 `{value, source:user, status:confirmed}`（读时兼容、写时升级）；导出 / 备份带 schema 版本号；tests 覆盖新旧格式与 Q-1 读侧兼容。

## 收工

- 落点表 `docs/发版对账_v1.0.79_Q5.md`（§〇 「为什么画像永远空」三段：键不在基线 / 两链各自为政 / 昵称没人读）；`docs/Q批_进度总账.md` 只改 Q-5 行（确认 Q-4 E 两键已加）；`agent_probe.ps1 -Done`；回访文案交 Q-0 / R79。

## 开新对话粘贴

```
读 docs/指令_Q-5_抽取两链合一与昵称解析_2026-09-09.md，按它做 Q-5（#263：skill_manager 抽取一次调用同时产出记忆事实与画像槽位候选并分发到新 profile_fill.py / 新 nickname_parse.py 解析联系人昵称「名/年龄/城市/职业」预填「来自昵称·待确认」/ 画像卡 AI 推断值待确认一键确认、冲突只通知不打断 / [profile] 日志 + 连续 50 条 0/0 告警 / 画像字段 schema {value,source,status} 读旧写新）。预算 $150。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与接口约定①、docs/发版对账_v1.0.78_O2.md（O-2 A–D 在它之上做），再 scripts\agent_probe.ps1 -Intent 登记；feature_registry.py 两键由 Q-4 代加你只读；goals/service.py 不动（Q-1）；AI 推断值不自动升 confirmed。每个 commit 带 #263 并立刻补落点表 docs/发版对账_v1.0.79_Q5.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-5 行并 agent_probe -Done。
```
