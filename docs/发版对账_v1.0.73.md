# 发版对账 v1.0.73（2026-09-04）

> 接 `docs/I批_进度总账.md`。1.0.72 已装三台坐席，kouxing 触发 FD 崩溃后人工拉回。
> 本版是 **I 批剩余项 + P0 FD 自愈** 的整包，不重发 1.0.72。

## 本包必含

| 项 | 落点 | 状态 |
|---|---|---|
| P0 入站 ASR 并发闸 | `voice_transcriber.py` + `unified_inbox_account_routes._ingest_asr_sem` | 在树 |
| P0 exit 78 壳侧重拉 | `desktop/backend-launcher.js` `scheduleRespawn` | 在树 |
| I-1 / guard-check 影子 | `unified_inbox_desktop_routes` `block: False` + `autosend_shadow_log` | 本线 |
| I-5 E3 人设选品 | `goals/service.py` / `goal_routes.py` `pick_products(..., persona_id=)` | 在树 |
| I-5 E4 出站不提撤回 | `withdrawn_cite.apply_to_reply` → `drafts.enrich_draft` | 本线（B 线） |
| I-5 E5① 去悬浮清单 | `_cpTearWanted` 恒空 + CSS 隐藏 | 本线 |
| I-2 #156 黄点绑人设 | `unified_inbox.html` `_openAcctPersonaBind` | 本线 |
| I-2 #159 未读可点 | `_acUnreadBadge` + `/account-unread` 浮层 | 本线 |
| I-2 #155 双向称呼 | `loadInfoSidebar` + `/conv-meta/address-names` | 本线 |
| 小智语料 | howto 4 条 + help_terms 4 条 | 本线 |

## 刻意不做 / 不拦发版

- A 线 `skill_manager` 挂 `apply_to_reply`：热区，unlimited outbound 线在改，B 线草稿已覆盖
- I-5 E1 LINE 语音/视频/贴纸真机：图已通，其余不挡包
- I-5 E5③ 群消失：只观测
- I-6 61 单回访：等老板批文案，不随包群发
- #158 坐席机人工点：1.0.72 已装，本包再验一次

## 发布动作（2026-09-04 14:3x 落地）

1. `desktop/package.json` → 1.0.73 / 1.073
2. `npm run dist:win:smart` 全量 backend → `dist\ChatX-Setup-1.0.73.exe` 562.4 MB（内测）
3. `npm run dist:win:clean` → `dist-clean\ChatX-Setup-1.0.73.exe` 494 MB（对外）
4. 坐席：yunsheng 装成（seatverify + smoke 1.0.73）；kouxing / lianbei **当时 LAN 不可达**（ssh/ping 超时），未装
5. `publish_chatx.ps1 -DistDir dist-clean -Yes`：VPS sha512 核过、R2 同步、公网 `latest.yml=1.0.73`
6. changelog + `chatxContent.ts` 对齐 1.0.73 / 494 MB / sha256 `9c345b5d…`；`deploy.ps1 -SkipAssets` 过 gate:content
7. 频道 @hykj7 `messageId=148`（只发频道，无自动更新尾巴）
8. 小智金标 4/4 GUIDED（未读 / 黄点 / 称呼 / 全自动影子）
