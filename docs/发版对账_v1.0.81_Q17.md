# 发版对账 v1.0.81 — Q-17（风控三级分级 + commitment 只评出站 + 摘标冷却 + 风控页公开：#277 P0 ②）

> 指令：`docs/指令_Q-17_风控分级公开与摘标冷却_2026-09-11.md`。底座：`docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-17 行）。总账：`docs/Q批_进度总账.md` Q-17 行。预算 $160。
> 接手方：先 `git -C D:\boundless log --oneline -10` 核对本表 commit 真在。全部是客户端 `.py` / 模板 / i18n 改动随 1.0.81；`.py`（risk_grader / drafts / protocol_autoreply / commitment_guard / risk_hold / reply_diagnosis / persona_manager / tags 路由 / reply_settings 路由）须实例重启（`deploy\instances\restart_instance.ps1`）才生效，模板 / i18n 包热更即生效。
> 执行：2026-09-11。意向板 slug `q-17-risk-grader-3-level-commitment-out-only-ris`。在 Q-2（commitment_guard / drafts 钩子）、Q-3（risk_hold / 打标即闸）、Q-15（adult_grader 钩子式接线）之上改，不重做。

## 〇 为什么 Cameron 被打三次标（15635715247）

1. **一把尺子**：`drafts.keyword_risk_hits()` 对**客户入站**也跑 `detect_commitment(t) or detect_commitment_claim(t)`——「Trying to show you the pictures they have sent me」这类叙述与「send me a pic」同判 `commitment:media` → high → `decide` L1 `review_required` → `tag_needs_human(high_risk)`；`_SENSITIVE_PATTERNS` 的 `your address / phone number / my address is` 出现即 high；`quick_analyze._RISK_TERMS["privacy"]`（my address / passport / password）出现即 high。
2. **没有冷却**：04:39:45 人工摘标（`clear_needs_human` 同步 `risk_hold.clear`）→ 04:50:12 同类再中 → 第三次打标；会话头「我知道了」走 `PUT /tags` 只清 handoff_meta，**连 risk_hold 都没解**。
3. **看不出类别**：`handoff_meta.reason=high_risk` → 横幅只写「命中高风险拦截」，级别 / 类别 / 命中词要翻草稿 `risk_reasons`。

## 0. 开工基线（全只读）

| 项 | 事实 |
|---|---|
| 意向板 | Q-17 登记；HEAD `05ba6658`（开工）→ `0e3dca25`（A 段提交前）；索引空闲 |
| 共享文件 | 我范围内他人在途：`templates/unified_inbox.html`（工作树整文件 CRLF/LF 漂移，Q-3 / Q-15 同款）/ `i18n_packs/zh_hant_auto.py`（生成物）→ 模板走 LF 归一化 hunk + `hash-object` / `update-index --cacheinfo`；繁体词条改进**新 pack** `i18n_packs/risk_grader.py`（`ZH_HANT` dict，pack 机制原生支持，不碰生成物）。`drafts.py` / `protocol_autoreply.py` / `commitment_guard.py` / `risk_hold.py` / `reply_settings.html` / `reply_settings_routes.py` / `persona_manager.py` / `reply_diagnosis.py` / `unified_inbox_workspace_tags_routes.py` 开工时干净（提交前再核） |
| 代码事实 | 入站 high 的来源有三：`quick_analyze`（privacy / money / self_harm / adult / cred_request / stop_contact 全 high）、`keyword_risk_hits`（支付词表 high + 承诺兜底正则 high + detect_commitment∪claim high）、Q-15 adult regrade。`decide` 只看 peer_risk 档位 + hard_stop 原因 → high 即 `review_required`。Q-2 `evaluate_inbound`（refuse_sent 罐头委婉延后）与 `keyword_risk_hits` 互不依赖——降级不影响 Q-2 处置。Cameron 原句在 HEAD 词表上已**不**命中（`detect_commitment` 无中），事故时的词表版本不同或原句有出入；门禁仍钉原句 |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| A 三级分级 + B commitment 只评出站 + C 冷却/幂等 + E 门禁 | #277② | `6ec6e0d6` | **新 `src/inbox/risk_grader.py`**：`grade(text, direction="in"\|"out", persona, *, lang, reasons, hits, cfg) -> {level, category, hits, action, found}`；类别表 `CATEGORIES`（公开给回复设置页）：高 self_harm / minor（新）/ threat（新）/ money_request / scam（新）/ payment_keyword，中 adult（Q-15 政策）/ stop_contact（O-1 A）/ request_contact / request_media / request_meet / request_gift / complaint，低 privacy / narrative；`regrade_inbound(svc, conv, text, lang, risk, reasons, hits)`（Q-15 同式）：高 → `max(原, high)` + reasons 追加 `risk:<category>`（打标仍走 decide→review_required→tag_needs_human 现状路）；中 → 原 high 且**只**由可降因子撑起（无 self_harm / stop_contact / credential_or_payment_request / money / adult 主因、支付词表未中）才降 medium（`privacy`→`privacy_mention`），会话打 `risk:medium` 标签、日志 `[risk] medium conv= category= hits= action=`；低 → 同判降 low、只落 `[risk] low conv= category= hits=`；`classify_reason(reason)`（原因码 → 类别/级别：adult:pressure→adult/high、commitment:media→request_media、crisis→self_harm、risk:<cat>）；`risk_overrides_of(persona)` 只收可覆写类别（request_* / complaint / privacy / narrative）且不低于地板；`public_table(persona)`。**`drafts.py`**：L1163 `keyword_risk_hits(t, direction="in")`；Q-15 钩子后一行 `risk_level, _peer_reasons, _rk = risk_grader.regrade_inbound(...)`；L1313 `tag_needs_human(reason=high_risk, level=_rk.level, category=_rk.category, hits=)`；enrich L1430 `direction="out"`；`keyword_risk_hits(text, direction=None)`：None/out 逐字旧口径（`detect_commitment or detect_commitment_claim` → commitment:<kind> high），in 不跑两者、只 `detect_request` → `request:<kind>`（money high / 其余 medium），承诺兜底正则（表[2]）客户侧只到 medium；`_SENSITIVE_PATTERNS` 的 `my address is / your … address / phone number` 从表[2] high 拆为表[3] **low**。**`commitment_guard.detect_request`**（`_REQ_CONTACT/_MEDIA/_MEET/_GIFT/_MONEY` 三语：send me / give me / can I have / what's your / show me yours / where do you live / need your address / 发我 / 给我 / 你住哪 / 你电话多少 / 加个微信 / 住所教えて…；`_CONTACT_EXCLUDE` / `_MEET_EXCLUDE` 复用；过去时叙述不算）。**`risk_hold.py`**：`touch`（保持期只更新 last_hit / last_hit_ts / hit_count，不刷 set_ts）；`cooldown(store, cid, category, *, minutes=30, level, by, reason)` → KV `risk_cooldown:<cid>={category, level, until, set_ts, by, reason}`；`cooldown_record` / `cooldown_blocks(cid, category, level)`（同类且级别不高于记录 → 挡；不同类 / 更高级 / 到期 / 空类别 → 放）。**`protocol_autoreply.py`**：`tag_needs_human(..., level="", category="", hits=None)`：缺省按 `classify_reason(reason)` 推；① 标在场 + `risk_hold.active` → `touch` + Q-15 adult 钩子照走 + 日志 `[needs_human] 保持` → return False（meta 不重写＝横幅不重弹）；② 标不在场且 `cooldown_blocks` → 日志 `[needs_human] 打标 … cooldown_skipped=true until= by=` → return False（不登记持有）；否则旧路 + handoff_meta 多 `level / category / hits` 三键（非风控原因 empty_reply 等不带，旧契约原样）+ 打标行 `level= category= hits= cooldown_skipped=false`；`clear_needs_human`：`risk_hold.clear` 之后，actor 非 `system*` → `cooldown(category=meta.category or classify_reason(meta.reason), level=meta.level, by=actor)`，摘标行带 `category= cooldown_min=`；`_risk_policy_decide` L89 `direction="out"`。**`unified_inbox_workspace_tags_routes.py`** PUT `/api/workspace/conv/{cid}/tags`（会话头「我知道了（摘标）」/ 手摘标签）：摘掉 HANDOFF_TAG 时改经 `clear_needs_human(actor="agent_ack")`（meta 同清 + **risk_hold 解除**——此前本口漏了 Q-3 同步，摘标后持有仍活跃 → 下一句 decide 强制 L1 又打标——+ 冷却 + 摘标日志）。**`reply_diagnosis.py`** high_risk finding：meta 带 category/level/hits 优先，缺则 `classify_reason`，params 多 `hits`。**`persona_manager.py`** `PROMPT_EXEMPT_FIELDS` + `boundaries.risk_overrides`。**`tests/test_risk_grader_gate.py`** 34 例 | `test_risk_grader_gate` 34 passed（15 句叙述含 Cameron 原句走完整 `DraftService.auto_generate_draft` → L2 / 零「需人工」/ 零 risk_hold；privacy 主因 high→low；5 句真高风险（索钱 / 威胁 / 露骨施压 / 未成年 / 诈骗）→ L1 + 标 + hold + meta 带类别；冷却：摘标后同类不重打 / 不同类可打 / 更高级别可打 / 31 分钟后可打 / system 自动摘标不冷却 / 原因码无类别按 classify 推；保持期幂等 touch；旧签名 Q-2 钉桩「Just need your address」→ high commitment:contact、in 只认索要、out 答应语 high；中级只打 risk:medium；覆写地板；真高风险不降）。邻接回归 `test_drafts_risk_policy / test_commitment_gate / test_commitment_guard / test_risk_hold_q3 / test_adult_grader_gate / test_b2_risk_audit / test_stop_contact_hard_stop / test_protocol_autoreply / test_handoff_tag_meta / test_needs_human_autoclear_207 / test_persona_prompt_chain / test_reply_diag_customer_waiting` 369 passed |

## 2. 已知边界（本单刻意不动）

- `quick_analyze._RISK_TERMS["money"]`（bank card / crypto / paypal / 转账 / 借钱 出现即 high）与 `_SENSITIVE_PATTERNS[0]` 支付词表（refund / password / OTP / 退款 / 密码）**不降**——「I changed my password yesterday」仍 high（账号安全词表属真高风险地板，且 `chat_assistant_service.py` 不在本单归属）。叙述降级只覆盖 privacy 主因（address / passport / ID）与承诺强短语。
- `stop_contact` 在公开表里是「中 · 冻结」，但判定与冻结语义全在 O-1 A / Q-3，本单只记类别不改流。
- `risk:medium` 会话标签只加不自动摘（信息标，坐席可手摘）。
