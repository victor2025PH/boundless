# 指令 O-2：记忆抽取基线（干净包首日即死 · 出站同抽 · 私事隔离 · 新增计数）

> 预算 **$100**（A 止血 ≤$30 先单独提交）。报告 **WYNN22**（现挂 #201，O-5 会改立新 P0 单；commit 先带 `#201`，O-5 立单后落点表补新号）、584AUV（#183）。目标版本 1.0.78；**A 段收工即写总账，N-5 打 1.0.77 时会带上**。
> 底座 `docs/修复指令_O批_并行_2026-09-08.md`（D-O5、文件归属）。落点表 `docs/发版对账_v1.0.78_O2.md`。

## 开工前必读

1. `tmp_diag\WYNN22\*_report.md`：17:20 重启后 8 小时 `[episodic] schedule run` 30 次全部 `skip: intent='direct_chat/greeting/small_talk' not extractable (match_all=False intents=[])`，6 客户零抽取零 promoted；同时 17:21:02 启动写「情景记忆已启用」。重装日志：「配置不存在，已从内置种子播种 ← config.desktop.min.yaml」「随包种子缺席(clean 包)…出厂默认由产品基线补齐」。
2. 代码事实（已核）：`src/skills/skill_manager.py` L192 `should_extract_intent`：`match_all` 否则按 `extract.intents` 白名单，**空即不抽**；L5584 skip 日志。`config/config.yaml` L1770 `memory.extract.intents: [direct_chat, small_talk, greeting, complaint]`（服务器基线有）；`config/config.desktop.min.yaml` **没有任何 memory / episodic 块**。L-5 做的产品基线机制：`src/utils/feature_registry.py` A 类「出厂默认」（`companion.goals.sprint.enabled` / `wellbeing.crisis_*` 就是这样补进去的，照抄那条路）。
3. 历史三单：W7ZTSB 记忆确认机制重设计（M-5 F 只做了「客户私事不进共享 KB」的口径）、DY534Y 出站 `origin=manual` 不抽（`schedule run user=客户号` 只挂入站）、SV62XJ③ DailyLearner 三分流无实现。
4. `scripts\agent_probe.ps1` 看 ACTIVE。

## 红线

- 只动 `config.desktop.min.yaml`、`feature_registry.py`、`skill_manager.py` **两处**（启动白名单日志 + `origin=manual` 抽取入口）、记忆页模板 / 路由、tests。
- 存量部署零回归：服务器 `config.yaml` 已有值的不变；基线只在**键缺席**时补，不覆盖用户显式设置的空列表（显式空 = 用户关了，日志 WARNING 但尊重）。
- 每 commit 带工单号；`git add` 显式路径。

## A（止血）`memory.extract.intents` 进产品基线 + 启动日志说真话 —— D-O5

- 修法：① `feature_registry` A 类加 `memory.extract.intents` 默认 `[direct_chat, small_talk, greeting, complaint]`（与服务器同值；不选 `match_all`——token 成本与 greeting / stop_contact 噪音），`memory.extract.enabled/use_llm` 同步补齐；`config.desktop.min.yaml` 加注释块指向基线；② 启动日志：「情景记忆已启用 · 可抽取意图 N 个：…」；白名单空 → `WARNING [episodic] 抽取白名单为空，记忆不会新增`，**不得再写「已启用」**；③ 首装 / 升级迁移：键缺席的存量安装（skuio 两台、钧一台都是）启动时补齐并落一行 `[episodic] 基线补齐 intents=…`。
- 验收：干净包模拟（删 config 起服务）→ 启动日志有白名单 → 一条 `direct_chat` 入站触发 `[episodic] schedule run` 不再 skip；tests：缺键补 / 显式空尊重 / 服务器值不变。

## B `origin=manual` 出站同走抽取（DY534Y）

- 现象：人工插话 / 人工接管发出的内容从不抽取，客户对人工说的约定不进记忆。
- 修法：抽取调度对 `direction=out & origin=manual` 的行同样入队（K-1 D「人工接管进记忆」做的是会话级标记，不是抽取）；`user=` 仍记客户号；日志 `[episodic] schedule run user=… source=manual_out`。
- 验收：人工发一条含地点 / 时间的消息 → 抽取运行 → 该客户记忆新增。

## C 私事只进客户记忆（W7ZTSB / M-5 F 收口）

- 修法：抽取结果带具体人 / 地 / 时 / 约定（NER 或规则）→ 只写该客户 episodic，**永不**进共享 KB / 通用事实；DailyLearner 收集时同规则过滤；日志 `[episodic] scope=customer|shared reason=…`。
- 验收：tests 三例（约会地点 / 对方生日 / 通用天气常识）。

## D 记忆页「自安装以来新增 N 条」

- 修法：记忆页顶部计数（`promoted` 累计 + 今日新增）；`N=0 且入站 ≥50` 红字「记忆没有在增长——检查抽取白名单 / 模型」+ 链接到日志说明；i18n 三语。
- 验收：干净包 + 修复后计数从 0 开始涨；未修复态显红字。

## 收工

- 落点表 `docs/发版对账_v1.0.78_O2.md`（§〇 「为什么第三次出厂默认没进基线」：列出 L-5 当时补的键与漏的键，给 N-5 / 未来发版一条「基线键清单核对」步骤）。
- `docs/O批_进度总账.md` 只改 O-2 行（A 段先改一次：「A 已收工 commit …，可并入 1.0.77」）。
- `agent_probe.ps1 -Done`；回访文案交 O-5（给 skuio：AI 记忆页 13:14 后开始有新增；验证：`[episodic] schedule run` 不再 skip）。

## 开新对话粘贴

```
读 docs/指令_O-2_记忆抽取基线_2026-09-08.md，按它做 O-2（WYNN22 / #201：memory.extract.intents 进产品基线 + 启动日志空即 WARNING / origin=manual 出站同抽 / 私事只进客户记忆 / 记忆页自安装新增计数）。预算 $100，A 段止血 ≤$30 先单独提交并更新总账 O-2 行（N-5 打 1.0.77 会带上）。先读 docs/修复指令_O批_并行_2026-09-08.md 的 D-O5 与文件归属矩阵，再 scripts\agent_probe.ps1 -Intent 登记，只动 O-2 归属文件；存量部署零回归（只在键缺席时补，显式空尊重）。每个 commit 带工单号并立刻补落点表 docs/发版对账_v1.0.78_O2.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/O批_进度总账.md 的 O-2 行并 agent_probe -Done。
```
