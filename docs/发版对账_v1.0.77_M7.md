# 发版对账 v1.0.77 — M-7 目标引擎可追溯与真出手（#236 = #166 族第五次复报）

> 指令：`docs/指令_M-7_目标引擎可追溯与真出手_2026-09-07.md`。1.0.76 已由 M-6 于 09-07 04:2x 发出（三渠道），本条落点随 **1.0.77**。
> 证据：`tmp_diag\{KH9FEP,H82N5Q,7FKNFV}`（09-07 02:27–02:35，1.0.75）+ 前四轮 `{5NAR3T,XJWZRG,V76EPK,AC5XP9,X8MYGP,5JH5RJ}`。
> 预算 $150；执行方 M-7 对话，09-07 05:4x 开工。

## §2 取证四问（先答后改）

### 问 1 ｜ 59 稿零 `[goal-inject]` 行：是 disabled DEBUG，还是没调注入？

**两者都不是。真根因＝整个 `src/companion/goals/*` 包的 INFO 在生产里从源头被丢弃。**

- 调用链核实：`auto_generate_draft`（`drafts.py` L979）只落停泊稿，正文由 `persona_reply.py` L616 `sm.generate_inbox_draft` 生成；该函数 L2851→L3662 之间除空文本/空 chat_key 两处早退外**无其他 return**，L3331 `self._inject_goal_context(chain="draft")` → `build_block_for_chat` **每稿必调**；直连回落路径 L698 同样调。KH9FEP 每稿都有 `[persona] tier=…` 与 `[episodic] schedule run` 两行（统一引擎跑完了），所以注入口确实被调了 59 次。
- 日志为何为零：`service.py` L31 `logger = logging.getLogger("GoalService")`（同族 `GoalSprintTicker` / `GoalStore` / `GoalNotify` / `GoalBridge` / `GoalSignals` / `GoalReport` / `GoalOrderPull` / `GoalProfileLLM` / `SiteCatalog`）都是**根级独立名字**。`src/bootstrap/logging_setup.py` L79 把 root 钉在 `max(level, WARNING)`，只给 `ai_chat_assistant.*`（主 logger）和 `src.*`（`log_setup.attach_src_file_handler` / `mirror_handlers_to_src`）放 INFO。`GoalService` 级别 NOTSET → `getEffectiveLevel()==30` → `logger.info()` 在 `isEnabledFor` 处直接返回，**handler 都没碰到**。
- 本机 5 行复现（root=WARNING + `src` 挂 INFO handler）：`GoalService.info` → 空；`GoalService.warning` → 有；`src.companion.goals.service.info` → 有。
- 为什么四天没人发现：J-2 A2 的测试 `test_goal_inject_keys_166.py` 用 `caplog.at_level(logging.INFO, logger="GoalService")` **显式抬级**，测试全绿、生产全哑；四天九份诊断包里唯一能看到的目标行都来自 `src.inbox.health_watchdog`（`src.*`）与 `ai_chat_assistant`（启动行）——与本结论完全吻合。
- 连带盲区：`[goal-sprint] 本轮排入 N 条`、`[goal-notify] 失守日报已发`、`[goal-sweep] 定时结算`、`record_*_beat_sent` 的 debug、目标创建/重置——**全部不可见**。这就是「近 3h 无目标创建/重置日志，无法判断深化→起步是谁改的」的原因：不是没记，是记了没人看得见。
- 所以 §4 B 的「disabled/no_goal 都 INFO」若只改级别不改 logger 名，等于零。**改法：goals 包全部 logger 改到 `src.companion.goals.<module>` 命名空间**（8 处测试 `logger="GoalService"` 随改）。

### 问 2 ｜ `sent_24h=4` 的 4 拍发到了哪？

- 口径：看门狗 `liveness.collect_send_liveness` 数的是**所有** active auto 目标（当时 `auto=7`）的 `goal_events`，`SENT_KINDS=("care_sent","beat_sent")`；而 `background_tasks._care_sent_hook` 每次真发**同时**写 `beat_sent`（`record_*_beat_sent`）和 `care_sent` 两条 → **一次真发计 2**。13:56 stalled → 14:01 recovered `sent_24h=4` ＝ 大概率 **2 次**真发，分布在 7 个目标里。
- 「真发」的定义：`care_dispatcher` L933 在 **入 deferred 队列成功**时调 `sent_hook`（`mark_sent` 在其后），`item` 只有 care 行字段，**无正文、无 deferred row_id、无消息 id** → `beat_sent` 事件天然不可追溯（detail 只有 `sprint:pN` / `daily:auto`）。
- 卡片「已推进 2 拍」＝`goal_routes._attach_beats` 数 **goal_actions** 表 `status∈{consumed,sent}` 且 `push_level!=none` 的行：`consumed`＝回复链把方向织进拟稿（被动），`sent`＝主动发出。与看门狗**不同表、不同口径**。
- BABY BEAR（WhatsApp，3+1 天＝natural 档）能否拿到主动拍：`due_daily` 要求 `natural_silence_min_sec=6h` 内客户**没开口**；该会话夜里 3h 59 条入站，白天同样活跃 → 每 tick `not_due`（ticker 只在 `last_skips` 内存计数，不落事件）。**结论：4（=2）拍发给了其他沉默会话；BABY BEAR 的「2 拍」是两次被动注入的 `consumed` 行**——用户在会话里看不到「目标驱动内容」，因为那是被织进普通回复的方向，不是独立的一条消息；而 `[goal-inject] injected` 那两行又被问 1 吞掉。
- 无法从本机反查 skuio 的 gstore（诊断包不含 `marketing_goals.db`）；上述为代码路径推断，A 项落地后下一次复报可直接从 `/beats` 端点读到答案。

### 问 3 ｜ 09-07 到期时发生了什么？

- 到期判定只有 settle-on-read（`refresh_goal` → `settle_goal`）：`companion.goals.sweep.enabled` **不在 A 类基线**（默认 False）→ `notify.scan_tick` 里 `settle_sweep` 不跑；靳靠下一次入站（inject）或卡片刷新（`for-conversation` → `_refreshed_view`）把 status 翻成 `expired`。BABY BEAR 消息密集，翻状态本身不成问题。
- 翻成 expired 后：`scan_sprint_miss`（即时单条）只收 `pace∈{today,session}`，自然档**不进**；`scan_miss_digest` 需 `len(rows)>=3` **或** 最老一条 `>=24h` → 1 条、未满 24h → `return`（静默）。24h 后即便出账，`goal_miss_alert` 只被 `webhook_notifier.py` 消费；`unified_inbox_realtime_routes._SSE_EVENT_TYPES/_NOTIF_EVENT_TYPES/_NOTIF_TYPE_I18N` 与 `workspace_base.html` 通知中心只登记了 `goal_completed_alert`，**没有任何 miss/expired 类型** → 工作台永远看不到。
- 卡片侧：`for-conversation` 无活跃目标时返回 `last`（最近一条终态）——用户 02:2x 重建新目标后 `goal` 非空、`last=None`，上一目标的结局从卡上消失。**结论：到期结算在工作台层面是结构性静默，不是门槛没到。**

### 问 4 ｜「白天 2/1 拍」超额后被动注入是否也停？

- **是，而且拍数上限从来只拦被动注入。** `effective_beat_cap` 全仓只有两处读：`service.refresh_goal` L489–495（`cap and n_existing>=cap → hold="pace_cap" → build_block_for_chat L1343 return None`，不带方向）和 `goal_routes._attach_beats`（显示）。冲刺 ticker（`run_once`）与 care 派发**不读 cap**——主动拍靠相位点（≤3）/ 每日一拍自限。
- 但对 BABY BEAR 不成立：3+1 天＝natural，`effective_beat_cap(natural)=0` → `pace_cap` 不会触发；natural 日槽 `consumed` 后同日再入站直接复用当日拍继续注入（L484 取到既有 action → 组块）。V76EPK 的「动作 2/1 拍」是 1.0.74 的 1 天目标（`cap=total_days=1`，`used=2` 含 z:/s: 槽），是显示口径不是限流。
- 改法（B）：`refresh_goal` 增 `inbound_turn` 分支——客户来消息（reply/draft 链）时 cap 不再 hold，只在主动链/系统调用上生效；`pace_cap` 达到时改记 `beat_blocked(pace_cap)` 事件（仅主动侧）。

### 取证结论 → 改动映射

| 问 | 事实 | 落点 |
|---|---|---|
| 1 | goals 包 logger 根级名 → INFO 全丢 | B：logger 全部迁 `src.companion.goals.*`；`disabled`/`no_goal`/`expired` 每稿一行 INFO（no_goal 节流降 60s） |
| 2 | `beat_sent` 在入队时记、无正文/消息 id；卡片数 actions、看门狗数 events 且双计 | A：事件表加 `conversation_id/message_id/text_head` 列；hook 传 care_id+conv；`/beats` 端点按 care 行反查正文与 deferred 投递真相；卡片 N 与看门狗都数 `beat_sent`（去 `care_sent` 双计） |
| 3 | 自然档到期无即时告警；日报不到工作台；重建后上一目标消失 | C：到期/失败当刻 `goal_settled_alert`（不经聚合）→ SSE + 通知中心 + 目标事件 `settlement`；卡片 `last_settled` 7 天 |
| 4 | cap 只拦被动注入 | B：`inbound_turn` 不受 cap；主动侧超额记 `beat_blocked` |

## 一、改动落点

（随各项 commit 追加）

## 二、验收

（随各项 commit 追加）

## 三、越界点名（非 M-7 归属文件，均为 M 批已收工线的文件，改前 probe 无 ACTIVE）

（随各项 commit 追加）

## 四、交 M-6 / 下一收口

（收工时写）
