# 交接摘要：值守 / 指令线（09-09 → 09-12 20:4x）

> 给接手这条「值守 + 出指令 + 发版」线的新对话读。**唯一进度真值是 `docs/Q批_进度总账.md`**，本文只是入口与口径。

## 一、这条线在做什么

1. **值守**：盯报障群（Telegram `telegram:6834964252:-1004345824259`）与联网 Cursor 现场 agent 落到 `D:\boundless\tmp_diag\<码>\` 的报告；`tools/duty_auto_ticket.py` 自动立单进 `bug_intake.db`（`D:\chengjie-instances\zhiliao\data\config\bug_intake.db`，只读查询用 `python -c` + `?mode=ro`）；事件流在 `D:\chengjie-instances\.ops\duty_watch_loop.log`。
2. **出指令**：每条修复线一个 `docs/指令_Q-n_*.md`（事实 / 段表 / 红线 / 文末粘贴块），老板复制粘贴块开新对话去修；同时登记 `docs/Q批_进度总账.md` 进度表一行 + `docs/修复指令_Q批_并行_2026-09-09.md` 投放表 / 去向表，只提交 docs。
3. **发版（R 批）**：老板决策 **D-Q11（09-11 14:31）「不要内测版，只要一个版本」**——只出 clean 包，坐席机与内测机装同一包，内测渠道只做同字节桥接。全链 SOP 在 `.cursor/rules/chatx-desktop-install.mdc`（含「单一版本」节），样板 `docs/发版对账_v1.0.81_发版.md` / `_v1.0.84_发版.md`。

## 二、当前状态（09-12 20:4x）

- **渠道版本 1.0.84**（09-12 09:39 公共 + 内测桥接 + 官网 + 坐席三台 yunsheng / kouxing / lianbei 1.084 clean/public）。
- **HEAD 已修未发**：接力记忆二～四期（`c8b043a3` `a52b7e3d` `817c0148`：人工发的图 / 语音进 AI 记忆、切回全自动生成接力摘要）、上下文深度四档 `0b606199`、Q-29（`.py` 须 zhiliao 重启才生效，尚未重启）。
- **工作树未提交（别线半成品，不要带走）**：Q-26 C–E（`reply_settings_routes.py` +42 / `reply_settings.html` +358 / `i18n_packs/automation_risk_q26.py` 新 / `persona_manager.py` +20 / `personas.html` +11 / `unified_inbox_send_routes.py` +2，最后动 06:10）；值守工具改动（`bug_intake.py` / `duty_auto_ticket.py` / `duty_channel_reminder.py` / 四个测试）。
- **未开工线**：Q-26 C–E（接手）、Q-28（wa Bad MAC，#279）、**Q-30～Q-36（今晚新出）**。
- **开放单**（`status not in fixed/closed`）：新单 #306–#317；老单 #44 #68 #74 #82（verified，历史）、#216 #258 #261 #265 #280–#283（对照日志 / 复验单，信息性）、#279（Q-28）、#305（Q-29 已修待发）。

## 三、新单 → 去向（09-12 11:43 → 20:19）

| 单 | 码 | 一句话 | 真因 / 去向 |
|---|---|---|---|
| #306 | BPMEWX | Mizuki 相册自拍无命中不发 | `[album_match] candidates=0` 界面零反馈 → **Q-35 C/D** |
| #307 | 4QW432 | 红条直出 `media_caught_repeat` | 原因码无人话 → **Q-30 D** |
| #308 | — | 顶栏全自动 / 草稿条写全局半自动 | 两处各算档位 → **Q-30 C** |
| #309 #310 #311 | — / VYY22Q / W2CSGA | 点开即「处理中 · 释放认领」；全自动出稿不发 | `resolve_seat_mode` 缺省 multi + `selectChat` 无条件 `renewClaim`；后端 autosend **不读** claim，真闸要查 `abort_ledger`（候选 `agent_typing`）→ **Q-30 A/B（P0）** |
| #312 #314 | MSYSKV / KXYRQ7 | jun 人工发图后 AI 不回 / 「挡话不识图」 | 日志 `peer_bot_guard skip reason=colleague:8852939166`——jun 自测账号在「永不自动回复」名单，A/B 两线都跳过，**不是识图**；可见部分 → Q-30 D；「AI 记住人工发过图」→ 接力记忆线已在 HEAD 待发 |
| #313 | 2JK95C | 几个菜说成一桌菜；客户提议发图被当索图拒 | caption 无约束 + 无 `offer_media` 意图 → **Q-36 A/B**；同报告见 skuio `7331682688` 仍对报障群拟稿 → **Q-36 C（Q-23 复验）** |
| #315 | JDKHPR | 1.0.84 启动对照日志 | 信息单，不立项 |
| #316 | 4HK54G | 相册批量上传失败 | 前端 `pmaUpload` 失败只 `fail++` 无文件名无原因；后端只见 2 张成功 + 一条 `not enough arguments for format string` logging error → **Q-35 A/B** |
| #317 | BS7E5Q | 筛选区 `dormant:ignored 6 / risk:medium 3 / 停联 1` 筹码处理完不消 | `#fp-tags` 是标签统计，标不摘 chip 不消；`dormant_review` 只加不减 → **Q-31 D** |

## 四、开工顺序建议（写在总账 20:4x 拍板行）

Q-30（P0）→ Q-35 / Q-36 / Q-28 并行 → Q-26 C–E 接手 → **R85**（须 zhiliao 重启装载 Q-29 / 接力记忆 `.py`；台账标 fixed 只在发版后）→ Q-31 / Q-32 / Q-33（同文件顺手）→ **R86** → Q-34 独立周（ACTIVE 无 inbox 模板线才开）。

## 五、值守时的手法（踩过的坑）

- 意向板 `D:\chengjie-instances\.ops\agent_intents\*.txt`（`scripts\agent_probe.ps1 -Intent/-Done`；被自动审查拦时直接写 / 读文件）。
- 共享文件只暂存自己的 hunk：`python tools/stage_hunks.py --file <路径> --mine <regex> --apply`；`git add` 显式路径；提交前 `git diff --cached --name-only` 为空；绝不提交别线半成品。
- 总账 / 底座插表行用 PowerShell `List[string].InsertRange` + `[IO.File]::WriteAllLines(..., UTF8Encoding($false))`，写前后核 CRLF 计数不变（`Set-Content` 会加 BOM / 改行尾）。
- Shell：命令以 `cd X;` 开头会吞输出 → 用 `git -C` / 显式路径 / `| Out-String`；`python -c` 含引号 → SQL 放 `$q` 变量经 `sys.argv[1]`；临时文件一律 `%TEMP%\r8x\`，不进仓库。
- 发版对账：回归红先串行复跑，再与上版 release commit 临时 worktree 对照，只有「本版红 / 上版绿」算引入；媒体产物按 `.cursor/rules/media-artifact-validation.mdc` 验 magic bytes。
- 台账动作：`tools/duty_reply.py --group neice --ticket N --text-file F [--status confirmed] [--dry-run]`、`duty_ledger_fix.py --mark-fixed-file JSON [--apply]`、`duty_update_hint.py --set`、`duty_notify_summary.py --reporter 8942577244 --ids … --msg-id … --apply`；频道播报复制 `tmp/r79_channel_post.py` 改 campaign。

## 六、必读文件

`docs/Q批_进度总账.md`（进度表 + 拍板记录）→ `docs/修复指令_Q批_并行_2026-09-09.md`（投放 / 去向 / 归属矩阵 / 决策 D-Q1～D-Q11）→ `docs/分析_1.0.81首日问题归类与工作台三视角方案_2026-09-12.md` + `docs/分析_聊天工作台_五视角美化优化方案_2026-09-12.md`（Q-30～Q-34 的方案原文）→ `docs/发版对账_v1.0.84_发版.md`（最近一次全链真值）→ `.cursor/rules/chatx-desktop-install.mdc`。

## 开新对话粘贴（接手值守线）

```
读 docs/交接_值守与指令线摘要_2026-09-12.md，接手值守 / 指令 / 发版线。先读 docs/Q批_进度总账.md 进度表与「老板拍板记录」最后三行，再 scripts\agent_probe.ps1 看 ACTIVE 意向（被拦就直接读 D:\chengjie-instances\.ops\agent_intents\*.txt）。职责：① 持续拉 D:\chengjie-instances\.ops\duty_watch_loop.log 的 NEW TICKET 与 tmp_diag\<码>\ 报告，用 python -c + ?mode=ro 只读查 bug_intake.db，新单先核对是否已在 HEAD / 渠道版本 1.0.84 修复、能归入已有 Q 线就写去向表，归不了就出新 指令_Q-n 文件（事实 / 段表 / 红线 / 粘贴块）并登记总账行 + 底座投放表 + 去向表，只提交 docs；② 老板说「发版」时按 .cursor/rules/chatx-desktop-install.mdc 单一版本链走 R85（基点 HEAD，先确认 zhiliao 重启窗口装载 Q-29 / 接力记忆 .py；干净 worktree D:\boundless-r80 复用；回归红与上版 release commit 对照；发公共 + 内测桥接同字节 + 官网五值 + 坐席三台 + 台账 fixed + 回访 + 频道 + 发版对账 + 总账 R85 行）；③ 不碰别线工作树未提交文件（Q-26 C–E / 值守工具改动），git add 显式路径且提交前 git diff --cached --name-only 为空；④ 每次值守结束在总账「老板拍板记录」加一行（时间 / 新单 / 去向 / 下一步）。当前未开工线：Q-30（P0）Q-31 Q-32 Q-33 Q-34 Q-35 Q-36 Q-28 与 Q-26 C–E，粘贴块在各指令文件文末。
```
