# 指令 Q-12：情感陪伴引擎 v1（1.0.80 专项立项——R79 发出后开）

> 预算 **$250**（专项，允许分两个对话：v1 接线 $150 + 仪式与评测 $100）。工单 **#264**（SXKKPA / 3CS9MQ；Q-0 拆单后以「需求：情感陪伴引擎」新单号为准）。目标版本 **1.0.80**。决策 **D-Q8**。
> **前置**：R79 已发（1.0.79 的 Q-1 已知即不问 / Q-2 现实承诺守卫 / Q-3 风险闸 / Q-5 抽取 已到用户手里）——调情必须建在「不乱承诺、记得住、不重复问」之上。
> 底座 `docs/修复指令_Q批_并行_2026-09-09.md`。落点表 `docs/发版对账_v1.0.80_Q12.md`。

## 开工前必读

1. `docs/聊天核心能力_三视角方案_2026-09-09.md` §1 需求行、§2.2 第 5 条、§3 Q-12。
2. `tmp_diag\SXKKPA\ 3CS9MQ\` 全文（九条方案 + 落地顺序：先接线 → 再仪式与推拉 → 最后风格预设与看板）。
3. 代码事实：启动日志「ContactGateway 已接入 intimacy_engine」、关系阶段 初识 → 试探 / 升温 → 暧昧陪伴 → 稳定陪伴、`speech_emotion` 配置、`proactive_topic`（Q-8 已开）、`reactivation_loop` 未启用；右栏「初识 100% · 轮次 78 · 亲密度」只计数**未进提示词**。锚点：`src/contacts/gateway.py`（intimacy_engine 接入）、`src/companion/`（阶段判定 / `companion_proactive.py` / `care_dispatcher.py` 仪式走真发 + 节奏）、`persona_reply.py` addenda 接线点（Q-6）、`spoken_style` few-shot（O-1 C）、学习队列（`daily_learner.py`）。
4. 边界（不可越）：危机检测最高优先（`wellbeing.crisis_*`）；调情强度受平台规则上限；**现实承诺守卫（Q-2）与停联冻结（O-1 A）不受战术包覆盖**；对方不适自动降档。

## 红线

- 只动 `src/contacts/gateway.py` 接线处、`speech_emotion`、新 `src/companion/tactics/`（战术包）、`care_dispatcher` 仪式入口（只加一类关怀项）、评测脚本、i18n、tests；`persona_reply.py` 经 Q-6 addenda 接线点加一个 addendum（写总账）。
- 不发版前提：**30 条含 低落 / 挑逗 / 无聊 三类客户消息，新旧盲评胜率 ≥80%**；未达标不进 1.0.80。
- `git add <显式路径>`；每 commit 带工单号。

## A 接线：阶段 + 情绪进提示词

- `tactics/stage_pack.py`：每阶段战术包（亲密词汇 / 昵称层级 / 调情强度 1–5 / 话题范围 / 消息长度节奏 / 每日仪式 / 5–10 条 few-shot）：初识 = 好奇 + 轻逗弄 + 多问少说；试探升温 = 推拉 / 玩笑挑逗 / 专属昵称 / 回调细节；暧昧陪伴 = 早晚安仪式 / 「想你了」主动 / 未来感 / 撩拨留白；稳定陪伴 = 情绪价值 / 倾听共情 / 惯例联系。阶段由 intimacy_engine 判定，用户可锁 / 降阶（人设页一格）。
- `speech_emotion` 扩到文本：判 孤独 / 开心 / 低落 / 挑逗 / 生气 / 无聊 → 回应模式 安抚 / 逗弄 / 肯定 / 好奇 / 撩；**低落先共情不调情**。
- addendum：「当前阶段 X · 情绪 Y · 本轮策略 Z」+ few-shot。日志 `[tactics] conv=… stage=… emotion=… mode=… intensity=…`。

## B few-shot 取真实高分对话

- 学习队列（`daily_learner`）改为按阶段收集真实高分对话（人工采用 / 客户正向回应）做 few-shot 池；47KNBV 截图样本入池；私事过滤沿用 O-2 C `memory_scope`。

## C 主动仪式

- 按阶段排 早安 / 晚安 / 午间 / 「想你了」，走 `care_dispatcher` 真发 + 节奏（O-1 D）+ 安静时段（Q-4 D 口径）+ 停联 / risk_hold 约束；每日上限按阶段；日志 `[ritual] …`。

## D 推拉与留白控制器

- 由引擎按阶段与对方投入度决定 回复延迟 / 主动频率 / 留白（留白 = 有钩子的暂停，非告别——与 Q-8 D 退场闸兼容）；与拟人节奏（O-1 D）联动。

## E 风格预设 × 强度

- 人设调情风格 4 原型（俏皮逗弄 / 温柔黏人 / 神秘冷感 / 直球大胆）× 强度 1–5；多语言调情词库；对方不适（负面情绪 / 拒绝词）自动降档并记日志。

## F 评分 + 盲评门禁

- 每回复四维评分（共情 / 个性化 / 推进 / 调情）落日志 + 看板卡；业务指标：回复率 / 次日回访 / 客户主动开口占比 / 「miss you」信号；`scripts/companion_blind_eval.py`：30 条三类消息新旧对比盲评，胜率 ≥80% 才发版。

## 收工

- 落点表 `docs/发版对账_v1.0.80_Q12.md`（§〇 v1 边界与不做清单）；`docs/Q批_进度总账.md` 只改 Q-12 行；`agent_probe.ps1 -Done`；回访文案交发版线。

## 开新对话粘贴（R79 发出后）

```
读 docs/指令_Q-12_情感陪伴引擎v1_2026-09-09.md，按它做 Q-12（#264 / SXKKPA：新 src/companion/tactics/ 阶段战术包 + speech_emotion 文本情绪 → 回应模式（低落先共情不调情）→ 经 Q-6 addenda 接线点进提示词 / 学习队列按阶段收真实高分对话做 few-shot / 主动仪式走 care_dispatcher 真发 + 节奏 + 安静时段 / 推拉留白控制器与拟人节奏联动 / 4 原型 × 强度 1–5 + 不适自动降档 / 四维评分看板 + companion_blind_eval 30 条盲评 ≥80% 门禁）。预算 $250（可分两对话）。前置：R79 已发 1.0.79；先读 docs/修复指令_Q批_并行_2026-09-09.md 与 D-Q8、docs/发版对账_v1.0.79_Q2.md（现实承诺守卫不受战术包覆盖）、Q-6 落点表 addenda 接线点，再 scripts\agent_probe.ps1 -Intent 登记；危机检测最高优先、平台规则上限、停联冻结不可越。每个 commit 带工单号并立刻补落点表 docs/发版对账_v1.0.80_Q12.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。盲评未达 80% 不进 1.0.80。收工只改 docs/Q批_进度总账.md 的 Q-12 行并 agent_probe -Done。
```
