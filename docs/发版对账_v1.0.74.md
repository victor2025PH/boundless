# 发版对账 v1.0.74（骨架，2026-09-05 J-4 建）

> 接 `docs/J批_进度总账.md`。1.0.73 已装 yunsheng；kouxing / lianbei 当时 LAN 不可达未装。
> 本版是 **J 批第一波（J-1 … J-5）整包**。五份落点表由值守在「全批收口」时合并进本文件，
> 本骨架只留位 + 已知的合并前提。**以各批落点表为准，本文件是索引。**

## 本包必含（按批留位，值守合并时逐行搬入）

| 批 | 主题 | 单号 | 落点表 | 合并状态 |
|---|---|---|---|---|
| J-1 | AI 出站内容正确性 + 记忆一致性 | #171 #175 #176 #177 | `发版对账_v1.0.74_J1.md` | 待合并（A `7a3fc6c9` / B `9d6426f3` / C `24229010` / D `5d46c4ee`；#177 接线 `dc923110` 由 J-3 落） |
| J-2 | 目标 / 工作链 / 自动化档位 | #166 #168 #167 | `发版对账_v1.0.74_J2.md` | 待合并（A1 `d0aa6560` / A2 `e4a62ee5` / B `983e05ba` / C `45261c78`；**D1b 进行中，收工后追加**） |
| J-3 | LINE 媒体链 | #169 #172 #180（+#181 观测） | `发版对账_v1.0.74_J3.md` | 待合并（A `c9d19638` / B `28ce86ba` / C `e3573a66`+`65e122a2` / #177 接线 `dc923110`） |
| J-4 | 工作台 UI 小件 | #170 #173 #164 #179 #178 #174 nag（+#188、J-2/J-3 交办四项） | `发版对账_v1.0.74_J4.md` | **已完成**（A `09bd0294` / B `3c101f0e`+`7b56f894` / C `345241a6` / D `17230974` / E `ed1b746e` / F `38bfda40` / G `9f045de9` / 交办 `3c1213e6` `c9759aea` `500d9b4d` / #188 `3fee4790`） |
| J-5 | 登记链与值守工具 | （bug_intake / duty 工具） | `发版对账_v1.0.74_J5.md` | 待合并（A `da1bbb86` / B `7be18026` / C `9ec70d37` / D `1287f489` / E-1 `6ed68d53`（已 --apply）/ E-2 `e4cf6896` / F `5f4f68f1` 待下发） |

## 合并前提（值守核对）

- [ ] **12:30 重启窗装载 zhiliao**：J-1 / J-2 / J-3 / J-5 全部 `.py` + J-4 三处 `.py`（#179 `persona_routes.py`、G `admin.py`/`goal_routes.py`、J-3 交办 `unified_inbox_account_routes.py`）在装载前均**未生效**
- [ ] D1b（J-2 追加）收工提交 + 落点表追加；期间 `goals/**` / `cp-goal.js` / `config.example.yaml` 不动
- [ ] I-2 线 09-04 的 #156/#159/#155 hunk（`unified_inbox.html` / `unified-inbox.css` / `inbox_workspace.py`）**仍未 commit**——1.0.73 已发的 #159 与 J-4 A 的浮层验收都依赖它，打包前必须落 commit（见 J4 落点表「树上仍有他线未提交」）
- [ ] 全量回归 `python -m pytest tests/ -n auto -q --timeout=90 --timeout-method=thread`；`scripts\gate_sweep.ps1 -Full`
- [ ] `docs/值守症状族对账单.md` 更新（#164/#170/#173/#174/#178/#179/#188 症状族归位）

## 刻意不做 / 不拦发版（各批汇总，值守合并时补全）

- J-2 D1b 老板未拍板项（冲刺豁免 care 闸 / `sprint.platforms` 加 wa+line / 自然档随 bridge 出厂开）——UI/日志已如实标注，不兜售
- J-3：LINE 入站缺省 `DEFAULT_INBOUND_MAX_BYTES` 20MB → 32–40MB（一行常量 + 一条测试，`line_media.py`）——等 J-3 线或值守
- J-3 D / #181 WA 断连：只观测，根因线索交 J-6
- J-5 E-3：61 单 fixed 未回访只记数，等老板批文案
- J-4：ops 卡「已发假声明」行（J-1 A 提到）未做，交值守 backlog
- `config.example.yaml` 两处文档键（J-1 A `sent_claim` / J-2 B `max_per_tick`）等 D1b 收工后补

## 发布动作（照 1.0.73 模板，值守填）

1. `desktop/package.json` → 1.0.74 / 1.074
2. `npm run dist:win:smart`（内测）/ `npm run dist:win:clean`（对外）→ 记体积
3. 坐席：skuio / 钧 两位内测机先装（报障人）；yunsheng / kouxing / lianbei 按 `chatx-desktop-install` SOP
4. `publish_chatx.ps1 -DistDir dist-clean -Yes`：VPS sha512 / R2 / `latest.yml`
5. changelog + `chatxContent.ts` 对齐；`deploy.ps1 -SkipAssets`
6. 频道 @hykj7 发版消息（只发频道）
7. 按 #号逐单回访（含 I-6 那 61 单，等老板文案）
