# 发版对账 v1.0.74 — J-6（WhatsApp 边车稳定性：403 无限重连 · ENOTFOUND 分类 · Bad MAC 自愈 · #181 配对时延）

> 指令：`docs/指令_J-6_WhatsApp边车稳定性_2026-09-05.md`。总账：`docs/J批_进度总账.md` J-6 行。根因清单沿用 `发版对账_v1.0.74_J3.md`「D 观测」，未重新分析。
> **本条不发版、不重启生产实例、不重启 117 边车。** Node 边车改动（`services/whatsapp-baileys/**`）随桌面包 1.0.74 到用户机；117 实例侧边车 + `platform_session_health.py` 由值守在下个重启窗一并装载。模板/i18n pack 改动已热更（ops 卡「🔌 平台会话健康」新 KPI 行在 Python 侧装载前只是不显示，不报错）。
> 接手：先读本表逐行核对，再 `git -C D:\boundless log --oneline -15 -- engines/chengjie/services/whatsapp-baileys` 确认三个 commit 真在。
> ⛔ 未碰：`unified_inbox.html`（J-4）、`unified_inbox_send_routes.py`、`store.py`、`drafts.py`、`whatsapp_baileys_login.py`（B-2 的坐席可见提示因此只做到后端字段，见「交 J-4 / 值守」）。
> 真机：未动任何 WA 号（含 117）。全部证据来自 J-3 已核的两机日志 + 本地单测；真机复核项列在「#181 待核」。

## 落点表

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注/剩余 |
|---|---|---|---|---|---|
| A 403 forbidden 终态 | skuio 88MP86 风暴（`wa_6mvl0luy` 10h 38 轮） | `aefd9a94` | `services/whatsapp-baileys/close-policy.js`（`CLOSE_CODES.forbidden=403`；`nextForbiddenState` 纯状态机：连续 403 计轮，中间任何非 403 close / open 即清；`forbiddenRoundsMax(env)` 读 `WA_FORBIDDEN_ROUNDS` 默认 **2**、0=关；`decideCloseAction` 新动作 `{action:"forbidden"}`，并按 `forbiddenStreak/attemptsPerRound` 折算轮数——编排器每 tick 打 `/reconnect` 把 `_reconnectAttempts` 清零时「giving up」永不发生，只按轮数永远判不到终态，这是首版单测没抓到、看编排器代码才补的）；`server.js`（消费 `forbidden`：条目 `status="forbidden"`、清快/慢重连定时器、`postStatus("forbidden", "[rc:forbidden] 账号被 WhatsApp 限制（403）…")`；`/accounts/:id/reconnect` 对 forbidden 账号 **30min 冷却**（`WA_FORBIDDEN_RETRY_MIN`）内回 `{ok:true, forbidden:true, retry_after_sec}` 不重连——编排器自身有 `MAX_RESTARTS=8` 熔断，两层叠加把「无限重连」压成「≤8 次 forbidden 应答后编排器放弃」；`/health.forbidden` 计数；logout 清状态）；`src/integrations/platform_session_health.py`（`UNHEALTHY_STATUSES` + `forbidden`，registry 翻 offline）；`src/web/templates/ops_overview.html`（行状态 ⛔ 403 已封禁/受限 + tooltip；按钮改「解锁重连」，冷却内 alert 而非假成功）；`src/web/i18n_packs/ops_overview_page.py`（`ov2_js_ps_forbidden/_tip/_unlock/_unlock_ok/_forbidden_cooldown` zh+en）；`test/close-policy.test.js` | Node `npm test` 全绿；Python `test_platform_session_health` / `_selfheal` / `i18n_coverage` / `ops_overview` / 五个模板门禁全绿 | 401 / 428 语义**零改动**（2026-07-22 事故回归钉保留）。首轮 403 仍走完一轮退避才计轮（防瞬时 403 误判） |
| B ENOTFOUND 分类 + DNS 短退避 + 配对计时 | 钧 3PZ95W（408 ×29 全是 `getaddrinfo ENOTFOUND`）/ #181 | `4e9b2c2b` | `close-policy.js`（`REASON_CLASSES` = dns/net/server/forbidden/logged_out/restart/other；`classifyCloseReason(code, reason, errCode)`——Baileys 把 DNS 失败包成 408，**必须看 error.code/文案而不是 statusCode**；`ReasonWindow` 10min 滚动窗 + 进程累计；`dnsRetryConfig(env)` 读 `WA_DNS_RETRY_MS`/`WA_DNS_RETRY_COUNT` 默认 3s×10；`reconnectDelay(p)`：dns 阶段固定 3s **不烧常规重连预算**，超次数回常规指数曲线）；`scan-signal.js`（`pairingObservation(entry)`：`pairing_ms` / `pairing_dns_fails` / `hint_code="dns_retry"`，阈 60s）；`server.js`（`noteCloseReason` 统一记账，含 `startLogin` 抛异常这条**不产 close 事件**的路径；`postStatus(detail="[rc:<class>] …")` 前缀；配对起点跨 entry 重建继承；`/login/:id/status` 多回 `pairing_ms/pairing_dns_fails/hint_code`；`/health.reason_10m/reason_total/dns_retry`；logout 清 `_dnsStreak/_lastReason`）；`platform_session_health.py`（`parse_reason_class`：只认句首 `[rc:]`，未知类归 other；`by_reason_class` 只数不健康事件；Prom `platform_session_close_reason_total{reason_class}`）；`ops_overview.html`（KPI「掉线原因分类」Top4）；`ops_overview_page.py`（`ov2_js_ps_reason_class`）；**新** `test/close-reason.test.js`；`tests/test_platform_session_health.py`（+3） | Node 全绿；Python 同上集全绿 | **B-2 坐席可见提示只到后端**：`hint_code=dns_retry` 已在 `/login/:id/status`，但 Python 轮询层 `whatsapp_baileys_login.py` 与消费面 `unified_inbox.html` 都不在本条可改清单（前者未列、后者 J-4 禁碰）——见「交 J-4 / 值守」 |
| C Bad MAC 自愈 | skuio 报告尾 `Session error: Bad MAC` 跨 3h | `ed20b1a0` | **新** `services/whatsapp-baileys/bad-mac-heal.js`（纯函数：`isBadMacStub`（CIPHERTEXT stub + 文案含 Bad MAC）/ `isPeerPlaintext` / `peerSessionJids`（participant→remoteJid，含 `@lid` alt，剔群/广播）/ `maskJid`（只留域+末 4 位）；`BadMacTracker` 按 `(loginId, jid)` 连击，阈 `WA_BAD_MAC_THRESHOLD` 默认 **3**、0=只观测；heal 后 `WA_BAD_MAC_COOLDOWN_MIN` 默认 30 不重删；明文清连击；`snapshot/snapshotFor/badMacDetailTag`）；`server.js`（`messages.upsert` 挂 `observeBadMac` → 阈值到即 `signalRepository.deleteSession(jids)` 让下一条消息 prekey 重建——**绝不 app-state resync、绝不动其他对端**；`/health.bad_mac`；节流 60s（heal 强制）经 `postStatus("authorized", "[bm:total,peers,heals,active] bad-mac")` 把累计快照带给 Python；logout 清）；`platform_session_health.py`（`parse_bad_mac`、每会话快照（标签从直显 detail 剥掉）、`dump().bad_mac` 聚合含 `accounts`、Prom `platform_session_bad_mac_total/_heals_total`）；`ops_overview.html`（KPI「Bad MAC 解密失败 / 已自愈 / 连击中对端」，零流量隐藏）；`ops_overview_page.py`（`ov2_js_ps_bad_mac/_tip`）；**新** `test/bad-mac-heal.test.js`；`tests/test_platform_session_health.py`（+2） | Node **63/63**；Python 指定集 **493 passed**；`test_alert_delivery_e2e` 95/96——唯一红 `phantom_unread_alert` 属他线（`health_watchdog.py` 13:53 未提交 194 行），非 J-6 | 既有「Bad MAC → app-state resync」路径未动（那是 Baileys 内部行为，本条只加对端级处置）。快照是累计值，漏发不影响正确性 |
| D #181 真机复核 | #181 | —（不做） | — | — | 等 `dr-mtnarhxg-o8apf7` 的 wa-sidecar **00:20–00:25 段**核 408/ENOTFOUND 次数与 515 时刻（值守，钧机包到手后）。装 1.0.74 后重扫一次，`/login/:id/status.pairing_ms` 与 `hint_code` 直接给出「等了多久、DNS 失败几次」 |

## `postStatus` 状态值清单（给 J-4 / ops 卡消费）

| status | 新/旧 | detail 形态 | 含义 / 处置 |
|---|---|---|---|
| `authorized` | 旧 | 空，或 **新** `[bm:total=T,peers=P,heals=H,active=A] bad-mac`（C，节流 60s） | 在线。Python 侧 `parse_bad_mac` 解标签后从直显 detail 剥掉 |
| `pending` / `reconnecting` / `failed` / `logged_out` / `expired` | 旧 | **新** 句首可带 `[rc:dns|net|server|forbidden|logged_out|restart|other] ` 前缀（B） | 语义不变；前缀是给坐席看的（`[rc:dns]`＝本机解析/代理问题，不是 WhatsApp 掉线），Python 另存 `reason_class` 聚合 |
| `forbidden` | **新**（A） | `[rc:forbidden] 账号被 WhatsApp 限制（403），需换号或申诉后手动重新配对` | **终态**，与 `logged_out` 平级：边车停快/慢重连；ops 卡行 ⛔ + 按钮「解锁重连」（30min 冷却内 `/reconnect` 回 `forbidden:true, retry_after_sec`）。Python `UNHEALTHY_STATUSES` 已含，registry 翻 offline，走既有 `platform_session_alert` 别名（无新 publish，故 e2e 表无需加行） |

`/login/:id/status` 新字段（B）：`pairing_ms`（配对起点到 open；0=未起/未定）、`pairing_dns_fails`、`hint_code`（`"dns_retry"` ＝ 配对 >60s 且期间有 DNS 失败；否则空）。

`/health` 新段：`forbidden`（账号数 + `forbidden_rounds_max/forbidden_retry_min`）、`reason_10m`/`reason_total`（按 class）、`dns_retry`（配置 + `in_phase`）、`bad_mac`（`bad_mac_total/bad_mac_peers/active/heals/threshold/cooldown_min/top[]` jid 打码）。

## 环境变量（边车，全部有缺省，不配即旧行为 + 新保守值）

| 变量 | 默认 | 说明 |
|---|---|---|
| `WA_FORBIDDEN_ROUNDS` | 2 | 连续 N 轮全 403 判终态；0=不判（回 J-6 前行为） |
| `WA_FORBIDDEN_RETRY_MIN` | 30 | forbidden 账号 `/reconnect` 冷却分钟 |
| `WA_DNS_RETRY_MS` / `WA_DNS_RETRY_COUNT` | 3000 / 10 | DNS 类 close 的短退避；count=0 关 |
| `WA_BAD_MAC_THRESHOLD` | 3 | 同对端连续 Bad MAC 达阈删其 Signal 会话；0=只观测不删 |
| `WA_BAD_MAC_COOLDOWN_MIN` | 30 | 删后不重删窗 |

## 交 J-4 / 值守

- **交 J-4（`unified_inbox.html` + `whatsapp_baileys_login.py`）**：B-2 的坐席可见提示。后端已就绪：`GET /login/:id/status` 回 `hint_code="dns_retry"` + `pairing_dns_fails`。接法两行：① `whatsapp_baileys_login.py` 轮询把 `hint_code/pairing_dns_fails` 透传进登录状态响应；② `unified_inbox.html::_applyConnectLiveHint` 认 `dns_retry` → 文案「网络解析 WhatsApp 服务器失败 N 次，正在重试（DNS 问题，检查本机网络/代理）」（i18n 键建议 `inbox.wa.hint_dns_retry`，J-4 自加）。
- **值守**：① 下个重启窗（12:30 / 22:30）装载 `platform_session_health.py` + 重启 117 的 whatsapp-baileys 边车（`services/whatsapp-baileys/start.ps1` 既有入口）；装载后 `GET /health` 应见 `forbidden/reason_10m/bad_mac` 三段。② `test_alert_delivery_e2e::test_source_alerts_all_have_e2e_payload` 当前红是 `phantom_unread_alert`（他线 `health_watchdog.py` 未提交），催该线补 `_EMITTED_ALERTS` 行，非 J-6。③ 1.0.74 装机后回看 skuio 88MP86：`wa_6mvl0luy` 应在 2 轮 403 后进 `forbidden`，日志从 185 条重连降到个位数；钧机 3PZ95W：408 应全部带 `reason_class=dns`。

## #181 待核

| 项 | 谁 | 何时 |
|---|---|---|
| `dr-mtnarhxg-o8apf7` wa-sidecar 00:20–00:25 段：408/ENOTFOUND 次数 + 515 时刻 | 值守 | 钧机包到手 |
| 装 1.0.74 后重扫一次配对：读 `/login/:id/status.pairing_ms`，若 DNS 短退避生效应 <60s（此前 4–5min） | 值守 | 钧机装机后 |
| skuio 88MP86 装机后 24h：`/health.forbidden` 应 ≥1、`giving up` 不再每 15min 一轮 | 值守 | 装机后次日 |
