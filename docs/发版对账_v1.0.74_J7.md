# 发版对账 v1.0.74 · J-7 / K-2 人设工作室体验与报错（2026-09-05）

> 指令：`docs/指令_J-7_人设工作室体验_2026-09-05.md`（K 批编号 K-2）。预算 $150，实花约 $110。顺序 C → E → A → B → D → F，**六件全部完成、全部 commit**（19:3x–21:0x，10 个 commit）。
> 报障人 skuio（DC8F，1.0.73），证据＝Cursor 报告 `tmp_diag\{HKGZ7C,DQVF55,5BV4U4,K5XHJ2,G9DACM,QEDG4X}\`。
> 开工条件：J-4 已做 #178 `ed1b746e` / #179 `17230974` 并释放 `personas.html` / `persona_routes.py`——**#178/#179 不由本条代做**。
> 模板 / i18n / CSS / 静态 JS = 热更新即上生产（每件双戳 `ui-build.txt`，末戳 `20260905-2100`）。
> **装载状态**：K-5 ①段 20:50:56 重启 zhiliao 装到 `b513ded9`（含本条 C/E/A/B/D 的全部 `.py`：`persona_stock.py` / `persona_media_routes.py`）；**F 的 `web_user_store.py` / `auth_user_routes.py`（`547c3c8a`）在其后，等下一次装载**；E 的 `desktop/main.js` 随 1.0.74 装机。
>
> **commit 清单**：C `8b1f7775` + 跨页 `1dbda120` / E `e46c615d` + 壳 `324f92ef` / A `7c9e39d2` / B `7466f250` / D `b513ded9` / F `547c3c8a`。
> 核对：`git -C D:\boundless log --oneline -12 -- engines/chengjie` 应能看到以上 10 条（K-3 / K-5 的 commit 夹在其间）。

## 落点表

| 项 | 工单 | 修什么 | 落点 | commit | 测试 | 状态 |
|---|---|---|---|---|---|---|
| C | #191 | 预览 tab「历史质检」点任一行 → 顶部红条「页面脚本出错，部分功能可能失效」 | **根因（异常字段结论）：不是数据字段脏，是 HTML 属性截断。** `_quizRenderHistory` 生成行 `onclick="_quizOpenHist(' + JSON.stringify(id) + ')"`，`report_id` 是 int → `JSON.stringify("5")` 带双引号 → 属性被截成 `_quizOpenHist(` → 点击即 `SyntaxError: Unexpected end of input` → `_boot_error_guard.html` 兜成红条（该守卫只从「X is not defined」提取函数名，SyntaxError 时 fn 为空，ops 卡上只见 type=SyntaxError 无从归因）。**每一行 100% 复现，与记录内容无关**；同形状的「📎 原文」链接 `_pdiShowSources` 从未能点开，一并修。修法：新 `_jsArgAttr(v)`＝`_escAttr(JSON.stringify(String(v)))`，两处内联实参改走它；历史列表逐行 `try/catch`（坏记录跳过并 beacon，不拖垮整张列表）；`_quizOpenHist` 渲染层单独 try/catch → 友好降级句 `psn_quiz_hist_open_fail`（zh/en）+ 新 `_feBeacon(fn,type)` 带函数名上报同一遥测端点。**新门禁** `tests/test_template_inline_handler_arg_quoting.py`：全站模板 + 静态 JS + shared 不得把 `JSON.stringify(...)` 裸拼进双引号 `on*` 属性（字符串拼接 / 模板字面量两形状），带 personas 两处回归钉 + 扫描器自检 | `8b1f7775` | 新门禁 4 例 + `test_inbox_inline_handlers_exported` / `test_template_orphan_refs` / `test_template_unique_ids` / `test_template_inline_js_syntax` / `test_template_duplicate_toplevel_functions` / `test_template_undefined_identifiers` / `test_template_cross_block_refs` / `test_i18n_coverage` 共 351 passed；ui-build `20260905-1947` | 在树（热更新已生效） |
| C 同类跨页 | （#191 衍生） | 新门禁首轮全扫命中 9 处同类哑按钮：`draft_review.html` 套用模板 / 建议句 / 模板推荐 / 发送翻译（4）、`knowledge.html` 未命中查询「→ 建条目」/「忽略」（2）、`workspace_dashboard.html` 坐席明细行 / 导出 CSV / 等待中明细（3）——全是点击即 SyntaxError | 各页在 `esc()` 旁加一行 `jsArg(v)`（JSON 字面量 + `&amp;`/`&quot;` 属性转义）替换 9 处。**越界说明**：三文件不在 K-2 归属矣，但树上无他线在途 hunk、意向板无人认领、且按「新门禁随首轮全扫一并修」惯例（CLAUDE.md 哑按钮 / 重复 id 门禁先例）处理；独立 commit 便于单独回退 | `1dbda120` | 同上 351 passed | 在树（热更新已生效） |
| E | #193 | 人设工作室弹「页面未能打开: /admin/voice-eval [尝试打开]」 | **真因不是指令列的三种**（路由缺失 / 角色门 / chunk 404）：`/admin/voice-eval` 一直注册在 `admin.py` L1875（`voice_eval_routes.py`，只走 `_page_auth` session auth，无角色门），侧栏 `voice_eval` 项随 `nav_schema` 正常渲染，入口与路由一致。链条＝「全局规则」tab 的 `beforeunload` 未保存守卫（`_grDirty`）→ **Electron 对 beforeunload 拦截的默认处理是静默取消导航**（不弹对话框，需 app 处理 `will-prevent-unload`）→ 点侧栏「声音评测」像没反应 → `_loading_overlay.html` 点击时已亮遮罩，15s 后文档仍在 → 误报「页面未能打开: /admin/voice-eval [重试打开]」。两层修：① 页面侧 `personas.html`：站内链接点击在 **window 捕获段**（先于载入层的 document 捕获监听）显式 `confirm`——留下 → `preventDefault`+`stopPropagation`（载入层不亮）+ 切到规则 tab + toast 提示保存；离开 → 清 `_grDirty` 放行；判定口径与载入层同源；i18n `psn_gr_leave_confirm`/`psn_gr_leave_stay` zh+en。② 壳侧 `desktop/main.js`：新 `wireUnloadGuard` 挂 `will-prevent-unload` → 原生「离开 / 留下」（默认/Esc＝留下），选离开 `preventDefault` 放行；接后台弹窗 / 主窗 / 工作台 webview 三处；`SHELL_STR` `unload.*` 5 键 zh+en。**越界说明**：`desktop/main.js` 不在 K-2 矩阵，树上干净、无人认领，独立 commit；随 1.0.74 装机生效 | `e46c615d`（页面）/ `324f92ef`（壳） | `tests/test_persona_leave_guard_193.py` 5 例（路由与入口一致 / 守卫形状 / 载入层让路 / 壳侧三处接线 / 词条双语）+ 模板门禁 25 passed；desktop `shell-i18n.test.js`（SHELL_STR 双语对等 + 主进程 CJK ratchet）/ `backend-popup-unique` / `ws-home-route` 全绿；`node --check main.js` 通过；ui-build `20260905-2001` | 页面侧在树（热更新已生效）；壳侧待 1.0.74 |
| A | #189 | 备货 tab：台词库（研发缓存当用户功能，专属 0/共享 11）/ 说话指纹「不适用」/ 就绪度卡 75% | **后端** `src/companion/persona_stock.py`（只被 stock-readiness 路由消费，随人设工作室线接管）：清单四项 `CHECKLIST_ITEMS = profile/voice/album/binding`；声音行「或选不发语音」——人设 `voice_profile.enabled=false` 或合并语音配置 `enabled=false` → `voice_off` 不适用；相册行只在 `capabilities.photos` 显式 true 时进分母（发图能力缺省关；素材数仍如实聚合供「已有 N 张在等开关」提示）；**新 binding 行** `collect_binding_usage(pid, pm, cfg)`：运行时账号注册表四平台 `meta.persona_ids`（removed 跳过）+ config `telegram/whatsapp_rpa/messenger_rpa` 的 `accounts[].persona_ids`/扁平 `persona_ids` + PM 会话绑定（`_profile_ref`/内联 id）+ 全局默认人设，任一命中即就绪，任一源读失败按 0；`lines` / `speech_print` 标 `internal=True` 永不进分母（数据保留给运营面板/高级区）；响应新增 `checklist` 数组，旧调用方不传 `binding` 时该行不适用（零行为变化）。`persona_media_routes.py` 路由喂 binding。**前端** `personas.html`：tab「备货」→「上线准备」、面板四行（相册关着 → 「去开启照片能力」；未绑定 → 「应用到账号…」直开 `PSApply`；旧后端无 binding 字段 → 显「随下次重启生效」）、**台词库整行 + 一键入库删除**（`_stockQuickLine` 删；ops「🎙️ AvatarHub 语音」卡 `prerender-lines/add` 入口保留）、**说话指纹只在 spoken_style 包可用时进「高级（运营）」折叠区**（缺包整块不出现）、语音 tab 留一句「常用短句会在夜间自动预合成以加快发送，无需维护」。i18n `psn_stock_*` 改口径 + 新 12 键 zh/en、台词库 8 键回收（`psn_stock_lines_*`/`psn_stock_add_fail`/`psn_stock_row_lines`/`psn_stock_pack_missing`）| `7c9e39d2` | `tests/test_persona_stock.py` 重写 15 例（含 #189 原景「专属 0/共享 11 → 满分」钉、声音 opt-out、相册缺省关、binding 四源 monkeypatch、路由端到端 checklist）；`test_impl74_b4alt_pins` B106 pin 改为「入库入口不得回到编辑器 + ops 卡入口必须保留」；模板/i18n 门禁 371 passed；ui-build `20260905-2021`（commit 信息误写 2031） | 前端在树（热更新已生效）；`persona_stock.py` / `persona_media_routes.py` **待装载**（装载前旧后端：binding 行显「随下次重启生效」、台词库仍在分母——前端徽标读的是服务端 `ready_count/applicable_count`，所以 75% 要等装载才变） |
| B | #190 | 相册 tab：试触发在底部 / 325 套表单 / 触发词全空 / 两套保存 | **顺带发现**：实施90 相册 UI 词条（AI 建议触发词 chips / 状态角标 / 搜索筛选 / 试触发被拦门等 50+ 键）09-01 `b7161370` 进了 `persona_apply_modal.py`，但 `personas.html` 侧在任何 commit 里都没落地——`auto_meta.triggers_suggest`（VLM 打标建议词）一直躺在库里没人消费，这正是「触发词全空」的一半原因。本次一并落地。`personas.html` 相册面板重写：① **试触发 + 搜索/筛选吸顶**（`.pma-sticky`，`.dpane` 是滚动容器），命中候选缩略图可点、首个命中自动翻到所在页、滚到并高亮（`.pma-hit` 动画），候选带 `blocked_by` 门（时段/季节/地点）半透明 + 悬停说明；② **卡片精简**：默认只显缩略图（优先 `thumb_url`）+ 触发词输入 + 一行「启用 / 命中 N / 🗑」，配文 / 多语配文 / 权重 / 关系门槛 / 重新识别收进 `<details class="pma-adv">`；状态角标只显异常（未启用 / AI 识别中 / 识别失败 / 敏感 / 疑似非本人）；③ **触发词引导**：AI 建议词 chips（去已有，点采纳 / 全部采纳）+ 建议关系门槛 chip；工具栏「🤖 AI 补标」（`retag-all only_missing`）+ 卡内「重新识别」；打标 pending 期间 4s 轮询列表（≤20 轮）刷新 chips；上传完新卡无触发词 → 自动切「缺触发词」筛选 + toast 引导 + 聚焦第一张；摘要行「N 个缺触发词」一键筛选；④ **统一保存**：卡片改动只标脏（`_pmaEdits`，与库值相同自动清脏，`.pma-dirty` 描边），底部吸底保存条「保存 N 处改动 / 放弃改动」逐条 PATCH（失败项保持脏态可重试），逐卡保存按钮 + `pmaSaveItem` 删除；切人设作废未保存改动 toast 点破。**性能**：分页 60 张 + 「加载更多（还有 N）」，搜索（触发词/配文/AI 描述/建议词/标签）+ 筛选 7 档，显示值＝库值 + 未保存改动（翻页/筛选/重载不丢输入）。i18n 新 12 键 zh/en，其余复用实施90 既有词条 | `7466f250` | `tests/test_persona_album_ui_190.py` 7 例（吸顶在网格前 / 高级折叠 / 无逐卡保存 / 建议词消费 / 上传引导 / 统一保存 / 分页筛选 / 词条双语）+ 模板门禁（含内联 JS 语法）39 passed + i18n 覆盖/路由 358 passed；ui-build `20260905-2038` | 在树（热更新已生效）；**真机验收**：325 张首屏只渲 60 张（目标 <1s），试触发命中后卡片高亮可见，全页只有底部一处保存 |
| D | #192 | 页头：带数字的下拉被截成「2…/7…」/ 两个「新建人设」/ 90 个标签占三四行 | ① `.pp-header` 内排序下拉 / 计数胶囊 / 按钮、概览「刷新」按钮一律 `flex:none`，新鲜度徽标 / tab 计数 / 计数胶囊 `nowrap`，快捷键「?」按钮定宽 `width:1.75rem;padding:0` 改 `min-width`（base `.btn` 自带 `overflow:hidden`，定宽必截字）。**⚠ 具体是哪个控件被截成「2…/7…」从 DOM 判不出**（05:17 的截图没进 zhiliao 媒体目录，最后一张是 03:02 的 1195），已把页头所有带数字控件全部防缩，**回访时请 skuio 补一张图确认**；② 删页头右上的「新建人设」，保留人设池工具栏那个（概览页自己的 CTA、首启引导里的各留一个，不同视图各一）；③ `persona_studio_core.js` `renderTagCloud`：默认只显高频 `TAG_CLOUD_TOP=12` 个 + 「更多 N 个标签 ▾」展开 / 收起（当前选中的长尾标签不藏），标签值改属性转义 + `dataset` 取值（旧写法把标签拼进 onclick 单引号串，含引号 / 尖括号的标签会炸——与 C 同一类）；`?v=20260905a`；i18n `psn_tags_more/less` zh+en | `b513ded9` | `tests/test_persona_header_192.py` 4 例 + 门禁 41 passed + i18n 覆盖 322 passed；`node --check` 通过；ui-build `20260905-2048` | 在树（热更新已生效）；验收：1024px 宽人设池头部无截断、页面只剩工具栏一个「新建人设」、标签云默认 ≤13 个 chip |
| F | #186 | 用户管理页：8 条 admin·master/127.0.0.1/Chrome 活跃会话（08-29→09-05 每天一条）/「登录：从未」与 8 活跃矛盾 / 本月用量 5203 无单位 / 没说是操作员账号 / 技术标注 | **真因**：桌面壳每次启动走 `POST /login` 的 `auth_token` 直登（`renderer backendLoginJS` / `bindBackendPopupLogin`）→ `create_session("admin", master, ip, ua)` 每次 INSERT 一行、旧行永不 revoke（壳从不 `/logout`）；该路径不经 `verify()` → `web_users.last_login` 永远空。**后端** `web_user_store.py`：`create_session(replace_same_device=True)` 同 `(username, ip, user_agent)` 旧活跃行随新登录作废只留最近一条（**只在令牌直登路径开**——账号密码登录不动，多机同 NAT 同 UA 的同名账号不敢一刀切）；空闲 > `SESSION_IDLE_DAYS=7` 的会话 `touch_session` 拒绝（要求重登）、`list_sessions` 懒标 revoked 不显；> 30 天登录时物理清理（原 `cleanup_old_sessions` 从未被调用）；新 `mark_login(username, fallback_role)`（常量用户名 `admin` 不存在 → 落到 master 角色）、`last_session_login_map()`。`auth_user_routes.py`：令牌直登接两者；`/users` 页 `last_login` 空时用最近会话创建时间兜底（悬停注明口径）。**前端** `users.html`：页头一句「这里管理的是智聊本身的操作员账号（谁能登录后台/桌面端），不是你的 Telegram/WhatsApp/LINE 聊天平台账号——那些在接入页」；「从未」走 i18n；用量 `5,203 字符 / 不限`；会话卡人话化——设备名（UA 含 Electron → 「智聊桌面端」，Edge/Chrome/Firefox/Safari/移动）+ 「操作员 @admin · 主帐号（全部权限）」+ 最后活跃，`IP / UA / role / jti` 收进「技术信息」`<details>`；区块标题「已登录的设备」+ 「同一台设备重复启动只保留最近一条；超过 7 天没动的登录自动失效」。`team_page.py` 新 11 键 zh/en | `547c3c8a` | `tests/test_web_sessions_recycle_186.py` 9 例（重启 3 次仍 1 条 / 不误伤他设备他用户 / 密码路径不回收 / 空闲过期 + 30 天清理 / mark_login 回落 / 路由接线 / 模板形状 / 双语）+ `test_web_users` + phase13 / phase5 / goal_routes 会话相关 + 模板门禁 + i18n 覆盖 467 passed；ui-build `20260905-2100` | 模板/i18n 在树（热更新已生效）；`web_user_store.py` / `auth_user_routes.py` **待装载**——装载前僵尸行仍在；装载后：下次桌面端启动即回收同设备旧行，>7 天未动的旧行列表里立即消失。**验收**：重启客户端 3 次后「已登录的设备」仍 1 条；主帐号卡「登录」显日期 |

## 验收

- 门禁（收工时在 HEAD 合跑）：指令列的 `test_inbox_inline_handlers_exported` / `test_template_orphan_refs` / `test_template_unique_ids` / `test_persona_media_routes` + 本条新建 6 个测试文件 + 模板内联 JS 语法 / 未定义标识符 / 重复顶层函数 / 跨块引用 / 动态点访问 / free-capture / ui-build 新鲜度 **113 passed**；`test_i18n_coverage` 多轮全绿；desktop `shell-i18n` / `backend-popup-unique` / `ws-home-route` 全绿。
- C（#191）：预览 tab → 历史质检 → 点任一行 → 下方展开该次报告（不再红条）；「📎 原文」可点开溯源弹层；构造 `items` 非数组 / `expect` 非串的脏报告 → 只出一句「这份报告暂时打不开」+ ops「🖱️ 前端哑按钮错误」卡出现 `fn=_quizOpenHist type=render`。值守可核：skuio 机 frontend-error 计数里 `page=/personas type=SyntaxError fn=""` 即此单，模板热更后应停止增长。
- E（#193）：全局规则改一条不保存 → 点侧栏「声音评测」→ 出 confirm（留下 / 离开），不再出「页面未能打开」；1.0.74 装机后在桌面端关闭带未保存改动的后台窗 → 原生「离开 / 留下」对话框。
- A（#189）：编辑器 tab 名「上线准备」；台词库整行不在；spoken_style 包缺席时无「说话指纹」行；zhiliao 已装载 → 徽标分母 ≤4，skuio 那个「3/4 · 75%」的人设应变 `n/n` 满分（若相册开关关着＝3/3）。
- B（#190）：325 张只渲 60 张 + 「加载更多」；试触发在顶部吸顶，命中后卡片绿框高亮并滚到；卡片只有「启用 / 命中 / 🗑」一行 + 「⚙ 高级」；改任一张 → 底部出「保存 N 处改动」；上传无触发词的图 → 自动切「缺触发词」筛选 + toast；「🤖 AI 补标」后卡片出现 ✨ 建议词。
- D（#192）：1024px 宽人设池头部无截断；页面只剩工具栏一个「新建人设」；标签云默认 ≤13 个 chip + 「更多 N 个标签」。**回访请 skuio 补一张「2…/7…」原图**，好确认那到底是哪个控件。
- F（#186）：装载 F 的 `.py` 后，重启桌面端 3 次 → 「已登录的设备」仍 1 条；主帐号卡「登录」显日期；用量显「5,203 字符 / 不限」；页头有操作员账号说明；会话卡 IP/UA 在「技术信息」折叠里。

## 刻意不做 / 交出去

- **交 K-5**：① 下次装载窗带上 `web_user_store.py` / `auth_user_routes.py`（F）；② `python -m scripts.i18n_hant generate` 再生繁体——`test_i18n_zh_hant` 覆盖率 ratchet **在本条动手前就已红**（98.95%，他线 170 键未再生；`zh_hant_auto.py` 树上有他线 230 行未提交，本条刻意不碰），本条又加 ~45 键（缺键回落简体不影响使用）；③ 1.0.74 打包含 `desktop/main.js`（E 壳侧）。
- **越界两处**（均树上干净、无人认领、独立 commit 便于回退）：`1dbda120` 改了 `draft_review.html` / `knowledge.html` / `workspace_dashboard.html` 各一行 helper + 9 处调用（新门禁首轮全扫命中的同类哑按钮）；`324f92ef` 改了 `desktop/main.js`（`will-prevent-unload`）。`src/companion/persona_stock.py` 只被 stock-readiness 路由消费，随人设工作室线接管。
- `_boot_error_guard.html`（全站共用）未改：SyntaxError 无函数名是浏览器给不出，改守卫无收益；归因靠各 handler 自报（本条 `_feBeacon` 范式）。
- B 未做：多选批量 / 系列分组 / 排序（实施90 词条里有，#190 没提）；拖拽上传（`pma_drop_hint` 词条留着）。
- #192 ①的「2…/7…」控件身份未确认——已把所有带数字的头部控件防缩；若补图后发现是 base.html 顶栏的用户名（`.user-btn .uname{max-width:80px}` 带省略号 + 下拉箭头，形态最像），那是 base 共用样式，另开小单。
- 台词库「入库」入口只保留 ops「🎙️ AvatarHub 语音」卡；说话指纹只在 spoken_style 包可用时于「高级（运营）」区可编辑。`psn_stock_lines_*` 等 8 个词条已回收，`zh_hant_auto.py` 里的对应旧键等再生时自然消失。
