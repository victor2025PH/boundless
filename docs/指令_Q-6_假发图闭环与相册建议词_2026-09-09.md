# 指令 Q-6：假发图闭环补全 + 相册建议词生效 + 提示词补丁接线

> 预算 **$160**。工单 **#263 #266**（D6V29H / J6RHBQ / GAWRU5② / CK8HCT / FFGMM9；Q-0 拆单后以新单号为准）。目标版本 1.0.79（第一波）。
> 底座 `docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-6 行、接口约定③）。落点表 `docs/发版对账_v1.0.79_Q6.md`。**前置**：P-3 已收工在 HEAD（`docs/发版对账_v1.0.79_P3.md`）——你在它之上补，不重做。

## 开工前必读

1. `docs/聊天核心能力_三视角方案_2026-09-09.md` §1 Q4 / Q12、§2.1 第 9 条、§3 Q-6。
2. 证据 `tmp_diag\D6V29H\ CK8HCT\`：Nori×Mike 23:19–23:25 四轮零 `send_media`（「here's that view I promised you」→「maybe it's still loading」→ 目标注入岔题 →「let me try sending it again」）；`image_autosend` 对该会话**零评估零 skip 日志**，同刻对 telegram 正常发图 → 模块可用、无相册匹配时静默放弃且不回喂模型；spiral 三次质问仍 False；相册 91 张：正式触发词全空、每卡片下 ✨ AI 建议词候选需逐张「全部采纳」，页面同屏「AI 已识别 91/91」与「91 张缺触发词」；随机兜底抓自拍答「风景照」（23:25「just found this one from a while back」）。
3. 代码事实：P-3 落点表 §未做（读它列的边界）；`src/inbox/image_autosend.py`（匹配 / 无匹配路径）；`src/ai/outbound_promise_guard.py`（P-3 已成动作型：`media_claim` / lie_caught 重发或实话）；`companion_selfie.detect_media_complaint` L1236；`album_semantic_recall.py`（匹配）；N-2 C `a1fb8d72`（相册上传默认 AI 识别 + 进度 + 补标引导——**没做「建议词生效 / 参与匹配」**）；`persona_media_routes.py`（autotag）；`persona_reply.py`（生成侧提示词——你是 Q 批唯一动它的线）。
4. 接口：`prompt_addenda.identity_addendum(persona, account)`（Q-1 提供）——你在 `persona_reply.py` 一处接线三个 addenda：相册可展示场景清单 / 本会话已发媒体清单 / 身份补丁；Q-1 未合并前用 `try: import` 兜底。
5. `scripts\agent_probe.ps1` 看 ACTIVE；`personas.html` 与 Q-2 分段（你只动相册区，前缀 `al-`，stage_hunks）。

## 红线

- 只动归属矩阵 Q-6 行；`autosend_worker.py` / `drafts.py` 不动。
- 原则不变：**要么发，要么拒，不许说发了**；被质问分支只允许 重发 / 实话 / 直接拒绝（禁 加载慢 / 再试 / 忘了）。
- `git add <显式路径>`；每 commit 带 `#263` / `#266`。

## A 生成前注入两张清单

- `persona_reply.py` 系统提示追加：①「相册可展示场景清单」（来自相册场景分类 E 段：自拍 / 室内 / 室外风景 / 美食 / 宠物 … + 数量）；②「本会话已发媒体清单」（近 7 天 mid / 场景 / 时间；为空写死「你从未给 TA 发过照片」）；③ Q-1 身份补丁。模型只能承诺清单内的场景。

## B 无匹配回喂

- `image_autosend` 评估每次都落日志 `[album_match] conv=… query=… candidates=n picked=… fallback=none|random`；无匹配 → 回喂生成侧「无可用照片（场景 X）」→ 只允许自然拒绝或延后（延后必须同时排一条真实定时发图任务，到点仍无图则改口实话）；**随机兜底默认关**（只有完全无词的相册才随机，且随机只在「泛化氛围」触发不在「索图」触发）。

## C `media_claim_blocked` + spiral

- 会话级 `media_claim_blocked` 10 轮：一次 lie_caught 后 10 轮内任何照片承诺句一律改写 / 拦下；spiral 阈值 = 同会话 2 次质问 → 降级审核 + 需人工置顶；`detect_media_complaint` 词表补 no picture / where? / didn't get / nothing came / still nothing / 没收到 / 哪呢。

## D lie_caught 分支动作（与 P-3 对齐、补齐）

- 第 1 次：有 mid → 重发原图 + 短话；无 → 注入纠正指令（承认未发、禁加载 / 再试 / 忘了）并 `media_claim_blocked`；第 2 次：降级审核 + 人工置顶；目标注入在 blocked 期间**不得岔题**（`goal-inject` 查此标志——写总账让 Q-1 加一行判断，或你在注入调用侧过滤）。

## E 相册建议词生效 + 三段计数 + 匹配权重 + 场景分类

- 建议词默认直接生效为触发词并标「AI 标注」（可配：`album.autotag.apply=auto|confirm`，默认 auto）；顶部「一键采纳全部 N 张建议词」；文案三段计数 已确认 / AI 建议 / 无建议，「缺触发词」→「待确认」；匹配：未确认建议词低权重（0.6）参与，只有完全无词才随机；场景分类字段（自拍 / 室内 / 室外风景 / 美食 / 宠物 / 其他）由识图产出并可改，供 A 段清单；多选批量加触发词；`al-` 前缀。
- 验收：91 张导入 → 计数 已确认 0 / AI 建议 91 / 无建议 0，一键采纳后 已确认 91；索「风景」→ 不再抓自拍。

## F addenda 接线（含 Q-1 身份补丁）

- `persona_reply.py` 单一接线点 `_prompt_addenda(persona, account, conv)`；tests：三个 addenda 各出现一次、为空不出现。

## 收工

- 落点表 `docs/发版对账_v1.0.79_Q6.md`（§〇 「为什么第四轮还在圆谎」：无匹配静默 / 模型不见媒体 / 建议词没生效 三段）；`docs/Q批_进度总账.md` 只改 Q-6 行；`agent_probe.ps1 -Done`；回访文案交 Q-0 / R79；门禁「10 条要照片只发图或拒绝」并入 Q-2 F 的 `commitment_gate`（或自建 `media_gate.py`）。

## 开新对话粘贴

```
读 docs/指令_Q-6_假发图闭环与相册建议词_2026-09-09.md，按它做 Q-6（#263 #266：persona_reply 生成前注入「相册可展示场景清单 + 本会话已发媒体清单 + Q-1 身份补丁」单一接线点 / image_autosend 每次评估落 [album_match] 日志、无匹配回喂只许拒绝或延后且随机兜底默认关 / 会话级 media_claim_blocked 10 轮 + spiral 阈值 2 + 复诉词补全 / lie_caught 第 1 次重发或纠正、第 2 次降级人工、blocked 期间不岔题 / 相册建议词默认生效标「AI 标注」+ 一键采纳 + 三段计数 + 低权重匹配 + 场景分类 + 多选批量）。预算 $160。先读 docs/修复指令_Q批_并行_2026-09-09.md 归属矩阵与接口约定③、docs/发版对账_v1.0.79_P3.md（在 P-3 之上补，不重做），再 scripts\agent_probe.ps1 -Intent 登记；你是 Q 批唯一动 persona_reply.py 的线；autosend_worker.py / drafts.py 不动；personas.html 只动相册区 al- 前缀并用 stage_hunks。每个 commit 带 #263 并立刻补落点表 docs/发版对账_v1.0.79_Q6.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-6 行并 agent_probe -Done。
```
