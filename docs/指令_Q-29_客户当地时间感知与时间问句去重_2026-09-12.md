# 指令 Q-29：客户当地时间感知——已知城市即知时区，AI 不再问「你那边白天还是晚上」

> 预算 **$80**。工单 **#305（2RKH3H，jun / B990，1.0.82，09-12 05:27）**：十翼会话客户已说过当地时间（「白天也上班，晚上…」），AI 又问「白天还是晚上」。落点表 `docs/发版对账_v1.0.84_Q29.md`。
> 前置：Q-1（槽位三态 + `repeat_question_guard`）/ Q-8 F（人设当地时间注入）/ Q-25（FactGate）已收工。

## 事实（证据 `tmp_diag\2RKH3H`；代码只读核对）

- 03:50 `[goal-inject] state=…location:confirmed…`——客户城市**已确认**；`[extract] facts=0 slots=0 llm=0`——客户「白天也上班，晚上…」这类时间叙述不是任何槽，抽取为零；`src/inbox/time_context.py` 只注入**人设**当地时间，没有客户时区概念；`profile_slots` 把 `morning here / evening here / time zone` 只当 `location` 的**答案线索**，不产出可用事实。
- 结果：模型不知道对方现在几点，就会用「白天还是晚上 / 你那边几点」当开场——对客户来说 = 忘了刚说过的话。

## 要做的四段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 城市 → 时区** | 新 `src/companion/peer_time.py`：`resolve_peer_tz(profile) -> tz|None`（`location` / `residence` confirmed 或 mentioned → 城市 → IANA 时区，内置 200 城市表 + `zoneinfo`；判不出返回 None）；`peer_local_time(tz, now) -> {hh:mm, period: 早上/白天/傍晚/夜里, weekday}` | 新文件 |
| **B 注入** | `time_context` 追加一段「对方当地：周三 22:40，夜里——**不要问对方几点 / 白天还是晚上**，按此时段自然问候」；`tz=None` 时注入「对方时区未知：若需要就顺口问一次，问过 24h 内不再问」 | `time_context.py`、`persona_reply.py` 接线点只读（经 `_prompt_addenda`） |
| **C 时间叙述即事实** | 客户说「我这边现在是晚上 / 刚天亮 / 现在 X 点」→ FactGate `kind=transient`（附原句，TTL 12h）写 `peer_time_hint`，B 段优先用它并反推时区（与城市表冲突以客户原话为准）；`repeat_question_guard` 词干表加时间问句族（几点 / 白天还是晚上 / 早上还是晚上 / what time is it there / morning or night） | `fact_gate.py`（只加 kind）、`repeat_question_guard.py` |
| **D 门禁** | `tests/test_peer_time_q29.py`：location=「東京」→ tz Asia/Tokyo + 注入含「不要问几点」；客户说「我这边晚上」→ 12h 内出站问「白天还是晚上」被拦；tz 未知 → 问一次后 24h 不再问；2RKH3H 回放 | tests |

## 红线

- 不动 `decide_probe_target`（Q-1）；`location` 槽语义不变。
- 时区推断错比不推断更糟：城市表命中不到就返回 None，不猜。

## 开新对话粘贴

```
读 docs/指令_Q-29_客户当地时间感知与时间问句去重_2026-09-12.md，按它做 Q-29（#305：新 peer_time.resolve_peer_tz 由已确认/提及的城市查 200 城市表 → IANA 时区，判不出返回 None；time_context 追加「对方当地 周几 HH:MM 时段——不要问对方几点/白天还是晚上」，tz 未知则「顺口问一次，24h 内不再问」/ 客户「我这边现在是晚上/几点」经 FactGate kind=transient TTL 12h 写 peer_time_hint 并优先于城市推断，repeat_question_guard 加时间问句族 / 门禁四条含 2RKH3H 回放）。预算 $80。先读 Q-1 / Q-8 F / Q-25 落点表（在其上加，不重做），再 scripts\agent_probe.ps1 -Intent 登记；不动 decide_probe_target 与 persona_reply.py（经 _prompt_addenda 接线）；城市表命中不到不猜。每个 commit 带 #305 并立刻补落点表 docs/发版对账_v1.0.84_Q29.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-29 行并 agent_probe -Done。
```
