# 发版对账 v1.0.72 — 对话 A（I-1：#160 风险扣稿全放行影子档 + #162 观测补丁）

> 指令：`docs/指令_I-1_放行策略_2026-09-04.md`。总账：`docs/I批_进度总账.md` I-1 行。
> **本条不发版、不重启实例。** 每完成一项立刻追加一行；未完成项也留一行写「进行中：已改 X，还差 Y，下一步 Z」。
> 下一个账号接手：先读本表逐行核对，再跑 `git -C D:\boundless log --oneline -15 -- engines/chengjie` 确认 commit 真在。

## 落点表

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注/剩余 |
|---|---|---|---|---|---|
| B 分类器整词化 | #160 | `da7a8787` | `src/ai/chat_assistant_service.py`（`_detect_intent` 停止联系改整词+对象+否定式排除；`_detect_risk` 词表全部 ASCII 整词/中文精确短语，新增 `credential_or_payment_request` reason；新 `_detect_risk_detailed` 回 hits，`quick_analyze` 增 `risk_hits`）、`src/inbox/drafts.py`（`_SENSITIVE_PATTERNS` 整词化，`骗`→`骗子/诈骗/被骗/骗钱/骗局`；新 `keyword_risk_hits`）、`tests/test_drafts_risk_policy.py`（第一层金标） | `test_drafts_risk_policy.py` 第一层 + `test_intent_rule_fix.py` + `test_chat_assistant_service.py` + `test_b2_risk_audit.py::keyword` 全绿（65 passed） | 踩坑：Python `\b` 把 CJK 当 `\w`，「请问可以refund吗」漏判 → 改 `(?<![A-Za-z0-9_])`/`(?![A-Za-z0-9_])` ASCII 边界，已加门禁。**编辑工具会把 LF 文件写成 CRLF**，提交前用 `%TEMP%\i1_eol.py` 归一（`git diff --cached --stat` 出现整文件级行数即是此坑） |
| A 放行决策单一入口（含台账写入核心） | #160 | `58aed354` | 新 `src/inbox/autosend_policy.py`（`decide()` 纯函数：shadow＝先算旧表 `legacy_level` 得 would_hold_level/hold_reason 再覆写 L2；非 auto_ai 档先于风险层短路 shadow=None；enforce＝旧表逐字含 review+high→L4；策略读 env `AITR_AUTOSEND_POLICY_MODE` > `inbox.l2_autosend.policy_mode`(runtime_config) > 默认 shadow）；新 `src/inbox/autosend_shadow_log.py`（`build_record`/`record` JSONL + 进程计数 `stats_snapshot` + `maybe_alert` 发 `autosend_shadow_alert` + `iter_records` 只读）；`drafts.py`：`risk_to_autopilot` 变薄壳、`auto_generate_draft`/`enrich_draft`/`apply_analysis` 经 `decide()` 定档并 `_record_shadow`（enrich 只在 AI 稿新引入/推高扣稿档时记，同稿不记两遍）、`resolve_with_audit` 关键词复核按 L2 行反推 auto_ai 防误写 L1、日志补 `shadow=<reason> hits=<词>`；`autosend_worker.py` 捞稿点加单一入口注释；4 个存量测试文件加 enforce autouse 夹具 | `test_drafts_risk_policy.py` 两层 55 例 + 7 个 draft/autosend 相关文件 194 passed；宽集 21 文件 481 passed（7 red 全在 `test_unified_inbox_stage1.py`，属 send_routes `origin` 形参/路由数 37≠34/模板字串——他线存量红，HEAD 即红，未动） | shadow 下 review+high → L1（人审档仍挂起但不再是 L4 主管闸）；enforce 下按旧表 L4。**台账 alert 事件已在 A 发布，但别名/格式化/i18n 在 C 补**（未补前 `test_alert_alias_coverage` 等 3 个门禁会红——C 必须紧接着做） |
| C 影子台账观测面 + CLI + 告警别名 | #160 | `a667783c` | `drafts_routes.py` metrics 段 `autosend_shadow`；`ops_overview.html` 新卡 `autosendShadowSection`/`renderAutosendShadow`/注册 `ashadow`(quality)/`_OPS_HIDE_HINT`（零命中 healthy 隐藏；stop/self_harm 有量亮黄灯；命中词经 `_esc`）+ `i18n_packs/ops_overview_page.py` 13 键 zh+en；`tools/autosend_shadow_report.py`（四段：reason 分桶/账号平台分布/命中词 Top/抽样 draft_id；`--days/--reason/--data-root/--dir/--json`）；`webhook_notifier.py` **仅 4 个 hunk**（别名 `autosend_shadow`→`autosend_shadow_alert`、`_BUSINESS_ALERTS`、`_build_message` 分支、`_CARD_META` 🟠运营）经 `tools/stage_hunks.py --mine autosend_shadow --theirs phantom_unread` 暂存，sibling 的 phantom_unread 块**仍未提交在工作树**；`cp-i18n.js` 双树 `cp.alert.autosend_shadow` zh+en + `cp-i18n-ext.zh_hant.js` 双树 regen（顺带补齐他线漏 regen 的 3 个 `cp.voice.*` 繁中键）；`tests/test_alert_delivery_e2e.py::_EMITTED_ALERTS` 登记；新 `tests/test_autosend_shadow_observability.py` | 18 文件 567 passed；3 red 全属他线存量：`autosend_gate`/`phantom_unread` 未归类（HEAD 即红 / sibling 未提交）、`cp-voice.js`&`cp-i18n.js` 缓存戳 app.html=20260904a vs unified_inbox.html=20260902a（#161 线只 bump 了 app.html） | **前端零改动**（ops 卡属值守面，非坐席 UI）。`cp-i18n.js` 词典改了但 `?v=` 双戳未 bump（`unified_inbox.html` 属 I-2）→ 交 I-2 顺手 bump 同值；未 bump 只影响告警渠道面板一个标签在缓存过期前显示裸键 |
| D #162 观测补丁 | #162 | `10dc0a84` | `telegram_client.py` 两处「收到消息 [」日志行（私聊 L2383 / 群组监控 L1031）补 `account=<id> conv=<cid>`，conv 经 `normalizer.conv_id` 与 B 线收件箱会话 id 同口径（可直接对上 `protocol_media/<platform>/<conv>.md`）；`automation_mode_log` 的 `source` 列与落行**store 早已实现**（human/bootstrap/takeover/snooze/standby/sweep/case:handoff…），本条补齐两个裸写入口：`web_chat_routes.py` → `webchat_default`、`unified_inbox_account_routes.py`（协议号陌生人请求预置档）→ `protocol_request_preset`（带 TypeError 回落）；新 `tests/test_inbound_log_account_conv_162.py`（含全站裸 `set_automation_mode(` 扫描 ratchet） | 15 passed（含 `test_daily_stats` 口径基准）+ `test_web_chat` 28 + `test_group_thread` 11 | 「inherit」不是写入（账号层生效期刻意不落盘，`resolve_automation_mode` 动态继承），故无 source 可补；UI 路径既有 source=`human`（未改名为 `ui`，沿用既有约定） |

## 验收清单结果（指令 §6，2026-09-04 10:0x 实跑，真库 + 真台账文件 + 真 CLI，`%TEMP%\i1_accept.py`）

| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 1 | `i didn't want to stop` → L2 自动发 + 零台账 | ✅ | level=L2, ledger=0 |
| 2 | `please stop messaging me` → L2 放行 + 台账 stop_contact + 值守事件；坐席界面零变化 | ✅ | hits=`['please stop messaging me']`，`autosend_shadow_alert` rate_key=`acct1:u2`；前端未改任何文件 |
| 3 | `I want to kill myself` → L2 放行 + 台账 self_harm + 值守事件；稿已过 crisis safety net | ✅ | hits=`['kill myself']`；B 线 `generate_inbox_draft`(skill_manager L2829) 内 L3582 `_apply_crisis_safety_net` 未动，enrich 收到的正文已是安全网处理后的稿 |
| 4 | AI 稿 `send the deposit to account 1234` → L2 放行 + 台账 reply_risk=high | ✅ | stage=reply, hold_reason=reply_risk, hits=`['send the deposit']` |
| 5 | 客户 `send me your bank card number` → L2 放行 + 台账 credential_or_payment_request | ✅ | hits=`['bank card','send me your bank card']`，不推值守（只 stop/self_harm 推） |
| 6 | `automation_mode=review` → 仍挂起且不进台账 | ✅ | level=L1, ledger 不增 |
| 7 | JSONL 落盘字段齐全无原文；CLI `--days 1` 读出 2–5 四条 | ✅ | `shadow_20260904.jsonl` 4 行、`RECORD_FIELDS` 全在、整条原文不在文件里（命中词是 ≤40 字片段）；CLI 四段输出含四个 reason |
| 8 | `metrics.autosend_shadow` 有分桶；零命中卡隐藏 | ✅ | by_reason 四桶各 1；卡按 `total=0` → `opsHideCardEl(...,'healthy')` |
| 9 | `tests/test_drafts*` / `test_autosend*` 不红 | ✅ | 3 + 11 文件 + 本条 3 个门禁文件 = 259 passed |
| 10 | enforce 反向验证：2–5 变回挂起 | ✅ | env `AITR_AUTOSEND_POLICY_MODE=enforce` → 四条全 L4，且不写影子台账 |

## 一个月后要读的东西（给没有本次上下文的对话）

- **JSONL（权威口径）**：`D:\chengjie-instances\zhiliao\data\logs\autosend_shadow\shadow_YYYYMMDD.jsonl`（生产进程 CWD=实例数据根；每天一个文件；字段契约 `src/inbox/autosend_shadow_log.RECORD_FIELDS`：ts/platform/account_id/conv_key/draft_id/would_hold_level/hold_reason/peer_risk/peer_reasons/reply_risk/reply_reasons/risk_hits/text_fp/text_len/stage/automation_mode/policy_mode；**不含原文**）
- **CLI**：`cd D:\boundless\engines\chengjie && python tools/autosend_shadow_report.py --days 30 [--reason stop_contact] [--json]`（数据根自动发现活跃实例；`--dir` 可直指目录）。四段：reason 分桶 / 账号平台分布 / **命中词 Top（哪个正则在误伤）** / 抽样 draft_id（回溯 `protocol_media/telegram/<chat>_<n>.md`）
- **ops 卡**：`/admin/ops` → quality 组「🕶️ 风险放行影子台账」（进程口径，零命中隐藏；stop/self_harm 有量亮黄灯）
- **metrics**：`GET /api/workspace/metrics` → `autosend_shadow`
- **值守通知**：webhook 别名 `autosend_shadow`（事件 `autosend_shadow_alert`，business 受众；需在告警渠道面板勾选订阅才会外发——当前 `notify_webhooks.json` 渠道状态由值守决定）
- **切 enforce**：overlay `inbox.l2_autosend.policy_mode: enforce`（热重载 ~30s）——当前 enforce＝v1.0.71 旧表逐字；新规则表读完台账再在 `autosend_policy.decide` 的 enforce 分支定，`legacy_level` 保留为基线

## 台账字段：已加 / 建议一个月后补不回来所以现在评估

- 已在 v1 追加（超出指令清单）：`stage`（peer/reply/analysis）、`automation_mode`、`policy_mode`。
- **没加、但一个月后补不回来的**（建议下一批评估）：① `persona_id`（哪个人设的会话在触发——「人设 A 的客户更常叫停」这类结论现在算不出）；② `lang`（会话语言——中英正则误伤率分开看）；③ `sent`（放行的稿最后**真发出去了没**——worker 的 send_block / mode_downgraded / channel_mutex 可能取消，台账现在只记「放行」不记「送达」）；④ `peer_msg_id`（比 conv_key+ts 更精确的回溯键）；⑤ `intent`/`emotion`（quick_analyze 已算出，白给）。这五个都是一行 `build_record(**)` 参数的事，加在 `drafts.py::_record_shadow` 调用点即可。

## 他线存量红（本条未动，如实报）

- `tests/test_unified_inbox_stage1.py` 7 例：`_fake_send_via() got an unexpected keyword argument 'origin'`×4 / 路由数 37≠34 / 模板字串×2 —— send_routes/模板/路由注册在 HEAD 已变、测试未跟（`unified_inbox.html` 属 I-2）。
- `test_alert_audience_catalog::test_every_alert_alias_is_classified`：`autosend_gate`（HEAD 即未归类）、`phantom_unread`（sibling 未提交）。
- `test_alert_delivery_e2e::test_source_alerts_all_have_e2e_payload`：`phantom_unread_alert`（sibling 未提交的 health_watchdog 发布点）。
- `test_copilot_i18n_keys::test_asset_stamps_consistent_across_hosts`：`cp-voice.js`/`cp-i18n.js` app.html=20260904a vs unified_inbox.html=20260902a（#161 线只 bump 了 app.html）→ **I-2 bump `unified_inbox.html` 两处 `?v=` 到 20260904a 即绿**，顺手也覆盖本条改的 `cp-i18n.js` 词典。
- `test_ui_build_freshness::test_ui_build_stamp_not_stale`：`ui-build.txt` 戳 09-03 06:26，而 #161 线 09-04 01:07 改了 `app.html`/`cp-voice.js` 没 bump（开工前即红）；本条的 `cp-i18n*.js` 也算前端面。按门禁文案「另一条线的中批状态由收口方 bump」→ **I-2 收口时 `python scripts/bump_ui_build.py` 一次消掉**（本条刻意不 bump：坐席 UI 零改动，单独 bump 只会让开着的坐席页白弹一次刷新提醒）。
- `test_stale_update_prompt` 3 例：`shared/copilot/app.html` 副驾条文案/severity 探针——他线未收口，与本条无关。
