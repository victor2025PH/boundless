# 发版对账 v1.0.80 — 内测包（微信实施97 / QQ route-1 / TikTok TK-1 进包 → 坐席三台）

> 老板 09-10 21:5x：「把微信、QQ、TikTok 的开发都打入生产包，进行内测」。只打**内测包推坐席三台**，不发三渠道 / 不动官网 / 不改 `latest-internal.yml` 线上指针；正式 1.0.80 发版（R80）另开。
> 依据：`docs/Q批_进度总账.md` 独立线块（WX / QQ / TK 三行均「1.0.80 进包」）；SOP `.cursor/rules/chatx-desktop-install.mdc`。意向板 `R80-internal …`（21:56）。

## 0. 一句话

**1.0.80 内测包已从干净 worktree 打出**：`D:\boundless-wx@3b721cd7` → `ChatX-Setup-1.0.80.exe` **593,878,656 B** sha256 `e82105cdeb417fab8449849d581f6a929c05886e098110c239be8ef08eea2a42`（R79 内测 593,254,839 B，+610 KB），后端 fingerprint `90474d84…` **files=2157**（R79 2123，+34：wechat_kf / wechat_pc / wecom_sso / relay_client / kf_window_guard / tiktok_shop_cs / tiktok_huoke_bridge / qq 诚实标位 …）。三线开关**全部默认关**（`wechat.*` / `qq` 驱动 `driver_state=mock` 演示态 / `tiktok.enabled` `tiktok.shop.enabled` `tiktok.huoke_bridge.enabled` = false），装机零行为变化；同包顺带带上 Q-8 / Q-10 / Q-15（1.0.79 未带的第二波）。坐席推送见 §3。

## 1. 基线

| 项 | 事实 |
|---|---|
| release 基点 | 主干 `2ed11f1c`（QQ 线收工 21:40）+ 本线 bump **`3b721cd7`**（`desktop/package.json` 1.0.79→1.0.80 / 1.079→1.080，index-only 单文件） |
| 进包三线 | WX `f00c4d00` `183e2322` `b156daa7` `69efc665` `4210e7cd`；QQ `54af3f00` `dbcc314f`；TK `16fe54f6` `36f1e53b` `3a6b485f`（A/B 早已入库） |
| 干净 worktree | 复用 WX 线的 `D:\boundless-wx`（junction `desktop/node_modules` + `build/ffmpeg`、边车 `node_modules` robocopy、gitignore 资产 emoji-picker 5 文件已在）：还原 `build/i18n-packs-staged` 生成物 → `checkout --detach 3b721cd7` → 树干净 |
| 未做（内测包不需要） | 繁体全量 regen / changelog / `bump_ui_build` / 三渠道 publish / 官网五值 / clean & lite 包 —— 留给 R80 |

## 2. 构建（21:56–22:13，`tmp/r80_dist_1080_smart.txt`）

| 步 | 结果 |
|---|---|
| `npm run dist:win:smart` | 增量档自拒（hiddenimports 变）→ **`build:backend` 全量** 8.5 min → fingerprint `90474d84…` files=2157 → `smoke_backend` 过 |
| predist 门禁 | `renderer-boot-invariants` **76** / `outbound-wiring` **16** / `package-layout` **81** / `uninstall-nsh` **303** / shell-i18n / cp-i18n-keys / backend-dist fresh / `stage_internal_assets` / `smoke_backend` / `smoke_internal` 全过 |
| after-pack | 随包交付物 **22 项**齐备 |
| 产物 | `D:\boundless-wx\engines\chengjie\desktop\dist\ChatX-Setup-1.0.80.exe` + `.blockmap` + `latest-internal.yml`（**只在本地，未上传**）；`build-info.json` flavor=internal commit=`3b721cd7` dirty 只有 build 自己生成的 `i18n-packs-staged/*` |

## 3. 坐席三台（`push_chatx.ps1 -Setup <包> -Install -Relaunch -Smoke`，日志 `tmp/r80_push_<node>.txt`）

| 机 | 结果 |
|---|---|
| yunsheng（173） | 见下方补记 |
| kouxing（198） | 见下方补记 |
| lianbei（104） | 见下方补记 |

fleet 终验：`deploy\desktop\chatx_fleet_status.ps1 -Targets yunsheng,kouxing,lianbei` → 见下方补记。

## 4. 内测要看什么（交值守 / 坐席）

- **零回归**是第一验收：三线默认关，装后收件箱 / 人设 / 目标 / 自动回复行为应与 1.0.79 一致；`backend.log` 启动段应有 `[baseline]` 汇总行且**没有** wechat / tiktok / qq 的 ERROR。
- QQ：工作台 QQ 平台组头 / 账号卡 / 连接面板应显示琥珀「边车已装 · 驱动未接入（演示态）」，事实卡「预览中（尚不能真收发）」——这是 A 段诚实标位的验收；真驱动 B 段随包但未 REQUIRED，不接真号。
- 微信：入口 `connect_guide.html` 在包；企微客服 / 个人微信 PC 副驾全部关，等企微凭证与主人拍板 C 段联调残留清单再开。
- TikTok：无入口无痕迹；权限获批后才做 A 真流量。
- 顺带进包的 Q-8 / Q-10 / Q-15 在坐席机首次可见：目标页「现在就问一个」直投 / 主题浅色深色即生效 / 需人工卡片「命中：成人内容（sex）· 露骨」+ 一键软回应。

## 5. 与 #272 的关系

skuio 18:27 已升 1.0.79，#272 报「未修：冲刺零真发 / 帮助语料 295 / 额度用尽 / wa Bad MAC」——帮助语料 295（Q-10 D）与冲刺直投（Q-8 G）在本包里，但**本包不推 skuio**（内测机走三渠道 internal 通道，等 R80）；额度用尽与 Bad MAC 不是包能解的，见值守日清。
