# 指令 Q-22：克隆语音语种不支持时不静默换系统音（发出去必须是人设声，否则不发）

> 预算 **$100**。工单 **#287（UMCTVF / V9YAAX / B75Z42 / BFBF6E / R33MZW / BPAPAK）/ #288（PUQ45W）**（skuio 1.0.81，09-11 16:33–16:53）。落点表 `docs/发版对账_v1.0.82_Q22.md`。
> 归属：TTS / 语音线（`src/ai/tts_pipeline.py`、`inbox/voice-send` 路由、语音行 UI）。与 Q-16 / Q-21 的翻译界面不重叠。

## 事实（证据 `tmp_diag\{V9YAAX,B75Z42}`；代码只读核对 `tts_pipeline.py` L1649–1667 / L1725 / L2339）

- Telegram STEVEN(7331682688)×JUNCTION 16:34 日语「何してるの？最近すごく忙しかっただけ」：`[tts] 合成生效 backend=avatar_clone voice=STEVEN 来源=人设STEVEN` → `文本语种 ja 超出克隆链 avatar_clone 能力 → 跳过克隆` → `avatar_clone failed clone_lang_unsupported:ja 回落 edge_tts` → `fallback ja-JP-KeitaNeural`（微软通用日语男声）→ `inbox/voice-send delivered provider=edge_tts fallback=avatar_clone xlate=ja`。**界面只黄字警告，仍允许生成并发出**——客户听到的不是人设的声音。
- 同一机制在钱包耗尽（L1667）、引擎冷启动 / 不可达（L1725 / L2339）也回落 edge：产品口径「可以不发，不能乱发」（08-15 罐头兜底移除同源）应同样适用于**声音身份**。

## 要做的四段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 语种不支持 = 阻断** | `clone_lang_unsupported` / 钱包耗尽 / 引擎不可达 三种「克隆不可用」→ 语音行**不出可发的音频**：返回 `blocked reason=clone_unavailable:<why>`，前端红条「人设声不支持 日语 / 引擎暂不可用，本条不发语音」+ 两个按钮「改发文字」「用系统音发（对方会听到非人设声）」——**默认不发**，系统音必须人为二次确认；`inbox/voice-send` 拒收 `provider=edge_tts fallback=avatar_clone` 且无 `confirm_system_voice=1` 的请求 | `tts_pipeline.py`（只改回落决策处 + 返回码）、`voice-send` 路由、语音行 UI |
| **B 能力前置可见** | 人设语音卡显示「克隆声支持语种：zh / en / …」（取引擎能力表）；会话目标语不在其中时语音按钮直接置灰带原因，不等合成失败才知道 | 人设页语音卡、语音行 |
| **C 自动链同口径** | 自动语音回复（非坐席手点）遇克隆不可用 → **改发文字**（不发系统音），日志 `[tts] skip_voice reason=clone_unavailable conv= lang=`；冷启动 20–30s 自愈场景改为**等一拍重试一次**再降级文字 | `tts_pipeline.py`、自动语音调用侧 |
| **D 门禁** | `tests/test_tts_clone_unavailable_q22.py`：ja 文本 × STEVEN → blocked 不出音频；带 `confirm_system_voice=1` 才出 edge；自动链改文字；`voice_selection_gold` / `voice_placeholder_guard` 既有测试不回归 | tests |

## 红线

- 不引入任何新的默认回落音；系统音只在坐席**显式二次确认**后出。
- 不动翻译链与 Q-16 / Q-21 的界面 hunk。

## 开新对话粘贴

```
读 docs/指令_Q-22_克隆语音语种不支持不静默换声_2026-09-11.md，按它做 Q-22（#287 #288：tts_pipeline 克隆不可用（clone_lang_unsupported / 钱包耗尽 / 引擎不可达）不再静默回落 edge 系统音——返回 blocked reason=clone_unavailable，语音行红条 + 「改发文字」「用系统音发（二次确认）」，voice-send 路由拒收无 confirm_system_voice 的回落音频 / 人设语音卡显示克隆支持语种、目标语不支持时语音按钮置灰带原因 / 自动语音链遇不可用改发文字并落 [tts] skip_voice，冷启动等一拍重试一次 / 门禁 4 例 + 既有语音测试不回归）。预算 $100。先读 tmp_diag\V9YAAX 与 B75Z42 报告，再 scripts\agent_probe.ps1 -Intent 登记；不新增任何默认回落音；不动翻译链与 unified_inbox.html 的 xl- 区。每个 commit 带 #287 并立刻补落点表 docs/发版对账_v1.0.82_Q22.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-22 行并 agent_probe -Done。
```
