# 指令 Q-22（09-12 04:5x 修订）：语音身份——克隆不支持的语种不静默换系统音 · 语音语种必须等于文本语种 · 登记克隆有结果面板

> 预算 **$130**。工单 **#287（UMCTVF / V9YAAX / B75Z42 / R33MZW / YNH6ZW 1.0.82 复验：ja=Keita ko=InJoon th=Niwat fr=Henri 仍静默换声）/ #288（PUQ45W）/ #239 追加（VESMM2 / 4EXEEY：登记 toast 一闪、无试听、体检空白，9-07 起未修）/ #304（GWJ2RZ，jun：TG 十翼会话人设 lin_jiaxin 语音「突然说广东话」，`voice=zh-CN-XiaoxiaoNeural backend=avatar_clone`）/ ANJPBG（粤语走克隆但读法不是粤语）/ MX9WE5（实测只有中英是克隆声）**。落点表 `docs/发版对账_v1.0.83_Q22.md`。
> 与 Q-21 的接口：Q-21 起草时产出 `conv_lang_plan.tts_lang`，本线只读它；本线不动翻译链。

## 09-12 追加事实

- **#304**：04:22 `[voice_reply] 分条语音已发 2/2 persona=lin_jiaxin` → 用户听到广东话；04:23 `[tts] 合成生效 backend=avatar_clone voice=zh-CN-XiaoxiaoNeural`，04:24 `verdict=ok cer=0.17`。~~要核：文本是否被 `xlate target=yue` 译成了粤语再念、还是克隆链对普通话文本出了粤语口音~~ → **05:07 已核实并修（`73c8ab6b`，落点 `docs/发版对账_v1.0.83_304语音.md`）**：两条假设都不成立。真因 = 04:21:55 `[lang_voice_route] 粤语文本 → 切 edge_tts (zh-HK-HiuMaanNeural) markers>=2`——`is_cantonese_text` 特征字表混着普通话口语字（啵嘞喇咩咯噶埋）与繁体字（靚曬囉），普通话回复凑两个即整段切香港声并停克隆；已拆独占/共用两档，共用字不再单独触发。同报告里 5/6 次 `synth_verify CER 0.32–0.47` 判「念错」是该机 ASR 吐繁体 vs 简体送稿的字形假阳性，`_cer_cjk` 已两侧 t2s 归一 + 入站转写统一简体。**语音语种必须 = 出站文本语种 = `conv_lang_plan.tts_lang`** 的一致性闸仍按 D 段做（第二道保险），但不必再为 #304 单独核因。
- **#239**：登记克隆音色后端已生成试听，界面 toast ≈1s 消失、试听按钮不显示、体检不回填——1.0.77 → 1.0.82 五版验收都没列——本线 E 段补：登记结果改**可停留的结果面板**（成功 / 失败 + 试听播放 + 支持语种 + 体检回填）。
- 同一批 `voice_burst` 熔断（60s 内 3 条语音 → 降级文字）是既有 A3 守卫，正常，不改。
> 归属：TTS / 语音线（`src/ai/tts_pipeline.py`、`inbox/voice-send` 路由、语音行 UI）。与 Q-16 / Q-21 的翻译界面不重叠。

## 事实（证据 `tmp_diag\{V9YAAX,B75Z42}`；代码只读核对 `tts_pipeline.py` L1649–1667 / L1725 / L2339）

- Telegram STEVEN(7331682688)×JUNCTION 16:34 日语「何してるの？最近すごく忙しかっただけ」：`[tts] 合成生效 backend=avatar_clone voice=STEVEN 来源=人设STEVEN` → `文本语种 ja 超出克隆链 avatar_clone 能力 → 跳过克隆` → `avatar_clone failed clone_lang_unsupported:ja 回落 edge_tts` → `fallback ja-JP-KeitaNeural`（微软通用日语男声）→ `inbox/voice-send delivered provider=edge_tts fallback=avatar_clone xlate=ja`。**界面只黄字警告，仍允许生成并发出**——客户听到的不是人设的声音。
- 同一机制在钱包耗尽（L1667）、引擎冷启动 / 不可达（L1725 / L2339）也回落 edge：产品口径「可以不发，不能乱发」（08-15 罐头兜底移除同源）应同样适用于**声音身份**。

## 要做的四段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 语种不支持 = 阻断** | `clone_lang_unsupported` / 钱包耗尽 / 引擎不可达 三种「克隆不可用」→ 语音行**不出可发的音频**：返回 `blocked reason=clone_unavailable:<why>`，前端红条「人设声不支持 日语 / 引擎暂不可用，本条不发语音」+ 两个按钮「改发文字」「用系统音发（对方会听到非人设声）」——**默认不发**，系统音必须人为二次确认；`inbox/voice-send` 拒收 `provider=edge_tts fallback=avatar_clone` 且无 `confirm_system_voice=1` 的请求 | `tts_pipeline.py`（只改回落决策处 + 返回码）、`voice-send` 路由、语音行 UI |
| **B 能力前置可见** | 人设语音卡显示「克隆声支持语种：zh / en / …」（取引擎能力表）；会话目标语不在其中时语音按钮直接置灰带原因，不等合成失败才知道 | 人设页语音卡、语音行 |
| **C 自动链同口径** | 自动语音回复（非坐席手点）遇克隆不可用 → **改发文字**（不发系统音），日志 `[tts] skip_voice reason=clone_unavailable conv= lang=`；冷启动 20–30s 自愈场景改为**等一拍重试一次**再降级文字 | `tts_pipeline.py`、自动语音调用侧 |
| **D 语音语种一致性（#304）** | 合成前核 `tts_lang == 出站文本检测语种 == conv_lang_plan.tts_lang`，不一致 → 不发语音改文字 + `[tts] skip_voice reason=lang_mismatch text_lang= tts_lang=`；`[tts] 合成生效` 行加 `text_lang= tts_lang= source=` 三字段；粤语（yue）只在克隆链声明支持 yue 时走克隆，否则按 A 段阻断 | `tts_pipeline.py`、`voice_reply` 调用侧 |
| **E 登记结果面板（#239）** | 人设语音 tab 登记克隆：toast 改为可停留的结果面板（成功 / 失败 + 原因 + 4 段试听播放 + 「支持语种：zh / en」+ 体检回填），关闭需点击；失败保留错误原文 | 人设页语音 tab、`voice_routes.py` 返回体 |
| **F 门禁** | `tests/test_tts_clone_unavailable_q22.py`：ja 文本 × STEVEN → blocked 不出音频；带 `confirm_system_voice=1` 才出 edge；自动链改文字；文本 zh × tts_lang yue → skip_voice lang_mismatch；登记返回体含 preview + supported_langs；`voice_selection_gold` / `voice_placeholder_guard` 既有测试不回归 | tests |

## 红线

- 不引入任何新的默认回落音；系统音只在坐席**显式二次确认**后出。
- 不动翻译链与 Q-16 / Q-21 的界面 hunk。

## 开新对话粘贴

```
读 docs/指令_Q-22_克隆语音语种不支持不静默换声_2026-09-11.md（09-12 修订版），按它做 Q-22（#287 #288 #239追加 #304：tts_pipeline 克隆不可用（clone_lang_unsupported / 钱包耗尽 / 引擎不可达）不再静默回落 edge 系统音——返回 blocked reason=clone_unavailable，语音行红条 + 「改发文字」「用系统音发（二次确认）」，voice-send 路由拒收无 confirm_system_voice 的回落音频 / 人设语音卡显示克隆支持语种、目标语不支持时语音按钮置灰带原因 / 自动语音链遇不可用改发文字并落 [tts] skip_voice，冷启动等一拍重试一次 / 合成前核 tts_lang == 文本语种 == conv_lang_plan.tts_lang，不一致 skip_voice lang_mismatch，合成生效行加 text_lang tts_lang source（#304 十翼会话突然说广东话先按此核因）/ 登记克隆 toast 改可停留结果面板 + 试听 + 支持语种 + 体检回填（#239）/ 门禁 6 例 + 既有语音测试不回归）。预算 $130。先读 tmp_diag\V9YAAX、YNH6ZW、GWJ2RZ、4EXEEY 四份报告，再 scripts\agent_probe.ps1 -Intent 登记；不新增任何默认回落音；不动翻译链与 unified_inbox.html 的 xl- 区；conv_lang_plan 由 Q-21 提供、未合并前以 hasattr 兜底读出站文本检测语种。每个 commit 带 #287 并立刻补落点表 docs/发版对账_v1.0.83_Q22.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-22 行并 agent_probe -Done。
```
