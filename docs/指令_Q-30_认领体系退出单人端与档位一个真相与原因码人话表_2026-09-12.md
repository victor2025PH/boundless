# 指令 Q-30：认领体系退出单人端 + 档位一个真相 + 原因码人话表（P0）

> 预算 **$200**。工单 **#307 #308 #309 #310 #311**（Vanessa / Jeeo，1.0.84，09-12 11:0x–12:17）+ **#312 #314**（jun，1.0.84，12:35 / 12:51，原因码可见部分）。落点表 `docs/发版对账_v1.0.85_Q30.md`。
> 前置：Q-26 A（`src/inbox/conv_state.py` 只读聚合 + `reason_code`）、Q-18 B（`agent_yield` defer 不丢稿）已在 1.0.84。方案全文见 `docs/分析_聊天工作台_五视角美化优化方案_2026-09-12.md` §五 P0-1/2/3。

## 事实（证据 `tmp_diag\VYY22Q` / `W2CSGA` / `MSYSKV` / `KXYRQ7`；代码只读核对）

- **#310** 用户描述「点开的会话列表变『我』，没点的是『AI』」；`unified_inbox.html` L8399 `selectChat()` **无条件** `renewClaim(c)`，L20104 每 90 s 续租——查看 = 认领。`_handlerBadgeHtml`（L6469–6519）按 claim 出「我 / 同事」，头部 `claim-hdr-btn` 出「处理中 · 释放认领」（#309 用户问「谁认领、释放什么」）。
- `src/web/ui_visibility.py` L273–285 `resolve_seat_mode`：无显式配置时 **用户表 ≥2 账号即 multi，取不到用户表也 multi**——单人客户装出来就是多坐席版；模板 L2230 `MULTI_SEAT` 缺省 true。
- **#311** `whatsapp:12084403608:917983336714` 12:11 / 12:13 / 12:14 三稿 `origin=auto` 出稿，无发送；报告关键词没抓到 autosend 行。后端 **没有任何 autosend 代码读 `conversation_claims`**（只有 `sla_watcher` / `scheduled_reporter` / `unified_inbox_context` 读）→ 「全自动停」的真闸不是 claim 本身，候选是 `autosend_worker._human_priority_gate` L974–977 的 `agent_sent / agent_typing`（前端 L15749 `/api/workspace/typing` ping）或 `conv_state` 的 held / risk_hold——**先查 `abort_ledger` 定闸再改**。
- **#308** 顶栏 `mode-select` 显示「本会话全自动」，草稿条 `cdraft-reason` 写「全局默认半自动」（`inbox.l1r.global_default`，L2018）——两处各算一遍档位。
- **#307** 红条直出技术码 `media_caught_repeat`；**#312 / #314** jun 自测账号 `8852939166` 在「永不自动回复」名单，`peer_bot_guard skip reason=colleague:8852939166`（`src/inbox/peer_bot_guard.py` L234–249）A/B 两线都跳过——不是识图问题，是硬名单，但界面零提示，用户以为 AI「挡话」。
- 原因码 → 文案现有三张表各自为政：`_HANDOFF_REASON_KEYS` L6619 / `_AD_LEVEL_KEYS` L6625 / `_RK_CAT_KEYS` L6640；`media_lie_caught_repeat` / `colleague:*` / `lang_unknown` / `clone_unavailable:*` / `agent_typing` 都无键。

## 要做的五段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 认领退出单人端** | ① `resolve_seat_mode` 缺省 **single**；multi 只在显式 `workspace.seat_mode: multi` **或** 30 min 内 ≥2 个不同登录会话在线（`agent_coordinator` presence，不是用户表行数）；② `selectChat` 删 `renewClaim(c)` 与 90 s 续租；认领只在 `tkoToggle()` 接管 / 人工发送 / 显式「认领」按钮写；③ `MULTI_SEAT=false` 时不渲染 `claim-hdr-btn`、`_handlerBadgeHtml` 不出「我 / 同事」、「我的」筛选与 `releaseClaim` 入口全隐；④ 升级一次性清空 `conversation_claims`（表无 source 列，全清即可，认领本来 15 min 过期） | `ui_visibility.py`、`unified_inbox.html` L2230 / L8399 / L20092–20152 / L6469–6519 / L785、`store.py` 迁移 |
| **B 找到真闸** | 用 W2CSGA 的 conv 查 `abort_ledger`（`reply_settings_routes.py` L701 端点）定 12:11–12:17 三稿的 defer / abort 码；若是 `agent_typing`：`/api/workspace/typing` 只在 textarea **真有字符**（len>0，非 focus / 非空）时 ping，且切会话立即 `resume_agent_yield(by=switch)`；若是别的码 → 按 D 段让它可见并按码处置。W2CSGA 回放进门禁 | `autosend_worker.py` L944–977、`unified_inbox.html` L15740–15760、`unified_inbox_realtime_routes.py` L563 |
| **C 档位一个真相** | `conv_state` 增 `mode_effective / mode_source ∈ conv \| global \| capped`；顶栏 `mode-select` 只显示会话档，删 `mode-eff-chip`；封顶时状态带说「全自动（新号预热封顶为审核）」；草稿条 `cdraft-reason` 只读 `conv_state.reason_code`，删 `inbox.l1r.global_default` 独立文案；门禁：同一 `/thread` 响应里 `mode-select` 值与状态带文案必一致 | `conv_state.py`、`unified_inbox.html` L797–806 / L1020、`autodraft_helpers.py`、`inbox_workspace.py` |
| **D 原因码人话表** | 新命名空间 `inbox.reason.<code>`（zh/en + 繁体 regen）：`media_lie_caught_repeat` → 「客户说没收到图 · 已连续 2 次 · 交给你」；`agent_sent` / `agent_typing` → 「你刚发过 / 正在打字 · N 秒后 AI 接回」；`colleague` → 「对方在『永不自动回复』名单（同事 / 本租户账号）· AI 不回 · 去 设置 › 自动化与风控 改名单」；`ops_group` → 「报障 / 运维群 · 永不自动回」；`lang_unknown` / `clone_unavailable:<lang>` / `risk_hold:<cat>` / `mode_*` / `off_hours` / `sidecar_down:*`；红条 / chip / 状态带 / 草稿条 **只经这张表**（三张旧表合并进来），缺键回落「AI 暂停：<code>」+ 详情。`peer_bot_guard` skip 要写进 `conv_state`（`held`, reason_code=`colleague:<id>`），不再只打日志 | `inbox_workspace.py`、`conv_state.py`、`peer_bot_guard.py` 回喂、渲染点 L6600–6700 |
| **E 门禁 + 回放** | `tests/test_seat_mode_q30.py`：无配置 + 1 账号 → single；无配置 + 2 账号但 1 会话在线 → single；显式 multi → multi；single 渲染无 claim 元素。`tests/test_reason_table_q30.py`：`abort_ledger` / `conv_state` / `ABORT_REASONS` 出现过的每个 code ⊆ `inbox.reason.*` 键集。VYY22Q / W2CSGA / MSYSKV 三回放 | tests |

## 红线

- 多坐席客户（显式 `seat_mode: multi`）行为不变：认领 / 我的 / 同事徽标全保留。
- `_human_priority_gate` 的 60 s 窗口与 defer 语义（Q-18 B）不改，只改「什么算打字」。
- 不动 `peer_bot_guard` 的判定（同事名单硬拦是对的），只让它可见。
- `.py` 改动须 zhiliao 重启才生效；模板 / 词包热更。

## 开新对话粘贴

```
读 docs/指令_Q-30_认领体系退出单人端与档位一个真相与原因码人话表_2026-09-12.md，按它做 Q-30（P0，#307 #308 #309 #310 #311 + #312 #314 可见部分：A resolve_seat_mode 缺省 single、multi 只认显式配置或 30min 内 ≥2 登录会话在线，selectChat 删 renewClaim 与 90s 续租，single 下 claim-hdr-btn / 我·同事徽标 / 我的筛选 / releaseClaim 全隐，升级清空 conversation_claims / B 先用 tmp_diag\W2CSGA 的 whatsapp:12084403608:917983336714 查 abort_ledger 定 12:11–12:17 三稿真闸再改，若是 agent_typing 则 /api/workspace/typing 只在 textarea 真有字符时 ping、切会话即 resume_agent_yield / C conv_state 加 mode_effective+mode_source，顶栏只显会话档删 mode-eff-chip，草稿条只读 conv_state.reason_code 删 inbox.l1r.global_default，门禁同一 /thread 两处一致 / D 新 inbox.reason.<code> 人话表合并 _HANDOFF_REASON_KEYS/_AD_LEVEL_KEYS/_RK_CAT_KEYS，含 media_lie_caught_repeat、agent_sent、agent_typing、colleague、ops_group、lang_unknown、clone_unavailable、risk_hold、off_hours、sidecar_down，缺键回落「AI 暂停：<code>」，peer_bot_guard skip 写进 conv_state held / E 门禁两文件 + VYY22Q W2CSGA MSYSKV 回放）。预算 $200。先读 docs/分析_聊天工作台_五视角美化优化方案_2026-09-12.md §五 P0 与 Q-26 落点表 docs/发版对账_v1.0.84_Q26.md（在 conv_state 上加，不重做），再 scripts\agent_probe.ps1 -Intent 登记；unified_inbox.html 是多线共享文件，提交前用 python tools/stage_hunks.py --file <路径> --mine <regex> --apply 只暂存自己的 hunk；显式 seat_mode: multi 的行为一字不改；不动 peer_bot_guard 判定与 _human_priority_gate 的 60s/defer 语义。每个 commit 带工单号并立刻补落点表 docs/发版对账_v1.0.85_Q30.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-30 行并 agent_probe -Done。
```
