# 指令 Q-30：单人端不再误判为多坐席 + 定位「全自动出稿不发」真闸 + 补两个原因码人话（P0，已按 HEAD 核对缩量）

> 预算 **$100**（首版写 $200，20:5x 按 HEAD 逐条核对后缩量——大半已在库，见「已修 / 未修」）。工单 **#309 #310 #311**（Vanessa / Jeeo，1.0.84）+ **#307**（红条技术码）+ **#308 复核**。落点表 `docs/发版对账_v1.0.85_Q30.md`。

## 已修 / 未修（HEAD `0b606199`，逐条核对）

| 项 | 状态 | 证据 |
|---|---|---|
| 单坐席不写认领、不起 90 s 续租 | **已修**（B13） | `unified_inbox.html` L20108 `renewClaim` 首行 `if(!MULTI_SEAT) return;` |
| 单坐席隐藏「我的」筛选 / 建议接 / 8 s 认领轮询 / 列表认领键 | **已修**（B13） | L5483 / L6422 / L6557 / L6924 / L12119 全有 `MULTI_SEAT` 守卫 |
| 「需人工」原因码人话 + 未知码回落 | **已修** | `_handoffTip` L6824（`_HANDOFF_REASON_KEYS` + Q-15 `_adWhyText` + Q-17 `_rkWhyText` + 回落原码）；L1 原因 `_L1R_KEYS` L17590（M-2 E，含 `inbox.l1r.other` 回落） |
| 状态带六种「不发」 | **已修**（Q-26 A/B，1.0.84） | `src/inbox/conv_state.py` + `#cs-band` |
| **`MULTI_SEAT` 缺省判定** | **未修** | `src/web/ui_visibility.py` L273–285 `resolve_seat_mode`：无显式配置时 **启用坐席账号 ≥2 即 multi，取不到用户表也 multi**；`admin.py` L1154 `_multi_seat_now()` 喂模板 L2245。客户机通常有 admin + 坐席两个账号 → 一律 multi → 上面所有 B13 守卫全部失效，回到「点开即认领 · 处理中 · 释放认领」（#309 #310） |
| **#311 全自动出稿不发的真闸** | **未定位** | `whatsapp:12084403608:917983336714` 12:11 / 12:13 / 12:14 三稿 `origin=auto` 无发送；后端 **没有代码按 claim 拦发送**（claim 只被 `sla_watcher` / `scheduled_reporter` / `unified_inbox_context` 读），所以「认领」不是它的闸；候选 `_human_priority_gate`（`autosend_worker.py` L974–977 `agent_sent / agent_typing`，前端 L15749 `/api/workspace/typing`）或 conv_state held——**必须查 `abort_ledger` 坐实** |
| `media_lie_caught_repeat` 人话（#307） | **未修** | `inbox_workspace.py` / `_HANDOFF_REASON_KEYS` 无此键 → 红条晒原码 |
| `colleague:<id>` / `ops_group` 可见 | **未修** | `peer_bot_guard` skip 只打日志（`autodraft_helpers.py` L1240），不进 `conv_state`；jun #312 / #314 就是自测账号在名单里而界面零提示 |
| #308 顶栏「本会话全自动」vs 草稿条「全局默认半自动」 | **待复核** | `_l1ReasonText` 只在 `autopilot_level==L1` 显；若会话显式 auto_ai，`_l1_explicit` 非 None 不该推出 `global_default`（`autodraft_helpers.py` L1258–1284）——要么该草稿生成于切档之前（陈旧稿），要么档位写入路径没进 `store.set_automation_mode`。复核后要么是「切档即作废旧稿」一行修，要么是无 bug |

## 要做的四段

| 段 | 内容 | 文件 |
|---|---|---|
| **A `resolve_seat_mode` 缺省 single** | 无显式 `workspace.seat_mode` 时：**默认 single**；multi 只在 ① 显式配置 或 ② `agent_coordinator` presence 近 30 min 内 ≥2 个不同 agent 在线（不是用户表行数）；取不到数据 → single。加设置项「多坐席协作」开关（自动回复设置 → 自动化与风控，Q-26 C 区）让多人客户一键打开；单元测试：1 账号 / 2 账号 1 在线 / 2 在线 / 显式 multi 四例；升级一次性 `DELETE FROM conversation_claims`（无 source 列，全清，租约本来 15 min 过期） | `ui_visibility.py` L273–285、`admin.py` L1154、`store.py` 迁移、`reply_settings.html`（与 Q-26 C 在途 hunk 协调） |
| **B 定 #311 真闸** | 用 `tmp_diag\W2CSGA` 的 conv 查 `abort_ledger`（`reply_settings_routes.py` L701 端点 / `tools/why_no_reply.py`）12:11–12:17 三稿的 defer / abort 码：若 `agent_typing` → `/api/workspace/typing` 只在 textarea **真有字符**（len>0）时 ping、切会话立即 `resume_agent_yield(by=switch)`；若 held / 其他 → 该码已在状态带（Q-26），本段只补文案；把 W2CSGA 回放进门禁 | `autosend_worker.py` L944–977、`unified_inbox.html` L15740–15760 |
| **C 补两个原因码** | ① `_HANDOFF_REASON_KEYS` + `inbox_workspace.py` 加 `media_lie_caught_repeat` → 「客户说没收到图 · 已连续 2 次 · 交给你」（zh/en + 繁体 regen）；② `peer_bot_guard` skip 时写 `conv_state`（state `held`，reason_code `colleague:<id>` / `ops_group:<id>`），状态带文案「对方在『永不自动回复』名单（同事 / 本租户账号）· 去 设置 › 自动化与风控 改名单」；门禁：`ABORT_REASONS` ∪ `conv_state` 出现过的 code 每个都有键或走回落 | `inbox_workspace.py`、`conv_state.py`、`autodraft_helpers.py` L1240 回喂 |
| **D #308 复核** | 用 Vanessa × Juris 会话：查 `store.get_automation_mode_if_set(cid)` 与草稿 `l1_reason`；若稿早于切档 → 切档路由里作废未发草稿（`_inflight_cancel` 已有，扩到 L1 稿）；若档位没落库 → 修写入路径。结论写落点表 | `autodraft_helpers.py`、`unified_inbox_aggregate.py` 切档路由 |

## 红线

- 显式 `workspace.seat_mode: multi` 的行为一字不改；B13 已有守卫不动。
- `_human_priority_gate` 60 s 窗口与 defer 语义（Q-18 B）不改，只改「什么算打字」。
- 不动 `peer_bot_guard` 判定（同事名单硬拦是对的），只让它可见。
- `.py` 改动须 zhiliao 重启才生效；模板 / 词包热更。

## 开新对话粘贴

```
读 docs/指令_Q-30_认领体系退出单人端与档位一个真相与原因码人话表_2026-09-12.md，按它做 Q-30（P0，#309 #310 #311 #307 + #308 复核；先看文档「已修 / 未修」表，B13 单坐席守卫 / 原因码回落 / 状态带都已在库不要重做：A resolve_seat_mode 无显式配置时默认 single，multi 只认显式 workspace.seat_mode 或 agent_coordinator 近 30min ≥2 个不同 agent 在线，取不到数据 single，加「多坐席协作」开关到 自动化与风控 区，四例单测，升级一次性清空 conversation_claims / B 用 tmp_diag\W2CSGA 的 whatsapp:12084403608:917983336714 查 abort_ledger 定 12:11–12:17 三稿真闸再改，若是 agent_typing 则 /api/workspace/typing 只在 textarea 真有字符时 ping、切会话即 resume_agent_yield，W2CSGA 回放进门禁 / C _HANDOFF_REASON_KEYS + i18n 加 media_lie_caught_repeat 人话，peer_bot_guard skip 写进 conv_state held reason_code colleague:<id> / ops_group:<id> 并给状态带文案，门禁 ABORT_REASONS ∪ conv_state 码集每个有键或回落 / D #308 复核 get_automation_mode_if_set 与草稿 l1_reason 时序，陈旧稿则切档作废未发 L1 稿，结论写落点表）。预算 $100。先读 Q-26 落点表 docs/发版对账_v1.0.84_Q26.md（在 conv_state 上加，不重做），再 scripts\agent_probe.ps1 -Intent 登记；reply_settings.html / reply_settings_routes.py 工作树里有 Q-26 C 未提交 hunk，开工先 git diff 看归属，提交用 python tools/stage_hunks.py --file <路径> --mine <regex> --apply；显式 seat_mode: multi 行为一字不改；不动 peer_bot_guard 判定与 _human_priority_gate 的 60s/defer 语义。每个 commit 带工单号并立刻补落点表 docs/发版对账_v1.0.85_Q30.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-30 行并 agent_probe -Done。
```
