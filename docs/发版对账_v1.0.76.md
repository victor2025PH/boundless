# 发版对账 v1.0.76（M 批整包；09-07 04:1x M-6 合并六份落点表）

> 接 `docs/M批_进度总账.md`。1.0.75 于 09-06 13:30 发布，首日复验暴露「关怀真发 / 目标冲刺真发 / Messenger 新号直接全自动」三条链打开而内容与通道校验没跟上 → 新单 #214–#235（22 张）。本版是 **M-1…M-6** 的整包。**只汇总，不复制全文**——每行给「工单 → commit → 验收口径」，细节以各线落点表为准（本文件是索引）。
> 服务端 zhiliao 已于 **09-07 01:54:03** 重启装载（preflight GO，`/login` 16s 就绪）+ Messenger 边车 01:54:47 重启（M-2 D）；桌面包 **1.0.76 已于 03:39–03:51 发布三渠道**（公共 clean 496 MB / 内测 smart 590 MB / lite 523 MB 首次实弹），官网 03:52 部署，坐席三台 04:0x 全 1.076 smart/internal，两台内测机等自更或手装（内测渠道链接见播报 mid 1289）。

## 本包必含（按线）

| 线 | 主题 | 单号 | commit（HEAD 全在） | 装机后验收口径（人话） | 落点表 |
|---|---|---|---|---|---|
| M-1 | 出站三闸止血（D-M2 / D-M3） | #214 #218 #234 #219 | A `fe8f7a8a` · B `d7735f97` · C `13b58e3b` | 「补一条关怀」默认原文直发、AI 润色先预览、AI 稿对客户档案核事实、首次真发/带地名人名先留人点头；模拟/真发单一真值（`/api/care/health` 与面板一致）；客户语言判不出转人工不自动发、只说 Hi 判英文、翻译校验失败一律拦、表情/单字不进翻译器（`[xlate] outbound … decided_by=message_script`）；手机端删自己消息工作台灰显「已撤回」且 AI 不再接着聊 | `发版对账_v1.0.76_M1.md` |
| M-2 | 通道真相 + 登录默认半自动（D-M1 / D-M9 / D-M10） | #232 #233 #235 #223 #156补 | A `ece8a0aa` · B `3fe64e53` `8d272236` `dc8c5e0a` · C `b085e4bf` · D `573de634` · E `3443edf1` | 发送 ✓ 只认边车回执；未连接账号红标 + 自动/手动同一闸拦下；登录/重登 10 分钟冷静期只起草、账号默认半自动、全自动按账号显式开并确认「将对 N 个会话开启」、连续 3 次失败自动降级红标；退避期手动每窗放行一次；L1 草稿写明原因；下拉带「本会话：」；切手动默认一直手动；边车 `restoreAll summary` / 404·500 落日志。桌面机升级首启日志 `全局自动化档位已按 1.0.76 默认收成半自动` | `发版对账_v1.0.76_M2.md` |
| M-3 | 媒体发送链（D-M4 / D-M5）——真根因＝全局 2MB body 闸 | #227 #228 #229 #231 钧#215 | A `7899cc9a`（前端半随 `ece8a0aa`）· B `04ad5bbf` · C `c943261e` · D `b80a3907` | 50MB 内视频 / 平台上限内图片能发，超上限选文件即「文件 X MB 超过上限 Y MB」，上传期间工作台不卡（backend.log `[send-media] request … mode=stream` / `rejected reason=` / `[http] … 413`）；.MOV 选文件即判（有 ffmpeg 转 MP4，没有当场拒绝）；附件按会话隔离、附件条「发往 平台 · 客户」；错误四分类。**真机四平台 12 次矩阵交内测机跑**（M-3 表） | `发版对账_v1.0.76_M3.md` |
| M-4 | 工作台第三轮（D-M7 / D-M8） | #222 #221 #230 #206补 | A `b9ea59f3` · B `898715ac`（`4358ca4b` 撤回误卷 hunk）· C `dfa46687` · D `198d1e17` | 角标 = 私聊能点开的未读、群/频道单列、装载首 tick `[buried] startup_sweep cleared N`；顶栏「自动(N)」页内筛选不刷新不变浅色、主题开关进头像菜单、徽标「旗舰版」、三档收缩；多选专用工具条；右侧会话工具整块撤掉、AI 状态弹层显全自动摘要 | `发版对账_v1.0.76_M4.md` |
| M-5 | 产品清理与上游（D-M6 / D-M7） | #217 #224 #226 #225 #220 | A `b5c24bc8` `d55f8ebb` · B `0a19204e` · C `169bcb6e` · D `40249ce5` · E `1090c4cf` | 用户版无「转化成交」类目、已建标「模板已下线」、无产品目标 AI 主动拍含报价/开户/支付整条不发（`[goal_no_product]`）、目标首条主动消息先进草稿审批（`goal_first_send`）；养号面板判词同口径 + 人话说明；出图改开发中态（`[image_gen] probe skipped`）；工具箱转录并入 `voice_transcriber`（`[toolbox_asr]`，探针夹具随包，无「asr/ser 域缺夹具」）；学习队列日/韩/泰/越出译文、未译不留空、同题合并 | `发版对账_v1.0.76_M5.md` |
| M-6 | 值守收口 | 钧 #215 + 22 单 + 1.0.76 | 门禁代修 `527700d8` · release `6403463d` | 见 `发版对账_v1.0.76_M6.md` §2–§4 | `发版对账_v1.0.76_M6.md` |

**本包还含（非 M 批、同 HEAD 打进去）**：小智对话球 v3 线（`docs/实施95_…`）在树上**未提交**的 `assistant_routes.py` 14 行（语音限频 6/分→可配 20/分）与 `assistant-ball.js` 1507 行随打包进入三包（工作树即包）；`config.example.yaml` 4 行同源。该线未登记意向，M-6 装载前核过自洽（`vcfg` 同函数已定义、装配 4 passed）。

## 用户可见的一句话公告口径（22 张新单，每单 ≤ 20 字；完整 fix_note 见 `engines/chengjie/tmp/m6_mark_fixed_1076.json`）

#214 关怀模拟/真发一个真值 · #215（钧）发图发视频两支 1.0.76 都关（等日志包定性）· #216 目标引擎推进——随 #236 定性暂不标验证 · #217 转化成交类目下线 + 无产品禁报价 + 首拍先审 · #218 补一条关怀默认原文直发 · #219 己方删除同步「已撤回」· #220 学习队列多语种译文 · #221 顶栏胶囊页内筛选不刷新 · #222 角标只算能点开的未读 · #223 作用域「本会话：」+ 手动粘性 · #224 养号判词同口径 · #225 工具箱转录并入工作台链 · #226 出图改开发中态 · #227 2MB 闸真根因 + 流式上传 · #228 附件按会话隔离 · #229 .MOV 选文件即判 · #230 多选专用工具条 · #231 四平台同口径 + 错误四分类 · #232 发送 ✓ 只认回执 + 登录默认半自动 · #233 退避锁手动优先 · #234 出站语言五级决策 · #235 发前确认原因可见

台账：20 单随 1.0.76 标 fixed（`duty_ledger_fix --mark-fixed-file tmp/m6_mark_fixed_1076.json`，dry-run 过目后 apply，两份汇总回访不逐单轰炸）；#215 保持 confirmed（无日志禁定性）；#216 保持 new（被 02:27 新单 #236 反证）。

## 合并前提（M-6 C 段核对）

- [x] 六线代码全在 HEAD（`a153cc16..520b0ee7` 27 提交 + M-6 两提交）
- [x] `restart_preflight` 01:53 **GO**（首跑 NO-GO：M-2 B 两端点未登记路由清单 → 代登记；小智线在途 `.py` 核自洽后 `-SkipActiveCheck`）
- [x] **zhiliao 装载** 01:54:03（`/login` 16s）+ **Messenger 边车** 01:54:47（`restoreAll summary dirs=1 restored=1 failed=0`）；装载后 202 行 app.log **0 ERROR / 0 Traceback**；M 批端点全非 404；`[channel_gate] 账号登录/重登 messenger:… → 冷静期 600s 只起草不投递；账号默认半自动（D-M1）` 已对生产 Messenger 账号生效
- [x] 全量回归（01:58–02:43）：**79 failed / 25637 passed / 36 skipped，45:08**（`tmp/m6_pytest_full.txt`）。对 L-7（75 / 25339）：71 同红、6 转绿、**新红 9（串行全稳定）→ 0 功能性回归拦发版**；归属：M-1 B ×5（含**文档翻译域外副作用**：单字单元格不再送引擎）/ M-1 C ×1 / M-2 A ×1（重发幂等裁决顺序，请 M-2 核功能含义）/ M-2 E ×1 / M-4 B ×1（M-6 表 §4.1）
- [x] `gate_sweep`（02:45）：**10 failed / 794 passed**：7 老债（J-8 `Dict` 注解 / L 批 seed_switch ×2 · inline_color ×3 · orphan_refs）+ 3 新（M-2 E `takeover_rearm` 未登记；**两条哑按钮门禁**：`_acctBulkMode` / `_gateClearDegraded` / `batchDeletePrompt` 全局不可达 → M-6 代补暴露 `527700d8`，17 passed）
- [x] 繁体再生 → **16539 键**（+157）；changelog v1.0.76 **11 条双语**；ui-build **20260907-0300**；`cp-*.js?v=` → `20260907a`
- [x] `docs/值守症状族对账单.md`：**新立族 8「通道真相 / 登录默认」**（M-2 五层）+ 族 4 补 #227 / #228 / #229 层（媒体 2MB 闸 = 静默失败最大单源）+ 族 2 补 #234 层 + 族 5 补 #218 / #217 层
- [x] `chatxContent.ts` 五值（1.0.76 / 496 MB / sha256 `82dec80a084b1ad7a850cbf3e15b5e0e6238795db79bf9a27e661947fe823595`）⇄ manifest；`gate:content` **PASS 6/6**

## 刻意不做 / 不拦发版

- 回归新红 9 条与 gate 老债 7 条：门禁 / 旧断言 / 域外副作用，交回各线（M-6 表 §4.1）。唯一有功能含义的两处：**M-1 B 文档翻译单字单元格不再翻译**（工具箱文档翻译退化，建议短路限定出站聊天）、**M-2 A 重发幂等裁决被挪到占位之后**（请 M-2 核是否会重复占位）。
- M-3 交办：其它 8 个 multipart 上传口（voice 样本 / 人设相册头像 / KB 文档 / 贴纸 / 联系人导入 / telegram / developer / admin）**同吃 2MB 闸**，>2MB 同样无声失败——**1.0.77 立单**；`zl_collect_*.ps1` 过滤词加 `[send-media]` / `[media]`。
- 钧 #215 日志包未到 → 不钉根因；#236（目标引擎开但不执行，02:27，#166 族第五次）→ 老板 02:5x 已追加 **M-7**（目标引擎可追溯与真出手）承接，1.0.77。
- 两机（skuio DC8F / 钧 B990）装 1.0.76 后的 L-5 §5.2 三样 + M 批六样核对交下一班（M-6 表「两机核版本」行）。
- lite 渠道 09-06 前装过 lite 的客户机不带 channel、仍跟公共渠道——有的话手装一次 `downloads/lite/ChatX-Setup-1.0.76.exe`（L-5 备注）。

## 发布动作（照 1.0.75 模板；M-6 C 段）

1. [x] release commit `6403463d`（版本 bump 1.0.75→1.0.76 / 官网五值 / changelog / 繁体 / ui-build / 打包同步产物 30 文件）
2. [x] `npm run dist:win:smart` → **590,804,448 B** + `latest-internal.yml`（20.4 min，冒烟三项 ✓）；`dist:win:clean` → **519,613,774 B** + `latest.yml`（首启零数据冒烟 ✓）；`dist:win:lite` → **522,942,649 B** + `latest-lite.yml`（首次实弹，冒烟 ✓）
3. [x] `publish_chatx.ps1 -DistDir dist-clean -Yes -Notes …`：VPS sha512 ✓ / R2 / `latest.yml=1.0.76` / `announcements release-v1.0.76` / 清理 1.0.73
4. [x] `publish_chatx.ps1 -DistDir dist -Channel internal -Yes`：`latest-internal.yml=1.0.76`，公共指针未动
5. [x] `publish_chatx.ps1 -DistDir dist-lite -Channel lite -Yes`：`downloads/lite/` 首次落地，`latest-lite.yml=1.0.76`
6. [x] `npm run gate:content` PASS → `scripts/deploy.ps1` 部署完成 [OK]（公网体检 healthy）
7. [x] 坐席 `push_chatx.ps1 -TargetSsh {kouxing,lianbei,yunsheng} -Install -Relaunch -Smoke` 三台 OK → `chatx_fleet_status` **三台 UP · 1.076 · smart/internal**（yunsheng 23:10 backend DOWN 随重装恢复）
8. [x] `duty_update_hint --set "1.0.76 已发布：…"`；报障群 bot 播报 **mid 1289**（`tmp/_broadcast_1076.txt`，`duty_reply --group neice`）；频道 @hykj7 **messageId=153**（`tmp/m6_channel_post.py`，VPS 本机 POST，campaign `release-1076`）
9. [x] 回访四条（03:57–03:58）：skuio 对账表 1.0.76 版 **mid 1290** + 21 单口径 **mid 1291**；钧 #215 定性 **mid 1292** + 1.0.76 复验口径 **mid 1293**（文案逐字见 M-6 表 §2 / §3）
10. [x] 台账：20 单 `new → fixed`（`tmp/m6_mark_fixed_20_{dryrun,apply}.txt`）→ `duty_notify_summary --reporter 8942577244 --msg-id 1290 --ids … --apply` 20 单记账 → fixed 未回访 61（＝8 月老积压，交下一班）
11. [ ] 两台内测机装 1.0.76 后核版本（交下一班：M-6 表「两机核版本」行六样）
