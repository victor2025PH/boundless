# 发版对账 v1.0.75（L 批整包；09-06 11:5x L-7 合并七份落点表）

> 接 `docs/L批_进度总账.md`。1.0.74 已装两位内测机（钧 B990 / skuio DC8F，**均为干净包**）+ 坐席三台（smart；yunsheng 09:24 已切内测渠道）。
> 本版是 **L-1…L-7** 的整包（20 张真未修 #194–#213 + 两条系统性缺口：内测机拿不到「出厂开」种子 / 桌面包带内网默认地址）。**只汇总，不复制全文**——每行给「工单 → commit → 验收口径」，细节以各线落点表为准（本文件是索引）。
> 服务端 zhiliao 已于 **09-06 11:45:26** 重启装载到 `b7e9ef5d`（preflight GO，`/login` 14s 就绪），装载范围 = 08:55 那次重启之后的全部 L 批提交 + 11:22 兄弟线整树分主题提交；桌面包 **1.0.75 已于 13:15–13:21 发布两渠道**（公共 clean 495 MB / 内测 smart 590 MB），坐席 kouxing / lianbei 已装，两台内测机等手装（见「发布动作」步 10）。

## 本包必含（按线）

| 线 | 主题 | 单号 | commit（HEAD 全在） | 装机后验收口径（人话） | 落点表 |
|---|---|---|---|---|---|
| L-1 | 出站可信度：拆条止血（D-L3 出厂关）/ 问候不编事实 / 无响应文案合一 | #210 #208 #194 | A `2f1cca8b` `630835d7` · B `ecf2a5e2` · D `0448aebf` · C 代码在 `a781e36b`（标记 `556d1aec`） | skuio 机首启日志「拆条已按 1.0.75 默认收紧」，Sceya 会话新出站单条；`[fabrication_guard] 无来源近况陈述` 行数不成规模；发媒体无响应只出一处提示 | `发版对账_v1.0.75_L1.md` |
| L-2 | 人设与语音闸门（D-L4） | #205 #203 #204 #202 #206 #192残 | B `a3216231` · A `3f31eeab` · C `a1e54876` · D `bda3bbce` · E `d0593c7b` · F `df2f5c2a` · ui-build `a2cbfb8a` · 繁体 regen `5e153a9a` | 语音 tab 三选一；Mizuki 保存红字「配置需修正」；新建人设默认不发语音；克隆引擎离线不播任何声音；steven 卡片「服务中 1 个账号」；人设备份三按钮；#192 残要 skuio 放大图 | `发版对账_v1.0.75_L2.md` |
| L-3 | 工作台状态真相 | #207 #170 #196 #209 | A `b3a34895` · B `19d60567` · C **在 `a924426f`**（8 文件卷入 L-5 B1 提交，按文件对）· D `a781e36b` | 装载后首 tick Loki 红标自动消失；列表上方琥珀横幅「有 N 条未读在已归档会话里」常驻；断线卡六出口、「不再提醒此账号」换机不弹；批量挂链弹层随主题 | `发版对账_v1.0.75_L3.md` |
| L-4 | 后台可见性分层（D-L2 / D-L5 / D-L6 / D-L7） | #197 #198 #212 #200 #201 #199 #211 | A `395acee7` · F `b5d146bd` · B `5512c0a6` · G `a4f453da` · C `f9edb52b` · D `e180d1b1` · E `d6942318` | 用户版无三卡 / 四页收起显「功能升级中」；`/developer` 开开发者模式带「研发」角标回来；学习队列存量清标日志 `学习队列存量清标（L-4 #201）`（zhiliao 11:47:02 已见：noise_rejected 2 / dup_recalced 7）；语音单一下拉 | `发版对账_v1.0.75_L4.md` |
| L-5 | 内测渠道与出厂默认（D-L1 = 两者） | #166残 #185残 | A `64f40009` · C `d5d30095` · B1 `a924426f` `85612834` · lite `7e628f4d` | 两机装 1.0.75 首启：`✅ 冲刺推进器已常备（goals.sprint.enabled=True…）` + `/api/crisis-events` `audit_on/escalation_on=true`；装 `downloads/internal/` 包后**无**「随包种子缺席」行；`chatx_fleet_status` 三台 `smart/internal` | `发版对账_v1.0.75_L5.md` §5 |
| L-6 | 通道与服务端 | #213 #195 内网外泄 secret_key | A `3cca7d84`（服务端 num_ctx 8192 已生效）· B `7c3e6e45` · C `2d804c5f` · D `e56b72a0` | 识图失败提示具体原因；钧再收一次动图贴纸不破图（.webm 会动 / .tgs 缩略图 + 角标）；客户机不再每 30 分钟「LAN GPU 不可达」红 toast、转录不再双级返空；桌面首启 `config.local.yaml` 有随机 `secret_key`、0.0.0.0 入口不退回回环 | L-6 落点表（收工时补） |
| L-7 | 值守登记链 + 收口 | 流程 + K-5 ④⑤ | A `085cd14b` `5f6f3916` · C `b7e9ef5d` | 下一份 Cursor 报告 2 分钟内群里「收到 <码>（主题）→ 已立/已挂 #N」；报障人要清单/进度不再静默（`request_for_info` 事件 + 值守告警）；值守用支持号发消息不再触发 P1 `seq_gap` 误报（`seq_gap_own` 事件） | `发版对账_v1.0.75_L7.md` |

**本包还含（非 L 批、同 HEAD 打进去）**：`d05c9fe2` LINE 副设备 token 7 天自动续期 · 11:22 兄弟线整树分主题提交：`2b05ef61` outbound `unlimited_mode` 单一开关（K-D1 孤儿线 8 文件——**老板未拍即被提交**，运行态与 09-04 起 zhiliao 一直在跑的工作树一致，无新行为）· `c4ce33a6` 小智悬浮球视觉 v2 · `db24720c` 平台覆写「跟随全局」占位显当前值 · `74850f39` i18n（危机审计 #185 错误文案 + staged 繁体包）· `873059fc` deploy 探针 v2。

## 用户可见的一句话公告口径（20 张新单，每单 ≤ 20 字；完整 fix_note 见 `engines/chengjie/tmp/l7_mark_fixed_1075_new.json`）

#194 发媒体无响应只出一处提示 · #195 动图/视频贴纸不再破图 · #196 断线卡片六个出口可静音 · #197 用户版系统设置收起商业三卡 · #198 帮助中心用户版收起 · #199 歌房用户版收起 + 资产门禁 · #200 语音回复单一下拉等三块理顺 · #201 学习队列止血四件 · #202 人设备份与迁移三按钮 · #203 JSON 导入对用户不可见 · #204 卡片显「服务中 N 个账号」 · #205 语音三态且克隆失败不换声 · #206 「补齐向量」改人话不遮挡 · #207 需人工 AI 回上即摘标 · #208 问候不编天气行程 · #209 批量挂链弹层看得清 · #210 拆条出厂关且条数一致 · #211 声音评测用户版收起 · #212 回复策略用户版收起 · #213 识图网关 8192 + 失败原因人话

1.0.74 那 49 张（去向表 35 + #32 + K-4 六单 + K-2 六单，含 #170 #173——L-3 已收口，D-L8 剔除条件解除）口径照 `发版对账_v1.0.74.md`；两批合计 69 单随 1.0.75 标 fixed（K-5 A 案：`duty_ledger_fix --mark-fixed-file`，不逐单轰炸，两份汇总回访）。

## 合并前提（L-7 E 段核对）

- [x] 七线代码全在 HEAD（L-6 四件 `3cca7d84` `7c3e6e45` `2d804c5f` `e56b72a0`；L-6 落点表由 L-6 收工时补，不阻塞发版）
- [x] `restart_preflight` 11:38 **GO**（五项全绿：loadable 静默 / 33 脏 py 语法 0 错 / app 装配 4 passed / Advise 正常 / 冷却 162 min）
- [x] **zhiliao 装载**：11:45:26 `restart_instance`（批处理窗 12:30±45 内），`/login` 14s 就绪；装载后 197 行 app.log **0 ERROR / 0 Traceback**；八个新端点全非 404（`/api/voice/preset-catalog` `/api/personas/schema-keys` `/api/personas/profiles/export` `/api/workspace/channel-alert/mute` `/api/developer/developer-mode` `/api/learner/drafts/translate` `/api/admin/license` 新三字段、`/api/admin/bug-intake` stats 出现 `request_for_info` / `seq_gap_own`）；启动行：冲刺推进器 ✅ / 危机升级桥 / `surface_in_ui=True` / 学习队列存量清标；服务器实例不打「随包种子缺席」（预期）
- [x] 全量回归 `pytest tests/ -n auto -q --timeout=90 --timeout-method=thread`（11:46–12:31，HEAD `b7e9ef5d` 工作树）：**75 failed / 25339 passed / 36 skipped，44 分 17 秒**（`engines/chengjie/tmp/l7_pytest_full.txt`）。对 K-5 那轮（68 / 24993）：K-5 已知红 **59** 仍红、K-5 红 **10** 本轮转绿（L-1 修好 `test_unified_inbox_stage1` 4 例 + `test_baseline_reconcile` 3 例；L-2 regen 消化 `test_i18n_*` 2 例；`test_no_fallback_discipline` 1 例）、**新出现 16** → 串行重跑 **13 稳定红 + 3 抖动**（`test_workflow_explain_168` ×3 串行绿）。归类见下表：**0 功能性回归**。
- [x] `gate_sweep.ps1`（12:4x）：门禁 40 文件 **7 failed / 797 passed**（3 分 05 秒；重跑仍红＝真红）：1 预存（`test_python_undefined_names` J-8 `Dict` lint，14.5h）+ 6 条 L 批门禁债（下表 ②③④⑤）。K-5 同项 1 / 801。
- [x] 繁体再生 `python -m scripts.i18n_hant generate` → 16382 键（+5，L-6 C `inbox.media.sticker_animated` 等），`test_i18n_zh_hant` / `test_i18n_extra_langs` 13 passed
- [x] `docs/值守症状族对账单.md` 更新：族 4 +16（#210）+17（#207）+18（#196）层、族 5 +4（#208）层、**新立族 7 后台可见性**（8 层，L-4 + L-2 三处）
- [x] `python -m scripts.feature_matrix --out docs/功能矩阵_ChatX.md` 重导出（L-1 bubbles 降 B 类后未再生）；`python scripts/bump_ui_build.py` 20260906-1019 → **20260906-1246**（unified_inbox.html 11:29 比戳新）

### 全量回归 13 稳定新红逐条（写文件 + 归属 + 处置）

| # | 测试 | 症状 | 归属 | 处置 |
|---|---|---|---|---|
| ① | `test_avatar_voice.py::test_pipeline_avatar_clone_synth_error_falls_back` / `…_unreachable_falls_back_to_edge` | 克隆失败现在 `ok=False + degrade_to_text`（D-L4 新契约），旧用例仍断言回落 edge 出声 | **L-2 B** `a3216231`（改了 `test_tts_pipeline_smoke` / `test_minicpm_clone_tts`，漏了这两例） | L-2 把两例改新契约（与 `test_tts_no_cross_gender_fallback` 同口径）；**新行为是预期，非回归** |
| ② | `test_seed_switch_upgrade_coverage.py` ×2 | 桌面种子新开关 `inbox.reply_style.bubbles.explicit_newline_only` 不在两套「升级安装可达」机制里；探测器用例同步红 | **L-1 A** `2f1cca8b` / `630835d7` | L-1 把该键登进 feature_registry 或升级迁移覆盖表（`_migrate_bubbles_1075` 已写 overlay，只差登记） |
| ③ | `test_template_inline_color_ratchet.py` ×3 | `_client_tier_upgrading.html` 用了未定义 token `--th-bd-amber3`（+ fallback 亮值漂移）；`unified_inbox.html` 内联硬色 12 > 天花板 11 | **L-4 G** `a4f453da`（L-2 落点表已点名）；内联色 **L-3** A/B 模板段 | L-4 在 `theme-tokens.css` 定义 token 或改用既有；L-3 把一处硬色改 token（或按规则升天花板） |
| ④ | `test_template_orphan_refs.py` | `reply_settings.html` 直接解引用 `#rps-po-empty`，元素已随「人设 ID 秒数表」删除 | **L-4 E** `d6942318` | 死代码（`rpsPoAdd` 已 `if(!body) return`、删除 handler 不可达），无运行时崩溃路径；L-4 删掉两行残留引用 |
| ⑤ | `test_packaged_capability_ledger.py::test_every_not_on_capability_is_dispositioned` | `inbox.reply_style.bubbles.enabled` 降 B 类后客户包不为 on，能力对账单没登记处置 | **L-1 A** `630835d7` | L-1 在对账单登 `ui_unavailable`（设置页可自助开） |
| ⑥ | `test_inbox_inline_handlers_exported` / `test_rpa_inline_handlers_exposed` | `_handoffBarGo` / `_handoffBarAck`（#207 原因条两按钮）IIFE 内未挂 window → 点击 ReferenceError | **L-3 A** `b3a34895` | **L-7 已代修 `22ffee95`**（只在暴露块加两名字，热更即生效；真机点得动才能验 #207）|
| ⑦ | `test_feature_matrix::test_doc_in_sync_with_registry` | 注册表变了文档没再生 | L-1 `630835d7` | **L-7 已重导出 `22ffee95`** |
| ⑧ | `test_ui_build_freshness::test_ui_build_stamp_not_stale` | 前端文件比 ui-build 戳新 | 发版步骤 | **L-7 已 bump 20260906-1246**（随 release commit） |

结论：13 新红里 3 条 L-7 收口顺手消化（⑥⑦⑧），剩 10 条全是门禁 / 棘轮 / 台账登记层（①②③④⑤），交回 L-2（2）/ L-1（3）/ L-4（4）/ L-3（1），**不拦发版**（与 K-5 ②段口径一致）。

## 刻意不做 / 不拦发版

- L-4 待老板四问（D 三档预设入口 / `plan_override` 徽标 / partner 是否藏 bug_tickets / 歌房资产不齐运行时视为关）：回访前贴给老板，不拦发版。
- L-2 #192 残「7 ▾」换图标：等 skuio 放大图确认后另开小单。
- L-3 D 暗/亮截图：装机后由 skuio 机取。
- L-5 「版本旁标 clean/smart」diag 元数据三处（壳 `x-diag-meta` flavor/channel + 官网 diag-upload 落字段 + `duty_evidence probe` 显示）：非本次桌面包必需，L-7 视预算做或转下批。
- K-D1 孤儿线 `unlimited_mode` 已被 11:22 整树提交带进 HEAD（`2b05ef61`）——老板知悉；运行态与之前一致。
- 既有失败 `test_workspace_sidebar` 5 例等预存红：沿 K-5 口径不拦发版。

## 发布动作（照 1.0.74 模板；L-7 E 段）

1. [x] `desktop/package.json` → **1.0.75 / 1.075**；繁体 regen 16382；`chatx-release-notes.json` `gen-chatx-changelog add --version 1.0.75`（中英各 14 条，源 `engines/chengjie/tmp/_notes_1075{,_en}.txt`）；ui-build 20260906-1246；`chatxContent.ts` 五值 → **release commit `df4797e2`**（前置修正 `22ffee95`：#207 两按钮补挂 window + 功能矩阵重导出）
2. [x] `npm run dist:win:smart` 12:47–12:58（后端 PyInstaller 全量重建，rc=0，11.5 min）→ `dist\ChatX-Setup-1.0.75.exe` **590,508,994 B** + `dist\latest-internal.yml`（无 `latest.yml`；K-5 残留的 1.0.74 `latest.yml` 已删）；`✓ 安装版后端冒烟全过 / ✓ 内测包产物冒烟全过`。日志 `tmp/l7_dist_1075_smart.txt`
3. [x] `npm run dist:win:clean` 13:03–13:07（后端复用，rc=0，4 min）→ `dist-clean\ChatX-Setup-1.0.75.exe` **495 MB（519,318,557 B）sha256 `99e3ca41428ed2c97f9ad25856f05096b5fa8b0531e636e35df9bde037a9ff5f`**；`✓ 干净包产物冒烟全过（首启零数据）`。日志 `tmp/l7_dist_1075_clean.txt`
4. [x] `publish_chatx.ps1 -DistDir dist-clean -Yes -Notes "1.0.75：…"` 13:15–13:18：VPS sha512 核过 / pointers + pm2 restart / R2 同步 / 公网 `latest.yml=1.0.75 manifest=1.0.75 exe=HTTP206` / announcements `release-v1.0.75` / 清理 1.0.71。日志 `tmp/l7_publish_1075_public.txt`
5. [x] `publish_chatx.ps1 -DistDir dist -Channel internal -Yes` 13:18–13:21：VPS sha512 核过 / R2 `downloads/internal` / 公网 `latest-internal.yml=1.0.75 manifest=1.0.75 exe=HTTP206`；**公共 `latest.yml` 未动仍 1.0.75**。日志 `tmp/l7_publish_1075_internal.txt`
6. [x] `npm run gate:content` **PASS**（发布后 manifest 对齐）；`website/scripts/deploy.ps1 -SkipAssets` 13:22:56 `部署完成 [OK]`（公网体检 healthy）
7. [x] 坐席：kouxing 13:25 / lianbei 13:27 `push_chatx -Install -Relaunch -Smoke` 全 OK（脚本行 `channel: internal (smart build w/ seed-data…)`，装后 `version: 1.0.75.0`，seatverify FRESH，smoke 六端点 OK）；`chatx_fleet_status` 13:28：**kouxing / lianbei 1.075 `smart/internal`**，yunsheng 1.074 `smart/internal`（已在内测渠道，等 4h 定期查或壳重启自更到 1.0.75——L-5 §3 没验的那一腿，核法：`chatx_fleet_status` 版本变 1.075 + VPS nginx `GET /downloads/internal/latest-internal.yml` UA electron-builder 后跟 `ChatX-Setup-1.0.75.exe` 下载）
8. [x] `duty_update_hint --set "1.0.75 已发布：内测机请用报障群里的内测版安装包装一次（不要点这里的立即更新）；装后按各单验收口径帮忙复验"`（热重载）；报障群 bot 播报 **message_id=1250**（文案 `tmp/_broadcast_1075.txt`，含手装链接 + D-L9 规则）；频道 @hykj7 **messageId=152**（`tmp/_channel_post_1075.py`，VPS 本机 POST）
9. [x] K-5 ④ `--apply`：49 单 + 20 单全 → fixed（事件 `ledger_fix` ×69，不触发回访），`--count-fixed-unnotified` 61 → 126（K-5 预估 130：#61 #63 #64 #67 早有 notify_ts）；⑤ 三条回访 13:30 发出：skuio **mid 1251 + 1252**（C6EFRS 46 项 1.0.75 版对账 + 优先验 5 项 / 需动手项 / D-L9）、钧 **mid 1253**（18 单逐条）；`duty_notify_summary --ids --apply` 记账 skuio 50 + 钧 15 = **65 单**（只记两份文案真提到的单）→ fixed 未回访回到 **61**＝8 月老积压（skuio 47 / 钧 14），补充汇总草稿已备 `.ops\duty_0906\r_{skuio,jun}_backlog_1075.txt`，交下一班发（每人 ≤2 条/日）
10. [ ] **两台内测机装 1.0.75**（无 SSH，靠回访里的手装链接）→ 核 L-5 §5.2 三样（boot 段无「随包种子缺席」/ `✅ 冲刺推进器已常备` / `crisis-events audit_on=true`）+ `duty_evidence probe` 版本；skuio 机首启日志「拆条已按 1.0.75 默认收紧」
