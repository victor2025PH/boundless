# 发版对账 v1.0.83 — Q-21（语言能力矩阵单点 · 起草语言计划可追 · 粤语生成链 · 译文字体：#290 #286 #302 Y82GWM）

> 指令：`docs/指令_Q-21_翻译界面三件_下拉可见与粤语与字体_2026-09-11.md`（09-12 04:5x 修订版）。底座：`docs/修复指令_Q批_并行_2026-09-09.md`（归属：`unified_inbox.html` / `reply_settings.html` 只动 `xl-` / `lp-` 前缀 hunk 并 `stage_hunks --mine`；`resolve_outbound_lang` 六级顺序不动；不动 TTS）。总账：`docs/Q批_进度总账.md` Q-21 行。预算 $100。
> **#289「我的消息」下拉看不见已由 `7c4aae3b`（xlate-pop 自适应，1.0.82）修**——本线只做深 / 浅主题对比复验截图，不重做几何。
> 接手方：先 `git -C D:\boundless log --oneline -8` 核对本表 commit 真在。全部随下一个桌面包；zhiliao 模板 / CSS / i18n 包 / copilot 组件热重载即刻生效，`.py`（lang_catalog 模块 / 路由 / 起草链 / 引擎路由）随下次重启装载——**本线不重启**（交值守 / 发版线）。
> 执行：2026-09-12 05:0x 起（前段 05:0x–05:5x 由上一段会话完成、未提交；06:0x 起本段接手核对、补齐、提交）。

## 〇 事故一句话

**#290**：回复工坊 10 语 / 右栏翻译工具 15 语 / 顶栏译文 34 语 / 人设语言 4 语——四处各写一份短表，粤语（`yue`）与繁体（`zh-tw`）只在顶栏那份里；华哥（粤语书面客户）三单复验仍「没有粤语」。**#302 / Y82GWM**：生成侧 `resolve_reply_language` 与出站侧 `resolve_outbound_lang` 各判各的：英文「Hi」全自动起草出中文，日志没有一行说「这条稿为什么用这个语言」；对方语言判不出时 `L1 reason=lang_unknown` 只在日志里，界面零原因，坐席误以为「人设未选」。**#286**：译文行 `font-size: calc(var(--bbl-fs) - 1px)` 在坐席把气泡字号调大后 ≈0.94×，拉丁译文视觉上比原文 CJK 还大，被读成「遮住原文」。

## 0. 开工基线（06:0x，全只读）

| 项 | 事实 |
|---|---|
| 意向板 | `q-21-lang-catalog-single-source-conv-lang-plan-l`（上一段 05:0x 登记，本段刷新）；同板在途：conv-model-route SLICE C（`unified_inbox.html` #reply-tools / `unified-inbox.css .rt-model-*` / `inbox_workspace.py inbox.mp.*`）、Q-26（已提交 `4412dffb`）、Q-27 / Q-22（已收工）、R83 发版线 |
| 共享文件处置 | `unified_inbox.html` 他人在途 hunk：conv-model-route（`inbox.mp.*` / model-wrap）；我的 hunk 全带 `lp-` / `xl-` / `q21` token → `stage_hunks --mine`。`personas.html` 他人 hunk：Q-26 `psn-ar-`（`--theirs` 排除）。`copilot-client.js` 双树他人 hunk：Q-22 `confirm_system_voice`（排除）。`app.html` 双树 `?v=` 行与 Q-22 `cp-voice` / `cp-i18n` bump 同 hunk（上下文粘连）——**随本线一起进 commit**（纯缓存戳、Q-22 已收工，落点表明示）。索引 06:4x 被 conv-model-route 线占用（17 文件已 staged）→ 等其提交后再 stage，不清别人的索引 |
| 代码事实核对 | `translation_engines._HYMT_LANGS` 34 码手写字面量 = 目录本体；`unified_inbox.html _XL_CATALOG` 已是 34 码同序（0830 重建）；`resolve_outbound_lang` ④ 人设语言本就是六级末位 → 「lang_unknown → HOLD」只在人设也没定语言时发生；`conv_state._src_lang`（Q-26，HEAD）读 KV `conv_lang_plan:<cid>` 的 `peer_lang_unknown` / `decided_by` → 本线计划写穿 KV 供其消费；真支持 `yue` 的翻译引擎：Hunyuan-MT / LLM(ai) / Google / Microsoft / Baidu；**不支持**：DeepL / 有道 / OpenCC（只 zh-tw） |

## 1. 落点

| 段 | 单 | commit | 文件 / 内容 | 验收 |
|---|---|---|---|---|
| A 语言目录单点 | #290 | `f3db1410` | **新** `src/i18n/lang_catalog.py`（+ `__init__.py`）：`LANG_CATALOG` 34 码（= Hunyuan-MT 模型卡，含 `yue` / `zh-tw`，顺序 = `_XL_CATALOG`）× 中 / 英 / 繁显示名 + 本族自称 + 能力位 `draft / translate / tts_clone`（`tts_clone` 仅 IndexTTS-2 / fish-speech 已验证集，`yue` 不在，**只是展示位**）；`normalize`（zh-HK→zh-tw、zh-yue→yue、fil→tl、en-US→en）/ `display_name` / `codes_with` / `catalog_payload`；`CATALOG_VERSION=2026-09-12.q21`。`translation_engines._HYMT_LANGS = set(lang_catalog.CODES)`（手写 34 码删）。**新端点** `GET /api/lang-catalog?ui_lang=&cap=`（`unified_inbox_translate_routes.py`；`ui_lang` 缺省取请求 UI 语言）+ 路由清单 `test_admin_route_inventory` +2。五处只读它：① 回复工坊 `cp-draft.js` 「跟随人设 / 账户」下拉 → `client.langCatalog()` → 同源 `fetch('/api/lang-catalog')` → 10 语短表只作兜底；② `cp-xlate-tools.js` 目标语同法（15 语短表兜底，`_ensureCatalog()` 回来就地重填 select）；③ `copilot-client.js` Web / Desktop 两适配器 `langCatalog()`；④ `personas.html #p-language` `_dpLoadLangCatalog()`（4 个静态 option 只作兜底，「跟随对方语言」空值恒首项，`caps.draft=false` 置灰）；⑤ 顶栏译文 / 对话翻译四个 select 的唯一灌入源 `_XL_CATALOG` 由门禁钉与目录逐项同序（模板字面量不能带 CJK，故不注入）。双树 `shared/copilot` ↔ `desktop/renderer/shared/copilot` 逐字节一致；`app.html` / `unified_inbox.html` 三组件 `?v=20260912q21` | `tests/test_lang_catalog_gate.py` 15 例：34 码 / 含 yue zh-tw / 能力位 / `HYMT_TARGET_LANGS == CODES` / `_AGENT_LANG_ALLOWED == CODES` / 旧字面量已删 / `_XL_CATALOG` 同序 / 两组件读端点 + 兜底 ⊆ 目录 / 双树同步 ×4 / `?v=` ≥ 20260912 / personas 兜底 ⊆ 目录 / 端点形状 + `cap=tts_clone` 无 yue + `ui_lang=zh-hant` → 粵語 |
| B 起草语言计划 | #302 Y82GWM #290 | `ea882136`（B+C 同提） | `outbound_translate.py` **只加**：`ConvLangPlan{reply_lang, xlate_target, tts_lang, decided_by, confidence, persona_lang, peer_known, via, fallback}` + `build_conv_lang_plan()`（`resolve_outbound_lang` 六级**原序原样调用** → 非 manual 且历史有客户明确语言请求 `lang_policy.latest_explicit_request` → `request` → 六级全空才补 `lang_prior.initial_lang_hint` → `account_prior` → 仍空 `unknown`；`message_script` 归并为 `decided_by=message`、原始来源留 `via`）→ 一行 `[lang-plan] conv= reply= xlate= tts= by= via= conf= persona= fallback=`；进程注册表（2000 / 6h）+ **写穿 KV `conv_lang_plan:<cid>`**（含 Q-26 读的 `peer_lang_unknown`；只在有决策时落，`unknown` 不落）；`peek_conv_lang_plan(cid, store=)` 注册表 → KV。**reply_lang 只读它**：`autodraft_helpers.enrich_auto_draft` 起草前建计划 → `generate_persona_reply(reply_lang=_plan_lang)`（主路径 + 回声守卫重试两处）；`make_auto_draft_cb` 在 `derive_l1_reason` 前先登记一份（不打日志）→ `l1_reason._lang_unknown` 与计划同源（`peer_known` 为真就不是 `lang_unknown`——英文「Hi」按文字系统判 en）。**lang_unknown 处置**（D-M3 修正）：对方零证据 × 人设有语言 → `decided_by=persona fallback=1`，全自动 / 半自动按人设语言起草**不 HOLD**；会话头 `#lp-bar`「对方语言未知 · 按人设语言（English）回」（Q-26 状态带 `cs-band` 已说同一句时让位，MutationObserver 同步）+ 稿头 `#lp-cdraft` 同标（`/api/drafts` 行内 `lang_plan`）+ **新端点** `GET /api/unified-inbox/lang-plan?conversation_id=`；人审档仍人审，L1 原因 chip「客户语言未定」+ lp chip 双显。i18n `inbox.lp.fallback_persona / fallback_default / chip_t` ZH / EN（`inbox_workspace.py`）+ **新**人工繁体包 `zh_hant_lang_plan_q21.py` | `tests/test_lang_plan_q21.py` 13 例：英文「Hi」× 中文人设 → `reply=en by=message via=message_script` 一行日志 + 注册表 + l1 不报 lang_unknown；零证据 × 人设 en → `by=persona fallback=1` + l1 仍 lang_unknown（人审档原因可见）；零证据 × 无人设语言 → `unknown` 不落 zh；manual 最高不被 request 推翻；六级返回顺序源码钉；`autodraft_helpers` 接线（计划先于 `derive_l1_reason`、`reply_lang=_plan_lang` ×2）；`/api/drafts` 带 `lang_plan`。`test_conv_state_q26` 20 例（Q-26 读 KV 口径）绿 |
| C 粤语生成链 | #290 | `ea882136`（B+C 同提） | `persona_reply.yue_style_directive(reply_lang)` 纯函数：`yue` → 口语粤语指令（繁体字 / 粤语虚词语气词 / 禁普通话书面词）+ 3 条 few-shot，其它语种 `""`；注入三处：统一路径 `_time_hint` addenda / 兜底 `ai.chat` `_lang_hint` / 开场白 `build_opener_directive`。`ai_client._LANG_NAMES` +`yue`「口語粵語（廣東話，繁體字）」+`zh-tw`（否则 LANGUAGE RULE 对 yue 短路不写）。翻译：`EngineRouter._translate_pick` 对 `supports_target(target)=False` 的引擎**直接让路**（不再「试一下失败再切」白付往返 + 记一次假失败）→ `yue` 只落到 Hunyuan-MT / LLM / Google / Microsoft / Baidu；`translation_service._log_xlate` `[xlate] hit= provider= tgt=` 加 `tgt=`（Q-16 §2 建议 ① 兑现，provider 本就带）。TTS **零改动**（`tts_lang=yue` 由计划透传，Q-22 消费） | `test_lang_plan_q21` C 段 4 例：指令仅 yue 出 + few-shot ≥3；`_LANG_NAMES` 有 yue / zh-tw；三条注入路径；路由让路（deepl 无 yue 不被调用 / 全体不支持 → `unsupported_target:yue`）。既有 `test_translation_engines / test_translation_service / test_outbound_translate` 邻居见 §3 |
| D 字体 + 复验截图 | #286 #289 | 本 commit | `unified-inbox.css` `.msg-translation`：`font-size: calc(var(--bbl-fs,13px) * .9)`（恒 0.9× 原文；旧 `-1px` 在坐席调大字号后 ≈0.94×）、`line-height:1.55` 与原文一致、`position:static; display:block; float:none; clear:both` 钉死普通块流（后来者不得做成浮层盖正文）；收到气泡译文色改跟气泡前景 `--ifg`（`.msg-row.in .msg-translation` 82% / auto 掺 28% `--xl-trans-text`）——浅色主题把收到气泡调深底时旧靛蓝 `#3730a3` 对比 <2:1 隐身。截图 `docs/shots/q21/`：`q21_xl_picker_{light,dark}.png`（对话翻译「对方消息 →」34 语第二屏，粤语在列）/ `xlate_pick_out_{light,dark}.png`（「我的消息 →」第二屏，粤语行高亮）/ `xlate_pop_clip_{light,dark}.png`（弹层主屏）/ `q21_msgflow_{light,dark}.png`（译文行在原文下方）/ `q21_conv_xlate_panel_light.png` | Playwright 实测（zhiliao 18799，1440×900，`?theme=light|dark`）：译文行 `11.7px` = 13px × 0.9、行高 `18.135px`（1.55）、`position:static display:block`，6 条样本 `overlap=false gap=6–8px`；「我的消息」第二屏 37 行含 `粤语`，浅：`rgb(17,24,39)` on `rgba(15,23,42,.055)`，深：`rgb(236,236,240)` on `rgba(148,163,184,.14)`；弹层选语控件 浅 `rgb(55,48,163)`/`rgb(238,242,255)`、深 `rgb(205,214,255)`/`rgb(40,44,64)`。**几何未动**（7c4aae3b） |
| 落点表 + 总账 | #290 | 本 commit | 本文件；`docs/Q批_进度总账.md` 仅 Q-21 行 | — |

## 2. 未做 / 边界（如实）

- **人设也没定语言 × 对方零证据**：计划 `unknown`（`reply_lang=""`，不落 zh），产线按旧 default 起草，出站闸 D-M3 仍 HOLD `lang_unknown`（`translate_hold:lang_unknown` 转人工）——没有语言可回落时 HOLD 是对的，**不改**；此时 lp chip 不显（没有「按 X 回」可说），L1 原因 chip「客户语言未定」仍在。
- `xlate_target` / `tts_lang` 只是**记录**（与 reply_lang 同源）；出站翻译闸仍自行 `resolve_outbound_lang`（顺序同一份，结果一致），TTS 由 Q-22 读 `peek_conv_lang_plan().tts_lang`。
- `reply_settings.html` 本线**零改动**：该页没有语言下拉（指令表里列它是保守项）；「回复工坊」= 右栏 `cp-draft.js`。
- `drafts.py` 本线零改动（工作树里的 `_unrestricted_skip` hunk 是 conv-model-route 线的）；计划调用点在 `autodraft_helpers.py`（起草链唯一入口）。
- 上一段（05:4x）截图里对话翻译第二屏的置顶搜索框压住首行「自动 · 跟对方：中文」半行——xlate-pop 几何（7c4aae3b 线）既有，本线不动，记给该线。
- 人审档「显示原因」= 会话头状态带（Q-26，读本线 KV）+ 稿头 lp chip + L1 原因 chip；未另做人审队列页改动。

## 3. 门禁 / 邻居

_(待填：提交后补)_

## 4. 红线自检

- `resolve_outbound_lang` 六级返回顺序零改动（源码钉 `test_resolve_outbound_lang_six_level_order_unchanged`）；计划函数只在其**上**叠 request / account_prior / unknown。
- `unified_inbox.html` 只动 `xl-` / `lp-` / `?v=` 三类 hunk（`stage_hunks --mine`）；`reply_settings.html` 零改动；不新增内联色（lp chip 全走 `--tk-*` 令牌）。
- TTS 零改动（`tts_pipeline.py` / `voice_*` 未碰；`tts_lang` 只透传）。
- 每个 commit 带 `#290`；`git add` 显式路径；提交前 `git diff --cached --name-only` 为空。
