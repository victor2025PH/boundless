# 发版对账 v1.0.77（N 批整包 + 接手 M-7；09-08 N-5 合并六份落点表）

> 接 `docs/N批_进度总账.md`。1.0.76 于 09-07 03:3x 发到三渠道，skuio 机 13:14 卸载重装后复验一整天 → 25 份报告 → #237–#249（13 张 = 6 件事）；钧 #250（= #215③ 第二次）。两件 P1 是 1.0.76 自己带出来的：人设备份恢复首次真用即 `_esc is not defined`；主动关怀「立即发」85 分钟不发。本版是 **N-1…N-4 + 接手 M-7 + N-5** 的整包。**只汇总，不复制全文**——每行给「工单 → commit → 验收口径」，细节以各线落点表为准。
> 服务端 zhiliao 装载 / 桌面三包 / 三渠道 / 坐席 / 播报 见「发布动作」节（收口时填实）。

## 本包必含（按线）

| 线 | 主题 | 单号 | commit（HEAD 全在） | 装机后验收口径（人话） | 落点表 |
|---|---|---|---|---|---|
| N-1 | 主动关怀「立即发」真投递（D-N4） | #243（并 #244–#249） | A `e3a58eda` · B `24240f04` · C `14087007` · D `193a09f7` · E `87875dd3` | 立即发＝同步直投：按钮「发送中…」→ 卡片「已发出 hh:mm」/ 失败写原因可再点不重复；bring_forward 置 `due_at=now` 立刻补拍；人工指定时间零错峰 + 卡片预计出手；deferred 队列随启动常备、每拍一行 `[care_dispatcher] tick`、每条出队 `[care-deferred]`；引擎室「发送通道」灯开着不出货变红。16:47 零出队真因＝队列启动期关闸从未 start、懒建只建不起 | `发版对账_v1.0.77_N1.md` |
| N-2 | 人设工作室 1.0.76 回归三件（D-N3） | #237 #239 #238 | A `9c72cdd2` · C `a1fb8d72` · B `2cd84c02` | #237 `_esc` 是 CSV 导出体内函数全局够不着 → 24 处改 `_escHtml`（含 JSON 导入预览同病）+ 恢复前预览「新增 N · 覆盖 N」+ 重名顾问 + 回执「已恢复 N 个人设…含相册引用 M 条」+ 失败人话落 renderer.log；#238 `album_ai.enabled` 默认 True + 官网网关端点放行 + vision_probe + 五态引导条 + 进度 + 完整度纳入相册；#239 常驻「已登记 X · ▶ 试听 · 解绑」+ 播放器常驻 + toast 9s 可关去内部名 + 抽屉假脏守卫 | `发版对账_v1.0.77_N2.md` |
| N-3 | 域包错配与预置清理（D-N1 / D-N2） | #241 #240 | A `01837c2b` · B `4331c851` · C `e2faf064` · D `1c93feba` + `63bac10d` · E `d46b62a9` | 新增 `business_domain` 单一真值（陪伴机＝companion；不换包名）：hook / KB 分类 / 模板 / 摸底槽位 / 画像跟随；陪伴摸底标签 = 称呼/坐标/职业/年龄/兴趣 + 家庭/婚恋/🔒收入/居住/🔒资产 + 自定义标签；画像无商机区（旧值归「其他已记录」）；「标成交」→「标记达成」（达成结果文案待老板）；「营销目标」→「工作目标」；KB 种子按域播、存量 19 条横幅一键清除不回灌；「帮助语料 291 条」是悬浮球语料（第三次说明）。启动一行 `business_domain=companion（hook=CompanionDomainHook，pack=conversion）` | `发版对账_v1.0.77_N3.md` |
| N-4 | 语音质量闸（钧 #250 = #215③） | #250（并 #251）+ #215③ | B `75848c5c` · C `609763e5` · D `1a46c65f` · E `42714553` | 红条橙条从未被判定点亮——`.pv-note{display:flex}` 架空 `hidden`（0831 起恒常可见，#121 三进宫同根）→ `[hidden]{display:none !important}` + 结果区唯一结论行四态；「过期」= identity 不译时基准记成 auto → 只有目标语真变才过期；阈值实录 86 样本 0 误判不改 + `synth_gate` 合成前拦配置非法 + `#profile=<id>&tab=voice` 深链；`[tts]` 前缀统一 + AGENTS 关键词与时间窗。两包 TAZ9QS / TWJ7C8 全程 `speech=voiced`、Mizuki 克隆链健康 | `发版对账_v1.0.77_N4.md` |
| 接手 M-7 | 目标引擎可追溯与真出手（D-M7-1…4） | #236（#166 族第五次；#216 随它） | A `d0933356` · B `9ff2e90a` · C `fe6a7861` · D `eeec70f1` · 对齐 `bf8523b8` | 真因：goals 包 logger 根级名 → 生产 INFO 全丢（四天九份包零 `[goal-inject]`）；每拍可追溯 `GET /api/goals/{id}/beats`（时刻 / 会话 / 正文 / 送达）；被拦记 `beat_blocked` 带原因；新目标 24h 内「等待首拍（预计 hh:mm）」、超 24h 零真发红字 stalled + 看门狗点名；到期当刻 `goal_settled_alert` 进铃铛 + 卡片留 7 天；skuio「11 分钟三启」= 首启 + 两次人为关开，非故障 | `发版对账_v1.0.77_M7.md` |
| N-5 | 值守收口 | 并单 / #242 / #215 定性 / zl_collect v1.3.1 / 去重 / 1.0.77 | B `add27c6d` · D `f3369b52` · 门禁代办 `0e200e67` · release（见下） | 见 `发版对账_v1.0.77_N5.md` §2–§6 | `发版对账_v1.0.77_N5.md` |

**本包还含（非 N 批、同 HEAD 打进去，工作树即包）**：DY（抖音 / TikTok）P0-3 / P0 round 2 / P1 骨架（`platform_registry` / `channel_policy` / `window_guard` / `douyin_official` 等，均 config 门控默认关）、QQ round 3（qqbot 富媒体）、实施97 微信接入（`wechat_kf*` / `wechat_pc/` / `desktop_bridge_presence` / `kf_window_guard`，**未登记意向、装载时仍在编辑**）、小智对话球 v3 在途 hunk。这些线的 `.py` 在打包 / 装载那一刻的状态随包；N-5 只核「语法 0 错 + 全量回归 + 门禁」，功能归各线。

## 用户可见的一句话公告口径（#236–#251，每单 ≤ 20 字；完整 fix_note 见 `engines/chengjie/tmp/n5_mark_fixed_1077.json`）

#236 目标每拍可追溯 + 到期结算 + 卡片说真话 · #237 备份恢复 `_esc` 修好 + 预览回执 · #238 相册上传默认 AI 识别 + 进度 · #239 克隆音色登记常驻反馈 · #240 KB 不预置支付话术 + 一键清除 · #241 业务域单一来源 + 陪伴摸底标签 · #242 版本确认关单 · #243 立即发点了就发（#244–#249 并入）· #250 语音面板单一结论（#251 并入）· #215 发图发视频＝2MB 闸（TAZ9QS 坐实 a 支）

台账：8 单随 1.0.77 标 fixed（`duty_ledger_fix --mark-fixed-file tmp/n5_mark_fixed_1077.json`，dry-run `tmp/n5_mark_fixed_1077_dryrun.txt` **过目后 apply**）；并单 / 关单 / 改挂 9 项（`tmp/n5_a_merge.py`，dry-run 过目后 apply）；#215 已 fixed（N-5 C）；#216 保持 new（skuio 机 backend.log 出现 `[goal-inject]` 即可标 verified）。

## 合并前提（N-5 E 段核对）

- [x] 六线代码全在 HEAD（N-1…N-4 / M-7 / N-5 commit 见上表；`git log b5eea0ae..HEAD`）
- [ ] `restart_preflight` GO（首跑 05:24 NO-GO：N-3 五端点未登记路由清单 → 代登记 `0e200e67`；DY UI 线 + 实施97 微信线在途 `.py` 实时编辑 → 等安静）
- [ ] zhiliao 装载（N-1 未动边车，Node 不重启）+ 装载后 app.log 0 ERROR + N 批端点非 404 + N-3 三行启动日志
- [ ] 全量回归（05:37 起，`tmp/n5_pytest_full.txt`）
- [ ] `gate_sweep`
- [x] 繁体再生 / changelog 8 条双语（`tmp/_notes_1077{,_en}.txt`）/ ui-build / `cp-*.js?v=`（收口时执行）
- [x] `docs/值守症状族对账单.md`：族 5 +7 层「立即发」、**新立族 9「域包错配」**
- [ ] `chatxContent.ts` 五值 ⇄ manifest；`gate:content`

## 刻意不做 / 不拦发版

- N-1：300s 粒度改精确调度不做；Messenger 无同步路径按「排队中」如实回。N-3：首装向导不建（基线 + 后台下拉替代）；不换域包名；存量不删；「达成结果」四选一文案待老板。N-4：阈值不改；`clone_with_preset_voice` 不进合成前拦截（钧机 Mizuki 正是这一档且声音正常）。M-7：`cp-goal-jump-message` 宿主接入 / `sent_hook` 带正文 / `list_goals` SQL 过滤 / 壳主进程日志落盘 / 「推进节奏可选」功能需求——交下一班。
- M-6 交办未收：其它 8 个 multipart 上传口同吃 2MB 闸（voice 样本 / 相册头像 / KB 文档 / 贴纸 / 联系人导入 / telegram / developer / admin）→ **仍待立单**；`zl_collect_*.ps1` 过滤词 `[send-media]` / 收 `run_sentinel.json` / boot 窗不过滤 → v1.3.2（N-5 §七）。
- 新报障 #252–#255（09-08 00:51–02:05）与 47KNBV / 3HNCJ7 需求：不在本版，见 N-5 §七 分派。

## 发布动作（照 1.0.76 模板；N-5 E 段）

1. [ ] release commit（版本 bump 1.0.76→1.0.77 / 官网五值 / changelog / 繁体 / ui-build / 打包同步产物）
2. [ ] `npm run dist:win:smart` / `dist:win:clean` / `dist:win:lite`
3. [ ] `publish_chatx.ps1 -DistDir dist-clean -Yes -NotesFile …`
4. [ ] `publish_chatx.ps1 -DistDir dist -Channel internal -Yes`
5. [ ] `publish_chatx.ps1 -DistDir dist-lite -Channel lite -Yes`
6. [ ] `npm run gate:content` → `scripts/deploy.ps1`
7. [ ] 坐席 `push_chatx.ps1 -TargetSsh {kouxing,lianbei,yunsheng} -Install -Relaunch -Smoke` → `chatx_fleet_status`
8. [ ] `duty_update_hint --set …`；报障群播报（`tmp/_broadcast_1077.txt`）；频道 @hykj7（`tmp/_channel_post_1077.txt`）
9. [ ] 回访：skuio 两条（`.ops/duty_0908/r_skuio_1077_p{1,2}.txt`）+ 钧一条（`r_jun_1077.txt`）——文案已过红线闸 dry-run
10. [ ] 台账：并单 9 项 apply → 8 单标 fixed apply → `duty_notify_summary` 记账
11. [ ] 两台内测机装 1.0.77 后核版本（交下一班）
