# 发版对账 v1.0.76 —— M-1（出站三闸止血：#214 #218 关怀真发 / #234 出站语言 / #219 己方删除同步）

> 断点续传介质。接手方先 `git -C D:\boundless log --oneline -8 -- engines/chengjie` 核对三个 commit 真在，再从「剩余 / 交办」继续。
> 指令：`docs/指令_M-1_出站三闸止血_2026-09-06.md`；底座 D-M2 / D-M3：`docs/修复指令_M批_并行_2026-09-06.md`；总账：`docs/M批_进度总账.md`。
> `.py` 改动**未装载生产**（等 `restart_preflight` → zhiliao 装载）；`care_schedule.html` / `care_page.py` 保存即热更（按 `preview_required` 特性探测：旧进程只会照旧直接排上，不会半吊子）。
> 花费约 $95（读证据 + 三项 + 三轮邻接回归 + 两次共享树 hunk 切分）。开工 23:01 / 收工 01:1x，2026-09-06→07。

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 状态 / 备注 |
|---|---|---|---|---|---|
| A 关怀真发止血（D-M2：默认原文直发 · AI 润色先预览 · 档案注入 + 事实校验 · dry_run 单一真值 · `[care-gen]` 落日志） | #214 #218 | `fe8f7a8a` | `src/contacts/care_profile.py`（**新**：`build_customer_profile`＝inbox 会话 display_name/language/first_seen + `conv_meta.tz_country`（只信 stated/phone/language 显式来源，behavior 统计推断不采）+ contacts `country_hint/language_hint` + `contact_profiles.known_since`；`profile_block` 注入块「对方是菲律宾人、就住在菲律宾（本地人，不是旅居）/ 认识一年以上（老朋友）/ 常用语言」；`forced_preview_reason`＝first_send / place / person；`check_care_reply` 三态 block / preview / pass）、`src/utils/proactive_fabrication_guard.py`（守卫家族第三条腿 `detect_profile_contradiction`：`local_as_foreigner`（客户国家在 gazetteer 命中 + 外来者框架 life in / moved to / 在 C 的生活习惯吗…）/ `old_as_new`（相识 ≥30 天 + nice to meet you / 很高兴认识你…）；`PLACE_GAZETTEER` 36 国 + `normalize_country_code` / `place_mentions`）、`src/contacts/care_dispatcher.py`（`profile_provider` 注入；LLM 档必带档案块；出稿后 `check_care_reply`：矛盾 → `mark_skipped(profile_contradiction:*)`，首次真发 / 含地名 / 含人名 → `store.mark_hold_for_preview`（note=`hold:<reason>`，行留 pending，此后 tick 跳过）；`effective_dry_run()` 唯一真值 + 翻转 INFO「dry_run 实时值变化 A → B」；`[care-gen]` INFO 记 mode/dry/topic/source/profile/ctx_chars/memory/goal/persona/reply 与 decision；`reply_gates` 跟随 provider 注入（生产恒注入；旧调用方零变化））、`src/contacts/care_schedule.py`（`hold_reason` / `mark_hold_for_preview` / `has_real_sent`；**不**碰 `dry_sampled_at`，held 行不混进 👍/👎 审核队列）、`src/web/routes/care_routes.py`（add 缺省 `mode=verbatim`；`mode=event` 无 `preview_confirmed` → **不入队**，回 `preview_required` + 预览稿 + 注入档案 + `check` 三态 + 理解回显；`_render_event_preview` 与派发同源，预览端点复用；held 行预览直接回扣下的原稿零 LLM；`_effective_dry_run` 供 health / plan / send-now 同读；plan items 带 `topic_norm / hold_reason / hold_text`，engine 带 `interval_sec`；send-now 响应带 `dry_run / held / interval_sec`）、`src/web/templates/care_schedule.html`（默认档 verbatim 芯片 on + 标签「要发的话」；AI 润色档「排上」→ 预览面板（我理解为 / 准备这样发 / 已注入档案 / 矛盾红字禁排 / 确认排上 / 改为原文直发 / 取消）；卡片「待你确认」徽标 + 扣下原稿 + 「就这样发」（send-text）；到期组状态条：模拟中（⏸ + 开启真发按钮）/ 排队 ≤N 分 / 待确认；模拟中「立即发」先说清不会真发，做完 toast 如实回话）、`src/web/i18n_packs/care_page.py`（`cs7_*` 35 键 zh/en）、`src/bootstrap/background_tasks.py`（`profile_provider=_care_profile` 接线；启动行注明「← 启动快照，实时值以 /api/care/health 与翻转日志为准」）、tests：新 `test_care_verbatim_default_218.py`（36 例：事故原句命中 local_as_foreigner / 老客当新客 / 不矛盾面 / 档案组装三源 / 派发拦-预览-放行-旧调用方不变 / dry 不拦 / verbatim 零 LLM / `[care-gen]` 行 / dry_run 真值与翻转日志 / 路由默认 verbatim、event 先预览、held 与 dry_run 透出、真值取派发器 / 静态钉）；`test_care_verbatim_intent.py` 两例改显式 `mode=event + preview_confirmed` | care 全家 + fabrication guard 14 文件 **265 passed**；`test_care_prompt_context.py` 2 例红＝**J-8 遗留**（`filter_relevant_memory` 把无关记忆「美式/东京」筛掉，用例仍断言在 prompt 里；HEAD 干净树同样红，非本条引入） | **在树，待装载**。事故复刻：LLM 出「How are you finding life in the Philippines?…」+ 档案 PH/一年以上 → `skipped` note=`profile_contradiction:local_as_foreigner`，`_send` 零调用；用户填「kim? are you at cebu now?」缺省即原文直发、零 LLM。**edge**：`reply_gates` 只在注入 `profile_provider` 时启用（生产 `background_tasks` 恒注入，静态钉 `test_background_tasks_injects_profile_provider`）。 |
| B 出站语言闸（D-M3：五级目标语言 · 判不出即 HOLD · 校验失败一律拦 · 表情/单字不进翻译器 · `decided_by` 日志） | #234 | `d7735f97` | `src/inbox/outbound_translate.py`（`resolve_outbound_lang` 单点决策：会话手动「发→X」> contacts `language_hint` > 客户消息证据（burst 强证据 > 加权多数决 > 持久 `conversations.language`）> **文字系统兜底** `script_language`（拉丁→en 或人设拉丁语、假名→ja、谚文→ko、泰文→th、越南变音→vi、汉字→zh、西里尔→ru…）> 出站历史参照 > 人设「回复语言」`persona_default_lang`；全无 → `("","")`；`translate_outbound_text` 目标全无 → **HOLD**（`item["_xlate_hold"]={"reason":"lang_unknown"}`、`delivery_block` 上报、`held_lang_unknown` 计数），旧「目标未知 → 发原文（no_target_sent）」拆除；`target_lang_mismatch` / identity 回显 / 引擎拒绝 / 引擎异常 → HOLD 带原因；`[xlate] outbound conv= target= decided_by= action=translated|hold|pass_*` 每条决策一行）、`src/ai/translation_service.py`（`_no_translatable_body`：表情 / 纯标点数字 / **单字符** → identity 早退不进引擎——15:37–15:53 30 余条 `src_len=1 … target_lang_mismatch` 刷屏根因）、`src/inbox/autosend_worker.py`（**只动出站翻译判定两处**：人工通过链 / 自动链的 `translate_hold` 改 `raise RuntimeError(_translate_hold_message(item))`；新增模块级纯函数 `_translate_hold_message`：`translate_hold:lang_unknown: 客户语言判不出…已转人工确认，不自动投递` / `translate_hold:<err>: 翻译失败待确认（target=x）…不发原文` / 旧回调通用文案——**stage_hunks 部分暂存**，M-2 六个 hunk（channel_gate / account_degraded / cooldown）未动）、`src/inbox/autosend_helpers.py`（翻译回调传 `contacts_store` / `cfg_root`，档案与人设两级有数据源）、tests：新 `test_outbound_lang_gate_234.py`（38 例：文字系统 11 形态 / 拉丁默认只跟人设拉丁语绝不落 zh / 五级优先级逐级 / 事故金标「记忆清零 + Hi → en 不是 zh」/ 零证据 HOLD lang_unknown（含英文文本、gate_only）/ mismatch HOLD 带 decided_by 不落库 / 引擎异常 HOLD / 表情放行 / `[xlate] outbound` 日志 / TranslationService 单字 identity 7 形态 / worker 文案三形态 / 静态钉两处都走助手 + 回调传源）；`test_outbound_translate.py` 两例「目标未知盲发」改 HOLD 口径；`test_translation_refusal.py` 两例输入 "1" 改 "ok ok"（单字符现已不进引擎） | 出站翻译 / 翻译服务 / worker / 守卫 13 文件 **431 passed**；预览树（index 态）133 passed；`test_lang_gate_coverage::test_scan_db_by_day_buckets` 1 红＝**跨午夜 flake**（用例用 `now-3000 / now-2000` 落在两天，00:39 跑必红，与本条无关） | **在树，待装载**。事故复刻：Messenger 会话语言 unknown、仅一条 "Hi" → target=en decided_by=message_script → 中文草稿真译成英文；证据全无 → 不发、失败链留痕 `translate_hold:lang_unknown`。**三条注意**：① HOLD 走既有投递失败链（`autosend_failed` 审计 + 会话内失败留痕气泡带 reason + recoverable 开时铃铛）——「转半自动 L1」的落地形态是「不自动投递 + 坐席在气泡上一键重发」，草稿行本身不改档（`drafts.py`/`store.py` 草稿状态改写不在授权面）；M-2 E 的原因卡直接读 `translate_hold:lang_unknown` 前缀即可；② care / reactivation 走 deferred 队列的同一函数：目标全无 → `DeferredSenderNotReady("lang_gate_hold")` 推后重试直到 staleness 过期——verbatim 关怀发给零语言证据的客户会被扣住，属 D-M3「全无禁投」的既定代价，运营可给会话设「发→X」或客户档案语言解开；③ `main._maybe_translate_outbound` / `protocol_autoreply` / `proactive_topic` 三个旧调用方未传 `contacts_store/cfg_root`（main.py 被两处静态门禁 regex 钉住、其余非本条文件），它们的第二级（档案）/ 第四级（人设）跳过，其余四级照常。 |
| C 己方删除消息同步（handler 独立 group · 出站行标撤回灰显 · AI 上下文/记忆剔除 · 轮询 top 对账兜底） | #219 | `13b58e3b` | `src/client/telegram_client.py`（**根因**：pyrogram 2.0.106 dispatcher 对每个 group 命中即 `break`，删除同步 RawUpdateHandler 与已读回执 RawUpdateHandler 同在 group 0 → 删除 handler **从未被调用过**（skuio 机 14:03–14:16「零删除事件」）；改注册 `group=_DELETE_SYNC_HANDLER_GROUP(7)`；删除更新落 INFO「收到删除更新 ids=…」；轮询兜底每 PRIVATE dialog 调 `_reconcile_deleted_by_top`（纯函数 `find_deleted_by_top`：本地 `platform_msg_id` > 服务端 `top_message.id`、未撤回/未软删、非 failed、存在 ≥90s → 服务端已删 → 同一删除同步入口；top 未变不重扫；零额外 RPC））、`src/integrations/protocol_bridge.py`（`report_deleted_messages` 裸 id 反查按方向分治：出站行 → `revoke_own_by_platform_msg_ids`（灰显不消失）/ 入站行 → 对端软删；两类都交记忆清理钩子；INFO 带 ids/对端软删/己方撤回/账本/上下文数；SSE `messages_deleted` op=`own_revoke|peer_delete|delete_sync` + `peer_deleted/own_revoked`；零命中 DEBUG）、**`src/inbox/store.py`（落点表点名）**：① 唯一新函数 `revoke_own_by_platform_msg_ids`（只动 `direction='out'` 且 `revoked=0`，本账号 / chat_key 收窄，幂等）；② `list_recent_messages` **AI 口径**（`include_deleted=True`）`del_sql` 追加 `AND revoked = 0`——撤回的（己方手机端删 / worker 上报撤回 / 工作台撤回）不再喂 B 线历史、`_replied_after` 等业务口径也不再把撤回当「已回过」；UI 口径（`include_deleted=False`）不变，前端按既有 `m.revoked` 灰显）、`src/inbox/peer_delete_purge.py`（`own_rows`；A 线 assistant 历史条 → `OWN_PLACEHOLDER`、`last_reply` → 占位、`recent_replies` / `_human_said_log`（#177）剔除；己方撤回账本）、`src/inbox/withdrawn_cite.py`（`_OWN_LEDGER` / `record_withdrawn(own=True)` / `own_quotes_for` / `build_withdrawn_hint(own_quotes=)`「【你已撤回·不得再提】…不要问对方看到没有、不要接着这个话头往下聊」/ `redact_history(…, own_quotes)` assistant 侧 / `apply_to_reply` 两本账本；**英文撤回原文匹配收紧**：`_EN_STOP` 泛词不算 + 拉丁词须同句 ≥2 个或整句包含——否则任何含 how/good/life 的英文回复会被剥空成 "Yeah."）、`src/inbox/inbound_enrich.py`（注入层同时取 `own_quotes_for`）、`src/web/routes/unified_inbox_account_routes.py`（`message-op revoke`——WhatsApp / Messenger 边车上报的撤回，**含己方在手机端删自己的消息**——标撤回前先取原文，成功后交同一记忆清理钩子 + INFO）、tests：新 `test_own_delete_sync_219.py`（13 例：store 只标出站 / 幂等 / 账号与 chat_key 收窄 / AI 口径剔除 UI 口径保留 / bridge 分治 + SSE + purger 收方向 / 纯己方 op / A 线四落点剔除 + 两本账本 / hint·redact·sanitize / enrich 接线 / `find_deleted_by_top` 六形态 / 对账端到端 + top 未变不重扫 + 镜像关不动 / 静态钉 group≠0 + INFO + 对账挂轮询 / message-op 钩子 / bridge 不再把出站按 peer 软删）；既有两例按新口径改（出站行进己方账本；工作台撤回 revoked 不进 AI 口径） | 删除同步 / 撤回 / store / 镜像 / enrich / 轮询 / dup guard / stale guard 等 20 文件 **129 + 178 + 81 passed** | **在树，待装载**。事故复刻：本地有 88808 出站行（够老）、服务端 top=88800 → 对账标 revoked=1，工作台重载灰显「已撤回」，`list_recent_messages` AI 口径不再含该句，A 线历史该条换占位、`last_reply` 换占位、`_human_said_log` 剔除，注入「你已撤回·不得再提」。**WhatsApp / LINE 复查结论**：WhatsApp（Baileys 边车 `extractProtocolOp` REVOKE，含 `fromMe`）与 Messenger（web 边车「本方撤回预览」）本就上报 `message-op revoke` → `revoked=1` 灰显，**此前缺 AI 侧处置**，本条补了钩子；LINE 边车零 unsend 处理（`services/` 无 unsend/revoke），LINE 己方撤回仍不同步——**排 1.0.77**（LINE 官方 webhook 有 `unsend` 事件，接进 `message-op revoke` 即可复用整条链）。**未做**：气泡文案仍是既有「此消息已撤回」（`inbox.msg.revoked`）而非按方向分「你撤回了一条消息」——需改 `unified_inbox.html` L13365 一行 + `inbox_workspace.py` 一键，两文件都是 HOT-ZONE 且 M-2/M-3/M-4 正在编辑，未动，交 M-4/M-6（见下）。 |

## 与 M-2 共用 `autosend_worker.py` 的实际落点

只动了指令允许的两处出站翻译判定（人工通过链 L≈975 / 自动链 L≈1264）+ 它们共用的模块级纯函数 `_translate_hold_message`（L≈100，M-2 的登录冷静期 / 通道降级 / 退避锁三处 hunk 一行未碰）。`stage_hunks.py --mine _translate_hold_message --mine "M-1 B #234" --theirs channel_gate --theirs account_degraded` 双向自证通过后暂存；预览树跑门禁绿再提交。M-2 收工时把它们的六个 hunk 正常提交即可，无冲突面。

## 交 M-4 / M-6（不需本条动手）

1. **己方撤回气泡文案**（#219 收尾 1 行）：`src/web/templates/unified_inbox.html` L≈13365 `_bubbleInner` 的 `window.T('inbox.msg.revoked')` 改为 `window.T(isOut ? 'inbox.msg.revoked_own' : 'inbox.msg.revoked')`，`src/web/i18n_packs/inbox_workspace.py` 加 `inbox.msg.revoked_own`＝「你撤回了一条消息」/ "You deleted this message"（繁体/越/泰/印尼 auto 包 regen）。数据面已齐（`m.revoked`+`direction`），纯展示。
2. **M-2 E 原因卡**：出站语言闸的两类 HOLD 已带稳定前缀 `translate_hold:lang_unknown:…` / `translate_hold:<err>:…`（`autosend_failed` 审计 reason、会话内失败留痕气泡 reason、`autosend_deliver_failed` 事件 reason 三处同串），原因字段直接切 `translate_hold:` 后第一段即可映射到「客户语言未知，转人工」/「翻译失败待确认」。
3. **LINE 己方撤回**：排 1.0.77（见 C 备注）。

## 交 M-6：验收口径（回访用）

- **#218**：「补一条关怀默认原文直发（你写什么发什么，不经 AI）；要 AI 润色需先看预览、确认后才排上；AI 稿会先对着客户档案核一遍——把本地人当外国人、把老朋友当新认识这类错直接拦下不发；首次对某位客户真发、话里带地名或人名的，到点前会先留给你点头。」skuio 机验：再补一条「问候早上好」→ 缺省就是原文档，到点原文发出；`backend.log` 每条关怀一行 `[care-gen] … mode=verbatim|event … decision=enqueued`。
- **#214**：「模拟 / 真发只有一个真值：面板到期组直接写『模拟运行中：到点只拟稿不真发』并给『开启真发』；日志启动行只是快照，切换时另打一行『dry_run 实时值变化 True → False』；模拟中点『立即发』会先告诉你不会真发。」skuio 机验：`GET /api/care/health` 的 `dry_run` 与面板一致；切档后日志见翻转行。
- **#234**：「客户语言判不出就不自动发（转人工确认，会话里能看到那条没发出去的原因『客户语言判不出』）；只发了一句 Hi 的客户按字母表判英文，不再中文直投；翻译校验失败一律拦下不发原文；表情/单字不再进翻译器（`src_len=1 mismatch` 刷屏消失）。」skuio 机验：装载后 Messenger 清数据重登 + 客户 "Hi" → `[xlate] outbound … target=en decided_by=message_script action=translated`。
- **#219**：「你在手机端删掉自己发的消息，工作台该条灰显『已撤回』（不再显 ✓），AI 不会再接着那句话聊，也不会把它记成你说过的事。」skuio 机验：装载后手机端删一条 → `backend.log` 见 `[mirror] 收到删除更新 ids=…` + `[protocol_bridge] 删除同步 … 己方撤回=1`；若实时更新丢失，12s 内轮询兜底 `[轮询兜底] 删除对账：…`。

## 事故根因备忘

- **A**：J-8 给了 verbatim 档但默认仍是「客户的事」；LLM 档 prompt 只有近 8 条原文 + 记忆，零客户档案（国籍/居住地/相识时长都在库里：`conv_meta.tz_country=PH`（用户时钟 stated 52 次）、`first_seen` 一年前、`contact_profiles.known_since`），也没有出稿校验；dry_run 三处各读一份配置快照（启动行 / 面板 / 派发器），面板「到点了，下轮巡检就处理」是固定文案不是状态。
- **B**：`translate_outbound_text` 的「目标未知 → 发原文」是 2026-08-17 无兜底纪律漏收的最后一条口子；生成端 `lang_policy` 对 "Hi"（letters=2）给 NONE → default zh 起草，翻译端零证据放行 → 两个脑子都错在同一条路；`src_len=1` 刷屏是 LLM 引擎对单字/表情回显被 `_output_lang_sane` 判 mismatch。
- **C**：pyrogram 同 group 只跑第一个命中 handler，删除同步 handler 注册以来从未执行过；`report_deleted_messages` 无「己方」语义；B 线 `list_recent_messages` 从不看 `revoked`。

## 剩余 / 已知边界

- `tests/test_care_prompt_context.py` 2 例 J-8 遗留红（HEAD 干净树同红）；`test_lang_gate_coverage::test_scan_db_by_day_buckets` 跨午夜 flake——两处均非本条引入，留给 M-6 全量回归时按口径修用例。
- 首次真发强制预览的判据是「该联系人此前无 `deferred:*` / `manual:*` 真发行」，dry_run 消费式旧行不算；已在真发的老会话不受影响。
- 出站语言闸的「转半自动」是失败链形态（不自动投递 + 气泡一键重发），不是草稿档位改写——受 `drafts.py` 禁改约束，若 M-2 E 想把这类稿留在待审列表，需要在 drafts 侧加「投递 HOLD → 回 pending L1」一步。
