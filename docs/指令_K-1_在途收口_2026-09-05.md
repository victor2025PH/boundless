# 指令 K-1：在途改动收口（I-5 收口五件 · D1b 收尾 · 对话 B 幽灵未读告警 · 孤儿线盘点）

> **预算硬上限 $120。** 正常花费 $50–80。花到 $100 停下写落点表 + 总账。
> 类型：**零新功能，只把树上已经写好、测试已绿、但 10–28 小时没人提交的改动按归属切成若干 commit 落地。** 你不写业务逻辑；你唯一的产出是 commit + 落点表。
> 为什么急：这些 hunk 散在 `skill_manager.py`（HOT-ZONE，J-10 phase2 正在改）、`unified_inbox.html`（26k 行，多线混改）、`drafts.py`（禁改文件）里，任何一条线一次 `git checkout -- <文件>` 就全没了；而它们对应的工单（#32 #145③ #155 #156 #159 #166）在 K-5 发版时都要标 fixed。
> **不要重新分析、不要改代码、不要「顺手优化」。** 发现某个 hunk 有 bug → 写进落点表交回原线，照旧不提交它。

---

## 0. 开工前必读（10 分钟内读完开工）

1. `docs/修复指令_K批_并行_2026-09-05.md`：去向表 / 文件归属 / 决策 K-D1 K-D4 K-D7。
2. `docs/I批_进度总账.md`「复查记录」节：I-5 剩余项原文（E3 `pick_products` persona_id / E4 `sanitize_outbound` 未接线 / E5① 悬浮清单 / I-2 交接三项 UI）。
3. `docs/J批_进度总账.md` **J-2 追加 D1b 行**（未提交文件清单原文）与 **J-4 行剩余②**（催 I-2 线提交 `unified_inbox.html` hunk）。
4. `python tools/stage_hunks.py --help`（共享树上的安全部分暂存：`--mine <子串>` 标我的 hunk、`--theirs <子串>` 反向自证、默认干跑、`--apply` 才写 index、`--snapshot` 上保险）。

## 1. 红线

- 提交只用 `git add <明确文件>`（整文件确认只有一条线的改动时）或 `stage_hunks.py --file … --mine … --apply`（混改文件）。⛔ `git add -A` / `-a` / `reset --hard` / `checkout .` / `restore .` / 裸 stash。
- **第一步先上保险**：`python tools/stage_hunks.py --snapshot --snapshot-tag k1-before`（stash create + tag，零副作用），再 `--untracked-zip tmp\k1_untracked_before.zip`。
- 每个混改文件先 `git diff -U0 -- <文件> | Select-String "^@@"` 看 hunk 头，逐个认归属；**不认识的 hunk 不提交**，写进落点表 §D。
- 每个 commit 前跑对应测试文件（下面写明），红就不提交，写落点表。
- 开工 / 每次动 `skill_manager.py` 前跑 `scripts\agent_probe.ps1`：`[HOT-ZONE]` 且 ACTIVE（≤10 分钟）→ 先做别的。
- 每个 commit 后 `git status --short -- engines/chengjie` 核对没装别人的文件。

**开工先登记意向板：**
```
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "K-1 commit orphan hunks: I-5 wiring (guard-check shadow / catalog persona / withdrawn_cite outbound / tear-off / account-unread+persona-dot+address-names UI), D1b liveness, phantom_unread_alert (#32 #145 #155 #156 #159 #166 #120)"
```

## 2. 盘点（先做，写落点表第一行）

```
git status --short -- engines/chengjie
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1
```
09-05 18:3x 快照：61 个 dirty + 18 个 untracked。按下面 A / B / C / D 四组归类，每组列文件 + hunk 头 + 归属依据（注释里的工单号 / 函数名 / mtime）。**ACTIVE 文件（当时是 `installer.nsh`、`memory_promises.py`、`verify_assistant_ui.py`、`assistant-ball.js`、`skill_manager.py`）一律先跳过。**

---

## 3. A —— I-5 收口五件（09-04 13:50–14:05 的孤儿改动；`tests/test_i5_remaining_wiring.py` 5 例 18:3x 实跑全绿）

### A1 桌面 guard-check 改影子（老板 09-04 12:5x 决策，I-1 遗留）
- 文件：`src/web/routes/unified_inbox_desktop_routes.py`（mtime 09-04 13:59，应只有这一条线）。核 `git diff` 只含 `"block": False` / `desktop_guard` / `track_outcome=False` 相关 → 整文件 `git add`。
- 测：`python -m pytest tests/test_i5_remaining_wiring.py -q --tb=line -k guard_check`
- commit：`fix(chengjie/desktop): #160 guard-check「填入并发送」改影子档——只记台账不拦（老板 0904 决策，I-1 遗留）`

### A2 #145③ 选品与出站守卫人设同源（I-5 E3 另一半）
- 文件：`src/companion/goals/service.py` 三处 `catalog_persona_id` hunk；`src/web/routes/goal_routes.py` L191–206 `persona_id=pid` hunk（同文件其余 hunk 归 D1b → B）。
- `python tools/stage_hunks.py --file engines/chengjie/src/companion/goals/service.py --file engines/chengjie/src/web/routes/goal_routes.py --mine catalog_persona_id --mine "persona_id=pid" --mine "persona_id=catalog_persona_id" --theirs preflight --theirs liveness --theirs sprint_live`（先干跑看选中哪些，再 `--apply`）。
- 测：`rg -l "pick_products" tests` 找到的文件 + `tests/test_goal_wiring.py`。
- commit：`fix(chengjie/goals): #145③ 选品 pick_products 显式传 persona_id——绑定货不再因 fail-closed 全黑，与 foreign_product_names 同源（I-5 E3 另一半）`

### A3 #146 族撤回引用出站剥离接线（I-5 E4 另一半；决策 K-D4 允许碰 `drafts.py` 这一处）
- 文件：`src/inbox/withdrawn_cite.py`（`apply_to_reply` + `__all__`，整文件）；`src/inbox/drafts.py` L1229 一处 14 行（`from src.inbox.withdrawn_cite import apply_to_reply`）；`src/skills/skill_manager.py` L5017 一处（`[withdrawn_cite] 剥离主动引用`）；`tests/test_withdrawn_cite.py`（+20 行）。
- **`skill_manager.py` 是 HOT-ZONE**：`agent_probe.ps1` 显示它 ACTIVE 或 J-10 phase2 意向 <10 分钟内有动 → 跳过，先做 A4 / B / C，最后再回来。到时 `stage_hunks.py --file engines/chengjie/src/skills/skill_manager.py --mine withdrawn_cite --theirs memory_promises --theirs episodic --theirs review_reason`，干跑确认**只选中一个 hunk**再 `--apply`。
- `drafts.py`：`stage_hunks.py --file engines/chengjie/src/inbox/drafts.py --mine withdrawn_cite`（当前只有这一个 hunk；若出现别的 hunk 不要一起提交）。
- 测：`python -m pytest tests/test_withdrawn_cite.py -q --tb=line`；再 `python -c "import ast,sys; ast.parse(open('src/skills/skill_manager.py',encoding='utf-8').read())"` 语法自检。
- commit：`fix(chengjie/inbox): #146 族 对端撤回后 AI 不主动提——withdrawn_cite.apply_to_reply 接进 skill_manager 出稿口 + drafts.enrich_draft（I-5 E4 另一半）`

### A4 #32 悬浮清单删除 + #155 / #156 / #159 三项 UI（I-2 交接项，对话 B 后端已在 HEAD）
- 文件：`src/web/templates/unified_inbox.html` 里 I-5 hunk（认领子串：`_cpTearWanted`、`data-ac-unread`、`data-ac-nopersona`、`_openAcctUnreadPop`、`_openAcctPersonaBind`、`persona_unselected`、`_loadAddrNames`、`_saveAddrNames`、`cust-call-peer`、`cust-peer-calls`、`inbox.info.call_peer`、`address-names`、`account-unread`）；`src/web/static/workspace/unified-inbox.css`（`.cp-tear-btn,#ws-cp-dockall-btn,#ws-cp-pip-btn{display:none !important;}`）；`src/web/i18n_packs/inbox_workspace.py`（`inbox.info.call_peer` / `inbox.info.peer_calls_you`，同文件可能混他线键，按 hunk）；`tests/test_i5_remaining_wiring.py`（untracked，整文件）。
- `unified_inbox.html` 当前 154 行 diff：除上述子串外还有 D1b 的 `cp-goal.js?v=` 行（归 B）与可能的他线 hunk。`stage_hunks.py --file engines/chengjie/src/web/templates/unified_inbox.html --mine <上面每个子串> --theirs "cp-goal.js?v" --theirs sticker`，干跑逐 hunk 看；选不干净就分两次提交。
- 后端路由 `GET /api/unified-inbox/account-unread`（`unified_inbox_read_routes.py` L1144）与 `GET/POST /api/unified-inbox/conv-meta/address-names`（`unified_inbox_intel_profile_routes.py` L68/L120）**已在 HEAD**，不需要动。
- 双戳：`ui-build.txt` 首行 + `unified-inbox.css` 的 `?v=`（在 `unified_inbox.html` 里 rg `unified-inbox.css?v=`）。
- 测：`python -m pytest tests/test_i5_remaining_wiring.py tests/test_inbox_inline_handlers_exported.py tests/test_template_orphan_refs.py tests/test_template_unique_ids.py tests/test_template_window_t_keys_resolve.py -q --tb=line`
- commit 可拆两个：`fix(chengjie/ui): #32 撕窗/悬浮清单入口下线（老板 0904 决策 E5①）` 与 `feat(chengjie/ui): #159 账号栏未读数可点直达 + #156 未选人设入口 + #155 双向称呼两格接客户级（I-2 交接三项 UI）`。

---

## 4. B —— D1b 收尾（#166 自动推进真开；决策 K-D7 由本条接管）

P0-1..3 `d34666f3` / P0-4 `abe96ba3` / P0-5 `981790e3` 已 commit。树上剩：
- untracked：`src/companion/goals/liveness.py`、`tools/goal_sprint_drill.py`、`tests/test_goal_liveness.py`
- modified：`goals/sprint_ticker.py`（L505 +14 / L764 / `__all__`）、`goal_routes.py`（A2 之外的全部 hunk）、`health_watchdog.py`（6 个 hunk：**含 `sprint`/`liveness`/`goal`/`ticker` 字样的归 D1b，含 `phantom_unread`/`buried`/`archived` 字样的归 C**）、`webhook_notifier.py`（同样二分：`_EVENT_ALIASES` +3 / `_build_message` 多处 / `_CARD_META` +2）、`cp-goal.js` **双树**（`desktop/renderer/shared/copilot/components/` + `shared/copilot/components/`）、`i18n_packs/goals.py`、`tests/test_goal_sprint_ticker.py` +13、`tests/test_goal_engine_status_166.py` +22、`config/config.example.yaml` **goals 段**（`--mine goals --mine sprint`；同文件 `outbound.unlimited_mode` 是孤儿线 → D；`inbox.workflows.auto_start.max_per_tick: 5` 是 J-2 B 没能切出来的，一并带上）、`unified_inbox.html` 的 `cp-goal.js?v=` 行 + `shared/copilot/app.html` 双树的 `?v=`。
- 测：`python -m pytest tests/test_goal_liveness.py tests/test_goal_sprint_ticker.py tests/test_goal_engine_status_166.py -q --tb=line`，绿后再 `python -m pytest tests/test_goal_*.py -q --tb=line 2>&1 | Select-Object -Last 15`（J-2 A2 口径 948 passed；有红先看是不是他线预存红，写落点表）；copilot 双树门禁 `rg -l "copilot" tests --glob "test_*.py"` 里带 `mirror`/`dual`/`sync` 的那个。
- 双戳 `ui-build.txt`。
- commit：`goals(D1b P0-6): #166 自动推进停摆看门狗 liveness + 演练 CLI goal_sprint_drill + 收尾（ticker/routes/cp-goal 双树/i18n/config.example）`
- 落点表：追加进 `docs/发版对账_v1.0.74_J2.md` D1b 段（一行），J 总账「J-2 追加 D1b」行状态改「已完成（K-1 收尾 `<hash>`）」。

## 5. C —— 对话 B `phantom_unread_alert`（#120 / #159 族被埋会话告警；J-4 / J-6 都点名的他线红）

- 文件：`health_watchdog.py` 里归 C 的 hunk、`webhook_notifier.py` 里归 C 的 hunk、`tests/test_alert_delivery_e2e.py` L511 `_EMITTED_ALERTS` +7。
- 测：`python -m pytest tests/test_alert_delivery_e2e.py -q --tb=line`（目标：`test_source_alerts_all_have_e2e_payload` 转绿）+ `rg -l "health_watchdog" tests` 里的 `test_health_watchdog*.py`。
- commit：`fix(chengjie/inbox): #120/#159 族 被埋会话·幽灵未读告警 phantom_unread_alert 接 webhook 格式器 + e2e 载荷清单`
- 若 B / C 在 `health_watchdog.py` 里切不干净（同一 hunk 两线混），就 B+C 合成一个 commit，信息里两个工单号都带，落点表注明。

## 6. D —— 只盘点不动（写进落点表 §D，等老板 K-D1 及各线认领）

每个文件一行：`| 文件 | +/- 行 | hunk 头函数 | 归属猜测（依据） | 建议 |`

- **「unlimited outbound mode」孤儿线**（09-04 13:50 起，意向板已无）：`src/ops/outbound_policy.py`（新）、`tests/test_outbound_policy.py`（新）、`rate_limiter.py`、`account_limiter.py`、`protocol_autoreply_limits.py`、`quota_state.py`、`token_ledger.py`、`companion_send_gate.py`、`config.example.yaml` 的 `outbound.unlimited_mode` hunk。做：`git diff --stat`、每文件 hunk 头、`python -m pytest tests/test_outbound_policy.py -q --tb=line` 跑一下看红绿、`python -c "import ast…"` 语法自检。**不提交不回滚。**
- `src/client/telegram_client.py`：看 diff 是否是 J-5 交值守①的透传（`account_id/sender_name/msg_id/reply_to_msg_id/last_name` 进 `trigger_verdict`/`observe_group_message`）。若是且自洽 → 跑 `tests/test_bug_intake.py tests/test_bug_bot.py` 绿则可提交，commit 带「J-5 交值守① 透传」；否则只登记。
- 其余不认识的 dirty：`deep_persona.py` `proactive_topic.py` `contacts/bootstrap.py` `care_budget.py` `reactivation_loop.py` `companion_proactive.py` `daily_ritual.py` `peer_bot_guard.py` `line_rpa/service.py` `whatsapp_rpa/service.py` `drafts_routes.py` `sticker-panel.js` `i18n_packs/errors.py` `zh_hant_auto.py` `admin.py` `help_terms.py` `base.html` `workspace_base.html` `config/eval/xiaozhi_dialog_questions.yaml` `desktop/build/i18n-packs-staged/**`（打包同步产物，值守侧）——每个 `git diff --stat -- <文件>` 一行 + 从 hunk 注释里找工单号/批次名猜归属。**ACTIVE 的（installer brand 线、J-10 phase2 线、assistant-ball 线）只记不动。**

---

## 7. 交付 + 换账号怎么接

- 落点表 `docs/发版对账_v1.0.74_K1.md`：§A1–A4 / §B / §C 每个 commit 一行（文件 / 认领子串 / commit / 测试结果）；§D 盘点表。
- 更新 `docs/K批_进度总账.md` K-1 行；`docs/J批_进度总账.md` D1b 行；`docs/I批_进度总账.md` I-5 行「剩余」里 E3 / E4 / E5① / I-2 三项 UI 划掉并写 commit。
- 清意向板 `-Done`。
- 结束报告：提交了几个 commit、各对应哪些工单；还剩哪些 dirty 文件与归属猜测；哪些 hunk 有疑问交回原线；K-3 / K-5 现在可以碰哪些文件了。

**下一个账号的新对话粘这段：**
```
接手 K-1（在途改动收口：I-5 五件 / D1b 收尾 / phantom_unread_alert / 孤儿线盘点）。按顺序做三件事：
1. 读 D:\boundless\docs\K批_进度总账.md 找 K-1 行，看已完成到哪
2. 读 D:\boundless\docs\发版对账_v1.0.74_K1.md —— 断点，已提交的 hunk 组不要重做
3. 跑 git -C D:\boundless log --oneline -15 -- engines/chengjie 核对 commit 真在
然后读 D:\boundless\docs\指令_K-1_在途收口_2026-09-05.md，从第一个未完成的 hunk 组继续。零新功能，只提交。预算上限 $120（含已花）。
```
