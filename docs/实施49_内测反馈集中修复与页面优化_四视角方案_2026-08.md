# 实施 49：内测反馈集中修复与页面优化（四视角方案，2026-08-20）

> **值守交接（2026-08-21 04:55，上一值守对话收尾）**：v1.0.45 已发布（官网+镜像+
> 应用内公告）；总账 B1-B21 + A1/A2 全部有结论。**新值守对话的开机三步**：
> ①读本文档（重点 B21/B14 两个在办语音问题——别混为一谈）②sync 双群积压
> （skuio 深夜可能还有新截图；小海等 1.0.45 验证回访）③重挂日志监听。
> 在办优先级：B21 试用钩子两行修复 → B14 第三层等 skuio 诊断码（或 173 坐席
> 内部复现抓栈）→ 用户逐项验证回访关单。群操作 SOP 在上一对话的交接指令里
> （API/force_lang/sync-history/读原图纪律等），老板处有存档。

> 来源：两个官方报障群 48 小时实测——@BUGTIJIAO（小海）+ 内测bug群 @neice111（skuio 花无缺，
> v1.0.43 客户端，一上午提交 10 项）。本文档 = 问题总账 + 已完成清单 + 四视角分析 +
> 新对话的开发工作清单。**执行对话读完本文档即可开工，无需回溯聊天记录。**

## 一、问题总账（用户原话 → 根因 → 状态）

| # | 报告人 | 问题（用户原话摘要） | 根因 | 状态 |
|---|---|---|---|---|
| A1 | 小海 | 克隆音色失败，提示要配阿里云 Key | 客户包语音引擎零通路（打包排除依赖+客户档静态关） | ✅ 已修，1.0.43 已发布 |
| A2 | 小海 | 人设植入了为什么用不了 | 账号级人设绑定只在 worker 启动时读取，无热应用 | ✅ 已修，1.0.43 已发布 |
| B1 | skuio | 对方语音消息无法识别 / **09:29-09:40 追报「装了新版本语音消息还是无法接收」**（两次截图+「这个问题没有解决」的真实指代，值守曾两次误判方向） | 双层：①客户包 ASR 零通路（`voice_recognition.enabled: false`，托管转写机制在但客户档没预授权）——**修复是今晨写的，用户更新的 1.0.43 是昨晚构建，不含此修复**（预期没对齐是值守沟通失误）；②更深一层（09:49 老板点破后查实）：识别不可用分支**连音频都不归档**（`telegram_client.py` 占位分支缺归档调用）——用户只见「识别功能未启用」占位，原音听不了，体感=「收不到」 | ✅ 双层已修待发版：①`_hosted_auto` 三层（运行时+种子+基线）；②占位分支补音频归档（钉 `tests/test_voice_inbound_no_asr_archive.py`）。**发版后必须逐项回访验证**；官网网关转写代理 `/api/ai/v1/audio/transcriptions` 已确认在线 |
| B2 | skuio | 「任务完成系统未开启」找不到设定 | 右栏目标卡「完成提醒」出厂默认关且**无 UI 开关**，提示文案甩配置键名给用户 | ✅ 已修待发版（`companion.goals.notify.enabled` 进 A 类基线，出厂即开，存量升级自动补） |
| B3 | skuio | 全局设定无效（设了默认全局人设，账号没变） | 人设优先级「会话覆写>账号绑定>全局默认」，账号绑过人设则全局默认永远被盖；**保存时零提示** | ⚠️ 机制如此非 bug；已群内讲解+给自查路径。**UX 根治待开发**（见工作项 3）；若其账号无绑定仍不生效则真 bug，等诊断码 |
| B4 | skuio | 「AI 回复策略」页意图映射导向不明，建议隐藏 | 高级配置直接暴露给普通用户 | 📋 已采纳，待开发（工作项 6）。**0825 03:22 复报（_401）**：v1.05x 该页仍露出（并问「全选」作用）——nav 收纳批漏网页，已点名补收 |
| B5 | skuio | 已通过的学习入库回复找不到查看/修改处 | 审核通过条目自动转入正式知识库；学习队列页只有统计数字，无跳转 | ✅ 已指路闭环（知识库页搜标题可查改）。**跳转链接+入口前移待开发**（工作项 4） |
| B6 | skuio | 实时日志页/开发者工具页不该对用户开放 | 运维/开发页面在客户端后台可见 | 📋 已采纳，待开发（工作项 6） |
| B7 | skuio | 浅色主题「更多操作」配色问题 | 收件箱消息右键菜单浅色主题配色断层（待样式细节确认） | 📋 待开发（工作项 7） |
| B8 | skuio | 跨平台档案填写中保存失败 | `contacts` 子系统+`origin_profile` 开关客户包全关，**面板却给填写入口**，错误还显示裸代码 `contacts_disabled` | ✅ 前端已修（未开通不给入口+错误码人话，热更已生效内部；客户端随 1.0.44）。**是否对客户端正式开通 contacts 待老板拍板**（工作项 2） |
| B9 | skuio | 主动给客户发消息，左侧没有聊天窗口 | `telegram.poll_fallback.mirror_outgoing` 出厂默认关——纯出站会话永不落收件箱 | ✅ 已修待发版（进 A 类基线默认开；媒体归档档仍保守关） |
| B10 | skuio | 处理完的会话从列表消失（显示「私聊都处理完了」），强烈主张：会话应像微信/TG 常驻，仅主动删除才消失 | 默认「待处理」队列视图=呼叫中心范式；客户心智模型是 IM 范式 | 📋 已采纳，**P0 待开发**（工作项 1，本批最重） |
| B11 | skuio | 记忆库设了「客户要照片」标准答案（即 B5 那条通过审核的学习条目），客户真问时 AI 答了别的（09:34 重发对话截图确认**未解决**：客户等标准答案，AI 回了英文通用共情句） | 候选三类（按嫌疑排序）：**①跨语言失配（新增强假设）**——其客户说英文（截图为英文对话），而客户包 `ai.embedding_base_urls` 置空 → KB 向量轨死、只剩 BM25 字面匹配 → 中文触发词对英文问句必然 miss。若坐实＝又一「客户包能力缺配」病例：**托管部署缺嵌入通道**（hosted gateway 现只有 chat/voice/asr，无 embedding 路径），影响所有客户端的 KB 语义检索与向量记忆；②触发词没覆盖该问法；③媒体意图通道抢跑。**报告人 09:38 主动收线（「之前处理的可以，不需要再讨论」）——转休眠，不再追材料；其再提或诊断码到手即重启诊断**。⚠️ 跨语言假设①独立成立与否不依赖本单：客户包嵌入端点为空=事实，托管嵌入通道缺失=事实，英语客户存在=截图实证 | 🔍 休眠待重启。**托管嵌入通道（ensure_hosted_embedding，与 ASR 同构）建议不等本单、按独立事实直接挂 P0.5**——它决定所有客户端 KB 语义检索与向量记忆的死活；触发近义扩展/优先级规则挂 P1 |
| B12 | skuio | 左侧会话列表「更多操作」点击没反应（v1.0.43 实机截图） | **不是死按钮**：真因是 CSS 注释陷阱吞规则（详见下方复盘），菜单其实开了、只是落在首屏之外 | ✅ 已修（2026-08-20 19:50，`ui-build 20260820-1950`）——注释改分词写法，真浏览器实测菜单 `onscreen=True`；门禁 `tests/test_css_comment_trap.py` 防复发 |
| B13 | skuio | 「认领/释放」功能导向不明，建议非必须则删 | 多坐席协作功能（认领=这个客户我跟，防重复回复）；单人部署纯噪音 | 📋 已采纳并已群内解释——**并入工作项 6 页面收纳**：单人使用场景默认隐藏认领/释放按钮组，多坐席才显示（判据可用坐席账号数） |
| B14=工单#3 | skuio | 语音发送失败+错误日志满屏乱码（10:32 读原图定案；**22:49 v1.0.45 复测仍失败**） | **三层**（逐层剥出）：①客户包缺 ffmpeg（✅已修，22:49 截图报错字节已是 `OggS`=转换层生效实证）；②异常 repr 喷字节流（✅已修，截断+人话映射）；③**上传层仍炸**——转成 OGG 后 pyrogram upload.SaveFilePart 仍抛 `<Queue …>` 异常。引擎源码态语音发送验证过是好的 → 嫌疑=**打包态（PyInstaller frozen）特有的 asyncio 跨环/事件循环问题**，打包态语音发送可能从未真正成功过（坐席机没人真发过语音） | ✅ **已修待重启窗装载→进 1.0.46**（2026-08-21 10:10 值守线定案，**免诊断码**：skuio 09:36-09:44 补发红框截图 _189/_191 把栈拍全——语音与图片发送同报 `<Queue … SaveFilePart …> is bound to a different event loop`，_191 是 photo（字节尾 FFD9），**B14 实为打包态全媒体上传通病**，不止语音）。根因：跨环封送守卫本身没错，但 pyrogram 2.0.106 save_file:108 用 **Client.loop**（构造期 loop）create_task 起上传 worker、队列却绑 save_file 运行所在的 session loop——构造与 start 分环（打包态形态）时 worker 与队列各在一环。修法（值守线已落地）：client_bound_loop 唯一解析点顺手扶正撒谎的 Client.loop（save_file 内部同环化）+ voice_sender 改经该解析点（原读裸 client.loop 会被骗）。回归钉 `test_tg_worker_loop_affinity.py` 3 枚新增 19 绿、波及面 7 文件 171 绿。**✅ 已验证关单**（2026-08-21 20:36 skuio 1.0.46 实测群内原话「发送语音，发送图片，现在都正常了」；工单 #3/#6/#9 已置 verified，msg 215 群内确认致谢） |
| B15 | skuio | 「聊天为什么不自动回复了」（07:24 漏答，10:40 读图定案） | **不是故障**：其截图顶部横幅明示「今日 AI 额度仅剩 0%——用尽后自动回复暂停到明天」——试用额度当日耗尽的设计行为；用户没理解横幅=额度状态的可见性/教育问题 | ✅ 已群内解答。UX 改进（并入工作项 3 生效可见性）：额度耗尽时会话列表/回复区加显式「额度已用完，自动回复暂停中」状态徽章（比顶部横幅更贴近操作现场）；内测用户额度调整=运营决策 |
| B16 | skuio | 人设改居住地/特质癖好不生效（07:24-07:25 漏答） | 同 B3 优先级语义：他改的人设不是会话实际生效的那个（其会话生效 su_wan，见 10:06 截图身份条）；改对目标即时生效无延迟 | ✅ 已群内解答（指路「回复区身份条=当前生效人设」）；根治并入工作项 3（保存时显示作用范围+受影响会话） |
| B17 | skuio | 永久搁置的会话还在列表；建议独立搁置队列+默认不再自动回复（07:24 漏答） | 现状：搁置会话入「搁置中」筛选分组 ✓ 但**搁置不停自动回复**（弹窗自己写着「客户再来消息时仍会自动回来」）——用户诉求=搁置即彻底不理 | 📋 已采纳：**新增工作项 12「搁置并停止自动回复」选项**（搁置弹窗加一档；实现=搁置时把会话 automation 切 manual/静音，解除搁置恢复；注意与 takeover 静音机制复用）。注意与 B10 的一致性：用户主动动作（搁置/删除）可以改变列表，系统自动（处理完）不行 |
| B18 | skuio | 「稍后处理」弹窗数字快捷键 1-5：按数字时字符掉进右侧语音输入框（09:59 红圈红箭头，10:35 催办） | **复现成立**，但不是「缺焦点抢占/缺监听」：`stopPropagation` 只切传播、**不取消默认插入行为**，而 `preventDefault` 只写在「映射内数字」「永久档」两分支 → 映射外的键（19:30 后只剩 3 档+永久=4，按 5 无人管）与任何字符键，在焦点被弹窗外元素持有时原样落进那个输入框 | ✅ 已修（2026-08-20 20:10，`ui-build 20260820-2010`）——onKey 加「target 不在卡片内 → 吞键 + 收回焦点」兜底；真浏览器实测 5/x/1 零落字（带对照组防空转）；门禁 `tests/test_snooze_dialog_keys.py` |
| B20 | skuio | 13:27-13:34 两截图求分析：自动回复设置「账号班表覆写」圈出「telegram:default」；人设工作室苏婉语音配置显示「zh-CN-XiaoxiaoNeural」 | **占位提示文字被当成实际配置**：前者是「平台:账号ID」格式示例占位，后者是留空兜底提示（该人设实际「已锁定克隆音色」✅）。通用 UX 缺陷：示例型 placeholder 与生效值视觉无区分 | ✅ 已群内解答并更正 AI 的错误建议（「人设绑定要重启账号」——错，1.0.43 起即时生效）。**客户报障的两处已加「例：」前缀**（2026-08-20 20:30，`ui-build 20260820-2030`，中英齐备，线上实测已生效）；渠道配置页同类占位留 backlog |
| B21 | skuio | 全新重装 1.0.45 走完向导领完试用后，登记克隆音色报「语音引擎未接入：请联系官方开通托管语音」（00:55 截图：顶部绿条证明试用已领/AI 已通/额度 62%，右栏登记面板红框报错） | **已代码验证的精确根因**：`license_routes.py:383` 试用领取钩子只调 `ensure_hosted_ai/telegram/vision`，**漏 `ensure_hosted_voice`+`ensure_hosted_asr`**——首启无令牌时五件套全 False，领取后钩子只补三件，语音两件要等**下次重启应用**才接通。窗口期=「聊天正常但语音未接入」 | ✅ **已修待重启窗装载**（2026-08-21 05:15 值守线）：license_routes 试用钩子补 `_link_voice()`（voice+asr 两件，双调用点=自有 Key 早返路径也补），`tests/test_hosted_gateway.py` 64 绿；refresh_daemon 审计=**本就五件套齐全**（hosted_gateway.py:952-956）无需改。已群内告知 skuio/钧「重启一次应用即可激活语音登记，不用重装」。随 12:30 重启窗装载。注意 B14 第三层（OggS 后上传 Queue 跨环）仍是**另一个独立在办问题** |
| **B19（用户自诊，B8/B10 真根因）** | skuio | 「左侧设定/编辑跨平台档案时输入字母 s 触发搁置——聊天列表消失是不是快捷键冲突」 | **完全正确**：S=搁置、E=归档是全局单键快捷键；「正在输入不触发」守卫用 `e.target.tagName` 判定，**Shadow DOM 组件（cp-origin 档案表单等右栏组件）内打字时 document 层 target 被重定向成组件宿主** → 守卫穿透 → 打含 s/e 的文字即搁置/归档当前会话 → 从列表消失。B8「聊天界面消失」与 B10 的直接诱因即此（此前「处理完退场」解释不准） | ✅ **已修并热更生效**（`_hotkeyInEditable`：composedPath()[0] 穿透 open shadow root + activeElement.shadowRoot 逐层下钻兜底 + SELECT/contenteditable 全覆盖；钉 `tests/test_hotkey_input_guard.py` 3 例）；客户端随 v1.0.44。**执行对话注意**：工作项 1 的会话列表改造与本修复同文件已共存（ui-build 1025/1052 两批），勿回退 |

| **B22（值守自察 2026-08-21 凌晨）** | 值守线 | 支持号 02:54-03:18 对 skuio 的带文字截图报障连发陪聊腔回复（「这个表情太合适了」「这么晚还在想我啊」——把截图里客户的话当成对自己说的），工单 #6 标题也是识图描述而非用户原话 | **Telegram A 线 caption 丢失**：`telegram_client.py:2462` 把消息 text **整个替换**成识图描述——用户 caption（「发送图片，报错，分析问题」等）从未进 AI/bug_intake 观察层/收件箱镜像。触发层引燃判定用的是 caption（判对了），应答层看到的却只有描述。LINE 线/`media_enrich.enrich_media_text`/`autodraft_helpers` 本就是 cap-first 拼法，唯 TG A 线是裸替换 | ✅ 已修待重启窗装载（2026-08-21 05:20：cap 在前、描述在后，对齐既定约定；钉 `tests/test_media_desc_guard.py::test_telegram_a_line_keeps_caption_with_desc`，相关门禁 97 绿）。已群内向 skuio 更正并道歉；镜像存量旧行不回填 |

| B23 | skuio | 人设绑定操作顺序死循环（05:25 追报，引用值守指路后实测）：选中人设后显示「已绑定」、没法再点「设为账号默认」，点了又提示「请先在上方选择一个人设」；**06:37 追报同根**：聊天输入框上方仍显示「未绑人设」，但账号默认已设为 steven | 绑定本身已生效（截图有「人设绑定已更新」toast + 身份条显示生效人设），乱的是**按钮/提示的状态机没跟上选中态**——两处 UI 互相矛盾；输入框「未绑人设」小条同病根（不识别账号级绑定，只看会话级） | ✅ **已修（2026-08-21 ~11:00 热更生效，impl49 执行线）**：cp-persona legacy 面板三处状态同源识别账号级绑定——状态行显示「账号默认：X」（新键 `cp.persona.acct_default_line` zh/en）、下拉预选账号默认、「设为账号默认」按可见绑定状态取值（下拉→peer 绑定→账号默认，全空才提示先选）；回复区身份条同步段先吃 30s 生效缓存（换绑后不再闪「未绑人设」）。`?v=20260821a` 双戳+镜像同步+ui-build 20260821-1055，门禁 22 绿；随 1.0.46 进客户包。**🔁 复测未过回炉（2026-08-21 20:43-20:45 skuio 1.0.46 实测，截图 _214/_217）**：右侧卡片解释文案已人话化（B30 部分生效实证），但核心状态打架仍在——「当前生效 steven（旧绑定）」与「输入框上方『未绑人设·账号1』橙条 + 按钮下『请先在上方选择一个人设』红字」同框矛盾，composer 条与按钮提示两个面未同源到旧绑定路径。已群内如实对账并提优先级（msg 218），下一版重点验；执行线注意：v=20260821a 那批的「三面同源」漏了「旧绑定生效 + 下拉未选」组合与 composer 橙条这第四面。**04:17 再补两证（_245/_246，Hjv 会话）**：跨会话均复现（Nicks/Hjv），全局状态逻辑问题实锤，修复以用户四张截图为验收用例 |
| B24 | skuio | 告警通道「发送测试消息」失败，报错显示「测试失败：HTTP 200」（05:27 截图，Chat ID 填了「110」） | 双层：①用户侧 Chat ID 填错（110 不是有效 user id）——已群内指路 @userinfobot 取法；②产品侧**报错文案甩 HTTP 状态码**：Telegram API 返 200 但 body ok=false（chat not found），错误映射层把状态码当话术，违反「人话报错」铁律 | ✅ ②**已由并行线根治**（2026-08-21，`webhook_notifier` 带失败原因 + 路由映射「没找到会话/请检查 Chat ID」人话；钉 `tests/test_webhook_test_error_humane.py`）；①已群内解答 |
| B25（反馈五连，凌晨被闸门误拦） | skuio | ①克隆音色登记入口应并入人设→语音处 ②「该段提示可以删除」（待确认指哪段）③英文版语音界面仍中文 ④额度用完应有显式提示+续费提醒 ⑤主动关怀只拟稿不发出是否费用问题 | ①③④=有效反馈/在办事项；⑤=出厂默认拟稿人审（能力看板可开自动发送）+额度用完暂停真发；②待用户指认 | ✅ 已全部群内补答（05:30）；①并入页面收纳批次——**09:34 用户锐化口径**：克隆音色登记/绑定挪进「人设」页（音色跟人走），工具箱只保留「生成语音」（已群内确认采纳，msg 188）；**0825 02:14 第四次催办（_379，工具箱克隆入口撤除）已高权重记录。****20:37 实景佐证**（纯图被限频拦、B33 归档 _212）：人设工作室→语音页签现状只有「语音 BACKEND + 音色/VOICE ID」技术填空（voice_profile 黑话直露），无克隆音色登记/选择入口——改造方向补充：人设语音页放「登记克隆音色/从已登记音色选」，技术字段收进高级折叠（已群内确认理解并留追问口，msg 213，20:42）、③EN-UI 线在办——**20:50 新实例（1.0.46 复测，截图 _219）**：Follow-up SOP 卡框架已英文、但预置跟进链模板本体（新客破冰/沉默唤回/成交后关怀/报价跟单+步骤描述）是内容库中文写死，i18n 层管不到内容数据；治理方向=预置模板出双语版随界面语言切换（已群内答复分析结论，msg 220，20:52）；**21:27 再两例（_223）**：桌面壳原生菜单（文件/编辑/视图/窗口/帮助）仍中文=main.js SHELL_STR 迁移未竟（EN-UI 线已在办的已知项）+ 右侧提示气泡中英夹杂（「Stage upgrade pending: 初识 → 试探/升温」）。**附带铁证**：同图右下四条「New draft [L2]」堆积=deliver 关草稿排队实拍，B37 诊断坐实（已群内答复并指路回复台「通过」放行，msg 226，21:31）、④并入 B15 UX 根治（新增「点全自动时给额度/续费提醒」）；词表第七轮已补（没法/没有自动/没有触发/可以删除/不合理等，`test_bug_intake.py` 41 绿）。**① Phase A 已落地（2026-08-21 ~14:30 热更，impl49 执行线）**：cp-voice 组件新增 `mode="enroll"` 档（双树+?v=20260821a 三处），人设页「语音」页签挂「克隆音色（跟随人设）」卡＝登记/改绑/对账同源组件+绑定状态行+解绑（psn_vc_* zh/en；enroll 档真浏览器冒烟过：目标人设预选/显示名预填/零异常；verify_cp_voice_ui 35/35 不回归）。**Phase B（下批）**＝工具箱 cp-voice 默认档退役登记折叠段+composer 语音链清尸——刻意两步走：替代面验证存活前不拆唯一登记入口 |
| B26 | skuio | 电脑版发语音方式不适用（06:24-06:26 三条「分析」+截图：红框圈回复框上方「发送语音」浮层）：应支持麦克风录音或发送本地语音文件；**06:33 追报锐化**：右侧工具箱已有「克隆语音发送」，这个浮层与之**功能重复**——要么改成发自己的真实语音消息，要么删除 | 现设计=「打字→克隆音色合成→发出」的 TTS 流，与工具箱克隆发送同功能双入口；用户口径成立：输入框位改「真实语音」（录音/本地文件）或撤除 | 📋 待产品定夺——**决策材料已备（2026-08-21 impl49 执行线）**：①composer 内联语音链 2026-08-05 生产实测**零流量**（52MB 日志窗，B 线 autosend 语音≈0、坐席手动 send-voice=0，见 CLAUDE.md cp-voice ⑪），克隆发送真实用量全在右栏 cp-voice → **建议撤除浮层**（成本≈半天：撤 UI+挪静态门禁登记，与 B25-① 工具箱收纳同批做）；②「真实语音」若要做，本地音频文件发送可复用 send-media(voice) 管线（成本≈1-2 天），麦克风录音需新采集 UI+权限链（≈3-5 天）——建议按需求热度另立项，不阻塞 1.0.46。**→ ✅ 已按建议撤除（2026-08-21 ~14:30 热更，impl49 执行线；老板对「按你的建议开始」三连批复视为采纳撤除方案）**：composer「语音 ▾」入口隐藏（`display:none`+墓碑注释）；DOM/JS 刻意暂留（`test_voice_send_verbatim/status/outbound_xlate` 三族解析函数体+孤儿引用门禁需元素在场），随 B25-① Phase B 一并清尸；**恢复=删一行 style**（老板若要改「真实语音」方向随时可逆）。埋点 `cbtn_voice` 自然归零可佐证无人受影响 |
| B27 | skuio | 翻译工具提交语音文件→「处理失败: 语音转写未启用 (config.audio_pipeline.enabled)」（06:26 报文字，随后截图 _165 拍到红字报错） | **不是 B21/ASR 未接入**（初判错误，已群内更正）：翻译工具走的是顶层 `audio_pipeline` 配置（`unified_inbox_translate_routes.py:917/1005/1327` 硬闸），客户包默认 `enabled:false`（`voice_translate.py:195` 注释原话「实例里全是 enabled:false 的死默认」），**重启无效**；`ensure_hosted_asr` 只注 `voice_recognition` 段不碰 `audio_pipeline`。另报错文案甩配置键（同 B24-② 病根） | ✅ **已修待重启窗装载（2026-08-21 ~10:20，impl49 执行线，随 12:30 窗 + 1.0.46 进包）**：`voice_translate.resolve_effective_audio_cfg` 单一事实源——audio_pipeline 关但 `voice_recognition._hosted_asr` 标记在 → 按注入形状（gateway-first 主位 / LAN-first fallback 条目）派生 openai 后端网关转写配置，设备令牌当次取 `AITR_HOSTED_AI_KEY`（AudioPipeline 不认 "hosted" 占位）；三条路由（translate-voice/video/message-media）同口径收口；报错人话化零配置键（`err.inbox.xl_asr_unavailable`/`xl_video_unavailable` zh/en + media-format.js 兜底文案去配置键）。门禁 `tests/test_voice_translate_hosted_fallback.py` 11 例 + 既有 25 绿。**✅ 已验证关单**（2026-08-21 20:36 skuio 1.0.46 实测「翻译工具翻译语音也正常」；工单 #8 已置 verified） |
| B28 | skuio | 回复台「语音克隆与人设绑定已移至工具箱」一次性搬家横幅：①「针对版本调整的提示，不需要」②点 ✕ 无反应（06:31 截图 _170 圈图确认，即 B25-② 的指认答案） | ①08-17 工具箱重组的一次性提示，存量用户价值低；②其 1.0.45 包上 `_cpMovedHintDismiss` 内联 onclick 疑似 window 挂载缺位或页面处于「服务重启中」半初始化态（截图同框） | ✅ 已修（2026-08-21 06:45 热更，ui-build 20260821-0645）：横幅整条撤除（div+JS 三函数+init+i18n 两键中英），空态说明书卡 s3 仍保留迁移说明；随 1.0.46 进客户包。已群内确认 |
| B29 | skuio | 客户发送的表情无法显示，只有文字提示（06:26 报，截图 _167 圈「[表情] 花痴 / 咪咪吐舌」占位) | 「花痴/眯眯吐舌」是 emoji demojize 中文名——**病根是纯 emoji 文本消息**被 `annotate_inbound_emoji` 整体替换成「[表情] X」注解（那是给 AI 媒体块解析的，其 docstring 自己写明「加注不进存储正文」）；真贴纸链路（media_type=sticker+归档+前端 `<img>`）本就存在 | ✅ **已修待重启窗装载（2026-08-21 ~11:45，impl49 执行线）**：A 线镜像正文经 `_inbound_mirror_text` 还原客户原始 emoji（AI 层注解不动，`test_annotate_pure_emoji` 契约无损）；门禁 `tests/test_inbound_mirror_text.py` 5 例。**已知局限如实记**：动图贴纸（.tgs/.webm）浏览器渲不了仍走占位+手动拉取，本批不扩面 |
| B30 | skuio | 人设绑定卡「会话级覆写未启用（inbox.persona_conv_override.enabled）——下方为旧版按 peer 绑定」表述不明所以（06:37，被限频闸拦后 sync 还原） | 开发内部话术（配置键+「旧版按 peer」）直接渲染给最终用户——与 B24-②/B27 报错同病根，违反「人话报错」铁律 | ✅ **已修（2026-08-21 ~11:35 热更生效，impl49 执行线）**：`cp.persona.disabled_hint` 人话化（「本版本按客户绑定人设：下方选择只对当前客户生效；『设为账号默认』对该账号全部客户生效」zh/en，零配置键零黑话），与 B23 状态机修复同批（cp-i18n `?v=20260821a`） |
| B31 | skuio | 操作页面顶部「AI 链路失败***」横幅：产生原因？该解决什么？在哪解决？（06:38，被拦后还原） | 横幅只报状态不给原因/动作，用户无从行动；今晚高频出现叠加因素=服务端 45s 超时/模型降档切换期 | ✅ **已修（2026-08-21 ~11:35 热更生效，impl49 执行线）**：两级语义落地——AI 降级自愈中（备用/本地顶班，ai-runtime-status.degraded）→ `ws.delivblock.text_soft`「正在自动恢复，无需操作」；非降级拦截 → 动作文案「到待发队列一键放行或手动回复；持续出现联系客服」（弃「按主机弹窗检修」——客户端用户不可执行）。zh/en 双语，ui-build 20260821-1112 |
| B32 | skuio | 主动关怀「确认后才发」逻辑异议（06:35 引用值守 5️⃣ 追辩）：设定计划时段本身就是确认，每条再人工确认属重复动作 | 产品逻辑反馈成立：计划类主动消息的「计划确认」与「逐条确认」语义重复，出厂拟稿人审对计划场景过度保守 | 📋 已登记待产品定夺（方向：计划类视为已授权直发+总开关可关）；已群内确认采纳（07:04） |
| B34 | skuio | 回复额度守卫「每日额度（轮/会话）」页面上限 500，建议改为用户自定义上下限（08:29，截图 _180；其守卫当前为关闭态=只展示不拦截） | 500 是刻意安全阀（防双机器人空转烧钱+单会话过密触发平台风控）；页面刻意不放 0=不限（YAML 手配才有，防误设） | 📋 已登记待产品定夺（方向：常规区间自由设+大批量业务放开高档位并配风险提示）；已群内解释守卫关闭态语义+安全阀理由并采纳建议（08:32，msg 181） |
| B35 | skuio | 首启向导第 2 步（绑定渠道）/第 3 步（挑人设）应改为纯图文教导指引，只描述去哪里做、不放任何跳转链接（09:26-09:27，截图 _182/_183） | 向导现在承担「实际操作入口」职责，用户主张向导只教不带走——与 B25-①（入口收纳）同一设计观 | ✅ **已修（2026-08-21 ~12:30 热更生效，impl49 执行线）**：welcome.html 五步向导第 2 步撤行内「去接入」跳转（渠道列表改纯状态读数）、第 3 步撤「从模板新建/打开人设工作室」两按钮（persona_create_wizard 引入+模态壳一并撤除，人设创建回归「人设」页单一入口）；两步 hint 改「操作路径：左侧边栏 → …」教导文案（zh/en），「刷新」保留为读数+完成闭环（计数>0 自动标完成）。4 个死键回收；`test_onboarding_persona_count` 计数契约无损，门禁 102 绿；ui-build 20260821-1235。**附带事故**：值守 AI（local qwen2.5:14b 26s 出稿）把该建议当求助，回了一篇「教你用向导」的乱稿——已群内更正；答非所问根因=KB 注入「扫码登录」词条+建议句式无求助词，观察层语义判别待开发线 |
| **B36（值守纪律升级 2026-08-21 10:50 老板口谕）** | 老板 | 三条铁律：①用户描述不准时**引导性确认原因**；②线索不够就**先要线索**（版本/步骤/截图）；③不能确认的问题**只记录不作答**——回 117 源码找出答案再回复；且群里**禁止聊天式回复** | 根因=引燃回复正文由本地小模型自由发挥（qwen2.5:14b），prompt_block 软约束管不住（09:36-09:44 四连胡答实录：）「重启看看/换个方式聊/教用户回消息」+误开工单 #9） | ✅ **已修待重启窗装载**（11:05 老板口谕收紧为 **AI 全静默**，取代 10:50 ack-only 初版）：报障群「登记/回复/整理全部由值守人工来，不需要云端 deepseek 和本地模型参与」。实现：`bug_intake.parse_cfg` 键 `ai_silent`（默认开）；`observe_group_message` 置 `ai_silent` 标记（工单登记/告警/收集窗照常运转=值守内部台账工具）；`skill_manager` 观察层之后**生成前短路**（return None，与超时路径同契约）——模型压根不跑，固定话术也不发，footer 自然不发。`test_bug_intake.py` 4 枚新钉 50 绿 + hosted_gateway 64 绿。装载后报障群 AI 零发言，全部群面沟通由值守（173 坐席线）经 API 人工发出。**装载实况**：12:30 窗被漏执行（发版占线），旧代码 19:37 又对钧插嘴一条「去检查设置页面」+误开工单 #10（后被 B37 征用为正经工单）；值守线 19:41 补执行重启（preflight GO，唯一入口，17s 窗口），全批 .py 装载完成，19:47 已群内更正并告知「群内机器人自动回复已彻底关闭」（msg 205）。**03:21 老板追加话术纪律（对值守人工回复）**：①不评论用户作息、不说「去休息/快睡吧/白天我们盯」这类话——别人的工作时间状态不由我们提醒；②只做专业客服两件事=答问题+报修复状态；③禁止跨条重复的客套话术（辛苦/致谢/表情收尾等每条都出现的模板句），每条回复内容不重复 |
| **B37** | 钧 JUN | 新人首引实测（19:36/19:41，截图 _201）：向导第 4 步「AI 自动化档位」点「应用此档位」被红字拦下「部分开关被护栏拦下：需至少 1 个会话设为『🚀全自动(auto_ai)』…」——新人此刻不可能有任何会话，第四步的「启动」永远走不通；建议删除或改纯图片介绍 | 护栏本身=刻意安全设计（`capability_toggle.py:71` 真发双重 opt-in，装好即全自动误发的防线，判定正确）；错在**向导流程**把「应用档位」放进新人首引=结构性死胡同（B35 只改了第 2/3 步，第 4 步漏网），且红字文案带 `auto_ai` 黑话（B24-②/B30 同病根） | 📋 已登记（工单 #10，采纳钧口径）：第 4 步改纯图文教导（介绍三档语义+装好后去哪开），撤「应用此档位」按钮；护栏红字人话化。**19:44 追报并入第 5 步**（截图 _204）：「发送测试消息」在 B35 改造后成无源之水——第 2 步已不在向导内接渠道，下拉恒为「暂无可用的 Telegram 协议账号」+红框报错，永远走不通；处理=第 4、5 步按钮全撤、向导整体纯图文教导，「发测试消息」挪到接完渠道后的工作台引导。并入向导优化批次（B35 续），下一版；已群内确认采纳并更正 19:37 AI 插嘴（msg 205/206，19:44/19:52）。**B36 首战实录**：钧 19:43 那条追报被新装载的 AI 全静默正确接住（app.log 19:44:46「报障群 AI 全静默：已登记…回复交值守人工」，零模型调用零自动回复），机制上线即生效。**20:06 三报（限频拦下但 B33 新机制把图归档 _207，sync 还原文字）**：「开了全自动还是不回复」——截图体检面板自答：①`inbox.l2_autosend.deliver=false` 真发总开关仍关（正是第 4 步被拦的后遗症：`welcome_routes.py:45-49` 档位应用本该开它）+②顶部横幅「今日 AI 额度仅剩 0%，自动回复暂停到明天」。已群内人话解释两道闸并指路（现已有全自动会话→回第 4 步重点一次「应用此档位」即可开成；额度明天刷新再验；内测追加额度=转运营定夺）（msg 208，20:12）。呈现层三缺陷（黑话红字/向导死胡同/横幅不醒目）均已在 B37/B15/B24-② 各自登记。**21:26 skuio 同卡（第 2/2 位内测用户）**：「已开启全自动为什么不回复，是额度还是设定」——其额度余 21% 非额度因，即真发开关出厂关+向导第 4 步被拦的组合死角；已群内给「体检面板自查+向导第 4 步重应用」两步路径（msg 222，21:29）。**两小时内测两人全员卡死在同一出厂默认上——「全自动开启流程」重做（向导教导化+真发开关人话化+首次点全自动时引导开启）应提为 1.0.47 最高优先**。**21:29 skuio 按指路开启成功**（截图 _224/_225：默认档位=全自动 + 上游总闸读数三绿 Worker/真发闸门/自动拟稿均已开启），真发链路打通；已群内收尾（新消息实测→草稿放行→额度 2% 预警三件事，msg 228，21:33）。待明日额度刷新后回访真发实测结果。**21:36 skuio 产品口径锐化（msg 230）**：「用户端点击开启全自动的时候就应该（同时开真发/当场询问），不该藏第二道开关让人猜」——与本行提级方向完全一致，采纳为 1.0.47 首要改造的验收口径（已群内确认采纳，msg 232）。**值守自纠**：曾两次把草稿按钮误称「通过」（内部管理页叫法），客户版实为草稿条→「面板」→「✓ 发送」，已群内更正致歉。**08-22 06:38-06:48 追加实锤（skuio 总闸复关后重开找不到入口）**：①客户包（nav flavor 裁剪后）**矩阵总览无「高级管理区/能力看板」露出**（完整模式滚到底=跨平台待审区即止，_260/_262 图证）——真发开关在客户包**唯一** UI 写入口=向导第 4 步应用；②自动回复设置页「上游总闸」卡的「去矩阵总览的能力看板调整」链接在客户包=**断头链接**（引到不存在区域，用户绕圈根源）。修复清单追加：断头链接按 flavor 隐藏或改指向导；真发开关必须给客户包一个直达写入口（并入①的流程重做）。已群内定论致歉+指回向导唯一路（msg 263，06:52） |
| **B38** | skuio | 首次安装后加载太慢（「正在载入工作台…」转圈久），关闭重启就快，建议优化——影响客户首次使用体验（20:23，截图 _209；装 1.0.46 实测时报） | 首启一次性成本叠加：PyInstaller 后端首次解包/杀软对新二进制全量扫描/字节码编译/数据目录与 DB 初始化/托管令牌首次联网接入——二次启动均有缓存故快。**AI 全静默正确接住（第三次），值守人工答复** | 📋 已登记待开发（1.0.47 批）：①测首启各段耗时（后端 sidecar 冷启动为主嫌疑）能压则压；②压不掉的给「首次启动初始化中，仅此一次」进度提示替代干转圈。已群内答复并催验 1.0.46 语音/图片发送+翻译工具三项（msg 210，20:26） |
| **B39** | skuio | 「跟进 SOP（工作链）管理」页顶部红横幅「页面脚本出错，部分功能可能失效（_TYPE_LABELS）」，工作链配置列表永卡「加载中…」，功能整体不可用（22:48，截图 _231，1.0.46 客户包实测） | **前端脚本悬空引用**：`src/web/templates/workflows.html` 第 405/450/659 行引用 `_TYPE_LABELS`，但该变量定义已不存在于文件中（疑近期 i18n/重构批次删除或改名未同步三处引用）——页面初始化即 ReferenceError，被脚本错误横幅接住；另横幅把内部变量名直接亮给用户（人话报错铁律又一例） | 📋 已登记待修（1.0.47，**优先级高**：客户包整页功能死）：补回/重建 `_TYPE_LABELS` 定义（或改用 i18n 词条表），三处引用同源化；建议补「模板 JS 未定义引用」门禁防复发；横幅文案人话化并入 B24-② 批。已群内答复登记并说明非用户操作问题（msg 232，23:47） |
| **B40（老板口谕 2026-08-22 00:46，值守线执行）** | 老板 | 内测期 7 天（至 **2026-08-29**）所有账号（含已装/新装）AI 额度放开为不限量；同时记录每账号 token 用量，对照官网报价折算每条消息/语音/图片消耗，产出价值判定与商业化接受度分析 | 额度闸=网站网关 `lib/ai-gateway.ts`：单机日预算 env `AI_GATEWAY_DAILY_CHARS`（默认 5 万字符）+ 全局日熔断 `AI_GATEWAY_GLOBAL_DAILY_CHARS`（200 万）+ 按主体覆写表（覆写优先且绕全局熔断） | ✅ **已执行生效**（01:00-01:15）：VPS `.env.local` 单机 5,000,000/全局 200,000,000（备份 `.env.local.bak-20260822-005533`），pm2 重启健康，`gw-budget` 实证 default_budget=5000000；清除低于新默认的 `IID:zhiliao_pilot` 25 万覆写（boss-grant 1000 万保留）。**计量**：网关 JSONL 流水（`/home/ubuntu/hualing-leads/ai-gateway.jsonl`，字段 t/ev/mid/in/out/ms）+ 117 每日 23:55 计划任务 `GwUsagePull` 归档至 `D:\chengjie-instances\.ops\gw_usage_archive`（防 10MB 轮转丢数），分析器 `C:\Users\Administrator\gw_usage_report.py`。**首照关键数**（24 天存量，口径 1tok≈1.7字/1U=1500tok）：chat 每次均 2,598 字≈1,528tok≈**1.02U/条**；峰值日全网 1,547U 等值（08-20）、单设备单日 2,073U 等值（07-31 E4DC）；tts/asr/识图用量尚少。**⚠️ 到期动作（08-29）**：还原 env 备份或删两行后 `pm2 restart yuntech`；出 7 天完整商业化分析报告。双群已发放开通知（msg 233 / 官方群 msg 46，01:17） |
| **B41** | skuio | 全自动真发打通后（B37 指路当晚），给真实客户 Nicks 的三连回复**整组原样重复发送两遍**（03:09，截图 _234：Ah 3-2…/He's closing…/I'd say that justifies… ×2） | 同一份草稿被投递链消费两次。初判=21:29 `inbox.l2_autosend.deliver` 热切换瞬间投递环节双跑（worker 双实例/队列重复消费竞态）；候选根因：①deliver 热生效时旧 worker 循环未停即起新循环 ②排队旧草稿与新投递叠发（但截图两组内容全同→更像同稿双投）③防重闸（ops「撞防重闸门竞态处置」口径）未覆盖此切换路径 | 📋 **已登记高优先（1.0.47）**：查 `autosend_worker` 去重闸 + deliver 热切换生命周期；加「同一草稿只许成功投递一次」硬保险（幂等键=draft_id，投递前 CAS 置 sent）。已群内答复初判+致歉+求证「后续新消息是否持续重复」（持续则临时切回拟稿人审），msg 235，03:14。**03:12 用户曾确证「之后没有重复」，03:24 复发→定性推翻，升级在办最高优先**。两种形态并存：①同稿双投（Nicks 案，字节级相同×2=投递层）②同问双答（_241 案，「financial analyst」两种措辞=生成层双触发；**网关流水铁证**：19:26:53.440 与 19:26:55.713 两次同规模完整生成（in 5544/5470，间隔 2.3s），各带一次 ~340 字小调用前导）。**已远程发起诊断取包**（skuio 机 DC8F-0935-5EE4-F3D9=@shiqn1817 台账、钧机 B990-0C62-FC1C-F668 顺带，request dr-mt3ch24d/dr-mt3ch24q，客户端 1 小时内自动上传）——拿 app.log 定位双触发链（候选：A线直发+L2 worker 并行 / worker 对同稿二次消费 / SOP 工作链与常规回复叠发——_241 截图右栏该会话挂着进行中的「新客破冰」链）。已群内答复两形态区分+远程取包进行中+临时规避=切拟稿人审（msg 242，03:32）。**03:37 三度复发（持续态）→首要嫌疑锁定 SOP 工作链叠发**：该会话挂着进行中的「新客破冰·7日跟进」链（_241 右栏实拍），workflows 页自述「自动推进档的话术步会自动拟稿并交给自动发送管线」——与常规自动回复在同一会话双通道出稿，正合「意思同、措辞异」形态。已群内发止血二选一（暂停该链 / 切拟稿人审）+ 请其观察暂停后是否止重（A/B 实证），msg 244，03:40。**117 修复口径**：跟进链话术步与常规自动回复在同一会话互斥（链活跃期抑制常规链，或合并进同一出稿队列去语义重）；同时保留投递层幂等钉（Nicks 同稿双投仍未解释，等日志包）。**04:06 远程诊断包到账（码 3DTSV9，功能首次实战闭环：登记→客户端自动打包上传→TG 通知，全程零用户操作）**。包内配置实证：`config.local.yaml` l2_autosend.enabled+deliver=true **且 companion_send_gate.enabled=true**——向导「全自动档」一键开了三类发送通道（常规回复/主动关怀/跟进链），叠发根因面坐实；另 fatal 日志显示 **03:04:36 应用重启**，5 分钟后即报 Nicks 同稿双投→重启后投递队列重放嫌疑。**修复口径定稿：同一会话同一时间仅允许一条通道出稿（通道互斥仲裁）+ 投递幂等钉 + 重启队列恢复去重**。**附带改进项（117）**：诊断包缺 app.log（打包器只收小文件，11KB）——`diag_upload.py` 加收 app.log 尾部 2MB，随下一包。钧机 B990 请求仍 pending（应用未开）。已群内同步进展（msg 247，04:27） |
| **B42** | skuio | AI 回复把**自己人设的名字**当成客户称呼：对客户 Nicks Villa 说「Ha, 'old reflexes' — you're not that old, **Steven**.」——steven 是该账号绑定的人设名（03:12，截图 _236） | 生成层身份混淆：提示词里「你是 steven」的名字被模型错用作对方称呼；客户显示名（Nicks）未被足够强调/锚定 | 📋 已登记（1.0.47）双道修：①prompt 层写死「对方称呼只能来自客户资料，绝不用自己名字称呼对方」（persona 注入处加 peer_name 锚）②出稿守卫扫「用自己人设名呼叫对方」的称呼样式，命中拦下重写（与 world-clock/fabrication 守卫同族）。已群内答复初判并求证出现频率（msg 238，03:17）。**03:16 用户确证「第一次发现」**→按偶发定级（修复照做，双道防线不减），请其后续见同类即报以攒频率数据（msg 240，03:19 收尾） |
| **B43** | skuio | 人设语音页克隆音色**登记成功但未实际生效**（05:04，截图 _248）：上传 steven 音频.ogg 登记显示「登记成功」，但同卡「实际使用」行=「后端 edge_tts · 音色 zh-CN-XiaoxiaoNeural · 由 avatar_clone 回落」——发声仍是通用音色 | 登记流程未联动克隆语音引擎开关：其机诊断包（3DTSV9）`config.local.yaml` 实证 `avatar_voice.enabled: false`（带 `_hosted_auto: true` 标记）——**10:32 回炉二次（1.0.5x，_305）**：入口统一后人设页登记也先查开关被拦「语音引擎未接入：请联系官方开通」——存量机 enabled:false 未被自动激活 → 登记被拦 → 「登记成功即联动开启」永远走不到＝**死锁闭环**；与 B55（hosted-vision 全天零调用）同族，**值守定性：托管能力（voice/vision/asr）在存量升级安装上的自动激活链专项**（fresh 装有效、升级装失效嫌疑），报错文案「联系官方开通」对已开通托管为假话一并修——cloud_light 档 hosted 自动配置把引擎关着，登记成功后合成时avatar_clone 不可用→静默回落 edge_tts。**登记成功≠生效**，且回落只在小字行可见 | 📋 已登记（1.0.47）：①登记成功即联动开启克隆引擎（hosted 形态ensure_hosted_voice 已接入，登记 hook 补 avatar_voice.enabled 翻转）②回落时显式提示「克隆音色暂不可用，正用通用音色」而非小字 ③并入 B25-① 改版一起验收。已群内答复（登记入口位置+回落 bug 定性+语言选项提醒，msg 249，05:09）。**注**：其登记语言误选日语（素材疑中/英文），已提醒。**08-22 22:53 钧变体（_278）**：工具箱→登记克隆音色直接被拦「语音引擎未接入：请联系官方开通托管语音…」——其诊断包（83V85P）实证与 skuio 同款 `avatar_voice.enabled: false, _hosted_auto: true`。**定性升级：克隆语音功能在 1.0.46 客户包整体不可用**（两位内测 100% 复现），且两个登记入口行为不一致（工具箱先查开关→拦；人设页不查→假成功+回落）。修复口径追加：两入口行为统一；报错话术重写（托管明明已开通，「联系官方开通」误导，B24-② 族）。已群内答复并案（msg 279，22:57） |
| **B44（值守自察 08-22 05:59-06:50）** | 值守发现（钧的客户端） | 钧账号连发多条「秒回+逐句复述值守话术」消息（05:59/06:40/06:43 语音/06:43/06:50，均在值守答复后数秒内出现，支持方口吻「已记进修复清单啦」「我帮你指」）——**其客户端 AI 把报障群当客户会话自动代答** | 钧昨日开全自动档+「新会话自动沿用此档位」（_224 同款设置），报障群在其收件箱里也是会话→被全自动接管；AI 借群上下文模仿支持口吻，第三方难辨真伪 | 📋 已登记（1.0.47，并入 B37 全自动流程重做）：**群聊会话默认排除自动回复**（需显式开启），至少「新会话沿用」不得覆盖群聊；已群内提醒钧把本群会话切手动（留「若是本人手打请说明」余地），msg 266，06:56。**06:53 警告后其 AI 5 秒又自动接话**（无视警告内容继续指路）——本人疑已入睡，AI 将对群内每条消息接话。**值守决策**：停止回应钧账号的 AI 样式消息（防「值守答→AI 答」互答循环刷屏），只回 skuio 真实提问，等钧本人醒后处理；刻意**不**动其网关额度（会误伤其全部 AI 功能，违背 B40 无限额度指令）。1.0.47 群聊默认排除自动回复的优先级据此再升 |
| **B45** | 钧 JUN | 会话列表未读角标已读后复活：点开看过的会话，过一会儿又跳出「1 条新消息」角标；截图可见多个会话挂着带陈旧时间的角标（08-22 21:28，截图 _269） | 已读状态未持久生效，被后台刷新/同步顶回（lastread 与新消息计数口径漂移嫌疑） | 📋 已登记（1.0.47）：已读状态持久化+同步不回退；验收=点开→退出→数分钟不复活。已群内确认登记（msg 270，21:33）。**附带**：其顶栏额度显示 996k=B40 无限额度端上生效实证 |
| **B46** | 钧 JUN | 客户发**酒店照片**（Amphitrite Resort），AI 回复的却是几小时前那张**粉丝面**（「这碗面看着好香呀，肉丸和粉丝我都馋了」）（08-22 21:34，截图 _271） | **识图无错、生成抢跑**：截图内「AI 识图」面板已正确描述度假村入口——回复生成未消费当前图的识别结果，接了历史里上一张图的话题（生成先于识图完成的竞态/当前消息 enrich 未注入生成链） | 📋 已登记（1.0.47）：客户发图后回复必须消费当前图识别结果；识别未完成则等待，超时则不引用图片话题。验收=隔数小时发两张不同图，回复各对各图。已群内答复定位（msg 272，21:38） |
| **B47** | 钧 JUN | 人设工作室「新建人设」：点「确认丢弃未保存的修改」后再要编辑，**编辑器无响应锁死**（08-22 22:39，截图 _273：丢弃确认弹窗与「发现未保存草稿 恢复/忽略」提示条同屏叠加） | 丢弃后编辑器状态未复位；草稿恢复条与丢弃弹窗两套未保存状态机打架嫌疑最大 | 📋 已登记（1.0.47）：丢弃后表单复位可编辑；草稿恢复条与丢弃确认合并为一套状态。验收=改字段→丢弃→立即可再编辑。已群内答复+临时绕行=切页再回/重开窗口（msg 275，22:42）。**22:42 追报数据丢失面**：放弃后再进入「就没有记录了」——丢弃即永久失内容；修复口径升级：丢弃前自动留可找回副本（草稿找回与丢弃确认合一）。已如实答复找回条件（有「发现未保存草稿」条→点恢复；无则丢失）并致歉（msg 277，22:47）。**其机器码/诊断包 QWEH6X 已收**（22:40 手动上传成功=B15 一键上传链路用户侧验证通过）。**另**：钧机 B990 远程取包实际 04:19 已兑现（码 83V85P）；**117 执行线已开工实证**——05:30 其自发对 B990 再发远程取包（码 SZHD2X，note=auto-reply not sending investigation），实施53 指令在执行中 |
| **B48** | 钧 JUN | 「同时跳出消息提醒和草稿箱，看怎么只有一个收件箱」——同一事件双重打扰：客户消息弹「新消息提醒」、AI 拟稿完成又弹「草稿待审」（08-22 23:04） | 通知面分散：消息提醒/草稿待审/发前确认/急需处理多个入口指向同一待办（其顶栏同屏挂 急需处理1+发前确认7+会话角标） | 📋 已登记（产品建议，1.0.47 批评估）：一个事件只提醒一次，消息与对应草稿合并进同一待办入口一步处理。已群内确认理解并留纠偏口（msg 281，23:07）。**0825 02:43 钧口径具体化（原话）**：铃铛消息中心「不用显示客户聊天记录，只需要系统的消息，或者需要人工处理的消息」——通知归拢的目标形态定义，交产品定稿 |
| **B49** | skuio | 主动关怀方案点「看 AI 会说什么」→ 红字「预览失败：AbortError: signal is aborted without reason」（08-23 00:05，截图 _282：关怀对象钧 JUN、计划 08:03 发早安） | 预览生成耗时超过前端等待上限 → AbortController 放弃；且 AbortError 内部代号直接亮给用户（黑话报错 B24-② 族） | 📋 已登记（1.0.47）：①预览等待加长/改异步「生成中…」不一刀切放弃 ②报错人话「生成超时，请重试」。已群内答复定位+临时办法=重点一次（msg 283，00:11） |
| **B50** | 钧 JUN | 之前聊天已获取客户年龄，AI 后续对话没有记忆、反过来再问对方年龄（08-23 00:27，截图 _284：AI 答完自己 36 又问「你呢，看着挺年轻的？」） | 捕获的客户档案属性未进画像/未被生成消费（复问已知信息） | 📋 已登记（1.0.47）：档案属性（年龄等）捕获即入画像，生成注入并禁止复问已知项。已群内确认（msg 286，00:33）。**08:21 复报加强（截图 _299）**：真实客户「小界」已三次自报 38 岁，AI 仍问「你大概哪个年龄段」（proactive/续聊场景，人设张浩然）——复问已知项实锤；该图定为验收用例；**值守建议升 P1**（真实客户对话直接受伤，两位测试者 8 小时内两报）。已群内答复（08:2x） |
| **B51** | skuio | 全自动回复**一字不差复读对方刚说的话**（08-23 00:28，截图 _285：对方（钧的 AI）发「哈哈被你猜中了，我36了…你呢，看着挺年轻的？」→ skuio 的 AI 原样回发同句） | 生成链把「对方原话」当成了「要发的回复」（具体环节待查该请求生成记录；两位测试者 AI 互聊场景暴露） | 📋 已登记（1.0.47）：①调该条生成记录定位环节 ②加出稿检查「回复不得与对方上一条雷同」（与既有守卫同族）。已群内确认（msg 286，00:33） |
| **B52** | 钧 JUN | 人设自述状态前后矛盾：先说「你睡你的，回头见」（要睡了），几十分钟后主动消息又说「刚练完，正准备冲个澡」（08-23 00:31，截图 _287，proactive_opener 链出稿） | AI 无「自述状态记忆」：主动触达/后续生成不检查人设近期自我声明，跨消息自相矛盾（world-clock 守卫只管时段问候，不管自述行为连续性） | 📋 已登记（1.0.47，与 B50 同族治理）：人设近期自述状态入短期记忆，后续消息（尤其主动搭话）须衔接或合理过渡，矛盾拦下重写。已群内确认（msg 288，00:36） |
| **B53** | skuio | 「求助与支持」一键上传持续报「连不上官网服务」（00:47 首报、02:44 复报仍失败，工单 #17 P0 告警） | **路径级故障非网络**：网关日志实证同机 AI 聊天通道 02:46 仍 200 正常，且其昨晨 05:57 曾上传成功（PJ6ZVM，app 1.047）——仅 support 上传链路断，持续 2h+。候选：客户端support 面板→本地后端转发→官网这段的转发路由/域名/报错映射（本地转发失败也被话术算成「连不上官网」） | 📋 已登记（高优先）：117 查 support 上传链路并修；报错区分「本地服务未就绪/官网不可达/上传被拒」三类人话。**值守自纠**：01:48 曾误判为用户网络瞬断，02:48 更正为客户端路径 bug（msg 292）。**02:51 117 值守线接管并给出最终口径（msg 293）**：官网入口日志其两次上传**零记录**=请求未走出用户线路——其机 AI 流量（代理线路）可达而上传（直连线路）不通，两证据兼容，173 值守「客户端 bug」推断证据不足已被覆盖；已给用户浏览器测试法+「复制全部信息贴群」替代路径。**双值守撞车实录**：02:48/02:51 两条口径矛盾的答复出自同一支持号（用户视角=客服自我更正，尚可接受）——**173 值守线就此停止群内作业**，交接给 117 值守线/新对话。**附带**：117 宣布 1.0.48 已发布（含 B23/B43/B41 修复），skuio 求助框里还报了「人设填写输入框偶发无光标无法输入」（117 已登记，B54）。**03:12 统一口径收官（msg 294）**：非用户网络、非官网——诊断上传与 AI 网关同机同 urllib 同代理语义（hosted_gateway._http / diag_upload 同库），网关小体量 POST 全程 200，而诊断 ZIP 在官网入口**零到达**（无 4xx/无 499）＝egress 对大/长 POST 掐断嫌疑最大；已告知用户免动作（撤回 02:52「换网络/浏览器测试」建议），「复制全部信息」为替代路径。**修复方向补遗**：①失败自动重试 ②报错三分（urllib HTTPError 现也被折叠成 unreachable，413/断网不可分辨） ③失败降级 mini 包重传（meta+fatal ~10KB，历史实证该体量可过其线路，兼野外自证大小假说） ④远程诊断上传腿复用同函数（hosted_gateway.py:993 import build_and_upload）＝同故障须一并修，修好前给 DC8F 建 diag-request 无意义 ⑤真实异常栈已落用户机 app.log（「诊断包转投官网失败」exc_info），上传修复后即可回收定案 |
| **B54** | skuio | 人设填写时输入框**偶发**没有输入光标、无法输入（写在求助面板备注框、随 _291 截图带出——因一键上传失败未走通诊断通道，靠值守读原图捕获） | 待排查：间歇性焦点丢失/不可输入，嫌疑与 B47（人设编辑器两套未保存状态机打架）、B19（Shadow DOM 焦点重定向穿透）同族；缺复现路径（哪个字段/何时触发/是否丢弃后） | 📋 **0826 05:23 并案升级（_526）**：钧成第二报告人，范围扩至主动关怀页（v1.0.55），描述「软件经常性输入框不能输入，退出重进有可能恢复」——两人两页+重启自愈签名，升 P1 不再等完整复现。**05:29 _530 复现路径到手**：主动关怀页「跳过关怀记录」弹窗点「取消」→输入框点不进（弹窗焦点陷阱未拆/遮罩残留实锤）。已教临时救法（Tab/点空白再点回）。见实施64 P1-16（已附复现步骤） | P1 在办（可复现） |
| **B55** | 钧 JUN | 收到人像照，AI 回「哈哈，这图是你家猫吗？」；被怼「是你妈妈」后改口称「阿姨」再被怼「你36岁叫人家阿姨」（03:17 报，截图 _295，1.048 实测，两测试号互聊场景；该报障被限频压制、截图归档还原） | **回复链未经识图就接图**：网关流水 08-22 全天事件分布 chat 1965/asr 7/tts 7/mint 4、**vision 0**（库中史上仅 08-02 一条测试指纹 vision_fail）——1.0.48 随包 hosted-vision _hosted_auto 在其存量升级机上未被调用；媒体无描述时生成链未走 media_degrade 诚实问询而是幻觉接话。称呼失当为第二面（无年龄信息称「阿姨」） | 📋 已登记（03:3x 群内答复）：①hosted-vision 客户包装配核查+存量 A 类补齐 ②无识图结果硬闸：拿不到描述不许对图内容下断言，诚实降级/不接图话题（与 B46 并案验收） ③称呼保守化（并 B42 呼格守卫族）。验收=发人像照：正确描述或诚实说看不清，绝不指鹿为马 |
| **B56** | skuio | 出站消息译文镜像地名偷换：AI 发给客户的英文原文「…see you in Cebu…」正确，工作台中文译文却显示「我很想在马尼拉见到你」（07:00 报，截图 _297，客户 Crisceya 会话 06:58 消息） | 翻译层专有名词幻觉：显示层 EN→ZH 镜像翻译把 Cebu（宿务）换成马尼拉——LLM 对地名做「上下文合理化」而非忠实转换；客户侧未受影响（原文正确），但操作员靠译文监督 AI 出稿，地名失真=监督链失真 | 📋 已登记（07:1x 群内答复）：①翻译提示词加专有名词保真铁律（地名/人名/数字不替换，无把握保留原词）②译后校验：源文地名/数字在译文缺失或被换→重翻或括注原词（与 fabrication 守卫同族）。验收=原句重译宿务不变马尼拉+地名数字保真抽查回归 |
| **B57** | skuio | APP 更新版本后 Telegram 已登录账号全体掉线需重新登录，WhatsApp 不受影响（09:19 报，文字）；官方群曾承诺「升级安装数据不丢」被打脸 | 待排查（高优先）：TG 走协议级会话文件、WA 走网页长会话；头号嫌疑=升级过程旧后端进程未完全退出，新旧双进程抢同一份 pyrogram 会话触发 Telegram 侧 AuthKeyDuplicated 强制注销（该错误族同时是 B59 误冻的输入）；已向用户要升级前后版本号 | 📋 已登记（10:2x 群内答复 msg 304）；复现路径=装老版登录 TG→原地升级→观察会话存活与 app.log 异常族 |
| **B58** | skuio | 联系人/会话标签只能新增，无法修改或删除（09:21 报，截图 _302 圈 aisida 标签） | 标签管理 UI 缺口：无改名/删除/取消关联入口 | 📋 已登记（10:2x 群内答复）；补标签改名/删除/取消关联，排后续批次 |
| **B59** | skuio | 发不出消息，顶部横幅「紧急停发开关已开启（运营手动冻结外发），解除后自动恢复」（09:50 报，截图 _303，Crisceya 会话）——用户被完全阻断外发且无处可解 | **风控急刹误触发嫌疑**（ban_signal.py：pause=PeerFlood 族 60min TTL 自愈 / ban=Unauthorized/AuthKeyUnregistered/SessionRevoked/SessionExpired/AuthKeyDuplicated 族**永久**冻结+注册表 meta.banned）——B57 的会话失效错误正落在 ban 族＝「升级掉线」被误判「被封号」；07-27 有对端注销误冻前科（该次修了 peer 侧，本次是自会话侧新变体）。三个叠加缺陷：①误判永久冻结不自愈 ②客户包无解除入口（解除卡只在 rpa_overview 运维页，客户 flavor 隐藏；横幅只给「复制说明给管理员」）③文案称「运营手动冻结」与 auto_ban 实情不符 | 📋 已登记（10:2x 群内答复，**值守建议 P0**：用户外发全断）；修复口径：①登录成功自动解除该账号 auth 族 auto_ban+meta.banned ②分类器对「登录变更窗口内」的 auth 族错误降档处理 ③客户包给管理员解除入口或横幅带一键解除（鉴权后）④文案按 actor 区分自动/手动。已请用户试发分辨 pause/ban 档，等回报 |
| **B60** | skuio | 左侧会话列表下拉无法拉到底，底部「已归档」入口行被截断看不到、归档组打不开（10:40 报，截图 _307，单账号筛选视图） | 列表滚动容器高度/底部留白计算缺口：滚动 max 高度没把「已归档」footer 行（实施49 P0-1 _renderArchivedFooter）与底栏算进去，被「群组动态」底栏遮挡截断 | 📋 已登记（10:4x 群内答复）：滚动区高度修正，验收=多会话+账号筛选下滚到底可见可点「已归档」；**绕行已给**（源码验证）：顶部「筛选」菜单→「归档」分组（data-f=archived，与底部入口同目的地） |
| **B61** | 钧 JUN | 工具箱「生成语音」预览音频播放没有声音（10:50 报，截图 _309：0:03/0:03 播放条正常、无声；人设张浩然 19 字文本） | **服务端证据链**：网关流水 02:47:55Z clone 12字/6.1s 200、02:49:06Z clone 19字/10.0s 200，真实音频字节回传（~184KB/3s）→ 生成链成功、内容疑静音；hub 主机 176 当前 pressure=high（free 2GB），有「健康 200 但产物异常」前科（voice-truth 专项昨夜正为此加引擎指纹）；**盲区**：指纹/校验只覆盖 wav/paced 路径，托管 clone 的 ogg 路径无内容校验，且 hosted 客户路径不在 voice_outage 台账内 | 📋 已登记（10:5x 群内答复：重试观察+回报）：①clone/ogg 路径加静音检测（能量阈值），哑音自动拦下重试 ②hosted 客户 TTS 纳入 voice_outage 观测 ③查 176 高压态 index_tts 是否出静音（能压则压显存）。**10:56 复测仍无声（_311）＝持续性退化**，已升级：.ops 协作单 ASK_from_duty_20260823_hub_silent_audio.md 交语音线（含全部服务端证据与盲区提醒），等回写后@钧验证。**0825 02:13 skuio 用户侧复证（_377）**：「克隆音色还是默认语音，延续几个版本」——其登记实际已成功（_331 后端=avatar_clone 实证），听感=默认声是合成失败回落兜底；hub 恢复后 skuio+钧 双人克隆链验收 |
| **B62** | skuio | 工具箱「生成语音」失败，红字「生成失败: ValueError: Invalid voice 'Steven'.」（10:57 报，截图 _312；音色行自述「将使用: steven · edge_tts · 来源: 手动指定」）；同条重提克隆入口收纳（=B25-①，归档作证据） | **回落链透传克隆名**：其机托管语音未激活（P1-15）→ steven 克隆不可用回落 edge_tts → 把克隆名「steven」原样当 edge 音色名传入 → ValueError 裸抛 UI（B24-② 黑话族）。人设页回落已人话化（B43 批），工具箱生成路径漏网 | 📋 已登记（11:0x 群内答复）：①回落自动映射合法通用音色+明示「克隆暂不可用，本条用通用音色」②该 ValueError 人话化；并入 P1-15 一体修（激活修好后此路径不再触发）。B25-① 证据归档 |
| **B63** | skuio | Messenger 无法发送消息，气泡旁「✗ 发送失败 重发」，底部状态栏「发送失败: 消息未送达: messenger send failed:」**冒号后为空**（12:09 报，截图 _314，账号 Rain Dy/美月·伊莎贝拉 → 客户 Pacasum Odal） | 待定位：客户包 messenger 走本机 RPA 网页自动化，服务端无该链路日志；其收件正常（对话在收）=页面会话活着，疑发送环节元素/接口失败；**报错空原因**=RPA 失败原因未透传到 UI（可诊断性缺口，B24-② 族）；注意其诊断上传通道仍坏（B53）取证受限 | 📋 已登记（12:1x 群内答复）：已要两个现场信息（重发是否持续失败/账号在线态）；产品侧先修「失败原因透传」。**12:33 补充（_320/_321）**：同账号跨会话（Christopher 等）持续失败=会话过期嫌疑上升，已指引到「账号与平台管理」自查登录态（若需重登注意 B64 验证窗坑）；**新 facet：发送失败的消息在会话里零留痕**（自动发送失败不渲染失败气泡，用户内容直接消失）——修复口径追加：失败必须留痕（失败标记+一键重发）。**14:47 定性（_322/_323 用户自证）**：全自动记录报「平台投递失败：messenger send failed: Server error 500」且当日自动发 7/7 全失败、收件正常——500 来自本机 messenger-web Playwright 边车（127.0.0.1:8791），已知成因族=composer 渲染超时/**「需要接受/PIN 浮层」挡 composer**/会话登出（messenger_web_login.py:80 注释 08-15 实锤）；**http_error_detail() 提取真因的工具 08-15 已建但发送路径未接**=报错空原因的直接根子。修复收敛：①/send 调用点接 http_error_detail 透传 error/reason_code ②边车浮层/登出态映射到账号卡状态提示（而非逐条 500）③失败留痕+一键重发。已指引用户走账号卡重登/补 PIN。**18:4x 根因终局（EC4UPG msg-sidecar）**：08-23 批量失败全为 reason=needs_accept（新私信请求未接受，composerCount=0）；补子项=界面内「接受请求」入口/可选自动接受。定案转装包验收 |
| **B65** | skuio | 「接入 LINE」弹窗（扫码流程）里邮箱/手机号输入框占位文案写着「登录 Facebook 的邮箱或手机号」——LINE 登录界面出现 Facebook 错误入口（15:11 报，截图 _325） | **共享组件文案未按平台分化**：表单中继（Form-Relay）组件占位键 `inbox.connect.relay_email_ph`（inbox_workspace.py:989）按 Messenger 场景写死 Facebook（对 Messenger 正确=FB 账密登录），LINE/其他平台弹窗原样复用=错误引导（用户可能把 FB 账密填进 LINE 表单） | 📋 已登记（15:1x 群内答复）：占位按平台参数化/分键（zh+en 同修）；LINE 弹窗是否该露账密表单（扫码为主）交产品确认；IG/Zalo/WA 同款弹窗一并核查防同类错 |
| **B66** | skuio | 称呼互换错乱：客户 Crisceya 叫其人设「baba」，AI 回复「And that means the world to me, baba.」——把对方对自己的爱称反用来称呼对方（08-24 00:10 报，截图 _327；其 AI 应叫对方 babe） | **B42 族新变体**：B42 修复（1.0.48）只锚「不用自己人设名称呼对方」+守卫只扫人设名呼格；「对方→我」的爱称/角色称（baba/daddy/爸爸）不在守卫面内→生成链把入站高频呼格学舌反用 | 📋 已登记（00:2x 群内答复）：①称呼对方唯一来源=客户档案「偏好称呼」字段（本例档案已有）②入站「对方称呼我」词集进呼格禁用表，persona_guard 呼格守卫加第四臂（与 strip_vocative_self_name 同宿主）命中拦下重写 ③【0826 16:42 样本3（_563）：客户称 ma'ma、AI 反用 ma'ma 称对方（v1.05x，美月·伊莎贝拉会话）——入呼格禁用金标】验收=_327 场景她叫 baba、AI 只能回 babe/偏好称呼。已追加进实施64 批次（P1-7） |
| **B67** | skuio | 客户（Yhang）发英文、会话开着「发→英」，AI 却把 19:49 的**三条中文**原样发给客户——出站语言未跟随（08-24 00:12 报，截图 _328） | 三条均为主动跟进内容（下月行程/约见面）→ **proactive/跟进链疑绕过出站翻译单闸**（普通回复链走 outbound_translate，此链旁路嫌疑；B22 同构：链路少走一站）。**08-24 07:56 案2（_337，v1.054）范围修正：即时应答也犯**（客户 nice to meet you too→连回三条中文）——旁路不限于 proactive 链，守卫必须全出站路径。**0825 02:19 案3（_382）**：用户直问「设定是否生效」——值守按实施67 读码结论如实答复：「发→英」现仅手动发送生效、自动链不可见（意图存储位置错误），修复施工中；三案场景为上线复验清单。**0825 02:57 案4（_396，黄金线索）**：触发条件收敛为**客户发图片/表情等非文本消息**时——语言判定失去文字依据回落中文（确定性复现规律）；「非文本入站后回复」列为专门验收场景 | 📋 已登记（00:2x 群内答复，追加实施64 P1-8）：①全部出站路径统一过 outbound_translate 无旁路 ②出站语言核对守卫（与客户语言/发→英 配置不符→翻译或拦） ③调 19:49 三条生成投递记录定位旁路链。验收=_328 场景主动消息也必须出英文。**🔧 0824 午间落码（实施67）**：根因=「发→英」只存前端 localStorage 服务端自动链不可见；修=服务端会话级持久化（conv-xlate-out）+ translate_outbound_text 显式目标最优先（gate_only 升级完整翻译），B 线/proactive 自动获益；随 22:30 窗装载 |
| **B68（值守自察）** | 值守线 | 报障群消息被 care_capture 误捕为「关怀约定」（00:12 日志实锤：contact=报障群，把报障文本当约定，到期日还解析成离谱远期）——若 proactive 关怀打进报障群=AI 全静默破防 | 群聊/报障 contact 未被排除在 care/画像捕获之外；巡查发现共 2 条（#7=今日 B67 报障文本捕成「旅行」、#6=08-22 B41 原始报障文本捕成「比赛」） | ✅ **现场已处置**（00:2x）：两条计划已 cancel（/api/care/schedule/{6,7}/cancel ok）；📋 根治登记（实施64 P2-i）：care_capture/画像捕获排除群聊与 bug_intake 会话。**🔧 0824 午间落码（实施67）**：正则链群聊+bug_intake 排除、LLM 影子链群聊排除、远期到期日 sanity（>370 天拦）单点两链共享；随 22:30 窗装载 |
| **B69** | skuio | 平台自动更新安装完毕后，顶部菜单栏（文件/编辑/视图/窗口/帮助）消失（08-24 07:42 报，截图 _336 红框；v1.054 实测） | 过渡期缺陷嫌疑：1.0.49+ 壳侧菜单并入页内改造（appmenu-inpage/titlebar-merge）与应用内更新装载路径叠加——更新装完旧壳/新页混装态菜单两头落空；重启应用可恢复（待确认） | 📋 已登记（10:5x 群内答复+临时办法=重启一次）：更新装载后菜单可用性验收进发版检查单；实施64 P2-j。**🔧 0824 午间落码（实施67）**：壳菜单认领看门狗（页内菜单 30s 未接管/加载失败→原生菜单条自动还回，两头落空变不可能）；随 1.0.55 包 |
| **B70** | skuio | Messenger E2EE 恢复 PIN 闭环断三处：①登录窗内我方「登录成功」浮层遮住 FB 的 PIN 输入页（08:45，_341，浮层背后即 PIN 六格）②应用内「输入恢复 PIN」弹窗输入无效/托管后无反应（09:41/10:13，_344/_345，toast「PIN 已托管…等下次出现时会自动输入」=死等，边车不被驱动重试）③弹窗文案「发送不受影响」与实况矛盾（其发送实际失败 ✗） | P1-4② 落码把 e2ee_pin 态亮到账号卡（1.052+ 已生效=「入站半死」横幅），但 PIN 中继只存不推：托管后无「立即驱动边车提交+进度反馈」闭环；成功浮层 z 序盖 checkpoint 页 | 📋 已登记（10:5x 群内答复+临时办法=点浮层右上 ✕ 后直接输 PIN）：①托管即触发边车重放并回报结果 ②成功浮层不得出现在 checkpoint/e2ee 页 ③文案与实况对齐；实施64 P1-9。**🔧 0824 午间落码（实施67）**：托管即重放（e2ee-pin 端点异步驱动边车钓浮层提交+状态回写）+ 成功浮层避让 PIN 页（auto-hide 前最后核对，未自愈不隐藏窗口）+ 文案照实改「加密会话发送也会失败」；随 1.0.55 包，验收=Roselyn 回访。**0825 03:38 持续实证（_413/_414）**：Roselyn 号 Tharwat/Ranie 两会话发送均持续失败（重发无效）=PIN 态未解整号瘫；值守已给用户验收路径（确认 1.0.55→重输 PIN→当场回报），结果即 B70 验收结论 |
| **B71** | skuio | 案例跟进页「检测到危机信号，请尽快人工关注」误报：客户正常聊接送孩子上学（Cause there going to school baba it's monday Im still in bed…）被标紧急/危机信号立案（08-24 08:06 前后，截图 _335） | wellbeing_guard 保守词表（自伤/轻生 severe + 深度绝望 elevated）某词在该英文口语上误命中——具体命中词待其机 app.log（[wellbeing] matched= 行）回收定案（已建 DC8F 远程诊断请求） | 📋 已登记（10:5x 群内答复，安全红线不关但误报要修）：回收日志→定位误命中词→收紧（句级排除/词边界），误报样本进金标；实施64 P2-k。**🔧 0824 午间先行收紧（实施67）**：【0826 误报样本2（_555）】skuio 复报：客户英文正面愿望 I wish next yr i can celebrate my own birthday party… 被标紧急+连续追问3（案例卡 2 天前生成，需核是否收紧装载前旧案）；样本进误报金标，英文口语词面再收窄；值守已教结案。另该卡内 AI 回复带 baba 呼格+I'm steven 自名（B66/B100 同框旁证）。读码锁定双义短语 can't go on/no reason to go on/hopeless（日常宾语/日期后续放行，危机形态照立案），_335 场景 6 例金标+危机面 47 绿；误命中词终案仍等 DC8F matched= 行；随 22:30 窗装载 |
| **B72** | skuio | 「回复额度守卫」设 0=不限后，「AI 自动发送已达让路线（160/160）」仍拦——用户认为设置无效（08-24 10:43 报，截图 _350：守卫页 0 已生效、Crisceya 今日 70 条照跑） | **两套护栏撞名**：其页面=会话级互刷保险丝（轮/会话）；让路线=账号级反封号闸门 companion_send_gate 动态 cap 的自动链份额（auto_cap，人工保留 reserve），不受会话级 0 影响；横幅未标来源→用户调 A 以为能影响 B | 📋 已登记（10:5x 群内答复讲清两套）：①让路线横幅标明来源+构成（点开见当日 cap/auto_cap/reserve 计算）②账号级份额是否开放调整并入 B34 额度自定义等拍板；_350 为验收用例；实施64 P2-l。**🔧 0824 午间落码（实施67）**：auto_yield 三键标【账号防封安全额度】来源+构成（AI 已发/上限/让路线算式）+显式与会话级守卫区分 zh+en（热更已生效）；份额可调仍并 B34 |
| **B73** | 钧 JUN | 「AI 暂时无法自动回复，部分消息已安全拦下」delivblock 横幅常驻，AI 停止自动回复（08-24 10:37 报，复制全部信息贴群：v1.054/B990） | 待定位：delivblock 通用横幅不带具体闸名；候选=翻译 HOLD/媒体块/发前确认积压/账号级闸门；已请其回传待发队列内的**拦截原因**截图+已建 B990 远程诊断 dr-mt6mxbne 拉 app.log | 📋 已登记（10:5x 群内答复+放行路径指引）；原因到手即定案；顺带产品改进：该横幅应带最高频拦截原因摘要（实施64 P1-10）。**🔧 0824 午间落码（实施67）**：seat_banner.top_reason（近窗最高频人话）→ 横幅主因行+「查看拦截明细」直达；随 22:30 窗装载；~~个案仍等 B990 包~~ **【0826 17:00 _566 定案：主因行=「**语音合成引擎异常**（近30分钟×1）」（P1-10 主因行实战首秀✅）＝B114 hub 故障连锁拦停带语音回复；已教临时关人设语音发送恢复自动回】【0826 16:23 复现于 **v1.056**（钧机 B990 复制全部信息原文，delivblock 横幅常驻）——已请其截「查看拦截明细」主因行（P1-10 主因行 1.0.55+ 在端上）；B86 队列空断裂未修前无放行入口，等主因定案】个案已由网关流水定案（值守 0825 01:0x，1.055 复现）**：入站语音 asr 200→回复生成 200→**克隆合成 tts_fail×3（~23.5s，17:00-17:03Z）**→按「宁可不回」拦队列＝横幅真身；**B73=B61 hub 故障下游级联**，hub 恢复随 B61 一并@钧复验关单。产品待决：故障期语音回复自动降级改发文字+提示 vs 拦下等待（受 08-17 no-fallback 决约束）；实况便签 .ops\NOTE_from_duty_20260825_hub_still_failing.md |
| **B74** | 钧 JUN | 客户问「我应该怎么称呼你」，AI（人设张浩然）回「叫我。朋友都这么喊我。」——**姓名位置为空**；报障描述为「人设池资料没被完全读取，甚至姓名都说不出」（08-24 11:08 报，截图 _352，v1.054） | 头号嫌疑：B42 呼格守卫第三臂 strip_vocative_self_name **自介误伤**——「叫我张浩然」句式形似呼格（call me X），守卫的自介排除盖「我是X/我叫X」但漏「叫我X」→自己名字被抹→「叫我。」；次要面：背景资料未生效或与其编辑器「有未保存的修改」（_342 实录）有关，已请其确认保存并给具体未生效字段 | 📋 已登记（11:1x 群内答复）：①守卫补自介例外（叫我X/我是X/我叫X/call me X/you can call me X），_352 进金标（B42 金标回归不回退）②等其回报后查背景注入链；实施64 P1-11。**🔧 0824 午间落码（实施67，根因修正）**：真凶不是呼格守卫（其两模式都要求逗号形态、「叫我X」不命中）而是 sanitize_self_name hard 档②+inline 抹名兜底（单句自介剥光→「叫我，」残句与 _352 逐字吻合）；修=自介句禁残剥（borrowed 借名档保留，David Lin 金标不回退）+ 生成时 resolved_name 直传守卫白名单（同源契约）；_352 金标 5 例；随 22:30 窗装载 |
| **B88** | 钧 JUN | 工作台会话列表里「内测BUG群」消失（0825 04:09，_433；其人正用手机在该群发言=群本身在） | 待定向（已给三步：搜群名/群组筛选 tab/归档分组）：候选=误归档、群组筛选/列表口径回归（0823 unread-trust 批动过聚合）、镜像会话行丢失 | 📋 已登记（04:1x 群内三步指引）；**04:11 定向（用户回报）**：未做任何操作、自动消失、**重启软件后恢复**＝运行态列表丢行 bug（实时更新/合并链误删行，重启重建列表恢复）；嫌疑区=0823 unread-trust 批 precise local-read merge guard/SSE 增量合并；**04:12 真相反转（_436）**：截图列表顶「群组」+「@我/未读」筛选片同挂（用户自述未点过）——「只剩 1 群」=未读筛选把无未读的 12 群藏了，非数据丢失；「自动消失/重启变化」与筛选片粘滞/自挂吻合。修复定向改为：①查「@我/未读」筛选无操作自启/跨启动粘滞 ②列表被筛选时显式提示当前筛选（防误判丢数据）③已教用户点掉筛选片自查（等回报确认 13 群复现）。**04:16 二层（_439，更重）**：其工作台自 04:07 起停收该群新消息（04:10+ 值守答复其仅手机可见）＝重启后群会话入站同步停摆（TG worker 群订阅/增量恢复失败嫌疑）；已请其再重启一次分流「实时断可补拉」vs「补拉也断」，等回报。**_439 截图三证**：①层一闭环——筛选片点掉后 13 群全回（消失=「@我/未读」筛选片藏行，坐实）；②层二实锤——会话内容冻在 04:07 花无缺消息，04:10+ 值守三条答复不在；③留档——其账号（张浩然）顶部挂「让路线 155/155 已达上限、AI 约 10:25 恢复」横幅（防封额度 UI 生效，与停收无关）。已建远程诊断 dr-mt7ok40m-svjonx（B990）。**04:18-04:21 定性反转（_441/_443/_444）**：①重启后 04:07 之后不补拉；②另群 Ai未来 视图冻在前晚 19:24，但列表行时间已跳 02:51、其 AI（智控王）02:50 仍在该群自动回花无缺→**引擎接收/处理正常，是线程视图停更（读取/渲染在某条消息后中断，毒消息嫌疑：转发嵌图/引用回复），重启不愈**；③筛选片点 × 删不掉（_441，「群组/@我/未读」双片挂死；04:27 _446 交叉确认：**清空筛选有效、单片 × 无效**→范围收窄到片上 × 的事件绑定）→筛选「自己挂上」实为挂上后清不掉。已改按视图停更立案。**诊断包 QTSE67 结论（04:20 自动上传，其机实为 v1.0.55）**：无致命堆栈；fatal_traceback 的 session 列表给出规律——两个冻结点都=某次重启前最后一条消息（Ai未来 19:24←19:31:30 重启；bug群 04:07←04:07:48 重启），此后 00:27/01:06/01:31/04:09 共五次重启均不自愈；引擎侧正常（08-25 已处理 193 条、AI 照回）→ 根因假说：**重启/离线窗口产生镜像缺口，线程视图增量游标卡在缺口处不再前进，且无自动补拉**。影响=工作台漏显客户消息（引擎仍回、人工看不到），候升 P0 |
| **B89** | 钧 JUN | 客户列表显示原始 @号/ID（hwq25801/niuniu2233/WJKJ2026）或「未命名客户」，无昵称无备注（04:37 _450，v1.0.55 客户列表页红框名称列） | 产品建议已采纳待排期：①名称列优先显示 Telegram 昵称、@号降副行；②加可编辑备注名（备注>昵称>@号），列表/搜索/CSV 同步生效；**04:39 _452 扩口径**：客户全景→消息记录页每条气泡只挂「Telegram」平台标不显发言人，昵称/备注口径统一覆盖该页。见实施64 P2-p | 登记 |
| **B90** | 值守自查（源自 _450） | 客户池数据问题：①同一 @号 hwq25801 重复两行（亲密 56/50，疑按本方账号各建一条未按对端唯一键去重）；②群聊（本群 @hwq25801）被当客户；③「未命名客户」两条=昵称抓取缺失 | 查会话→客户映射：去重键应为 platform+对端 id（跨本方账号合并）；客户池过滤群/频道只收私聊；昵称链随 B89 补。见实施64 P2-q | 登记 |
| **B91** | skuio 花无缺 | 英文界面下「Follow-up SOP (Workflows)」卡内链条模板名/描述仍是中文（新客破冰·7日跟进/沉默唤回·3步/成交后关怀·72h/报价后跟单，04:45 _455）——卡片框架文案已切英文，未切的是内置模板种子数据（仅中文版，不吃语言开关） | 模板 name/desc 双语化（按 UI 语言取）；顺带：工作目标 vs 工作链两卡易混（skuio 建议删一），实为策略层/执行层配对（目标注入 AI 思路、链排分步动作、有目标时配套链自动置顶），已答复不删，卡上加一句区分说明。见实施64 P2-r | 登记 |
| **B92** | skuio 花无缺 | 人设工作室「同步到 personas.yaml」按钮与确认弹窗全是开发者术语（yaml/git/config 路径/「第2层」，04:47 _456）——用户端要白话 | 文案整改（i18n 键 psn_sync_to_yaml/psn_js_360/psn_src_published_t/psn_js_010/075/342 一组）：按钮改「保存人设存档」口径；弹窗白话（存档=稳定备份可恢复、重启自动加载；Messenger 自动导入需先转正才入档）；文件名/路径收进小字。中英双语同步；**04:55 _462 扩口径**：全局规则区导入/导出同病（用户不知导出文件类型/如何打开）——整改范围扩为人设工作室全部导入导出/存档类按钮与弹窗（导出文件名可读化+「供导入恢复用，无需手动打开」说明）。见实施64 P2-s | 登记 |
| **B93** | skuio 花无缺 | 默认人设（兜底页，其机名 Crishmar）可编辑字段远少于正式人设卡，问生效逻辑并求补齐（04:50 _459） | 已答复真实链条：会话绑定>账号绑定>域默认兜底，且上线自动补绑（ensure_account_default_persona，B3/P0-3 旧案同源）——兜底页极少真正出场。改进两案报产品拍板：①编辑面补齐到人设卡同权；②（值守倾向）默认人设改为从人设池选完整人设（platform_login.default_persona_id 底层已支持，只差 UI），不再维护两套编辑面。见实施64 P2-t | 登记（待产品拍板） |
| **B94** | skuio 花无缺 | 全局规则「新增约束」无任何引导/参考，普通用户不知怎么写，问优先级与是否触发人工审核、建议若冗余则删（04:57 _464） | 已按源码答复：reply_constraints 注入为跨人设「硬性守则块」（提示词级强约束，优先于人设自由发挥；与人工审核草稿无关；出厂自带合规默认规则，用户为追加）→功能不冗余不删。整改：规则编辑器加模板库/常用示例（点选即用）+占位引导（一条规则=一句禁令/要求）+作用平台标注。见实施64 P2-u | 登记 |
| **B95** | skuio 花无缺 | 侧栏管理页组「今天都打不开」：数据洞察（今日概览/运营总览/运营分析/主管看板）+用量与计费（用量与额度/会员中心）+安全合规（危机审计/操作记录）+账号资产中心（05:02 _466，v1.0.55）；其人猜测是否在换新方案 | 已澄清均为现役功能（0816 管理面改造正式导航组）非下线。三闸候选：主管闸 302 回工作台（role 误判）/feature_gate 档位误锁（1.0.55 lite 授权面串到内测机嫌疑）/前端故障。05:06 _469 回报定性：点击→「智聊 ChatX 正在连接…」加载屏挂起→自动弹回后台控制主页；整组一致、人设工作室等其他页正常→**非权限闸/非档位锁，是该组管理页在应用壳里加载失败后回退（前端壳连接/载入链故障，1.0.55）**。升 P1 派单，见实施64 P1-14 | P1 在办 |
| **B96** | skuio 花无缺 | 账号浮层底部「复制视角链接」点开跳网页版登录页，用户不解其用途、疑无用（05:04 _467） | 已答复用途：复制当前平台/账号/筛选视角的直达链接（inbox.acct.copy_link_t），供多屏/多人/书签场景，浏览器首开需登录属正常。整改：按钮加悬浮说明+复制成功提示写清「粘贴到浏览器可直达当前视角（需登录）」，中英双语。见实施64 P2-v | 登记 |
| **B97** | skuio 花无缺 | 英文界面下原生菜单栏（文件/编辑/视图/窗口/帮助）仍中文（05:58 _472，v1.0.55） | 桌面壳原生菜单不吃网页语言开关。改：壳侧菜单构建读界面语言即时重建（含全部子菜单项），语言切换无需重启；首装默认跟系统语言。与 B69 菜单看门狗同域（menu builder），可同一改动带上。见实施64 P2-w | 登记 |
| **B98** | skuio 花无缺 | LINE 入站不同步：客户（vic001）发的消息工作台看不到、己方能发出（06:47 _483，v1.0.55）；前晚 Ai未来群旁证（_443 02:51「收不到消息/能发出去」）与之吻合 | 已问两事实：手机 LINE 可见否（分流「同步断」vs「对方未发成」）+最近消息时间点。【0826 16:37 回报（_562）：手机端已收到=对方发送成功→**定性=工作台同步断**，非对方未发成；已告知高优先在修】注意：DC8F 诊断包（CUN2TM）不含 LINE 通道日志——包内容缺口并入 B76 修复项。见实施64 P1-15 | 待定向 |
| **B99** | skuio 花无缺 | Messenger 再登后 FB 弹「您被暂时阻止」（疑似滥用临时封锁，06:49 _485）——B70 残留的连锁反噬：反复重登+sidecar 无退避重试触发 FB 风控计数 | 已教止损（停重试/切手动/等自动解除）。修复并入 P1-9 残留：重试指数退避+检出封锁提示即账号级冻结（接 ban_signal/kill_switch，messenger 面此前未接）| 已答复，随 P1-9 修 |
| **B100** | skuio 花无缺 | AI 对客户的称呼未按跨平台档案偏好：档案记「称呼叫 babe」（WhatsApp 导入），客户打成 baba（06:50 "Good morning baba"），AI 回复镜像 baba（07:05 _487，v1.0.55）| 与 B74/75（AI 自称）不同线：这是客户侧称呼。改：回复生成注入档案「称呼」字段并以其为准，客户消息拼写变体不作称呼依据；档案无记录才跟客户用法。见实施64 P2-x | 登记 |
| **B101** | skuio 花无缺 | 产品异议：让路线（账号防封自动发送上限）不应硬限制，应改「提醒+客户可自主放开上限」（07:47 _491，起因其号 225/225 触发让路）| 产品决策项，报老板拍板（与 B34 额度自定义同族）。上报带双面：用户自主权诉求 vs 防封模型风险外移（当日其 FB 号临时风控为实景）；折中案：默认限+触达提醒+确认风险后当日放开/分档可调。见实施64 待拍板 | 待老板拍板 |
| **B102** | 钧 JUN | 登了两个 TG 号，人设「应用到…」账号默认列表只有旧号（张浩然），新加账号无法指定（0826 02:51 _495，v1.0.55） | 已定位（persona_routes ~1866）：弹窗账号列表源=TelegramAccountRegistry.from_config（仅 config 声明账号），运行时协议登录新号落 account registry（SSOT）未被读取=双源真相类。后端绑定接口（api_account_persona_set）本就支持 registry 账号。修：列表合并 config+registry 在线账号。已教绕法：工作台新号会话右栏「设为账号默认」（另一接口，认新号）。见实施64 P2-y。02:56 用户确认绕法生效（工作台可指定新号人设），并要求工作室弹窗必修 | 绕法已验证，待 P2-y 修弹窗 |
| **B103** | 钧 JUN | 需求：UI 语言包增加繁体中文，强调很重要（0826 02:58，面向港台/海外华人运营） | **03:02 范围修正**：界面不做繁体；要的是 **AI 回复语言**加两项——繁體中文（zh-Hant）+ 粤语（yue，聊天口语向）。改动=回复语言链三处：语言选择器选项、lang_policy 证据识别（繁体字形/粤语助词 係唔嘅咗）、外发翻译目标；提示词 _LANG_NAMES 补两项及指令行；粤语 TTS 供应商支持另查。过渡用法已教（人设/全局规则写「对此客户用繁體/粤语回覆」）。见实施64 P2-z（已改写） | 登记 |
| **B104** | 钧 JUN | AI（沐宸人设）对新聊天对象说「你之前不是说想来台北玩嘛」——可见上文对方从未说过，虚构既往对话套近乎（0826 03:25 _504） | 已问分流：全新联系人（=纯幻觉回忆句式）vs 同一真人别平台聊过（=跨平台档案/AI 记忆关联错人）。修法：①生成守卫——「你之前说过/上次你说」句式需记忆确有出处否则禁用（prompt 约束+后置句式检测）；②记忆/档案隔离核查。03:30 分流回报=**全新联系人**→定性纯幻觉回忆话术，主修①生成守卫，②档案隔离降为顺带自查。见实施64 P2-aa | 已定向 |
| **B105** | 钧 JUN | 人设小柔背景/性格已写风格（软软的/句尾呀啦耶嘛/撒娇），实际聊天语气不符（0826 03:32 _508，v1.0.55） | 已发两步自分流：①出问题会话右栏生效人设是否=小柔（_495 时小柔仅服务 0 账号·1 会话，绑定错位嫌疑大，B104 那条会话绑的是沐宸）；②工作室「试聊」测语气——试聊对=真实会话注入链丢风格，试聊也不对=字段覆盖丢（B74/75 压缩残留嫌疑）。03:37 _510 回报：绑定确认=小柔（仅此会话）。**值守核图反转**：截图内三条回复实际带「呀/啦/😊」=合规→问题改定性为**间歇性丢风格**（部分回复丢注入），已请其指认不像的具体消息（对照是否同一回复路径：主动发起/即时回复/长文）+补试聊。B67 教训：不同回复路径加工链不一致 。03:42 用户主动搁置：待 B103 繁体回复语言上线后复测（其客户为台湾人，语气不满与简繁输出纠缠——「国中生」语境）| 挂起（B103 上线后复测） |
| **B106** | 钧 JUN | 台词库导入 50 条「没见触发」，以为 AI 该学会并主动使用（0826 03:44 _514/_516） | 已答复机理：台词库=语音预渲染库（voice_prerender，夜间预合成音色语音条，AI 文本命中+语音发送才复用，不影响文本生成）；命中率见运营总览 AvatarHub 语音卡。其真实诉求指路知识库/话术或人设档案。界面改进：备货页台词库行加用途说明（「用于语音提速，不教 AI 说话」），中英繁同步。见实施64 P2-ab | 已答复 |
| **B107** | skuio 花无缺 | 产品建议：AI 学会引用回复（引用客户某条发言作答，0826 03:56 _521） | 可行性已答：发送层半就绪（unified-inbox send 已有 quote 参数/quote_applied 回执；TG/WA 原生 reply-to，Messenger 需 RPA 模拟，LINE 待查）。核心为选择性引用决策（连发多问挑一答/隔久回/插话题时引用，连续对话不引用，防机器人感）。见实施64 P2-ac | 登记（产品建议） |
| **B108** | 钧 JUN | AI 口头禅复用频繁（同会话反复「被你抓到啦/洗耳恭听」，0826 04:17 _523） | 修法：回复生成注入「本会话最近 N 条 AI 消息的开场/收尾句式」做规避提示（现有 dup_guard 只拦全同文本，不拦句式复用）。见实施64 P2-ad | 登记 |
| **B109** | 钧 JUN | 感觉工作目标未执行、AI 不主动搜集客户资料（04:18） | 值守核图证伪：其截图内 AI「那你叫什么呀?」正是按目标（快速搜集客户资料）找切入点；右栏今日拍建议待其点「采纳并拟稿」，另有「达成推送未接通」待接通。已教：采纳/自治档调高/去接通；资料回写档案若缺失再按 bug 报。UI 观察项：目标执行动作用户感知弱，「AI 做了什么」行可加最近动作示例。**0826 06:33 _532 追问**（单日目标 1/1 天·11%，AI 晨间寒暄未见目标动作）：已给 10 秒链路自测法（点「采纳并拟稿」秒出草稿=通/不出=断）+建议目标设 3-7 天与调高自治档，等自测结果 | 已答复（等自测回报） |
| **B110** | 钧 JUN | 主动关怀话术死板（「昨晚的想念，今天醒得早又想起你了」通用抒情模板，达不到效果，0826 05:25 _527） | 话术质量专项：①关怀句指涉客户具体事实（档案/近聊提取）；②结尾带轻问题钩子引导回复；③按关系阶段分档口吻；④关怀句式防重（与 B108 同修）。见实施64 P2-ae | 登记 |
| **B111** | 钧 JUN | 资料收集类目标推进太保守：聊得投机 AI 也不问名字/年龄/基础信息（0826 06:41，B109 延伸） | 已教立即用法：目标文本改写为明确字段清单（称呼/年龄/城市/职业，一次一项）+自治档调最高。产品调优：资料收集目标内置清单模板（勾选字段）、进度=已收集 n/m 项、rapport 热络时提问节奏加速；**06:43 追加主张**：基础信息收集应为默认技能（不建目标也该会）——已并入 P2-af 增补（新客户前 N 轮默认渐进收集轻信息，全局开关，目标为加速器），涉默认行为变更标注待产品确认。已答复底层机理（目标+今日拍注入思考上下文，默认权重偏舒适节奏）。见实施64 P2-af | 登记 |
| **B112** | 钧 JUN | 人设为台湾人但聊不出台湾女生语感（呀/啦/耶/嘛少见），问 AI 能否地区自适应（0826 06:53 _538） | 登记「地区语感自适应」并入 P2-z 繁体交付包：人设地区=TW 时提示词自动带台湾口语指令+用词表（喔/欸/齁 语尾、影片/传讯息用词、避大陆网络语）+例句 few-shot。已教过渡写法：人设风格写具体例句（比形容词管用）。与 B105（挂起）同验收；**07:00 扩围**：全地区自适应（人设 region 档案体系，首批 TW/HK/马新，默认档地区中性防大陆网感外溢），交付顺序 TW 先行 | 登记（已扩围） |
| **B113** | 钧 JUN | 开着全自动 AI 不回：会话「十翼」全自动绿、客户已发 Oh really? / Haha…，工作台无跟进（0826 08:41 _543）。右栏初识 100%+「确认进阶」待点 | **案情修正（撤回图 _544/_545 归档还原）**：09:17 起已自愈恢复回复——客户追问「这句话你为什么跳过了」后 AI 接上，09:41 正常聊；定性=**个别消息被跳过不回**（08:13 Haha… 空 ~1h），非全停、非进阶闸。修复方向改为：漏回检测（客户消息超时未回自动补答+记原因）+ 调该条为何被跳过。旁证：TG 引用回复原生可用（_544 引用气泡）→B107 发送层 TG 面无需另做。见实施68 P1-17 | P1 待修（已收窄） |
| **B114** | skuio 花无缺 | 克隆语音上传新音频绑定人设，登记成功但音色仍默认（0826 15:47 _549，v1.0.55；「实际使用」行=edge_tts·Xiaoxiao·由 avatar_clone 回落，B43 同签名） | **网关流水定案（15:46 UTC+8）**：DC8F 的 register_spk 失败 23.2s、clone 失败 23.3s——请求已达 hub，**hub 侧超时失败**→客户端按设计回落通用音色。客户端激活链（P0-2）成立，非 B43 复发；真身=B61 hub/176 负载病灶首次直接卡死用户克隆 | 📋 已答复（登记有效、恢复自动生效）；**升级 B61 优先级**：hub 侧修复由语音线扛，值守留证据便签 .ops\NOTE_from_duty_20260826_dc8f_clone_fail.md；恢复后回访 skuio 验声；【0826 17:00 影响面扩：钧机（1.056）delivblock 主因=语音合成引擎异常＝同一 hub 故障拦停其带语音自动回复（_566），两位测试者均可感——**再催语音线**】 |
| **B115** | skuio 花无缺 | 产品建议：工具箱「生成语音」面板里的克隆**录入+登记克隆音色**整块撤下，克隆登记只留人设页语音区一个入口（0826 15:54 _551；B25-①/B62 入口散乱案的口径落定） | 工具箱生成语音保留（试听/生成用），仅撤录入/登记块；与 B62 回落人话化同域，撤块后该路径病灶自然消失 | 📋 已采纳登记（15:5x 群内答复）；入实施68 P2 批 |
| **B116** | skuio 花无缺 | 登录 Messenger 后「不断跳出这个页面」（0826 15:56 _553）：headed Chrome 反复弹出并停在同一 E2EE 会话（Julito），页面明示 aren't connected / not contactable on Messenger | **P1-9 残留第二景**：对不可达对象的待发消息发送失败 → 边车 composer not found → re-navigate 无退避循环重试，1.0.55 headed 形态下每次重试=弹一次窗；死局对象无终局判定永不放弃。顶部 --no-sandbox 灰条=B77 同框 | 📋 已答复（临时=该会话切手动/删待发；窗口可关）；修复并入 P1-9：退避+上限之外**新增硬要求⑥**：检出 not contactable/aren't connected 等死局文案→该条消息终局失败留痕，绝不重试 |
| **B117** | skuio 花无缺 | 客户发**贴纸/表情包**，AI 当真实照片评论（问「这小朋友挺可爱，亲戚家的？」，0826 17:12 _576）；用户点名要方案：贴纸=表达心情，AI 应回应情绪而非画面 | 识图链未按媒体类型分流：TG/WA 协议层可辨 sticker vs photo，现全走照片评论链（B46/B55 识图族新面） | 📋 已答复方案并登记：①消息类型硬分流（贴纸/GIF 不走照片评论链）②贴纸走情绪回应（识别情绪、不描述画面）③拿不准就轻接不下断言。验收=_576 场景贴纸不被当娃照。入实施68 P2 批 |
| **B118** | skuio 花无缺 | WhatsApp 全自动回复把**括号内心独白当正文直发**：客户「我只是生你的气」→AI 发「（我没生气，就是心里堵得慌）」（全角括号原样出站，0826 17:24 _578，+856 客户，人设 steven 线）；客户随即更怒。用户初判「表情触发+括号描述」——实为生成旁白未剥 | 生成层写了舞台指示/内心独白，出稿守卫无括号旁白剥除面；该条另有答非所问（上下文断）待调生成记录 | 📋 已答复分析并登记：①出稿守卫加括号旁白剥除/重写（整句括号包裹即命中），与 dup/echo 守卫同宿主；②调 _578 该条生成记录查上下文注入。验收=_578 场景不再出括号旁白。用户 17:33 定调（_582）：**直接废除内心独白设定**（暴露 AI 风险）→升级为生成端硬禁小说体旁白+出稿守卫兜底重生成，两层；已确认执行。升 P1 优先。入实施68 |
| **B119** | skuio 花无缺 | 人设已开「相册/发图功能」并传自拍，客户（TG 小界，英文）说「Just send a previous selfie」，AI 不发图、用调情话推脱（0826 17:32 _580 设置页 _581 现场）| 发图两道闸：人设级开关 + 全局 companion.selfie.enabled；全局关时注入「别承诺发图」硬边界→推脱话术，与现场吻合。全局闸只在 config，人设页仅小字提示「还需全局开启」无入口无状态显示，用户无法自查 | 📋 已答复两闸机制并请用户核对变现页发图能力状态。登记修复：①人设相册开关旁显示全局闸状态+一键开启；②英文要图关键词回归（send a previous selfie）；③若全局开仍不发→查付费相册闸(decide_selfie)与触发判定。入实施68 P2 批 |
| **B120** | skuio 花无缺 | 自动回复设置里「启用语音回复」已关（_588，触发方式=对方发语音才回），会话 cinderella 仍出语音条（_589 圈 17:24 条，0826 21:17 报）| 全局闸只覆盖 A 线自动语音回复（sender._maybe_send_voice_reply 查 telegram.voice_reply.enabled）；B 线主动/autosend 与坐席手动语音不吃该闸；~~另需核对 17:24 条是否早于关闸时刻~~ **0827 07:18 新证据（_msg 已归档）：关闸态下早安「主动关怀」语音直发客户=B 线穿透实锤，时间线问题作废** | 📋 已答复并请用户核对关闸时间。登记修复：**所有发语音链路统一过全局开关**。入实施68 |
| **B121** | skuio 花无缺 | AI 出站英文混汉字：「I'm 我 going anywhere.」直发客户（Yhang 会话 21:16，_590，0826 21:18 报「造成这种机制的原因」）| 生成/翻译拼接层错位，出站无单语种校验；与 B118 同级暴露风险 | 📋 已答复登记**加急**：出站单语种校验——英文句检出 CJK（或反向）整条重生成，绝不出站。入实施68 P1 |
| **B122** | skuio 花无缺 | 生成回复一段段产生并分段填入对话框，直接发出也是多条；坐席要改稿再发时碍事，建议整段生成（带标点连排，0827 03:32）| 分段是刻意拟人节奏（split_send 族），但「填入草稿」场景不该继承分段 | 📋 已答复登记：**草稿/填入模式提供整段选项**（标点保留不切段），全自动分段节奏不变。入实施68 P2 |
| **B123（值守自察）** | 值守线 | 117 离线期间（0827 03:18~04:16）报障群消息不回填：引擎回网后 bug_intake 不补处理离线期群消息（03:32/03:36 两条无归档无登记，靠值守手工 sync 才发现）| 群监控只吃实时 update，无 catch-up | 📋 登记：bug_intake 启动/重连时按群 last_seen 补拉并照常登记（图一并归档）。入实施68 P2 |
| **B87** | 钧 JUN | 手机端（TG 官方客户端）删除消息后，工作台镜像未同步删除、消息仍显示（0825 03:49，_419） | 镜像链只消费新消息/编辑，未消费删除事件（deleted_messages）——删除态不落 store | 📋 已登记（03:5x 群内答复）：消费删除事件同步镜像；产品选择：**04:03 钧口径定稿（原话）**「最好的办法是同步删除，显得专业，当然AI可以有记忆，但是不要主动提删掉的信息」＝界面同步删除 + AI 记忆保留上下文但**禁主动提及已删内容**（生成守卫规则）；值守灰标倾向作废，按用户口径实施。【0826 16:19 催问（_559 叫醒的猫）】钧复报手机删、工作台仍在，并问「这个功能可以实现吗」——已答复可实现、在单上（同步删+AI不主动提） |
| **B86（加急）** | 钧 JUN（与 skuio B81 同病并案） | 「AI 回复状态体检」报「4 条回复等人审核（最早 17.2 小时前）」+按钮「打开待审队列」，点开=「暂无待审草稿」空（0825 03:46，_416/_417，v1.055）——计数为真（即 B73 被拦回复），落点漏显示 | 计数（全局）与落点（当前会话小面板）口径断裂；与 B81（待处理 12 点开为空）同一队列面断裂，疑 0823 unread-trust/last_in_ts 批对 attn/待审聚合的回归 | 📋 **加急已登记**（03:5x 群内答复+如实告知修复前旧草稿无可靠放行入口）：①体检按钮直达全局待审视图（逐条带会话名+放行钮）②计数与列表同源契约（点开必见同一批）③B81 并案；**注意：该断裂使值守此前「到待发队列一键放行」指引全体失效**，B73/delivblock 的用户补救路径依赖此修复。**04:02 案3（_422）**：「需要人工」计数 6 同样无落点——三入口集齐（待处理 12/待审 4/需要人工 6）；用户验收口径原话「应该链接到需要的会话的处理」＝计数点开必见同批条目+会话名直达。**04:04 补证**：同名「需要人工」计数 skuio 机可点、钧机「点不了」——可点性跨机不一致，修复须统一所有计数入口可点并双机验收。**04:06 参照物（_429，skuio 演示）**：其机「需人工」点击=会话列表筛出待处理项、形态正确——即三个计数入口的对齐目标（待审/待处理落点照此改）；钧已 Got it，个案教育闭环 |
| **B85** | skuio | Messenger 发送失败，状态栏报错完整透传（P1-4① 修复生效实证）：500 + `elementHandle.click: Element is not attached to the DOM`（0825 03:33，_410，Roselyn/61584011289581 会话） | **边车点击竞态**：Playwright 定位发送按钮后、点击瞬间 Messenger 页面重绘换掉元素（stale element）——网页自动化经典 flake，e2ee 恢复态页面重绘频繁加剧 | 📋 已登记（03:3x 群内答复+重发即可）：边车 /send 点击前 re-resolve 定位+ stale 失手自动重试一次；验收=同会话连发不再出现该错。**03:38 修正**：该会话重发亦失败=非偶发——表面竞态错误下的真身是 B70 PIN 态整号瘫（点击竞态修复仍保留，但本案并入 B70 验收） |
| **B84** | skuio | 学习队列把「你是哪个国家的」这类人设型闲聊问题当知识收录；且 AI 以客服腔回避（「国籍不重要，聚焦解决问题」）而非按人设回答（0825 03:27，_405） | 两面：①学习队列收录无题型过滤——人设属性/闲聊类无标准答案问题不该入库（学习库应只收业务知识）②生成层客服思维残留——人设属性问题应按档案角色化回答（B75 档案不进对话的表现面之一：手里没料只能打官腔） | 📋 已登记（03:3x 群内确认采纳）：①学习队列收录规则加题型过滤（人设属性/闲聊不入） ②出稿检查加「人设属性问题禁客服式回避」；与 B75/实施67 档案注入修复并案验收（档案进对话后自然角色化） |
| **B83** | 钧 JUN | 产品建议：「业务助手这里的悬浮副驾以及其他地方都有这个悬浮…为了美观，没有实际意义的悬浮就删除了」（0825 03:24，_402）——悬浮入口多处重叠（业务助手侧栏/悬浮副驾/右下悬浮球） | 助手入口经多批迭代出现冗余露出；用户口径=只留有用入口、纯装饰撤除 | 📋 已登记（03:2x 群内确认采纳）：全站悬浮入口盘点去重，路由 assistant/UI 线与 B79（悬浮球转人工）同批处理 |
| **B82（值守自察，疑似）** | 值守线（自 skuio `_399` 截图带出；追记 0826 17:41 _585「仅剩 10%」；0826 21:17 _589/_590 已掉 **0%** 并预告明天暂停自动回复——**升级为测试阻断级**，需抢在 0827 早前放行） | 0825 03:00 其顶栏横幅「今日 AI 额度仅剩 12%——用尽后自动回复将暂停到明天」——**B40 无限量期（至 08-29）内不应出现额度倒计时**；掉 0 将端上自暂停=即将到来的测试阻断 | 嫌疑：升级 1.05x 后端上额度快照/试用档位未继承 B40 放开态（08-22 钧机 996k 徽章曾实证端上生效；skuio 现为体验档 12%）——lite 世代 trial 模型切换与存量升级的叠加面 | 📋 已登记+群内预警（03:0x，嘱掉 0 即报）：执行线查 1.05x 客户端额度快照对 gateway 放开态的消费；若端上自暂停=B40 破功需热修。**进度追记**：03:33 已掉至 7%（_410 顶栏），自暂停临近。**0827 值守定案+放行**：0827 05:51 skuio 实证「真停了」→ 查网关流水（gw_usage 归档）DC8F 日烧 0824=738 万 / 0826=865 万字符，而 gw_budget 无其覆写、吃 env 默认 500 万（B40「不限量」实为高上限；客户端 quota_probe 有 unlimited 消费位但服务端从不下发该键=代码层无限量根本没实现）→ 已经 VPS 本机 admin API 设覆写：DC8F 5000 万、B990/E4DC 各 2000 万（note 标 B40 到 08-29；覆写主体免全局熔断）。⚠️ 08-29 到期动作追加：清除这三条覆写（与 env 还原同批） |
| **B81** | skuio | 顶栏「待处理」角标显示 12 条，点开列表为空（0825 02:40 报，截图 _389 场景，v1.055 世代） | 角标计数与列表口径不一致（B45 角标族）：计数源聚合了列表筛选不显示的项，或处理完成后计数未清零/未随状态回收；已问用户具体入口（待处理/发前确认/急需处理）定位计数源 | 📋 已登记（02:4x 群内答复+定位问题）；badge-truth 家族回归（UI Phase0 曾修 attn 口径），待入口回报后交执行线 |
| **B80** | skuio | Messenger 回复在手机上显示正常单条，工作台镜像里**显示重复**（0825 02:00 报，截图 _373，Rocky↔Roselyn 会话，v1.055 世代） | 镜像显示层问题（客户侧无恙）。两嫌疑待分流（已问用户「有失败标记 vs 全一样」）：①P1-4③ 失败留痕+重发对（failed 行+resent 行都渲染，应折叠/明示关系）②E2EE 恢复后边车重读历史重复入库（去重键对 e2ee 消息失效） | 📋 已登记（02:0x 群内答复+分流问题）；**02:03 用户回报「全一样」→ 定向②坐实**：ingest 去重失效（同一消息重复入库，E2EE 恢复后重拉历史嫌疑最大——恢复前后消息标识不一致/去重键未覆盖）。修复口径：①messenger_web 读线程 ingest 补稳固去重指纹（平台消息 id 优先，内容+时间窗指纹兜底）②存量重复行收敛清理（migration）③验收=Rocky 会话重复行消失且新消息不再重复 |
| **B79** | 钧 JUN | 产品建议：「只需要在悬浮球AI加入转人工服务」（0825 01:48，接 B78 撤网站客服位的延伸——撤位后悬浮球是唯一入口，需要转人工出口） | 建议成立：悬浮球助手（assistant-ball/xz 线在建）动作池已有 act/plan 框架，可评估加「转人工/联系客服」动作（呼起支持面板/工单/@坐席） | 📋 已登记（01:5x 群内确认采纳）；路由=小智/assistant 线动作池评估（impl62 家族）；与 B78 撤预置同批考虑 |
| **B78** | 钧 JUN（skuio 0824 07:23 同求） | 要求删除预置「无界科技·在线顾问」账号（Web 站点客服演示位，persona 林小雨，零会话；0825 01:42 报，截图 _364——两位测试者客户端均被预置且均要求删除） | 内部 flavor 种子预置演示位对测试者是噪音；**删除持久性待验证**：seed merge=missing-keys-only 语义下，删除（键消失）是否会在更新/重启时被种子重建复活 | 📋 已答复删除路径（01:4x，账号芯片⋯→删除账号）+请用户观察复活即回报；产品待决：内部 flavor 是否继续预置该演示位（或改为可选引导创建）；若复活=种子重建 bug 登记修。**01:47 升级（_366）**：管理面板该位**只有刷新/改名、无删除入口**（值守 01:45 给的删除路径对 Web 渠道位无效，已群内更正）——修=①Web 渠道账号补删除/隐藏入口（与其他账号一致）②用户产品意见原话：「我们已经有悬浮球AI工作，不需要网站在线客服了」→ 支持撤预置方向。**01:51 用户明确表态：采用方案②（直接撤预置）**（msg 371，已确认记录） |
| **B77** | 钧 JUN | 接入 Messenger 打开的真实浏览器登录窗顶部露出「您使用的是不受支持的命令行标记：--no-sandbox…」技术灰条（0825 01:39 报，截图 _362；其本体问题「跳到网页登录」=1.055 headed 交互登录新设计，按预期答复非 bug） | 边车拉起 Chrome 带 --no-sandbox 被浏览器信息条提示——技术参数直怼用户脸（B24-② 观感族）；顺带：headed 新流程首开缺一句「请在此窗口完成登录」的引导（用户误当故障报障） | 📋 已登记（01:4x 群内答复=新设计说明+登录用法）：①去 --no-sandbox 或抑制信息条（services/messenger-web launch 参数面）②headed 窗首开注入一条引导横幅；随下批 |
| **B76（值守自察）** | 值守线 | 两台客户机诊断包（EC4UPG/KVRX2Y，均 1.054）**都没有主 app.log**——只有 sidecar/fatal/stats/config；B71/B73 类日志取证被阻塞 | P2-12「runtime logger 树 file handler 收集」在打包态失效嫌疑（frozen 下 logger 命名/handler 形态不同，或客户部署主日志无 file handler） | 📋 已登记（实施64 P2-m）：打包态验证主日志收集；验收=客户机诊断包含 app 主日志尾；证据便签 .ops\NOTE_from_duty_20260824_evidence.md |
| **B75** | 钧 JUN | 自建人设（张浩然/WARM202xx）档案字段疑未进生成链：已保存+重启 3 次，AI 仍无视父母职业/工作/背景资料、即兴自编（问职业答「商业咨询」，11:19 报，截图 _355 场景；承接 B74 次要面升级独立案） | 待定位：档案→指令文本编译 / 「应用到…」环节 / 会话注入三段链，断点未知；已给用户两个自检（工作室「验收考题」+「应用到」确认）分流编译面 vs 注入面；B990 远程诊断在拉 | 📋 已登记（11:2x 群内答复）：执行线可本地复现（新建自定义人设填档案→绑定会话→问档案题），修复目标=填了就算数或明示哪些字段不进对话；实施64 P1-11 附案。**11:26 升级（_356 场景）**：用户实测「所有聊天都不按人设框架」（职业/背景/英文名全不用，AI 对客户否认自己说过的行业），并直问「核心逻辑是否被修改」——值守如实答是（1.054 携 0823 夜批记忆三件套 _inject_known_profile/self_state/reply_echo_guard）；**回归排查点**：夜批改动与 persona 档案注入链的相互作用（known_profile 硬注入是否挤占/覆盖人设块、echo_guard 与 persona 块相互裁剪）；值守承诺=专项结论第一时间回访，期间同类现象并本案。**🔧 0824 午间落码（实施67，根因坐实）**：真凶=客户包交付档 persona_block_detail: compact 静默丢弃 70% 档案字段（背景/父母职业 context.family/英文名 names.english/好恶/记忆全在 full-only 编译面）——「填了不算数」是档位设计与自建人设的组合盲区；夜批 _inject_known_profile 无预算冲突（主链无截断机制，排查点①还清白）。修=显式绑定 tier 自动升 full（「填了就算数」）+ compact 补名字事实行 + 字段防丢弃契约门禁（编辑器字段 ⊆ full 编译面，缺一 CI 红）；随 22:30 窗装载，装载后按「新建张浩然填档案→绑会话→逐项问答」验收回访 |
| **B64** | 钧 JUN | 接入 Messenger：官方登录窗里的机器人验证「确认您是真人」只渲染一小块且无法放大操作 → 验证过不去 → FB 不下发 6 位两步验证码 → 接入对话框永远等码（12:30/12:31 报，截图 _316 白页+微型验证件 / _317 等码弹窗，「等待服务器端登录」流程） | 接入流程缺陷：RPA 拉起的 Chrome 登录窗视口/缩放受限，FB checkpoint 验证组件渲染异常且无放大手段；等码界面未说明「未过机器人验证不会发码」=用户干等 | 📋 已登记（12:4x 群内答复，附临时尝试：最大化+Ctrl 加号缩放/重开登录窗换验证方式）：①登录窗支持缩放/最大化 ②验证反复失败给「换系统浏览器完成登录」逃生口 ③等码文案补实情说明。验收=带两步验证的 FB 账号能走通接入 |
| B33 | skuio | 两条带图报障图丢失待重发：①「发送图片，报错，分析问题」（06:37）②「显示问题，显示不完整，建议排版修复」（06:33） | 截图被限频闸拦截未入库（protocol_media 无新文件）——限频误拦二次伤害：文字可 sync 还原、媒体不可 | ✅ 已兑现（09:36-09:44 skuio 补发 5 图 _189/_191/_193/_195/_197，红框报错原文直接促成 B14 定案免诊断码）；「显示不完整」指认=左上角状态控件文字截断挤竖条（_193 圈图），并入页面排版批次。**值守自察**：该轮 AI 值守稿连发 4 条胡答（「重启看看/换个方式聊/教用户回消息」+误开工单 #9）——caption/词表修复在树未装载所致，10:0x 已群内更正作废（msg 198），12:30 重启窗装载后应消停；原文案 ⏳ 已群内请重发两图（07:04）；**值守自察**：限频闸 06:33-06:38 连拦 6 条真反馈（凌晨陪聊风暴烧掉用户预算的连带债）——**机制已修待重启窗装载（2026-08-21 ~11:55，impl49 执行线）**：「先登记再压制」＝回复照拦（防刷屏契约不动），但真反馈（分类命中/带字截图/收集窗内）全文进 `bug_events`（`rate_capped_report`）+ 收集窗截图照记 + trigger 层把被压制的带图消息归档 protocol_media（`capped_photo_archived` 事件带 URL，值守翻台账即可找回，不必请用户重发）；损失从「永久丢失」降为「回复延迟」。门禁 `test_bug_intake` 46 绿 |

**系统性病根**（今天四例同构：B1/B2/B8/B9）：功能在演示机靠 overlay 开着、客户包里静态关、
**界面不同步隐藏**——用户看到入口→操作→必然失败→报障。这不是四个 bug，是一个流程漏洞：
没有「打包能力对账单」门禁（工作项 5）。
→ **该门禁已落地**（2026-08-23 impl64 P0-2⑥：`test_deploy_profiles.py::
test_ship_config_disabled_capabilities_are_all_accounted`——交付档每个
enabled:false 必须 registry 在册 / `_hosted_auto` 预授权 / allowlist 附理由）。

### B45-B65 执行线回写（2026-08-23 22:15，impl64 exec 线；细节见《实施64》施工状态区）

- **落码待装载（22:30 窗重启 + 1.0.52 出包）**：B57+B59（updater 等旧后端真死 +
  登录成功自动解冻 auth 族 + 登录变更窗口降档；横幅出口/文案=killswitch 线已装）、
  B43回炉+B62+假话文案（enroll 预授权入口拆死锁 + safe_edge_voice 克隆名不透传 +
  文案按托管实情）、B61 工程兜底（哑音检测重试 + preview 进 voice_outage +
  预览无声警示；hub 侧仍在语音线）、B50/B51/B52（年龄捕获+已知画像禁复问 /
  出稿反复读闸 / 自述状态衔接含 proactive）、B56（专名铁律+译后对账括注原词）、
  B63①②（http_error_detail 已在树核实 + 发送败因→账号卡会话健康）、
  B53（上传重试+报错三分+mini 包降级；远程诊断腿同修——**装载后可对 skuio 重发
  dr 请求**）、B65（relay 占位/检查点平台分键）、B49（预览等待反馈+超时人话）。
- **兄弟线已装载（1.0.52 出包后值守回访实测）**：B45（last_in_ts gate）、
  B58（标签改删）、B59 可见性两面（横幅归因+一键解除）、B46+B55（拟稿等图+
  盲断言闸门，`_271`/`_295` 双场景验收）。
- **22:41 round2 补齐**（同窗二次装载，细节见《实施64》施工状态区）：
  B63③ 失败留痕+一键重发（store failed 行+worker 终局镜像+resend_of 收口+
  六处「真发出去了」口径过滤；test_failed_outbound_trace 12 绿）；B64 大头
  （兄弟线 viewport:null + 本线「确认您是真人」文案判据进 login_classify
  （Node，随 1.0.52 包走）+ 等码/检查点实话文案 hot-live）。B64「换系统
  浏览器」完整逃生口＝需 cookie 导入面，交产品决策。
- **未做交下批**：B60（滚动区）、B47（编辑器状态机合一）、B25-③/P2-11/B15。
- **等复现**：B54（光标，已向 skuio 要细节）。B51 的 `_285` 生成记录环节定位
  待生产日志复查（闸已兜住）。

## 二、已完成清单（随 v1.0.44 发布，执行对话**勿重做**）

代码全部落盘、门禁全绿（152/152 注册表全家 + 41/41 值守 + 53/53 双树）：

1. ASR 托管自动接入三层（`hosted_gateway.ensure_hosted_asr` auto_enable + `ASR_ENV_AUTO`
   回放 + cloud_light/desktop.min 种子 + `voice_recognition._hosted_auto` A 类）。
2. `companion.goals.notify.enabled` A 类基线（出厂即开+存量补齐）。
3. `telegram.poll_fallback.mirror_outgoing` A 类基线（同上）。
4. cp-origin 未开通态收口（无必败入口+`cp.origin.unavailable` zh/en+错误码人话；
   `?v=20260820a/b` 双戳+镜像同步+ui-build 已走完）。
5. 报障值守整轮加固：报障群闲聊/纯图静默（@点名仍接话）+ 支持腔硬化 + `feedback`
   独立分类（确认+登记+感谢应答口径）+ 词表四轮扩容 + 限频 4→10。
6. （1.0.43 已带）语音克隆托管自动接入、账号人设热应用、报障工单/通知/验证闭环。
7. **B12 会话列表「更多操作」失灵**（2026-08-20，`ui-build 20260820-1950`，css `?v=20260820g`）：

   排查结论与「死按钮」直觉相反，值得单独记一笔——**handler、事件绑定、window 暴露全是好的，
   菜单每次都真的开了**，只是被渲染到首屏之外（真浏览器实测 `box=(0,900) 1440×293`，视口高 900）。
   根因是 `unified-inbox.css` 里一条**说明注释**把 token 族写成 `--text-*` + `/` + `--accent`：
   那两个字符正好是注释结束符，注释于是提前闭合，真正的结束符 `*/` 变成游离符 → 解析错误 →
   浏览器的错误恢复把**紧随其后的整条 `.ws-ctx-menu` 规则丢弃**。花括号照样平衡、CSS 不报任何错，
   只是静默少一条规则；丢掉 `position:fixed` 的菜单退化成文档流末尾的普通块，左键「⋯」与右键
   两个入口都表现为「点了毫无反应」。同一文件另一处（`--bdg-*` + `/` + `--tk-*-ink`）同样吞掉了
   `.connect-mode-notice` 的蓝底规则。

   讽刺的是两处都是**修配色时新写的说明注释**：2026-08-19 那次 B7（菜单亮色下黑底黑字）把规则
   内容改对了，却被自己的注释把整条规则删掉——所以 B7 的修复从未生效，还顺带长出了 B12。
   修法是注释改分词写法（`--bdg-* 与 --tk-*-ink`），**零规则改动**；修后规则数 2288→2290，
   菜单 `onscreen=True` 且锚在按钮下方（196×305）。门禁 `tests/test_css_comment_trap.py`
   （扫静态 CSS + 模板内联 `style`，模板先剥 Jinja 注释）已挂 `gate_sweep`。打包端
   `static-staged` 由 `build_backend` 从 src 现生成，下个包自动带修复。

8. **B18 搁置弹窗数字键落进右栏语音框**（2026-08-20，`ui-build 20260820-2010`）：

   报障复现成立，但故障点与登记时的猜测（「1.0.43 缺焦点抢占行」）完全无关——那两行
   自 2026-08-08 起就在，且真浏览器实测弹窗打开确实抢到首个档位按钮的焦点。真因是
   **`stopPropagation` 与 `preventDefault` 的分工被混同**：`onKey` 一进门就
   `stopPropagation`，全局快捷键因此被干净隔离（面板期再按 S 不会叠开第二层，实测 1 个），
   但**浏览器的默认插入行为只看 `defaultPrevented` 与当前焦点元素**，跟传播链无关。
   而 `preventDefault` 只写在「映射内数字」「永久档数字」两个分支里，于是漏两类键：
   ① **映射外的数字**——预设档随时段变（19:30 之后「今晚」档消失，只剩 3 档 + 永久=4），
   按 5 就没有任何分支接住；② **任何普通字符键**。这两类键在焦点被弹窗外元素持有时
   （右栏语音框 / 回复框 / 异步渲染抢焦点的组件；Shadow DOM 里的组件在 document 层
   还会被重定向成宿主，与 B19 同一族）会原样打进那个输入框——正是截图里的现象。

   修法是加一道「`e.target` 不在卡片内 → 吞掉本键 + 把焦点收回首个档位按钮」，位置有讲究：
   **排在数字映射之前**（否则焦点在外时一次游离按键会顺手把会话搁置掉）、**排在 Escape
   之后**（焦点在外也必须能退出）。语义上首击只做「收回焦点」不选档，是刻意保守。
   验证走真浏览器探针并**带对照组**（无弹窗时同样按键确实落字），否则「按了没落字」可能
   只是探针根本没按到、整轮验证空转；实测 5 / x / 1 三键零落字且焦点每次自动回卡片，
   焦点回来后再按 1 正常选档并发出 snooze 请求（探针 route 拦截，不写生产会话）。
   门禁 `tests/test_snooze_dialog_keys.py`（静态 ratchet：兜底存在 + 三者顺序 + 开窗抢焦点）
   已挂 `gate_sweep`。纯模板改动，热更即生效，客户端随下个包。

9. **B20 示例型占位被读成实际配置**（2026-08-20，`ui-build 20260820-2030`）：

   客户两张截图圈出的「telegram:default」（自动回复设置·账号班表覆写的账号键）与
   「zh-CN-XiaoxiaoNeural」（人设工作室·音色/Voice ID）都是空输入框的 placeholder，
   与真值只差一层灰度——客户读成「系统已经这么配了」，并据此追问「苏婉为什么用微软合成音」
   （她实际已锁克隆音色）。两处加「例：」前缀（en `e.g.`）。

   **刻意只加这两处**：同一张班表表格里另两个占位不动——workdays 的「每天」、timezone 的
   「跟随全局」描述的是**留空时的兜底行为**（填空才改变结果，本身就是有效信息），加「例：」
   反而把「留空=每天」这条语义说错；而账号键与 Voice ID 留空没有兜底含义，纯粹是格式示例。
   即「示例型」与「兜底型」占位是两类东西，全站统一加前缀会伤到后者。

   顺带把班表那处从硬编码中文串改走 `RPS_I18N`（新键 `rps_ws_js_key_eg` /
   `psn_voice_id_eg`，中英齐备，落 `i18n_packs/reply_settings_page.py`、`persona_studio.py`）
   ——原先英文界面也会看到中文占位。验证＝登录态拉两页真实源码：班表已无裸
   `placeholder="telegram:default"`、人设页渲染 `例：zh-CN-XiaoxiaoNeural`。
   渠道配置页（telegram/messenger 语音字段）的同类占位留 backlog，本批不扩面——那些页面
   有并行线在动，且不是客户实际报障点。

## 三、四视角分析

### 开发工程师视角

1. **「打包能力对账单」是本批最高杠杆工程项**：四个默认关病例证明靠人记不住。做成门禁：
   遍历 feature/config 键集，凡「example 有、客户档关」的能力，必须在登记表里三选一——
   ①托管替代（如语音的 `_hosted_auto`）②UI 明示不可用（如 cp-origin 新做法）③UI 彻底隐藏。
   无登记即 CI 红。一次建设，永绝此类。
2. **配置生效可见性**是第二根：B3 的本质是「保存成功≠生效」不可见。方案：设置类保存
   响应统一带 `effective_scope`（生效范围+被谁覆盖），前端 toast 展示；人设页保存全局默认时
   查询并提示「N 个账号有账号级绑定（优先于全局），[一键同时应用到这些账号]」。
3. **值守链技术债**（本对话顺手清）：历史回填丢 sender_name/sender_id；被压制消息不落
   镜像（观测应与接话解耦）；AI 应答缺工单上下文（同报告人在办工单摘要注入 prompt）。
4. 会话列表范式切换（工作项 1）动 `unified_inbox.html` 热区——先探活/意向板协调，
   改动按「数据不动、视图层切换」设计：会话查询本就有全量口径，改默认筛选+角标渲染，
   风险集中在前端状态机，勿动 store 层。

### 市场经理视角

1. **响应速度即卖点**：今天上午 skuio 10 项全部当场有结论（最快 4 分钟定根因），这个
   服务水平写进官网「专属技术管家：报障 3 分钟响应、24 小时结论、修复主动回访」是
   竞品拉不开的差异化。四视角执行后建议市场侧出一篇「内测 48 小时，我们修了什么」。
2. **诊断码是信任资产**：「把码发我，不用你描述」把支持体验从「客服问答」升级为
   「远程会诊」；旗舰档可加「授权远程协助」权益（P2）。
3. **IM 范式切换（B10）关乎留存**：会话「消失」直接制造「产品丢我数据」的恐慌感，
   这类恐慌是卸载级情绪，优先级高于一切美化。
4. 新手任务卡（B2 关联）是激活漏斗抓手，修复后应加完成率埋点，市场侧月度看激活数据。

### 用户视角

1. 用户不区分「关了/坏了/藏了」，只有「用不了」。所有不可用状态必须人话：
   「更新到最新版即可使用」「此功能未开通」——绝不出现配置键名/英文错误码（今天清了
   两处：DASHSCOPE 提示、contacts_disabled 裸码；对账单门禁把这条固化）。
2. 心智模型对齐：会话列表=微信（常驻）；后台=工具箱（用得上的才摆出来）；
   设置=改了就生效（生效不了要当场说清为什么）。
3. 操作零门槛纪律（老板原则，已两次实锤纠偏）：修复一律「点更新」送达；
   排查一律「发诊断码」替代「回答问题」；永不让用户改文件/记路径。

### 美术设计 UI/UX 视角

1. **状态徽章体系**（一次建设处处复用）：「生效中/需更新/未开通/待重启」四态统一视觉
   语言（色板+图标+文案模板），消化 B2/B3/B8 三类「状态不可见」问题；浅色/深色双主题
   同步出稿。
2. **浅色主题全站审计**：以 B7 为入口，所有弹层/菜单/下拉/徽章双主题截图对照，产出
   断层清单统一走主题 token（项目已有 token 纪律，缺浅色覆盖验收）；先修「更多操作」菜单。
3. **后台信息架构收纳**：客户端形态导航三层——「日常」（收件箱/人设/知识库/学习队列）、
   「高级」（AI 回复策略/意图映射，折叠）、「隐藏」（实时日志/开发者工具/运维页，客户端
   不渲染）。nav_schema 已支持简洁/完整双模式，扩一档 flavor 维度即可。
4. 会话列表 IM 化的视觉配套：常驻列表需要「已处理」低噪表达（灰点/淡化时间戳，
   拒绝红点泛滥）；当前会话高亮锚定；空态文案只在真正零会话时出现。

## 四、开发工作清单（新对话执行）

### P0（先做，客户可感）

**1. 会话列表 IM 范式切换 + 当前会话钉住**（B10+B8 后半）
- 默认视图=全部会话常驻；处理状态角标表达；仅主动删除/归档移除；「待处理」降为可选筛选
  （保留给内部坐席习惯，按部署 flavor 定默认：客户端=全部，内部坐席=可保持队列）。
- 当前打开的会话任何筛选下钉在列表（filtered-out 时淡化显示）。
- 涉及：`unified_inbox.html`（热区！先 agent_probe 意向板协调）+ `unified-inbox.css`
  （`?v=` 双戳纪律）+ ui-build.txt；发版走 desktop 镜像。
- 验收：处理完会话行不消失；「私聊都处理完了」空态仅零会话时出现；密度 ratchet 不回退
  （header 134/窄屏 149/会话行 74 天花板只降不升）。

**2. 跨平台档案（contacts/origin）去向拍板执行**
- 老板二选一：①对客户端开通（`contacts.enabled`+`origin_profile.enabled` 进桌面种子；
  评估 contacts 随包启动的连带面——gateway/limiter 等子模块是否各有自己的闸）
  ②客户端彻底隐藏面板（panel-manifest 按 flavor 不挂载）。
- 半开半藏状态已被本批前端收口兜底，但正式去向需定。

**3. 设置生效可见性**（B3 根治）
- 人设页保存「默认全局人设」：响应带「N 个账号有账号级绑定、M 个会话有专属人设」，
  前端提示+提供「同时应用到全部账号」按钮（走既有账号人设热应用链批量执行）。
- 通用化：设置类保存 toast 统一三态（已生效/需重启/被更高优先级覆盖+指路）。

**4. 学习闭环补链**（B5）
- 学习队列「已通过」条目行→对应知识库条目跳转链接（approve 时已回写 entry_id，纯前端）。
- 通过学习的确认 toast 里直接给「查看知识库条目」链接。

**5. 打包能力对账单门禁**（防复发，工程杠杆最高）
- 新门禁测试：遍历 feature_registry+关键能力键，比对 example vs desktop.min/cloud_light，
  客户档关闭的键必须在登记表（`_PACKAGED_CAPABILITY_LEDGER`）声明三选一处置+理由；
  未登记→红。首轮登记现状即固化今天四例的修法。

### P1（本周内）

**6. 后台页面收纳**（B4+B6）：nav_schema 增 flavor 维度——客户端隐藏 实时日志/开发者
  工具/运维页，意图映射折叠进「高级」；内部坐席不变。
**7. 浅色主题审计**（B7）：先修收件箱「更多操作」菜单断层，再全站弹层双主题对照清单。
  ✅ 2026-08-20 第一批已上线（ui-build `20260820-1218`）。断层根因不是「某处颜色写错」，
  是 `--bg-hover` / `--line-soft` / `--border-soft` / `--text-dim` / `--text-med` /
  `--ink-slate5` 六个变量**全站零定义**、只以「引用带字面回落值」的形态散落在
  `unified-inbox.css`，于是恒走回落值、而回落值各写各的（hover 写 `rgba(0,0,0,.04)`
  暗色下＝没有反馈；分隔线写 `#eee` 暗色下一道白线；弱化字写死 `#64748b` 暗色 ~3.3:1）。
  已在 `unified_inbox.html` 亮/暗两块补齐定义，全站消费点零改动自动跟随。「更多操作」
  菜单本身的橙色项硬编码 `#d97706` 压白底仅 3.4:1 → 改走 `--tk-warn-ink`。
  **「全站对照清单」不做成截图文档，做成机器可检的账本**：`tests/test_css_token_refs.py`
  从只守 `--tk-`/`--bdg-` 两族扩到全部 CSS 变量（08-19「亮色右键菜单黑底黑字」引用的
  `--card-bg`/`--th-hover` 根本不在两族里，旧门禁一句话都没说），未定义变量必须落三本
  账之一；其中 `_PENDING_UNDEFINED` 就是待修清单，当前 10 条——membership
  (`--bg2`/`--chip-bg`/`--ok-bg`)、knowledge(`--warn-bg`)、strategy_analytics(`--cyan`)、
  reply_settings(`--emerald`)、templates(`--d`)、settings(`--tx`/`--th-bg-teal6`)、
  messenger 渠道页(`--mt`)。**`--mt`/`--tx`/`--th-bg-teal6` 连回落值都没有**＝该属性
  直接不生效（不是配色偏差，是颜色压根没应用），下一批优先。修一条删一条，账本过期会红。
  ✅ 第二批同日完成（`20260820-1232`），**账本已清空**：无回落三个里 `--tx`/`--mt`/`--d`
  是引用侧手滑（全站从没有过这三个名字）→ 就地换成本壳既有词汇 `--t`/`--t3`/`--red`；
  `--th-bg-teal6` 是**映射表缺项** → 补进 `tools/inline_color_codemod.py` 并 `--emit-css`
  重生成 `theme-tokens.css`（该 CSS 是生成物，勿手改）。有回落的六个在 `base.html`
  亮暗两块补定义，能接现有语义的一律接**别名**（`--ok`→`--green`、`--ok-bg`→`--gs`、
  `--warn-bg`→`--as`、`--chip-bg`→`--bg2`、`--emerald`→`--green`），只有 `--bg2`/`--cyan`
  给字面值——多一个独立色号就多一处会漂的真相。B7 至此收口，后续只剩「新页别再造新
  token」的门禁维持。
**8. 值守链增强**（本对话陆续做，新对话可不管）：AI 带工单上下文应答；修复通知后
  24/72h 自动回访 @提问人；回填保 sender；压制消息落镜像。
**9. 支持通道完善**：「关于」页机器码常显可复制；报错 toast 内嵌「一键上传给客服」；
  帮助菜单加诊断上传入口。
  ✅ 2026-08-20 完成（ui-build `20260820-1338`）。**做法比原条目多一层收口**：三个入口
  （关于页 / 报错 toast / 帮助菜单）如果各写各的，文案与上传口径必然分叉，故先把「支持面板」
  做成**共享分片** `src/web/templates/_support.html`（两个壳 base.html + workspace_base.html
  同 include），三个入口都只是 `openSupportPanel(note)` 的调用点，Ctrl+K 顺带白捡第四个入口。
  - **后端另开 `/api/support/*` 而不是复用 `/api/admin/diagnostic-upload`**：`admin.py` 的
    `_agent_api_allowed` 把 `ROLE_AGENT` 挡在 `/api/admin/*` 外——报障的人恰恰主要是坐席，
    复用等于「要么坐席用不了、要么给坐席开管理员面」。新命名空间只暴露 info + diag-upload。
  - **上传实现收成单一事实源** `src/utils/diag_upload.py`（admin 老端点一并改走它）：过程中
    发现老端点失败分支调 `tr()` 却没 import，即「官网不可达」时用户看到的不是提示而是 500——
    合并实现顺手修掉。上传走后端 server-to-server 转投（工作台是 127.0.0.1 源，浏览器直
    POST 官网必撞 CORS 预检）。
  - **诊断包带上「报障现场」**：从报错 toast 点进来时前端把 `页面路径 | 报错文案` 作为 `note`
    一起传（`sanitize_note` 压成单行 + 200 字硬上限——出事那刻 toast 里可能是整段响应体，
    无上限＝把业务数据搬进诊断包），客服因此不必先问「你当时在做什么」。
  - **toast 内嵌 CTA 用 MutationObserver 而非改两套 toast 实现**：`base.html::showToast` 与
    `crm-widgets.js::CRMW.toast` 是两套独立实现且后者容器懒建，观察两个容器的 childList 给
    `.err` 条追加「⤴ 报障」，两套实现零改动、将来第三套也自动获益。配套把 `showToast` 的
    err 档停留时间抬到 ≥8s（**红条 3.5 秒就消失，CTA 根本点不到**——这是「加了按钮但没人
    点得到」级缺陷，只抬底不压顶）。
  - **桌面壳原生「关于」同步**（`desktop/main.js`）：机器码进对话框正文 + 「复制详情」
    + 「上传诊断给客服」，帮助菜单加同名项；上传是 90s 量级的长操作，故先弹系统通知再
    走流程（对话框按钮点完即关，没有通知用户会以为没反应），按钮派发改**按 id** 而非
    按索引（能力可选＝按钮数量可变，索引派发是定时炸弹）。
  - **能力位不许说谎**：`/api/support/info.upload` 由 `find_spec("src.utils.diagnostic_bundle")`
    实测得出而非常量 True——精简装机漏带打包器时后端根本出不了包，前端据此**不渲染**那颗
    主按钮、直接显示「本版本请复制全部信息发客服」；前端另把 404/403 单独标 `off`（模板热
    更新先于 .py 重启是本仓常态，那个窗口里 support 路由还不存在），避免用户对着「上传失败」
    反复重试。探测异常一律 fail-open（宁可点一次拿真错，也不平白关掉报障入口）。
  - 门禁：`tests/test_support_routes.py`（17 例，含「坐席白名单含 /api/support」「admin 端点
    必须复用共享实现」「upstream_unreachable 键中英齐备」「upload 位跟随真实能力」四条静态
    反漂移）。收口时另撞到两条本线自造的红并当场修掉：菜单词条带 🩺 顶穿 workspace emoji
    棘轮（顶栏图标位本就是 `data-ui-icon="help"`，词条再带 emoji＝双图标）→ 词条去 emoji；
    `_support.html` 末次编辑晚于构建戳 → 重 bump。

**11. 桌面包捆绑 ffmpeg**（B14 定案后升 P0）：resources 携带静态 ffmpeg 二进制，
  音频转换器解析顺序改「包内 → PATH」；打包门禁断言产物含 ffmpeg 且转换真跑通。
  这是客户机语音发送可用的硬前提（缺它=原样传 WAV 被平台拒）。
**12. 「搁置并停止自动回复」选项**（B17）：搁置弹窗加档；实现复用 takeover 静音
  机制（搁置时会话切 manual，解除恢复原档）；与 B10 一致性原则：用户主动动作可
  改变列表/行为，系统自动不行。

### P2（拍板后）

**10. 远程协助 v1**：一次性授权码→官网中转只读会诊；架构评审+安全边界单独立项。

## 五、施工纪律（执行对话必读）

- 共享树协议：动工前 `scripts\agent_probe.ps1` 探活+`-Intent` 登记；`unified_inbox.html`
  是热区（今晨群组频道线活跃）；模板/i18n/共享组件热更新**直接上生产**，每步自洽。
- 前端双戳：CSS/组件改动 bump `?v=` + `ui-build.txt`；shared/copilot 改动跑
  `desktop && node copy-shared.js` 镜像同步。
- `.py` 改动攒批，重启窗口 04:00/12:30/22:30，先 `restart_preflight.ps1`。
- 发版：`dist:win:clean`（客户包）→ `publish_chatx.ps1 -DistDir dist-clean`（含应用内
  更新公告）；本批已完成项（§二）随包自动带上。
- 收口跑 `gate_sweep.ps1`；新增客户可见文案 zh+en 双语，绝不出现配置键名/裸错误码。

## 六、验收口径

- skuio 逐项回访确认（值守对话负责）；同类问题内测群复发为零。
- 「找不到功能/功能消失」类反馈周环比归零；诊断码附码率 ≥60%。
- 对账单门禁上线后：任何新「客户档关闭」能力无登记不能合入。
