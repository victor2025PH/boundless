# 实施73：小智三大功能升格——信息架构重构（教学模式 / 替我做 / 手机操控）

> 日期：2026-08-27 ｜ 状态：**P0 已实施上线（热更）；P1-P4 待排**
> 实施记录见 §9（含三处与方案原判断不符的订正）。
>
> ⚠ **本文的分析部分（§1-§2）标尺有误，已由 [实施74](实施74_小智定位纠偏与报障闭环P0_五视角重分析_2026-08.md) 纠正**：
> 本文用「代操作坐席日常工作」当分母，而小智的真实定位是**使用帮助 + 设置协助 +
> 报障通道**；且当时**还没有用户**（开发阶段），把 `qa_log` 仅 8 条误读成了"功能
> 没人用"。凡以"代操作"为前提的结论作废；与定位无关的部分（重复入口、虚线观感、
> hint 截断、埋点缺分母、令牌三套）仍然成立且 §9 的 P0 已修。
> 关联：实施58（小智全能助教 P0-P6）、实施59（对话球活体能量核）、实施62（四功能深化 P0）
> 老板拍板（2026-08-27）：
> ① 三大功能要**突出**，做出更显眼的模式；
> ② 「大家常问」独立成面板，**排在「我的」之后**；
> ③ 每个面板**保持清爽**；
> ④ 手机操控采用**标题栏图标 + 连接状态点**方案（不占模式条第四格）；
> ⑤ 「常问」做**完整面板**（本页高频 + 全站高频 + 搜索 + 空态 seed，允许动后端）。

## 0. 一句话

三大功能不是做得不好，是**没人找得到**——线上 26 天 101 次球初始化，三大功能入口
点击合计 **7 次**、手机扫码配对 **0 次**；但凡进去的人都用得很深（教学开 2 次产生
8 次讲解 + 9 次追问，替我做 plan 4 → done 4 完成率 100%）。**漏斗断在第一步，
价值验证已过关**，所以本批的重心是把「小智怎么帮我」升格为一级导航，而不是继续加功能。

---

## 1. 线上读数（证据，先于观点）

数据源：`D:\chengjie-instances\zhiliao\data\config\ui_event_trend.db`（只读查询，
`ui_event_trend_daily`），窗口 **2026-08-01 ~ 2026-08-27，25 个有数据日**。

| 事件 | 计数 | 语义 |
|---|---:|---|
| `asb_orb_boot_lv2` | 101 | 球初始化（**页面加载即发，不是用户意图**） |
| `asb_teach_ask` | 9 | 教学气泡里追问小智 |
| `asb_teach_click` | 8 | 教学模式下点页面控件看讲解 |
| `asb_agent_open` / `plan` / `done` | 4 / 4 / 4 | 替我做：打开 → 规划 → 完成 |
| `asb_agent_resume` / `replay` | 4 / 2 | 跨页续跑 / 重放流星 |
| `asb_teach_on` / `off` | 2 / 1 | 教学模式开关 |
| `asb_pair_open` | 1 | 手机配对弹层打开 |
| `asb_pair_scan` | **0** | **手机扫码配对成功（一次都没有）** |
| `asb_agent_step_ok` / `step_skip` / `skip` | 1 / 1 / 1 | 动作步确认/跳过 |

对照同窗口工作台其他埋点：`goal_card_expose` **502**、`nav_attn_click` 91、
`iflt_more_open` 50。

**三条结论**：

1. **入口触达率极低**：三大功能入口点击合计 = 2 + 4 + 1 = **7**。
2. **进去之后留存很好**：教学人均 4 次点击 + 4.5 次追问；替我做完成率 100%、
   零失败零撤销。
3. **手机操控完全空转**：`pair_open` 1 次、`pair_scan` 0 次——最容易做 demo 的功能
   （扫码即用）从没被完整演示过。

### 1.0 ⚠ 按日拆解后的订正（比上面三条更重要）

P0 实施时把同一份数据按天拆开，结论**实质性改变**：

| 日期 | orb boot | 功能类事件 |
|---|---:|---|
| 2026-08-22 | 45 | 0 |
| 2026-08-23 | 49 | **全部 26 次（教学 12 / 代办 17 / 配对 1）** |
| 2026-08-26 | 6 | 0 |
| 2026-08-27 | 1 | 0 |

**全部非 orb 的 `asb_*` 事件集中在 08-23 单日**——那正是实施62 的开发日，
即这 7 次「功能触达」**是开发者自己 dogfood 的**，不是坐席用量。
08-23 之后 7 次球加载里，真实功能使用 = **0**。

所以正确的表述不是「触达率低」，而是：**三大功能自上线起从未被真实坐席用过一次**，
且此前**没有任何观测能发现这件事**（唯一有数的 `asb_orb_boot` 是页面加载即发的）。
这让 P0-1 从「补个埋点」升级为**本方案里最不该省的一步**。

### 1.1 ⚠ 两个数据可信度缺口（必须先修，否则后续裁决无效）

**缺口 A：没有分母。** `assistant-ball.js` 的 `orbBeacon` 只发 5 个 `asb_orb_*`
事件（boot / listen / degrade / say / calm），**没有任何一个用户意图埋点**：
没有「点球打开面板」、没有「提问」、没有「点了大家常问 chip」、没有「切 tab」、
没有「提交报障」。因此 101 是「页面加载了球」，**真实打开率至今未知**，
本方案的任何改动都无法量化验收。

**缺口 B：口径自相矛盾。** `asb_teach_click` = 8，但 `asb_teach_term_hit` 与
`asb_teach_term_miss` **都是 0**。按 `assistant-teach.js:384-386`，每次 `showBubble`
必发其中之一，两者之和应等于 click 数。已排除落库封顶（`ui_event_trend.py`
`_DAY_ACTION_CAP=150`，实际 distinct action 仅约 25）。剩余可能：坐席机跑的
teach.js 版本早于 term_miss 上线（实施62 §1.1 新增），或 beacon 链路在该路径断了。
**校准前，上述数字只作方向性证据，不作精确裁决依据。**

**P0-2 结论（2026-08-27）**：按日拆解 + 代码复核后判定为**同日版本错位**，
不是链路断。依据：① 全部 8 次 click 与 term_miss 的上线**同在 08-23**，
而当天该文件正被热更多次改写（VER 20260823g 是当日第 7 版），点击发生在
beacon 落地之前完全成立；② 现网实测三模块均为最新版且 `showBubble` 的
hit/miss 分支健在；③ 无法用 git 复核（见 §9.2 —— `shared/assistant/` 从未进过
git）。**处置：不改代码**，等 P0-1 的新埋点跑出第一周真实数据后自然验证
（届时 `term_hit + term_miss` 应等于 `teach_click`；若仍不等，才是真断链）。

---

## 2. 五视角问题清单

### 2.1 开发工程师视角

1. **三大功能是「外挂」进面板的，不是面板的一部分。**
   `assistant-ball.js` 只渲染骨架（`renderPanel()` @1263）；教学与替我做由
   `assistant-teach.js` / `assistant-agent.js` 各自 `MutationObserver` 监听
   `.asb-panel`，再 `panel.insertBefore(row, tools)` 插入 `.xzt-row` / `.xza-row`
   （teach @185、agent @426）。**升格为一级模式 = 推翻这个注入模型**，而
   `tests/test_orb_sibling_dom_contract.py` 正是钉住该 DOM 契约的——必须先改协议
   与门禁，再动渲染。

2. **版本戳有 7 个手工同步点**（实施时才发现是 7 不是 6）：三个文件各一个 `VER`
   + 两壳 loader 各一份 `?v=`（`base.html` ~1702、`workspace_base.html` ~4266）
   + **`src/web/routes/assistant_pair_routes.py::_AGENT_JS_VER`**（手机页 `/xz`
   服务端注入 agent 组件时自带的戳）。第 7 个是 P0 实施时被
   `test_assistant_pairing.py` 抓出来的——方案阶段漏数了。
   漏改一处，不是门禁红就是线上跑旧版。

3. **样式三套并行**：`--as-*`（ball）/ `--xzt-*`（teach）/ `--xza-*`（agent），
   三份 `injectCss()` 各自映射同一批宿主 token。**不先合并就做不出视觉统一的模式条。**

4. **埋点缺分母 + 口径存疑**（见 §1.1）。

5. **热更与打包双轨 → 分叉期**。`shared/assistant` 在 zhiliao 走 HTTP 热更即时生效，
   坐席机桌面包走 PyInstaller `_MEIPASS` 拷贝、需新安装包。整屏重构会出现
   「总部新版 / 坐席旧版」窗口，且两版 DOM 契约不兼容。

6. **规模已到临界**：ball 2445 + teach 737 + agent 1763 = **4945 行**，全 ES5、
   字符串拼 HTML、无构建。继续往 `renderPanel` 里堆模式路由会加速腐化。

7. **i18n 双轨无定论**：`i18n_packs/assistant_ball.py` 的 55 键（`asb.*` + `ov2_as_*`）
   服务于路由 `tr()` 与 ops 卡；**面板 DOM 文案在各 JS 内的 `I18N` 对象**。新面板词条
   进哪边没有定论 → 容易写死中文，伤白标与出海。

8. **常问数据源存在但不够用**：`assistant_routes.py:231` 的
   `qa_log.top_questions(days=14, limit=5, page=...)` 是页级真实高频，
   但**无全站口径、无分类、无搜索**——完整面板必须扩端点。

### 2.2 市场经理视角

1. **最强差异化被做成脚注**：三大功能是相对「又一个客服系统」的全部差异，现在是
   面板最底部两行**虚线小按钮**，视觉权重低于上方「问功能 / 报障 / 学操作」三张实心卡。
2. **手机操控零成功配对 = 白做**：这是天然的销售「哇」时刻（扫码 → 手机说句话 →
   电脑设置改了），至今一次完整演示都没有。
3. **「替我做」没说清边界**：后端 20 个动作（4 个 L0 查询 + 1 导航 + 15 个 L2 写入），
   用户只看到「一句话交给小智」——不知道能做什么就不敢用，也不敢卖。
4. **「手机操控」是技术词不是收益词**：听起来像远程桌面，实际价值是「不用坐在
   电脑前也能指挥」。名字本身在劝退。
5. **新手心智没被接住**：线上「大家常问」真实条目里有 **「我是新手，不会用。」**
   ——产品自己采集到的最强信号，面板里却没有任何新手路径。
6. **没有价值回收**：`src/ops/value_report.py` 的 AI 价值周报里，小智替坐席做了多少次
   操作、省了多少步，一条都没有。老板看不到就不会推。
7. **白标风险**：面板名已走 `dispName()`/brand，但模式名若写死中文会露馅。

### 2.3 用户视角（坐席 / 老板）

1. 打开先读 **4 行欢迎文字**才知道能干嘛。
2. **同一件事两个入口两个名字**：大卡「学操作 · 点哪教哪」与底栏「教学模式 ·
   点哪学哪」点下去是同一个 `XZTeach.start()`。
3. **最该看到的承诺被截断**：「进入后点击页面任意位置看讲解，不会真…」——
   「不会真的执行操作」被吃掉，这正是没人敢点教学模式的直接原因之一。
4. **虚线框 = 未完成**：`border:1px dashed`（teach.js:146、agent.js:255）在所有设计
   系统里都表示占位/拖放区。主功能长成这样，第一反应是「还没做好」。
5. **点「替我做」会发生什么不知道**：会不会直接改我的设置？后端其实有确认卡和撤销，
   但点之前看不到任何「会先给你看计划」的承诺。
6. **扫完码手机上能干嘛，零字说明。**
7. **「做过什么」是第四个按钮**：真要撤销误操作时根本找不到。
8. **「动效：饱满」看不懂**：画质档偏好常驻功能区。
9. **大家常问一半是别人的问题**（如「怎么给客户退款打八折并兑现」）→ 觉得小智不懂我。
10. **窄屏很痛**：桌面壳 405px 下要滚很久才到底部功能按钮。
11. **两个 AI 入口挨着**：面板下方紧贴右栏 copilot 的「AI 生成图片」卡，不知该用哪个。

### 2.4 美术设计视角

1. **一屏四套形态语言**：tab 下划线 / h3 实心圆角卡 / chips 实线胶囊 / 三大功能虚线胶囊。
2. **虚线是语义错配**（同 2.3-4，既是 UX 也是视觉问题）。
3. **强调色被稀释**：全屏只有一个蓝紫 `#4f6ef7`，在四个虚线按钮上平均分配——
   四个都强调等于一个都不强调。
4. **emoji 当图标**：💬🐞🎓⚡📱📜 跨平台三套渲染，无法统一描边/尺寸/色彩，
   且 🎓 出现两次（大卡 + 底栏）。
5. **垂直节奏接近等距**：各区块间距都在 `.4rem–.8rem`，无呼吸感、眼睛找不到落点。
6. **打开面板 = 品牌消失**：实施59 给球做了活体能量核（极光层 + 粒子 + 声波涟漪 +
   bloom），**面板里一点都没有**，是普通灰底卡片。
7. **深色无自持基线**：靠宿主 `--tk-*` / `--card` 继承，虚线在深底上几乎不可见。
8. **空态全是「…」或纯文字**：报障 / 我的 / 常问 三面板零插画。

### 2.5 UI/UX 设计专家视角

1. **信息架构错层——五类东西平铺同层**：模式（怎么帮）、通道（手机）、内容（常问）、
   记录（工单/历史）、设置（动效）。**这才是「不清爽」的根因**：不是元素太多，是类别太杂。
2. **重复入口普查**：报障 2 个（tab + 大卡）、教学 2 个（大卡 + 虚线按钮）、
   问答 3 个（大卡 + chips + 输入框）。删重复即可砍掉近三分之一可点目标。
3. **首屏可点目标约 17 个**，且层级差异极小 → Hick's law 直接税。
4. **没有自治档位（autonomy）显示**。2026 年 agent UX 的核心共识（Smashing Magazine
   *Designing For Agentic AI* 的 Autonomy Dial、aiuxdesign.guide 的 Autonomy Spectrum、
   Cursor 的 Ask/Edit/Agent）是**在动作发生的地方显示当前权限档**。后端已有 L0/L1/L2
   分级与确认卡，**UI 上完全没暴露**，用户只能靠猜。
5. **没有独立活动面板**。多份 2026 研究（Zylos、Fuselab、Hatchworks、Brainy）结论一致：
   agent 必须有**与对话流分离的 activity panel**（计划 / 进度 / 收据 / 撤销）。
   我们把它压成了一个叫「做过什么」的小按钮。
6. **输入框无模式感知**：placeholder 恒为「输入问题，回车发送…」。
7. **空态被浪费**：空态是最好的教学位，现被 4 行文案 + 通用 chips 占满，
   而非「本页你可以问什么 / 学什么 / 让我做什么」。
8. **可访问性欠账**：tab 用 `<button>` 无 `role="tablist"` / `aria-selected`；
   模式条做 segmented 需 `role="radiogroup"` + `aria-checked` + 方向键 +
   `prefers-reduced-motion` 时禁滑块动画。
9. **窄屏只拉宽未重排**：`@media` 仅 `left:8px;right:8px`，密度一点没变。
10. **反馈闭环未闭**：有 👍👎，但「这条没解决 → 转报障」的 `to-report` 不显眼。

---

## 3. 目标信息架构

**设计主张**：把「小智怎么帮我」升格为一级导航，把「我的东西」降格为次级页签，
让每个面板只装一类内容。

```
┌──────────────────────────────────────┐
│  ✦ 小智 · 替我做          [📱•] [✕]  │  手机=通道，标题栏图标 + 连接状态点
├──────────────────────────────────────┤
│  ┌─────────┬─────────┬─────────┐     │  ← 模式条（主视觉，滑块指示）
│  │ 💬 问答 │ 🎓 教学 │ ⚡ 替我做│     │
│  └─────────┴─────────┴─────────┘     │
│                                      │
│        （只放当前模式的内容）          │
│                                      │
├──────────────────────────────────────┤
│  [说一件要我做的事…]        [🎤] [→] │  ← placeholder 随模式变
│  报障 · 我的 · 常问 · ⚙              │  ← 次级页签（弱视觉）
└──────────────────────────────────────┘
```

### 3.1 为什么手机操控不占模式条第四格（已拍板）

它**不是第四种能力**，它是「替我做」的**远程输入端**——同一个
`/api/assistant/agent/plan` + `/act`，只是嘴巴换到手机上。做成第四个平级模式，
用户会以为是另一种能力，反而更看不出它与「替我做」的关系（这大概正是
`pair_open` 只有 1 次的原因之一）。

**落法**：标题栏图标 + 连接状态点（未配对灰 / 已配对绿 +「手机已连」），
**同时**在「替我做」模式内给一张大卡「换个说法：用手机指挥」。两处曝光，语义诚实。

### 3.2 各面板承载物（清爽 = 每个面板只装一类）

| 面板 | 只装 | 从别处搬走 / 删除 |
|---|---|---|
| **问答** | 一句话欢迎 + 本页相关 3 条建议问题 + 对话流 | 删 3 张 h3 卡（全是重复入口）；5 条 chips 减为 3 条并移走「全站高频」到常问 |
| **教学** | 状态卡 + 「开始点哪学哪」大按钮 + 副按钮（带我走一遍 / 教我这个页）+ 本页可讲解 N 处 + 安全承诺全文 | 从底栏虚线行升格 |
| **替我做** | 任务态优先（在跑=进度时间线）；空闲=说一句 + 本页能力 chips + 「最近做过」3 条 + 自治档说明 + 手机指挥卡 | 「做过什么」按钮并入；从底栏虚线行升格 |
| **报障** | 描述 + 截图 + 环境 + 提交 | 去掉与问答重复的引导 |
| **我的** | 工单列表 + 刷新 | 不变 |
| **常问**（新） | 本页高频 / 全站高频分组 + 搜索 + 点击直接切问答并发问 + 空态 seed | 承接原 chips 的「全站」部分 |
| **⚙**（新） | 动效档 + 语音开关 + 术语提示 +（admin）快捷键 / 命令面板 / 引导 / 诊断上传 | 「动效：饱满」从功能区移入 |

**量化收益**：首屏可点目标 ~17 → ~12，**主视觉层级只剩 3 个大目标**。
「清爽」的真实定义不是元素少，是**层级清楚**。

---

## 4. 分批方案

### P0 · 地基与可信度 —— ✅ **已实施上线（2026-08-27，全热更）**

| # | 事项 | 状态 |
|---|---|---|
| P0-1 | **补齐意图埋点**：`asb_open` / `asb_open_ext` / `asb_tab_<x>` / `asb_ask` / `asb_chip` / `asb_h3_<x>` / `asb_report_submit` / `asb_report_ok` | ✅ done。`asb_mode_*`、`asb_faq_search` 属 P1（模式条与常问面板尚不存在） |
| P0-2 | **校准口径** | ✅ done，判定为同日版本错位、**不改代码**（见 §1.1） |
| P0-3 | **修两处当场可见的伤**：hint 不截断 + `dashed` → 实线 | ✅ done（teach/agent 两行同规格） |
| P0-4 | **合并三套 CSS 变量**为 `--xz-*` | ✅ done，203 处替换、零残留 |
| P0-5 | **门禁** | ✅ done：新增 `tests/test_assistant_beacons.py`（13 例）+ 补 ui-build 观测面缺口。DOM/模式路由协议**顺延至 P1 开工时**——协议内容取决于模式条最终形态，此刻空写会写成废纸 |

**埋点归因模型**（后续读数按此口径，别另算一套）：`asb_ask` 是问答主链的**唯一
计数点**（chip / 输入框 / 外部投问都汇入）；来源归因由各入口自己的
`asb_chip` / `asb_h3_*` 承担。因此「点 chip 提问」会同时产生 `asb_chip` + `asb_ask`
两枚——这是**刻意的**（来源一枚、主链一枚），不是重复计数。

**两个刻意的静默路径**（防分母被灌水）：`renderPanel` 初始化的
`switchTab('chat', true)` 不计埋点（否则 `asb_tab_chat` 恒等于开面板数，
「坐席主动切页签」的信号被稀释）；教学/代办线的程序化投问走
`togglePanel(true, 'ext')` → 计 `asb_open_ext` 而非 `asb_open`（否则人工打开率虚高）。
`togglePanel` 另有 **关→开 跃迁守卫**，面板已开时重复调用不再计数。

**为什么 `asb_h3_*` 不在原方案清单里却加了**：欢迎三卡是 P1 计划**删除**的重复
入口，而「没人用」在删之前必须有证据——这三枚埋点就是删除决策的裁决依据。

### P1 · 信息架构重构（本批核心）

| # | 事项 | 要点 |
|---|---|---|
| P1-1 | **模式条上位**：问答 / 教学 / 替我做 三格 segmented | `role="radiogroup"` + `aria-checked` + 方向键；滑块指示 <200ms；`prefers-reduced-motion` 下禁动画 |
| P1-2 | **手机操控进标题栏**，带连接状态点 | 已拍板；配对态读现有 `/api/assistant/pair/sessions` |
| P1-3 | **删三张 h3 重复卡** | 学操作=教学模式、报障=页签、问功能=输入框 |
| P1-4 | **输入框 placeholder 模式感知** | 三句文案，成本极低，收益直接 |
| P1-5 | **次级页签行**：报障 · 我的 · **常问** · ⚙ | 满足「常问在我的之后」；动效档移入 ⚙ |
| P1-6 | **新建「常问」完整面板** | 见 §4.1（含后端半件） |
| P1-7 | **「做过什么」并入替我做**，做成常驻「最近做过」3 条 + 撤销 | 撤销必须在手边 |

#### 4.1 「常问」面板的后端半件（已拍板做完整版）

现状 `qa_log.top_questions(days=14, limit=5, page=...)` 只有页级口径。需要：

- **扩参**：`scope=page|global|both`、`limit` 放宽到 ~20、返回带 `n`（频次）与
  `page`（来源页）便于分组与排序；
- **搜索**：`GET /api/assistant/faq?q=` 走既有 help_kb BM25，与问答同一检索栈
  （**不新建第二套语料**）；
- **空态 seed**：库里零数据时回落 `seed_corpus` 的入门问题集，避免新装机空面板；
- **不做人工维护的 FAQ 库**——`qa_log` 是真实高频数据源，人工维护必然与实际漂移。

### P2 · 三个功能各自做深

| # | 事项 | 说明 |
|---|---|---|
| P2-1 | **教学**：状态卡 + 「本页 N 处可讲解」+ 安全承诺全文 + 副按钮两枚 | 「本页 12 处可讲解」是最好的进入理由 |
| P2-2 | **铺 `data-anchor`**：收件箱 / 回复设置 / 人设 三页 Top20 控件 | 实施58/62 明确推迟项；`term_miss` 就是没铺点的直接读数（但需先过 P0-2 校准） |
| P2-3 | **替我做：显式自治档**——只看 / 先问我（默认） / 做完告诉我 | 对齐 2026 Autonomy Dial 共识；后端 L0/L1/L2 已具备，属把已有能力显性化。档位**按任务类型存**而非全局（业界经验） |
| P2-4 | **替我做：计划预览 + 活动时间线 + 收据/撤销**三件套常驻 | activity panel 必须与对话流分离 |
| P2-5 | **手机：一句话收益 + 首次引导 + 配对成功桌面回执**；改名候选「用手机指挥」/「手机遥控台」 | 0 次成功配对说明这条链从名字到引导全断 |

### P3 · 美术与品牌

| # | 事项 |
|---|---|
| P3-1 | **图标系统替 emoji**：统一 20px 线性图标，`stroke: currentColor`，消掉 🎓 重复 |
| P3-2 | **品牌延续进面板**：顶部延续能量核的极光渐变，让「打开面板」不再是品牌断点 |
| P3-3 | **三面板空态**统一插画 + 一句引导文案 |
| P3-4 | **深色对比度自持基线**；层级靠 elevation + blur 而非描边（对齐 2026 material realism / 新玻璃拟态，但守 WCAG 对比度，并提供降透明选项） |
| P3-5 | **窄屏 405px 专门重排**（不只拉宽），模式条改紧凑档 |

### P4 · 数据裁决（两周后，不写代码）

- **模式使用分布** → 决定默认落哪个模式（现硬编码 `switchTab('chat')`）
- **手机配对成功率**（`asb_pair_open` → `asb_pair_scan` 转化）→ 决定 P2-5 是否加码
- **`asb_teach_term_miss` Top N** → 照单往 `help_terms.py` 补词条，闭环
- **替我做 undo 率**（业界经验 >15% 说明计划质量不过关）→ 决定是否收紧动作池
- **小智动作数接进 AI 价值周报**（`src/ops/value_report.py`）→ 给老板看的账单

---

## 5. 刻意不做（避免方案膨胀 / 避免推翻既有决策）

| 项 | 理由 |
|---|---|
| 像素级 computer-use | 实施58 已定：白名单动作调用是对的路线 |
| 手机页内麦克风 / 自签 HTTPS | 实施62 调研后已否决，维持键盘听写 |
| 通用 Flow DSL + 断点续传 | 已按 YAGNI 收敛为登录模式运行器，2 条流够用 |
| 人工维护的 FAQ 库 | `qa_log` 是真实数据源，人工维护必然漂移 |
| 手机操控做第四个模式格 | 信息架构错层，会让用户以为是另一种能力（已拍板走标题栏） |
| 把右栏 `cp-image` 并进小智 | 两条独立产品面，合并成本远大于收益；只需在视觉上做区隔 |

---

## 6. 施工纪律与风险

**并发**：`shared/assistant/**` 属高冲突热区。开工前
`scripts\agent_probe.ps1 -Intent "xiaozhi panel IA (shared/assistant/*)"` 登记意向板，
收工 `-Done`。

**发布双戳 + 六点同步**：三个文件的 `VER`、两壳 loader 的 `?v=`
（`base.html` ~1702 / `workspace_base.html` ~4266）、`ui-build.txt`。

**门禁**：`tests/test_assistant_teach_wireup.py`、`test_assistant_agent_wireup.py`、
`test_orb_sibling_dom_contract.py`、`test_assistant_actions.py`(25)、
`test_assistant_agent_planner.py`(14)、`test_assistant_flows.py`(7)、
`test_assistant_pairing.py`、`tools/verify_assistant_ui.py`（在 `gate_sweep -Full`）。
**P0-5 必须先改 DOM 契约门禁**，否则 P1 一落地就红。

**三条风险**：

1. **DOM 契约推翻**（最大）：teach/agent 的 `MutationObserver` 插行模型要改成模式路由。
   建议先单独定协议 + 改门禁 + 保留一版兼容垫片，再动渲染。
2. **坐席机分叉期**：`shared/assistant` 热更只惠及 zhiliao，坐席机（173/198/104/140）
   需新安装包。分叉期两版 DOM 不兼容 → 需提前排发版，或让新版对旧 loader 优雅降级。
3. **i18n 落点未定**：新增面板文案若继续写进 JS 内 `I18N`，白标与出海会露馅；
   建议本批一并定规矩（推荐：面板文案随 bootstrap 下发，源仍在 `i18n_packs`）。

**重启**：P0 的埋点补齐 + 常问端点扩参属 `.py` 改动，攒批走 `restart_preflight.ps1`
+ `restart_instance.ps1 -Instance zhiliao`，落在 04:00 / 12:30 / 22:30 三窗之一。
其余前端改动全部热更。

---

## 7. 验收判据

| 阶段 | 判据 |
|---|---|
| P0 完成 | `asb_open / asb_orb_boot` 得出**真实打开率**；`term_hit + term_miss == teach_click`（口径自洽） |
| P1 完成 | 首屏可点目标 ≤ 12；`asb_mode_*` 三档分布可读；常问面板 `asb_faq_*` 有非零流量 |
| P2 完成 | 教学 `term_miss` 占比下降（铺点生效）；替我做 undo 率 <15%；`asb_pair_scan` **首次非零** |
| P3 完成 | `verify_assistant_ui.py` 全绿；窄屏 405px 首屏不滚动即见模式条 + 输入区 |
| P4 | 三大功能入口点击合计从 **7/26天** 提升至可观测量级；小智动作进 AI 价值周报 |

**唯一的硬指标**：三大功能的**入口触达率**（点开面板 → 进入任一模式）。
当前无法计算（缺分母），P0 之后必须成为常驻读数。

---

## 8. 外部参照（2026 年做法，用于佐证而非照抄）

- **Autonomy Dial / Autonomy Spectrum**（Smashing Magazine 2026-02；aiuxdesign.guide）：
  四档 Observe&Suggest → Propose&Confirm → Act&Notify → Full Autonomy，
  **按任务类型存档位而非全局**，且档位必须显示在动作发生处。→ 对应 P2-3。
- **Activity panel 与对话流分离**（Zylos / Fuselab / Hatchworks / Brainy 四家结论一致）：
  chat 负责意图输入与澄清，activity panel 负责计划 / 进度 / 收据 / 撤销的可审计日志。
  → 对应 P1-7、P2-4。
- **Agent UX 成熟度三级**（Hatchworks）：L1 chat-first（只能做 demo）→
  **L2 guided agent（taskboard + 时间线 + 收据 + 人工检查点，v1 应瞄准这里）**→
  L3 trusted autonomy。→ 小智当前在 L1.5，本方案目标是 L2。
- **空态 3-4 条建议提示**是 chatbot UX 单项收益最高的模式；一旦有历史即让位。
  → 对应 P1-3（chips 5 减 3）与「常问」独立成面板。
- **Progressive disclosure**（Nielsen 1995 → 2026 agentic 版）：新手看到干净表层，
  深度一键可达、可折叠、记住用户偏好。→ 对应 §3.2 的面板分工与 ⚙ 收纳。
- **Segmented control 可访问性规范**：`role="radiogroup"` + `aria-checked` +
  方向键 + 标签不截断 + `prefers-reduced-motion` 禁滑块动画。→ 对应 P1-1。
- **2026 视觉走向**：material realism（层叠卡 + 柔和高度 + 模糊而非硬描边）、
  purposeful motion（<200ms 反馈动画，reduced-motion 为默认考量）、
  玻璃拟态回归但须守对比度并提供降透明选项。→ 对应 P3。

---

## 9. 实施记录（P0，2026-08-27）

### 9.1 改动清单

| 文件 | 改动 |
|---|---|
| `shared/assistant/assistant-ball.js` | +`beacon()`（orbBeacon 薄封装，共用通道与守卫）；`togglePanel(force, src)` 加关→开跃迁守卫与 ext 归因；`switchTab(tab, silent)`；`sendQuery` 计 `asb_ask`；chip / 三卡 / 报障各自埋点；令牌 `--as-*`→`--xz-*`（97 处）；VER→`20260827a` |
| `shared/assistant/assistant-teach.js` | 令牌 `--xzt-*`→`--xz-*`（32 处）；`.xzt-row button` dashed→solid+`--xz-input` 底；`.xzt-row .hint` 独占整行不再截断；VER→`20260827a` |
| `shared/assistant/assistant-agent.js` | 令牌 `--xza-*`→`--xz-*`（74 处）；同款 dashed/hint 修复（两行必须同规格）；VER→`20260827a` |
| `src/web/templates/base.html`、`workspace_base.html` | 六处 `?v=` 同步 `20260827a` |
| `src/web/routes/assistant_pair_routes.py` | `_AGENT_JS_VER`→`20260827a`（**第 7 个同步点**，见 §9.2） |
| `tests/test_assistant_beacons.py` | **新增**，13 例：意图埋点在场 / 单一 sendBeacon 通道 / 程序化路径静默 / 跃迁守卫 / 令牌单源 + 三模块等值 / 入口行非 dashed / hint 不截断 / 安全承诺文案完整 |
| `tests/test_ui_build_freshness.py` | `WATCHED_ROOTS` 补 `shared/assistant`（见 §9.3） |
| `src/web/static/workspace/ui-build.txt` | `20260827-1036`→`20260827-1037` |

**未动**：任何后端业务逻辑、任何 DOM 结构、任何文案（除 CSS 布局）。
P0 全部属热更范畴，**唯一需要重启才生效的是 `_AGENT_JS_VER`**（手机页 `/xz` 的
服务端注入戳，纯缓存破坏用，可搭任意后续重启的便车，无紧迫性）。

### 9.2 三处与方案原判断不符的订正

1. **版本同步点是 7 个不是 6 个**。方案 §2.1-2 只数了「三个 VER + 两壳 loader」，
   漏了 `assistant_pair_routes._AGENT_JS_VER`。它被既有门禁
   `test_assistant_pairing::test_pair_routes_ver_synced_with_agent_component`
   当场抓出——**这正是门禁互验的价值**：我按自己的方案数漏了，机器没漏。
2. **`shared/assistant/` 整个目录从未进过 git**（`git status` 显示 `??` untracked）。
   4945 行前端代码、直发生产、无版本历史——所以 P0-2 想用 `git log -S` 定位
   term_hit 的引入时间时**无从查起**。这是比埋点缺口更深的一层地基问题，
   已列入下一阶段建议。
3. **`shared/assistant` 不在 ui-build 新鲜度门禁的观测面内**。
   `test_ui_build_freshness.WATCHED_ROOTS` 有 `shared/copilot` 却没有
   `shared/assistant`，而两者同样经 HTTP 直发坐席、同样热更即上生产——
   即「改了小智前端忘 bump」此前**不会被任何门禁发现**，坐席标签页照跑旧 JS
   且无刷新提醒。本批已补（1 行 + 说明）。

### 9.3 验证

- `node --check` 三文件全过（令牌批量替换未破坏语法）。
- 小智全套门禁 **124 passed**（`test_assistant_beacons` / `teach_wireup` /
  `agent_wireup` / `orb_sibling_dom_contract` / `actions` / `agent_planner` /
  `flows` / `pairing` / `routes`）。
- ui-build 新鲜度门禁 2 passed（含补入 `shared/assistant` 后）。
- **生产实例热更实测**（`http://127.0.0.1:18799/assistant-shared/*`）：三模块
  HTTP 200、`VER=20260827a`、边框 solid、令牌仅 `--xz-*`、8 枚意图埋点全部在场。
- **埋点接收链活性实测**：`ui_event_trend_daily` 在 08-26 落了 22 种 action、
  08-27 当日已落 5 种 —— `/api/telemetry/ui-event` → 按日落库整条链健康。
  这是 §1.1 判定「term_hit/miss 归零是版本错位而非断链」的第三条佐证。
- **广域门禁 `gate_sweep.ps1`**：909 passed。5 红中 4 个是 xdist worker 在生产
  占用下崩溃（`node down: Not properly terminated`），自动复跑全部转绿；
  剩 1 个真红 `test_template_inline_color_ratchet`（`workspace_base.html: 5 > 天花板 4`）
  **不属本批**：红灯账本首见 **7.8h 前**，而本线意向登记仅 1.0h；本批对该模板
  的全部改动是 6 行 `s.src='...?v=20260827a'` 纯脚本地址字符串，零 `style=` 属性。
  按共享树纪律**只报告不代改**（该文件当时仍在他线活跃编辑窗内）。

### 9.5 P0 上线基线（冻结值，供两周后 before/after 对照）

上线时刻 `asb_*` 全量累计（**全部为 P0 之前的旧埋点**，新意图埋点从 0 开始）：

```
asb_orb_boot_lv2 101 | asb_teach_ask 9 | asb_teach_click 8
asb_agent_{resume,plan,open,done} 各 4 | asb_teach_on 2 | asb_agent_replay 2
asb_teach_off 1 | asb_pair_open 1 | asb_agent_{step_skip,step_ok,skip} 各 1
```

判读时切记 §1.0：**这 14 个数里除 orb_boot 外全部产生于 08-23 单日的开发自测**，
不是坐席用量基线——真正的基线要等新埋点跑出来。

### 9.4 首个可读数时间

意图埋点按日落库（`ui_event_trend`，重启无损）。**最早 2026-08-28 起**可读到
第一批真实数据，判读顺序：

1. `asb_open / asb_orb_boot` → **首个真实打开率**（此前完全未知）；
2. `asb_tab_*` 分布 → 坐席点开后去哪；
3. `asb_h3_*` → 欢迎三卡是否真的没人用（P1 删卡的裁决依据）；
4. `asb_chip` → 「大家常问」搬迁后要不要在首屏留入口；
5. `asb_teach_term_hit + term_miss == asb_teach_click` → 反向验证 §1.1 的
   「同日版本错位」判定；不等则说明真有断链。
