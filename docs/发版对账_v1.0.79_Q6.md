# 发版对账 v1.0.79 — Q-6 假发图闭环 + 相册建议词（D6V29H / J6RHBQ / GAWRU5② / CK8HCT / FFGMM9 → #263 #266）

> 指令：`docs/指令_Q-6_假发图闭环与相册建议词_2026-09-09.md`；底座 `docs/修复指令_Q批_并行_2026-09-09.md`（归属矩阵 Q-6 行、接口约定③）。建在 P-3（`07f2168b` / `5d63a9d5`，`docs/发版对账_v1.0.79_P3.md`）之上——词表 / 回执账本 / lie_caught 投递层动作（重发 | 实话 | 连续第二条转人工）**不重做**，只补生成侧清单、无匹配回喂、会话级封锁、建议词匹配与相册 UI。
> 证据：`tmp_diag\D6V29H\ CK8HCT\`（Nori×Mike 23:19–23:25 四轮零 `send_media`；`image_autosend` 对该会话零评估零 skip；相册 91 张正式触发词全空、建议词需逐张采纳；随机兜底自拍答「风景照」）。
> 预算 $160；执行方 Q-6 对话，09-10 12:11 开工（`agent_probe -Intent` 已登记）。**每个 commit 带 #263**（相册 UI 兼 #266）。

## §〇 为什么第四轮还在圆谎（三段，按代码事实）

| # | 病因 | 代码位置（P-3 落地后） | 本条怎么改 |
|---|---|---|---|
| 1 | **无匹配静默、不回喂模型** | `image_autosend.pick_registered_media` / `select_media`：关键词未命中且（索图时）通用池也挑不到 → `None`，调用方继续走文本；生成侧看不见「相册没这张」。D6V29H 对该会话**零评估零 skip 日志**，同刻 telegram 正常发图＝模块活着 | B：每次评估落 `[album_match] conv= query= candidates=n picked= fallback=none\|random`；无匹配 `note_album_miss` + 生成前 `persona_reply` 自探相册，注入「无可用照片（场景 X）」，只许拒绝（无 worker 钩子＝禁说稍后发） |
| 2 | **模型不见媒体事实** | `persona_reply` 不注入相册场景清单、不注入本会话已发媒体；P-3 只在投递层剥谎，生成时仍可承诺清单外场景 / 声称发过 | A/F：单一接线点 `_prompt_addenda` 追加三块（空段不出现）：相册可展示场景 kind 计数 / 已发媒体（空则「你从未给 TA 发过照片」）/ Q-1 `identity_addendum`。C/D：会话级 `media_claim_blocked` 10 轮 + spiral 同会话 2 次转人工；blocked 期间掏空 `_inject_goal_context`（不改 `skill_manager.py`） |
| 3 | **建议词没生效 + 随机自拍答风景** | `persona_media._partition` 只看正式 `triggers`；`triggers_suggest` 仅 UI；91 张全空 → 索图走通用池随机自拍。`extract_requested_scene` 无「风景」 | E：`album.autotag.apply=auto`（默认）建议词 0.6 权重参与匹配；整册完全无词且非索图才 `fallback=random`；`kind:selfie\|indoor\|outdoor\|food\|pet\|other`；索风景硬匹配 outdoor。UI `al-` 三段计数 + 一键采纳 + 场景下拉 + 多选批量；「缺触发词」→「待确认」 |

## 一、改动落点（每 commit 一行，落地即写；均带 #263）

| 项 | commit | 落点 |
|---|---|---|
| **C/D** 会话级 blocked 10 轮 + spiral 2 + 复诉词 + 生成侧不岔题 | `42b9640c` | 新 `src/inbox/media_claim_block.py`：进程内表 `note_lie_caught` / `is_blocked` / `tick_outbound` / `blocked_addendum`（`BLOCK_TURNS=10`，`SPIRAL_THRESHOLD=2`，不碰 `store.py`）。`companion_selfie` lie_caught 补 `didn't get it` / `you didn't send` / `没收到图`。`outbound_promise_guard.apply_blocked_media_rewrite` + 词表 `still loading` / `加载中`。`persona_reply` 入站质问记 spiral，第 2 次 `tag_needs_human(reason=media_lie_caught_repeat)`；blocked 期间 `_SkipGoalInject` 掏空 `sm._inject_goal_context`（含统一引擎路径）；出站后 rewrite + tick。新 `media_gate.outbound_ok_for_photo_ask`（10 条要照片门禁，Q-2 F 未开工故自建）。**不重做** P-3 `autosend_helpers` 重发/实话/连续 handoff |
| **B** `[album_match]` + 无匹配回喂 + 随机关 + 建议词 0.6 + 索风景 | （待填） | `persona_media._partition` 三池（keyword / suggest×0.6 / generic）；`select_media(allow_random, suggest_ok, required_scene_kind)`；`requested_scene_kind("风景")=outdoor` 时 kind 存货可作候选（不再随机自拍）。`image_autosend.pick_registered_media` 每次评估 `logger.info("[album_match] …")`；索图无匹配 `note_album_miss`。随机仅「整册完全无词且非索图」。`extract_requested_scene` / `scene_class_of` 认风景。`album_semantic_recall.candidate_text` 并上建议词与 `kind:` |
| **E** 建议词生效 + 三段计数 + 场景 kind + 采纳/批量路由 | （待填） | `media_auto_tag.resolve_album_ai_cfg.apply` 默认 `auto`；prompt/parse/`derive_tags`/`build_auto_meta` 产出 `kind` / `kind:` 标签 / `scene_kind`。`persona_media_routes` list 回 `counts={confirmed,ai_suggest,none}`；`POST …/media/adopt-suggest`；`POST …/media/batch-triggers`；PATCH `scene_kind`。`config.example.yaml` 注释 `apply: auto`（工作树该文件整文件翻动，**本线未整文件提交**，默认在代码里） |
| **E UI** `al-` 前缀 | （待填） | `personas.html`：`al-counts` 已确认 / AI 建议 / 无建议、`al-adopt-all` 一键采纳、`al-ai`「AI 标注」、`al-kind` 场景下拉、`al-chk` 多选 + `al-batch` 批量加触发词。i18n `persona_apply_modal`；「缺触发词」→「待确认」。新控件一律 `al-`（Q-2 见面政策 `mp-` 分段） |
| **A/F** `_prompt_addenda` 三块接线 | （待填） | `prompt_addenda.album_scene_addendum` / `sent_media_addendum` / `album_miss_addendum`（纯格式化，零 I/O，不 import persona_reply）。`persona_reply._prompt_addenda(persona, account, conv)` 单一接线点拼进 `_time_hint`（统一引擎 / 直连 / 兜底三条 extra_hint）。生成前自探相册，避免 B 线 autosend 在拟稿之后导致本轮仍圆谎。Q-1 `identity_addendum` 非空即追加 |

## 二、验收

| 测试 | 钉什么 |
|---|---|
| `tests/test_media_claim_block_q6.py` | 10 轮封锁 / spiral 2 / tick 到期 spiral 仍在 |
| `tests/test_media_gate.py` | 10 条谎无图不可出；10 条诚实拒绝可出 |
| `tests/test_album_match_q6.py` | 建议词 0.6 / 正式词优先 / 索图随机关 / **风景→outdoor 不抓自拍** / `[album_match]` 日志 / miss consume |
| `tests/test_prompt_addenda_q6.py` | 三块各出现一次、空段不出现、无匹配禁稍后发 |
| `tests/test_album_q6_ui.py` | `al-` 控件 + 三段计数词条 + 「待确认」 |
| 随改 | `test_media_auto_tag` `apply=auto`；`test_album_autotag_default_238` 仍认 `pma-ai-cover` |

**活体口径（R79 装载后）**：相册 91 张导入后计数 **已确认 0 / AI 建议 91 / 无建议 0**，一键采纳 → 已确认 91。索「风景」日志 `[album_match] … fallback=none`，不得再发自拍。Nori×Mike 类会话要图无存货 → 正文拒绝且无「发了/加载中」；第二次「没收到」→ 需人工，期间无 goal-inject 岔题。`.py` 须实例重启。

## 三、越界点名

| 文件 | 归属 | 本线动了什么 | 为什么 |
|---|---|---|---|
| `src/inbox/prompt_addenda.py` | **Q-1**（已收工） | 加三个纯格式化函数，不改 `identity_addendum` | 接口约定③：Q-1 提供身份补丁，Q-6 接线并可加相册/已发媒体函数；测试仍禁止本模块 import persona_reply |
| `src/ai/companion_selfie.py` / `outbound_promise_guard.py` | **Q-6**（矩阵本行） | 复诉词 + blocked rewrite + still loading；不改 P-3 模板函数语义 | 矩阵明文归属 Q-6 |
| `autosend_helpers.py` / `autosend_worker.py` / `drafts.py` / `skill_manager.py` / `store.py` | 他线 | **零改动** | 红线；goal-inject 在 persona_reply 调用侧过滤 |
| `personas.html` | 与 Q-2 分段 | 只动相册区 `al-` | Q-2 见面政策 `mp-` |
| `tests/test_admin_route_inventory.py` | 共享 | `stage_hunks --mine Q6_ALBUM` 只暂存本 hunk | 工作树另有 wecom / DY / **删 Q-14 块** 他人 hunk，未入 |
| `config/config.example.yaml` | 共享（整文件翻动） | **未提交** | 默认 `apply=auto` 在 `resolve_album_ai_cfg` 代码里 |

## 四、交 Q-0 / Q-1 / R79

- **Q-1**：blocked 期间 goal-inject 已在 `persona_reply` 调用侧掏空 `_inject_goal_context`（含 `generate_inbox_draft` 内部那次，靠临时替换方法）。若 Q-1 要在 `goals/service.py` 再加 `is_blocked` 判断，属加固不是本线缺口。
- **Q-2 F**：门禁「10 条要照片只发图或拒绝」本线自建 `src/inbox/media_gate.py` + `tests/test_media_gate.py`，R79 可并入 `commitment_gate`。
- **回访文案（交 Q-0 / R79，只给版本号）**：
  - **#263（D6V29H / J6RHBQ）**：「1.0.79 起，AI 答应发图时必须真的从相册发出去；相册里没有对应场景就直接说没有，不再编『还在加载 / 我再发一次』。客户说没收到：第一次系统重发或承认没发出，第二次转人工。」
  - **#266（CK8HCT / FFGMM9）**：「1.0.79 起，相册 AI 建议词默认参与匹配（卡片标「AI 标注」），顶部可一键采纳全部建议词；要风景照不会再随机抓自拍。」
  - **#269 身份补丁接线**（Q-1 F 依赖本线）：「账号昵称与人设名不一致时，AI 知道对方可能用昵称称呼自己，不再否认。」

## 五、花费

（收工时填）
