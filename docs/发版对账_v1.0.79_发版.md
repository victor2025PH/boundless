# 发版对账 v1.0.79 — 发版线 R79（P 批 + Q-1～Q-6 + Q-14 收口：装载 → 回归与门禁 → 干净 worktree 三包 → 三渠道 + 官网 → 坐席三台 → 台账 → 回访）

> 指令：老板 09-10 原话——读 `docs/Q批_进度总账.md`（按 D-Q6 修正：Q-1 与 Q-3 两行须收工，Q-2/4/5/6/14 已收工的一并带上，未收工的随 1.0.80）与 `docs/P批_进度总账.md`（P-1～P-5 收工），照 `docs/发版对账_v1.0.78_发版.md` 的全链发 1.0.79。
> 接手方：先 `git -C D:\boundless log --oneline -30` 核对本表 commit 真在；再看「断点 / 交接」节。任何写台账先 dry-run；对外只走 `tools/duty_reply.py`（每条带工单号、只给版本号）；**自检只许 GET**（N-5 purge 事故）；发版线不改业务代码——**本次三处代补**（§4.1，按 N-5 `105b384c` / R78 `07d86c02` 先例），每处都是干净树打包 / 回归扫出来的「HEAD 带病、工作树掩盖」。
> 执行：2026-09-10 14:5x → 18:2x。意向板 `R79 release 1.0.79 closeout`。

## 0. 一句话

**1.0.79 已于 09-10 17:57–18:09 三渠道发布**（公共 clean **522,064,046 B** sha256 `2c294c6d1bf2d6bad486da410e6c7dbe54e32bc24e5262c0946434b187a6d2a2`，官网 498 MB / 内测 smart **593,254,839 B** / lite **525,392,577 B**）+ 官网 `gate:content` PASS + `deploy.ps1` 部署完成（含 Q-14 C 网关代码，pm2 reload rolling）+ 坐席三台 **yunsheng / kouxing / lianbei 全 UP · 1.079 · smart/internal**（三台 smoke 全 OK）+ 群播报 **mid 1389** / 频道 @hykj7 **msg 160**；台账 **#259 #260 #270 confirmed→fixed、#262 #263 #264 #266 #267 #269 new→fixed（#252 #254 R78 已 fixed，工具跳过）→ 12 条回访逐单发出（#265 只发进展不标修复）→ 记账 9 单，积压 0**。zhiliao 15:10 装载（16s 就绪，0 ERROR），全量回归（干净 worktree）**149 红 / 26647 绿**，对 R78 基线 62 新红全部归因（§3.1），**0 条 P/Q 功能性回归**；`outbound_style_gate` 200 样例 0 违规。**三处本线自己抓的坑，全部靠「从 release commit 的干净 worktree 打包 + 在 worktree 上跑回归」暴露，工作树上全被别线的未提交文件掩盖着**：① `persona_reply.py`（Q-6 A/F `79112004` 卷进微信线未入库的 `compliance.runtime.platform_scope`）→ 安装包里人设回复主链 **ImportError 整条不可用**；② `personas.html`（Q-2 B `48969425` 顶掉 `function _selOpt(` 函数头）→ 人设工作室**整页 JS SyntaxError**；③ `cp-goal.js`（Q-1 E / Q-5 C 两处原生 `prompt()`）→ 桌面端槽位「确认 / ✎ 改」按钮**静默死**。三处代补进了 release commit **`2adef601`**，三包重打两次（§4）。**Q-8 / Q-10 / Q-15 在本链进行中收工（16:09–17:30），不在 1.0.79 里（§4.2）。**

## 1. 开工基线（14:5x）

| 项 | 事实 |
|---|---|
| 总账核对 | P-1～P-5 收工；Q-1 `ebc37c4e` / Q-3 `eed161fa` / Q-2 `720b1ac7` / Q-4 `3fab55c1` / Q-5 `2b7372f7` / Q-6 `92bcc5b8` / Q-14 `e83a7d53` 六行 + Q-14 收工。**Q-8 / Q-10 / Q-15 第二波 15:0x 刚登记意向**（Q-8 `goals/*` / Q-10 工作台小件 / Q-15 adult_grader），未收工 → 按老板原话「未收工的随 1.0.80」不带 |
| R79 前必落交接项 | Q-4→Q-3 `autosend_worker.py` 两处（班表扣留分支接 `log_off_hours_hold`；复班补觉判定改 `catch_up_regen_due(..., shift_started_ts=schedule_state(...)["shift_started_ts"])`，`_regen_h` 撤）+ tests +3（`test_autosend_work_schedule.py` 27 passed）；Q-5 `unified_inbox.html` `cp-goal.js?v=20260910a→b` 单行 index-only 入库（工作树被微信线整文件改写为 LF 且缺 Q-14 `aif-` 块，不能整文件 add）→ **`4ba5e0bc`（15:14）＝release 基点** |
| 文书 | `tmp/_notes_1079{,_en}.txt`（15 条）/ `_announce_1079.txt` / `_broadcast_1079.txt` / `_channel_post_1079.txt` / `r79_mark_fixed_1079.json`（11 单）/ 回访 `tmp/r79_{259,252,254,260,262,263,264,266,267,269,270,265}.txt`（取各线落点表「回访」节；#265 只进展）/ `r79_bump_version.py` `r79_site_facts.py` `r79_stage_cpgoal.py` `r79_channel_post.py`（campaign `release-1079`） |

## 2. 装载与活体核对（15:09–15:33）

| 步 | 状态 | 事实 |
|---|---|---|
| `agent_probe` | ✅ | P/Q 一波无在途；Q-8/10/15 二波意向刚登记、`.py` 未落盘 |
| `restart_preflight` | ✅ 15:09 **GO** | 脏 py 语法 0 错 / 装配 passed / 冷却过 / 二波线「in-flight – not declared ready」提示核对后 GO（`tmp/r79_preflight_0.txt`） |
| **zhiliao 装载** | ✅ 15:10:39 | `restart_instance.ps1 -Instance zhiliao -Reason "R79 …"`：hard-stop → `/login` **16s**；app.log **0 ERROR**（`tmp/r79_restart_zhiliao.txt`；stop 阶段的 `could not be terminated` 是子进程树已随父退出的常规噪音） |
| 回放核对（装载后代码上、只跑测试不投生产消息） | ✅ 226 / 227 | Q-1 同会话已答不再问（K24YJ2 回放）/ Q-3 切手动取消在途（XBGPBN + 「切手动 1 秒取消」）/ Q-2 邀约 20 拒 + 要照片 10 只发图或拒（`commitment_gate`）/ P-2 登录零自动草稿 + 停联持久 / P-3 要照片三轮（6SRA2B）/ Q-4 `test_release_notice_q4` + 基线红线 14 例 / Q-14 `test_ai_fail_marker_q14` 等（`tmp/r79_replay_tests.txt`）。**唯一红** `test_template_wires_chip_and_diag_code`＝微信线工作树的 `unified_inbox.html` 覆写掉 Q-14 B `aif-` 块（HEAD 5 处 / 工作树 0 处）——**zhiliao 现场没有 Q-14 灰标 chip**，release commit 用 HEAD 字节不受影响 |
| Q-4 出厂态 | ✅ | `netstat -ano \| findstr 18799` 只见 `127.0.0.1`；班表 / 额度出厂关（`release_defaults_changed.json` 已含 `releases.1.0.79` 四行） |
| Q-14 `ai.timeout=1` 出灰标 | ⚠ 未触发 | 15:19 临时设 1 → 15:33 无一条入站（zhiliao 无客户流量）→ 回滚 40。由 `test_ai_fail_marker_q14` 单测 + #262 回访「自己验：临时把 ai.timeout 设 1」交 skuio |
| `outbound_style_gate` | ✅ 200 / 0 | `samples=200 violations=0`（fixes punct=534 style=246 trimmed=48）；`--draft-log` 真机样本 0（装载后无 `[draft]` 行）→ 随坐席装机后复读 |
| **zhiliao 载入范围** | ⚠ | 15:10 载的是当时工作树：**不含 `4ba5e0bc` worker 代补**（15:14 提交）与 §4.1 三处代补——下一次重启者顺带 |

## 3. 回归 / 门禁

| 项 | 结果 |
|---|---|
| 全量回归（**干净 worktree**，release 基 `0b0b78c3`） | `scripts\regression.ps1` **149 failed / 26647 passed / 42 skipped，1:21:08**（`tmp/r79_pytest_full.txt`；与三包构建同机并跑）。对 R78 119 红：32 转绿、**62 新红 → worktree 串行复跑仍 62 红（`tmp/r79_newred_serial.txt`）→ 全部归因见 §3.1**；代补后复跑 28 转绿（`tmp/r79_newred_serial2.txt`）。工作树上的首跑 25% 即杀（Q-8/10/15 实时存盘污染，`tmp/r79_pytest_full_dirtytree_aborted.txt`） |
| `gate_sweep`（worktree） | **25 failed / 408 passed**（`tmp/r79_gate_sweep_worktree.txt`；复跑同值）：**14 项 >48h 旧债**（`assistant_qa_eval` ×2 / `deploy_profiles` / `inline_color_ratchet` ×3 / `orphan_refs` / `legacy_blue` / `input_theme_contrast` / `silent_exception` / `takeover_trigger_surface` / `copilot_tiny_font` / `workspace_emoji` / `inbox_inline_handlers layer2`）+ **11 项新**：微信线 6（`route_inventory` 20 端点全是 wecom/wechat_kf/wechat_pc/heartbeat 未入库文件、`inbox.kf.transfer*` 缺 zh、`kfTransferToHuman` ×2、`rpa layer1`、`tailwind ops_glance`）/ **Q-2 B personas.html 语法错 ×2（代补）**/ Q-2 B personas i18n ×2（`'直接'` 字面比较，非展示串）/ Q-14 B `toTimeString().slice`（灰标时间格式）。工作树同刻扫也是红（emoji 60 > 54） |
| 预发门禁（三包各一次） | `renderer-boot-invariants` 76 / `outbound-wiring` 16 / **`package-layout` 81（P-4 必须在 / 必须不在）**/ `uninstall-nsh` 303 / 菜单词条 / backend-dist fresh / 安装版后端冒烟 / 各 flavor 产物冒烟 / `[after-pack]` 随包交付物 **22 项**（R78 17 项，+handoff yaml 等） |
| 导入扫雷（新做法） | `tmp/r79_import_sweep.py` 1192 模块 import 0 错；`tmp/r79_import_names_sweep.py` 静态解析全部 `from src.X import name` 8087 处 → 8 处不存在，7 处均在 `try/except` 内（`purpose_scope` / `kf_window_guard` / `src.strategy` 等），**唯一裸的就是 `persona_reply.platform_scope`（§4.1 ①）** |

### 3.1 62 条新红归属（交回各线，不拦发版）

| 组 | 条数 | 断言 | 归属 / 处置 |
|---|---|---|---|
| `test_persona_reply` ×18 / `test_time_context` ×10 | 28 | `ImportError: cannot import name 'platform_scope' from src.compliance.runtime` | **Q-6 A/F `79112004` 卷进微信线（实施97）未入库改动** → R79 代补 `451c4e34`（容错导入），复跑 28 绿 |
| `test_login_default_semi_m2` / `test_persona_doc_import` / `test_telegram_directory_sync` | 3 | `config/config.yaml` 不存在 | worktree 无 gitignore 的 `config.yaml`（测试环境项，借主树文件复跑即绿） |
| `test_image_autosend` ×7 / `test_caption_truthfulness` ×3 | 10 | `run_autosend_image` 通用池随机自拍不再发 | **Q-6 B `be6378c7`「索图随机 fallback 关」有意行为变更**，测试未同步更新——Q-6 线补测试 |
| `test_send_gate_feedback` ×2 | 2 | `origin=manual` 到顶仍放行 | **Q-4 B「人工恒放行」有意变更**（#267）——Q-4 线补测试 |
| `test_business_domain_single_source_241` | 1 | payload 多 `discovery_paused` | **Q-1 E** 新字段——Q-1 线补测试 |
| `test_group_show_console` ×5 / `test_messenger_send_failures` ×2 / `test_companion_prompt_service_tone::content_contract` / `test_i18n_extra_langs` | 9 | 主树脏树上绿 | 依赖各线**未提交**改动（`test_messenger_send_failures.py` 本身 M）——各线尽早提交 |
| 门禁类 | 9 | 见上表 gate_sweep 新红 | 微信线 6 / Q-2 B 2（`_selOpt` 代补后绿）/ `test_no_bare_prompt` cp-goal.js 2 > 0（代补后绿） |

## 4. E 段全链

| 步 | 状态 | 事实 |
|---|---|---|
| 收工核对 / preflight / 装载 / 活体 | ✅ 14:5x–15:33 | §1 §2 |
| bump / 繁体 / changelog / ui-build | ✅ 15:3x（**回归完成前先做**，publish 仍以回归为闸——与 R78 顺序不同，为并行省时） | `desktop/package.json` 1.0.78→**1.0.79** / 1.078→**1.079**（`r79_bump_version.py` 字节替换保 CRLF）；`python -m scripts.i18n_hant generate` → **17393 键**；`gen-chatx-changelog add --version 1.0.79 --date 2026-09-10` **15 条双语**；`bump_ui_build.py --severity notice` → **20260910-1537** → 主干 **`23fd13dd`** |
| release commit（worktree） | ✅ | `git worktree add --detach D:\boundless-r79 4ba5e0bc` → cherry-pick `23fd13dd` → **`0b0b78c3`**（不带 `ca4cd456` Q-10 C / `7c829d25` Q-15 A 等 15:14 后的主干提交）→ 代补 `54768ff3`（personas）→ `cfc4409d`（persona_reply）→ **`2adef601`**（cp-goal uiPrompt）= 三包所用 |
| **三包（干净 worktree，打了三轮）** | ✅ 15:48–17:49 | junction `desktop/node_modules` + `desktop/build/ffmpeg`；`services/*/node_modules` robocopy；补 gitignore 资产（`static/vendor/emoji-picker-element` 5 文件、`lin_xiaoyu` 16 段 mp4）。**第一轮**（`0b0b78c3`）smart 打完即被 gate_sweep 扫出 personas 语法错作废；**第二轮**（`54768ff3`）三包打完被回归扫出 `platform_scope` 作废；**第三轮**（`2adef601`）`build:backend` 全量（fingerprint `9b229d82…` files=2123）→ `dist:win` smart **593,254,839 B** + `latest-internal.yml` → `dist:win:clean` **522,064,046 B** sha256 `2c294c6d…6d2a2` + `latest.yml` → `dist:win:lite` **525,392,577 B** + `latest-lite.yml`（`tmp/r79_dist_1079_{smart,clean,lite}.txt`）。包内核对：`backend/_internal/src/web/templates/personas.html` 含 `function _selOpt(`；asar `cp-goal.js` `window.uiPrompt` ×5 / 原生 `prompt(` ×0、`app.html` `?v=20260910c`（`tmp/r79_asar_check.js`）。与 1.0.78 对比：smart +431 KB / clean +426 KB / lite +427 KB |
| publish 三渠道 | ✅ 17:57–18:09 | 各先 `-DryRun` 再 `-Yes`（`-DistDir D:\boundless-r79\…`）。**公共**（`-NotesFile tmp/_announce_1079.txt`）：VPS sha512 ✓ / 指针 + pm2 reloaded (rolling) / R2 / 公网核 `latest.yml=1.0.79 manifest=1.0.79 exe=HTTP206` / `announcements.json` 带 `release-v1.0.79`（20 条）/ 清理 **1.0.76**。**内测**：`latest-internal.yml=1.0.79`，公共指针未动，清理 1.0.76。**lite**：`latest-lite.yml=1.0.79`，清理 1.0.76。日志 `tmp/r79_publish_1079_{public,internal,lite}.txt` |
| 官网 | ✅ 18:11 | `chatxContent.ts` 五值（`r79_site_facts.py 498 <sha256>`；首轮 sha 为第二轮包值，第三轮包出来后字节替换改成 `2c294c6d…`）→ `npm run gate:content` **PASS**（发布前 5/6 因 manifest 仍 1.0.78 预期红，publish 后 6/6）→ `scripts/deploy.ps1` **部署完成 [OK]**（healthy / webhookSecret / adminKey / deepseek 全 True；发布物差量全「远端同内容跳过」；R2 同步）。日志 `tmp/r79_website_deploy.txt`。Q-14 C 网关代码随部署；pm2 `yuntech` 仍 fork 模式，切 cluster 未获授权未动 |
| 坐席三台 | ✅ 18:0x–18:14 | `pwsh deploy\desktop\push_chatx.ps1 -TargetSsh {kouxing,lianbei,yunsheng} -Setup <worktree>\dist\ChatX-Setup-1.0.79.exe -Install -Relaunch -Smoke`：三台 `version 1.0.79.0`，smoke ready 10.2s / 18.3s / 9.8s，六端点 OK + vision active + pacing seeded；`chatx_fleet_status`：**yunsheng / kouxing / lianbei 全 UP · 1.079 · smart/internal**（`tmp/r79_fleet_1079.txt`） |
| release 真值 commit | ✅ **`4dc1a806`** 18:13 | `website/lib/chatxContent.ts` + `desktop/build/i18n-packs-staged` 19 文件（15 改 + 4 新：`ai_fingerprint_card` / `ops_glance` / `release_notice` / `work_schedule_q4`，自 worktree 构建拷回） |
| 播报 | ✅ 18:14 | `duty_update_hint --set "1.0.79 已发布：…手装 …/internal/ChatX-Setup-1.0.79.exe…装完请重启一次实例"`（热重载）；报障群 `duty_reply --group neice --text-file tmp/_broadcast_1079.txt --no-mention` → **mid 1389**；频道 @hykj7 `r79_channel_post.py --apply` → **messageId=160** |
| 台账 / 回访 | ✅ 18:14–18:18 | `duty_ledger_fix --mark-fixed-file tmp/r79_mark_fixed_1079.json`（dry-run `tmp/r79_mark_fixed_dryrun.txt` → `--apply`）：**#259 #260 #270 confirmed→fixed / #262 #263 #264 #266 #267 #269 new→fixed / #252 #254 已 fixed 跳过**；12 条回访先 `--dry-run` 全过红线闸（#265 初稿含 `config.local.yaml` 被红线拦 → 改「关怀页开关卡截图」）→ 正式 18:15:06 #259 → 18:17:49 #265 逐单 `bug-intake/reply ok`（间隔 ≥13s，bot 链带工单 footer）；`duty_notify_summary --reporter 8942577244 --ids 252,254,259,260,262,263,264,266,267,269,270 --msg-id 1389 --apply` → **已记账 9 单，积压 0** |
| 总账 | ✅ | Q 批 R79 行 + 全批收口两项；P 批「全批收口」勾选 |

### 4.1 三处代补（发版线破例，逐条可回滚）

| # | 文件 / commit | 病灶 | 为什么工作树看不见 | 代补 |
|---|---|---|---|---|
| ① | `src/inbox/persona_reply.py` → 主干 `451c4e34` / worktree `cfc4409d` | Q-6 A/F `79112004`（12:51）提交 `persona_reply.py` 时把微信线（实施97）在同文件里的 `from src.compliance.runtime import platform_scope` 一并卷入，而 `src/compliance/runtime.py` 新版至今只在工作树（`M`，+167 行）。HEAD / 安装包内 `generate_persona_reply` 一进函数就 `ImportError` → **人设回复主链整条不可用**（回归 28 红即此） | 工作树里 `runtime.py` 有 `platform_scope`，zhiliao 也是从工作树载的 → 主树跑测试全绿 | `try: from … import platform_scope except ImportError: from contextlib import nullcontext as platform_scope`——缺席＝不改变现状（nullcontext 吞掉 platform 参数）；微信线入库后自动走真实作用域 |
| ② | `src/web/templates/personas.html` → 主干 `17e6c6b0` / worktree `54768ff3` | Q-2 B `48969425`（12:33）在「Option cards」处插入 `_mpPreview/_selMeetingPolicy/_selMpStyle` 时把原 `function _selOpt(el, hiddenId, cardsId) {` 那一行顶掉，函数体成了孤儿 → 首个 `<script>` 块 `Unexpected token '}'` → **人设工作室整页 JS 死**（Q-6 E UI `ca1deed8` 又在其上叠了一层） | 主干 HEAD 同病（主树 gate_sweep 也红，只是混在 13 条里没人认领） | 补回一行函数头（主干版本上下文多了 Q-15 B `_setAdultPolicy`，cherry-pick 冲突改手工同一行）；`test_template_inline_js_syntax` / `test_template_undefined_identifiers` 11 绿 |
| ③ | `shared/copilot/components/cp-goal.js`（+ `desktop/renderer` 镜像 + 两处 `app.html` + `unified_inbox.html` 戳 `20260910b→c`）→ **只在 worktree `2adef601`** | Q-1 E `5057865b` `_slotConfirm`（无现值时）/ Q-5 C `2b7372f7` `_slotEditConfirm` 用原生 `prompt()`——Electron 渲染进程调用即抛（`typeof` 仍是 function，#173 口径）→ 桌面端「确认 / ✎ 改」**静默死**；`test_no_bare_prompt` cp-goal.js 2 > 0 | 门禁红在主树也在，但 Q-8 同时在 cp-goal.js 上活跃编辑 | 改 `window.uiPrompt`（`cp-rel-stage.js:193` 同写法，宿主未装弹层视同取消）。**主干未动**：Q-8 编辑窗内（15:51 / 17:16 两次提交，戳已到 `20260910d`），按「别线活跃窗不静默改」交 Q-8 / 下一位在主干补同两处（§5） |

### 4.2 Q-8 / Q-10 / Q-15 为什么不在 1.0.79

- release 基点 `4ba5e0bc` 切于 15:14；Q-10 收工 16:09、Q-15 16:12、Q-8 17:30，都在链条进行中落地。总账「Q-8 / Q-10 收工 → 若 R79 未发则并入」这句与老板原话「未收工的随 1.0.80」在切基点时不冲突（当时三线均在途）。
- 到 17:30 若改基点到主干 HEAD：三线共约 25 个 commit（Q-8 改 goals 主链注入 / `outbound_humanize` 退场闸 / `persona_reply` 加 try-block / `care_dispatcher` 真发），**未经 zhiliao 装载与活体核对**，且要重跑 1:21 回归 + 45 min 三包 → 发布落到 21:00 后；R78 §4.2 已定「主链半成品不进公共包」。**决定：不并入，1.0.79 = 基点 + 版本链 + 三处代补；Q-8 / Q-10 / Q-15 随 1.0.80。** 老板如要反过来，成本就是上面那一句。
- 注意 Q-15 A/B/C 的 `.py` 与 Q-8 的 `.py` **已在主干 HEAD**，任何人下次重启 zhiliao 都会把它们载进生产（与 1.0.79 安装包行为不一致），值守知悉。

## 5. 交回各线 / 交下一班

- **Q-8（cp-goal.js 活跃线）**：主干 `cp-goal.js` L1290 / L1318 两处原生 `prompt()` 请照 worktree `2adef601` 改 `window.uiPrompt`（双树镜像 + 戳），`test_no_bare_prompt` 即绿。`test_admin_route_inventory` 的 `/api/goals/defaults*` 未登记（Q-15 已点名）。
- **Q-6**：`79112004` 卷入微信线 `platform_scope` 引用（已代补容错）；`test_image_autosend` ×7 / `test_caption_truthfulness` ×3 请按 `be6378c7`「随机 fallback 关」更新断言；`config.example.yaml` 相册默认仍未整文件提交。
- **Q-2**：`48969425` 顶掉 `_selOpt` 函数头（已代补）；personas.html `'直接'` 字面比较触发 i18n 门禁 2 条（登记 `_ACCEPTED` 或改常量）。
- **Q-4**：`test_send_gate_feedback` ×2 按「人工恒放行」更新；`config.example.yaml` L3595 `catch_up_regenerate_hours` 注释 + `unified_inbox.html` 「作息外」悬停句两件顺手活**本次未做**（两文件都在别线脏改中）。
- **Q-1**：`test_business_domain_single_source_241` 期望字典加 `discovery_paused`。
- **Q-14**：`unified_inbox.html` 灰标时间 `toTimeString().slice` 触发 `sealed_templates_no_legacy_date_formatters`；工作树 `unified_inbox.html`（微信线 LF 版）**缺 `aif-` 块与 `cp-goal.js?v=…d`**，谁整文件 `git add` 就会退回——已被 Q-3 / Q-10 / Q-15 三线点名，仍未解决，**建议微信线立刻 `git checkout -p` 只保留自己的 hunk**。
- **微信 KF / PC / 企微 SSO 线**：`src/compliance/runtime.py` / `__init__.py`（+206 −143）与 26 个 untracked 文件请尽早入库——`persona_reply.py` / `window_guard.py` / `kf_window_guard` 引用已在 HEAD，靠 try/except 撑着；`route_inventory` 20 端点、`inbox.kf.transfer*` zh、`kfTransferToHuman` 挂 window 同一批。
- **值守 / 下一位重启者**：zhiliao 现载 15:10 工作树，**不含** `4ba5e0bc` worker 代补与 §4.1 三处；下次重启同时会载入 Q-8 / Q-10 / Q-15 主干 `.py`（§4.2 第三点）。zhiliao 工作台现场无 Q-14 灰标 chip（模板被覆写），重启也不会有，除非微信线先修工作树。
- **构建**：① 干净 worktree 打包是这版救命的做法——**建议固化为 `scripts/release_worktree.ps1`，并把 `tmp/r79_import_names_sweep.py`（静态 `from src.X import name` 存在性扫描）加进 gate_sweep**，它 25 秒就能抓到 ① 这类「HEAD 引用了未入库的名字」；② `dist:win:smart` 的 `A || B && C` 仍未改；③ worktree `D:\boundless-r79` 保留（三包在其 `desktop/dist*`），`D:\boundless-r78` 可 `git worktree remove --force` 回收磁盘。
- **skuio 回访待收**：#262 自验 `ai.timeout=1` 出灰标；#265 补 9/7 15:00–16:00 backend.log + 关怀页开关卡截图；P-1 真机 `outbound_style_gate --draft-log`（≥20 稿）与 `[pacing] composing=sent` / `[claim-guard]` / `[album_match]` 实证随 1.0.79 装机后收。

## 6. 断点 / 交接

- 14:5x 读总账 + 落点表 → 15:0x agent_probe / 文书 → 15:09 preflight GO → 15:10 装载 → 15:1x 回放 226/227 + style_gate → 15:14 `4ba5e0bc` → 15:19–15:33 ai.timeout 试验（未触发）→ 15:3x bump 链 `23fd13dd` → 15:4x worktree `0b0b78c3` + junction/robocopy → 15:45 gate_sweep（worktree）/ 15:48 回归（worktree）/ 15:48 smart 首打 → 16:0x gate_sweep 25 红 → personas 语法错定位 → 16:14 `17e6c6b0` / `54768ff3` → 16:2x 第二轮三包 → 17:10 回归 149 红 → 串行复跑 62 红 → `platform_scope` 定位 → 17:26 `451c4e34` / `cfc4409d` → 导入扫雷 8 处 → cp-goal prompt → `2adef601` → 17:3x–17:49 第三轮三包 → 17:5x 内测 / 公共 dry-run → 17:57–18:09 三渠道 → 18:11 官网 → 18:0x–18:14 坐席三台 → 18:13 `4dc1a806` → 18:14 标 fixed + hint + 播报 1389 → 18:15–18:18 回访 12 条 → 记账 9 → 频道 160 → 18:2x 本表 + 总账。
- 未做 / 不做：zhiliao 重装（等安静窗；重启即载入 Q-8/10/15 主干 `.py`，值守决定时机）；主干 cp-goal.js `prompt()` 两处（Q-8 编辑窗）；Q-4 两件顺手活；`D:\boundless-r78` 回收。
- 意向 `R79` 于本表提交后 `-Done`。
