# 指令 Q-24：Messenger 网页代发链止血——输入框脱落重绑 · 失败原因进界面 · e2ee PIN 可见 · 反应入站 · 登录窗必隐藏 · 人工图不当客户图

> 预算 **$200**。工单 **#298（P0，9 份：DXAPAX / SRKZ45 / Y82GWM / QD3B9P / R6PYN3 / FJM9ER / VNA2Q3（已撤回）/ P7P8FY / 6RXSKT）/ #297 / #299（TKV3HB）/ #300**（skuio 1.0.81–1.0.82，09-11 18:50–23:27）。分析 `docs/分析_1.0.81首日问题归类与工作台三视角方案_2026-09-12.md` §一 F、§二 R3 R5。落点表 `docs/发版对账_v1.0.83_Q24.md`。
> 归属：`services/messenger-web/server.js`（边车）+ Python 侧 Messenger 发送路由 / 入站桥 + 工作台发送门错误映射。**不动** `autosend_worker.py`（Q-23 线在动）、不动翻译区。

## 事实（证据 `tmp_diag\{DXAPAX,SRKZ45,QD3B9P,R6PYN3,FJM9ER,P7P8FY,TKV3HB}`；代码只读核对 `server.js`）

- **发送**：18:50 / 18:56 / 18:57 / 18:59 同号 4 连败：边车 500 `elementHandle.type: Element is not attached to the DOM`（composer 句柄在输入时失效）；`composer not found → re-navigate accepted=false`；`insertText integrity failed → full typing` 仍脱落；`streak=4 backoff=40s`；新登 `冷静期 600s`。20:41–20:44 另一号三会话同病。**用户已确认手机 Messenger 能发、对方能收——是网页代发问题不是号坏。** 界面只有「发送失败 重发」四个字，两条相同气泡。
- 边车已有原因码但没到界面：`server.js` L4183 `503 e2ee_pin_pending`、L4197 `429 send_backoff`、L4237–4271 composer 恢复一击（只 re-navigate 一次）。
- **e2ee PIN**：登录后 PIN 未过 → 会话空、左侧仍 7/3、手机发的抄不进（R6PYN3 / FJM9ER）；半死态自愈只在读线程时触发（L951–960）。
- **反应**：对方点赞（Double tap to like）智聊无痕（P7P8FY）；边车 L259–391 有 reaction 桥（对齐 WA），但该会话未落 → 核 `_reactionSeen` 去重 / 桥地址 / Python `/reaction` 是否入库显示。
- **窗口**：21:52 登录后本应隐藏的网页一直露着，URL `messenger.com/e2ee/t/…`，顶栏黄条「--no-sandbox 不受支持」，不停刷新抢前台（TKV3HB / QD3B9P：来电页 `Nabil is calling` 把窗口拉前台）。
- **人工图**（#300）：坐席在工作台上传发出的图（`origin=manual`）被 AI 当成客户发的图接话；同报告：出站中日混发、记忆把「客户越南出差」收成「一起去」（后者归 Q-25）。
- **草稿语言**（Y82GWM，同号）：客户只发英文「Hi」，全自动起草中文（顶栏译文=中文）——归 Q-21 扩 / `conv_lang_plan`，本线只在落点表记证据。

## 要做的七段

| 段 | 内容 | 文件 |
|---|---|---|
| **A composer 重绑** | 输入前**重新 query** composer 并等 DOM 稳定（≤2s，`MutationObserver` 静默 300ms）→ `focus` → 输入；`Element is not attached` 一律**同 thread 内重查重试 1 次**（不 re-navigate）；再失败才 re-navigate 一次；连败 ≥2 → 该会话进「待重试队列」（后台每 60s 重试至 3 次）+ 坐席铃铛「Messenger 发送卡住，已排队重试」，**不再只是 backoff 40s 静默** | `server.js` send / send-media 输入段、恢复一击 |
| **B 失败原因码贯通** | 边车所有失败结构化 `{ok:false, code, reason, detail, retry_after}`，`code ∈ composer_detached \| thread_not_found \| e2ee_pin_pending \| call_overlay \| send_backoff \| login_expired \| upload_failed`；Python 发送路由原样透传；工作台红字三段式「Messenger 输入框丢失 · 已自动重试 2 次 · 你可以手机发，或 40 秒后再试」+ 「重试」按钮；`reply_diagnosis` 新 finding `sidecar_send_fail`；`why_no_reply` 同步 | `server.js`、Python Messenger 发送路由、`unified_inbox.html` 发送门错误映射、`reply_diagnosis.py`、i18n |
| **C e2ee PIN 可见 + 手机出站回抄** | 登录后 / 心跳检测 `e2ee_pin_state` → 会话头黄条「Messenger 需在手机确认 PIN 后才能同步」（含 how-to）；PIN 过后自动 backfill 该账号；其他设备（手机）发出的消息作为 `direction=out origin=external` 抄进会话（不起草、不算坐席动作、不触发让位） | `server.js` 心跳 / backfill、Python 入站桥、会话头（`ms-` 前缀） |
| **D 反应 / 点赞入站** | 核 P7P8FY 会话为何无 reaction 事件（去重 / 桥地址 / 入库显示）；入站「反应」类型显示为气泡角标（不起草、不计强未读、可选轻提示）；贴纸 / 表情包按媒体占位 | `server.js` L259–391、Python `/reaction` 入库与工作台渲染 |
| **E 窗口纪律** | 登录成功后**强制隐藏**（headless 或 `hide()`），任何 navigate / backfill / 未读跳会话 / 来电 overlay **不得** `bringToFront`；来电 / 视频通话 overlay 自动关闭（`Ongoing call` 页）；`--no-sandbox` 黄条治本（`--disable-infobars` 或去掉 no-sandbox）；隐藏失败落 `[msg-sidecar] window visible reason=` 并报坐席 | `login_window.js`、`server.js` 导航处 |
| **F 人工图不当客户图** | 出站媒体（`origin=manual` / auto）在起草上下文里标为「我方发出的图」，只有 `direction=in` 的媒体才进「客户发的图」描述；识图（vision）只对入站媒体跑 | Python 起草上下文装配（`autodraft_helpers` / 媒体描述注入） |
| **G 门禁** | 边车 node 测试：composer 脱落 → 重查重试成功；连败 → 队列 + 铃铛；原因码全覆盖；Python 契约测试：错误码透传 + i18n；回放 DXAPAX 四连败序列 → 零静默失败 | `server.js` 测试、Python tests |

## 红线

- 不动 `autosend_worker.py` / `autosend_policy.py`（Q-23 线在动）；让位 / 风控闸不在本线。
- 不动 WhatsApp / Telegram 边车。
- 发送失败**永不**自动换文案重发（防双发：两条相同气泡是 VNA2Q3 的现象，先查幂等钉）。

## 开新对话粘贴

```
读 docs/指令_Q-24_Messenger网页代发链止血_2026-09-12.md，按它做 Q-24（#298 P0 #297 #299 #300：server.js 输入前重查 composer 等 DOM 稳定再输入，Element is not attached 同线程重查重试 1 次再 re-navigate，连败 ≥2 进待重试队列 + 坐席铃铛不再静默 backoff / 失败结构化 code composer_detached|thread_not_found|e2ee_pin_pending|call_overlay|send_backoff|login_expired|upload_failed 透传到工作台红字三段式 + 重试按钮 + reply_diagnosis sidecar_send_fail / e2ee PIN 未过会话头黄条 + PIN 过后自动 backfill + 手机发出的消息 direction=out origin=external 回抄不起草不触发让位 / 核 P7P8FY 反应事件为何丢 + 反应入站角标 / 登录后强制隐藏窗口、任何导航 backfill 来电不得 bringToFront、来电 overlay 自动关、--no-sandbox 黄条治本 / 出站媒体不进「客户发的图」上下文 / 边车 node 测试 + Python 契约 + DXAPAX 四连败回放零静默失败）。预算 $200。先读 tmp_diag 里 DXAPAX / FJM9ER / P7P8FY / TKV3HB 四份报告与 server.js L4183–4271，再 scripts\agent_probe.ps1 -Intent 登记；不动 autosend_worker.py / autosend_policy.py（Q-23 在动）、不动 WA / TG 边车、不自动换文案重发。每个 commit 带 #298 并立刻补落点表 docs/发版对账_v1.0.83_Q24.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-24 行并 agent_probe -Done。
```
