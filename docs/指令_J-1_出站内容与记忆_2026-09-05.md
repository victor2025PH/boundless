# 指令 J-1：AI 出站内容正确性 + 记忆一致性（#171 谎称发图 · #175 客服话术 · #176 翻译元回复 · #177 人工消息不进记忆）

> **预算硬上限 $190。** 正常花费应在 $90–130。花到 $150 时不管做到第几件，停下来写落点表 + 进度总账，剩余交下一个账号（§7）。
> **四件互相独立，优先级 A（#171，P1）→ B（#175，P1）→ C（#176）→ D（#177）。** 做不完的如实说明剩哪件，不要为了做完而糊弄。
> 报障人 skuio 花无缺，机器 DC8F，**1.0.73**。证据全部在本地：`D:\boundless\tmp_diag\88MP86\logs\app\backend.log`（09-04 17:09→09-05 02:40）+ `tmp_diag\DSY7ST\`（Cursor 报告）。

---

## 0. 开工前必读（两份，不可跳过）

1. `docs/J批_进度总账.md` —— 先看 J-1 那一行，已完成的件不重做。
2. `docs/修复指令_J批_并行_2026-09-05.md` 的「文件归属矩阵」+「共同纪律」+「省钱纪律」+「断点续传协议」。

**读完立即从 A 开工，不要先输出整体计划。**

## 1. 红线 + 文件边界

- 提交只用 `git add <明确文件路径>`；禁止 `-A` / `.` / `commit -a`；一件一个 commit，做完立刻提交。
- ⛔ 绝对禁止 `git reset --hard` / `git checkout .` / `git restore .` / 裸 `git stash push`。
- ⛔ `skill_manager.py`（11k 行）、`autosend_worker.py`、`unified_inbox_send_routes.py` **禁止整读**，先 `rg -n` 定位再 `Read` 窗口。
- ✅ 你可以改：`src/ai/outbound_promise_guard.py`、`src/ai/media_pending.py`、`src/inbox/autosend_worker.py`、`src/skills/skill_manager.py`（**只动** `_apply_media_promise_guard` / `_update_after_reply` 附近的守卫与钩子段）、`src/utils/persona_guard.py`、`src/ai/translation_service.py`、`src/ai/translation_memory.py`、`src/ai/translation_confidence.py`、新建 `src/inbox/human_outbound_memory.py`、`src/eval/media_consistency_eval.py`、`src/eval/persona_consistency_eval.py`、`tests/**`、`docs/**`。
- ⛔ 不要碰：`src/inbox/store.py`、`src/inbox/drafts.py`（谁都不改）；`unified_inbox.html`（J-4）；`unified_inbox_send_routes.py`（J-3 —— #177 要在里面加一行调用时，先 `agent_probe.ps1` 看意向板，J-3 活跃就把「插在哪 + 那一行」写进结束报告交 J-3）；`goals/**`（J-2）。
- `skill_manager.py` 是热区（unlimited outbound 等他线可能在改）：开工前 `agent_probe.ps1` 看它是否 ACTIVE；ACTIVE 就先做 C/D，回头再做 A/B 的 A 线半边。

**开工先登记意向板：**
```
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "J-1 outbound truth: claimed-sent guard / sales-tone guard / xlate memory refusal / human outbound memory (#171 #175 #176 #177)"
```

---

## 2. A —— #171 AI 谎称已发照片（P1）

### 报障人原话 / 现场
> 「分析这个聊天，ai 一直说发送了照片，实际上在欺骗客户」（图 1169）

WhatsApp 账号 Evelyn（12137839654，人设 Mizuki）↔ 客户 John（17147158961）。22:26 AI：「Oh, sorry — I just sent it, you should have it now.」→ John：「I never got it」→ 22:27 AI：「Hmm, that's weird — let me try sending it again for you.」

### 证据（88MP86 backend.log，已核）
- 22:20–22:28 该会话只有文本草稿（`auto_generate_draft … 17147158961 level=L2`），**零** `send_media` / `[autosend image]` / selfie / promise 行。
- **22:49:17** 才出现 `send_media dispatch whatsapp:12137839654 … type=image origin=auto` → `已发相册媒体 acct=12137839654 type=photo`——真图比假声明晚 23 分钟。
- 结论：客户要图 → 出图链（`plan_autosend_image` / `detect_selfie_request` / offer-accept 桥）**没触发**，文本 LLM 用「我刚发了」把窟窿盖住；现有 `media_promise_guard`（Phase18/20）只识别**将来时承诺**（等我拍/I'll send），对**过去时假声明**零覆盖。
```
Select-String -Path D:\boundless\tmp_diag\88MP86\logs\app\backend.log -Pattern '^\[2026-09-04 22:(2[0-9]|4[6-9]).*17147158961' | Select-Object -First 30
```

### 修（三层）
1. **`outbound_promise_guard.py` 加「已发假声明」检测** `detect_sent_claim(text) -> str`（与 `detect_media_promise` 同风格纯函数）：EN `I (just |already )?sent (it|you|the (photo|pic|picture|image))`、`you should have it (now|by now)`、`(let me )?(re)?send(ing)? it again`、`did you get (it|the photo)`前后文视具体形态；ZH「已经发了/发过去了/刚发（了）/我再发一次/你收到了吗」。排除面：否定（didn't send / 没发）、问句里第三方（客户说的），只看 AI 出站稿。
2. **B 线接线**（`autosend_worker._autosend_deliver` 的 promise 分支旁）：命中 `sent_claim` 且**近 N 分钟（默认 30）该会话无媒体真发记录**（B 线看 `persona_media_sends` 账本 / `_media_sent_log` 回写；A 线看 `user_context._media_sent_log`，`media_pending.py` 已有「点亮/熄灭」判据可复用）→ 走既有 fulfil：`autosend_image(assume_intent="selfie")` 真发一张（相册/生成照常过预算与关系闸）；发不出 → 走既有 `_depromise_autosend_text` 改写为诚实文本（「我这边发不出去，稍后补」口径，语言对齐）。**A 线** `_apply_media_promise_guard` 同判同剥。
3. **出图意图英文问法补齐**：`detect_selfie_request` 的 EN 词表按 John 原话补（原话截图在等 skuio，**不要等**——先把 `send me a pic / can I see you / show me (a photo|what you look like) / got any pics` 这类常见形态补上，截图到了值守回读再补一条）。

### 验收
- `tests/test_outbound_promise_guard.py` 加金标：实录两句 → `sent_claim` 命中；「I didn't send anything」「did you send it?」（客户问）不命中。
- `src/eval/media_consistency_eval.py` 加「无图称已发（英文过去时）」金标，实录句必红。
- 观测：`metrics_snapshot()` 增 `sent_claim_detected / sent_claim_fulfilled / sent_claim_retracted`（与 `promise_*` 同族）。
- 落点表写明：A 线半边若因 `skill_manager` 热区避让未做，剩余项写清楚。

---

## 3. B —— #175 陪聊人设漏出客服 / 销售话术（P1）

### 原话 / 现场
> 「分析为什么会触发这种客服话术？」（图 1190）
Mizuki→John 01:06：「My assistant will reach out with the account details and a quick setup guide—should take you about ten minutes.」客户回：「I'd rather speak to you directly about anything. I don't like these parties.」

### 证据
- 88MP86：01:05:25 `auto_generate_draft … 17147158961 level=L2` → `enrich_draft … risk=low` 放行；该会话全窗口**零 `[goal-inject]`**（不是目标注入带进来的）。
- 同账号挂有 SOP「积极推进产品 → 推荐短期黄金交易」，`inbox.workflows.auto_advance.enabled: true`（88MP86 config L56）。来源二选一：**SOP 话术步指令**（`workflow_autorun` 到点拟稿）或 LLM 从历史里自由发挥。
```
Select-String -Path D:\boundless\tmp_diag\88MP86\logs\app\backend.log -Pattern '^\[2026-09-05 0(0:5|1:0).*(workflow|chain|17147158961)' | Select-Object -First 40
```

### 修（两层，都要）
1. **`persona_guard`（`src/utils/persona_guard.py`）英文客服/销售腔词表**：`my assistant will`、`our team will (reach out|contact you)`、`account details`、`setup guide`、`onboarding`、`your account manager`、`I'll have someone`；命中 → 与既有客服腔同处理（拦下重写）。先 `rg -n "客服腔|customer.service|persona_guard" src/utils/persona_guard.py` 看现有词表结构，**复用它的模式，不另造守卫**。
2. **SOP 话术步 prompt 钉人设口吻**：`workflow_autorun` / 链话术步拟稿的指令里明确「以当前人设第一人称、朋友口吻推进；不得出现助理/团队/开户/引导页/几分钟搞定这类客服框架」。落点在 `workflow_autorun.py`（**属 J-2**）——你只写守卫层；把「钉子文案 + 建议插入位置」写进结束报告交 J-2。
3. 来源核实（便宜，先做）：按上面命令看 01:05 前后有没有 `workflow`/链拟稿行，有＝SOP 指令来源，结束报告写明。

### 验收
- `tests/test_persona_guard*.py` 加实录句金标：命中；「my friend will call you」不命中（误伤面）。
- `src/eval/persona_consistency_eval.py` 加该实录，必召回。

---

## 4. C —— #176 「Exactly」译成「好的，请发送需要翻译的内容。」

### 原话 / 现场
> 「翻译错误，分析原因」（图 1191）：客户 John 说「Exactly」，收件箱译文行显示「好的，请发送需要翻译的内容。」

### 证据 / 根因（代码已核）
- `src/ai/translation_confidence.py::looks_like_engine_refusal`（#115，0831）对这句**必命中**（译文含元词「翻译」而源文没有 + 框架词「请发送」）。
- 但 `translation_service.py::translate` 的**记忆/缓存命中路径在守卫之前返回**：L391 `_cache_get(key)`、L396 `_memory_get(key)` 直接 return；守卫只挂在 L484 引擎新译成功分支。⇒ 0831 之前被 LLM 引擎写进翻译记忆的坏译文（「Exactly」→ 元回复）**每次命中记忆都原样吐出，永不重验**。
- 88MP86 里 01:13–01:17 零翻译日志（入站翻译不记 INFO）——顺手补一行。

### 修
1. `_memory_get` / `_cache_get` 命中后先过 `looks_like_engine_refusal(src, out)`：命中 → 删该记忆条目（`translation_memory.py` 提供 `delete(key)`/`invalidate`，先 rg 现有接口）、按 miss 处理走引擎重译。
2. 一次性清洗 CLI `tools/xlate_memory_purge.py`（默认 dry-run 列出命中条目，`--apply` 才删；数据根按 `scripts/_data_root` 契约；生产实例库只读打开 dry-run）。
3. `translate` 出口记一行 INFO：`[xlate] hit=memory|cache|engine provider=… src_len=… ok=…`（不记原文）。

### 验收
- `tests/test_translation_service.py` 加：预置坏记忆 → 命中被拒 + 重译 + 条目被删；正常记忆条目不受影响。
- 清洗 CLI dry-run 在 `tmp` 假库上跑通。

---

## 5. D —— #177 人工接管期间发的话 AI 不记得

### 原话（skuio Cursor 报告 66SFWD / 32HB5P）
> WhatsApp Olivia(17345893506) ↔ BABY BEAR(13308422244)：中途人工接管回复（告知有女儿、邀请未来同住），切回全自动后 AI 似乎不记得人工发出的内容，只记得 AI 自己发的。

### 现状（已核）
- `src/inbox/persona_reply.py` L133：`direction=out` 一律映射成 `assistant` → 人工出站**在历史窗口内是可见的**。
- 出窗后靠情景记忆，而 episodic 抽取 / `last_reply` 环 / 人设自述状态只挂在 **AI 回复之后**（`skill_manager._update_after_reply` 一族）；手动发送路由（`unified_inbox_send_routes.py` L809 `record_agent_send` 成功分支）**不触发任何记忆钩子**。人工替人设说的「我有个女儿」这类**自述事实**永不入库 → 窗口一滚就矛盾。
- 用 32HB5P（该会话最近 60 分钟全量）核窗口大小与人工出站条数，写进结束报告。

### 修
1. 新模块 `src/inbox/human_outbound_memory.py`：`on_human_outbound(platform, account_id, chat_key, text, *, conversation_id, persona_id)`——复用 `skill_manager` 的记忆写入口（episodic 抽取标 `author=human`、`last_reply` 环、`_inject_self_state` 所依赖的自述状态），**必须过 `memory_grounding` 接地护栏**（与 AI 侧同口径，宁漏勿错）。任何异常零阻断发送。
2. 接线点＝手动 send 成功分支（`record_agent_send` 之后）**一行调用**。`unified_inbox_send_routes.py` 属 J-3：意向板显示 J-3 不活跃 → 你加这一行并单独 commit；活跃 → 写进结束报告交 J-3。
3. 只对 `channel=inbox` 人工文本出站；语音/媒体人工出站只记「[语音]/[图片]」占位不抽事实。

### 验收
- 单测：人工出站「我有个女儿」→ episodic 出对应事实（author=human）；「你好」类无事实不入库；接地护栏拒绝无词汇重叠的抽取。
- 结束报告写明接线是否落地。

---

## 6. 顺手要看一眼（不修，只在结束报告里说）

- 88MP86 里 `[mutual_chat] AI 对聊提醒：4 个全自动会话双向高频`（01:54）——John / BABY BEAR 都在列，看门狗怀疑对端也是 AI。若 John 侧确认是真人（skuio 说是客户），说明该判据阈值偏紧，写进报告交值守。

---

## 7. 交付 + 换账号怎么接

- 一件一个 commit，信息带工单号（如 `fix(chengjie): #171 已发假声明守卫——过去时 sent-claim 走 fulfil/撤回`）。
- 落点表 `docs/发版对账_v1.0.74_J1.md`，四件各一行（含未做的，写清为什么）。
- 更新 `docs/J批_进度总账.md` 的 J-1 行。
- 收工清意向板：`scripts\agent_probe.ps1 -Intent "J-1 outbound truth: claimed-sent guard / sales-tone guard / xlate memory refusal / human outbound memory (#171 #175 #176 #177)" -Done`
- 只跑相关测试文件：`tests/test_outbound_promise_guard.py tests/test_persona_guard*.py tests/test_translation_service.py tests/test_media_consistency_eval.py` + 你新建的。
- 结束报告：四件逐件说结论；交 J-2 的 SOP 话术钉子文案；交 J-3 的 send 路由一行调用（若未落地）；B 的来源核实结论。

**下一个账号的新对话粘这段：**
```
接手 J-1（AI 出站内容正确性 + 记忆：#171 #175 #176 #177）。按顺序做三件事：
1. 读 D:\boundless\docs\J批_进度总账.md 找 J-1 行，看四件各自做到哪
2. 读 D:\boundless\docs\发版对账_v1.0.74_J1.md —— 这是断点，逐行核对
3. 跑 git -C D:\boundless log --oneline -15 -- engines/chengjie 确认第 2 步的 commit 真在
然后读 D:\boundless\docs\指令_J-1_出站内容与记忆_2026-09-05.md，从第一件未完成的继续。
⛔ store.py / drafts.py / unified_inbox.html / goals/** 都不能改；send 路由属 J-3。
预算上限 $190（含前一轮已花的，按总账余额算）。
```
