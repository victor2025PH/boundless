# 发版对账 v1.0.75 · L-3 工作台状态真相（2026-09-06）

> 指令：`docs/指令_L-3_工作台状态真相_2026-09-06.md`。预算 $150，实花 ≈ $105。四件（A #207 / B #170 / C #196 / D #209）全部完成，**一件一个 commit**。
> 模板 / i18n / CSS / notify-bus / ui-prompt = 热更新即上生产（已双戳：`unified-inbox.css?v=20260906b`、`notify-bus.js?v=20260906a` 双壳、`ui-prompt.js?v=20260906a` 双宿主、`ui-build 20260906-0948` notice）。
> `.py` 七处（`protocol_autoreply.py` / `autosend_worker.py` / `cockpit.py` / `health_watchdog.py` / `unread_aggregate.py` / `platform_session_health.py` / `unified_inbox_setup_routes.py` / `unified_inbox_workflow_routes.py`）**须重启装载**，装载前请走 `scripts\restart_preflight.ps1`。
> ⚠ **共享 index 竞态两次**（见 §「提交归属异常」）：C 项 8 个文件被卷进 L-5 的 `a924426f`；D 项 `a781e36b` 反向卷入了 L-1 暂存中的 5 个文件（已验绿）。代码都在树上、内容完整，只是提交信息归属错位，**不改写历史**。

## 落点表

| 项 | 单 | 修什么 | 落点 | commit | 状态 |
|---|---|---|---|---|---|
| A | #207 需人工闭环 | ① `HANDOFF_AUTO_CLEAR_REASONS`（dup_guard_blocked / empty_reply / generate_error / send_error / quota_* / circuit_open / off_hours）+ `auto_clear_needs_human`：**AI 回上即摘**——`autosend_worker._deliver_one` 在 `_delivered_ok=True` 后加**一处**调用；`protocol_autoreply.hook` 在 `res.sent` 时同调；crisis:* / high_risk / 人工手打 / 无元数据一律保留。② `sweep_stale_needs_human`：启动扫描「末条出站且晚于打标＝已消化」→ 摘（Loki 形态装载即消）。③ 会话头原因条：`_paintAcctBanner` 新增 `.acct-out-banner.handoff` 分支「需人工：{为何}——{何时} 由{谁}打标」+「去回复台处理」（聚焦输入框）/「我知道了（摘标）」（走既有 `PUT /api/workspace/conv/{cid}/tags`，服务端同清 handoff_meta）。④ 互斥：列表行 `has-handoff` → `.conv-handler.h-ai` / `.conv-done-tick` 降灰。⑤ 打标/摘标均落 INFO（谁/何时/为何）；`cockpit.collect_intervention_queue` 回 `needs_human_by_reason`（dup_guard_blocked / crisis / manual…分列）。 | `src/integrations/protocol_autoreply.py` · `src/inbox/autosend_worker.py`（+8 行）· `src/inbox/cockpit.py` · `unified_inbox.html`（`_buildConvItemDom` 一行 + `_paintAcctBanner` 一段）· `unified-inbox.css` · `i18n_packs/inbox_workspace.py`（`inbox.handoff.bar*` / `r_crisis` / `ai_muted_t` zh+en） | `b3a34895` | .py 待装载；模板/CSS 已热更 |
| B | #170 幽灵未读闭环（不重做 J-4 入口） | ① **上屏**：`HealthWatchdog(surface_in_ui=True)`（配置 `health_watchdog.surface_in_ui` 可关，与 webhook 的 `alert_on_warn` 正交）——`_check_buried_conversations` 每 tick 把「有 N 条未读在 M 个已归档会话里（最久 X 小时）」写进工作台通知中心 `notif_queue`（sys_status 按 id 合并、归零即撤），不受 4h 重提节流；`unified_inbox.html` 的 `#buried-hint` **挪出 `.list-header-body`**（那块列表一滚就折起＝skuio 一夜没找到的根因）成 `.list-header` 兄弟节点，常驻直到归零，文案「有 N 条未读在已归档会话里（最久 X 小时）」+「取消归档这 N 条」/「逐条查看」，小时数走 `/api/admin/buried-conversations` 5 分钟节流探测。② **核实浮回**：`tests/test_unarchive_on_inbound.py` 走真实协议入站口 `protocol_bridge.ingest_incoming → ingest_collected_chats → InboxStore.ingest_batch → _unarchive_on_inbound`，真 SQLite：归档后客户再开口 → archived=0 **绿**；归档前历史重放不复活 **绿**。⇒ **被埋 7→8 = 坐席把带未读的会话人工归档，不是浮回缺陷**（与 J-4 A 结论一致，未改接线）。③ 入口引导：角标浮层前 3 次出现带一行「角标数字就是下面这几条：点开任一条，或点『全部标已读』即可清零。归档会话里的未读不计入角标，看列表上方的琥珀横幅」（本机 localStorage `ws_acup_hint_n`）。④ 孤儿未读：`unread_aggregate.purge_orphan_unread`（只动**有墓碑**的已删会话，占位会话不动）+ 看门狗 `_startup_hygiene` 首 tick 一次并落 INFO（同一处顺带跑 A② 的启动扫描）。 | `src/inbox/health_watchdog.py`（只动上屏路径 + 启动卫生扫描，检测逻辑未动）· `src/inbox/unread_aggregate.py` · `unified_inbox.html`（`#buried-hint` 位置 + `_renderBuriedHint` + `_openAcctUnreadPop` 三处）· `unified-inbox.css` · i18n `inbox.buried.banner*` / `view_each` / `inbox.acct.unread_pop_hint` | `19d60567` | .py 待装载；模板已热更 |
| C | #196 断线弹窗出口 + 告警归拢/分级 | ① 出路卡 = **去重连**（messenger/whatsapp 走凭据级直重登，其它平台开收件箱账号抽屉该账号详情）/ **标为已停用** / 登出 / **1 小时** / **24 小时** / **不再提醒此账号**；胶囊动作改「处理 ›」重开出路卡（原「1 小时」不再是唯一出口）。**按账号静默**：本机 localStorage `ws_chandown_mute` 镜像即时生效 + `POST /api/workspace/channel-alert/mute` 落注册表 meta `chandown_mute_until`（-1 永久 / epoch），`_channel_health_snapshot` 按 `channel_alert_muted` 剔除（**换机不丢**）。**标为已停用** `POST …/channel-alert/disable`（主管）：停 worker + `status=offline` + `offline_reason=operator:disabled` → `session_expected_online=False`（横幅不亮 / 看门狗不催）+ 账号栏按 offline 灰显；凭据不清，重新登录一次即归位（归位路径本就清 offline_reason）。② **归拢**（notify-bus）：同 `domKey` 再报**原地更新**（换文案 / ×N / 重置停留，DOM 节点不动＝不重弹）；同屏最多 1 张告警卡（warn/error 且进中心），第二项到来两张折成汇总卡「有 N 项需要注意 ›」点开进通知中心；操作结果（center:false）与 info/success 不参与。③ **分级** `opts.tier`：customer > channel > infra > quality，后两级默认只进通知中心（`forceToast` 覆写）；AI 降级 / 官网连通两处标 `infra`；chandown 标 `channel`。 | `workspace_base.html`（chandown 段 + 两处 tier + setSummaryText 注入）· `src/web/static/workspace/notify-bus.js` · `base.html`（仅 ?v=）· `src/integrations/platform_session_health.py`（`channel_alert_mute_until/muted` / `set_channel_alert_mute` / `mark_account_disabled`）· `src/web/routes/unified_inbox_setup_routes.py`（快照剔除 + 两端点）· i18n `ws.chandown.options/snooze24h/mute_acct*/reconnect_btn/disable_*/cancel`（inbox_workspace）+ `ntf.summary_n`（notify_center） | **`a924426f`**（⚠ 卷入 L-5 B1 提交，见下） | .py 待装载；模板/JS 已热更 |
| D | #209 批量挂链弹层 | ① `ui-prompt.js` 令牌链改 **cp（App）→ tk（工作台壳）→ card/t/bd/p（经典壳）→ bg-panel/text-main/border/accent（收件箱）→ 中性回落**（旧链读的 `--tk-bg-card` / `--bg-card` 三宿主都没定义 → 回落 `#fff`，即白底根因）；新增 `opts.hint / min / max / step / unit / preview`：number 带范围渲染 −/+ 步进器并夹值（越界回弹、↑↓、下界禁 −），`preview(v)` 120ms 去抖实时渲染；按钮显式取宿主正文色（取消不再像禁用）；ok 无有效值置灰。② `WF.bulkStart`：默认 **3 天**（链自带 silence_days 优先，夹 1–90），标题/说明分行，预览「符合条件 N · 将挂 min(N,50) · 跳过 M」走 bulk 端点 `dry_run`，确认即落地（不再二次原生 confirm），结果「已挂 N 个（15 秒内可撤销）」右下卡带「撤销」逐条 `POST chain-executions/{id}/cancel`。③ 路由 `bulk-start`：dry_run 回 `matched/eligible/skipped/limit`（计数扫全量，落地仍按 limit 截断）；真跑回 `exec_ids`。④ 「工作链配置」标题右侧空元素 = 空的 `#seed-msg` 在 flex gap 里占位 → `#seed-msg:empty{display:none}`。 | `shared/copilot/ui-prompt.js` + 镜像 `desktop/renderer/shared/copilot/ui-prompt.js`（`sync_copilot_mirror` 一致）· `workflows.html` · `src/web/routes/unified_inbox_workflow_routes.py` · i18n `wf_bulk_prompt_title/hint/unit_days/ok/cancel/preview*/done_undo/undo*`（workflows_page）· 宿主 `_win_unique.html` + `shared/copilot/app.html`（+镜像）?v= · `ui-build.txt` · `tests/test_admin_route_inventory.py`（登记 C 项两端点） | `a781e36b`（⚠ 反向卷入 L-1 5 文件，见下） | .py 待装载；模板/JS 已热更 |

## 提交归属异常（共享工作树，两次 index 竞态）

| commit | 应属 | 实际 | 卷入内容 | 处置 |
|---|---|---|---|---|
| `a924426f` feat(release): L-5 B1 内测更新渠道 | **L-5** | 含 **L-3 C 全部 8 文件**（`platform_session_health.py` +97 / `unified_inbox_setup_routes.py` +77 / `notify-bus.js` +123 / `workspace_base.html` +184（我的 7 hunk）/ `base.html` / `inbox_workspace.py` +33 / `notify_center.py` +3 / `tests/test_notify_coalesce_196.py` +250） | 我 `stage_hunks --apply` + `git add` 后、`git commit` 前，L-5 跑了无路径 `git commit` 把 index 全量带走 | 不改写历史。#196 的 commit 记 `a924426f`；L-5 落点表若按 commit 数行请**忽略这 8 文件**（与 L-5 B1 无关）。教训：**add 与 commit 必须在同一条命令里**（A/B/D 已如此） |
| `a781e36b` fix(workflows): #209（L-3 D） | **L-3** | 额外含 **L-1 暂存中的 5 文件**（`src/ai/ai_client.py` +31 / `src/skills/skill_manager.py` +83 / `src/utils/greeting_lexicon.py` +29 / `src/utils/proactive_fabrication_guard.py` +299 / `tests/test_greeting_no_fabrication.py` +179） | 我 add+commit 同命令，但 L-1 已先把这 5 文件放在 index 里（agent_probe「index 占用」窗口） | 已验：`tests/test_greeting_no_fabrication.py` **10 passed**、pyflakes 干净，提交不破。**交 L-1**：这 5 文件的 C 项（#208 问候不编事实）代码已在 `a781e36b` 里，L-1 落点表请指向该 commit（或在自己下一条 commit 里补说明），不必重做 |

## 验收口径（交 L-7 回访）

- **#207**：AI 回上即摘标（`autosend_worker` 投递成功 / `protocol_autoreply` 直发成功两路）；打开带标会话回复区上方有红色原因条「需人工：{为何}——{何时} 由{谁}打标」+「去回复台处理」/「我知道了（摘标）」；列表里「需人工」红 chip 与「AI」蓝徽标**不再同亮**（蓝徽标降灰）；装载后首个看门狗 tick（≈60s）Loki 会话红标自动消失（日志 `[needs_human] 启动扫描：带标 N 个，已消化自动摘 1`）；crisis / high_risk / 人工手打的标不会被机器摘。
- **#170**：收件箱默认视图**列表上方**（筛选头下、不随滚动折叠）常驻琥珀横幅「有 N 条未读在已归档会话里（最久 X 小时）」→ 点「取消归档这 N 条」→ 横幅消失、账号栏角标归零、看门狗下 tick 被埋数归 0（日志不再有「被埋会话：」WARNING）；通知中心（铃铛）有一条「有 N 条未读在 M 个已归档会话里…」系统状态，归零后自动撤；第一次点账号角标数字，浮层带一行引导；启动日志若有「孤儿未读归零 N 个会话」即墓碑残值已清。
- **#196**：断线卡片有六个按钮（去重连 / 标为已停用 / 登出 / 1 小时 / 24 小时 / 不再提醒此账号）；选「不再提醒此账号」后 24h 内**不再重弹**（本机即刻熄灭、服务端快照不再返回该账号，换电脑同样不弹）；选「标为已停用」后账号栏该号灰显、看门狗不再催、重新登录即恢复；右下角胶囊按钮是「处理 ›」而非「1 小时内别再提醒」；同一告警每小时刷新只**改文字不重弹**；两项不同告警同时出现时右下只有一张「有 2 项需要注意 ›」。
- **#209**：SOP 管理页「批量挂链」弹层随主题（暗色壳深底浅字、亮色壳白底深字），天数框预填 3、有 −/+、越界回弹 1–90，输入即在框下看到「符合条件 N · 将挂 X · 跳过 M」，「取消」是正常按钮，确认后右下「已挂 N 个（15 秒内可撤销）」带「撤销」；标题「工作链配置」右侧不再有空白占位。截图：暗色/亮色各一张（装机后由 skuio 机取，本机无浏览器渲染）。

## 验证（本机）

- 新增 5 个测试文件 **42 例全绿**：`test_needs_human_autoclear_207.py`（12）/ `test_unarchive_on_inbound.py`（3）/ `test_buried_alert_surface_170.py`（11）/ `test_notify_coalesce_196.py`（7，含 node 跑 notify-bus 源码：同 key 3 次只 1 张卡且同一 DOM 节点、第二项折汇总、infra/quality 不弹）/ `test_ui_prompt_theme_209.py`（7，含 node 跑弹层：默认 3 / 步进夹值 / 预览 / Enter / Esc + 令牌链在四宿主各角色至少一枚已定义）。
- 既有相关测试全绿：`test_handoff_tag_meta` / `test_buried_conv_watchdog` / `test_buried_unread_badge` / `test_autosend_dup_guard_wiring` / `test_chandown_lifecycle`（钉住的 `_chanSnooze(sig, 3600000)` / `onDismiss` / `ws.chandown.dismiss_t` 等全部保留）/ `test_notify_center_denoise` / `test_notif_center_i18n_gate` / `test_no_bare_prompt` / `test_copilot_shared_sync` / `test_template_inline_js_syntax` / `test_admin_route_inventory` / `test_workflow_deal_engine` / `test_workflows_feature_flag` / `test_i18n_zh_hant`（覆盖率仍 ≥99%，未 regen）/ `test_i18n_extra_langs` / `test_template_inline_color_ratchet`（我改的文件无新增内联色）。
- **树上既有红（非本条引入，HEAD 与工作树计数一致）**：`test_template_bare_fetch_ratchet`（多文件超天花板 0 + personas 台账过期）、`test_legacy_blue_ratchet`（`unified_inbox.html` 17 > 15，HEAD 亦 17）、`test_notify_unification::test_native_dialog_ratchet`（alert 82>72 / confirm 144>135 / prompt 25>24，含 personas.html 等他线在途）、`test_template_inline_color_ratchet::test_th_token*`（personas.html `--th-ink-red0/blue0` 未定义，L-2 在途）。交值守/对应线。

## 交出去

- **交 L-6**：C③「基础设施类告警」（LAN GPU / Embedding 探测红噪音）的真解在 **L-6 B 桌面剥离 LAN 默认值**，本条只做 notify-bus 分级归拢（infra 默认不弹）；L-6 B 落地后 desktop_mode 下这一级应几乎不再出现。
- **交 L-5**：`a924426f` 内 8 个 L-3 文件的归属说明（见上）。
- **交 L-1**：`a781e36b` 内 5 个 L-1 文件的归属说明（见上）。
- **交 L-7**：#170 / #173 的 fixed 时机按 D-L8 等本条收口——本条已收口，可随 1.0.75 一并标 fixed；四单验收口径见上。
- **未做 / 刻意不做**：#207「会话被归档也摘标」未单独接归档端点——归档会话本就不在 `list_tagged_conversations`（archived=0）与 rail 计数里，客户再开口浮回后 AI 回上即摘；`buried_conv_alert` 未接 SSE（通知中心走 notif_queue 回放，页面刷新可见；实时性由收件箱横幅承担）；D 项暗/亮截图本机无法出，装机后取。
