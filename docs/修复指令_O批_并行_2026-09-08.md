# 修复指令 O 批（客户识破 AI · 记忆抽取死 · 摸底被动 · 引用回复）—— 并行公共底座

> **本文件只是「索引 + 去向表 + 文件归属 + 老板决策」的公共底座，不要拿本文件开对话。**
> 每条指令各自成文，**一条指令 = 一个新对话**。开对话粘贴话术在各指令文件末尾，也汇总在 `docs/O批_进度总账.md`。
>
> 依据：09-08 00:49–02:30 skuio 14 份报告（1.0.76）→ #252–#255 + 挂 #183 #201；三日复盘 `docs/三日复盘_2026-09-05至08.md`。N 批四线 + M-7 已收工（1.0.77 代码齐），N-5 收口在跑（B/D 05:22 已提交）。
> **结论：这批是三天来最重的一次——WhatsApp 客户 Sinue 21:02 原话「Grammar, typing speed, and inconsistencies … obviously handled by an AI assistant, so stop writing to me」→「Never write me again」，系统在 `stop_contact` 命中 `risk=high` 的情况下又发了两条（09-04「L3/L4 全放行只记录」决策没有豁免项），240 分钟全局零 typing/pacing 日志、起草→发出固定 12–13 秒、账号名 Vanessa 人设名 Mizuki、一天 5 条 em dash、客服三段式收尾。同时发现干净包记忆抽取从安装起就是死的（`memory.extract.intents` 不在最小配置里，第三次「出厂默认没进基线」）。**
> 目标版本 **1.0.78**（若 N-5 尚未打 1.0.77，O-1 A / O-2 A 两件止血可并入 1.0.77——N-5 开 E 段前看本总账 O-1 / O-2 行）。

| 指令 | 预算 | 类型 | 内容 | 文件 |
|---|---|---|---|---|
| **O-1** | $200 | 拟人与反识破（P0，对客 + 封号风险） | A `stop_contact` 硬停（最多一条告别 → 冻结 → 切人工）+ risk=high 不直发（09-04 放行的豁免项）/ B 出站确定性去 AI 标点 + 总结句式 / C 陪伴域系统提示词重写 + 客服腔守卫 / D 拟人节奏默认档（读延迟 / 打字时长 / composing / 夜间）/ E 账号名≠人设名提醒 + 出站文本落日志 + 发版盲评门禁 | `docs/指令_O-1_拟人与反识破_2026-09-08.md` |
| **O-2** | $100 | 记忆抽取退化（P0，干净包首日即死） | `memory.extract.intents` 进产品基线 / 启动打印白名单、空即 WARNING / origin=manual 出站同抽 / 私事只进客户记忆 / 记忆页「自安装新增 N」 | `docs/指令_O-2_记忆抽取基线_2026-09-08.md` |
| **O-3** | $150 | 目标摸底主动腿（P1） | auto=3 sent_24h=0 到期未发根因 / 第 1 天起每天 ≥1 自然摸底问句 + 客户活跃自适应 / 注入变硬带 target 槽 / 卡片诚实三计数（注入 / 主动出手 / 已采集）/ 「现在就问一个」/ 采集链接 O-2 | `docs/指令_O-3_目标摸底主动腿_2026-09-08.md` |
| **O-4** | $120（MVP） | 引用回复与表情回应（需求评估 + MVP） | 一期：#37 已有「连发 ≥2 带引用」之上加 旧消息>30min 必引 + 10–15% 随机基线 + 禁引约束 + `[quote]` 日志 + 开关默认开；二期 / 三期只评估不做 | `docs/指令_O-4_引用回复MVP_2026-09-08.md` |
| **O-5** | $60 | 值守（不与 N-5 重叠） | 回 3HNCJ7 排期答复 / WYNN22 从 #201 改立 P0 单 / HM7XBA 从 #255 改挂 #236 族 / #253 并 #252 / 回访口径汇总交 N-5 E 或 1.0.78 | `docs/指令_O-5_值守_2026-09-08.md` |

**投放顺序**：**O-1 A 与 O-2 A 是止血，先开**（各自独立文件）→ O-1 其余 / O-3 / O-4 并行 → O-5 随时。N-5 E 打包时看 O-1 / O-2 行：止血件已收工就一并进 1.0.77。
**跨对话 / 跨账号交接唯一入口**：`docs/O批_进度总账.md`。
**背景**：`docs/三日复盘_2026-09-05至08.md`、`docs/发版对账_v1.0.77_N3.md`（业务域单一来源 `business_domain`；**conversion 包 = 陪伴包，系统提示词原样未动**）、`docs/发版对账_v1.0.77_M7.md`（目标卡说真话）、`docs/发版对账_v1.0.75_L1.md` A（拆条出厂关）、`docs/发版对账_v1.0.72.md` §I-1（09-04 风控全放行影子档）。证据 `tmp_diag\{EC7QZM,K9F7PU,XAM4KV,FW78ZP,Q9H2HM,RYE8Y8,8FJDUK,584AUV,WYNN22,TDNJHQ,KT992X,78W8DN,5XRQ7C,HM7XBA,47KNBV,3HNCJ7}\`。

## 去向

| 去向 | 单 / 报告 | 说明 |
|---|---|---|
| O-1 | **#252**（P0 confirmed；EC7QZM + 已并入 #253 的 K9F7PU XAM4KV FW78ZP Q9H2HM RYE8Y8 8FJDUK）**#254**（P0 confirmed；KT992X 78W8DN 47KNBV 3HNCJ7 + 已并入 #255 的 TDNJHQ） | 8FJDUK「四处连带」的①系统提示词归 O-1 C；②③④ N-3 已做（1.0.77）。#253 / #255 已 closed（O-5 09-08 08:30） |
| O-2 | **#256**（WYNN22，P0，自 #201 摘出改立，O-5 09-08 08:30）584AUV（#183） | 与 L-5 同根：基线缺键 |
| O-3 | **#257**（HM7XBA 5XRQ7C，P1，自 #255 摘出改立、挂 #236 族，O-5 09-08 08:30） | 与 M-7 D 衔接：卡片真相已随 1.0.77，主动腿与采集链没做；采集链依赖 #256 |
| O-4 | #254（47KNBV 3HNCJ7） | #37（1.0.74）已有基线：客户连发 ≥2 条带引用；欠 3HNCJ7 二期 / 三期工期估算回 #254 |
| O-5 | 上述归位 + 3HNCJ7 答复 —— **已做**（`发版对账_v1.0.78_O5.md`） | N-5 正在做并单 / 1.0.77，O-5 不碰 N-5 清单 |

## 文件归属矩阵（共享工作树；开工先 `scripts\agent_probe.ps1` 看 ACTIVE 再动）

| 文件 / 目录 | 归属 |
|---|---|
| `src/inbox/autosend_worker.py`（**只动**：risk=high / stop_contact 门禁一处 + 拟人节奏调用点）、`src/inbox/drafts.py`（**只准**读 `shadow=stop_contact` 结果，不改判定）、新 `src/inbox/outbound_humanize.py`（确定性去 AI 标点 / 句式）、`src/inbox/outbound_translate*`（`pass_gate_only` 门挂后处理）、`src/utils/persona_guard.py`（客服腔黑名单扩）、`domains/conversion/prompts/**`（陪伴系统提示词重写——**按 `business_domain` 选文件**，销售域保留旧稿）、`src/inbox/reply_pacing_settings.py` + `reply_settings.html`（「拟人程度」三档）、`src/inbox/channel_adapters.py` / 边车（composing）、`src/integrations/protocol_autoreply.py`（冻结切人工）、对应 tests | **O-1** |
| `config/config.desktop.min.yaml`、`src/utils/feature_registry.py`（A 类基线加 `memory.extract.intents`）、`src/skills/skill_manager.py` **只动** `should_extract_intent` 调用处日志与 origin=manual 抽取入口两处、记忆页模板、对应 tests | **O-2** |
| `src/companion/goals/{planner,service,sprint_ticker,liveness}.py`、`cp-goal.js` 双树、`goal_routes.py`、`i18n_packs/goals.py`、对应 tests | **O-3** |
| `src/inbox/reply_quote_policy.py`（#37 基线在此）、`autosend_helpers.py`（引用发送接线，**只动**引用决策调用）、边车 quoted 能力位、`reply_settings.html` 引用开关、对应 tests | **O-4** |
| `tools/duty_*`、台账（只经端点 / `duty_*`）、`docs/**` | **O-5** |
| N-5 正在动的：`tools/duty_auto_ticket.py`、并单脚本、发版脚本 | **N-5，O 批不碰** |
| `store.py`、孤儿线、DY / QQ 线文件 | **谁都不碰** |

## 老板决策（默认按此执行；不同意的划掉）

| 决策 | 默认（= 我的建议） | 为什么 | 影响 |
|---|---|---|---|
| **D-O1** 09-04「L3/L4 全放行只记录」加豁免 | **`stop_contact` / `self_harm` 命中一律硬停**：最多一条告别（人设口吻、不解释是 AI）→ 该会话冻结（不起草 / 不关怀 / 不推目标）→ 切人工 + 列表标签「客户要求停联」；`risk=high` 稿在全自动下不直发、转人工审。词表补 never write me again / don't contact me / leave me alone / stop texting | 客户说「别再写了」之后再发两条，是投诉与封号的直接原因；09-04 放行的初衷是敏感词误伤，不是这个 | O-1 A |
| **D-O2** 出站文本确定性后处理 | **采纳 RYE8Y8 / 78W8DN**：U+2014/U+2013 → 逗号或拆句、弯引号 → 直引号、分号 → 句号、`…` → `...`、°C 等 → 口语、冒号列表 / 项目符号 → 散文、连续 ≥2 感叹号或 emoji 串截断；删结尾总结 / 感悟句与 isn't it / aren't they 反问尾；一条消息 ≤2 句默认（MaxTokens 60–80）；`punct_fix=n style_fix=n` 落日志；**verbatim 原文直发与人工手发除外** | 提示词禁令模型遵守率低，一天 5 条 em dash 跨 3 个账号；确定性处理才可验收（200 样例 0 em dash） | O-1 B |
| **D-O3** 陪伴域系统提示词重写 | **重写为「私人聊天」底稿**：第一人称、口语短句、有情绪、允许不完美；明确禁止客服 / 助理 / 品牌代言语气与 I hear you / I understand / Take care / I'll be around / Feel free to / Let me know if / 如有需要 / 很高兴为您；覆盖自动回复 / 关怀润色 / 目标冲刺 / 问候 / 翻译改写；按 `business_domain` 选文件，销售域保留旧稿 | 1868 字销售/客服底稿是客服腔之源（9JNGFX → XAM4KV 三轮），词表守卫只能摘叶子 | O-1 C |
| **D-O4** 拟人节奏默认档 | **采纳 FW78ZP 骨架**：读延迟 3–6s + 每 10 词 +1s（上限 20s）±30%；思考 3–8s；打字 英文 35–45 wpm / 中日韩 60–90 字/分 ±30%（上限 90s）；边车持续 composing、发出前 1s 停；客户连发合并等 8–15s 回一次；首次接触 / 沉寂 >6h 首回延 1–5 min；夜间按账号时区 01–08 不回、08 后随机 0–40 min 补（可关，默认开）；界面顶部「拟人程度」自然（默认）/ 偏快 / 偏慢 一键改全部；专家参数收进折叠区 | 起草→发出固定 12–13 秒、全天秒回、零 composing 是客户点名的 typing speed；新用户不该自己调参数 | O-1 D |
| **D-O5** 记忆抽取默认 | **`memory.extract.intents` 进产品基线**（与服务器 `config.yaml` 同值：direct_chat / small_talk / greeting / complaint），或 `match_all: true` 并排除 greeting / small_talk / stop_contact；启动日志打印白名单，空即 WARNING 不得写「已启用」；`origin=manual` 出站同走抽取 | 干净包安装即死、卡片写「已启用」——第三次「出厂默认没进基线」 | O-2 |
| **D-O6** 引用回复 MVP | **做一期**（Telegram reply_to + WhatsApp quoted；#37 基线之上加 旧消息 >30 min 必引 + 10–15% 随机 + 禁引约束 + `[quote]` 日志 + 开关默认开）；二期（高概率 / 镜像 / 三档）与三期（表情回应）只评估排期不做 | 真人习惯性引用，是低成本的真实度增量；#37 已有骨架 | O-4 |

## 共同纪律 / 省钱纪律 / 断点续传

与 J/K/L/M/N 批完全相同，见 `docs/修复指令_J批_并行_2026-09-05.md` 对应三节。再钉：提交前 `git diff --cached --name-only` 为空再 add；**每个 commit 落地后立刻补落点表一行**；模板 / i18n 热更新即上生产；对外答复一律走 `tools/duty_reply.py`。
