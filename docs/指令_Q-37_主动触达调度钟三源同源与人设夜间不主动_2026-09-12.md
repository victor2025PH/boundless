# 指令 Q-37：主动触达的调度钟三源同源（客户 tz → 人设 tz → 服务器）+ 人设夜间不主动 + 文本时刻与调度钟同一只尺（#318）

> 预算 **$60**。工单 **#318（VV7BRY，skuio，1.0.84，09-12 21:23）**：WhatsApp Nori/19892968016 × Michael/46 New York，本地 **15:22** AI 主动发「It's 3am here and I'm wide awake wondering how the Marina's treating you lately」。落点表 `docs/发版对账_v1.0.85_Q37.md`。
> 前置：Q-29（`companion/peer_time.resolve_peer_tz`：`location:confirmed` 城市 → IANA，New York → America/New_York）、Q-8 F（人设当地钟 `excuse_budget.build_time_schedule_addendum`）、实施53 P2-2（`proactive_topic._persona_clock_note` 人设钟批注）、2026-08-19「上午 10:12 晚安」事故（`schedule_clock trust=replace-only`）都已在库——本条只**接线**，不新造时钟。

## 事实（HEAD `1ca3425b` 代码只读核对；报告日志窗 18:23–21:23 没盖到 15:22，链路归属要用 skuio 的 `outreach_log` 坐实）

- **15:22 上海 = 03:22 纽约**。「It's 3am here」不是幻觉：`proactive_topic._persona_clock_note`（L1947–1978）在人设居住地与调度钟时差 ≥3h 时把**人设当地钟**批注进 prompt，Nori 人设落在美东 → LLM 如实写 3am。**错的是发送时机**：主动消息在客户（纽约）与人设都是凌晨 3 点时发出。
- 安静时段闸只认两只钟：`integrations/companion_proactive.py` L243–250 无 `user_clock_provider` 时用**服务器钟**整轮判；L358–369 有 provider 时按会话 `_resolve_user_clock(cid)` → `_user_in_quiet_hours(...)`，且 08-19 事故后 `schedule_clock` **trust=replace-only**——只有显式核实过的客户钟才替换服务器钟。`user_clock_resolver` 的居住地启发式（Q-19 C 收紧为 `place_value_ok`）**没有读 Q-29 的 `resolve_peer_tz`**（`location:confirmed` = New York 本应是最强的核实信号）→ 该会话回落服务器钟 15:22 → 不在 23–8 → 放行。
- 人设钟只进 prompt 不进闸：没有任何一处判「人设当地是深夜就别主动开口」——于是产品行为变成「一个凌晨三点睁着眼主动找你聊天的人设」。
- `care_dispatcher`（主动关怀，Q-4 D）安静时段是另一套（客户 → 人设 → 账号 → 服务器）；`proactive_topic` 与它**不同源**。到底是哪条链发的 15:22 这条：查 skuio `outreach_log` 该会话 15:2x 的 `batch_id`（`care:*` = 关怀；否则 proactive）——先坐实再改对应链，另一条链顺手对齐。
- 「Marina」：客户只发过一张水边房子照片 → 大概率是入站图描述 / 记忆抽取把「码头 / marina」写成了事实再被主动开场引用。**归 Q-36 B**（入站图 caption 经 FactGate `kind=observation`，不当事实用），本条不做；请在落点表记 skuio 该会话 episodic 里是否有 marina 事实（回访时让 skuio 在记忆页搜一下即可）。

## 要做的四段

| 段 | 内容 | 文件 |
|---|---|---|
| **A 调度钟三源接线** | `user_clock_resolver` 新增第一源：Q-29 `resolve_peer_tz(profile)`（confirmed > mentioned，location > residence）命中 → 作 **replace 级**核实钟（与显式时区信号同级，不放宽 trust 语义）；无客户钟 → **人设钟**（`persona_location.resolve_place_with_fallback` 已有）→ 服务器钟。收成一个 `resolve_schedule_clock(cid) -> (hour, source∈peer_tz\|persona_tz\|server\|explicit)`，`companion_proactive.plan_proactive_sends` 与 `care_dispatcher` 安静时段**都只读它**；plan / diag 带 `clock_source` | `companion/user_clock.py`、`user_clock_resolver`、`integrations/companion_proactive.py` L243–369、`contacts/care_dispatcher.py` 安静段 |
| **B 人设夜间不主动** | 人设当地 23–8（复用 `quiet_start_hour/quiet_end_hour`）且客户近 30 min 无入站 → 本轮不发，diag `persona_quiet_hours`，日志 `[proactive] skip=persona_quiet conv= persona_hour=`；客户刚活跃（≤30 min 有入站）放行——那是接话不是深夜骚扰 | `companion_proactive.py`（紧跟 `user_quiet_hours` 判定之后） |
| **C 文本时刻与调度钟同一只尺** | 既有 `world_clock_guard.detect_daypart_conflict(text, hour)`（L2828 问候词×时刻守卫）加第二把尺：文本里的**人设自述时刻**（「3am here / 半夜了 / 刚起床」）按人设钟核，客户时段词按调度钟核；任一矛盾 → 重写一次仍矛盾放弃本轮（沿用 L2836–2846 既有分支，不新建） | `proactive_topic.py` L2820–2850、`world_clock_guard.py`（只加人设钟入参） |
| **D 门禁 + 回放** | `tests/test_schedule_clock_q37.py`：① New York confirmed + 服务器 15:22 → `clock_source=peer_tz` hour=3 → 不发；② 无客户钟 + 人设美东 → `persona_tz` 03 → 不发；③ 客户 25 min 前刚发过 → 放行；④ 服务器钟回落且 14:00 → 照旧发；⑤ VV7BRY 回放：同上下文生成「It's 3am here…」→ 被 C 拦（本就不该到生成，①已拦，此例验尺子）；Q-29 `test_peer_time_q29` 86 例零改动 | tests |

## 红线

- Q-29 `peer_time` 语义与 86 例门禁一字不改；`schedule_clock` **trust=replace-only** 不放宽（只是多认一个已核实源）。
- 不改 pacing / 冷却 / 回复率反哺任何数字；不动 care 的文案与预算逻辑，只换它读钟的那一行。
- `_persona_clock_note` 继续注入（人设知道自己几点是对的），B 段是「几点别开口」不是「别说几点」。
- `.py` 改动须 zhiliao 重启才生效。

## 开新对话粘贴

```
读 docs/指令_Q-37_主动触达调度钟三源同源与人设夜间不主动_2026-09-12.md，按它做 Q-37（#318 VV7BRY：先用 skuio 报告里的会话 whatsapp:19892968016:<Michael> 定 15:22 那条是 care 还是 proactive 链（outreach_log batch_id），再做 A user_clock_resolver 接 Q-29 resolve_peer_tz 作 replace 级核实钟，收成 resolve_schedule_clock(cid)->(hour, source) 让 companion_proactive 与 care_dispatcher 安静时段同源读它，diag 带 clock_source / B 人设当地 23–8 且客户 30min 无入站 → 不主动，日志 [proactive] skip=persona_quiet / C world_clock_guard 加人设钟这把尺，人设自述时刻与人设钟矛盾、客户时段词与调度钟矛盾 → 重写一次仍矛盾放弃本轮（沿用既有分支）/ D tests/test_schedule_clock_q37.py 五例 + Q-29 86 例零改动）。预算 $60。先读 Q-29 落点表 docs/发版对账_v1.0.84_Q29.md 与 companion_proactive.py L243–369、proactive_topic.py L1947–1978 / L2820–2850（都已在库，只接线不新造时钟），再 scripts\agent_probe.ps1 -Intent 登记；schedule_clock trust=replace-only 不放宽；不改 pacing / 冷却数字；「Marina」不在本条（归 Q-36 B），只把 skuio 记忆里是否有 marina 事实记进落点表。每个 commit 带 #318 并立刻补落点表 docs/发版对账_v1.0.85_Q37.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-37 行并 agent_probe -Done。
```
