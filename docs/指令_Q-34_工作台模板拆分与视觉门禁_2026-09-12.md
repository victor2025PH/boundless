# 指令 Q-34：工作台模板拆五区 + 组件级视觉门禁 + 长期红门禁清零（工程底座，独立周）

> 预算 **$200**。五视角方案 P3-19～22（`docs/分析_聊天工作台_五视角美化优化方案_2026-09-12.md` §五）。落点表 `docs/发版对账_v1.0.86_Q34.md`。
> **排期红线**：不与 Q-30～Q-33 同周开工——拆文件会让所有在途 `unified_inbox.html` hunk 失效。开工前 `scripts\agent_probe.ps1` 看 ACTIVE 里没有任何 inbox 模板线。

## 事实

- `unified_inbox.html` ≈ **23,400 行**单文件：`<style>` 块 + 五个功能区 + 数十个 JS 模块，内联 `onclick` **404 处**；多线并行只能靠 `stage_hunks.py --mine` 前缀所有权（`ay- / rk- / ad- / tko- / cs- / xl-`）分 hunk，冲突与误暂存是 Q 批最大的返工源。
- 没有截图基线：Q-31 / Q-32 / Q-33 的「美化」只能靠人眼验，回归无门禁。
- 14 项长期红门禁（五视角 §三）没有归零计划。

## 要做的四段

| 段 | 内容 | 位置 |
|---|---|---|
| **A 拆五区 include** | `templates/inbox/_header.html` / `_list.html` / `_thread.html` / `_composer.html` / `_xlate.html` + `static/js/inbox/{header,list,thread,composer,xlate}.js`；`unified_inbox.html` 只留骨架 + include + 全局变量声明；`<style>` 块全部迁 `unified-inbox.css`（Q-33 之后做，diff 干净）；**先做纯移动 commit（零逻辑改动，`git diff --color-moved` 可证），再做第二个 commit 改事件委托**；`stage_hunks.py` 加 `--file` 多文件支持，文件归属矩阵改成文件所有权 | 模板 / 静态 / `tools/stage_hunks.py` / `docs/修复指令_Q批_并行_2026-09-09.md` 归属矩阵 |
| **B 内联事件委托** | 404 处 `onclick` → `data-act` + 各区 `.js` 一个委托入口；`_uiBeacon` 埋点保留同名 | 五个 `.js` |
| **C 组件级视觉门禁** | Playwright：三视口（405 / 1024+右栏 / 1280）× 浅深主题 × 四区（头部 / 状态带五态 / 列表行 / composer 两层）截图基线进 `tests/visual/baseline/`；`scripts\gate_sweep.ps1` 加 `-Visual` 开关（默认关，R 批门禁开）；像素差 >0.5% 红并落 diff 图 | `tests/visual/`、`gate_sweep.ps1`、SOP `.cursor/rules/chatx-desktop-install.mdc` 加一行 |
| **D 长期红门禁清零** | 14 项（`inline_color_ratchet ×3` / `legacy_blue ×2` / `workspace_emoji_ratchet` / `copilot_tiny_font` / `input_theme_contrast` / `orphan_refs` / `silent_exception` / `takeover_trigger_surface` / `assistant_qa_eval ×2` / `deploy_profiles`）：Q-33 已清的封顶 0；其余每项一个 commit 清零或写明豁免理由进门禁白名单文件（不是删门禁） | 各门禁脚本 |

## 红线

- A 段第一个 commit **必须**是零逻辑改动的纯移动；回归 `scripts\regression.ps1` 全量与拆分前同红同绿（对照上一个 release commit 临时 worktree）。
- 拆分后所有 `T('inbox.*')` 词条键不变；`_uiBeacon` 事件名不变（运营看板依赖）。
- 独立周：ACTIVE 意向里有任何 inbox 模板线就不开工。

## 开新对话粘贴

```
读 docs/指令_Q-34_工作台模板拆分与视觉门禁_2026-09-12.md，按它做 Q-34（五视角 P3-19～22：A unified_inbox.html 拆 templates/inbox/_header/_list/_thread/_composer/_xlate.html + static/js/inbox/*.js，<style> 迁 unified-inbox.css，第一个 commit 纯移动零逻辑改动（git diff --color-moved 可证），stage_hunks.py 加多文件支持并把归属矩阵改成文件所有权 / B 404 处内联 onclick 改 data-act 事件委托，_uiBeacon 事件名不变 / C Playwright 三视口 × 浅深主题 × 四区截图基线进 tests/visual/baseline/，gate_sweep.ps1 加 -Visual 开关，像素差 >0.5% 红 / D 14 项长期红门禁逐项清零或写豁免白名单不删门禁）。预算 $200。开工前 scripts\agent_probe.ps1 看 ACTIVE 里没有任何 inbox 模板线（Q-30～Q-33 全收工）才 -Intent 登记；纯移动 commit 后全量 scripts\regression.ps1 与上一 release commit 临时 worktree 对照必须同红同绿；T('inbox.*') 键与 _uiBeacon 事件名一字不改。每个 commit 带 Q-34 并立刻补落点表 docs/发版对账_v1.0.86_Q34.md 一行；git add 显式路径且提交前 git diff --cached --name-only 为空。收工只改 docs/Q批_进度总账.md 的 Q-34 行并 agent_probe -Done，并在 .cursor/rules/chatx-desktop-install.mdc 门禁段加 -Visual 一行。
```
