# 发版对账 v1.0.79 · P-2 登录 / 切档不群发 + 停联名单持久 + 自聊排除（#259 #252）

> 指令：`docs/指令_P-2_登录切档不群发与停联持久_2026-09-08.md`；底座 `docs/修复指令_P批_并行_2026-09-08.md`（决策 **D-P1 72h** / **D-P4 账号级停联名单**）。证据 `tmp_diag\{H3BAJD,ZH3ZQ5,A7PB2F}`。建在 O-1 A（`docs/发版对账_v1.0.78_O1.md` A 行：`stop_contact.py` 冻结 = 会话档位 + 标签）之上做持久层。
> 执行：2026-09-08 15:4x 开工（`agent_probe -Intent` 已登记）。**每个 commit 落地后立刻补一行。**

## §〇 先答一问：为什么登录 / 切全自动会群发？

**答：三段链每一段都缺一道闸，历史消息一路被当成「刚到的新消息」。**

1. **边车把历史当新入站推**：`services/whatsapp-baileys/server.js` 登录后 `syncFullHistory: true`，`messaging-history.set` 携带每个会话的末尾 `WA_BACKFILL=20` 条消息 → `pushWaMessage(entry, msg, true)` 逐条 `POST /api/internal/protocol/ingest`，payload 与实时消息**零差别**（H3BAJD 15:00:12 pairing completed → 15:00:15–16 六个老会话进箱）。占位会话拉历史（`fetchMessageHistory` → `syncType=ON_DEMAND`）同一条路（ZH3ZQ5 14:37:43 「WA history fetch requested」同秒起草）。Telegram `backfill_telegram` 首连 `get_dialogs` 末条走 `emit_incoming` 实时 sink，同病。
2. **后端没有「回填」概念**：`ingest_incoming` → `normalize_chat(unread=1)` → `ingest_collected_chats` 对每条新插入的入站行发 `_new_inbound_cbs`（auto-draft / 关怀捕获 / 影子扫描）+ SSE + 意图情绪 + 记忆抽取；`normalizer.py` 只用 `is_self` 定方向，`peer == self`（Message yourself）也照常当客户。
3. **起草层没有年龄闸**：`drafts.auto_generate_draft` 只看冻结 / 幂等 / 风险；`l1_reason._first_contact` 只数「入站 ≤1」把老会话当首次接触 → 全自动下就是 L2 直发（`chain=opener` 还会编造「you mentioned that hiking trail」）。停联冻结（O-1 A）记在会话标签 + 档位上，clean 重装即丢 → Sinue 又被起草。

**修法 = 三道闸 + 一张清单 + 一份名单**：① 边车 / 桥打 `backfill` 标，落库不触发（A）；② `auto_generate_draft` 入口 72h 年龄闸，超龄进「沉寂会话待你决定」清单（B / D）；③ 切档 / 登录确认框两栏，默认只碰「最近 3 天有来信」（C）；停联名单账号级持久 + 登录同步后扫描复冻（E）；自聊会话 `self_chat=1` 排除全部自动化（F）。

## 一、根因（每张单一句）

| 单 / 报告 | 症状 | 根因 |
|---|---|---|
| #259 H3BAJD | 重装登录 3 秒对 6 个老会话 `auto_generate_draft L1 reason=first_contact`，含昨晚「Never write me again」的 Sinue | 边车 history.set → 无 backfill 标 → new_inbound 回调；停联冻结随重装丢；无年龄闸 |
| #259 ZH3ZQ5② | 切全自动同秒对 6 月无消息的会话起草 opener | 占位会话 ON_DEMAND 拉历史同一条路；无年龄闸 |
| #252 A7PB2F / H3BAJD① | `whatsapp:17345893728:17345893728` 自聊入箱触发自动化 | normalizer 不识别 peer == self |

## 二、落点（每 commit 一行，落地即写）

| 项 | commit | 落点 |
|---|---|---|
| **A / F** 回填标记落库不触发 + 自聊会话（#259 #252） | `9a9877a7` | **边车** `services/whatsapp-baileys/server.js`：`pushWaMessage(entry, msg, skipEmpty, backfillSource)` 第四参；`messaging-history.set` 传 `onDemand ? "resync" : "history_set"`、`messages.upsert` 按 Baileys 语义 `type !== "notify"` → `upsert_<type>`；payload 多两键 `backfill: true` / `backfill_source`（实时消息 undefined，JSON 不带）。**桥** `src/integrations/protocol_bridge.py` `ingest_incoming(..., backfill=False, backfill_source="")`：回填 / 自聊 → `source.backfill=1` + `backfill_source` / `source.self_chat=1`，`unread=0`，`publish_events=False`，出站镜像事件同闸；每条一行 `[inbound] backfill=1 self_chat=0 source=history_set conv=… ts=… inserted=1 skipped_automation=1`；`backfill_telegram` payload 打 `backfill=True / tg_dialogs`（`deep_backfill_tg_history` 本就走 `ingest_thread` 无回调，核对无需改）。**落库分流** `src/inbox/ingest.py` `ingest_collected_chats`：`store.ingest_batch` 之后 `_quiet_inbound(conv, lm)`（`normalizer.is_backfill_source` / `is_self_chat`）命中 → `continue`——不清 snooze、不发 SSE、零 `_new_inbound_cbs`（auto-draft / care_capture / 影子扫描）、不更新意图情绪、不做 deep_persona 抽取、不判 CSAT。**normalizer** `src/inbox/normalizer.py`：`BACKFILL_KEY / BACKFILL_SOURCE_KEY / SELF_CHAT_KEY`、`is_backfill_source(source)`、`backfill_source_of`、`is_self_chat(platform, account_id, chat_key, source)`（WA / TG：`normalize_chat_key(chat_key) == account_id`；default 账号判不出；其它平台只认 source 显式标）；`store_row_to_chat` 多回 `self_chat`，自聊行 `unread / synced_unread` 恒 0。**路由** `src/web/routes/unified_inbox_stored_read_routes.py` 未动；`src/web/routes/unified_inbox_account_routes.py` `api_protocol_ingest` 读 body `backfill / backfill_source` 透传 + 回填 / 自聊**不进** `maybe_auto_reply` 协议链直发（自聊记 `[inbound] self_chat=1 … skipped_autoreply=1`）。tests `tests/test_backfill_no_automation_p2.py` 5 例：回填落库 + 零回调 + unread 0 / 之后真实入站照常触发 / source 派 backfill 生效 / 自聊判定 7 例 + 落库零回调 + `self_chat=True unread=0` / 边车 + TG + 路由接线静态钉。装载：`server.js` **需重启边车**；`.py` 需重启后端。 |

## 三、验收

| 项 | 门禁 | 结果 |
|---|---|---|
| A 回填落库不触发 | `tests/test_backfill_no_automation_p2.py` | 5 绿 |
| A 相邻回归 | `test_protocol_bridge / test_unarchive_on_inbound / test_e1_conv_ctx_e2_auto_draft_e3_health / test_media_fetch_backfill` | 118 绿 |

## 四、越界 / 未做 / 待老板 / 交接

- `src/inbox/ingest.py` / `src/integrations/protocol_bridge.py` / `src/web/routes/unified_inbox_account_routes.py` 不在归属矩阵 P-2 行点名，但它们就是「边车 → 落库 → 回调」这条链的三个必经点（矩阵写的 `normalizer.py` 只是判据所在）；三文件 git status 干净、意向板无人认领，按最小改动执行并在此登记。`normalizer.py` 上微信线的 `wechat_kf` 显示名 hunk 用 `stage_hunks` 排除、未卷入。
- `messages.upsert type=append` 一律按回填：Baileys 语义 append = 「已在会话里补进来的」（含 `sock.sendMessage` 自己发出的回显 fromMe=out）；出站方向本就不触发自动化，编排器镜像与回显同 wamid 去重，无行为变化。若某版本把离线期真新消息也标 append，代价是这批消息不自动回而进「沉寂清单」（B / D 段兜底），不会丢消息。
