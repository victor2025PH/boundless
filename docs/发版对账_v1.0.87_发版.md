# 发版对账 v1.0.87 — 发版线 R87（单一版本：P0 离线回标准档 / 配文语言 / 生成图闸 + P1 拦截人话 / 体检 / 承诺收口 + P2 克隆声旁注 / 生活小事 / 急需处理 → 一个 clean 包 → **公共渠道 + 内测桥接 + 官网 + 播报**）

> 规矩：`.cursor/rules/chatx-desktop-install.mdc` 「单一版本（D-Q11）」——一个 semver = 一个 clean 包。前例 `docs/发版对账_v1.0.86_发版.md`。
> 范围：公共渠道 1.0.86 → 1.0.87。Q-26 C–E / Q-28 / Q-32～Q-34 **不进包**。#312 #314 保持 confirmed。#279 / #265 本版只核定、不标 fixed。
> 接手方：先 `git -C D:\boundless log --oneline -16` 核对本表 commit 真在。意向板 slug `r87-release-1-0-87-single-clean-seats-first`。
> 执行：2026-09-17 03:2x → 14:5x，见 §5。

## 0. 一句话

**1.0.87 单包** `ChatX-Setup-1.0.87.exe` **528,524,058 B** sha256 `998dbc2e6d8e0b37cdea19337b84b90587d5e8f9eb32ff0d2fe017adadd10f76`（504 MB，clean 形态，`MZ` 头核过，build-info flavor=clean commit=`f10e0855`，干净 worktree `D:\boundless-r80@f10e0855`）。内容 = 1.0.86 全部 + **P0** 无限制档离线回标准档 / 配文跟会话语言 / 真人相册默认不发生成脸 + **P1** 拦截人话 / 体检「为什么没回」/ 承诺发图收口 + **P2** 克隆声语种旁注 / 生活小事不跨日重讲 / 急需处理标签 + 系统占位不外发（`de24deac`）。坐席三台 **yunsheng / kouxing / lianbei 全 UP · 1.087 · clean/public**。公共渠道 + 内测桥接（同字节）+ 官网 + 台账 5 单 + 回访 + 频道播报见 §4。

## 1. 本版收掉的坑

| 事故 | 根因 | 本版 |
|---|---|---|
| 无限制档本机模型离线后静默（#330） | 路由 prefer 本地、离线无代答 | P0-1 `conv_route` 5 分钟离线窗 + 标准档代答 + 黄条 |
| 日/泰会话中文配文 | caption 池未跟会话铆定语 | P0-2 `caption_lang_pin` |
| 真人相册仍发 AI 生成脸（#329） | 无匹配默认生成 | P0-3 默认关 + 「AI 生成」角标/筛选/开关 |
| 节奏页 `dup_guard_blocked`（#331） | 原码直出界面 | P1-1 人话 + 去会话 |
| 空口「马上拍给你」连环 | 承诺撤回不累计 | P1-3 `promise_streak` |
| 日语会话把讲过的生活小事再当今天（#321） | 中文 bigram 判不中 | P2-3 `script_mismatch` |
| 顶栏急需处理数字对不上（#325） | 混档也标 urgent | P2-3 全严重才标急需 |
| yunsheng 装不上 1.0.87 | NSIS `CopyFiles`/`SHFileOperation` 对大树挂死（7z 已解完） | 人工 `robocopy` 自 7z-out 收口；SOP 记第三坑 |

## 2. 开工基线

| 项 | 事实 |
|---|---|
| 主干 | 版本链基点 `f10e0855` |
| 意向板 | slug `r87-release-1-0-87-single-clean-seats-first` |
| 工作树 | Q-26 C–E / #333 视觉身份 / `health_watchdog` 等脏文件**未动进包**（包切自 r80@f10e0855） |
| 版本链 | `desktop/package.json` 1.0.86→**1.0.87** / 1.086→**1.087**；`i18n_hant generate` → **17719** 键；changelog **10 条双语**；`bump_ui_build` → **20260917-0324**；小智 help +5 / howto +5；dialog 金标 +5 → **`f10e0855`** |
| 官网五值 | `chatxContent.ts` 1.0.87 / 504 MB / 998dbc2e… → **`68798889`** |

关键 commit：`c24321c2` P0-1 · `9d7fcb48`/`693b463f` P0-2/P0-3 · `f3530502` P1-1 · `189e61c1` P1-2/P1-3/P2 · `636ca507` 克隆声 KV · `7fda82d7` #321 · `36ff0e93` #325 · `de24deac` 系统标签泄漏。

## 3. 回归 / 门禁（干净 worktree `D:\boundless-r80@f10e0855`）

| 项 | 结果 |
|---|---|
| `import_sweep` | modules=**1285** bad=0 / from-import names=**3696** bad=0 **ALL GREEN** |
| predist（clean） | `build:backend` 全量 + `dist:win:clean`：干净包；`latest.yml` version 1.0.87 size 528524058 |
| 发版门槛 | **874 passed / 1 skipped / 1 failed**（仅 `test_ui_build_stamp_not_stale` checkout 同秒 mtime 伪红，与 R85/R86 同形） |
| `gate_sweep -NoSyntaxScan` | 并行 worker 崩连坐；红集对照 R86 旧债 + emoji 天花板（unified_inbox 58>54）——**不挡发版**，不重打 |

## 4. 全链

| 步 | 状态 | 事实 |
|---|---|---|
| 坐席三台 | ✅ | `push_chatx.ps1 -Setup <r80>\dist-clean\ChatX-Setup-1.0.87.exe`：kouxing / lianbei exit 0。yunsheng NSIS CopyFiles 挂死（7z-out 9118/1.278GB 已齐）→ `robocopy` 进 INSTDIR + relaunch + seatverify FRESH pid=46980 / 18799 login 200。`chatx_fleet_status` **三台 UP · 1.087 · clean/public** |
| publish 公共 | ✅ | `publish_chatx.ps1 -DistDir <r80>\dist-clean -Yes`：VPS sha512 ✓ → 指针 + pm2 → R2 → 公网核 `latest.yml=1.0.87`；清理 1.0.84 |
| publish 内测桥接 | ✅ | 同字节 → `-Channel internal -Yes -NoAnnouncement`：`latest-internal.yml=1.0.87`，公共指针未动 |
| 官网 | ✅ | `deploy.ps1 -FromHead` → **部署完成 [OK]**；releases 页含 1.0.87 +「本机模型离线」 |
| 台账 / 回访 / 播报 | ✅ 14:51–14:53 | `duty_update_hint` → `mark-fixed` **5 单**（#330 #329 #331 #321 #325）new→fixed → 逐单回访 5 条 `bug-intake/reply ok`（14:51:41–14:52:48，间隔 15s）→ `notify_summary` **记账 5 单**（JUN 4 + skuio 1）该筛选积压 0 → 频道 @hykj7 **messageId=175**（campaign `release-1087`）。**未发报障群总播报**（频道已链群，SOP 禁双发） |

## 5. 断点 / 交接

- 发版 worktree 保留 `D:\boundless-r80@f10e0855`（`dist-clean\ChatX-Setup-1.0.87.exe`）。
- **117 / zhiliao**：本线未重启 zhiliao。坐席三台 = 包内新后端。117 后端半边仍等 `restart_preflight` GO（工作树仍有他线脏 `.py`，勿擅自重启）。
- yunsheng 卸载器字符串已摘（NSIS 未走到 WriteUninstaller）；下次升级走 NSIS 时可能跳过「卸旧」直接覆盖——属可接受。
- 临时文书全在 `%TEMP%\r86\r87\`，未入库。

## 6. 交回各线

- **Q-26 C–E** / **Q-28**（#279 Bad MAC 入站可见）仍不在包；#279 自愈（J-6 C `ed20b1a0`）已随更早版本，本版不标 fixed。
- **#265** 安静时段：1.0.79 已修主路径，升级翻回开关无日志，保持 new。
- **#312 #314** 保持 confirmed。
- **Q-32 / Q-33 / Q-34** 美化线待拍板。
- **#333** 视觉身份线在主干后续 commit，**不在 1.0.87 包**（包定格 `f10e0855`）。
