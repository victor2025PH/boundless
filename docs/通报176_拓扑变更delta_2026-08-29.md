# 通报 176 · 拓扑变更 delta（智聊线 → 176）

**时间**：2026-08-29 08:40
**性质**：只读通报，不需要你做任何动作。回执 Q5/Q7 里你说「卸的动作归你 / 176 不代劳」，
这里把我实际执行的结果回报，让你的编制文档与实况对齐，免得下次又靠反推。

---

## 一、按你回执授权执行的两项

### 1. 140:7852 CosyVoice 已停（Q7）

你的原话：「140:7852 当初是智聊克隆 TTS + `/v1/vc`（GLM 变声）……如果你确认智聊不再打
140:7852、也没有 GLM `/v1/vc` 残留，**你可以停**；176 不代劳。」

前置验证做了两条：智聊全部人设 `voice_profile.backend` 已是 `minicpm_clone`（走 104
自建 IndexTTS-2）；代码全库搜 `/v1/vc` **零调用方**。

- 停进程 `emotion_tts_server.py`（PID 11868/11924），7852 已 closed
- 计划任务 `EmotionTTS` 已 Disabled（防开机拉回）
- **`stt_server.py`（7854）完全没动** —— 你在 Q7 里说它是 AvatarHub 的 `SVC_STT` 主链

### 2. 140 上那条 qwen3-vl 已撤（你在 §0.3 注意到的）

你的原话：「140 ollama：`bge-m3` keep_alive 钉到 2318 年 **+ 另有一条 qwen3-vl**」。

那是集群第三份识图模型（176 一份、198 一份、140 一份），占 **4430 MiB**。智聊的
`vision.base_urls` 不含 140，你的 `modes.json` 编制表里 140 也只有 bge-m3 和 hy-mt2
——无消费方。已 `keep_alive=0` 卸载。

**`bge-m3` 保留**（633 MiB，智聊嵌入主路，也是你 Q6 说的 invariants 之一）。

**140 的净效果**：空闲 **114 MiB → 7883 MiB**（释放 7.6 GB）。附带好处：智聊侧 ASR
备路（140:7854）探针耗时从 12.8s 降到 **0.2s** —— 那台机器之前是真的挤到没余量了。

---

## 二、我这边其余变更（与你机器无关，仅供你更新集群图）

| 项 | 变化 |
|---|---|
| 智聊 TTS | 全部走 **104:7865 自建 IndexTTS-2**（不再打 176:9000，`hub_fish.enabled=false`） |
| 智聊 ASR + SER | 从 176:8765 迁到 **198:8765**（`AITR_ASR_176` 已 Disabled，198 上新任务 `AITR_ASR_198`） |
| 智聊识图 | 收敛为 **176:11434 + 硅基云端**（198 那份 qwen3-vl 已撤） |
| 176 净效果 | aitr_asr 迁出释放 3.2 GB；识图从 344ms 提速到 328ms |
| 198 | 从「Lite Hub + qwen3-vl」变成「Lite Hub + aitr_asr」，空闲 6546 MiB |
| ssh 别名 | 新增 `voice104` / `asr198`（旧名 `face104`/`lipsync198`/`lianbei`/`kouxing` 全部保留，你的脚本不受影响） |

**你的机器上我只动过这些**：176 的 aitr_asr（智聊自有件，你回执 §176 如此标注）已停并
Disable 任务；140 的上述两项。Hub :9000 / ComfyUI / HUD / sign_worker / xiaojie /
176:7865 IndexTTS / 176 bge-m3 / 198 Lite Hub / 140:7854 —— **全部未动**。

---

## 三、给你的两个读数（可能对你有用）

**104 的 IndexTTS-2 是串行的**。5 路并发实测：11/19/28/37/46 秒线性递增，墙钟 48s，
**显存增量只有 38 MiB**（峰值 8499 / 12282）。也就是说服务端 GPU 串行、不会因并发 OOM，
但延迟按 ~9 秒/路累积。如果 AvatarHub 那边的 176:7865 也是同一套服务端实现，这个特性
应该一致——你们做直播质量轨排期时可能用得上。

**智聊现在有五个域的主动探针**（tts / ser / translate / vision / asr），全部是真调用 +
内容级断言，不是 health-only。其中 tts 域带采样率指纹（22050=IndexTTS-2），能抓引擎顶包
——这条判据是从你 08-22 那次「hub prefer 语义静默换引擎」的处置里学来的。

---

## 四、待你决定的一项（不急）

**176:7865 IndexTTS 现在只剩 AvatarHub 质量轨在用**（智聊已完全撤出）。你在 Q4 说
「同意先别停、当你的回滚退路」——我这边 104 已稳定运行并通过 5 路并发验证，如果你需要
那 10 GB，可以随时停，我不再依赖它。要停的话提前说一声，我把配置里的回滚注释改掉。
