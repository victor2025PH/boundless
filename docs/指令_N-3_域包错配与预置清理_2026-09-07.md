# 指令 N-3：域包错配与预置清理——陪伴人设别再跑在销售域包上（#241 #240）

> **预算硬上限 $150。** 正常花费 $80–120。花到 $130 停下写落点表 + 总账。
> 类型：**业务域装配层 + 种子数据。** 证据 `tmp_diag\{78PDGB,3NVM7Q,4CNK6G,TN736F,VAQGZY,ZNW5CN,698J28}\`，3NVM7Q 已定性，不必再取证。
> 为什么单独一条：13:14 启动日志同一秒里——`Domain hook registered: ConversionDomainHook` / `Domain persona set: 线上陪伴` / `KB categories set from domain 'conversion'` / `System prompt loaded from domain pack` / goal_routes 注册名「营销目标路由」/ 模板库预置 20 条种子。**人设是陪伴，域包是销售。** 于是：工作目标「客户摸底」标签是 BANT 六项（业务痛点 / 在用平台 / 团队规模 / 预算档 / 决策角色 / 上线时间），右栏客户画像同一套字段，「标成交」弹产品 / 金额，KB 分类含命理与 GXP 支付话术，1.0.75 M-5 删的「转化成交」模板也是这里长出来的。之前 #217 / #240 / #184 是在摘叶子，这条是砍根。
> 默认按底座 **D-N1 / D-N2** 执行。

---

## 0. 开工前必读

1. `docs/修复指令_N批_并行_2026-09-07.md`：文件归属 / D-N1 D-N2。
2. `docs/发版对账_v1.0.76_M5.md` A（#217 「转化成交」用户版隐藏 + 无产品禁报价——本条把它变成域级规则）；`docs/发版对账_v1.0.74_J9.md`（#184 KB `source` 隔离 + 首装不带 KB——但 `kb_store` 自己的话术种子没挡）；`docs/发版对账_v1.0.75_L4.md` A（`ui_client_hide` / partner 层——域可见性复用它）。
3. 代码位置：域包注册（grep `Domain hook registered` / `ConversionDomainHook` / `Domain persona set` / `KB categories set from domain`）、`src/companion/goals/profile_slots.py`（L6 注释即 BANT：Size / Budget / Authority / Timeline；L65–71 团队规模 / 预算档 / 决策角色）、目标模板种子（20 条）、客户画像 schema（右栏「客户关系 → 客户画像」字段集，与摸底标签同 schema）、`goal_routes.py`（「营销目标路由」）、`src/utils/kb_store.py`（`gxp_hint_utr_query` 等「系统话术·直接」种子与分类）、`i18n_packs/goals.py`。

**读完先做 §2 的一问，再 A→E。**

## 1. 红线与边界

- 提交只用 `git add <明确文件>`；提交前 `git diff --cached --name-only` 为空再 add；⛔ `git add -A` / `-a` / `reset --hard` / `checkout .` / `restore .` / 裸 stash。
- **不删数据、不删域包**：conversion 域包对销售客户仍是正确的；本条是让「装的是哪个域」成为单一来源并让一切跟随，陪伴域下不渲染 / 不注入销售域物料。
- 存量用户已填的摸底标签值不丢（只是界面不再列 BANT 项；有值的仍显示）。
- 不动 `skill_manager.py` / `drafts.py` / `store.py` / `autosend_worker.py`。

开工登记意向：
```
powershell -ExecutionPolicy Bypass -File engines\chengjie\scripts\agent_probe.ps1 -Intent "N-3 domain pack mismatch: single-source business domain (hook/KB cats/prompt/templates/slots follow) + companion slot set + profile schema + kb seed by domain (#241 #240)"
```

---

## 2. 先答一问

**为什么 skuio 机装的是 conversion 域包？** 是首装默认（没让选）、还是内测 seed 指定、还是 `desktop_mode` 缺省？答案写落点表首行，决定 A 的入口在哪（首装向导 / 后台设置 / 配置基线）。

## 3. A —— 业务域单一来源（D-N1）

- 引入 `business_domain` 单一真值（首装选一次：陪伴 / 销售 / …；已装机器按当前人设域推导一次并写入，可在后台改）；`Domain hook` / KB 分类 / 系统提示词 / 目标模板库 / 摸底标签 / 客户画像 schema **全部读它**，启动日志一行 `business_domain=companion（hook=CompanionDomainHook）`。
- 陪伴域下不注册 `ConversionDomainHook`，不导入 conversion 模板种子（#217 那四张自然不出现），KB 分类不含销售 / 命理类。

## 4. B —— 陪伴域摸底标签集（D-N1）

`profile_slots.py` 按域给槽位表：陪伴域 = 称呼 / 坐标 / 职业 / 年龄 / 兴趣 + **家庭状况 / 婚恋状态 / 收入水平 / 当前居住地 / 个人资产**；删 6 个 BANT；加「+ 自定义标签」；**收入 / 资产等敏感项默认不勾选**，勾了也只允许 AI 多轮自然带出、禁止连问（摸底段 prompt 硬约束，接 I-3 #38「一轮只问一个空槽」）。

## 5. C —— 客户画像 schema 同源 + 「标成交」收起

右栏「客户关系 → 客户画像」字段集与摸底槽位同一 schema（TN736F：必须一起改）；商机区（业务痛点 / 在用平台 / 团队规模 / 预算档 / 决策角色 / 上线时间 / 流失原因）陪伴域不渲染；「标成交」弹层的产品 / 金额字段陪伴域收起，改为陪伴域的结果标记（如「关系升温 / 见面 / 转付费陪伴」——落点表给候选，老板定文案）。

## 6. D —— 「营销目标」→「工作目标」

用户界面与路由注册名、日志里的「营销目标」字样统一为「工作目标」（`goal_routes` 注册名 / i18n / 目标页标题）。

## 7. E —— KB 种子按域过滤 + 存量清除（#240，D-N2）

- `kb_store` 播种「系统话术·直接」（`gxp_*` 支付话术等）按 `business_domain` 过滤：陪伴域不导入；导入的种子 `source=system` 且**默认停用**。
- 存量：知识库页「系统预置」一键清除（J-9 B 的 `purge-source` 已有，扩到 `system` 里的 gxp 系列）；升级时提示清理。
- 落点表写清：**「帮助语料自动播种 291 条」是悬浮球 AI 助手的帮助语料，不进客户知识库、不参与对客检索**——skuio 第三次把它当 KB 预置（B3PT5D → 698J28），回访口径交 N-5。

## 8. 验收

- `tests/test_business_domain_single_source_241.py`：companion 域启动无 ConversionDomainHook / 模板库无「转化成交」/ 摸底槽位为陪伴集 / 画像 schema 同源 / KB 分类无销售类。
- `tests/test_kb_seed_by_domain_240.py`：companion 域首装 `gxp_*` 零条；purge 覆盖 system 系列。
- 真机（交 N-5）：skuio 机升级后创建目标 → 客户摸底标签为陪伴集；右栏画像无商机区；知识库无 gxp 条目。

---

## 9. 收工

- 落点表 `docs/发版对账_v1.0.77_N3.md`（首行写 §2 答案）；总账 **N-3 行**。**每个 commit 落地后立刻补落点表一行。**
- **待老板**：C 项陪伴域「标成交」替代文案候选。
- **交 N-5 回访口径**：#241「摸底标签换成陪伴用的（家庭 / 婚恋 / 收入 / 居住 / 资产），敏感项默认不问；客户画像同一套；没有『营销目标』字样了」/ #240「知识库不再预置支付话术；那 291 条是悬浮球帮助语料不是你的知识库」。
- 收工清意向板 `-Done`。

**开对话粘这段：**

```
执行 N-3（域包错配与预置清理：#241 陪伴人设跑在 conversion 域包上 → 业务域单一来源 + 陪伴域摸底标签集 + 客户画像 schema 同源 + 「标成交」字段收起 + 「营销目标」→「工作目标」/ #240 KB 种子按域过滤 + 存量一键清除）。先读 D:\boundless\docs\N批_进度总账.md 与 D:\boundless\docs\修复指令_N批_并行_2026-09-07.md（决策 D-N1 D-N2），再读 D:\boundless\docs\指令_N-3_域包错配与预置清理_2026-09-07.md，先答 §2（为什么装的是 conversion 域包），再 A → B → C → D → E。不删数据不删域包；存量标签值不丢。证据 tmp_diag\3NVM7Q TN736F VAQGZY ZNW5CN，不必再取证。每项一个 commit 带单号；每个 commit 后立刻补落点表一行 docs/发版对账_v1.0.77_N3.md；预算上限 $150。
```
