# 实施15 · 智聊全新上线（双实例 GO）+ 官网生产发布 + 五机任务落地 + 登记翻正

> 日期：2026-07-19 ｜ 上承：实施14（通译 .117 真迁 / 事件链首通 / 密钥铺设）
> 老板决策输入：**现网智聊确实没有在跑，批准在本机（.117）直接拉起智聊**；其余按实施14 建议推进。

---

## 一、智聊 ChatX 实例全新上线（.117，双实例形态收口）

老板确认无现网数据 → 跳过 README §3.2 迁移流程，按 §3.1 全新初始化（zhiliao 模板/端口/品牌）：

| 项 | 值 |
|---|---|
| 数据根 | `D:\chengjie-instances\zhiliao\data`（仓外，与通译对称） |
| 端口 | 18799（备用 18787），PID 22632 |
| AI 引擎 | 内网 Ollama（幻声机 .176:11434/v1）`qwen3:30b-a3b-instruct-2507-q4_K_M`——零 API 成本起步，老板可随时在实例 overlay 换云端 key |
| Telegram 协议号 | **未配**（走纯收件箱/网页路径，err 日志仅提示 api_id 未配属预期；老板后续人工填） |
| 品牌断言 | 登录页 `登录 · 智聊 ChatX`、manifest `智聊 ChatX · 坐席工作台` PASS |
| 日巡检 | `Boundless_Zhiliao_DailyVerify` 每日 09:35（与通译 09:30 错开），实跑退出码 0 |

**双实例总览**：`status_instances.ps1` → zhiliao GO + tongyi GO，VERDICT **GO**——实施09 定下的
"智聊/通译拆双实例"目标在生产机全部落地。

## 二、官网生产发布（website waves 09-14 上线 bd2026.cc）

1. **合并 main**：`feat/website-positioning` → `main`（46f50dd）。三个文件冲突
   （license_server / live_interpreter / telemetry）根因 = 老板 29459d8 净码把 avatarhub 统一成
   LF、我们分支是 CRLF——git 视为整文件冲突。解法：**以老板净码为基底（ours），把我们的三处
   小增量（遥测埋点 194+25 行、license outbox 13 行）转 LF 重放**，live_interpreter 三个 hunk
   上下文漂移改手工插入，全部 py_compile 通过。绝不覆盖老板 515 行净码改动。
2. **原子部署**：67MB tarball（排除 node_modules/.next/data/logs/env）→ VPS `deploy.sh`
   （自动备份→rsync→npm ci→build→pm2 restart→健康检查，失败自动回滚）→ **DONE**，
   部署前把 rsync --delete 会清掉的仓外杂项（bootstrap_key.py 等 4 件）先归档到 ~。
3. **上线验证 6/6**：营销首页 / /admin / /console 200；`/api/collect` 200（老密钥仍有效）；
   `/api/sync/personas/grants` 与 `/api/sync/personas/purges` 从 404 → **200**（P5 机器通道生产可用）。
4. **配套激活**：`CONSOLE_KEY`（48-hex）已配入 VPS env；`ledger-backfill.mjs` 回填
   9 订单 + 5 留资入集团账本；**首份真实 KPI 周报**在生产双库上生成成功（漏斗含真实支付数据）；
   crontab 挂每周一 09:35 自动产出（与老板营销周报 09:30 错开）。

## 三、五机任务矩阵落地（cron v2 全面 -Execute）

| 机器 | 已装任务 | 实测 |
|---|---|---|
| .117 通译机 | uploader-zhiliao / uploader-tongyi / purge（双根）/ export（双根）/ grants_sync，共 5 个 | uploader 空跑 0；**purge 双根 commit 对生产队列轮询成功（无待办）**；grants_sync 拉取 OK |
| .176 幻声机 | uploader / purge / export / grants_sync（facefusion conda python） | grants_sync 对生产拉取 OK；uploader spool 未生成前幂等 0 |
| .198 智拓机 | uploader / purge / export / grants_sync（Python311） | 同上，grants_sync OK |
| .104/.140 算力节点 | 不装（无引擎数据，按矩阵设计） | — |
| VPS | crontab：KPI 周报每周一 09:35 | 手动实跑出真实报告 |

**P5 grant 同步通道自此在三引擎机每 30 分钟自动刷新缓存**——enforce 切换的 7 天证据期从今天起算。

## 四、登记翻正（stack.json 与现实对齐）

- 旧 `chengjie` 单实例条目 → `enabled=false` + 退役注记。**关键安全动机**：其 `start_main.ps1`
  有"清幽灵端口"逻辑，误启会强杀 18799 持有者=杀掉智聊新实例；
- `chengjie_zhiliao` / `chengjie_tongyi` → `enabled=true`（生产实况 + 数据根/AI/协议号现状注记）；
- `deploy.ps1 -Action status -Only chengjie_zhiliao,chengjie_tongyi` → 双 GO。

## 五、幻声机 hub 的事实与边界（记录，不硬来）

尝试把 hub 从 git 仓重启以载入新代码（四视图端点+遥测）：杀旧进程后，老板的 coexist 守护
bat（`start_avatar_hub.bat` 等待接管循环）**先于我们的拉起抢占 9000**，现跑的仍是老部署路径
（`D:\projects\模仿音色`）的老代码，health 200 功能无损（等于回到动手前状态）。
**结论**：幻声机 hub 的 git 仓切换属于老板 coexist 迁移收口，留他的窗口做；两套并存期间
不再远程强切（历史事故注释明示两个 hub 抢 9000 的风险）。任务与密钥已就位，hub 切换当天
埋点即自动生效。

**另一处方案修正**：原计划"幻声机试点 AVATARHUB_PRODUCT_ID=huanying"作废——幻声机是
四产品全功能开发机，裁剪侧栏会挡老板用其他产品功能；产品视图的正确试点场景是**客户交付
实例**，写入 APPLY.md 待交付时启用。

## 六、本轮相对方案的二次优化汇总

1. 合并策略：不是"接受一边"，而是 ours 净码基底 + LF 化补丁重放 + 手工 hunk——两边工作零丢失；
2. 发现并删除旧 `Boundless-chengjie-uploader` 任务（与新 uploader-tongyi 并发同一 spool，
   违反"勿并发同一 spool"铁律）；
3. 四视图试点场景从"老板开发机"修正为"客户交付实例"；
4. VPS 部署前先盘 crontab + 归档 rsync --delete 影响面内的仓外文件；
5. 智聊 AI 以内网 Ollama 起步（零成本、数据不出内网），云端 key 留为运营升级项。

## 七、验证汇总（全部实际执行）

- 双实例：status GO×2；智聊品牌断言 PASS；app.log 运行正常；
- 官网：deploy.sh 全步成功 + 健康检查 OK；6 端点 200；backfill 9+5；KPI 周报真数据生成；
- 任务：三机 grants_sync 对生产全部拉取成功；purge 生产轮询首通；旧任务清理；
- 合并：三冲突文件 py_compile 0；main push 成功（pre-push repo_doctor OK）；
  两引擎机 pull 到 46f50dd。

## 八、下一阶段（实施16 建议）

1. **观察期收口（两实例并行盯 7 天）**：日巡检 JSON 连续 GO 后——对外暴露（通译
   `lingox.bd2026.cc` 子域反代 / 智聊按销售形态定）+ 开机自启任务 + 实例级 license.key 签发
   （去掉最后一个 verify WARN）；
2. **老板窗口两件事**：幻声机 hub 切 git 仓跑（coexist 收口，埋点/四视图端点即生效）；
   智聊 overlay 填 Telegram 协议号与（可选）云端 LLM key；
3. **Console 开账**：用 CONSOLE_KEY 初始化 master 账号，跑 `run_export -Transfer -Import`
   三段式把 avatarhub/chengjie 人设与授权导入集团账本，客户 360/商机页开始有真数据；
4. **enforce 灰度**（≥7 天后）：enforce_readiness 连续通过 → 先 avatarhub 开
   `PERSONA_GRANT_ENFORCE=1`；
5. **KPI 首个自动周报**（07-20 周一 09:35）落地后校准口径，KPI 页接入周报存档；
6. **P2 深化**：website /admin 与 /console 的运营分工文档化（营销运营 vs 集团皇冠资产），
   给运营同事开 viewer/admin 分级账号。
