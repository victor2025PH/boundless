# 值守交接_20260827 06:50（供新会话接手「智聊报障群」值守）

> 上一会话：0826 白天~0827 清晨。本文是唯一交接源，新会话开工先读完。

## 0. 角色与通道

- 座位机 **173**（本机，Windows/PowerShell），实际工作全在 **117**：`ssh chengjie117`。
- PowerShell 引号会毁复杂远程命令：**一律写 `.py` → `scp` 到 `chengjie117:C:/Users/Administrator/` → `ssh` 执行**。脚本内用 `sys.stdout.buffer.write((s+"\n").encode("utf-8"))` 输出。
- 报障群监听：`ssh chengjie117 "python C:\Users\Administrator\tmp_tail_log2.py"` 挂后台，
  notify 正则 `bug_intake|1004290740529|1004345824259`。（tmp_tail_log2 已过滤 okline 刷屏行；老版 tmp_tail_log.py 勿再用）
- 群与发送 API（在 117 上执行）：
  - 内测bug群 `-1004345824259`，官方群 `-1004290740529`，账号 `6834964252`
  - `POST http://127.0.0.1:18799/api/unified-inbox/send`，Bearer `f7b2db05f287503354f1f81ef400ff7bf4035106763af45b`
  - 参数：`skip_translate:true, force_lang:1`，`client_msg_id` 用固定值防重发
  - 发完把会话拨回 `automation mode=auto_ai, confirm_group:true`
  - 群消息同步读取：`python C:\Users\Administrator\tmp_duty_read.py`
- 群截图归档：`D:\chengjie-instances\zhiliao\data\protocol_media\telegram\6834964252_<msgid>.jpg`，scp 回 `C:\Users\admin\duty_tmp\` 看图。
- 报障群 AI 全静默，**回复全靠值守人工**；只答有据可查的内容，不闲聊。

## 1. 台账（都在 117 D:\boundless\docs\）

- **实施49**…四视角方案：总账，B1~B123 全史，新报障必登。
- **实施64**…1.0.52指令：派工账（P0/P1/P2）。
- **实施68_0826未修派工_给117.md**：0826 值守整理的未修清单（117 施工依据），后续新案都往这里追加。
- 运维笔记：`D:\chengjie-instances\.ops\NOTE_from_duty_20260826_173_vllm_recovered.md`、`NOTE_from_duty_20260827_117_net_incident.md`。
- 现网 testers 版本 **1.0.55**。

## 2. 0827 凌晨事故（已闭环，但有遗留）

事故链：117 有线网卡断链 → 默认路由被 ZeroTier 抢走 → 外网全断（03:18~05:49）→ pyrogram 断连、
okline(LINE) HMAC 桥崩环刷 stderr 1.46GB/40min 并写崩 app.log 日志器。
值守处置：config.yaml 加 `platform_login.line.protocol_enabled: false`（**TEMP-OFF 20260827 注释块，
okline 修好后删块回滚**）→ 06:13 重启引擎（哨兵拉起）→ 删 1.46GB err log。06:19 全面恢复。

**遗留盯梢**：
- [ ] vision true_probe 一直 FAIL（03:04 配置热改后 400/401）→ 117 施工线查 vision 凭据/endpoint。
- [ ] avatar_voice 7852 预热超时 → 观察是否自愈；不愈合并 B61 hub 域。
- [ ] LINE 当前协议关闭：LINE 客户（B98 报障人）收发全停，修 okline 时带熔断退避 + err 轮转，修好回开。
- [ ] 117 网线要现场检查（昨夜反复闪断的根源）。
- [ ] 173 的 vLLM：已 systemctl enable + WSL 驻留保活（Start-Process sleep infinity）；机器重启/注销保活会丢，
      治本要老板做管理员计划任务或 .wslconfig。

## 3. 未答复/待跟进（新会话优先处理）

- [ ] **B82 额度阻断（最急）**：skuio 机 0826 晚已掉 0%+「明天暂停自动回复」预告。已在 _594 请他确认
      是真停还是只弹提示——**盯他回复；若真停，立刻协调放行（B40 内测无限量期到 08-29）**。
- [ ] B120 语音穿透：等 skuio 回「关开关是在 17:24 那条语音之前还是之后」。
- [ ] B119 发图：等 skuio 回变现页「发图能力」开/关状态，若开着仍不发→查 decide_selfie 付费闸+触发词。
- [ ] 官方群回访小海（语音克隆验证，多次催未回）。
- [ ] B40 到期 08-29：回滚 + 7 天用量商业化报告。

## 4. 0826~0827 新立案速查（详情在实施49/68）

| 案 | 一句话 | 状态 |
|---|---|---|
| B113 | 全自动单条漏回（自愈），修漏检 | P1-17 派工 |
| B114 | 克隆音色回落=hub 超时（B61 族），已单独知会语音线 | 派工+知会 |
| B115 | 工具箱撤克隆录入，登记只留人设页 | P2 |
| B116 | Messenger 不可达联系人弹窗循环=P1-9 残留，加终止重试硬要求 | 并 P1-9 |
| B117 | 贴纸/表情包别当照片评论，改情绪回应 | P2 |
| B118 | **废除**内心独白（括号旁白直发客户，暴露风险）；生成端硬禁+守卫重生成 | **P1** |
| B119 | 发图两道闸：全局 companion.selfie.enabled 无自查面 | P2 待定向 |
| B120 | 语音关了仍发：B线/手动链不吃全局闸 | 待时间线 |
| B121 | 「I'm 我 going anywhere.」出站单语种校验 | **P1 加急** |
| B122 | 草稿填入提供整段模式（勿动全自动分段） | P2 |
| B123 | bug_intake 断线不补拉群消息（本次漏 2 条实锤） | P2 |
| B82 | 额度显示→升测试阻断 | **加急** |

另：自家账号互聊不回=peer_bot_guard 设计内（对 skuio 已解释）；0826 已给 skuio 发过 8 项 1.0.55 验收清单。

## 5. 常用脚本（117 C:\Users\Administrator\ 已就位）

`tmp_duty_read.py`（同步读群）/ `tmp_tail_log2.py`（过滤版监听）/ `tmp_duty_send*.py`（发送模板，
最新 159b 可抄）/ `tmp_engine_health.py` / `tmp_net_layers.py` / `tmp_list_media.py` / `tmp_gap_check.py`。
本地备份全在 `C:\Users\admin\duty_tmp\`。

## 6. 接手动作清单

1. `ssh chengjie117 "echo alive"` 验通道。
2. 挂监听（见 §0），确认 `[tail-start]`。
3. `tmp_duty_read.py` 拉群消息，处理 §3 里等回复的项。
4. 照常值守：新报障 → 取图 → 分析（先查代码/日志再答，勿猜）→ 群内答复 → 登实施49+68。
