# 发版对账 v1.0.76 — M-5（产品清理与上游：#217 #226 #225 #220 #224）

> 指令：`docs/指令_M-5_产品清理与上游_2026-09-06.md`。总账：`docs/M批_进度总账.md` M-5 行。决策：底座 **D-M6 / D-M7** 全部按默认执行（转化成交类目用户版整类隐藏 + 引擎无产品禁报价 + 首条真发预览；空壳面板用户版收起、复用 L-4 `ui_client_hide`、隐藏不删数据）。顺序 A → C → D → E → B，**五项全部完成、全部 commit**（2026-09-06 23:0x – 09-07 01:0x）。
> **本条不发版、不重启生产实例。** `.py` 改动（`goals/templates.py` / 新 `goals/product_guard.py` / `goal_routes.py` / `bootstrap/background_tasks.py` / `image_gen_routes.py` / 新 `ai/audio_pipeline_intake.py` / `ai/voice_translate.py` / `voice_transcriber.py` / `unified_inbox_translate_routes.py` / `utils/daily_learner.py` / `learner_routes.py` / `nurture_routes.py` / `desktop/build/build_backend.py`）由值守在下个重启窗装载；模板 / i18n pack / `shared/copilot` 组件与词典已热更（**中间态自洽**：后端缺新字段时前端一律回落旧行为——`template_retired` / `dev_state` / `received` / `src_langs` / `summary` 缺键＝不渲染新块）。
> 接手：先读本表逐行核对，再 `git -C D:\boundless log --oneline -30 | Select-String "M-5"` 确认五个 commit 真在：A `b5c24bc8` / C `169bcb6e` / D `40249ce5` / E `1090c4cf` / B `0a19204e`。
> ⛔ 未碰：`care_dispatcher.py` / `care_schedule.py` / `companion_proactive.py`（M-1；A 的出站硬规则经 `wrap_care_send` 包在 `background_tasks._care_send` 外面实现）、`translation_service.py`（M-1 B 正在改，只 import `detect_language`）、`unified_inbox.html` / `workspace_base.html` / `app.html`（M-2/M-3/M-4；`?v=` 未 bump，见待办）、`drafts.py` / `store.py` / `skill_manager.py`（只调用 `upsert_draft` 公开 API 建 L1 草稿）、养号 / 出图功能本体与引擎逻辑。
> 真机：未动任何账号。证据全部来自 `tmp_diag\{2D26JS,48JB6U,ETR3XA,6TGCPC,9TJCFK,WPHAD7,X6MDC5,5FW4KK,5NXHUW,RJF9N7,DMS45P}`（老板指令：不必再取证）+ 本地单测。

## 根因一句话

五件事一个原则——**没准备好的功能不出现在用户版；出错要说真话**。#217 是 1.0.75 冲刺引擎真发后「工作目标 → 转化成交」四张预置（两张厂商卖智聊的销售剧本 + 两张无产品字段空壳）仍对用户版直出，且引擎对「目标没绑产品」零约束（#175「黄金市场」＝无货可推时的编造）；#226 出图面板框架完整但服务不存在，报「联系运维」（内部话术）且探测结果不落日志；#225 工具箱与工作台两条转录链两套配置（自建 openai 客户端 vs `voice_recognition`→`OpenAITranscriber`），同机同分钟一超时一成功，提示把超时一律译成「不可用」，且探针夹具仓库有、桌面包没打；#220 学习队列页面「含汉字即中文」不请求译文（日语含汉字被跳过）、原语靠猜、失败留空、同题两份并列待审；#224 养号面板判词读健康灯、数字读生命周期，两边口径不同就矛盾，灰置原因只在悬浮里，三阶段无说明。

## 落点表

| 项 | 工单 | commit | 改了哪些文件 | 测试结果 | 备注/剩余 |
|---|---|---|---|---|---|
| **A 转化成交类目用户版隐藏 + 无产品禁报价 + 首条真发预览** | #217（P1，D-M6） | `b5c24bc8` | `companion/goals/templates.py`：`CLIENT_HIDDEN_KINDS=("conversion",)`（按 kind 判，四张 = conversion_unlock / conversion_subscribe / acquire_and_convert / retention_expand）、`is_client_hidden_template` / `client_hidden_template_ids` / `list_templates(client_hide=)` · `goal_routes.py`：`_client_hide(request)`（＝L-4 `client_hide_active(cfg, resolve_developer_mode(session))` 同口径）→ `/api/goals/templates` 不下发该类目；`POST /api/goals` 与 `/batch` 对该类目 403 `err.goals.template_retired_client`（藏了入口还放 API 建＝旧前端「上次用过」/脚本仍能把 AI 拉去推销智聊——刻意闸新建，存量不动）；`_attach_tier` 给 for-conversation / list / detail / create 视图加 `template_retired=True`（仅 client 且该类目） · 新 `companion/goals/product_guard.py`：`goal_product_binding`（`item_id / product_id / tier / last_plan` 任一非空、目录模板且目录真有货、custom note 明写价格/买卖动作 → 绑定）、`find_sales_talk`（报价 / 货币+数字 / 支付收款 / 下单开通 / 开户注册引导页 zh+en 词表 **+ `persona_guard.matches_service_frame`** J-1 B 词表）、`guard_goal_outbound`（未绑产品 × 命中 → 整条拦，不剥句）、`first_send_pending`（goal_events 无 `care_sent` / `beat_sent` / `first_send_preview`）、`build_first_send_draft`（`upsert_draft` 载荷：inbox 源、`source_id=goal_first:<gid>` 幂等、`autopilot_level=L1`、`risk_reasons=["goal_first_send"]`）、`wrap_care_send`（包 send_callback：`extra.care_id` 回查 care 行 `topic_norm` → 目标；非 goal 行 / `manual_rewrite` 原样透传；拦下 → care 行 `skipped:goal_no_product` + 目标事件 + WARNING `[goal_no_product] gid=… hits=…`；首拍 → L1 草稿 + care 行 `skipped:preview:draft:<id>` + 事件 `first_send_preview` + INFO `[drafts] level=L1 reason=goal_first_send`，之后按档真发；inbox store 缺席退回直发并点名） · `bootstrap/background_tasks.py`：`send_callback=wrap_care_send(_care_send, …)`（catalog_probe 读 site_catalog；接线失败退回裸 `_care_send`） · `cp-goal.js`（双树）：卡上 `template_retired` → ⚠「该模板已下线，建议改用自定义目标；目标没绑产品时 AI 不会编报价/开户话术」 · i18n `goals.py` 2 键 zh/en · `tests/test_goal_templates_client_tier_217.py`（40 例） | 新 40 例 + `test_goal_routes` / `test_goal_templates` / `test_goal_sprint_ticker` / `test_goal_wiring` / `test_goal_engine_status_166` / `test_goal_autoadvance_d1b` 243 passed | 存量 1.0.75 已真发的目标（BABY BEAR 等有 `care_sent`）**不会**被回头拦成预览；首拍进 L1 的那一条不计 `record_sprint_beat_sent`（由人从草稿队列发出，`_care_sent_hook` 不触发），第二拍起正常计数。M-2 E 的 `reason` 字段若落到 `drafts.py`，本条 `risk_reasons=["goal_first_send"]` + 日志 `level=L1 reason=goal_first_send` 已同格式。`auto_create` / `retention_auto` / `reconvert_auto` 三条生命周期自建（全部默认关）仍指向 conversion 模板——在客户机若被显式开启会建出「已下线」卡，未在本单闸（另立） |
| **C 出图空壳改「开发中」态** | #226（P2，D-M7） | `169bcb6e` | `image_gen_routes.py`：`_client_hide(request)` 同口径；`/api/image/config` 在 client 形态回 `dev_state=True`（**不探出图服务**——用户版不敲内网，L-6 B 同原则；不下发引擎/额度，只留人设 + `album_pick`），首请落一行 `[image_gen] probe skipped: user edition dev_state`；`probe_log_line` + `_cached_probe` 落 `[image_gen] probe host=<netloc> ok=<bool> ckpts=<n> unets=<n> took=<ms>ms`（首次 / 换地址 / 可达性翻转 INFO，同态续探 DEBUG） · `cp-image.js`（双树）：`dev_state` → `_renderDevState()`＝「AI 生成照片功能开发中，暂不可用。」+ 相册存货条置顶（有货即点选直发）+ 人设选择 + 结果区（发送/下载）；不出引擎/尺寸/提示词/场景词/生成按钮/额度行；结果区不出「重新生成」；不续探 · `cp-i18n.js`（双树）：`cp.image.*` 全部「联系运维 / ask·contact ops」→「联系客服 / support」（15 处 zh+en），新 `cp.image.dev_state` / `dev_state_hint` · `cp-i18n-ext.zh_hant.js` 重生（`scripts.i18n_desktop_ext generate --langs zh_hant`；顺带把手改的 `cp.voice.result_unknown` 归位） · `tests/test_image_gen_client_tier_226.py`（7 例） | 新 7 例 + `test_image_gen_errors` / `test_cp_app_i18n_keys` 57 passed；`test_cp_i18n_parity` / `overlay_coverage` / `copilot_i18n_keys` / `sync_copilot_mirror` / `i18n_en_payload_gate` / `copilot_panel_manifest` 全绿 | partner / internal / 开发者模式原面板不变（引擎 + 探测照旧）；生成 API 未封（藏而不废）。partner/internal 失败卡文案里的「客服」对内部坐席略别扭，是 D-M7「去联系运维」的取舍 |
| **D 工具箱转录并入 voice_transcriber + 错误分类 + 探针夹具** | #225（P2） | `40249ce5` | `ai/voice_translate.py`：`workspace_transcriber(full_cfg)`＝`media_enrich.lazy_voice_transcriber`（工作台客户语音就是它建的那份，含 L-6 B `apply_hosted_asr` 网关接管 / LAN 回落链）；`build_workspace_transcribe_fn`（language 恒 `auto`，失败带回转录器 `last_error`）；`resolve_toolbox_transcribe(full, want_segments)` → 要分段（SRT）且 audio_pipeline 开 → `pipeline`；工作台可用 → `workspace`（**默认**）；只有 audio_pipeline → `pipeline`（B27 派生回落保留）；皆无 → None；`translate_voice` 失败附 `asr_code` / `asr_model` / `asr_latency_ms` · 新 `ai/audio_pipeline_intake.py`：`probe_audio_file`（WAV 读头、OGG Opus/Vorbis 读末页 granule、其余有 ffprobe 才问；0 字节 / 头不认 → `upload_failed`）、`classify_asr_error` 六分类 timeout / upload_failed / format / rejected / unavailable / no_speech（判序：超时最先——5NXHUW 原话就是超时被说成不可用）、`asr_error_i18n_key` · `voice_transcriber.py`：基类 `last_error`（file_not_found / file_too_large / empty_result / 异常串）、`OpenAITranscriber` 两处 except + 令牌缺失写入、`FallbackTranscriber` 拼各级 · `unified_inbox_translate_routes.py`：`translate-voice` 与 `translate-message-media` 语音分支改走 `resolve_toolbox_transcribe`（视频分支 want_segments=True 仍取 pipeline）；`_toolbox_asr_intake` 落盘核查 → `received={bytes,duration_sec,container}` / 失败 `upload_failed`；`_attach_asr_failure_message` 按 `asr_code` 取 `err.asr.<code>`；一行 `[toolbox_asr] src= chain= bytes= dur= ok= reason= code= model= latency=` · i18n `errors.py` 5 键 zh/en（timeout / rejected / format / upload_failed / unknown） · `cp-xlate-tools.js`（双树）：成功与失败都回显「已收到 x 秒音频」（算不出秒说 KB）· `cp-i18n.js` 2 键 + 繁中 overlay 重生 · `desktop/build/build_backend.py` DATAS 补 `(assets/probe, "assets/probe")`（`asr_probe.wav` 133KB + 逐字稿 sidecar；`true_probe.ASR_FIXTURE` 用 parents[2]/assets/probe 定位＝冻结后 `<_MEIPASS>/assets/probe`） · `tests/test_toolbox_asr_unify_225.py`（27 例）；`tests/test_voice_translate_hosted_fallback.py` 两条 B27 路由测试改钉新口径（默认走 workspace）+ 新增「工作台缺席仍派生」回落用例 | 新 27 例 + B27 43 passed；`test_desktop_no_lan_defaults` / `test_media_translate_route` / `test_desktop_package_datas` / `test_backend_refresh_datas` / `test_desktop_backend_freshness` / cp-i18n 五门禁 100 passed；`-k "voice_transcriber or voice_translate or audio_pipeline or asr"` 165 passed | 5NXHUW 里 `audio_pipeline … dur=0.0s` 对 openai 后端**恒 0**（只有 faster_whisper 回 duration），不是「文件未收到」的证据——所以收件量改在 intake 侧自己量。**夹具随包要等下一次 `build_backend` 打包（1.0.76）才生效**，装载 `.py` 不解决；打包后 skuio 机 backend.log 应不再每 10 分钟出「asr/ser 域缺夹具」。`_probe_audio_file` 对 mp3/m4a/webm 无 ffprobe 时只报字节数（不算失败） |
| **E 学习队列语种按文字系统判 + 未译不留空 + 同题合并** | #220（P3） | `1090c4cf` | `utils/daily_learner.py`：`query_lang`（复用 `translation_service.detect_language`：假名→ja、谚文→ko、泰文→th、越南声调→vi，再才看汉字；导入失败按本模块兜底脚本表）、`needs_translation`、`normalize_query_key`（去标点/空白/大小写）；`save_drafts` 对已有 pending 同键条目累加 `hit_count` 不新建（同批第二份同题也合并）；`merge_duplicate_pending`（存量并列待审合并为最早一条，译文取任一非空，其余 `rejected` + `reviewed_by=system:m5_dup_merge` 可追溯不删数据）；构造期 `_dup_merge_backfill_once`（kb_meta `learner_dup_merge_m5` 幂等） · `learner_routes.py`：`/api/learner/drafts/translate` 回 `src_langs`（真实检测码）+ `src_lang_labels`（zh 界面中文名 / en 回落 `LANG_NAMES`）+ `skipped`（zh / identity / no_body）+ `failed`（原因）；`source_lang=lang` 显式传翻译服务（引擎 prompt 不再猜）；L-4 F 缓存直出 / 中文不产译文契约不变 · `learner.html`：`_needsTranslation` 按脚本判（假名/谚文/泰文/西里尔/阿拉伯/天城/希伯来/希腊/高棉即请求；**只有纯汉字+标点数字才不请求**；拉丁含越南声调请求）；译文行带「（原语：日语）」；失败标「未译 · 翻译失败」+「重试」按钮（请求整体失败整批同标，绝不留空）· i18n `learner_page.py` 4 键 zh/en · `tests/test_learner_lang_script_220.py`（32 例） | 新 32 例 + `test_learner_triage_201` 59 passed；`-k "learner or daily_learn or kb_dedup or kb_source"` 170 passed；`test_i18n_coverage -k workspace_i18n_keys` 全绿 | 「原语: Chinese」在代码里找不到出处（疑为 LLM 译文自带的注记）——现在原语标注由后端 `detect_language` 给、页面自己渲染，不再依赖译文内容。纯汉字日语（无假名）与中文不可分，仍按 zh 跳过（诚实局限）。**与 M-1 B 互指**：两处共用 `translation_service.detect_language`（M-1 B #234 `_no_translatable_body` 也落在该文件，本单未改它） |
| **B 养号面板矛盾文案 + 灰置原因 + 人话说明** | #224（P3） | `0a19204e` | `nurture_routes.py` 新纯函数：`attention_fields`（需关注 = 健康灯 amber/red ∪ 生命周期 offline / restricted / banned / pending；理由：灯的中文句原样 + `stage_<stage>` 机器码）、`fleet_summary`（`normal + attention == total`；判词：有 red 灯或 banned/restricted → red，有需关注 → amber，否则 green，无账号 unknown）、`nurture_explain_facts`（档位日预算 8/15/25、最小间隔 60/40/25 分、活动时段、行为清单、退避阈值 1/5——全部取自 `nurture_scheduler` 常量 + 配置）；`/api/nurture/status` 下发 `summary` / `explain` / 每号 `light / health_reasons / attention / attention_reasons` · `cp-nurture.js`（双树）：概览优先读 `summary`（旧后端缺键回落旧口径），「需关注」悬浮 + 卡内点名到号与原因（≤4 行 + 「等 N 个」）；「模拟运行」灰置时按钮下方一行写明「先在下方启用至少一个账号的养护并保存」（`_syncCounts` 保存后就地隐现）；新增可展开「养号是怎么运作的？（记录 → 模拟 → 自动）」三阶段说明 + 动作清单 / 频率上限 / 时段 / 失败回退 / 默认关且自动养护需再确认 · `cp-i18n.js` 19 键 zh/en + 繁中 overlay 重生 · `tests/test_nurture_panel_224.py`（8 例） | 新 8 例 + `test_nurture_canary_route` / `test_nurture_ui_review` / `test_nurture_scheduler` / cp-i18n 四门禁 63 passed | **不改引擎逻辑**：`ops.nurture.enabled` 默认 false、`dry_run` 默认 true、`go_live` 内联确认（试点名单点名）原样；「开启需二次确认」＝真执行（自动养护）那一步已有确认，模拟运行零动作不加确认。「需关注」原因里 account_health 的句子只有中文（英文界面原样显示中文句，与 ops 看板同现状）。`tools/verify_nurture_ui.py` 浏览器门禁本单未跑 |

## 交 M-6 验收口径（回访文案用）

| 单 | 口径 |
|---|---|
| #217 | 用户版（桌面包 client 形态，未开开发者模式）建目标向导不再有「转化成交」类目（四张预置全部不见，「关系经营 / 唤回沉默 / 信息摸底 / 自定义目标」原样）；直接调 API 建该类目 403 并指向自定义目标；升级前已建的转化目标不删，卡上标「该模板已下线，建议改用自定义目标」。目标没绑产品（付费解锁没选项 / 官网产品目录为空 / 自定义没写卖什么）时，AI 主动拍里出现报价、价格、付款、下单、开户、注册链接、引导页任一句 → **整条不发**，目标时间线出「已拦下含报价/开户/支付话术的主动消息」，backend.log 有 `[goal_no_product]`。任一目标的**第一条**引擎主动消息先进草稿审批（L1，原因 `goal_first_send`），你过目后发出，第二条起按目标自治档正常发；1.0.75 已经在发的目标不受影响 |
| #226 | 用户版工具箱「AI 生成图片」显示「AI 生成照片功能开发中，暂不可用。」+ 相册有现成照片时缩略条置顶可直接发送；不再有引擎 / 尺寸 / 提示词 / 场景词 / 生成按钮 / 额度 0/20；全部文案无「联系运维」。backend.log 首次打开出一行 `[image_gen] probe skipped: user edition dev_state`；内部机每次真探出 `[image_gen] probe host=… ok=…` |
| #225 | 工具箱→翻译工具→语音 上传后先显示「已收到 x 秒音频」（算不出秒显 KB），转录与工作台客户语音**同一条链**（`asr_chain=workspace`）；失败按原因分说：超时 →「转录超时（服务响应太慢）…」/ 被拒 →「转录服务拒绝了本次请求（授权或配额问题）」/ 格式 →「音频格式无法识别…」/ 0 字节或坏文件 →「文件未正确接收…请重新上传」；backend.log 每次一行 `[toolbox_asr] chain= bytes= dur= ok= code=`。**探针夹具**：装 1.0.76 新包后 backend.log 不再出「asr/ser 域缺夹具」，`true_probe` asr 域真探 |
| #220 | 学习队列日语（含汉字）/ 韩语 / 泰语 / 越南语条目都出中文译文，译文行带「（原语：日语）」等真实检测语种；翻译失败的条目显示「未译 · 翻译失败」+「重试」，不再留空；同一题目不再两份并列待审（存量已合并为一条，被合并的在「已拒绝」页可见，`reviewed_by=system:m5_dup_merge`），后续定时采集同题只累加次数 |
| #224 | 养号面板「机群概览」判词与三个数字同一口径：有掉线 / 待登录 / 受限 / 被封或健康灯黄红的号 → 判词「有账号需要留意」且「需关注」≥1 并点名到号与原因（断线 3 天的号会被算进去）；全部正常 → 「机群状态良好」+ 0 需关注。「模拟运行」灰置时按钮下方写明原因；概览下方可展开「养号是怎么运作的？」三阶段说明（做什么动作、每档每天最多几次、隔多久、只在哪些时段、失败怎么退）。引擎默认关（enabled=False / dry_run=True）未动 |

## 待老板 / 待办

- **交 M-6**：① 装载窗带上本条全部 `.py`（页首清单）；② `unified_inbox.html` / `personas.html` / `shared/copilot/app.html`（+ desktop 镜像）里 `cp-i18n.js?v=` / `cp-i18n-ext?v=` / `cp-image.js?v=` / `cp-goal.js?v=` 未 bump（三个宿主文件分属 M-2/M-3/M-4，本单不碰）——收口 gate_sweep 时统一 bump；`ui-build.txt` 已 `bump_ui_build.py --severity notice`（20260907-0107）；③ #225 夹具随包要等 1.0.76 打包，回访口径里说清「装新包后」；④ #217 回访请让 skuio 机看一次目标卡「模板已下线」标与草稿队列里的 `goal_first_send` 卡。
- **待老板**：① `auto_create` / `retention_auto` / `reconvert_auto`（默认关）仍指向 conversion 模板——用户版是否也彻底闸掉（现只闸手建 + 卡上标下线）；② 首拍预览是否也覆盖「回复链顺势推进」（本单只覆盖引擎主动拍——对客户来消息时的顺势推进不拦，那是回复不是主动发）；③ 出图 partner/internal 面板文案里「客服」替代「运维」是否要按形态分两套。
- **P3**：`_probe_audio_file` 对 mp3/m4a/webm 的时长依赖 ffprobe（桌面包若随 M-3 带 ffmpeg 即可用）；纯汉字日语无法与中文区分（需上下文/会话语言）；养号「需关注」原因英文化（account_health 句子只有中文）。
- **收口时**：`python -m scripts.i18n_hant generate` 再生 `zh_hant_auto.py`（本单在 `goals.py` / `errors.py` / `learner_page.py` 加了简体键；`test_i18n_zh_hant` 目前仍绿）。

## 已知既有失败（非本单引入）

- `tests/test_desktop_integration_contract.py::test_language_alignment_honors_lang_query`（`<html lang="en-US">`）与 `test_i18n_coverage::test_workspace_i18n_keys_translated_in_both_langs`（`inbox.acct.bd_scope_`，跑第二次已绿）＝ M-2/M-4 在途的 `unified_inbox.html` 改动，与 M-5 无关。
- 共享 index 竞态两次遇到（D、E、B 提交前 index 里有 M-3 #228 / M-4 #230 的暂存文件），按红线轮询到 index 为空再 add + commit 放同一条命令，未卷入他人文件；提交说明留 `tmp_diag/m5_commit_{A,C,D,E,B}.txt`。

## 预算

估算 ≈ $85–95（A 约 $25、D 约 $22、E 约 $15、C 约 $12、B 约 $12）；未超 $120 上限。
