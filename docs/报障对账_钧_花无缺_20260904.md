# 报障对账 · 钧 JUN / skuio 花无缺（值守线 0904 08:xx 交老板）

台账 `D:\chengjie-instances\zhiliao\data\config\bug_intake.db`，去重后 **152 单**：
`fixed 97`（其中 **61 单从未回访**）/ `closed 30` / `confirmed 15` / `new 6` / `verified 4`。
报障量：skuio 花无缺 76 单、钧 JUN 44 单（含「钧」4 单）、BOUNDLESS 25 单，其余 5 单零散。
日报障量已从 8/30 峰值 42 单降到 9/03 的 10 单、9/04 的 1 单。

---

## 一、H 批（1.0.72）实际完成度 —— 与 `修复指令_H批_并行AB_2026-09-04.md` 对账

**对话 B 全部完成 + 对话 A 只完成 A1。**

| 项 | 工单 | commit | 状态 |
|---|---|---|---|
| A1 | #154 发→X 铆定被清空 | `2afc4e0f` + 前端 hydrate/source 分流已在 | ✅ |
| A2 | #158 审批页出不来 | — | ❌ 未动工（`draft_review.html` 无返回按钮） |
| A3 | #160 全自动放行策略 | — | ❌ 未动工（`src/inbox/autosend_policy.py` 不存在） |
| A4 | #162 入站日志不带账号 | — | ❌ 未动工（日志行仍无 `account=`） |
| A5 | 发版对账 + 症状族 | — | ❌ `docs/发版对账_v1.0.72.md` 未建 |
| B1 | #161 克隆声多语种 | `929ba165` + `23cb7dba` | ✅ |
| B2 | #159 幽灵未读 | `2f8fc70f` + `26794d2e` | ✅ |
| B3 | #155/#156 人设归属 | `933da0f8` | ✅ |
| B4 | 症状族 + 落点表 | `85da055e` | ✅ |
| 附 | zl_collect v1.3.1 上传修复 | `d46784cc` | ✅ 代码已修，**未下发到两台坐席机** |

**A2/A3 都是 P1**，其中 A3（全自动被风险词扣稿）是老板已拍决策的放行策略重定义。

---

## 二、skuio 花无缺 未修 8 单

| 单 | 级 | 症状 | 现状 |
|---|---|---|---|
| #158 | P1 | 进草稿审批工作台后点「聊天」无反应、无返回按钮，只能重启软件 | 未动工 → 指令 A2 |
| #160 | P1 | 全自动模式 AI 干净草稿被 L4 扣留：`i didn't want to stop` 子串命中 `stop` 判「停止联系」；卡片只显英文类别不显命中词；聊天页零提示 | 未动工 → 指令 A3 |
| #152 | P2 | ① AI 输出复读机化（同词根几百遍真发给客户）② 工作目标显示第 3/3 天却零推进 | ①已修 `30951d10`；②只加了观测 `e99a61c7`，**执行链未修** → 指令 C |
| #153 | P2 | WhatsApp 会话 3 天零目标推进 | 同 #152②，只有观测 → 指令 C |
| #64 | P2 | 出站混语残留 `I'm 我, and I'm not going anywhere.`，**复报 3 次** | fix_note 称已挂混语守卫，**从未回访、从未验收** → 指令 F 验收 |
| #37 | P2 | 希望 AI 学会引用回复（引用对方发言再回） | 基建已在（`unified_inbox_send_routes.py` L832 原生引用），缺 AI 自动链决策 → 指令 D |
| #154 | P1 | 客户发日语 AI 跟着说日语 | ✅ 已修，待发版回访 |
| #155/#156/#159 | P2 | 爱称两格 / 新账号自动套人设 / 幽灵未读 | ✅ 已修，待发版回访 |

---

## 三、钧 JUN 未修 11 单

| 单 | 级 | 症状 | 现状 |
|---|---|---|---|
| #162 | P2 | 两号同聊阿森：档位 manual 让位不回（是否被程序改档待查）；`dup_guard` sim=1.00 拦成需人工；同句 14s 收两次疑似串号；「收到消息」日志不带账号无法定层 | 未动工 → 指令 A4（观测先行） |
| #67 | P1 | 人设相册上传照片失败 / 无法 AI 生成图片 | fix_note 称三层齐修（落盘路径/完整性校验/归因），**未回访未验收**，需用户重传相册 → 指令 F |
| #145 | P2 | ① 工具箱克隆声仍不成功 ② LINE 收发图片/语音/视频仍不行 ③ AI 说起**其他人设**推荐的产品并贬低 ④ 虚拟未读数字 ⑤ 手机删除消息工作台不同步 | ①②③⑤ 未修 → 指令 E；④ 并入 #159 已修 |
| #61 | P2 | 人设「应用到」只认第一个登录的 TG 账号 | fix_note 称已改为合并运行时注册表，待验收 → 指令 F |
| #63 | P2 | 新登录账号应默认人审、既有账号全自动不受影响 | fix_note 称已加「按账号管接管方式」，待验收 → 指令 F |
| #36 | P2 | AI 回复客户增加繁体语言包 + 粤语语言包（界面保持简体） | 出向 zh-tw/yue 已在 8/29-8/30 四批交付，**用户不知道**；真剩余 = 地区语气自适应 → 指令 F 验收 + 指令 D |
| #40 | P2 | 目标不推进 + 设定台湾人却全是大陆语气，要「所有国家的地区自适应」 | 目标半边 → 指令 C；语气半边 → 指令 D |
| #38 | P2 | AI 不主动搜集客户信息，目标定制没在执行 | 两人两票，只有观测 → 指令 C |
| #65 | P2 | 3 小时期限目标只推进 9%（`goals/pace.py` 已出货但未接） | → 指令 C |
| #32 | P2 | ① 无意义悬浮副驾要删 ② 手机端删除消息不同步 ③「需人工 6」点不进对应会话 ④ 群组自己消失重启才回来 | 未动工 → 指令 E |

---

## 四、跨单的三处结构性问题

**1. 回访链断了（61 单）。** 61 单全部**已有 fix_note**，即修复文案早就写好、一条都没发出去。按人分：skuio 47 单、钧 14 单。这正是他们反复复报的原因（#64 复报 3 次、#145 里「这个问题为什么一直没有解决」）。`bug_intake.list_pending_notify` / `mark_notified` 接口齐备，是冲刷动作从未执行。

**2. 取证链带病运行。** `zl_collect` 上传失败根因已查明（`x-diag-meta` 头含中文备注 → PS 5.1 `Invoke-RestMethod` 发送前直接抛，服务器零请求），v1.3.1 已修（`d46784cc`），但**两台坐席机还在跑 v1.2.2**。0903 两机共 6 次 `UPLOAD_FAILED`，#161/#162 的报告是用户手工粘贴进群的。

**3. 目标不推进是最大的重复报障族。** #153 / #65 / #40 / #38 四单、两位报障人、跨 5 天。`src/companion/goals/pace.py`（11.4 KB）已出货但节奏档没接进规划器，F2 只是让「为什么没推进」可见。
