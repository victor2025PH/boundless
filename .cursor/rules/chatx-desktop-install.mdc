---
description: 智聊(ChatX)桌面安装包的打包与坐席机安装/发版 SOP。凡是「打包智聊 / 安装智聊 / 发版 / 装给坐席机 173/198/104/140 / push_chatx」这类任务，先按本规则执行，不要重新排查 173 的安装崩溃。
alwaysApply: false
---

# 智聊桌面包发版 SOP（2026-08-13 三机实测有效）

## 机器与 SSH 别名（由 deploy/machines.json 生成，勿手改 ssh config）

| 口头说法 | SSH 别名 | 系统 | 备注 |
|---|---|---|---|
| 173 | `yunsheng` | Win11 | 用户是 `admin`；NSIS 静默安装高频崩溃（见下） |
| 198 | `kouxing`（旧名 zhituo） | Win10 | C 盘小，装前先查剩余空间 |
| 104 | `lianbei`（旧名 huanyan-node） | Win10 | |
| 140 | `tingxie`（旧名 tongchuan） | Win10 | 历史坐席节点 |

## 标准流程（按序执行，每步都有实测背书）

1. **共享树协议**：`engines\chengjie\scripts\agent_probe.ps1 -Intent "..."` 登记；`services/messenger-web`、`shared/inject`、`desktop/` 若 ACTIVE（≤10 分钟内有人在写）先等静默——它们都会被打进安装包（extraResources）。
2. **bump 版本**：`engines/chengjie/desktop/package.json` 的 `version` 与 `displayVersion` 一起改（如 1.0.23 / 1.023）。
3. **后端指纹**：`cd engines/chengjie/desktop && python build/check_backend_freshness.py`；红了先 `npm run build:backend`（约 12 分钟，PyInstaller）。
4. **打包**：`npm run dist:win`——predist 自带门禁（boot-invariants、种子暂存、安装版+内测种子双冒烟），坏包出不了生。产物 `dist/ChatX-Setup-<ver>.exe`（约 476MB）。
5. **逐台安装（保数据）**：
   `powershell -File deploy\desktop\push_chatx.ps1 -TargetSsh <别名> -Install -Relaunch -Smoke`
   清档重装才用 `-FreshInstall`（内部顺序=备份改名→卸载→装，绝不先 wipe）。
6. **终验**：`deploy\desktop\chatx_fleet_status.ps1 -Targets yunsheng,kouxing,lianbei` 全 UP 且 displayVersion 是新版。
7. **收尾**：删各机暂存目录旧安装包（只留当前版，见下）；意向板 `-Done` 撤登记。

## 173 专属坑（每次装都会撞，按此处置、别再排查）

- **现象**：`installer exit=-1073741819`（0xC0000005）连续崩——NSIS System.dll 在 SSH 会话里的已知抖动，Win11（173）上高发；**不是安装包坏了**（sha256 上传后已校验）。
- **处置**：不需要人工干预。`install_chatx_node.ps1`（2026-08-13 修复后）自带 4 次静默重试 + 控制台会话计划任务兜底（要求 173 控制台有人登录，schtasks /IT）。若整条 push 仍失败，**原样重推一次** push_chatx——历史实测最多两轮必成。
- **别做**：别 RDP 上去手点安装包；别在重试中途 kill；别删脚本里的重试/兜底逻辑（注释里有 3 台×多轮实测记录，2026-08-09 与 08-13 两次翻车都有归档）。
- **风险意识**：脚本是先卸旧版再装新版——install 一旦失败，该机处于**无安装状态**，必须立刻重推直到成功，不能放着收工。

## 暂存目录纪律

每次发版会在目标机 `C:\Users\Administrator\Downloads\chatx` 留下约 476MB 的安装包，积压会吃满 C 盘（198 实测曾只剩 2.5GB 导致装不进）。装完健康验证后：删旧安装包，只保留当前运行版本对应的那个。
