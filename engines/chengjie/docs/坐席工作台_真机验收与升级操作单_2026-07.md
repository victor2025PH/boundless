# 坐席工作台 · 真机验收清单 + 准生产升级操作单（2026-07）

一~十五期工作台改造收尾配套：①手机真机 5 分钟人工验收（自动化覆盖不到的触感/键盘/声音）；
②18799/18899 滚动升级操作单（为什么这次没有自动执行，见文末风险说明）。

---

## 一、真机验收清单（约 5 分钟，手机浏览器打开 `http://<host>:18799/workspace`）

| # | 步骤 | 预期 | 过 |
|---|------|------|----|
| 1 | 竖屏打开工作台并登录 | 顶栏不换行：图标化主导航 + 铃铛 + 头像；无横向滚动条 | ☐ |
| 2 | 看屏幕底部 | 平台条常驻：全部 + Telegram/LINE/WhatsApp/Messenger 图标 + 未读角标 | ☐ |
| 3 | 点任一平台图标 | 列表即时过滤到该平台；再点「全部」恢复 | ☐ |
| 4 | 打开一个会话 | 底部平台条隐藏（空间让给聊天）；左上有「← 返回会话列表」 | ☐ |
| 5 | 点输入框呼出软键盘 | 输入框贴在键盘上方可见，不被遮挡（iOS Safari 重点验） | ☐ |
| 6 | 键盘收起 → 返回列表 | 布局复原，无残留空白/错位 | ☐ |
| 7 | 列表滚到底 | 有更旧会话时自动加载（或出现「加载更早的会话」按钮） | ☐ |
| 8 | 让同事从另一账号转派一个会话给你 | 听到提示音 + toast「XX 把 1 个会话转给了你」，点击直达该会话 | ☐ |
| 9 | 打开刚收到转派的会话 | 顶栏铃铛角标即时回落（打开即消） | ☐ |
| 10 | 切语言（头像菜单→切换语言，连点两次到 Tiếng Việt） | 筛选条/空态/按钮为越南语，无键名裸奔（形如 `inbox.xxx`） | ☐ |

任何一条不过：截图 + 手机型号/浏览器版本，回报即可（多数为 CSS 断点或 visualViewport 兼容问题，定位很快）。

---

## 二、准生产实例滚动升级操作单（18799 / 18899）

### 现状（2026-07-20 盘点；同日 20:15 已按本单完成一轮升级）

| 实例 | 启动方式 | 角色 | 备注 |
|------|----------|------|------|
| 18799 | `start_main.ps1` / `scripts/restart_main.ps1`（读 `config/config.yaml`） | **生产**：Baileys WA / Messenger ingest 桥指向它 | 2026-07-20 20:15 已升级（47s 窗口，验收 22 项 ALL PASS + PERF OK） |
| 18899 | cmd 包装启动，env 指向 `D:\chengjie-instances\tongyi\data`（`CHENGJIE_PRODUCT_ID=tongyi`） | **tongyi 产品实例**（独立数据目录/发布流程） | **不在本单管辖范围**——由 tongyi 自己的发布流程升级，勿动 |
| 18901 | 开发隔离实例（`AITR_WEB_PORT=18901` + `config.desktop.min.yaml`） | dev | 随用随起；**与 18799 共用 metrics 端口 19199**，见下方前置条件 4 |

### 为什么模板已经是新的、但仍需重启

Jinja 模板与 i18n 词条包是**热加载**的——18799 现在已经在渲染新 UI；但 Python 路由是**进程内**的，
以下修复必须重启才生效（当前 18799 上仍是坏的）：

- **批量/CmdK 转派静默空转修复**（P23 起 `updated` 恒 0，十二期修复）+ viewer 拒写 + 审计
- 会话列表游标分页 API（`before_ts`，前端已优雅降级，重启后「加载更早」才真可用）
- 转派实时通知事件（`conversation_assigned` SSE 注册在 Python 集合里）

### 前置条件（不满足不要执行）

1. **工作树干净**：当前 `engines/chengjie` 有并行工作流未提交改动
   （`persona_manager.py` / `persona_routes.py` / `replybus_routes.py` / `personas.html` / `ai_studio.html`）。
   重启会把这些**半成品带上线**。先让相应负责人提交或 stash，`git status` 确认后再动。
2. **运营低峰窗口**：重启会断开 Telegram client 与 SSE 连接（约 30~60s 恢复），Baileys sidecar 不受影响但 ingest 会短暂堆积。
3. ~~确认 18899 归属~~（已查明：tongyi 产品实例，移出本单范围，勿动）。
4. **先停 18901 开发实例再重启 18799**：监控 API 端口 19199 未按 AITR_WEB_PORT 偏移，
   两实例共抢——18901 活着时 18799 新进程的 metrics 绑定必失败（WinError 10048，
   2026-07-20 升级实测踩中，主服务不受影响但监控线程哑掉）。

### 执行步骤（18799）

```powershell
cd D:\workspace\boundless\engines\chengjie
git status --short .                       # ① 必须只剩已知/已提交改动
python -m pytest tests/ -n auto -q --timeout=90 --timeout-method=thread   # ② 全量门禁
# ③ 重启。⚠ 机器上若有 18899(tongyi)/18901(dev) 等其它 main.py 实例，
#    官方 restart_main.ps1 的"多实例防护"会把它们全部杀掉——
#    多实例共存时**不要**直接跑它，改用"端口级精准重启"（2026-07-20 实操版）：
#    只杀 18799 持有者 → 清 logs\run_sentinel.json → 确认端口释放 →
#    CIM Create 启动（不继承句柄）→ 轮询 /login 到 200。单实例机器可直接：
scripts\restart_main.ps1
# ④ 验收（token 换成该实例真实值）
python tools\smoke_acceptance.py --base http://127.0.0.1:18799 --token <TOKEN>
python tools\perf_conv_list.py    --base http://127.0.0.1:18799 --token <TOKEN>
```

回滚：`git checkout <上一个稳定提交> -- src/` 后再次重启（模板/词条热载无需回滚）。

### 18899（tongyi 产品实例）

不属本单范围：独立数据目录与发布流程，升级/重启由 tongyi 侧安排，本机操作时注意别用
`restart_main.ps1` 连坐（见步骤 ③ 警告）。
