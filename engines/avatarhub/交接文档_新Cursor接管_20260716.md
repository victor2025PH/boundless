# 交接文档 · 新 Cursor 接管开发（2026-07-16 23:20 交棒）

> 交棒人：Cursor Agent（本机 DESKTOP 会话）。接棒人读完本文 + 下列两份文档即可无缝续作：
> **① `四视角功能与版块优化方案_20260716.md`（§1–§16 = 完整工作日志与决策记录，最重要）**
> **② `设计规范_图标与令牌.md`（全站视觉/图标/窗口公约，改 UI 前必读）**

---

## 1. 源码位置与 Git

| 项 | 值 |
| --- | --- |
| 真实源码目录 | `D:\projects\模仿音色` |
| 工作区别名 | `C:\模仿音色`（**Junction 链接** → D 盘上面那个目录；服务脚本/终端习惯用 C 路径，两者是同一份文件） |
| Git 远端 | `https://github.com/victor2025PH/mfys20260618.git`（origin，可 fetch/push） |
| 当前分支 | `2026-06-20-3q30` |
| 远端最新提交 | `c069b5f`（2026-07-16 09:24 "feat(faceswap): 换脸程序开发完成"）——本地与远端此提交同步 |

**⚠ 交接前最重要的事：2026-07-16 全天的开发（P0–P8 批次）尚未提交**——本地有 42 个已跟踪修改 + 37 个未跟踪新文件（含 `static/ui_src/` 拆分源目录、`webview_shell.py`、`tools/_build_ui.py`、`tools/_favicon_lint.py`、两份核心文档、像素基线等）。**若接棒机从 GitHub 克隆，拿到的代码没有今天的任何工作**。交棒人已请示原机主执行 commit+push；若你克隆后发现远端最新提交仍是 `c069b5f`，先向机主确认同步方式（推送 or 直接在原机工作区继续）。

## 2. 运行环境（本机实况）

- **构建/门禁解释器**：`.venv_launcher\Scripts\python.exe`（Python 3.10.20；装有 PySide6-essentials、PyInstaller 6.20、Playwright、Pillow、**pywebview 6.2.1+pythonnet**）。
- **服务解释器**：Miniconda `envs\facefusion`（Hub/换脸/同传等），其余引擎各有 conda env（单一真相=`app_config.SERVICES`，`python app_config.py` 可打印解析结果）。
- **系统 PATH 无裸 python**：一律用上面两个显式路径。
- 环境变量入口：`env_config.bat`（服务地址 SVC_*、密钥）；**坑**：`SVC_FACESWAP` 默认指生产机 `.104`，本机要自跑换脸需 `start_faceswap_api.bat`（Hub 不会替"远端服务"自愈本机副本）。

## 3. 服务拓扑与启动

| 端口 | 服务 | 启动方式 |
| --- | --- | --- |
| 9000 | Hub `avatar_hub.py`（主控） | `start_avatar_hub.bat`（看护循环，防重复启动） |
| 8000 | 换脸 `faceswap_api.py` | `start_faceswap_api.bat`（见上面 SVC 坑） |
| 7900 | 同传 `live_interpreter.py` | Hub `/api/engine/start?name=interpreter` 或启动台 |
| 7855/7854 | fish_tts / stt | Hub supervisor 自动拉起 |
| — | 桌面启动台 `launcher_qt.py` | `.venv_launcher\Scripts\pythonw.exe launcher_qt.py`（或冻结版 `dist\AvatarHub.exe`） |

## 4. 铁律（违反会被门禁拦或造成漂移）

1. **`static/ui.html` 是生成产物勿手改**：源 = `static/ui_src/ui_shell.html` + `ui_src/tabs/<12 个页签>.html`，改完跑 `python tools/_build_ui.py` 拼装（`--check` 为门禁）。
2. **图标改 `tools/_gen_brand_icons.py` 重跑**（47 枚，勿手编 svg）；跨端口页面（8000/7900）按"先入库再内联"公约。
3. **设计令牌单源** `static/design-tokens.json` ↔ `brand.css` ↔ `launcher_theme.py`（`_tokens_lint` 拦）。
4. **窗口通道**：站内窗口 原生壳 WebView2 优先（`webview_shell.py`）→ Edge `--app` 兜底；`AVATARHUB_APP_SHELL=edge` 强制旧通道。Edge 已运行时会忽略几何参数——别退回"改 Edge 参数"的老路（§15 有血泪史）。
5. **每阶段收尾必跑**（全部用 venv 解释器）：
   ```
   .venv_launcher\Scripts\python.exe run_all_tests.py            # 14 套件（含 tokens/favicon/build_ui 门禁）
   .venv_launcher\Scripts\python.exe test_ui_optimization.py     # 462+ 条 UI 断言
   .venv_launcher\Scripts\python.exe ui_visual_regress.py        # 12 图像素回归（视觉改动后 --update-baseline）
   ```
6. **工作方法**（机主要求）：分阶段实施 → 每阶段测试 → 结果回写 `四视角功能与版块优化方案_20260716.md`（追加 §17、§18…）→ 收尾连跑全量 2 轮全绿 → 汇报"优化结果 + 是否再优化 + 下一阶段"。

## 5. 当前状态（截至交棒）

- **P0–P8 全部完成**（详见方案文档 §1–§16）：窗口图标/最大化、任务流侧栏、三系七品命名、巨石拆分、PRO 版位引导、emoji 图标化、线色单源、原生壳转正、pywebview 发版纳入（exe 40.9→45.4MB，冻结态验收通过）。
- **运行态**：本机全栈已重启生效，交付体检 7/7 全绿，21 张界面截图在 `ui_snapshots/preview_lingox/restart_*.png`。
- **待办（全部条件触发）**：
  1. pro_upsell 埋点回看（≈07-30，管道已验证）；
  2. 旧线名兼容位删除（等全部部署机 Hub ≥07-16 版确认：home `LINE_ALIAS`、launcher `_WB_ROW_PLAN` 旧名、home LINE_RGB 兜底表）；
  3. 发布链后续：签名/装机包/上传走 `publish_release.py`（需机主凭据）；装机回归清单在 §16 末尾；
  4. `--virtual-time-budget` 观察期（若 /ui 截图再现超时 → 备选 CDP 方案已验证，§12）。
- **已知边界**：原生壳窗口内 `target=_blank` 走系统默认浏览器；旧 Edge 应用窗口需手动关闭才换新通道。

## 6. 高频坑速查

- `C:\模仿音色` 与 `D:\projects\模仿音色` 是同一目录（Junction），日志/进程命令行两种路径都会出现。
- onefile exe = bootloader 父进程 + 运行时子进程：**窗口在子进程**，按父 pid 找窗口必空（§16 排障留档）。
- 像素回归吃"运行态确定性"：页面加"首访浮层/异步徽章"必须挂 `?uivr=1` 抑制口径（搜 `_uivr`），否则基线抖动假红。
- PowerShell 5 跑含中文的 .ps1 需 BOM，否则解析炸——临时脚本尽量纯英文输出。
- 发布双管线要同步改：安装器 `installer/AvatarHub.iss` ↔ OTA `build_packs.py`（`APP_TREE_EXCLUDES` 互为注释锚点）。
