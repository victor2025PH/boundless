# 四视角功能与版块优化方案（开发工程师 · 市场经理 · 用户 · 高级美术设计师）

> 日期：2026-07-16　范围：桌面启动台(launcher_qt) · 统一首页(/) · 网页控制台(/ui) · 对话端(/phone) · 换脸面板(:8000/ui) · 同传页(:7900) · 看板/运维/交付等运营页
> 触发：应用窗口任务栏/标题栏显示 Edge 图标、窗口非最大化 → 已当日修复（见 §1）；借此机会做一次全端功能与版块的四视角体检。
> 关联：`设计规范_图标与令牌.md`（图标/令牌/窗口公约）、`全域项目整理方案_BOUNDLESS_三视角_20260715.md`（仓库级整理，本文不重复）。

---

## 1. 当日已修复：窗口图标与最大化（根因 + 变更清单）

**为什么所有窗口都是 Edge 图标？** 三个原因叠加：

1. 桌面端站内页面统一用 Edge/Chrome `--app` 应用窗口打开（`launcher_qt.py _open_app_window`）。这种窗口的任务栏/标题栏图标**取自页面 favicon 的位图**；而全站此前只挂了 SVG favicon（`/static/icon.svg`），Chromium 不用 SVG 光栅化窗口图标 → 回退 Edge 图标。
2. 浏览器兜底请求的 `GET /favicon.ico` 在 Hub(9000) 上是 **404**（实测复现），换脸面板(8000)与同传页(7900)则**完全没有任何 favicon**。
3. 窗口尺寸只传了 `--window-size` 预设，从未传最大化旗标。

**已落地的修复**（均过 `_tokens_lint` / `_frozen_assets_test` 门禁与语法编译）：

| 文件 | 变更 |
| --- | --- |
| `launcher_qt.py` | `_open_app_window()` 默认最大化：`--start-maximized` + 主屏工作区尺寸钉位兜底（部分 Edge 版本对 `--app` 忽略最大化旗标）；`AVATARHUB_APP_MAXIMIZED=0` 可退回原 `_WIN_SIZES` 预设 |
| `avatar_hub.py` | 新增 `GET /favicon.ico`（优先 `assets/app.ico`，回退 `static/app-icon-256.png`）；`/help` 模板补双格式 favicon |
| `faceswap_api.py` | 新增 `GET /favicon.ico`；`/ui` 面板 head 挂 favicon |
| `live_interpreter.py` | 新增 `GET /favicon.ico`；主页/字幕层/术语表/会话导出/转写复盘 5 个内嵌页 head 挂 favicon |
| `static/*.html` ×13 | ui/home/phone/dashboard/ops/ask/converse/delivery/verify/setup/order/download/landing 统一补 `<link rel="icon" type="image/png" sizes="256x256" href="/static/app-icon-256.png"/>`（SVG 继续管标签页，PNG 管窗口/任务栏） |
| `设计规范_图标与令牌.md` | 「入口与窗口公约」写入两条新铁律：窗口默认最大化、favicon 双格式公约 |

**生效方式**：重启 Hub / faceswap / 同传服务后重开窗口即可（页面与 favicon 路由均 no-cache；若 Edge 图标缓存顽固，关闭全部 Edge 窗口再开一次）。

---

## 2. 现状盘点（版块地图，分析的事实基础）

**四类入口，三套分组口径**（这是后面多条问题的根）：

| 触点 | 版块划分 | 条目数 |
| --- | --- | --- |
| 桌面启动台 | Hero + 主 CTA「开始直播换脸」+ 4 张能力卡（直播换脸/克隆你的声音/听懂客户提问/直播·客服接入）+ 工作台（吃 /api/features）+ 高级运维 | 4 能力 + 20 功能 |
| 统一首页 / | 按产品线 6 区：VoiceX 音色 · ChatX 对话 · LiveX 直播 · LingoX 同传 · 运营 · 合规可信 | 20 功能 |
| 控制台 /ui 侧栏 | 按抽象名词 3 组：角色(角色库/克隆) · 创作(语音/唱歌/批量) · 运营(看板/开播/同传/历史/交付体检/日志/设置) | 12 页签 |
| 独立端口页 | 换脸面板 :8000/ui（自成一派）· 同传 :7900（品牌令牌内联） | 2 |

**单页版块**（以截图的 /ui#voice 为例，自上而下）：窗口标题栏 → 顶栏（品牌/GPU/版本/授权/自检/演示…约 10 个胶囊）→ 促销/任务横幅 → 页签条 → 「文字驱动数字人」主卡 → 试听胶囊组 → 大 CTA「开口播报」→ 历史列表 → 情感试音间 → 角色对比 → 引擎对比 → 输出到直播·录制。内容前的固定头部约占 130–160px。

---

## 3. 四视角深度分析

### 3.1 开发工程师（可维护性 / 一致性 / 防回归）

**亮点**：单一真相意识强（/api/features 注册表、design-tokens.json、brand-icons.svg、_WIN_SIZES）；门禁文化成熟（tokens lint、冻结态资源、像素回归）。

**痛点**：
- **D1 巨石文件**：`avatar_hub.py` ≈3.1 万行（全产品路由耦合）、`ui.html` ≈8.5 千行（12 个页签挤在一个 Alpine 单文件里）——任何页签改动都要动全站文件，回归面大。
- **D2 三套 UI 栈漂移**：控制台(Tailwind+Alpine) / 同传页(内联令牌，人工同步) / 换脸面板(**完全脱离令牌体系**，Arial + `#e94560` 红)。今天的 favicon 事故正是这种"每个服务自己造页面"的典型产物：公约只覆盖了 Hub，8000/7900 两个端口天然漏网。
- **D3 窗口公约无门禁**：favicon/最大化/尺寸公约写在文档里，但没有 lint 拦截，下次新增页面仍可能漏。

**建议**（对应 §4 P1/P2）：新增 `tools/_favicon_lint.py` 扫描所有被 `_open_app_window` 打开的页面 head（P1）；`ui.html` 按页签拆分模块（P2）；换脸面板并入控制台或重皮肤（P1，同时是设计诉求）。

### 3.2 市场经理（品牌一致性 / 转化 / 演示体验）

**亮点**：free/pro 版位已进注册表；order/download/sales_onepager 商业前门齐全；白标(brand.js)可整体换肤。

**痛点**：
- **M1 品牌时刻掉链子**：给客户演示/交付验收时，任务栏一排 Edge 图标 = "这是网页不是软件"（今日已修）。同理，窗口非最大化开屏显小气（已修）。
- **M2 三套叫法打架**：启动台说「直播换脸/克隆你的声音」、首页说「VoiceX/ChatX/LiveX/LingoX」、控制台说「角色/创作/运营」。买家从官网 → 安装 → 使用要换三次心智词表，销售话术无法贯通到界面。
- **M3 促销横幅抢 C 位**：顶部横幅（自动播放/水印任务提示）常驻黄金位置，演示模式下干扰主叙事；pro 功能在控制台里无升级引导（版位信息只在首页展示）。

**建议**：页签副标题挂产品线名（如「语音 · 幻声 VoiceX」，P1）；演示模式收纳横幅+隐藏日志/运维页签（P1）；pro 页签对试用户显示锁标+一键到 /order（P2）。

### 3.3 用户（任务动线 / 认知负担 / 上手）

**亮点**：命令面板 Ctrl+K 全局可达；data-tip 富提示统一「标题|作用|何时用」；首启向导/交付体检闭环。

**痛点**：
- **U1 四个门不知走哪个**：启动台、/home、/ui、独立面板各有入口逻辑，功能重叠但清单不一致（如「实时对话」在首页有卡、控制台侧栏却没有对应页签，藏在启动台按钮里）。
- **U2 分组不贴任务流**：核心动线「建角色 → 克隆声 → 试音 → 开播/对话」在侧栏里横跨「角色→角色→创作→运营」三个抽象组；「同传」列在「运营」组（它明明是核心能力）。用户按"我要干什么"找，界面按"东西是什么"摆。
- **U3 头部太厚**：标题栏+顶栏+横幅+页签四层固定头，1280×860 窗口里内容起点近 1/5 屏（最大化默认后有缓解，但小屏仍在）。
- **U4 术语墙**：试听胶囊「整声/开口/检索/声乐」等行话无首次解释（有 tip 但要 hover 才知道）。

**建议**：侧栏按任务流重分组：**我的分身**(角色库/克隆) · **内容创作**(语音/唱歌/批量) · **上线**(开播/对话/同传) · **数据与运维**(看板/历史/交付体检/日志/设置)——只动 `hub.js tabs[].group` 与 `tabGroups`，20 分钟工作量（P0）；「对话 /phone」以页签形式入侧栏（新窗口打开，P1）；顶栏胶囊按角色折叠（用户模式只留 授权+自检+演示，其余进「更多」，P1）。

### 3.4 高级美术设计师（视觉系统 / 细节品质）

**亮点**：令牌体系（色/圆角/字阶/暗色层级）与 43 枚 lucide 线性图标已成体系且有门禁；emoji 三分类公约明确。

**痛点**：
- **A1 换脸面板是"外星页面"**：`#e94560` 洋红 + Arial + 无暗色层级令牌，与全站无界蓝紫气质完全断裂——它还是用户高频页面。
- **A2 窗口标题栏配色不跟品牌**：Edge `--app` 窗口标题栏颜色取 `<meta name="theme-color">`，目前只有 phone/home/landing 挂了 `#080b10`，ui/dashboard/ops/delivery 等没挂 → 深色页面顶着一条浅色标题栏。
- **A3 功能位残留 emoji**：启动台托盘菜单「🌐 官网」「💬 联系客服」、部分按钮「🎭/📡」前缀，按公约应换线性图标（内容位/状态位保留）。
- **A4 图标身份刚统一到位**：icon.svg（声波纹）与 app.ico/app-icon-256 同母版——favicon 双格式公约落地后，需在像素回归基线里补"窗口图标"检查项，防再漂移。

**建议**：全站 `<meta name="theme-color" content="#080b10">` 补齐（P0，一行/页）；换脸面板令牌化重皮肤（P1）；托盘/按钮 emoji 清理进下一次图标批次（P2）。

---

## 4. 优化方案（分级排期）

### P0 · 本周内，零风险高回报（✅ 全部完成 2026-07-16）

| # | 事项 | 改动点 | 状态 |
| --- | --- | --- | --- |
| P0-1 | 窗口图标 + 最大化 | §1 全部 | ✅ 重启服务后任务栏见无界声波 logo、窗口满屏 |
| P0-2 | 侧栏按任务流重分组（U2） | `hub.js` tabs 改四组：我的分身·内容创作·上线·数据与运维；「同传」移出运营 | ✅ Playwright 实测渲染正确；像素基线已重建 |
| P0-3 | theme-color 全站补齐（A2） | 13 个 static 页 + /help 模板 + 换脸面板 + 同传 4 内嵌页 | ✅ 由 `_favicon_lint` 门禁长期守护 |

### P1 · 动线与一致性（✅ 全部完成 2026-07-16，实施中三处升级见「实施记录」）

| # | 事项 | 实施结果 | 状态 |
| --- | --- | --- | --- |
| P1-1 | favicon/窗口公约门禁（D3） | 新增 `tools/_favicon_lint.py`（favicon 双格式 + theme-color + 三服务 /favicon.ico + 最大化公约），入 `run_all_tests.py` | ✅ 20 项全绿 |
| P1-2 | 换脸面板令牌化（A1/D2） | `/ui` 面板整体换品牌令牌皮肤（内联自包含），标题改「幻颜 FaceX · 换脸面板」；长期仍规划并入 /ui#stream 侧板 | ✅ 离线渲染截图验收 |
| P1-3 | 产品线叫法统一（M2） | **升级**：不止挂 tooltip——注册表 line 直接更名「三系七品」官方口径（幻声/智聊/幻影/通译），首页/启动台/命令面板三端自动跟随；页签 tooltip 运行时取注册表（tabTitle()，零重复维护） | ✅ 含旧线名兼容别名 |
| P1-4 | 「对话」入侧栏 + 顶栏减负（U1/U3） | **升级**：phone 作跳出型页签（↗ 标识，新窗口打开、控制台不离场，深链/持久化已防呆）；顶栏「对话/看板」胶囊因与侧栏重复直接退役（比原计划的「更多」菜单更省一层） | ✅ Playwright 实测弹窗行为 |
| P1-5 | 演示模式收纳（M3） | 告警收敛已有（demoAlertsOpen）；补缺口：交付体检/日志页签标 `opsOnly`，演示模式自动隐藏 | ✅ 实测隐藏生效 |

### P2 · 一个月内，结构性还债

| # | 事项 | 说明 |
| --- | --- | --- |
| P2-1 | `ui.html` 拆模块（D1） | 每页签一个 js/html 片段，构建期拼装或运行期 fetch；先拆最大的 开播/语音 两签 |
| P2-2 | pro 版位升级引导（M3） | 试用/free 态在 pro 页签角标 🔒，点击浮层直达 /order；数据埋点看转化 |
| P2-3 | emoji 功能位清理（A3） | 托盘菜单/按钮前缀 emoji → `_sprite_icon` 线性图标，走既有图标批次流程 |
| P2-4 | 原生窗口壳预研（D2 长期） | 若要求任务栏图标 100% 可控 + 免 Edge 依赖：评估 WebView2/pywebview 壳（AvatarHub.exe 图标直接生效），与现有 `--app` 方案并行灰度 |

### 不做清单（明确刹车）

- 不合并 /home 与 /ui（首页=前门/白标展示，控制台=作业台，职责不同）；
- 不给运营页做移动端适配（桌面交付场景为主，phone 已单独适配）；
- 不在本轮动 FEATURE_REGISTRY 的 id/href（三端同源，动 id 波及命令面板与工作台）。

---

## 5. 实施与验收路径

1. 每步改完必跑：`python tools/_tokens_lint.py` → `python tools/_favicon_lint.py` → `python run_all_tests.py` → `.venv_launcher\Scripts\python ui_visual_regress.py`（视觉大改后 `--update-baseline`）。
2. P0-2/P0-3 一起提一版基线（分组与标题栏都会动像素）。
3. P1-1 门禁先行合入，再做 P1-2/P1-3（新公约先有牙齿，后铺量）。
4. 验收口径：新装机演示流程（启动台 → 开始直播换脸 → 开播页 → 对话页）全程任务栏无 Edge 图标、全窗最大化、三端叫法一致。

---

## 6. 实施记录（2026-07-16 · P0+P1 落地）

**验证结论**：`_favicon_lint` 20 项全绿；`test_ui_optimization` 454/459 通过（5 项 SKIP=在线用例）；`run_all_tests` 116 PASS / 0 FAIL（p13 两套件在 .venv_launcher 缺 numpy 属环境问题，用 facefusion 环境复跑 ALL PASS）；像素基线重建 12 张；Playwright 实测：新分组渲染、演示模式隐藏运维页签、「对话」页签弹新窗且控制台不离场。

**实施中三处方案升级**（深入后发现的更优解）：

1. **叫法统一升维（P1-3）**：原方案只在页签 tooltip 挂产品线名，仍是第四套词表。实施时改为在功能注册表（单一真相）直接把 line 更名为官方「三系七品」口径——首页分区标题、启动台工作台行分组、命令面板提示三端零改动自动跟随；tooltip 用 `tabTitle()` 运行时查注册表，不产生任何重复维护点。配套：home.html 线色表/启动台行排布保留旧线名别名（护住 localStorage 旧缓存与旧版 Hub 的过渡窗口）、前后端拼音字表补「幻智聊影通译」六字（huansheng/hs 检索不断档）。
2. **顶栏减负改走「去重」而非「折叠」（P1-4）**：原方案要新做「更多」收纳菜单；实施时发现对话/看板入侧栏后顶栏同名胶囊纯属重复入口，直接退役即达到减负目的，少造一个交互层。剩余顶栏项的「用户模式折叠」降级为下一阶段可选项。
3. **跳出型页签的防呆细节**：`/ui#phone` 深链、`hub_tab` 持久化均排除 href 页签（否则会选中一个没有面板的空页签）；侧栏展开态给 ↗ 角标传达「新窗口打开」。

**遗留小事**（下一阶段一并处理）：`run_all_tests.py` 对 p13 两套件应改用 `app_config.conda_python("facefusion")` 解释器（或 venv 补装 numpy），消除环境性假红。

---

## 7. 下一阶段实施计划（P2 · 一个月内）

> 实施状态看 §8「P2 实施记录」（2026-07-16 起逐阶段回写）。

按依赖顺序：

1. **P2-0 测试解释器修正**（半小时）：上述 p13 假红治理，让 `run_all_tests` 在任何解释器下全绿。
2. **P2-1 `ui.html` 拆模块**（3–5 天，最大技术债）：8.5 千行单文件按页签拆 `static/tabs/<id>.html` 片段，构建期拼装（新增 `tools/_build_ui.py`，产出仍是单文件 ui.html——不引入运行时框架、不破坏离线交付）；先拆 开播/语音 两个最大页签试点，`test_ui_optimization` 字符串断言改读拼装产物。
3. **P2-2 pro 版位升级引导**（1 天）：free/试用态给 pro 页签（唱歌/批量/开播/同传）加 🔒 角标 + 点击浮层直达 /order；埋点 `data-ev` 看转化。注册表已有 edition 字段，前端只读。
4. **P2-3 emoji 功能位清理**（1 天）：启动台托盘菜单「🌐 官网/💬 客服」、换脸面板卡片标题 emoji → `_sprite_icon`/线性图标，按图标批次流程走（改 `_gen_brand_icons.py` 补缺失符号）。
5. **P2-4 线色进注册表**（半天）：home.html `LINE_RGB` 迁移为注册表 `line_rgb` 字段（home 注释已预留此方向），删除前端别名表——彻底单一真相。
6. **P2-5 原生窗口壳预研**（2 天，可选）：pywebview/WebView2 壳验证「任务栏图标 100% 可控 + 免 Edge 依赖」，与现 `--app` 方案并行灰度；若通过，`_open_app_window` 加第三优先级。

**明确不做**（维持 §4 刹车清单）：不合并 /home 与 /ui；不动注册表 id/href；运营页不做移动端适配。

---

## 8. P2 实施记录（2026-07-16 起，逐阶段回写）

### P2-0 测试解释器修正 ✅（2026-07-16）
- `run_all_tests.py`：p13 两套件（依赖 numpy）在当前解释器缺 numpy 时自动改用 `app_config.conda_python("facefusion")`；`_EXTRA_ARGS` 机制支持带参套件。
- **测试**：全量套件 14 个 ALL GREEN（PASS=116 / FAIL=0），p13 假红消除。

### P2-1 ui.html 拆模块（开播/语音试点）✅（2026-07-16）
- 单一真相迁移：`static/ui_src/ui_shell.html`（壳 5526 行）+ `ui_src/tabs/stream.html`（2301 行）+ `ui_src/tabs/voice.html`（763 行）；`tools/_build_ui.py` 拼装回单文件 `static/ui.html`（顶部生成横幅），交付方式/服务方式零变化。
- 门禁：`_build_ui.py --check`（产物与源逐字节一致，手改产物/改源忘重跑都红灯）入 `run_all_tests.py`。
- **测试**：拆分无损校验 PASS（产物=原文件+一行横幅）；全量套件 ALL GREEN；`test_ui_optimization` 454/459（5 SKIP=在线用例）。
- **实施中深挖修复一个存量问题**：像素回归 `ui_mobile_390x900` 报 9.11% 差异，排查确认与拆分无关（产物逐字节一致）——根因是顶栏授权徽章异步加载后文字变长，390px 窄屏顶栏换行行数随加载时机漂移（截图竞速，此前基线恰好截到加载后状态而掩盖）。已根治：licChip 在 `?uivr=1` 回归模式保持占位（与 dlvChip/tour/onboard 同一确定性口径）。复跑 12/12 全绿（最大差异 0.15%），连续两跑稳定。

### P2-2 pro 版位升级引导 ✅（2026-07-16）
- 唱歌/批量/开播/同传（注册表 `edition==='pro'`，单一真相）页签挂金色 **PRO** 徽（桌面侧栏 + 移动横向导航），点徽直达 `/order` 并埋点 `/api/ui/event {ev:'pro_upsell'}`（进 /ops 入口热度漏斗）。
- **保护规则**（付费客户零打扰）：正式旗舰/企业授权、试签旗舰中、演示模式、授权态未加载 → 一律不显示；授权态复用既有 `bd-lic` 单源广播（licChip 60s 轮询），零新增轮询。
- **实施中修复一个衍生 bug**：`loadFeatures()` 原本把 `/ui#` 站内项过滤掉（只留跨页入口喂命令面板）→ P1-3 的页签产品线 tooltip 与本阶段 PRO 徽都取不到数据。改为存全量注册表、去重下沉到命令面板消费处。
- **测试**：Playwright 实测四态全对（本机旗舰 0 徽 / 注入试用态 4 徽=唱歌·批量·开播·同传 / 点击弹 /order / 演示模式隐藏）；全量套件 ALL GREEN；`test_ui_optimization` 通过；像素回归 12/12 全绿（uivr 模式授权不加载→徽章天然隐藏，基线零漂移）。

### P2-3 emoji 功能位清理 ✅（2026-07-16）
- 图标库 43→46 枚（`_gen_brand_icons.py` 新增 plus/scissors/shirt，XML 校验通过）。
- 启动台托盘菜单：「🌐 官网 / 💬 联系客服 / 🧰 生成诊断包 / ⬆ 检查更新」emoji 前缀 → `_sprite_icon` 线性图标（与网页同源同色）。
- 换脸面板：9 处卡片标题/按钮/上传区 emoji（🔗🎭👤➕🎨⚡🦲📁✅💇👗）→ 11 个占位符渲染时从图标库现取内联（**优化点**：不手抄 SVG 拷贝，`_panel_icon()` 直接读 `brand-icons.svg` 单一真相，库更新面板自动跟随、零手抄漂移；缺符号优雅退纯文字）。保留：h1 的 🎭（品牌位）、状态反馈 ✅/❌/⚠（状态位）、`<option>/<optgroup>` 内 emoji（HTML 无法渲染 SVG 的位置）。
- 发现 `CAP_ACTIONS` 为无消费死配置（grep 全仓仅定义处），未动——留待后续清理批次统一处理。
- **测试**：编译通过；离线渲染截图确认 11/11 图标就位、观感统一；全量套件 ALL GREEN。

### P2-4 线色进注册表 ✅（2026-07-16）
- 后端 `_FEATURE_LINE_COLORS` 随 `/api/features` 下发 `line_colors`（产品线视觉身份单一真相）；`home.html` 改为 `lineColor()` 优先吃注册表、`LINE_RGB` 退化为离线/旧后端兜底；localStorage 功能缓存同步存 `line_colors`。
- **实施中比原计划更优的一步**：原计划"删前端别名表"，实测发现真实过渡窗口——static 文件即改即生效而 Hub 进程要等重启，旧 Hub 下发旧线名时新前端分区色掉回默认蓝（Playwright 实锤：智聊 ChatX 掉色）。改为 `LINE_ALIAS` 旧→新**归一化映射**（比原来的"重复色值别名表"少一份色值维护，语义也更对）；旧后端绝迹后可删。
- **测试**：Playwright 实测六条产品线分区 `--line-rgb` 全部正确、零 JS 错误（对照修复前 ChatX/LiveX/LingoX 掉默认蓝）；`test_ui_optimization` 新增 8 项"四P2"检查（线色单源/PRO 角标接线与保护规则/任务流分组/拼装门禁/旧线名清理）全过，461/466（5 SKIP=在线用例）。

### P2-5 原生窗口壳预研 ✅ 结论：可行（2026-07-16）
- 环境实测：本机 WebView2 运行时 150.0.4078.65 在位；`.venv_launcher` 装 pywebview 6.2.1（+pythonnet 3.1.0）成功。
- POC（`tools/_webview_shell_poc.py`，手动运行会真弹窗，不进自动化门禁）：WebView2 原生壳打开 /ui **一次通过**——最大化承载页面、`SetCurrentProcessExplicitAppUserModelID` 任务栏身份独立、`WM_SETICON` 注入 assets/app.ico（任务栏/标题栏/Alt-Tab 同步生效）、生命周期干净退出。
- **结论**：任务栏图标 100% 可控 + 免 Edge 依赖的目标技术上成立；冻结进 AvatarHub.exe 后连图标注入都可省（exe 图标即默认）。
- **刹车（留给决策后再做的集成项）**：正式并入 `_open_app_window` 第三优先级前需要两个决定——① pywebview+pythonnet 进 AvatarHub.exe 打包清单（安装包体积/装机兼容面回归）；② 多窗口进程模型（launcher 是 Qt 主循环，webview.start 阻塞，需子进程壳或事件循环整合）。当前 Edge `--app` 方案在 P0-1 修复后已满足品牌诉求，不急切换。

---

## 9. P2 收尾总结（2026-07-16）

**全部 6 个阶段完成**：P2-0 假红治理 → P2-1 巨石拆分（试点）+ 拼装门禁 → P2-2 版位引导 → P2-3 emoji 清理 → P2-4 线色单源 → P2-5 原生壳预研（可行结论）。

**最终验收（连跑两轮，全绿）**：
| 轮次 | run_all_tests（14 套件） | test_ui_optimization | ui_visual_regress（12 图） |
| --- | --- | --- | --- |
| 第 1 轮 | ALL GREEN · PASS=116/FAIL=0 | ALL PASSED · 461/466（5 SKIP=在线用例） | 12/12 全过（最大差异 0.15%） |
| 第 2 轮 | ALL GREEN · PASS=116/FAIL=0 | ALL PASSED · 461/466（5 SKIP=在线用例） | 12/12 全过（最大差异 0.15%） |

生效方式提醒：静态资源（ui.html/hub.js/home.html/图标库）即改即生效；`avatar_hub.py`（注册表 line_colors）、`faceswap_api.py`（面板图标）、`launcher_qt.py`（托盘图标）需重启对应进程。

**下一批次候选**（按价值排序，非承诺）：
1. `ui_src/tabs/` 继续拆余下 10 签（profiles/sing/batch/clone/settings 优先——改动频度高）；→ ✅ P3-1 完成
2. 原生壳集成决策（见 P2-5 刹车两项）；→ ✅ P3-2 灰度接入（进程模型解决；打包决策仍留）
3. `CAP_ACTIONS` 死配置清理 + 启动台工作台按钮 emoji 残留巡检；→ ✅ P3-3 完成
4. pro_upsell 埋点观察两周 → 数据反哺 /order 定价页文案；→ 时间条件未到（埋点 2026-07-16 才上线）
5. `LINE_ALIAS` 与 launcher `_WB_ROW_PLAN` 旧线名兼容位：确认全部部署机 Hub 升级后删除。→ 需部署确认，保留

---

## 10. P3 实施记录（2026-07-16 晚 · 逐阶段回写）

### P3-1 余下 10 页签全部拆分 ✅
- `ui_src/tabs/` 新增 profiles(382 行)/sing(809)/history(274)/clone(252)/batch(178)/settings(366)/selfcheck(120)/logs(29)/interp(27)/dashboard(13)，加上首轮 stream(2301)/voice(763) 共 **12 签全模块化**；壳 `ui_shell.html` 从 8.6 千行缩到 **3023 行**（顶栏/侧栏/弹层/授权脚本）。
- **实施中修复拆分脚本一处边界 bug**：首版终点探测越过已拆面板的 `BUILD:TAB` 占位行，把 stream/voice 占位吞进 profiles 片段（拼装门禁当场拦下=门禁价值实证）。修正为「占位行是硬切割边界」，并给对账脚本补"拼装失败即 FAIL"防假阳性。
- **测试**：拆后重拼与拆前 `ui.html` **逐字节一致**（PASS）；全量套件 ALL GREEN。

### P3-2 原生壳灰度接入 ✅（默认关闭）
- 新增生产版 `tools/webview_shell.py`（区别 POC）：按 **pid** 找窗柄注入图标（页面会改窗题，按标题找会漂）、`document.title` 同步窗题（对齐 Edge --app 行为）、localStorage 持久化到 `data/webview_profile`（页签记忆/演示模式偏好不丢）、`--autoclose` 供验收。
- `launcher_qt._open_app_window()` 前置灰度分支：`AVATARHUB_APP_SHELL=webview` 时子进程拉壳（**子进程模型绕开 Qt 主循环与 webview.start 阻塞的冲突**——P2-5 刹车之一就地解决）；解释器可由 `AVATARHUB_APP_SHELL_PY` 指定，缺任一环节自动回退 Edge。
- **仍留给决策**：pywebview+pythonnet 进 AvatarHub.exe 打包清单（安装包体积/装机兼容回归）——灰度期用 `.venv_launcher` 解释器即可跑。
- **测试**：壳直跑 `--autoclose 3` 正常（打开 /ui、最大化、干净退出）；`data/webview_profile` 持久化目录落盘；编译通过。

### P3-3 CAP_ACTIONS 清理 + 启动台 emoji 全面巡检 ✅
- 删除 `CAP_ACTIONS` 死配置（grep 全仓零消费）；`CAP_TAGLINES` 有消费保留。
- 图标库 46→**47** 枚（新增 refresh）。`_btn()` 增 `icon=` 参数：实底按钮（primary/accent/danger/boot）白描、幽灵/链接按钮次级文字色，并登记 `_icon_btns` 由 `_apply_theme` 随主题重染（与工作台图标同一机制，堵住"切主题图标不变色"的缺口）。
- 清理范围：托盘菜单 9 项全图标化；「更多运维」菜单 8 项（🧩🔧✅📋🧪🩺🧰⏪→线性图标）；底栏 设置/授权/停止全部；日志栏 4 按钮；高级运维 2 按钮；错误卡 4 按钮；一键演示 ✨；到期弹窗 🔑。**保留**（公约内容位/状态位）：▶ 主 CTA 播放隐喻、☀/☾ 主题切换字形、● 托盘状态行、💡 提示文案、BOOT_TIPS/客户状态文案。
- **测试**：编译通过；全量套件 ALL GREEN；启动台视觉验证待下次重启（图标走工作台同一条已验证管线，风险低）。

### P3 最终验收（连跑两轮，全绿）
| 轮次 | run_all_tests（14 套件） | test_ui_optimization | ui_visual_regress（12 图） |
| --- | --- | --- | --- |
| 第 1 轮 | ALL GREEN · PASS=116/FAIL=0 | ALL PASSED · 461/466（5 SKIP=在线用例） | 12/12 全过（最大差异 0.15%） |
| 第 2 轮 | ALL GREEN · PASS=116/FAIL=0 | ALL PASSED · 461/466（5 SKIP=在线用例） | 12/12 全过（最大差异 0.15%） |

生效方式：ui.html/图标库即改即生效；`launcher_qt.py`（托盘/按钮图标、原生壳通道、CAP_ACTIONS 清理）需重启启动台。

**P4 批次候选**（时间/决策条件满足后）：
1. pro_upsell 埋点数据观察（2026-07-30 前后回看 /ops 入口热度）→ 反哺 /order 文案与定价陈列；→ 时间未到，保留
2. 原生壳打包决策：若采纳，pywebview+pythonnet 进 AvatarHub.spec hiddenimports + 安装包体积/装机兼容回归，`AVATARHUB_APP_SHELL=webview` 转默认开；→ ✅ P4-A 完成"转正预备"（见 §11），拍板即可一行开启
3. 旧线名兼容位（home `LINE_ALIAS`、launcher `_WB_ROW_PLAN` 旧名）：确认全部部署机 Hub ≥2026-07-16 版后删除；→ 需部署确认，保留
4. 控制台顶栏授权卡内 JS 生成按钮的 emoji（⚡🎫🔄🧾）→ 线性图标；→ ✅ P4-B 完成
5. `ui_shell.html`（3023 行）二次拆分评估：→ 评估结论=暂不拆（单人维护、无并行改动冲突，拆分收益<管理成本；多人协作时再启动）

---

## 11. P4 实施记录（2026-07-16 晚 · 逐阶段回写）

### P4-A 原生壳"转正预备"✅（打包决策已备好数据，默认行为零变化）
- `tools/webview_shell.py` → 根目录 `webview_shell.py`（可被冻结打包 import 的模块形态）；POC 脚本退役删除。
- **冻结态派发通道**：`AvatarHub.exe --webview-shell <url>` 入口（`launcher_qt.main` 顶部、先于 QApplication/单实例锁派发）；`_open_app_window` 灰度分支双轨——冻结态自派发 exe 子进程、开发态经 venv 解释器跑脚本。
- **关键防呆（深挖发现）**：pywebview 已装在构建 venv 里，若 shell 模块直接 `import webview`，PyInstaller 静态分析会**悄悄把 pywebview 吞进下一次打包**——打包决策就被架空了。改为 `importlib.import_module("webview")` 字符串导入 + `available()` 探测，pywebview 是否进包 100% 由 spec 显式决定；spec 已写入决策注释与实测数据。
- **体积实测**：webview 1.9MB + pythonnet 3.2MB + clr_loader 0.3MB ≈ **5.4MB 裸增量**（未计 UPX 压缩收益）。
- **测试**：编译通过；根目录壳直跑 `--autoclose 2` 正常；全量套件 ALL GREEN。

### P4-B 授权/交付徽章与授权卡 emoji 图标化 ✅
- 顶栏 `licChip`（🔑/⚡→lock/zap）与 `dlvChip`（🚦→probe）静态占位与 JS 动态刷新全部改内联线性图标；授权卡头（🔑）、五个动作按钮（⚡试用/↩还原/🎫输码导入/🔄刷新/🧾续费申请→zap/clock/download/refresh/file）含 busy 态文案同步图标化；服务状态（🩺）与运维告警（🔧）两个悬浮层标题同批。**保留**：⚡ 旗舰试用中横幅（状态位）、✓/✗/◂ 对比表符号。
- **实施中发现并修复一个渲染坑**：Tailwind preflight 把裸 `<svg>` 重置为 `display:block`，图标独占一行把徽章胶囊撑爆（实拍抓到）——`bdIcx()` 内联写死 `display:inline-block`（与 brand.css 的 `.bd-ic` 同理），并给两枚徽章补 `whitespace-nowrap`。
- **测试**：Playwright 实测两徽章/授权卡按钮 SVG 就位、零残留 emoji、零 JS 错误；截图人工复核（档位对比表/指纹/按钮排版完好）；`test_ui_optimization` 462/467 全过；全量套件 ALL GREEN；像素基线已按新徽章更新。

### 跳过项与理由
- P4-1 埋点回看：埋点 2026-07-16 上线，数据窗口未满，约 07-30 回看。
- P4-3 旧线名兼容位删除：删除安全性取决于"全部部署机 Hub 已升级"，本机无法确认，保留兼容位。
- P4-5 壳二次拆分：当前单人维护、无并行冲突，评估结论=暂不拆。

### 像素回归 ui_mobile 摘牌（P4 收尾时的诚实处置）
- 现象：`ui_mobile_390x900` 在两个稳定态间**跨次振荡**（同一代码 0.00% ↔ 9.20%，整页 ~33px 垂直位移），且早于本批次即偶发（P2-1 已抓过一次 licChip 竞速，当时修复了一个成因但非全部）。
- 排查已排除：WS 断连横幅（已 `!_uivr` 门 + `x-cloak`，顺手修掉一个真实用户可见的开屏闪条）、licBanner、header→nav 间元素（Playwright DOM 探针 390px×3 连开全稳定 118px）——Edge 无头截图路径下仍复现，振荡源未定位。
- 处置：按该矩阵**既定口径**（"零误报优先于覆盖面"，与 dashboard/stream/batch/voice 剔除同理）把 ui_mobile 摘出门禁矩阵、注释保留复牌条件；移动档覆盖由 `phone_mobile_390x844`（长期 0.00% 稳定）承担。
- **净收益**：门禁矩阵回到确定性；同时消灭一个真实缺陷（WS 横幅开屏闪现把整页顶下 24px，真机用户也能看到）。

### P4 最终验收（连跑两轮，全绿）
| 轮次 | run_all_tests（14 套件） | test_ui_optimization | ui_visual_regress（11 图） |
| --- | --- | --- | --- |
| 第 1 轮 | ALL GREEN · PASS=116/FAIL=0 | ALL PASSED · 462/467（5 SKIP=在线用例） | 11/11 全过（最大差异 0.12%） |
| 第 2 轮 | ALL GREEN · PASS=116/FAIL=0 | ALL PASSED · 462/467（5 SKIP=在线用例） | 11/11 全过（最大差异 0.12%） |

**P5 批次候选**：
1. pro_upsell 埋点回看（07-30）→ /order 陈列反哺；→ 时间未到，保留
2. 原生壳打包拍板（数据已备：+5.4MB；拍板后 spec 加三行 + 装机回归）；→ ✅ 已决策（见 §12）：**本轮不打包，维持灰度**
3. 旧线名兼容位删除（部署确认后）；→ 需部署确认，保留
4. ui_mobile 振荡源专项（DOM 级逐帧 diff 定位后复牌）；→ ✅ P5-A 定位根修并**复牌**
5. 角色抽屉/回收站/语音包订阅等 Alpine 弹层内按钮 emoji 批次；→ ✅ P5-B 完成

---

## 12. P5 实施记录（2026-07-16 夜 · 逐阶段回写）

### 决策记录：原生壳打包——本轮不打包，维持灰度通道
理由：Edge `--app` 方案经 P0-1（favicon/最大化）修复后已满足品牌诉求；+5.4MB 与新增 pythonnet/.NET 运行面（装机兼容回归成本）当下收益不成比例。灰度开关与冻结态派发通道已备好（P4-A），未来客户侧出现 Edge 依赖问题时可零开发切换。

### P5-A ui_mobile 振荡源定位 → 根修 → 复牌 ✅
- **猎捕**：用回归器同款 Edge 无头命令连拍 10 张聚类 → 实锤双稳态（9:1 / 4:6 两轮复现，整页 ~30px 位移）；CDP 直连同参数 14 连开 DOM 全稳定（差异只存在于 `--screenshot` 抓帧路径）→ 排除 DOM 布局振荡。
- **对照实验定罪**：同参数 ± `--virtual-time-budget` 各拍 6 张——现状 2 簇，加虚拟时间 **1 簇**。根因=**`--screenshot` 在 load 后立即抓帧，页面异步任务（Alpine 托管/轮询首拍/字体回流）跑到哪一步纯看运气**，settle 重拍机制在"每次都随机落在两个吸引子之一"时失效。
- **根修**：`ui_visual_regress.shot()` 给 **/ui 系截图**加 `--virtual-time-budget=8000`（页面先跑完 8 虚拟秒任务再抓帧）；/phone 系不加——实测其免提长轮询会与虚拟时钟互锁偶发 40s 超时，且 phone 基线长期 0.00% 本就稳定。超时 40→90s（虚拟时间下真实耗时上升）。
- **顺手修掉两个存量问题**：① `capture_settled()` 中途 shot 失败会把当前帧留成"文件不存在"，下游 `diff_pct` 直接 FileNotFoundError 崩整个回归（B 轮实锤）——补"上一帧回退"保底；② header 内 8 个裸 `x-show` 徽章（▶角色/兜底/直播中/变声中/高负载/过载/运维告警/GPU 条）补 `x-cloak`，Alpine 托管前不再闪现（真机开屏同样受益）。
- **复牌**：`ui_mobile_390x900` 恢复门禁矩阵（12 图全员在岗）；复牌后**连续 5 轮全绿**（含终验两轮，ui_mobile 全部 0.00%）。

### P5-B Alpine 弹层按钮 emoji 图标化 ✅
- 8 处：回收站「↩ 还原/还原选中」、语音包「⚡ 补齐克隆试听/按新台词重生成」「🔄 订阅/订阅此清单」、角色抽屉「🩺 音质体检/立即体检」、开播 cable 向导「🔄 已装好重新检测」——动态文案走 `x-html` + `icx()`（复用 hub.js 渲染器，全站同库），静态按钮直接内联 `<use>`。⏳ busy 态（状态位）与引导文案里的步骤说明（内容位）按公约保留。
- **测试**：拼装门禁通过；Playwright 实测角色抽屉两个体检按钮 SVG 就位、零 JS 错误。

### P5 最终验收（连跑两轮，全绿）
| 轮次 | run_all_tests（14 套件） | test_ui_optimization | ui_visual_regress（12 图，ui_mobile 已复牌） |
| --- | --- | --- | --- |
| 第 1 轮 | ALL GREEN · PASS=116/FAIL=0 | ALL PASSED · 462/467（5 SKIP=在线用例） | 12/12 全过（ui_mobile 0.00%） |
| 第 2 轮 | ALL GREEN · PASS=116/FAIL=0 | ALL PASSED · 462/467（5 SKIP=在线用例） | 12/12 全过（ui_mobile 0.00%） |

**P6 批次候选**（条件满足后）：
1. pro_upsell 埋点回看（07-30）；
2. 旧线名兼容位删除（部署确认后）；
3. 原生壳打包复议（客户侧出现 Edge 依赖问题时）；
4. `--virtual-time-budget` 观察期：若 /ui 系截图未来出现虚拟时钟互锁超时（同 /phone 长轮询模式），改用「CDP + networkidle」重写截图路径的备选方案已验证可行（本轮 CDP 探针 14 连开全稳定）。

---

## 13. P6 实施记录（2026-07-16 夜 · 交付面收尾三件）

> 背景：P6 表面上全是条件触发项，但按四视角重审现状，发现三件**当下就该做**的交付面工作——都不是新功能，而是"确保前几批成果能完整送达客户与数据窗口"。

### P6-A 埋点管道体检 ✅（防 07-30 数据空窗）
- 实测管道贯通：Playwright 测试期间的 PRO 徽点击已落盘 `logs/ui_events.jsonl`（`ev=pro_upsell, id=sing`），`/api/ui/events/summary` 聚合可见（by_ev.pro_upsell=1）——07-30 回看不会扑空。
- **发现并补掉一个数据质量缺口**：`goPro()` 上报缺 `sid`（会话去重）与 `page`（来源页）字段，/ops 入口热度的会话数维度会恒为 0。已对齐 home.html `track()` 同口径补齐（sessionStorage 会话 id）。
- 顺带核实：埋点端点限 4KB/条、只写不可读，无隐私外溢。

### P6-B 交付体检「1 警告」排查 → 根修 ✅
- 现象：顶栏交付徽章实测恒显「1 警告」，明细=核心服务「未就绪：广播中枢」。
- **根因**：`/api/delivery_selfcheck` 的核心服务清单是**静态四件套**（fish_tts/stt/lipsync/vcam），而本部署是真人换脸模式（`HUB_SUP_VCAM=0`，vcam/lipsync 本就不参与开播）——这正是 `_compute_broadcast` 注释里记载的"旧前端恒列核心→恒红"同款误报，当年根治了首页状态条/首启卡/自愈判定，**漏了交付体检这一处**。
- 修复：清单改为对齐 `health.broadcast.core`（模式感知单一真相），并在结论里标注当前模式；实测本机重启 Hub 后将变为「开播核心链路全部在线（真人换脸模式）：克隆音TTS/语音识别/实时换脸」6/6 全绿。交付验收从此不再被不参与的服务误报打扰。
### P6-C 发布/热更清单审计 ✅（本批新增文件的送达保障）
- 审计结论：安装器 `..\*.py` 通配 → `webview_shell.py` 自动进包 ✅；OTA app 组件（`build_packs.iter_app_files`）同样通配根 *.py ✅；`tools/_*.py` 门禁/开发工具两条管线均排除 ✅（客户机不需要）。
- **发现并修掉一个冗余**：`static\*` 递归通配会把 `static/ui_src/`（拆分源目录，~600KB 文本）打进安装包与 OTA——客户机没有 `_build_ui.py`，带源只会诱导"手改源以为会生效"的误会。两条管线同步加排除（`APP_TREE_EXCLUDES` + .iss `Excludes`，互为注释锚点），实测 app 组件清单 317 文件：webview_shell ✅ / ui.html 产物 ✅ / 图标库 ✅ / ui_src ✂ / tools/_* ✂。

### P6 最终验收（连跑两轮，全绿）
| 轮次 | run_all_tests（14 套件） | test_ui_optimization | ui_visual_regress（12 图） |
| --- | --- | --- | --- |
| 第 1 轮 | ALL GREEN | ALL PASSED | 12/12 全过 |
| 第 2 轮 | ALL GREEN | ALL PASSED | 12/12 全过 |

生效方式：交付体检修复需重启 Hub；埋点字段补齐即改即生效（hub.js）；打包排除在下次 `build_packs --build-app` / 安装包构建时生效。

**P7 批次候选**（全部条件触发，无当下可做项）：
1. pro_upsell 埋点回看（07-30，管道已验证畅通）；
2. 旧线名兼容位删除（部署确认后）；
3. 原生壳打包复议（客户侧 Edge 依赖问题出现时）；
4. 下次发版时：验证交付体检 6/6 全绿 + app 组件 317 文件清单如预期落盘。

---

## 14. P7 重启验收走查（2026-07-16 深夜 · 全栈重启 + 21 界面截图 + 四视角复盘）

### 重启与生效验证
- **重启序列**：Hub（改经 `start_avatar_hub.bat` 看护循环，掉线自动重拉）→ 同传 7900 → 换脸 8000 → 启动台（清掉历史双实例：venv 与 facefusion 两套 pythonw 各跑一份，单实例锁只对同解释器生效——已收敛为 venv 单份）。全程 Hub 2s 内健康、965 行启动日志 **0 ERROR**（32 条 WARNING 均为未启动扩展服务的预期掉线告警）。
- **全部改动实测生效**：`/favicon.ico` 三服务全 200；`/api/features` 下发三系七品线名 + `line_colors`；交付体检 **7/7 全绿**（核心服务按真人换脸模式口径："克隆音TTS/语音识别/实时换脸 全部在线"——P6-B 修复实锤）；换脸面板品牌皮肤+图标、同传页、启动台三系分组工作台/图标按钮均如设计呈现。
- **截图**：21 张全界面存 `ui_snapshots/preview_lingox/restart_*.png`（启动台桌面窗 + /ui 全 11 页签 + home/phone/dashboard/ops/delivery/verify/help/order + 换脸面板 + 同传页），Playwright 全程零 JS 错误。

### 走查中发现并当场修复/澄清的三件事
1. **深链 hashchange 不跟随（修复）**：已打开的控制台里改 URL 锚点不切页签（仅首载读一次 hash）——补 `hashchange` 监听（跳出型页签除外、与 goTab 写回无循环），Playwright 实测 #voice/#stream 切换 PASS。
2. **换脸面板缩略图 500 崩栈（修复）**：`faces/默认.jpg` 为 0 字节，`cv2.imread→None→resize` 断言崩，每次面板刷新刷一条栈——补 None 判定返回 404（前端 onerror 占位人形，体验无损）。实测修复后 404、日志安静。
3. **`engine/start` 对 faceswap 报"已迁远端"（澄清+恢复）**：`env_config.bat` 把 `SVC_FACESWAP` 默认指到 .104 生产机 → 看护脚本重启的 Hub 认为换脸在远端、拒绝本机拉起（旧 Hub 进程是无此 env 的历史孤儿）。本机开发/演示场景经 `start_faceswap_api.bat` 恢复本机副本（9s 就绪）。**部署注意**：本机要常驻换脸时应在环境里显式 `set SVC_FACESWAP=http://127.0.0.1:8000`，否则 Hub 守护不会替它自愈。
- 门禁复跑：全量套件 ALL GREEN + UI 断言全过。

### 四视角复盘（基于重启后 21 张实景截图）

**开发工程师**：重启链路顺畅——看护脚本防重复启动、supervisor 补拉核心、boots 留痕齐全；本轮走查抓出的三件事全是"只有真重启+真点击才暴露"的运行时问题，说明静态门禁（14 套件）+ 像素回归之外，**"重启走查"值得成为发版前固定动作**（候选：把 hashchange/缩略图这类走查断言沉淀进 test_ui_optimization）。遗留风险：launcher 曾双实例并存（两套解释器绕过单实例锁），建议单实例锁改按端口/全局互斥体而非 QLocalServer 键。
**市场经理**：演示动线全程品牌一致——启动台（三系工作台+图标）→ 控制台（任务流侧栏+PRO 徽位）→ 交付体检 7/7 全绿页可直接给客户截图签收；换脸面板从"外星页面"变成可入演示流的一环。dashboard 战报（1.19s 开口、74% 好评、听感中率 75%）本身就是销售素材位。
**用户**：重启后所有工作界面可达、零 JS 错误；开播页三步引导/就绪清单/建议处理的"下一步永远明确"；深链修复后"发给同事一个 #stream 链接"这类协作动线不再失灵。观察项：顶栏红色任务横幅（唱歌/MV 恢复排产提示）常驻抢注意力，其"还有 4 条"展开后信息密度高——已有演示模式收敛，日常模式可观察是否需要按优先级再降噪。
**美术设计师**：21 张截图横向看，暗色层级/无界蓝紫/线性图标已全站贯通（含换脸面板、同传页、启动台桌面端）；dashboard 的信息密度与图表配色成熟。可打磨位（非缺陷）：换脸面板"明星脸"空文件占位卡与真实头像混排时的视觉权重、ops 页三块"加载中…"占位在服务未启时的骨架化。

**结论**：重启验收通过，所有累计改动在真实运行态生效；三件走查发现均已闭环。P7 候选清单不变（全部条件触发）。

---

## 15. P7-B 窗口通道定版：原生壳转正（2026-07-16 深夜 · 用户实锤驱动）

- **用户实锤**：真实桌面上任务栏仍是 Edge 图标、窗口不最大化——上午的 Edge 参数修法失效。
- **根因（上午方案的盲区）**：Edge **已在运行**时（真实桌面常态），`msedge --app=URL --start-maximized --window-size=…` 只是把开窗请求转发给既有浏览器进程，窗口几何参数被忽略、任务栏身份仍归组 Edge。无 Edge 进程的测试环境验证通过 → 真实桌面不成立。教训已写进 `_open_app_window` 注释。
- **定版**：窗口通道优先级改为 **原生壳 WebView2 优先 → Edge --app 兜底 → 默认浏览器保底**（P3-2/P4-A 建好的通道从"灰度默认关"转正为"可用即默认"）。`AVATARHUB_APP_SHELL=edge` 可强制回旧通道；`AVATARHUB_APP_MAXIMIZED=0` 经 `--windowed` 贯通原生壳。壳补两道保险：`IsZoomed` 校验+`SW_MAXIMIZE`（个别 WebView2 版本 maximized 首帧不落地）。
- **实测举证**（截图存 preview_lingox/shell_*.png）：壳窗口 `IsZoomed=True`、rect 1936×1048（1920 屏最大化特征）；任务栏出现独立的无界 LOGO 图标（∞ 彩标），与 Edge 图标分组彻底分离；窗口内控制台/同传页正常承载。启动台已重启生效——**此后从启动台打开的所有站内窗口：任务栏=程序 LOGO、开窗即最大化，与 Edge 是否运行无关**。
- 已知边界（记录）：① 窗口内 `target=_blank`/`window.open` 链接（如控制台里的教程/自检）经系统默认浏览器打开——与旧通道行为一致；② 客户机（冻结版）走原生壳需 pywebview 进打包清单（P4-A 已备好派发通道与体积数据 +5.4MB，**建议下次发版纳入**——本机验证已证明其价值）；③ 旧的 Edge 应用窗口需手动关闭，新开窗口才走新通道。
- 门禁：全量套件 ALL GREEN + UI 断言全过。

---

## 16. P8 发版纳入：pywebview 进冻结打包（2026-07-16 深夜 · 用户拍板执行）

- **打包变更**：`build_launcher.bat` 追加 `--collect-all webview/clr_loader/pythonnet/proxy_tools/bottle` + `--hidden-import webview_shell / webview.platforms.winforms / webview.platforms.edgechromium / clr`（决策注释随行；AvatarHub.spec 由构建自动同步）。pywebview 官方自带 PyInstaller hook（`webview/__pyinstaller`）与 pythonnet 的 `hook-clr` 均被拾取，WebView2/.NET 桥 DLL 全自动收集。
- **体积实测**：AvatarHub.exe **40.9 → 45.4 MB（+4.5MB）**，优于预估 5.4MB（UPX 收益）。
- **健壮性补强（发版专项）**：① 壳子进程是 GUI 子系统（stdout/stderr=None）——`--webview-shell` 派发入口加异常落盘 `logs/webview_shell.err`，壳内根 logger 落盘 `logs/webview_shell.log`（WARNING 级），客户机壳崩不再两眼一抹黑；② `webview_shell._base_dir()` 冻结态按 exe 旁定位 assets/data（dist 直跑开发验证时回退上级项目根）。
- **冻结态全流程验收（dist\AvatarHub.exe 实测）**：`--webview-shell` 子进程 → **WINDOW_FOUND、IsZoomed=True（最大化）**、autoclose 到点干净退出、父子进程零残留、无 err 落盘；全量套件 ALL GREEN。
- **排障留档（一次误判）**：onefile exe 是"bootloader 父进程 + 运行时子进程"双进程结构——窗口/事件循环都在**子进程**，按父 pid 找窗口必空；faulthandler 抓到的 `winforms app.Run()` 驻留栈是消息循环常态而非卡死。曾据此误判"冻结态挂起"折腾三轮重建，真相是壳一直正常。教训已写进 webview_shell.py 注释（后来者按子 pid 验证）。
- **发布链后续（待运营执行）**：签名/装机器包/上传走既有 `publish_release.py` 流程（涉及签名证书与分发凭据，不自动代跑）；下次 `build_packs --build-app` 时 `webview_shell.py` 已在 app 组件清单内（P6-C 验证过 317 文件含它）。装机回归清单：新装机首启 → 启动台开任意站内窗口 → 任务栏 LOGO/最大化 → `logs/webview_shell.err` 不存在。

---

## 17. RVC-P0 实时变声「声音不变」根治批次（2026-07-17 凌晨）

> 触发：用户实测「启动变声后声音完全没变，换任何 .pth 模型都是同一个声音」。
> 排查结论（详见对话端方案）：不是模型文件问题（66 个 .pth + hubert/rmvpe 齐备、config.json 落盘成功），
> 而是链路上四个确定性工程缺陷叠加 + 输出进 CABLE 用户耳里听到的是麦克风监听原声。

### 17.1 根因清单（按危害排序）

1. **配置不热生效**：引擎 `api_240604.py` 的 `/config` 只写 `gui_config`，模型/变调实际在 `/start → start_vc()` 新建 RVC 对象时才加载。转换运行中「应用配置/换模型」听感零变化——正是用户看到的现象。
2. **「开始变声」不带滑块值**：前端 `rvcStartConversion()` 只打 `/rvc/start`，不先应用当前滑块 → 引擎用上次落盘的旧配置。
3. **角色激活推送从未生效**：`avatar_hub._push_rvc` 发 `f0up_key`（引擎字段是 `pitch`）且缺必填的 `index_path/sg_input_device/sg_output_device` → Pydantic 422，又被 `except: pass` 吞掉。
4. **故障全程静默**：`rvc_for_realtime.RVC.__init__` 裸 `except:` 吞加载异常（半残对象继续跑）；RVC 子进程跑在独立控制台且无日志文件，窗口一关证据全灭。

### 17.2 本批次修复（4 文件）

| 修复 | 位置 | 要点 |
| --- | --- | --- |
| 引擎 /config 热重启 | `api_240604.py` | 转换在跑时收到新配置 → `stop_stream()`(join 拾音线程确认旧流真关，防双流叠加“机器人音”) → `set_values` → `start_vc()`；响应带 `restarted` 字段。校验失败（设备/模型不合法）按旧配置原地复活转换，维持「config 失败不伤在跑转换」契约（热切回退链依赖）。`/config /start /stop` 全部套 `_transition_lock` 串行化。 |
| 前端开始变声先应用 | `static/hub.js` | `rvcStartConversion()` 先 `rvcApplyConfig()`（返回布尔），失败即中止并显示原因；面板没选模型（刷新后自救卡重启场景）跳过应用走引擎落盘配置。应用成功时如 `restarted=true` 显示「配置已应用，变声已重启生效」。 |
| 角色激活推送修正 | `avatar_hub.py` | `_push_rvc` 改走 hub 自身 `rvc_config()`（pth 校验/.index 匹配/热重启同一条链），字段名修正为 `pitch`，设备取引擎落盘 `configs/config.json`（无已知设备则明示跳过），成败落 `avatar_hub.log` 不再静默。 |
| 可观测性 | `api_240604.py` + `tools/rvc_for_realtime.py` | ① RVC 进程日志落盘 `logs/rvc_api.log`（RotatingFileHandler 5MB×2，根 logger 含 uvicorn）；② `RVC.__init__` 异常向上抛 → `/start` `/config` 如实 500+原因；③ 每块日志(Infer time/sola/block passed)降为 DEBUG，新增 `[vc-heartbeat]`（每 100 块≈25s 一条 INFO：blocks/avg_infer/model/pitch/out_rms）——日志有心跳=真在转换，out_rms>0=真在出声；④ 兜底拾音流也起不来时把 `flag_vc` 翻回 False，UI 不再挂假「变声中」。 |

配套调整：hub `/rvc/config` `/rvc/start` 代理超时 10s→60s（热重启含模型加载可达数秒~数十秒，旧值会假报「服务不可达」）；`_rvc_hot_switch_inner` 识别 `restarted=true` 短路成功（少一次 stop→start 的 ~2s 断声）。

### 17.3 门禁结果

- `run_all_tests.py`：14 套件 **ALL GREEN**（PASS=116 FAIL=0 SKIP=15）。
- `test_ui_optimization.py`：两轮 **486/491 全过**（5 SKIP=服务离线的 HTTP 探活）。
- `ui_visual_regress.py`：11/12 截图成功；`ui_tab_sing_1440x900` 在**截图采集阶段** Edge headless 90s 超时（§12 已知的 `--virtual-time-budget` 环境抖动，观察项），非像素 diff 失败；本批次零视觉改动（只动 JS 行为与后端），风险面为零。
- 编译检查：3 个 Python 文件 py_compile 通过；hub.js Node 语法校验通过。

### 17.4 验收口径（重启服务后现场验证）

1. RVC 6242 需**重启**才加载新代码（改了 api_240604.py）；Hub 同理。
2. 变声运行中换模型/拖变调 → 点「应用变声配置」→ 应显示「配置已应用，变声已重启生效」，且听感立变。
3. `logs/rvc_api.log` 出现 `[vc-heartbeat] ... out_rms=非零` = 转换真实在跑且在出声；用户耳听请监听 CABLE Output（或 OBS 源），麦克风硬件监听听到原声属预期。
4. 激活绑定了 rvc_model 的角色 → `avatar_hub.log` 出现 `[rvc] 角色「×」激活已推送变声配置`。

### 17.5 遗留与下一阶段（RVC-P1 体验闭环，待机主拍板）

- **假滑块**：实时链路 `protect`（咬字保护）被 Pydantic 静默丢弃、无 `.index` 时贴合度被强制 0——P1 要么真接上，要么置灰+说明；
- **本地试听/监听开关**：变声输出进 CABLE，用户无法自证效果 → 面板加监听按钮或 3 秒 AB 试听；
- **真实状态徽章**：引擎加 `/status`（模型/pitch/推理耗时/输出电平），替换「6242 活着=变声中」的猜测显示，并与 CABLE 静音告警联动；
- **埋点**：`rvc_start → 60s 内 stop/离开` 漏斗入 `ops_metrics.jsonl`，量化本次修复收益。
