# AvatarHub 安装包下载站（/order /pricing /download 已迁入 Next.js 应用本体）
location /releases/ { alias /var/www/dl/releases/; autoindex off; }
# 每日视频动态等媒体文件（发布机 scp 直传，nginx 直出，部署不影响）
location /media/ {
    alias /var/www/media/;
    expires 7d;
    add_header Cache-Control "public";
}
